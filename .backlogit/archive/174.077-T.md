---
artifact_type: task
commit: "35453406"
created_at: 2026-09-26T04:08:10.132697Z
id: 174.077-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - core-lifecycle
    - harness-ready
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: done
title: Protect blocked envelope in generic updates and write boundary
updated_at: 2026-09-26T05:33:33.6596649Z
---

Wave 20 work unit U20C1 (unit token U20C1, P-002.6 prefix ^TestU20C1_) for the 155-S final-review remediation. Finding STD-P2-01 (in scope under P-021 C1/C3): a generic custom_fields update can replace or remove the blocked envelope (reason, timestamp, member snapshot) while the shipment is blocked.

Fix A (updateArtifactUngated, immediately BEFORE mergePreserveReservedSizingKeys): when the artifact is a shipment and previousStatus (loaded under the locks) is blocked, shallow-clone updates and its custom_fields map (the caller's map is never mutated), delete every envelope key from the clone and copy each prior value back if it existed, then pass the cleaned clone to mergePreserveReservedSizingKeys so the persisted fields, audited changes, and post hooks share one cleaned map. Envelope keys: blocked_reason, blocked_at, member_status_snapshot, branch, blocked_by, resume_checkpoint_ref, defined once in a single key-list helper (doc comment points to stash 8AF55264). No new preservation for items.

Fix B (writeArtifactFileGoverned, immediately AFTER guardBlockedShipmentMembershipMutation): refuse with ErrShipmentBlockedRequiresEnvelope when the previous record (parsed from Markdown) is a blocked shipment, the next is still blocked, !envelope.allowGovernedShipmentMutation, and any envelope key differs by presence or canonical value (blocked_at via canonicalBlockedTimestamp, falling back to raw canonical JSON when either side is unparsable; other keys by canonical JSON).

Domain: core (Go). Class: NORMAL harness-required (P-002.4 exempt classes do not apply). Files: implementation 1 (internal/core/artifacts.go: updateArtifactUngated, writeArtifactFileGoverned, 1 envelope-key-list helper, at most 1 comparison helper); harness 1 (internal/core/shipment_blocked_envelope_u20c1_harness_test.go, package core, no t.Parallel). Wave W1; no dependencies. Seeded by the W0 draft fold (C1 portion).

Authoritative contract: plan Wave 20 sections 20.3.1, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=15m -v -run '^(TestUpdateArtifact_|TestUR1B_|TestUR2_|TestUR3_|TestUR10_|TestP1C4_|TestP1C7_|TestNormalizeBlockedShipment)' ./internal/core"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly two top-level functions in internal/core/shipment_blocked_envelope_u20c1_harness_test.go, and no other function in the repository matches ^TestU20C1_.
  - TestU20C1_GenericUpdatePreservesBlockedEnvelope: canonical block (>=1 member, BlockedBy and ResumeCheckpointRef set); two-value lookups prove blocked_reason, blocked_at, member_status_snapshot, blocked_by, resume_checkpoint_ref present and non-empty and branch absent; snapshot the envelope. Subtest forged (seeded from the W0-folded draft TestBlockedShipmentGenericCustomFieldsPreserveEnvelope): round-trip update with unchanged items, custom_note, forged blocked_reason/blocked_at/blocked_by, empty member_status_snapshot, new branch -> NoError, envelope deep-equal to snapshot, branch absent, custom_note applied, correlated artifact_mutation audit event without forged values, submitted map still holds forged values (clone proof). Inline control while still blocked: dropping items is refused with ErrShipmentConflict and file bytes unchanged. Subtest omission: items + custom_note_2 only -> envelope deep-equal to snapshot; validatePersistedBlockedShipmentEnvelope NoError; Doctor reports no malformed-blocked finding; UnblockShipment(queued, Confirm: true) succeeds (last step). Inline control: on a separate queued shipment a round-trip branch update is applied.
  - TestU20C1_UngovernedWriteCannotAlterBlockedEnvelope: direct ungoverned persistArtifact changing blocked_reason and removing member_status_snapshot (items unchanged) is refused with ErrShipmentBlockedRequiresEnvelope and bytes unchanged. Inline control: identical envelope + changed title succeeds (re-serialized blocked_at exercises canonical comparison). Inline error-shape row: changing only blocked_at to an unparsable string is refused with ErrShipmentBlockedRequiresEnvelope and bytes unchanged.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C1_' ./internal/core -> exactly 2 top-level PASS when GREEN.
  - RED at 3a240fe091422e96d22b75b9af0854f66b60340f (deliverable absent): function 1 fails on envelope equality; function 2 fails because the write succeeds (including the unparsable blocked_at row). Compile check go test -run '^$' -count=1 ./... passes. '[no tests to run]' or 'no test files' at any exit code is a halt, not RED. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready.
AC2 (Fix A): the caller's custom_fields map is not mutated; the persisted artifact, audited changes and post hooks carry the prior envelope values; envelope keys are defined once in one helper shared with Fix B.
AC3 (Fix B): the refusal fires only for blocked->blocked ungoverned writes that change an envelope key; governed block/unblock/normalize writes are unaffected.
AC4 (Regression, exact counts, fail closed): the green-regression-contract command reports exactly 34 top-level PASS (TestUpdateArtifact_ 13 + TestUR1B_ 3 + TestUR2_ 3 + TestUR3_ 5 + TestUR10_ 1 + TestP1C4_ 1 + TestP1C7_ 2 + TestNormalizeBlockedShipment 6), with zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC5 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/core/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check HARNESS_BASE_SHA HEAD -- <task files> clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/core/artifacts.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope, and CAREFUL (W0 relocation of the uncommitted draft; writeArtifactFileGoverned is the write boundary every rewrite passes through).
W0 precondition (plan 20.5, Ship via the harness-architect skill, before any Wave 20 harness): write the provenance patch with git diff --output=logs/diagnostics/wave20-draft-fold.patch -- internal/core/shipment_blocked_recovery_harness_test.go, record SHA-256, check-ignore it, and verify it is non-empty and contains both draft function names; relocate TestBlockedShipmentGenericCustomFieldsPreserveEnvelope into this task's harness file and refactor it there in the same pass (drop the unused root at :1498, add the unit prefix, extend to the full forged payload); park the C2 portion in logs/diagnostics/wave20-draft-u20c2.go.txt (check-ignored, hashed); verify git diff --quiet on the tracked file, go test -run '^$' -count=1 ./internal/core passes, and no file W0 writes starts with EF BB BF. Write files with the editor tool or UTF-8 without BOM, keeping line endings. No git restore/checkout/stash/discard. Halt to the operator if the tracked file cannot return to HEAD by relocation.
The 174.077-T scaffold adopts the W0-seeded file in place; its RED capture and HARNESS_COMMIT cover the completed file. Stage harness commits by exact path only; the C1 portion lands only in this task's HARNESS_COMMIT and neither harness commit lists shipment_blocked_recovery_harness_test.go.
SHAs: one harness commit per task (HARNESS_COMMIT); HARNESS_BASE_SHA = its parent (RED metadata); START_SHA = git rev-parse HEAD at the task's claim before its first edit. Captures: logs/diagnostics/174077-red-harness.txt + .metadata.json and GREEN equivalents, never staged. Before scaffolding record git grep -n 't.Parallel' -- internal/core/*_test.go internal/mcp/*_test.go (H10).
Wave W1 = {174.077-T, 174.079-T, 174.082-T}; production files disjoint. Wave convergence per plan 20.4 records FULL_SUITE_OPERATOR_DEFERRED: wave 1. No go test ./... with tests selected or unselected package runs. No push until W3 convergence and the W3 pre-push gate are GREEN.
Size: 2 files, at most 4 declarations. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage; never widen scope in place.
Documented deviations: EW-1, QG-1, QG-2, TG-1, TG-2 (plan 20.2/20.3). Related deferred captures (do not implement): 8AF55264 (envelope key consolidation and path-choice bypass).

Harness record (174.077-T):
* Command: go test -count=1 -timeout=10m -v -run '^TestU20C1_' ./internal/core
* Compilation: PASS; go test -run '^$' -count=1 ./internal/core exited 0
* Red Phase: CONFIRMED (scoped); both top-level tests produced assertion RED; see logs/diagnostics/174077-red-harness.txt and .metadata.json
* HARNESS_COMMIT: f615dcfe7c6916605a762e80b6bd473c16f766b6
* HARNESS_BASE_SHA: 6c4fe120714b39cd1a467eecba5a70ef4f53a245
<!-- END:implementation-notes -->