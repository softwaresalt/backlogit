---
artifact_type: task
commit: fad706aa882e2596563e53db0adde0f1db816a5d
created_at: 2026-06-30T22:50:59.019833-07:00
dependencies:
    - 071.002-T
    - 071.003-T
id: 071.007-T
labels:
    - size:M
    - phase-2
    - unit:U7
    - test-first
    - work-item:4-mutation-cli
parent_id: 071-F
priority: medium
references:
    - docs/exec-plans/2026-06-30-backlogit-deterministic-gates-slice-plan.md
status: done
title: Add core.SetArtifactSize body-preserving size mutation seam
updated_at: 2026-06-30T23:51:18.7378776-07:00
---

Execution posture: TEST-FIRST. Depends on U4 (071.002-T) and U6 (071.003-T). Scope: add core.SetArtifactSize(ctx,ws,id,size) in new internal/core/artifact_size.go as the SINGLE body-preserving size mutation seam. Steps: (a) enum-validate size in FieldDef.Values for the task schema (TARGETED — do NOT retrofit global enum enforcement into ValidateArtifactFields, which would regress legacy artifacts); (b) acquire per-task lock (U4), defer release; (c) read file + mdfront.Decode; (d) set custom_fields.size in the decoded frontmatter map, leaving all other frontmatter and the ENTIRE body bytes untouched; (e) mdfront.Encode -> atomicfile.WriteFileAtomic (already short-write-guarded + Windows-rename-safe; do NOT reimplement); (f) db.UpsertItem to keep the SQLite index in sync. CRITICAL: UpsertItem is INSERT OR REPLACE on the FULL row — reconstruct a FULLY-POPULATED *models.Artifact from the SAME decoded frontmatter+body (ArtifactFromFrontmatter/findArtifact) with custom_fields.size set, NOT a partial {ID,CustomFields} stub (which would null title/status/priority in the index and re-open markdown<->DB drift). Generic UpdateArtifact/WriteArtifactFile path left UNCHANGED (bounded blast radius). Size-only mutations intentionally emit no hook event. ACCEPTANCE CRITERIA: (1) valid size persists to custom_fields.size AND the DB row reflects it while title/status/priority index columns are UNCHANGED (partial upsert MUST fail this); (2) invalid value (XXL) rejected before any write; (3) GOLDEN: body bytes byte-identical + frontmatter semantically equal to original plus size (parse+compare, not raw line diff); (4) idempotency: re-applying same size yields no body change; lock acquired+released; go test ./internal/core/ -run ArtifactSize green.