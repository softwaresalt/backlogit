---
archived_from: .backlogit/queue/106.001-T.md
archived_status: done
artifact_type: task
commit: 1f046895e8058e97c5eec1fe5784fba758df7ce2
created_at: 2026-07-31T00:59:11.7762282Z
id: 106.001-T
labels:
    - governance
    - formal-gate
parent_id: 106-F
priority: high
references:
    - docs/decisions/2026-07-17-formal-gate-architecture-spike-findings.md
    - docs/exec-plans/2026-07-30-formal-gate-f-series-restage-plan.md
status: archived
title: 'F2: canonical serialization + hash primitive for gate evidence/manifest'
updated_at: 2026-08-10T07:21:07.2550489Z
---

F2 bounded unit from formal-gate spike findings: define one canonical serialization and SHA-256 hash helper and route governed evidence/report/manifest hash seams through it. Scope is <=2h, test-first, and must not implement F1 authenticity or F5 journaling.

<!-- BEGIN:acceptance_criteria -->
Test-first, table-driven t.Run subtests with a t.Helper golden-vector helper. Home the primitive in a STDLIB-ONLY leaf internal/canonical (imports zero internal packages; a package-import assertion test keeps it import-free), NOT internal/core/gate, so future db-side consumers avoid a wrong-direction db->core dependency; prove behavior with differential golden byte-equality. Go surface: Canonicalize(v any) ([]byte,error) plus a hash helper (string,error), accepting JSON-like any (documented json.Number handling), no context.Context. Canonical byte contract: UTF-8 no BOM; LF-only reached by CRLF->LF and lone-CR->LF conversion, NEVER CR deletion (deleting CR would collapse "a\rb" into "ab"; converting a lone CR to LF keeps a distinct break byte so it never vanishes into adjacent text); object keys sorted lexicographically by UTF-8 code-unit order recursively; array order preserved; minimal JSON string escaping; integers only in v1; canonicalizer FAIL-CLOSES via exported sentinel/typed errors (canonical.ErrNonIntegerNumber, canonical.ErrUnsupportedType) wrapped with %w so tests assert errors.Is/As; NO omitempty on hashed collection/optional fields (present-empty vs absent are distinct; golden vectors pin empty/null cases); timestamps canonical UTC Z via models.NowUTC() (golden asserts trailing Z and no offset); compact whitespace; exactly one trailing LF before SHA-256 lowercase hex. Record deliberate RFC 8785 (JCS) divergences (trailing LF, integer-only numbers, UTF-8 byte-order key sort). Tests: LF-vs-CRLF and unsorted-vs-sorted-key hash identically; a lone CR converts to LF (not deleted) so "a\rb" hashes DISTINCTLY from "ab" (CR cannot disappear into adjacent text); array reorder changes hash; non-integer input errors; trailing-newline enforced; an evidence event, a formal report, and a SECOND structurally-distinct generic nested payload (NOT labeled F1's manifest-binding payload) all hash through one helper. Do NOT couple to internal/db/manifest.go (merge-sync filesystem snapshot, no gate hash seam). Re-routing gateReportHash is DEFERRED to F1 (owns the report payload contract, adds a hash-scheme version field to the append-only evidence log); F2 only adds a characterization test documenting the current raw-broker-bytes hash without changing any recorded value; the grep/lint guard is a BASELINE/ALLOWLIST check that rejects NEW ad hoc crypto/sha256 (or equivalent hash) sites outside internal/canonical on governed payload paths while explicitly allowlisting the one pre-existing seam (internal/core/gate_evidence.go gateReportHash) until F1 re-routes it, so F2 passes its own acceptance without deleting the deferred legacy hash (F1 removes the allowlist entry when it re-routes gateReportHash).
<!-- END:acceptance_criteria -->