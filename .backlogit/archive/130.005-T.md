---
archived_from: .backlogit/queue/130.005-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-28T22:42:30.9225057Z
id: 130.005-T
labels:
    - durable-writes
    - mcp
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: archived
title: U5 - map durability classes to explicit MCP append_comment outcomes
updated_at: 2026-07-29T01:23:15.016221Z
---

internal/mcp/tools.go handleAppendComment plus a shared durabilityOutcomeResult helper (colocated with domainError/gateErrorResult, no new file): replace the blanket InternalError mapping. Classify core.AppendComment errors via blerrors.IsWriteNotApplied and blerrors.IsWriteIndeterminate and return via the gate_errors.go envelope as an MCP error result built with marshalGateError/NewToolResultError: not-applied -> error=write_not_applied, retryable=true; indeterminate -> error=write_indeterminate, retryable=false. Indeterminate MUST be machine-distinguishable from the generic internal error (which agents auto-retry) and MUST NOT collapse into it. Evaluate not-applied before indeterminate. Non-durability errors keep the internal mapping; success ok=true response unchanged. CLI parity: MCP-only is an intentional, accepted asymmetry. Injectability seam (Copilot review): EventWriter.fsyncFileImpl/fsyncDirImpl are unexported in package events and core.AppendComment is called directly, so neither is injectable from package mcp. Ship MUST ADD a package-mcp seam - a package-global appendCommentFn = core.AppendComment that the handler calls and tests override to return wrapped blerrors.ErrWriteNotApplied / ErrWriteIndeterminate - so the outcome-mapping and exactly-once retry tests are implementable in package mcp (no t.Parallel). Add a cross-package propagation assertion that IsWriteIndeterminate is true on the error the handler receives. Test-first (append_comment_test.go). Acceptance criteria (<=3 scenarios): (a) indeterminate -> write_indeterminate/retryable=false, distinct from the generic internal error (non-durability errors stay on internal, unchanged); (b) not-applied -> write_not_applied/retryable=true (success ok=true response unchanged); (c) retry-idempotency: exactly one comment after a not-applied retry, and the cross-package IsWriteIndeterminate propagation holds. Plan unit U5.