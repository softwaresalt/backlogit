---
artifact_type: task
commit: d1a13df12f2436ffb2b9a398ca356534a7d610f1
created_at: 2026-08-22T01:05:11.3698937Z
dependencies:
    - 146.012-T
    - 146.006-T
    - 146.013-T
    - 146.014-T
id: 146.015-T
labels:
    - stage-harvested
    - unit-U6
    - domain-code
parent_id: 146-F
priority: high
references:
    - docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md
    - docs/decisions/2026-08-21-success-shaped-evidence-loss-deliberation.md
status: done
title: Return and surface the persisted checkpoint context keys
updated_at: 2026-08-22T08:08:55.2597381Z
---

Plan unit U6. Populate the CreateCheckpointResult U0a declared and adapt both surfaces. Both marshal this EXACT value, mirroring the docline.LintReport pattern, replacing the map[string]string literals which cannot carry a string slice. On the V1 path ContextKeys comes from Keys() whose error is PROPAGATED, never discarded. On the legacy verbatim path it comes from scanning the top-level context object of the WRITTEN bytes, sorted with sort.Strings; the scan is strictly best-effort and MUST NOT fail the create - any decode failure or non-object context yields an empty non-null array and the create still succeeds. Also update the Cobra Short, Long, Example, and GoDoc so U10a regenerates against fresh text.

<!-- BEGIN:Notes -->
Gate run 9 remediation (Go P3 / Parity P3): (a) **Call ordering is pinned** — `CheckpointContext.Keys()` MUST be invoked BEFORE `syncWriteFileAtomic` on the V1 path, so a serialization failure deterministically prevents the write instead of leaving a file on disk whose keys cannot be reported. Wiring `Keys()` after the write is forbidden: it recreates the applied-write-with-unreportable-result inversion this shipment exists to remove. The second `emit()` call is intentional; capturing keys out of the marshal step is acceptable only if done in the same pre-write step. (b) Acceptance now also requires U5a scenario 4 to pass — the constructed `checkpoint create` Cobra `Short`/`Long`/`Example` strings must carry the open-`context`, closed-namespace, and `context_keys` sentences, so this unit's contract-text edit has an upstream red harness (R11a).
<!-- END:Notes -->