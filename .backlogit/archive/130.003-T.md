---
archived_from: .backlogit/queue/130.003-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-28T22:41:09.4342148Z
id: 130.003-T
labels:
    - durable-writes
    - events
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: archived
title: U3 - re-attempt parent flush on durable append retry
updated_at: 2026-07-29T01:22:30.4136881Z
---

internal/events/stream.go EventWriter.mkdirAllDurable and appendDurable: the durable existing-logsDir path must UNCONDITIONALLY re-fsync the parent (idempotent, ErrWriteNotApplied-safe pre-append step) instead of the os.Stat early-return, so a retry after a failed parent flush re-attempts it and never double-appends the event. Seam: fsyncDirImpl fires for BOTH the mkdir parent flush and the per-append logsDir flush, so the test MUST fail the seam SELECTIVELY BY PATH to isolate the parent-flush retry. Test-first (stream_durable_test.go). Acceptance criteria: (a) first durable append parent fsync fails -> error; retry re-attempts the parent fsync and succeeds, event line count exactly one (no duplicate); (b) durable-off byte-for-byte unchanged. Plan unit U3.