---
artifact_type: task
commit: 67f15de4
created_at: 2026-08-22T01:05:42.2396259Z
dependencies:
    - 146.002-T
id: 146.020-T
labels:
    - stage-harvested
    - unit-U9a
    - domain-tests
parent_id: 146-F
priority: high
references:
    - docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md
    - docs/decisions/2026-08-21-success-shaped-evidence-loss-deliberation.md
status: done
title: Lock the degraded-corpus lint shape and exit code on both surfaces
updated_at: 2026-08-22T06:05:43.704222Z
---

Plan unit U9a, narrowed to BEHAVIOR ONLY during the PR #372 remediation cycle. Every assertion is about what the two surfaces DO with a malformed corpus; the contract-text assertions moved to U9c (146.024-T), which is U8b's red harness. Scenario 1: backlogit docs lint still exits non-zero and prints a report containing the decode_error finding. Scenario 2: the marshalled JSON is inspected directly for a present and non-null findings array using a .([]any) type assertion, which fails for both an absent key and a JSON null. Scenario 3: MCP returns a successful tool result carrying the same findings rather than InternalError, and the CLI and MCP finding payloads for the same corpus are byte-identical. TestDocsTools_CLIParity is not sufficient on its own: both sides marshal the same struct, so a shape defect drifts identically and compares equal - scenario 2 must read the marshalled JSON, not the producing struct. Test-first and RED BEFORE U8: all three scenarios fail against pre-U8 LintTree and handleDocsLint, every identifier they touch exists from U0b (146.002-T), and U8 (146.018-T) makes them green. The retired 146.019-T dependency scheduled this red harness after the two units implementing the behavior it harnesses. Red-result contingency: if a scenario is still red after U8, this unit becomes the red harness for U9x (internal/docline/report.go, guarantee a non-nil Findings slice at the producing boundary) which must land before U10a; it may NOT be marked green by relaxing the .([]any) assertion.

<!-- BEGIN:Notes -->
Gate run 10 remediation: U9a gains **two green-throughout containment guards** in `internal/mcp/docs_tools_test.go` / `internal/cli/docs_test.go`. Scenario 4: a `--path` that escapes the workspace makes the CLI exit non-zero with NO report rendered and NO `decode_error` finding. Scenario 5: the same input through `backlogit_docs_lint` returns a structured `validation_failed` result — never `InternalError` and never a successful result carrying a finding — pinning that `handleDocsLint` keeps routing `docline.ErrPathEscapesWorkspace` through `errors.Is` to the validation outcome. These live here rather than in U8 (146.018-T) because U8's files are all in `package docline` and cannot execute an assertion against `handleDocsLint`, which would have left that acceptance criterion with no home file. U9a is now five scenarios: 1-3 red before U8, 4-5 green at every commit boundary. Recorded as Task Granularity deviation (9).
<!-- END:Notes -->