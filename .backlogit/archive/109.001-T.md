---
archived_from: .backlogit/queue/109.001-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-15T18:45:06.3118027Z
id: 109.001-T
labels:
    - spike
    - investigate-first
    - size-estimation
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: archived
title: Research base-contract vs backlogit extension ownership and typed size surface inventory (2h max)
updated_at: 2026-07-18T03:53:30.8785512Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. First task in the size-extension contract spike; no dependencies. Inventory the typed size surface and the base-contract/extension ownership boundary: task-only size enum stored under custom_fields.size (.backlogit/header-def.yaml); no feature/shipment size field; no size_source/size_ruleset_version/estimate-history anywhere in internal/, schemas/, or header-def; the single mutation seam core.SetArtifactSize(ctx, ws, id, size) is (id, size)-only; CLI update --size and MCP update_item size route only size through it. Record the authoritative ownership boundary: docline owns only the base Markdown/frontmatter ingestion contract, and size, size_source, size_ruleset_version are optional backlogit-owned extension properties that remain TOP-LEVEL in the derived backlogit contract and that docline never calculates, defaults, aggregates, validates, or synthesizes (docline re-emits/serializes caller-provided extension properties at the top level when present but attaches no semantics). CURRENT-VS-TARGET GAP (mandatory inventory): preserve the operator's authoritative design that backlogit INHERITS the docline base contract and OWNS the derived size contract, but state current reality honestly - current .backlogit artifacts are parsed and written by backlogit's own models/core codec (models.ArtifactFromFrontmatter, core.WriteArtifactFile), NOT automatically by internal/docline.Scope or internal/docline.Normalize. The inheritance model is the TARGET contract for interoperable ingestion (graphtor/engram), not proof that current backlog writers already use the docline codec. Inventory this gap explicitly and hand it to 109.004-T, which must decide/record the concrete bridge (which base fields/schema/codec invariants backlog artifacts must implement or reuse) WITHOUT assuming direct reuse of docline.Scope. Deliverable: written ownership boundary, the current-vs-target inheritance gap inventory, plus typed surface inventory feeding 109.002-T, 109.003-T, and 109.004-T. No code, schema, or CLI changes.


<!-- BEGIN:reconciliation-model-a -->
RECONCILIATION (Model A, 2026-07-16; AUTHORITATIVE - overrides the top-level-extension framing above). The base-contract vs backlogit extension-ownership question is ANSWERED: Model A (deliberation 052-DL, feature 110-F; operator 2026-07-16). docline owns the closed base contract (top level additionalProperties:false); backlogit owns an owner-scoped namespace NESTED under the docline map as docline.backlogit.* (per-owner schema_version, additive-not-enveloping), validated by a layered ext-schema (allOf on base v1) governing only the docline.backlogit subtree. This task CONSUMES that decision - inventory the typed size surface against docline.backlogit.* (e.g. docline.backlogit.size) - and MUST NOT re-derive a TOP-LEVEL model. CAVEAT (durability scope): docline.backlogit is round-trip durable for docline DOCUMENTS via docline own codec, but NOT automatically on .backlogit ARTIFACT frontmatter, where the generic codec carries only custom_fields; persisting docline.backlogit on artifacts needs an artifact-codec bridge whose selection stays OPEN in the 109.x spike.
<!-- END:reconciliation-model-a -->