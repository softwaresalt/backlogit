---
artifact_type: task
created_at: 2026-07-28T04:47:41.2132969Z
dependencies:
    - 123.002-T
id: 123.004-T
labels:
    - durability
    - filesystem
parent_id: 123-F
priority: medium
references:
    - docs/exec-plans/2026-07-27-durable-writes-fsync-protocol-plan.md
status: done
title: U4 — Windows canonical atomic replace (unconditional safety fix)
updated_at: 2026-07-28T07:51:08.8002562Z
---

Replace Windows remove-before-rename with golang.org/x/sys/windows.MoveFileEx, same-volume, no COPY_ALLOWED; never remove dest first; failure=>ErrWriteNotApplied. Split concerns: MOVEFILE_REPLACE_EXISTING (atomic-replace safety) is UNCONDITIONAL since the pre-remove is a real data-loss defect; MOVEFILE_WRITE_THROUGH (synchronous durability flush) is GATED on durable_writes so off-mode adds no write-through latency on Windows. The seam takes the durable bool from WriteFileAtomicWithOptions; flags = REPLACE_EXISTING | (durable ? WRITE_THROUGH : 0). Reject ReplaceFileW. Build tags atomicfile_windows.go + atomicfile_other.go; promote x/sys to direct dep. Test-first: existing/absent/locked dest never-missing + fail-closed; flag-gating assertion (durable off => REPLACE_EXISTING only, on => both); POSIX parity. Deps: U2(123.002-T). Plan unit U4.