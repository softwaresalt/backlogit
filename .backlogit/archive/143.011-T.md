---
artifact_type: task
commit: 817d46794342f0747f381e4d42e899d75d01c3cf
created_at: 2026-08-18T00:58:42.8505436Z
dependencies:
    - 143.004-T
    - 143.007-T
    - 143.012-T
    - 143.008-T
    - 143.009-T
    - 143.010-T
id: 143.011-T
labels:
    - docs
    - policy
    - harness
    - coherence
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: done
title: Align contract doc, policy, reconcile skill, and Ship agent
updated_at: 2026-08-19T03:21:50.8993932Z
---

# Align contract doc, policy, reconcile skill, and Ship agent

## Description

<!-- BEGIN:description -->
Unit 11. Documentation and policy only; no code change. 143.004-T deliberately breaks the P-007 ship-always-archives postcondition, so four surfaces that encode it must gain a third branch or they will instruct the next agent to `git restore` over the intended residue. (1) `docs/design-docs/governed-mutation-recovery-contract.md`: add the shipment ship path as a governed `MutationPartialError` producer; add `CheckShippedEventCompleteness` and both finding names to the recovery-discovery enumeration; document `FailedStep` via the shared constant, `CompensationState` values not-compensated and partially-compensated, and the producer-scoped recovery semantics. AMEND the two sections that would otherwise mis-describe the newly listed producer: the classification-precedence section, which states that an untagged plain error compensates, and the failure-branches not-applied section, which describes the same default; both need an explicit producer-scoped carve-out stating that an unproven append outcome is indeterminate, never compensates, and halts rather than continuing. Widen that document's closed `CompensationState` enumeration to include partially-compensated, mirroring the 143.002-T code-comment change. (2) `.github/policies/workflow-policies.md` P-007: when `backlogit_ship_shipment` returned `mutation_partial` with `classification: indeterminate` and `failed_step: shipped-event-append`, do NOT run `git restore .backlogit/archive/`; run the doctor audit and reconcile per the named-limitation procedure. (3) `.github/skills/shipment-reconcile/SKILL.md` post-mode: the same third branch so the gate does not emit HALT-restore-archives for an intended outcome, plus the three dependent surfaces in the same file that would otherwise contradict it, namely the recommendation enumeration, the two post-mode steps that flag missing archive files (which fire on every halted-archival result before the gate decision is reached), and the gate decision itself; also correct that file's two dangling `.github/agents/ship.agent.md` and `stage.agent.md` references, which are dot-prefixed in this repository. (4) `.github/agents/.ship.agent.md` Step 6 sections 1.b (`:521`), 1.c (`:533`), and 1.d (`:537`): the same third branch; this is the surface that actually executes the sequence the policy and skill describe. (5) `.autoharness/drift-ignore`: record `workflow-policies.md` and `.ship.agent.md` as intentional local customizations and amend that file's header NOTE, which currently enumerates both as deliberately NOT ignored. Execution posture: documentation-first.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- The governed-mutation-recovery contract doc lists the ship path as a producer, enumerates both new doctor findings, and carries a producer-scoped carve-out in its classification-precedence and failure-branch sections stating that an unproven append outcome is indeterminate, never compensated, and halts rather than continuing; its closed `CompensationState` enumeration also admits `partially-compensated`.
- P-007, the shipment-reconcile skill post-mode, and Ship agent Step 6 all carry the same third branch and none of them prescribes `git restore` for the indeterminate case.
- The reconcile skill's recommendation enumeration, its two post-mode steps that flag missing archive files, and its gate decision are all consistent with the third branch, and its two dangling `.github/agents/ship.agent.md` and `stage.agent.md` references are corrected to the dot-prefixed paths.
- `.autoharness/drift-ignore` records both files and its header NOTE no longer contradicts the body.
- `backlogit docs lint` and markdownlint are clean; no source, test, or build-config code changed.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `docs/design-docs/governed-mutation-recovery-contract.md`, `.github/policies/workflow-policies.md`, `.github/skills/shipment-reconcile/SKILL.md`, `.github/agents/.ship.agent.md`, `.autoharness/drift-ignore`. Drift-ignore suppresses drift reporting only; add re-apply-the-third-branch-after-any-autoharness-template-adoption to the closure follow-ups.
<!-- END:implementation-notes -->
