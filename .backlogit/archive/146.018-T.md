---
artifact_type: task
commit: 927de4e15829256e8cbed4100f2612d2f8c9dc3b
created_at: 2026-08-22T01:05:29.0406558Z
dependencies:
    - 146.016-T
    - 146.017-T
    - 146.021-T
    - 146.020-T
id: 146.018-T
labels:
    - stage-harvested
    - unit-U8
    - domain-code
parent_id: 146-F
priority: high
references:
    - docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md
    - docs/decisions/2026-08-21-success-shaped-evidence-loss-deliberation.md
status: done
title: Accumulate decode_error findings and continue the docs lint scan
updated_at: 2026-08-22T07:54:14.951785Z
---

Plan unit U8. Replace the blanket return-nil-err on decodeDoc failure with an explicit THREE-WAY split discriminated by sentinels, never by error text: ErrPathEscapesWorkspace (raised INSIDE decodeDoc, not only in collectInScopeDocs) still returns; ErrFrontmatterDecode becomes a per-file finding and continues; anything else including os.ReadFile still returns. Wrap ErrFrontmatterDecode at the Decode branch so the discriminator exists (two-%w form needs Go >= 1.20; go.mod declares go 1.24.0). GATE RUN 9 REMEDIATION - the split lives in TWO unexported helpers in internal/docline/service.go: (1) classifyDecodeFailure(err) (kind decodeFailureKind, cause error) is POLICY-NEUTRAL, returning decodeFailureContainment / decodeFailureRead / decodeFailureFrontmatter plus the unchanged cause, carrying no Rule and no Finding, which is what makes it reusable by PlanMigration under stash 1787FD85; a nil input returns (decodeFailureRead, synthesized error) so the fail-closed default is propagate. (2) applyDecodeFailure(err, rel) ([]Finding, error) holds the LINT POLICY and is the ONLY branch LintTree may use: for a frontmatter decode it returns a one-element slice and nil, otherwise (nil, cause) unchanged. LintTree's per-file body is exactly that one call. Files: service.go, report.go (doc comment only), decode_failure_test.go (new). The table test covers BOTH helpers, and injects ErrPathEscapesWorkspace and a wrapped *fs.PathError into applyDecodeFailure - closing the fail-open gap where a U8 that turned every decodeDoc error into a finding (leaking the *fs.PathError absolute host path into Fix) would still pass every U7b scenario. Acceptance also requires handleDocsLint to map docline.ErrPathEscapesWorkspace to validation_failed, never InternalError. U9a (146.020-T) is an upstream dependency, so the behavioral red harness is observed failing before this unit lands.

<!-- BEGIN:Notes -->
Gate run 10 remediation: (a) `LintTree`'s per-file body is exactly, inside the existing `if err != nil` branch, `fs, fatal := applyDecodeFailure(err, rel); if fatal != nil { return nil, fatal }; findings = append(findings, fs...); continue`. The trailing `continue` is LOAD-BEARING: without it execution falls through to `FromMap(frontmatterOrEmpty(md))` with a nil `md`, `frontmatterOrEmpty(nil)` returns an empty map, and `ValidateFields` would append spurious missing-title/source/doc_type findings on top of the `decode_error` finding, breaking U7a scenario 1's exactly-one-finding contract. (b) The `handleDocsLint` → `validation_failed` assertion is NOT owned by this unit: U8's files are all in `package docline` and cannot execute it. It lives in U9a (146.020-T) scenarios 4 and 5, which already extend `internal/mcp/docs_tools_test.go`; U8's acceptance only requires that guard to remain green. (c) The two-helper split is an acknowledged in-scope structural cost for an out-of-scope consumer: R7 alone would be met by `applyDecodeFailure`, and `classifyDecodeFailure`'s nil-input branch is unreachable from `LintTree`'s own call path. It is accepted because shipping one helper and splitting later would turn stash 1787FD85 into a refactor of shipped gate code rather than an adoption. See `docs/compound/2026-07-29-fsutil-neutral-leaf-caller-classifies.md`.
<!-- END:Notes -->