---
artifact_type: task
commit: d4ffa74e
created_at: 2026-09-26T04:11:29.45624Z
custom_fields:
    harness_status: scaffolded
id: 174.082-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - core-lifecycle
    - harness-ready
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: done
title: Guard normalize against pending aggregate journals
updated_at: 2026-09-26T06:14:42.8392683Z
---

Wave 20 work unit U20C6 (unit token U20C6, P-002.6 prefix ^TestU20C6_) for the 155-S final-review remediation. Finding ADV-B guard hardening (adversarial, confirmed in scope under P-021 C1/C3): once MCP normalize always routes through NormalizeBlockedShipmentForRecovery (174.080-T), the target-shipment guard must refuse pending journals that reference the target aggregate (target or member IDs), not only same-shipment lifecycle intents.

Fix (internal/core/shipment_recovery.go): refusePendingShipmentLifecycleIntent and its call site stay unchanged. Add one aggregate-guard helper (for example refusePendingShipmentAggregateJournal(ws, shipmentID, memberIDs)) called in normalizeBlockedShipment right after the locked reload and membership-equality check and before readShipmentBlockedSnapshot, with exactly the locked memberIDs; both normalizers reach this site. It refuses with ErrShipmentConflict when (a) a valid lifecycle journal in phase intent references the aggregate via ShipmentID, Preimage.Shipment or Preimage.Members IDs (related-artifact IDs are NOT matched; stash 09D06A75), or a return-blocked journal in ANY phase or schema version (including legacy and committed-but-unremoved) references it via journal.Shipment.ID or journal.Item.ID; or (b) a return-blocked validation error is attributed to the aggregate by file name through validateShipmentOperationJournalName on filepath.Base(path). Match precedence: member IDs first, then the target ID; the first match is reported. The refusal message names the journal base name and the exact token kind=<lifecycle|return-blocked> matched=<id> (fixed display labels, never the raw kind). The helper scans via inspectShipmentOperationJournalsReadOnly (never writes or moves a journal); a non-missing ops-root scan error refuses (defensive, unreachable because the earlier same-shipment check fails first). Unrelated journals and unrelated malformed files (for example unrelated-poison.json) are ignored. Update only the NormalizeBlockedShipmentForRecovery doc comment.

Domain: core (Go). Class: NORMAL harness-required. Files: implementation 1 (internal/core/shipment_recovery.go: normalizeBlockedShipment call site, 1 new aggregate-guard helper, at most 1 name-attribution helper, the NormalizeBlockedShipmentForRecovery doc comment); harness 1 (internal/core/shipment_normalize_guard_u20c6_harness_test.go, package core, no t.Parallel). Wave W1; no dependencies. 174.080-T depends on this task.

Authoritative contract: plan Wave 20 sections 20.3.6, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=15m -v -run '^(TestNormalizeBlockedShipment|TestUR3_|TestP021|TestP1C7_)' ./internal/core",
    "go test -count=1 -timeout=10m -v -run '^TestDiagnosticToolsRemainAvailableWithPoisonedLifecycleJournal$' ./internal/mcp"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly two top-level functions in internal/core/shipment_normalize_guard_u20c6_harness_test.go; no other function in the repository matches ^TestU20C6_.
  - TestU20C6_RefusesPendingReturnBlockedJournalForTargetAggregate: plant a v2 intent return-blocked journal for a member of the target shipment (TestNewWorkspace_RecoversPendingReturnBlockedJournal fixture shape). NormalizeBlockedShipmentForRecovery returns ErrShipmentConflict; message contains the journal base name and kind=return-blocked matched=<member ID>; shipment and member bytes unchanged; journal count and bytes unchanged. Inline journal-shape rows with identical assertions (TG-1/TG-2): v2 committed left unremoved; legacy schema-less (writeReturnBlockedJournal shape); malformed file named filepath.Base(returnBlockedJournalPath(root, target, member)) pre-checked with validateShipmentOperationJournalName to return the return-blocked kind; target-only malformed file named filepath.Base(returnBlockedJournalPath(root, target, nonMember)) whose message contains kind=return-blocked matched=<target ID>.
  - TestU20C6_RefusesPendingLifecycleIntentReferencingTargetMembers: plant a lifecycle intent for a DIFFERENT shipment whose preimage members include a target member ID; refusal message names the journal base name and kind=lifecycle matched=<member ID>; same no-write assertions. Inline control: a disjoint intent referencing neither the target nor its members does not block normalize, which succeeds.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C6_' ./internal/core -> exactly 2 top-level PASS when GREEN.
  - RED at 3a240fe091422e96d22b75b9af0854f66b60340f: normalize succeeds in both functions (the guard sees only same-shipment lifecycle intents). Compile check go test -run '^$' -count=1 ./... passes; '[no tests to run]' or 'no test files' is a halt. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready.
AC2 (Fix): aggregate guard with member-first precedence, fixed display labels, read-only scan, unchanged same-shipment check and message, unchanged lifecycle validation-error behavior; only the NormalizeBlockedShipmentForRecovery doc comment changes.
AC3 (Regression, exact counts, fail closed): command 1 exactly 16 top-level PASS (TestNormalizeBlockedShipment 6 + TestUR3_ 5 + TestP021 3 + TestP1C7_ 2); command 2 exactly 1. Zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC4 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/core/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/core/shipment_recovery.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope.
SHAs: one harness commit per task (HARNESS_COMMIT, staged by exact path); HARNESS_BASE_SHA = its parent; START_SHA = git rev-parse HEAD at the task's claim. Captures under logs/diagnostics/174082-*, never staged. H10 t.Parallel audit before scaffolding.
Wave W1 = {174.077-T, 174.079-T, 174.082-T}; production files disjoint. W0 draft fold must be complete before any Wave 20 harness. Wave convergence records FULL_SUITE_OPERATOR_DEFERRED: wave 1. No go test ./... with tests selected. No push until W3 convergence and the W3 pre-push gate are GREEN.
The scan-error refusal branch is defensive and has no harness row (the unchanged same-shipment check fails first on every ops-root error, shipment_recovery.go:1091 inspectShipmentOperationJournals).
Size: 2 files, at most 4 declarations. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage.
Documented deviations: EW-1, QG-1, QG-2, TG-1, TG-2. Related deferred captures (do not implement): 09D06A75 (related-artifact matching, scan consolidation, NormalizeBlockedShipment doc comment), 9900D0DD (uniform refusal format). Stash 388C586D (JSONL rewrite in shipment_recovery.go:822) remains deferred and out of scope.

Harness record (174.082-T):
* Command: go test -count=1 -timeout=10m -v -run '^TestU20C6_' ./internal/core
* Compilation: PASS; W1 compile-only convergence `go test -run '^$' -count=1 ./...` exited 0 at 041cfd20d332c6b42250eabf5a37cd1c5ec31500 (Go 1.24.0; no tests executed).
* Red Phase: CONFIRMED (scoped); both top-level tests failed through assertions; the disjoint-intent control passed; no compile error, panic, or timeout; see logs/diagnostics/174082-red-harness.txt and logs/diagnostics/174082-red-harness.metadata.json.
* HARNESS_COMMIT: 041cfd20d332c6b42250eabf5a37cd1c5ec31500
* HARNESS_BASE_SHA: dbf55bcc3886fc94d08236a54da3b09a122062ba
* H10: harness uses no parallel tests or shared write-seam overrides.
<!-- END:implementation-notes -->