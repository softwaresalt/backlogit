---
artifact_type: task
commit: 1a5393010a306528a146a66072fbef3a2b4d851e
created_at: 2026-09-15T20:45:13.0137098Z
dependencies:
    - 174.041-T
    - 174.047-T
id: 174.048-T
parent_id: 174-F
priority: high
status: done
title: 'R10: Subprocess crash/reopen GREEN tests'
updated_at: 2026-09-22T01:15:20.2135476Z
---

Subprocess-based integration tests that launch backlogit as a child process, kill it mid-operation, and reopen to assert recovery for block, unblock, and branch-scoped bootstrap. Domain: tests. AC: (1) crash-during-block reopens fully-applied-or-compensated (policy-aware terminal state) AND — using the real recovery + lock mechanism defined by R9 174.047-T — a competing governed operation DETERMINISTICALLY contends on the workspace-GLOBAL lock during recovery (the recovering process holds the workspace-global lock so the competitor is deterministically forced to wait until recovery completes; scheduler-independent); (2) crash-during-unblock preserves exact preimage/snapshot; (3) crash-during-bootstrap recovers via durable intent + snapshot file. Cross-cutting post-conditions on EVERY scenario: the recovered CANONICAL (Markdown source + append-only event) state is authoritative; the SQLite projection CONVERGES after `sync` (sync/rebuild convergence — NOT immediate-projection equality during recovery); and a correlated TERMINAL audit event is present. <4 scenarios (contention proof, projection convergence, and terminal-audit checks are folded into the three existing scenarios as assertions/post-conditions; no new scenario). Depends R3,R9.

Wave-3 verification-ownership amendment (2026-09-21): this GREEN task now OWNS the deterministic shared workspace-global-lock CONTENTION proof relocated from RED task R3 174.041-T (closing cycle-4 review P1 finding 1) — the authentic observable lock-contention hook exists only post-implementation, after R9 174.047-T defines the recovery + lock mechanism (already a dependency of this task, so no dependency edge changes). It also asserts SQLite projection convergence via `sync` (review P1 finding 3) and terminal correlated audit evidence. Task ID, dependencies (174.041-T, 174.047-T), priority, status, and shipment membership are UNCHANGED.

Wave-3 rev11 ownership-confirmation (2026-09-22): the `^TestUR3_` red-deliverable now CLOSES at WAVE 7 via R9 174.047-T (green-maker mapping corrected on RED 174.041-T to `green_maker_tasks: 174.047-T`, close-wave 7). This task (R10) is NOT a green-maker of `^TestUR3_`; it remains a SEPARATE post-implementation GREEN task gated by its OWN non-red-deliverable GREEN completion under its own subprocess selector. Its obligations are RETAINED IN FULL and NOT weakened: subprocess crash/reopen recovery for block/unblock/branch-bootstrap; the deterministic shared workspace-GLOBAL-lock CONTENTION proof (relocated from RED 174.041-T in rev8); SQLite sync/rebuild projection convergence; and correlated TERMINAL audit evidence — all folded into the existing 3 scenarios (no new scenario). Dependencies (174.041-T, 174.047-T), priority, status, and shipment membership are UNCHANGED.