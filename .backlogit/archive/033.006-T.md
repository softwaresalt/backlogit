---
artifact_type: task
created_at: 2026-04-19T16:22:51.85127-07:00
id: 033.006-T
labels:
    - cli
    - format
parent_id: 033-F
priority: medium
references:
    - docs/exec-plans/2026-04-19-cli-ux-output-formatting-plan.md
status: done
title: 'Unit 6: Wire `--format` to all read commands'
updated_at: 2026-04-19T18:32:01.0735894-07:00
---

Add persistent `--format` flag at root command. Resolve TTY detection at the root and pass bold flag to renderers. Default resolution: `table` for `list`, `get`, `queue view`; `json` for `stash *` and `shipment *` (preserves agent contract — PR-001 Option A). `--json` becomes silent alias for `--format=json` on commands that already had it.

Wire commands: list, get, queue view, stash list/get/fetch, shipment list/get. Characterization-first: write golden tests against current output BEFORE refactoring; ensure default-format byte equivalence per command. Goldens use fixed 3-row fixtures per command — column-width changes intentionally invalidate them; regenerate via `go test ./internal/cli -update`.

Files: internal/cli/root.go (modify: persistent flag + helper), internal/cli/list.go (modify), internal/cli/get.go (modify), internal/cli/queue_cmd.go (modify), internal/cli/stash.go (modify), internal/cli/shipment.go (modify), internal/cli/*_test.go (modify/new), internal/cli/testdata/*.golden (new).
Effort: ~3 hr. Skill domain: code. Test-first (characterization).
Depends on Unit 5 (Tile renderer must exist before --format=tile is wired).