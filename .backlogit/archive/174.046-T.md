---
artifact_type: task
commit: a5103b6df86eb4049eca2847643bd104d21ff441
created_at: 2026-09-15T20:44:59.6213701Z
dependencies:
    - 174.043-T
    - 174.044-T
id: 174.046-T
parent_id: 174-F
priority: high
status: done
title: 'R8: CLI + MCP parity for block/unblock/status'
updated_at: 2026-09-21T23:29:03.8153709Z
---

Expose block and unblock through both CLI and MCP over the same shared core contract, plus read/list status parity (blocked visible in get/list, excluded from active claim selection). Both surfaces invoke the EXACT same core seam — `core.BlockShipment(ctx, ws, id, core.BlockOptions{Reason, BlockedBy, ResumeCheckpointRef})` and `core.UnblockShipment(ctx, ws, id, core.UnblockOptions{Target, Confirm, UnblockedBy})` — with a 1:1 mapping from surface inputs to option fields so CLI and MCP cannot diverge: CLI `--reason`/MCP `reason` -> `BlockOptions.Reason` (required non-empty), `--by`/`by` -> `BlockedBy`, `--resume-checkpoint`/`resume_checkpoint_ref` -> `BlockOptions.ResumeCheckpointRef`; unblock `--to queued|active`/`target` -> `UnblockOptions.Target`, `--confirm`/`confirm` -> `Confirm` (required for BOTH unblock targets), `--by`/`by` -> `UnblockedBy`. Both surfaces surface the `blerrors.ErrNotImplemented`/governed sentinels identically. Domain: code. AC: (1) CLI and MCP block/unblock invoke identical core with identical option mapping (no surface-specific defaults or divergent field handling); (2) get/list surface blocked consistently across surfaces; (3) parity test asserts isomorphic results (same options -> same core call -> same result/error). <5 functions, <4 scenarios. Depends R5,R6.