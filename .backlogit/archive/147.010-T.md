---
archived_from: .backlogit/queue/147.010-T.md
artifact_type: task
created_at: 2026-08-24T19:03:15.2735464Z
dependencies:
    - 147.009-T
id: 147.010-T
labels:
    - checkpoint
    - core
    - plan-u5b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: archived
title: 'U5b: state-scoped quarantine classification (state-truthful refusal for non-active conforming targets)'
updated_at: 2026-08-25T01:33:00.0000000Z
---

Plan unit **U5b**. State-scoped classification pinning invariant I3 plus a real state-truthful refusal delta on the quarantine surface.

Files: `internal/core/checkpoint_disposition.go`, `internal/core/checkpoint_disposition_test.go`.

Production delta (cycle-8 rebalance under P-004): extend `QuarantineCheckpoint`'s in-memory classification so that when a target is valid+conforming (`validTarget == true` after 147.009-T / U5's `parse OK && validate OK && conformance OK` check) **and** `cp.Status != "active"`, refuse with `ErrCheckpointNotActive` wrapped as `%w: status=%s` rather than the bare `ErrCheckpointUseAbandon`. Only a valid+conforming **active** target continues to be refused with `ErrCheckpointUseAbandon`. Change form, inserted between the `validTarget` classification block and the archive/audit path:

```go
if validTarget {
    if cp.Status != "active" {
        return fmt.Errorf("%w: status=%s", blerrors.ErrCheckpointNotActive, cp.Status)
    }
    return blerrors.ErrCheckpointUseAbandon
}
```

This corrects a misleading redirect the current path emits: today, a `status:"resolved"` conforming document hitting quarantine returns `ErrCheckpointUseAbandon` ("use abandon instead"), but that same document then hits `AbandonCheckpoint`'s existing `status != "active"` gate and is refused with `ErrCheckpointNotActive` — the operator is bounced between verbs with neither surface naming the actual state-class problem. After U5b, both mutation verbs converge on the same truthful state-class sentinel for the non-active conforming case, so the pre-existing double-refusal state-conflict class named in the plan's Decisions and Rationale (Row 3 of the I3 discussion) is disclosed at first contact. `errors.Is(err, ErrCheckpointUseAbandon)` continues to hold for the sole active-scope classification remainder (U5's row-2 hard gate, unchanged); `errors.Is(err, ErrCheckpointNotActive)` now holds on the quarantine surface for the non-active conforming case, matching abandon's shape on the same document. The MCP mapping layer (`internal/mcp/errors.go:329`) already routes `ErrCheckpointNotActive` to `checkpoint_not_active`, so this change adds no new MCP code and no U7/U7d follow-up.

State-scoping rationale: the pre-existing state-conflict class is **not** widened to be accepted — widening quarantine to accept it would change what "quarantine" means, from "these bytes are untrustworthy" to "I want this file gone". Quarantine still refuses every non-active conforming target; the refusal is narrowed only on the sentinel it returns so that both mutation verbs name the truthful state-class problem instead of pointing at each other. Invariant I3's totality is scoped to `status: "active"` — the sole classification remainder inside the active scope is the conforming-active row that returns `ErrCheckpointUseAbandon`, and U5's row-2 guard pins it. This unit's tests pin state-scoped totality and cross-surface state-class agreement together, so a future reader cannot break either without a failing test.

<!-- BEGIN:acceptance-criteria -->
- `QuarantineCheckpoint`'s classification path returns `fmt.Errorf("%w: status=%s", blerrors.ErrCheckpointNotActive, cp.Status)` when the target parses, validates, is conforming, and `cp.Status != "active"`; it continues to return `blerrors.ErrCheckpointUseAbandon` (bare or wrapped consistently) only when `cp.Status == "active"`
- A `status:"resolved"` conforming row asserts `QuarantineCheckpoint` refuses with `errors.Is(err, ErrCheckpointNotActive)` and the error message names the status
- The same `status:"resolved"` conforming row asserts `AbandonCheckpoint` refuses the same document with `errors.Is(err, ErrCheckpointNotActive)` and the error message names the status (pre-existing shipped behaviour, colocated so a future edit cannot decouple the two surfaces' state-class agreement)
- A `status:"active"` conforming row asserts `QuarantineCheckpoint` refuses with `errors.Is(err, ErrCheckpointUseAbandon)` — 147.009-T / U5's row-2 hard gate, unchanged
- Quarantine is **not** widened to accept non-active conforming targets; it still refuses them, only with the truthful state-class sentinel
- The existing `TestQuarantineCheckpoint_ValidRefusesNamingAbandon` test (whose fixture uses `Status: "active"`) remains green because the active branch still returns `ErrCheckpointUseAbandon`
- No new sentinel is introduced; no error-message enrichment for observability alone; MCP mapping in `internal/mcp/errors.go` is unchanged (`ErrCheckpointNotActive` already routes to `checkpoint_not_active`)
- Expected red: case 1's `errors.Is(err, ErrCheckpointNotActive)` on the quarantine surface fails against 147.009-T / U5's landing state where `QuarantineCheckpoint` returns bare `ErrCheckpointUseAbandon` for the resolved conforming target — `errors.Is(ErrCheckpointUseAbandon, ErrCheckpointNotActive)` is false. Cases 2 and 3 are declared regression guards for pre-existing shipped behaviour (abandon's status guard and U5's row-2 refusal on the sole active-scope classification remainder). P-004 is satisfied by case 1 as the single red assertion; the earlier "no production change" / "green on landing" exemption is retired, and the prior cycle-8 observability-wrap proposal is retracted as out-of-scope refusal-observability.
<!-- END:acceptance-criteria -->