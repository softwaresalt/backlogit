---
archived_from: .backlogit/queue/068.004-T.md
archived_status: done
artifact_type: task
commit: 7450271a3334fc9c780ba757de6cf390a40edf3c
created_at: 2026-06-27T23:03:51.9184229-07:00
dependencies:
    - 068.001-T
    - 068.002-T
id: 068.004-T
labels:
    - refactor
    - doctor
    - characterization
parent_id: 068-F
priority: medium
references:
    - docs/exec-plans/2026-06-27-shared-frontmatter-codec-extraction-plan.md
status: archived
title: Migrate internal/core/doctor.go onto leaf packages
updated_at: 2026-06-28T12:41:42.6741756-07:00
---

Rewrite rewriteArchivedFromField to decode/encode via mdfront; replace atomicWriteArchiveFile call with atomicfile.WriteFileAtomic; delete frontmatterOpenLen, splitAtFrontmatterFence, atomicWriteArchiveFile. CRITICAL F1 guard: mdfront.Decode returns HasFrontmatter=false + nil map + no error on missing fence, but current code returns a HARD ERROR (caller skips). Add explicit if !md.HasFrontmatter { return error } guard AND guarantee non-nil map before fm[archived_from]=newValue, to preserve error->skip contract and avoid nil-map panic / synthetic-frontmatter corruption. Guard must exist + be tested BEFORE deleting helpers. Out of scope (F7): scanArchivedFrom keeps models.ParseFrontmatter (read-only detection). Posture: characterization-first. Depends on U1+U2. Maps to plan unit U4.

## Acceptance criteria

- `go test ./internal/core/...` green; `go vet` + `gofmt` clean.
- Characterization tests pin behavior BEFORE swapping the impl: (1) self-referential record rewrites ONLY archived_from, body bytes byte-identical, output byte-identical to captured pre-refactor bytes; (2) fence-less/malformed record returns an ERROR (error->skip parity — NOT a panic, NOT a synthetic-frontmatter write); (3) archive-write preserves the target's existing mode (clamped) + a read-only-destination Windows rename-fallback case.
- Runtime fixture uses a SELF-REFERENTIAL archived_from record (asserts byte-identical repaired output + preserved mode).
- SEPARATE assertion that a malformed record (038-DL/039-DL, value `done`) is left byte-UNCHANGED (flag-only, per the 067-S operator decision).
- `backlogit doctor --check-archived-from` and `--fix-archived-from` behave identically on both fixtures.