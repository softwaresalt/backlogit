---
artifact_type: task
commit: 1cb7b448
created_at: 2026-09-15T20:44:29.9793836Z
dependencies:
    - 174.052-T
id: 174.041-T
parent_id: 174-F
priority: high
status: done
title: 'R3: Crash/reopen BEHAVIOR RED harness'
updated_at: 2026-09-22T00:30:00.0000000Z
---

BEHAVIOR RED harness for durable-intent/preimage crash recovery. Depends on the declaration-only stubs (Rd 174.052-T) so it COMPILES against the declared block/unblock symbols and fails on BEHAVIOR (not compile). Models workspace startup + governed-operation recovery INVOCATION reading the durable intent + preimage under the same locks. Specifies subprocess kill+reopen expectations for block, unblock, and branch-scoped bootstrap. Domain: tests. AC: (1) tests COMPILE against the Rd declarations and FAIL on behavior until recovery/normalizer + subprocess GREEN land; (2) assert startup/governed recovery INVOCATION rolls an intent-without-commit forward/back deterministically per POLICY-AWARE TERMINAL STATE — compensated ⇒ exact preimage restored; committed ⇒ target fully applied — with the recovered CANONICAL authority (Markdown source + append-only `shipment_status_changed` event), never the index, as the state of record; (3) assert preimage restores exact member/shipment state AND a CORRELATED TERMINAL audit event is present (correlation id ties intent → committed/compensated outcome) — for a COMPENSATED restore this correlated terminal evidence is specifically a `shipment_status_changed` event recording the restored preimage status (a RETAINED, non-weakened obligation); (4) any SQLite index check is expressed as SYNC/REBUILD CONVERGENCE (equality asserted only AFTER `sync`), NOT immediate-projection equality during recovery — the index is a rebuildable, non-authoritative projection, so immediate-projection equality is explicitly NOT a RED-harness requirement. <4 scenario groups. Depends Rd (174.052-T).

EXCLUSION (Wave-3 verification-ownership amendment, 2026-09-21): this RED harness does NOT author the deterministic shared workspace-global-lock CONTENTION proof. The only observable hook available pre-implementation is immediately before the artifact lock, so deterministic proof that recovery contends on the workspace-GLOBAL lock cannot be authored reliably here (it would pass a global-lock-less implementation whenever the competing goroutine is scheduled late). That proof is OWNED by post-implementation GREEN task R10 174.048-T, after R9 174.047-T defines the actual recovery + lock mechanism. Task ID, dependencies, priority, status, shipment membership, and the red-deliverable-contract (green_maker_tasks 174.047-T/174.048-T, close-wave 8) are UNCHANGED.

Wave-3 fresh-review preservation note (2026-09-21): the requirement that the COMPENSATED restored-preimage outcome carry CORRELATED `shipment_status_changed` terminal evidence for the restored preimage status is a RETAINED, NON-WEAKENED harness obligation — the concurrent fresh-review P1 raising it is a clarification, NOT a contract defect (it was already required by AC (2) canonical authority and AC (3) correlated terminal audit; AC (3) now names it explicitly). No status, dependency, priority, membership, or red-deliverable-contract change.

Wave-3 rev11 red-deliverable mapping correction (2026-09-22): the `^TestUR3_` selector is CLOSED at WAVE 7 by R9 174.047-T, whose AC (1) turns R3's recovery tests GREEN — the TestUR3_ functions invoke recovery IN-PROCESS via workspace reopen (the subprocess mechanism only PRODUCES the crash state), so all behavior they assert is supplied by 174.047-T at wave 7. `green_maker_tasks` is therefore corrected to `174.047-T` and `green_maker_closes_wave` to `7` (was `174.047-T, 174.048-T` / close-wave 8), resolving the `WAVE_RED_DELIVERABLE_EARLY_GREEN` (P-002.6) block that fired because the selector greened at wave 7 while the mapping declared close-wave 8. R10 174.048-T is NOT a green-maker of `^TestUR3_`: it is a SEPARATE post-implementation GREEN task (subprocess crash/reopen integration + the rev8-relocated deterministic global-lock contention proof + SQLite sync/rebuild convergence + terminal correlated audit) gated by its OWN non-red-deliverable GREEN completion under its own subprocess selector; its obligations are RETAINED and NOT weakened. This corrects ONLY the mapping wording and the two `green_maker_*` fields — it does NOT make a green selector artificially red, and it SUPERSEDES the rev9 note's "no red-deliverable-contract change" scope for this rev11 mapping correction only. Task id, parent, `status: done`, priority, `dependencies` (174.052-T), and shipment membership are UNCHANGED; the dependency GRAPH is UNCHANGED (174.048-T still depends on 174.041-T, 174.047-T).

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Crash/reopen BEHAVIOR RED harness pinning durable-intent/preimage recovery; it compiles against the declaration-only stubs (Rd 174.052-T) and its in-process recovery-invocation assertions turn green after the recovery+normalizer (R9 174.047-T) lands at wave 7, so its TestUR3_ functions are behavior-red by design when it completes. The subprocess crash/reopen GREEN tests (R10 174.048-T, wave 8) are a SEPARATE post-implementation verification under 174.048-T's own non-red-deliverable GREEN gate (its own subprocess selector), NOT a green-maker of ^TestUR3_.
red_selector_command: go test -count=1 -run '^TestUR3_' ./internal/core
green_maker_tasks: 174.047-T
green_maker_closes_wave: 7
```
<!-- END:red-deliverable-contract -->