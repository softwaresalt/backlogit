---
artifact_type: task
commit: 817d46794342f0747f381e4d42e899d75d01c3cf
created_at: 2026-08-18T00:56:57.5001772Z
dependencies:
    - 143.006-T
id: 143.007-T
labels:
    - doctor
    - audit-log
    - core
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: done
title: Detect shipped_unarchived_residue with stranded-scope enumeration
updated_at: 2026-08-19T03:20:55.3321598Z
---

# Detect shipped_unarchived_residue with stranded-scope enumeration

## Description

<!-- BEGIN:description -->
Unit 7, second production task on the detection track, completing the audit. Emit exactly one `shipped_unarchived_residue` finding for a shipment whose `status` is shipped and which is not archived, reporting the path the canonical scan actually observed (`artifactRef.path`) rather than re-deriving it; the harness owns the routing-registry expectation (`.backlogit/registry.yaml` routes neither status set to shipped). Record in the finding `Description` whether the shipped event is present, because that determines which branch of the named-limitation recovery procedure the operator follows. Also enumerate the archive candidates left behind, matching what `collectArchiveCandidateIDs` (`internal/core/shipment_lifecycle.go:664-673`, called at `:515`) would have archived, which is the release-scope items AND any linked deliberations, because the indeterminate branch returns before that call and strands the whole archive set, not the shipment alone. Derive membership by re-reading the shipment artifact from the canonical scan, following the `detectInconsistentShipmentMembership` precedent, not from the SQLite projection. Apply the same unreadable-log rule as 143.006-T. Carry the same verify-the-actual-history caveat wording from `internal/core/doctor.go:545`, since the state is legitimately transient during an in-flight ship. Never write, synthesize, or rewrite JSONL. Execution posture: implementation.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- The `shipped_unarchived_residue` finding reports the path the canonical scan observed, not a re-derived one.
- The finding `Description` records whether the shipped event is present.
- The finding enumerates the stranded archive candidates, including linked deliberations, derived from the canonical scan rather than the DB projection.
- The transient-during-an-in-flight-ship caveat is carried in the finding text, and the unreadable-log rule matches 143.006-T.
- The check never writes or rewrites JSONL, asserted by the unchanged-log-bytes guard.
- Both 143.005-T scenarios are green and the 143.006-T non-mutation guard is still green.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/core/doctor.go`. The `archived_status` plumbing already landed in 143.006-T; this task adds only the second finding and its detail payload. Verify with `go test ./internal/core/ -run 'TestDoctor'`.
<!-- END:implementation-notes -->
