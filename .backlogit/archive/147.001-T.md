---
archived_from: .backlogit/queue/147.001-T.md
archived_status: done
artifact_type: task
commit: 4dc01945
created_at: 2026-08-24T19:00:36.2956863Z
id: 147.001-T
labels:
    - checkpoint
    - errors
    - plan-u1
    - harness-ready
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: archived
title: 'U1: non-conforming sentinel, typed error, and QuarantineIsRemedy predicate'
updated_at: 2026-08-28T08:15:59.3803679Z
---

Plan unit **U1**. Add the non-conforming sentinel, its typed error, and the canonical remedy predicate.

Files: `internal/errors/checkpoint_errors.go`, `internal/errors/checkpoint_errors_test.go`.

Add `ErrCheckpointNonConforming`, `CheckpointNonConformingError{Fields []string}` with `Error()` and `Unwrap() error` returning the sentinel (mirror `CheckpointUnknownFieldError` at `internal/errors/checkpoint_errors.go:81-108`), and exported `QuarantineIsRemedy(err error) bool`. `Fields` carries sorted, de-duplicated key **paths** only — never key values.

**Diagnostic bounding is not in this task (PR #377 cycle 16 split).** Quoting, the 16-path / 128-byte caps, and the truncation marker are the single bounded projection owned by **147.030-T / U1b**. They must serve `Error()`, the MCP `unknown_fields` array, and the CLI key list from one implementation, and a cap-trip case is a fourth effective flow this task cannot carry inside the four-scenario limit. This task declares the carrier; 147.030-T renders it.

**Harness-first (cycle-31, P-002.1).** The compile-capable **source-shape** harness lands **first** and is observed red. The `TestU1_` functions parse `internal/errors/checkpoint_errors.go` with `go/parser` and assert the declared shape through `go/ast`, so they name no undeclared identifier, compile against the pre-declaration tree, and fail on assertions rather than on a build error. The sentinel, the typed error, and `QuarantineIsRemedy` then land **together with their behaviour** in the green step and turn those functions green; the behavioural assertions land in that same commit as `TestU1Guard_` regression guards. **No declaration stub is committed ahead of the harness** — the cycle-16 "two-step red" ordering is withdrawn as a Principle II carve-out.

Red verification: `go test -count=1 -run '^TestU1_' ./internal/errors`. Harness test functions carry the mandatory `TestU1_` prefix so the selector cannot match 147.030-T / U1b's tests.

<!-- BEGIN:acceptance-criteria -->
- `errors.Is` matches the sentinel through the typed error and `errors.As` recovers `Fields` from a wrapped error
- `Error()` renders the sorted joined field list
- `QuarantineIsRemedy` is true for `ErrCheckpointUseQuarantine` and `ErrCheckpointNonConforming`, false for `ErrCheckpointNotActive` and `nil`
- Red harness (P-002/P-004 gate): all three scaffolded `TestU1_` source-shape functions compile against the **pre-declaration** tree and fail on assertions naming the absent shape (`ErrCheckpointNonConforming` is not declared, `CheckpointNonConformingError` has no `Fields []string` / `Unwrap() error`, `QuarantineIsRemedy` is not declared); the harness commit contains no passing function and no production stub
- Green-step guards (`TestU1Guard_*`, landing with the implementation): the three behavioural assertions — `errors.Is`/`errors.As` recovery through the wrap, the sorted joined `Error()` rendering, and the `QuarantineIsRemedy` truth table
<!-- END:acceptance-criteria -->