---
artifact_type: feature
created_at: 2026-07-22T19:13:23.0919819-07:00
custom_fields:
    harness_status: pending
    source_stash_id: 131CEAE4
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: 'OS-crash / power-loss durability for the size event+write path (out of scope for 099-S, Copilot G3). Both shared writers are sync-free: atomicfile.WriteFileAtomic (internal/atomicfile/atomicfile.go:15-63) does temp-write -> Chmod -> Close -> Rename with NO fsync before rename; events.EventWriter.AppendEvent (internal/events/stream.go:40-64) does O_APPEND write with NO f.Sync. So the 099-S event-before-write ordering guarantees PROCESS-crash safety only, NOT OS-crash/power-loss. Closing that gap requires a separate fsync protocol coordinated across BOTH shared writers (fsync the appended event file AND the atomic temp file before rename, plus parent-dir fsync). This is cross-cutting (changes the whole repo write path, not just the size seam) and must be its own release unit with its own review. Follow-up: design a repo-wide durability/fsync protocol for atomicfile + events. [ROUTING 2026-07-22: route as an isolated spike workflow (repo-wide atomicfile+events fsync durability protocol); its own release unit.]'
id: 120-F
labels:
    - spike
    - investigate-first
    - durability
priority: low
status: done
title: 'Spike: repo-wide atomicfile+events fsync durability protocol'
updated_at: 2026-07-23T10:56:37.2454111-07:00
---

SPIKE (its own future release unit). OS-crash/power-loss durability for the size event+write path. atomicfile.WriteFileAtomic (internal/atomicfile/atomicfile.go:15-63) and events.EventWriter.AppendEvent (internal/events/stream.go:40-64) are sync-free; 099-S guarantees only PROCESS-crash safety, not OS-crash/power-loss. Closing the gap needs a repo-wide fsync protocol coordinated across BOTH shared writers (fsync appended event file AND atomic temp file before rename, plus parent-dir fsync). Cross-cutting write-path change. Investigate/design only; do not implement this cycle.

SPIKE COMPLETE 2026-07-23 — conclusion: PROCEED. Findings artifact: docs/decisions/2026-07-23-atomicfile-events-fsync-durability-protocol-spike.md. Release-gating verdict: DO NOT hold v1.7.0 (sync-free is a pre-existing documented limitation, not a defect). Deferred implementation follow-up: feature 123-F (SEPARATE later release unit, NOT v1.7.0).