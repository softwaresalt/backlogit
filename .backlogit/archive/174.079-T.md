---
artifact_type: task
commit: d9a01e6fb242a9d73824b2e8f0df865e4d227d69
created_at: 2026-09-26T04:09:29.4055639Z
custom_fields:
    harness_status: scaffolded
id: 174.079-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - core-lifecycle
    - harness-ready
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: done
title: Order compensation evidence before compensated journal phase
updated_at: 2026-09-26T08:47:36.2118269Z
---

Wave 20 work unit U20C3 (unit token U20C3, P-002.6 prefix ^TestU20C3_) for the 155-S final-review remediation. Finding STD-P2-03 (in scope under P-021 C1/C3): compensation persists Phase=compensated before appending the terminal lifecycle event, so a crash between them leaves incomplete evidence that recovery skips.

Invariant (BlockShipment and UnblockShipment compensate closures in internal/core/shipment.go), on every compensation path that writes a terminal: (1) restore the preimages; (2) append compensated status evidence (phase applied, target and status = preimage shipment status); (3) append the terminal compensated event; (4) write the journal as compensated. Set mutationApplied BEFORE the governed shipment write is attempted (restore is idempotent; an indeterminate cause still skips compensation via the existing early return, DR1). Steps 2-4 run in order and stop at the first failure. If step 2, 3 or 4 fails the journal stays intent and the returned error carries ONLY blerrors.ErrWriteIndeterminate in its %w chain, renders the original cause and the failed-step error with %v, and names the step with its exact label: append compensation status evidence (new), append compensation event, or persist compensation journal (EW-1). Restore-failure behavior is unchanged (stash 7D8717B1). inspectShipmentLifecycleRecoveryEvidence, reconcileShipmentLifecycleIntent and recoverPendingShipmentOperations are NOT modified (they are the oracle). At most 1 shared helper may own steps 2-4.

Domain: core (Go). Class: NORMAL harness-required. Files: implementation 1 (internal/core/shipment.go: BlockShipment and UnblockShipment compensate closures, at most 1 shared helper); harness 1 (internal/core/shipment_compensation_order_u20c3_harness_test.go, package core, no t.Parallel). Wave W1; no dependencies. 174.078-T depends on this task.

Authoritative contract: plan Wave 20 sections 20.3.3, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=15m -v -run '^(TestUR1B_|TestUR3_|TestUR10_|TestP021|TestP1C4_|TestP1C6_)' ./internal/core",
    "go test -count=1 -timeout=10m -v -run '^TestUR8_' ./internal/cli"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly two top-level functions in internal/core/shipment_compensation_order_u20c3_harness_test.go, each table-driven over op in {block, unblock} (block fixture has >=1 active member); no other function in the repository matches ^TestU20C3_.
  - Injection: persistArtifactWriteFn on the FIRST governed write of the shipment in the target status; inside that same seam call, before returning, arm the obstruction. Function 1 injects landed-then-failed (call the real writer, then return the sentinel); function 2 injects failed-before-writing.
  - Obstructions: event log = move events.LogPathForItem(WorkspaceLogsRoot(root), id) aside and plant a directory; journal = move shipmentOpsRoot(root) aside and plant a regular file. One shared uniquely named helper does bounded rename retries, Lstat verification, restore by removing only the planted entry then renaming back (require.NoError on both), idempotent via a restored flag or sync.Once so the t.Cleanup is a no-op after the explicit pre-recovery call. 174.081-T calls this helper by name.
  - Error-shape rows (TG-2): function 1 adds a row whose sentinel wraps ErrShipmentConflict; function 2 adds rows wrapping ErrShipmentConflict and ErrWriteNotApplied. Every row: errors.Is false for ErrShipmentConflict, ErrValidation, ErrWriteNotApplied, ErrNotFound; errors.As into *blerrors.MutationPartialError false.
  - TestU20C3_CompensationEventFailureLeavesRecoverableIntent (log obstructed): IsWriteIndeterminate; message contains the sentinel text and 'append compensation status evidence'; journal still intent; immediately after the failed call shipment and members equal the preimage (for unblock the blocked envelope is valid); after removing the obstruction recoverPendingShipmentOperations returns NoError, journal compensated, state still preimage; log has exactly one correlated terminal compensated event and the last applied status target is the preimage status; Doctor reports no torn_shipment_lifecycle_intent and no evidence conflict. RED at 3a240fe0: journal already compensated when the event append fails; for unblock the landed write is never restored.
  - TestU20C3_CompensationJournalFailureAfterTerminalEvidenceRecovers (ops root obstructed): IsWriteIndeterminate; message contains the sentinel text and 'persist compensation journal'; compensated applied status evidence precedes exactly one terminal compensated event; after removal recovery NoError, journal compensated, Doctor clean, no conflict. RED at 3a240fe0: no compensated status evidence, recovery fails with 'terminal evidence lacks matching final status evidence'.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C3_' ./internal/core -> exactly 2 top-level PASS when GREEN.
  - RED at 3a240fe091422e96d22b75b9af0854f66b60340f as above; compile check go test -run '^$' -count=1 ./... passes; '[no tests to run]' or 'no test files' is a halt. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready. If a case is unreachable with existing seams or nondeterministic on Windows, harness-architect STOPS and returns the task to Stage; no production seam may enter the harness commit.
AC2 (Fix): order restore -> compensated status evidence -> terminal compensated event -> compensated journal, stopping at first failure; early mutationApplied; ErrWriteIndeterminate-only %w chain with the exact step labels.
AC3 (Regression, exact counts, fail closed): command 1 reports exactly 17 top-level PASS (TestUR1B_ 3 + TestUR3_ 5 + TestUR10_ 1 + TestP021 3 + TestP1C4_ 1 + TestP1C6_ 4); command 2 exactly 3 (TestUR8_). Zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC4 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/core/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/core/shipment.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope.
SHAs: one harness commit per task (HARNESS_COMMIT, staged by exact path); HARNESS_BASE_SHA = its parent; START_SHA = git rev-parse HEAD at the task's claim. Captures under logs/diagnostics/174079-*, never staged. H10 t.Parallel audit before scaffolding.
Wave W1 = {174.077-T, 174.079-T, 174.082-T}; production files disjoint. W0 draft fold must be complete before any Wave 20 harness. Wave convergence records FULL_SUITE_OPERATOR_DEFERRED: wave 1. No go test ./... with tests selected. No push until W3 convergence and the W3 pre-push gate are GREEN.
The obstruction helper defined here is reused BY NAME in 174.081-T (never copied; a duplicate identifier in package core breaks compilation).
Size: 2 files, at most 3 production declarations. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage.
Documented deviations: EW-1 (extended), QG-1, QG-2, TG-1, TG-2. Related deferred captures (do not implement): 7D8717B1 (restore-failure double-fault classification; member-write mutationApplied timing).

Harness record (174.079-T):
* Command: go test -count=1 -timeout=10m -v -run '^TestU20C3_' ./internal/core
* Compilation: PASS; W1 compile-only convergence `go test -run '^$' -count=1 ./...` exited 0 at 041cfd20d332c6b42250eabf5a37cd1c5ec31500 (Go 1.24.0; no tests executed).
* Red Phase: CONFIRMED (scoped); both top-level tests failed through assertions, no compile error, panic, or timeout; see logs/diagnostics/174079-red-harness.txt and logs/diagnostics/174079-red-harness.metadata.json.
* HARNESS_COMMIT: dbf55bcc3886fc94d08236a54da3b09a122062ba
* HARNESS_BASE_SHA: bfc74f8897d90b8db312e570ae6707e3711528e8
* H10: parallel-test audit found no parallel test calls to persistArtifactWriteFn or persistArtifactPreLockHook.
<!-- END:implementation-notes -->