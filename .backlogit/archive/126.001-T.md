---
artifact_type: task
commit: 59269785c502ec268a9eaaf11c6e70b76e3cee0a
created_at: 2026-07-25T22:31:43.7516025Z
id: 126.001-T
labels:
    - config
    - markdownlint
parent_id: 126-F
priority: medium
status: done
title: 'U1: Add .markdownlint.json (MD001/MD025/MD041, MD025 _title retarget) + gitignore cli2 runner'
updated_at: 2026-07-26T07:43:37.4017898Z
---

Domain: config. Create `.markdownlint.json` with EXACTLY this content: `{ "default": false, "MD001": true, "MD025": { "front_matter_title": "^\s*_title\s*[:=]" }, "MD041": true }` (no other rule IDs). Rationale (the crux, repo-wide doctor-to-compliance — supersedes the earlier scoped-ignore direction): MD025's `front_matter_title` is retargeted to a non-existent `_title` key so a frontmatter `title:` is NOT counted as a heading; this eliminates all 229 frontmatter-`title:` + body-`# H1` MD025 double-counts with ZERO file edits. HARD GUARDRAIL: MD041 MUST stay `true` with DEFAULT options (its `front_matter_title` keeps matching `title:`) so every frontmatter file still credits MD041; retargeting MD041 to `_title` or `""` would fail ~1,262 frontmatter files. Also create `.markdownlint-cli2.jsonc` carrying ONLY the runner option `{ "gitignore": true }` — NOT an `ignores` array, NOT an inline `config` key, NOT a `customRules` key — so cli2 lints the non-gitignored corpus (in a clean CI checkout this equals exactly the tracked set) and excludes local gitignored scratch (`.copilot/`, `.autoharness/`, `logs/`); cli2 still auto-discovers `.markdownlint.json` for the rule set. Empirically verified with markdownlint-cli2@0.23.1 over 1839 tracked files: default rule set = 250 violations (MD001=1 / MD025=229 / MD041=20); the `_title` config alone (zero file edits) = 21 (MD001=1 / MD025=0 / MD041=20). The residual 21 are remediated in U-remediation so the repo reaches 0 violations repo-wide. Files: `.markdownlint.json` (rules) and `.markdownlint-cli2.jsonc` (runner: gitignore-aware globbing).