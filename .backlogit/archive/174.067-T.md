---
artifact_type: task
commit: 9b5b7a0e
created_at: 2026-09-24T01:19:22.1333518Z
id: 174.067-T
parent_id: 174-F
priority: high
status: done
title: Close singular typed-writer pending-recovery barrier and reconcile shipment lock-order inversion
updated_at: 2026-09-24T02:04:42.7557455Z
---

Corrective work unit (Wave 14) closing the R-A HIGH/P1 (3/3) and R-B MEDIUM/P1 (2/3) final-review blockers at HEAD 5e4a04ca. Same-contract completion of the shared shipment serialization contract (provenance 174.064-T lock-order work, already done); created as a NEW corrective task because the prior final review-fix authorization is exhausted and owning tasks are complete. Concurrency/lock-hierarchy domain only.

<!-- BEGIN:acceptance-criteria -->
AC1 (R-A closed at the correct propagation point, NOT unconditionally in the singular helper): close R-A so a singular typed-writer mutation of a shipment lifecycle MEMBER enters the shipment-lifecycle-global pending-intent recovery barrier BEFORE the artifact lock. The fix MUST be applied at the barrier-gate in persistArtifactWithLinkPolicyAndGuard (shipment.go ~1838-1857) - where `ctx = lockedCtx` propagates the shipmentLifecycleGlobalLockContextKey reentrancy token - by broadening its current `artifact.ArtifactType == "shipment"` condition to also cover shipment lifecycle members, OR by making the singular helper return the locked context so the token propagates. It MUST NOT be applied as an unconditional acquire inside lockArtifactMutation (shipment.go:1293): that helper is the generic persist primitive for EVERY artifact type and returns only (func() error, error), so an unconditional barrier there would (a) route ALL non-member/standalone writes through the single global lock and run recoverPendingShipmentOperations on every write (broad serialization/behavior regression) and (b) risk a fail-closed break via validateShipmentLifecycleGlobalReentry (shipment.go ~184-190) when a nested persist is reached under an already-held singular artifact lock without the global marker. Preserve the reentrancy held-check (a caller already holding global via the context token MUST NOT re-acquire - no self-deadlock/double-lock). Unrelated standalone-artifact writes MUST NOT be newly barriered.
AC2 (Canonical GLOBAL-FIRST invariant, scoped - not a total order): enforce that on the R-A/R-B-implicated paths shipment-lifecycle-global is acquired BEFORE any membership, item-log, or artifact lock. Do NOT re-order or refactor unrelated already-conforming callers. The item-log<->artifact RELATIVE order is explicitly NOT constrained on the snapshot/ship path (ClaimShipment/ShipShipment acquire artifact locks before item-log locks in shipment_lifecycle.go and are dominated by the global barrier); the canonical assertion is the GLOBAL-FIRST invariant only, not a total order across item-log and artifact.
AC3 (R-B inversion eliminated; extended global hold acknowledged): reconcileShipmentToShippedImpl PHASE A (shipment_reconcile_transaction.go ~82) and lockShipmentReconcileCThenB (shipment_reconcile_lock.go) MUST acquire the global barrier BEFORE lockShipmentMembership / item-log locks, matching AddItemToShipment's global->membership order (shipment.go ~1414-1434). It is EXPECTED AND ACCEPTABLE that global is then held across the reconcile's slower Phase C evidence/merge-commit I/O (rare recovery operation); this extended global hold is INTENTIONAL and MUST NOT be later flagged as a regression. After the fix no path holds membership/item-log before global while Add holds global before membership.
AC4 (Lock-order audit, GLOBAL-FIRST only): enumerate in implementation-notes and/or test evidence every lock-acquisition site on the R-A/R-B-implicated paths (persistArtifactWithLinkPolicyAndGuard, lockArtifactMutation/lockArtifactMutations, AddItemToShipment, reconcileShipmentToShippedImpl, lockShipmentReconcileCThenB) with its acquisition order, and assert no residual membership/item-log/artifact-before-GLOBAL path remains. The audit asserts the GLOBAL-FIRST invariant ONLY; it does NOT assert a total order over item-log vs artifact and does NOT require touching unrelated conforming callers.
AC5 (Deterministic singular-member-wait test - NON-VACUOUS): a deterministic regression proving a singular typed-writer mutation on a NON-shipment shipment MEMBER artifact (the path that presently omits the barrier) WAITS on the held global barrier and then proceeds. The test MUST target a non-shipment member artifact, NOT the shipment artifact itself (mutating the shipment artifact already acquires the barrier via the ArtifactType=="shipment" gate and would pass VACUOUSLY green-before-fix).
AC6 (Deterministic Reconcile-vs-Add contention test - asserted at the LOCK layer): a deterministic regression proving concurrent reconcile (global-first) and AddItemToShipment membership contention cannot deadlock and both serialize on the canonical global-first order. Because AddItemToShipment requires an ACTIVE shipment while Reconcile targets a shipped/archived shipment, the contention MUST be constructed and asserted at the LOCK-ACQUISITION layer (which precedes the business-status precondition) - e.g., via the existing p021ObserveGlobalLock hook plus a reconcile-side gate - with a bounded double-timeout RED (inversion reproduction) and convergence GREEN, INDEPENDENT of whether both operations business-complete. Run under -race.
AC7 (No weakening of existing serialization): preserve TestP021ClaimSerialization_MembershipAndGenericWritersUseGlobalLock (including generic_update_waits) and every existing serialization guarantee unchanged; generic/membership-writer serialization MUST NOT be weakened, skipped, or made conditional; the reentrancy "verified_reentry_is_a_no_op" guarantee is preserved.
AC8 (Scope confinement / whitelisted R-B sites / no new primitive): changes confined to internal/core/shipment.go (persistArtifactWithLinkPolicyAndGuard barrier-gate) and - as the INDISPENSABLE R-B inverse-path sites - internal/core/shipment_reconcile_transaction.go (reconcileShipmentToShippedImpl Phase A) and internal/core/shipment_reconcile_lock.go (lockShipmentReconcileCThenB), plus the exact test files. No other production file. Introducing ANY new production lock primitive or lock API beyond the existing lockShipmentLifecycleGlobal barrier is OUT OF SCOPE BY DEFINITION -> Ship HALTs and classifies.
AC9 (RED-before / GREEN-after): the new singular-member-wait and Reconcile-vs-Add contention regressions MUST fail deterministically BEFORE the fix (a bounded deadlock/timeout reproduction is an acceptable cheap RED - a full hang is not required) and pass deterministically AFTER. Targeted GREEN: go test -run for the affected serialization/lock-order tests over ./internal/core, under -race. Core and full-repository suites deferred to the shared Wave 14 authorized operation (AC11).
AC10 (Revertibility + preserve EXISTING fail-closed; NO new runtime detector): the change is revertible by restoring the prior gate/order. PRESERVE the existing fail-closed behavior and its structured diagnostic (shipmentLockOrderDiagnostic), whose runtime detection covers the ARTIFACT<->GLOBAL order it can actually observe from the context lock-set. Do NOT introduce a NEW runtime lock-order detector for the membership/item-log tiers - those tiers are enforced BY CONSTRUCTION plus the AC4 audit, not by a new runtime mechanism (a new detector would be YAGNI and would collide with AC8's no-new-primitive HALT gate). No silent lock downgrade or order relaxation.
AC11 (Shared circuit / full-suite disposition): the same-operation full-suite circuit is OPEN. Do NOT run go test ./... within this task. After BOTH Wave 14 corrective commits (174.067-T and 174.068-T) land, the full suite runs ONCE as a NEW separately-authorized final-gate operation at a new commit/phase (explicit operator authorization required immediately before it); then exactly one standard + one adversarial review over ONLY the changed surfaces of both tasks; stop gate zero P0/P1. If a full-suite run is forbidden even post-correction, the compliant alternative is targeted per-package verification (./internal/core plus affected packages) under explicit authorization - never a skipped mandatory gate.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Root cause (R-A, HIGH/P1 3/3): singular writes reach lockArtifactMutation (shipment.go:1293) with no global barrier; persistArtifactWithLinkPolicyAndGuard (~1838-1857) acquires the barrier ONLY when artifact.ArtifactType=="shipment", so non-shipment shipment-member writes skip the pending-intent recovery barrier that the plural lockArtifactMutations (1304-1315) always takes.
Root cause (R-B, MEDIUM/P1 2/3): reconcileShipmentToShippedImpl Phase A (shipment_reconcile_transaction.go ~82) / lockShipmentReconcileCThenB (shipment_reconcile_lock.go) acquire membership/item-log BEFORE global (via the plural call's internal global), opposing AddItemToShipment's global->membership (shipment.go ~1414-1434): a true opposing-order cycle on {global-sentinel, membership(shipmentID)}.
Corrected fix location (per concurrency+correctness review): apply R-A at the persistArtifactWithLinkPolicyAndGuard barrier-gate (preserving ctx=lockedCtx token propagation and the reentrancy held-check), NOT unconditionally in lockArtifactMutation (which is the generic all-types persist primitive and returns no ctx - an unconditional acquire there over-serializes every write, runs recoverPendingShipmentOperations on every write, and risks a validateShipmentLifecycleGlobalReentry fail-closed break). Apply R-B by making Reconcile Phase A acquire global first. Reentrancy is keyed by the shipmentLifecycleGlobalLockContextKey context token, so a caller already under global no-ops correctly.
Extended global hold across reconcile Phase C is intentional (rare recovery op). Canonical assertion is GLOBAL-FIRST only; item-log<->artifact relative order is not constrained on the snapshot/ship path (dominated by the global barrier). Do NOT add a new runtime membership/item-log lock-order detector (YAGNI + collides with no-new-primitive).
Scope: shipment.go (persist barrier-gate) + shipment_reconcile_transaction.go (Phase A) + shipment_reconcile_lock.go (CThenB) + exact tests. No new lock primitive.
Membership: governed member of active 155-S; no dependency edge (membership-as-gate). Provenance 174.064-T (done) informational, not a blocks edge.
<!-- END:implementation-notes -->