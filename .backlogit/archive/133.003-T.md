---
archived_from: .backlogit/queue/133.003-T.md
archived_status: done
artifact_type: task
commit: 47dfcc93698a6b0b2c5420c701c365a538895580
created_at: 2026-08-01T04:56:53.4714584Z
id: 133.003-T
labels:
    - tests
    - ship
    - cascade
parent_id: 133-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-shipshipment-partial-feature-archive-cascade-plan.md
status: archived
title: Add feature-inclusive and body-planned ship regression tests
updated_at: 2026-08-01T20:21:30.4842244Z
---

Domain: tests (plan Unit 1b). Execution posture: test-first — land red before Unit 2. Add two new ShipShipment tests. Sibling of Unit 1a; both must be landed (red) before Unit 2.

New tests:
1. Feature-inclusive full-feature characterization: manifest lists the feature ID with all children terminal → feature is set done and archived. Locks the full-feature close path (baseline preserved — passes both before and after Unit 2).
2. Body-planned-only regression (the critical R3/R4 test — exercises the transitive parent-status rollup seam): covering feature whose ONLY recorded children are the shipped tasks; ship children-only. On current main this fails because completeReleaseScope marking the recorded children done cascades (cascadePersistedParentStatuses -> ComputeParentStatus) and rolls the covering feature to done + relocates it. After Unit 2 (rollup neutralized for the non-member feature) it passes.

CRITICAL — avoid the false-green: the rollup path closes+relocates the feature WITHOUT adding it to result.ArchivedIDs, so asserting only ArchivedIDs absence false-greens. The body-planned regression MUST assert the covering feature's findArtifact status is NOT done AND that its file is still under .backlogit/queue/.

## Acceptance Criteria
- Body-planned regression fails against current main (feature wrongly done/relocated) and passes after Unit 2; the feature-inclusive characterization passes both before and after (baseline preserved).
- The body-planned regression asserts covering-feature status (not done) AND queue-directory location — NOT merely result.ArchivedIDs absence (which false-greens against the rollup path).
- Tests compile and run under `go test ./internal/core/...`.