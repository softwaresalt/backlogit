---
artifact_type: task
commit: 4d8a08fd
created_at: 2026-09-23T17:19:55.9707172Z
dependencies:
    - 174.044-T
    - 174.045-T
    - 174.047-T
id: 174.064-T
parent_id: 174-F
priority: high
status: done
title: Canonicalize shipment-lifecycle/artifact lock order; close inverse-caller paths
updated_at: 2026-09-24T01:00:51.9986008Z
---

Corrective work unit closing the lock-order inversion exposed by the final R1 shared-serialization remediation on 155-S. updateArtifactUngated (internal/core/artifacts.go:523) and AddItemToShipment/lifecycle mutators acquire lockShipmentLifecycleGlobal (the shipment-lifecycle-global membership lock, shipmentLifecycleGlobalLockID) BEFORE lockArtifactMutations -- global then artifact. Pre-existing persistence callers reach the lifecycle pending-recovery barrier AFTER already holding artifact locks via UpdateArtifact -> updateArtifactUngated -> persistArtifact -> persistArtifactWithLinkPolicyAndGuard -- artifact then global. The two orders form an ABBA inversion that deadlocked the core suite at TestCheckChildrenTerminal_NonTerminalChild_ReturnsBlockingError. Naively releasing the global lock before artifact locks breaks generic-writer serialization (TestP021ClaimSerialization_MembershipAndGenericWritersUseGlobalLock/generic_update_waits). Define ONE canonical lock hierarchy and route every implicated caller through it, preserving P-021 generic/membership-writer serialization. Same-contract mechanical consequence of R1/shared-serialization; separately planned because the prior review-fix cycle is exhausted and all owning tasks (174.044-T, 174.045-T, 174.047-T) are already done. Domain: code (concurrency/locking). Scope: internal/core lock-ordering only; no shipment-lifecycle semantic change beyond acquisition order. Bound ~<=2h.

<!-- BEGIN:acceptance-criteria -->
1. CANONICAL HIERARCHY: define and document a single global->artifact ordering (shipment-lifecycle-global acquired before artifact-mutation locks) as the ONLY permitted acquisition order across all shipment-lifecycle and artifact-persistence callers; no code path acquires the artifact-mutation lock then the shipment-lifecycle-global lock. 2. INVERSE-PATH CLOSURE + HOLD-SPAN + RE-ENTRANCY: eliminate every artifact->global inverse path implicated by the pending-recovery barrier -- specifically UpdateArtifact -> updateArtifactUngated -> persistArtifact -> persistArtifactWithLinkPolicyAndGuard and any peer persistence caller that enters recoverPendingShipmentOperations / the lifecycle barrier while holding artifact locks. The shipment-lifecycle-global lock MUST be acquired first AND HELD CONTINUOUSLY spanning artifact-lock acquisition as one nested critical section -- no acquire/release/reacquire window that a concurrent artifact->global holder could interleave. Because updateArtifactUngated already holds the global lock before persistArtifact re-enters the barrier's own lockShipmentLifecycleGlobal, the barrier's global acquisition MUST detect prior ownership via a ctx-carried held-lock token and become a verified no-op when already held; if that token is absent while the global lock is contended it MUST error, never silently skip or downgrade. No double-acquire and no single-goroutine re-entrant self-deadlock. 3. SERIALIZATION PRESERVED: TestP021ClaimSerialization_MembershipAndGenericWritersUseGlobalLock (all subtests incl. generic_update_waits) still passes; the generic_update_waits sub-assertion remains a POSITIVE wait assertion (not skipped/short-circuited); generic-writer, membership-writer, and Claim writers continue to contend on the same global lock; serialization is NOT weakened. 4. DETERMINISTIC DEADLOCK REGRESSION (assertion-based, NOT timeout-based): add a scheduler-independent regression whose pass/fail signal is the AC7 out-of-order-acquisition diagnostic/assertion rather than an observed hang or timeout. It MUST (a) deterministically FAIL under the pre-fix ordering and pass under the canonical order for the scenario behind TestCheckChildrenTerminal_NonTerminalChild_ReturnsBlockingError; (b) exercise concurrent membership/generic-writer contention under -race across repeated iterations, proving no ABBA remains, reusing the p021ObserveGlobalLock seam where applicable; and (c) cover the single-goroutine re-entrant self-acquire case (persist path re-entering the barrier while already holding global) failing pre-fix and passing post-fix. 5. LOCK-ORDER AUDIT (exhaustive + transitive): enumerate every caller of lockShipmentLifecycleGlobal / lockShipmentMembership(shipmentLifecycleGlobalLockID) and lockArtifactMutations, AND trace the transitive call graph of every artifact-lock holder into persistArtifactWithLinkPolicyAndGuard / recoverPendingShipmentOperations / the lifecycle barrier and the P-021 claim/generic-writer sites, across artifacts.go, shipment.go, shipment_lifecycle.go, shipment_recovery.go, archive.go, doctor.go, queue.go. PROVE this set is EXHAUSTIVE for the global lock (a repo-wide search for the global-lock handle returns only these sites; otherwise widen the audit repo-wide) and confirm uniform compliance with the canonical hierarchy (audit captured as implementation-note or test comment). 6. VALIDATION: focused regressions pass (TestCheckChildrenTerminal_*, TestP021ClaimSerialization_*, TestLifecycleMutators_RunPendingRecoveryBarrierBeforeAggregateAccess); full go test ./internal/core/... passes without timeout AND under -race; full-repo go test ./..., go vet ./..., golangci-lint run, gofmt -l . all clean. 7. ROLLBACK/DIAGNOSTICS: fix is revertible as a self-contained change; on lock-acquisition failure or a detected out-of-order / re-entrant acquisition emit a structured diagnostic (lock id, caller, ordered set of held locks) sufficient to localize the inversion AND to drive AC4's deterministic assertion; no silent lock downgrade or skip. Record the intentional broadened critical section (all artifact writes now serialize on the global lock) so a later change does not optimize it back into the inversion.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Do NOT reopen completed task statuses; 174.044-T/174.045-T/174.047-T remain done. Preserve the uncommitted R1-R5/R8 Ship implementation (10 files, internal/core/*) -- this task builds on it, does not revert it. Prefer a single canonical acquisition order (global->artifact); if the pending-recovery barrier must run before artifact locks, HOIST the barrier rather than release-and-reacquire in a way that opens a serialization gap. Keep change minimal: <3 source files, <4 test scenarios. Dependencies on 174.044-T (global-lock serialization owner), 174.045-T (generic-writer routing through governed writer), 174.047-T (pending-recovery barrier under locks) encode provenance/order only -- all are done, so the task is immediately eligible; membership in active shipment 155-S gates PR readiness until this task is done.
<!-- END:implementation-notes -->