---
artifact_type: task
commit: 817d46794342f0747f381e4d42e899d75d01c3cf
created_at: 2026-08-18T00:56:48.2096322Z
dependencies:
    - 143.005-T
id: 143.006-T
labels:
    - doctor
    - audit-log
    - core
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: done
title: Detect missing_shipped_event with archived_status plumbing
updated_at: 2026-08-19T03:20:47.3654183Z
---

# Detect missing_shipped_event with archived_status plumbing

## Description

<!-- BEGIN:description -->
Unit 6, first production task on the detection track, gated behind the observed-failing 143.005-T harness. Register the check, gated on `opts.CheckShippedEventCompleteness`, beside the `opts.CheckOverArchivedFeatures` block (`internal/core/doctor.go:522`), extracting the body into a `detectMissingShippedEvents(...) ([]DoctorFinding, error)` helper rather than inlining it, following the newest in-file precedent (`detectPartialCommitAssociations`, `detectInconsistentShipmentMembership`). The check MUST read `archived_status`, not just `status`: `ArchiveItem` sets `status: archived` and stores the pre-archive status in `archived_status` (`internal/core/archive.go:223`), so an archived shipped shipment presents as `status: archived`. Neither `artifactRef` (`internal/core/canonical_scan.go:21`) nor the doctor-local struct (`internal/core/doctor.go:230-236`, `:253-260`) carries `archived_status` today, so both must be extended; without that filter the audit false-positives on shipments archived from abandoned or any other status. Scan queue AND archive from the canonical raw-Markdown scan, not the SQLite projection. Emit exactly one `missing_shipped_event` finding for an archived shipment whose `archived_status` is shipped and whose item JSONL carries no `shipment_status_changed` event with status shipped. An item log that cannot be READ AT ALL (open or scan failure, as distinct from absent or malformed-line-skipped) must NOT be reported as a missing event: report it as the same finding type with an explicit log-unreadable, event-presence-unknown `Description` so a false negative can never masquerade as a clean result, and never abort the whole doctor run for one unreadable file. Carry the verify-the-actual-history caveat wording from `internal/core/doctor.go:545`. Never write, synthesize, or rewrite JSONL. The second finding type is 143.007-T and is out of scope here. Execution posture: implementation with one colocated non-mutation guard.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- `artifactRef` and the doctor-local struct carry `archived_status`, added additively and unused elsewhere.
- The check filters on `archived_status` and does not flag shipments archived from a non-shipped status.
- Detection reads authoritative state from the canonical raw-Markdown scan across queue and archive, not the DB projection.
- An unreadable item log is reported explicitly as event-presence-unknown, never counted as a missing event, and never aborts the doctor run.
- The colocated non-mutation guard asserts byte-identical `.backlogit/logs` by recursive comparison with `fix_orphans` off, and is green from this task onward.
- 143.005-T scenario 1 is green including both negative subtests, and scenario 2 is still red because the second finding does not exist yet.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/core/doctor.go`, `internal/core/canonical_scan.go`, `internal/core/doctor_shipped_event_test.go` (the colocated non-mutation guard is added to the harness file). Widening `artifactRef` is mandatory, not conditional; the struct is shared with the create-time uniqueness guard, so the added field must be additive and unused elsewhere. Verify with `go test ./internal/core/ -run 'TestDoctor'`.
<!-- END:implementation-notes -->
