---
archived_from: .backlogit/queue/146.005-T.md
archived_status: done
artifact_type: task
commit: 15ab30a2a394439f52e5338fc94d1c50e3f395ae
created_at: 2026-08-22T01:04:08.2379889Z
dependencies:
    - 146.001-T
id: 146.005-T
labels:
    - stage-harvested
    - unit-U1b
    - domain-tests
parent_id: 146-F
priority: high
references:
    - docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md
    - docs/decisions/2026-08-21-success-shaped-evidence-loss-deliberation.md
status: archived
title: Red harness for checkpoint marshaller-safety guards
updated_at: 2026-08-24T08:21:38.6155719Z
---

Plan unit U1b. Pin the traps a hand-written MarshalJSON on a value-typed struct field can fall into, plus the modeled-key drift guard and the HTML-escape guard. Every scenario builds input from literal JSON bytes decoded with json.Unmarshal, never by marshalling a CheckpointContext, with one carve-out: scenario 2 may inject a modeled key into Extra directly after decoding, because a correct UnmarshalJSON makes that state unreachable through the decode path.

<!-- BEGIN:Notes -->
Gate run 9/10 remediation: U1b gains **scenario 5, the Extra key-injection and key-escape guard**. Two `Extra` keys are decoded from literal-JSON fixtures and re-emitted: one containing `"`, `\`, and a newline (for example `foo","shipment_id":"pwned` followed by a newline), one containing `a > b && b < c`. The written bytes must still decode into a `map[string]any` whose `context` carries each exact literal key with its original value and whose `shipment_id` is unchanged, and the raw bytes must NOT contain `\u0026`. This fails any `emit()` that splices raw key text into a buffer AND any `emit()` that encodes keys with escape-enabled `json.Marshal`. U1b now has five scenarios: 1, 4, and 5 are red before U2; 2 and 3 are green throughout. Recorded as Task Granularity deviation (6).
<!-- END:Notes -->