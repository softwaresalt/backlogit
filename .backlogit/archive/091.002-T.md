---
archived_from: .backlogit/queue/091.002-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-10T19:17:43.4145225-07:00
custom_fields:
    source_stash_id: 53FCC92A
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: |-
        Docs cleanup: remove all documentation that instructs `npm install` / `npx @backlogit/backlogit-mcp` for backlogit, since npm is no longer a valid install/publish mechanism (npm publishing was retired in v1.5.0 / PR #205, and @backlogit/* is not published).

        Problem: several docs still tell users to install via npm, which does not work. Replace those with the valid paths: `copilot plugin install softwaresalt/backlogit` (plugin), the curl/irm install scripts, `go install github.com/softwaresalt/backlogit/cmd/backlogit@latest`, and direct GitHub Releases binary download (SHA256-verified).

        Known locations to audit/fix (grep docs + README for `npm install`, `npx`, `@backlogit/backlogit-mcp`):
        - README.md (Plugin Installation section: `npm install -g @backlogit/backlogit-mcp`).
        - docs/plugin-guide.md (Option 2 npm global install; the "Tier 2 npm optional dep" row in binary resolution).
        - docs/installation.md (if it references npm).
        - Any harness/instruction docs (.github/**, AGENTS.md, copilot-instructions) that mention npm install.

        Acceptance criteria:
        - No doc instructs `npm install` or `npx` for obtaining/running backlogit.
        - The documented install paths are: Copilot plugin, install script, go install, and GitHub Releases download.
        - A grep of README + docs for `npm install`/`npx @backlogit` returns 0 instructional hits (historical/changelog mentions may remain if clearly past-tense).

        Coordination / relationship: this OVERLAPS stash 60B8564F (plugin-off-npm migration) task 2, which also updates these docs. Triage should either fold this into 60B8564F or run it first as a standalone accuracy fix. Caveat: until 60B8564F changes the plugin resolver, plugin/.mcp.json still launches via `npx @backlogit/backlogit-mcp`; docs should describe the currently-valid user-facing install paths accurately and not over-promise a not-yet-built resolver.
id: 091.002-T
parent_id: 091-F
priority: medium
status: archived
title: Remove user-actionable npm install docs
updated_at: 2026-07-10T19:40:38.0279967-07:00
---

Remove npm install user instructions from README and docs/plugin-guide, replacing them with valid install paths while preserving the factual current plugin npx bootstrap description.