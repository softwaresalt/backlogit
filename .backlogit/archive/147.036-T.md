---
archived_from: .backlogit/queue/147.036-T.md
archived_status: done
artifact_type: task
commit: a93425a3
created_at: 2026-08-25T16:56:12.0374605Z
dependencies:
    - 147.035-T
id: 147.036-T
labels:
    - checkpoint
    - events
    - plan-u13
    - seam
    - harness-exempt
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: archived
title: 'U13: guarded rewrite seam implementation'
updated_at: 2026-08-28T08:35:53.6044313Z
---

Plan unit **U13** (partition 2). Implements the preconditions 147.035-T / U12 asserts.

Files: `internal/events/checkpoint_rewrite.go`.

Exact order: `validateCheckpointFilename` -> `ensurePathContained` -> `os.ReadFile` -> `ParseCheckpoint` -> `ValidateCheckpoint` -> `CheckConformingTopLevelNamespace` -> `mutate` -> `jsonutil.MarshalReadable` -> `syncWriteFileAtomic`. Any precondition failure returns the raw verdict error **before** any marshal or write.

This task lands no new tests: 147.035-T / U12 owns the contract. Its verification command is U12''s selector, which must go from three failures to zero.

**Test-lifecycle classification**: `harness-exempt: covered-by 147.035-T / U12`. This task scaffolds no test function of its own; it is the implementation half of U12''s red gate, and U12''s three failing functions must go from three failures to zero. No green-step guards.

**Why this exemption is valid (P-002.1).** This is the plan''s single behaviour-changing exemption and the explicitly allowed `covered-by` carve-out. The named harness owner `147.035-T` / U12 exists in this release unit, is a declared dependency of this task, and must carry `harness-ready` at claim time — eligible only after harness generation runs — rather than an exemption. **Cycle-23 correction — the owner conditions are split across two gates.** Ship''s Step 2a static intake validates only what is knowable before harness generation: the owner exists, is a declared dependency, is not itself exempt, and `harness_owner_command` is exact. The owner''s red evidence (`harness-ready` applied, `Compilation: PASS`, `Red Phase: CONFIRMED`, harness commit landed) is re-evaluated at Ship **Step 4.1a**, immediately before this task is claimed. Checking it at intake was a deadlock: intake runs before `harness-architect` scaffolds U12, so the evidence cannot exist yet and the shipment halts on its own gate. If any claim-time condition fails, halt with `EXEMPT_OWNER_NOT_RED` rather than scaffolding a substitute harness here.

Verification: see the harness exemption contract below.

**Harness exemption contract (P-002.1 / P-002.3, machine-readable).** Static intake reads these
keys; Ship Step 4.1a runs `exempt_verification_command` once against the pre-work tree and MUST
observe it fail before this task may be claimed.

<!-- BEGIN:harness-exemption-contract -->

```text
harness_exemption_class: covered-by
harness_exemption_reason: This is the implementation half of 147.035-T / U12's red gate; U12 owns the three failing seam-contract functions and this task turns them green, so scaffolding a second harness here would duplicate the owner's contract.
harness_owner: 147.035-T
exempt_verification_command: pwsh -NoProfile -Command 'if (-not (Test-Path "internal/events/checkpoint_rewrite.go")) { exit 1 }; $t = Get-Content "internal/events/checkpoint_rewrite.go" -Raw; foreach ($p in @("validateCheckpointFilename","ensurePathContained","ParseCheckpoint","ValidateCheckpoint","CheckConformingTopLevelNamespace","syncWriteFileAtomic")) { if ($t -notmatch [regex]::Escape($p)) { exit 1 } }; $o = go test -count=1 -v -run "^TestU12_" ./internal/events 2>&1; if ($LASTEXITCODE -ne 0 -or ($o -match "no tests to run") -or (@($o | Select-String -Pattern "^--- PASS: TestU12_").Count -lt 3)) { exit 1 }; Write-Output 'EXEMPT_VERIFY_OK:147.036-T'; exit 0'
exempt_precondition: must-fail-before-deliverable
harness_owner_command: pwsh -NoProfile -Command '$o = go test -count=1 -v -run "^TestU12_" ./internal/events 2>&1; if ($LASTEXITCODE -ne 0 -or ($o -match "no tests to run") -or (@($o | Select-String -Pattern "^--- PASS: TestU12_").Count -lt 3)) { exit 1 }; exit 0'
```

<!-- END:harness-exemption-contract -->

**Loop command versus gate command.** `harness_owner_command` is what `build-feature` drives from
red to green — U12''s three failing functions are the specification, and this task must not add,
edit, or weaken them. `exempt_verification_command` is the completion gate: it additionally pins
this task''s **own** final deliverable by requiring the seam file to exist and to name every
precondition in the declared order''s call set, so a green U12 obtained by any route other than the
guarded seam does not satisfy it.

**Baseline probe (cycle 23, worktree HEAD `e8b974e`)**: exit **1**, as declared —
`internal/events/checkpoint_rewrite.go` does not exist and no `TestU12_` function has been
scaffolded (the selector returns `[no tests to run]` at exit 0, which P-002.3 classifies as
failure).

**Class delta surface (P-002.4)**: `internal/events/checkpoint_rewrite.go` only. Adding or
modifying **any** `*_test.go` file under this class is `EXEMPT_DELTA_EXCEEDS_CLASS` — the owner
owns the harness.

<!-- BEGIN:acceptance-criteria -->
- `RewriteCheckpointFile` enforces its declared precondition order exactly — `validateCheckpointFilename` → `ensurePathContained` → `ParseCheckpoint` → `ValidateCheckpoint` → `CheckConformingTopLevelNamespace` → `mutate` → `jsonutil.MarshalReadable` → `syncWriteFileAtomic` — returning the raw verdict error before any marshal or write on the first failing precondition
- `147.035-T` / U12's three scaffolded `TestU12_` functions go from three failures to zero; this task scaffolds no test function of its own
- Red harness: none. This task is `harness-exempt: covered-by`, owner `147.035-T` / U12; U12 owns the failing seam-contract harness this task turns green
- Green-step guards: none
- Width: 1 file, 3 scenarios owned by `147.035-T` / U12 turned from red to green; no new test file
<!-- END:acceptance-criteria -->