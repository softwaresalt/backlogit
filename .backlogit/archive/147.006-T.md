---
archived_from: .backlogit/queue/147.006-T.md
archived_status: done
artifact_type: task
commit: ea2cab4c
created_at: 2026-08-24T19:02:04.4826319Z
dependencies:
    - 147.004-T
    - 147.037-T
    - 147.016-T
id: 147.006-T
labels:
    - checkpoint
    - events
    - plan-u3
    - harness-ready
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: archived
title: 'U3: ResolveCheckpoint validity gate'
updated_at: 2026-08-28T08:18:43.1816351Z
---

Plan unit **U3**. Add the missing validity gate to `ResolveCheckpoint`.

Files: `internal/events/checkpoint_lifecycle.go`, `internal/events/checkpoint_lifecycle_test.go`.

`ResolveCheckpoint` (`:139-184`) currently has **no** validity gate. On a legacy document it replaces the whole file with a fabricated skeleton, destroying all evidence. Both Stage and Ship session-start recovery call it.

After `ParseCheckpoint`, the already-resolved short-circuit, and the `ErrCheckpointCannotResolveAbandoned` guard — all three keep their current positions — add `ValidateCheckpoint(cp)` refusing with `fmt.Errorf("%w: %w", backlogiterrors.ErrCheckpointUseQuarantine, valErr)`. **Multi-`%w`, not `%v`** — `%v` would drop `ErrCheckpointInvalid`. The gate does not write; the file stays byte-identical on refusal.

<!-- BEGIN:acceptance-criteria -->
- A legacy-shaped document is refused and **both** `errors.Is(err, ErrCheckpointUseQuarantine)` and `errors.Is(err, ErrCheckpointInvalid)` hold, with the file SHA compared before and after
- A conforming active document still resolves
- An already-resolved conforming document is still an idempotent no-op
- Red harness (P-002/P-004 gate): one scaffolded `TestU3_` function (case 1), which fails because resolve currently succeeds and rewrites
- Green-step guards (`TestU3Guard_*`, landing with the implementation): cases 2 and 3, which pin the shipped accept and idempotent-no-op paths
<!-- END:acceptance-criteria -->