---
archived_from: .backlogit/queue/174.071-T.md
archived_status: done
artifact_type: task
commit: cfe37339
created_at: 2026-09-24T07:02:01.3596834Z
id: 174.071-T
parent_id: 174-F
priority: high
status: archived
title: Add test-only NewWorkspaceWithoutRecoveryForTest seam for external core_test fixture
updated_at: 2026-09-24T07:23:27.7928151Z
---

Corrective work unit (Wave 17A) — test-only Go fixture seam isolating the broad external core_test setupTestWorkspace from the recovery-enabled workspace constructor. Root cause: setupTestWorkspace (internal/core/artifacts_expansion_test.go:24, 100+ callers, unique temp roots) invokes recovery-enabled core.NewWorkspace; a newly-started hierarchy test hangs inside Windows FindFirstFile/EvalSymlinks during synthetic global-lock root canonicalization (no lock waiter/holder cycle, no shared-root contamination, no journal scan reached). P-021 C1 OUT OF SCOPE for all completed 174-F tasks incl. 174.069-T/174.070-T: distinct Go test-infrastructure seam (the recovery-enabled constructor), not same-contract completion. Test-only; NO production default/lock/recovery change. Source stash 885263D2; deliberation 070-DL.

<!-- BEGIN:acceptance-criteria -->
AC1 (Test-only seam): add a NEW package-core _test.go file exporting NewWorkspaceWithoutRecoveryForTest(ctx, root) (*Workspace, error) that delegates to the existing unexported newWorkspace(ctx, root, false). It MUST NOT duplicate/fork newWorkspace body and MUST NOT be added to a non-test file. Use the unqualified *Workspace return type (the file is package core).
AC2 (External fixture switch — enumerated): switch the recovery-agnostic external core_test fixtures to the new seam. The exact set to convert is enumerated (not open-ended): setupTestWorkspace (internal/core/artifacts_expansion_test.go:24) and setupTestWorkspaceWithBugLevel; before switching, confirm by inspection that NO recovery/journal-exercising test transitively obtains its workspace through a converted fixture (if one does, it MUST keep explicit core.NewWorkspace and be excluded from the switch). Do not convert any fixture outside internal/core/artifacts_expansion_test.go without adding it to this enumerated set and re-confirming.
AC3 (Preserve recovery coverage): every test that intentionally exercises recovery/journal behavior MUST retain explicit core.NewWorkspace construction; no such test loses recovery coverage. The implementer records the concrete list of recovery-exercising tests confirmed to still use NewWorkspace.
AC4 (No production change): do NOT modify NewWorkspace, newWorkspace, NewDiagnosticWorkspace, the autoRecover default, locking, or recovery logic; do NOT repurpose production NewDiagnosticWorkspace as the seam. File scope = the new package-core _test.go seam file + internal/core/artifacts_expansion_test.go (plus any additional _test.go file holding an enumerated sibling fixture per AC2). No production .go file changes.
AC5 (RED-before / GREEN-after, observable): the RED-before state is a Windows hang/timeout in the hierarchy/expansion fixture; the test author MUST make RED observable via a bounded per-test/context timeout reproduction (not merely the package deadline) and name the concrete targeted selector(s) run (hierarchy/expansion tests through the converted fixture + at least one recovery selector proving direct NewWorkspace still recovers/rejects a pending journal). Targeted selectors GREEN after the seam switch.
AC6 (Circuit / full-suite disposition): shared with 174.072-T — do NOT run go test ./... in this task; after BOTH corrective tasks land, one NEW separately-authorized full-suite run (explicit operator authorization immediately before), then the 155-S zero-P0/P1 stop-gate and final standard + adversarial review over the changed surfaces.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Signatures: setupTestWorkspace @ internal/core/artifacts_expansion_test.go:24 -> core.NewWorkspace(ctx,root); core.NewWorkspace @ workspace.go:131 -> newWorkspace(ctx,root,true); NewDiagnosticWorkspace @ :138 -> newWorkspace(ctx,root,false) (DO NOT repurpose); newWorkspace @ :142 (unexported, autoRecover bool). The test seam must live in package core (a _test.go there can call unexported newWorkspace) and be consumed by the external core_test package fixture. Membership: governed member of active 155-S; no blocks edge (membership-as-gate); provenance informational. Domain: Go test infrastructure.
<!-- END:implementation-notes -->