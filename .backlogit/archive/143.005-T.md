---
artifact_type: task
commit: 7401e69a
created_at: 2026-08-18T00:56:36.9394214Z
id: 143.005-T
labels:
    - doctor
    - audit-log
    - tests
    - red-harness
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: done
title: Add RED harness for the doctor reconciliation audit
updated_at: 2026-08-18T06:21:35.0347197Z
---

# Add RED harness for the doctor reconciliation audit

## Description

<!-- BEGIN:description -->
Unit 5, RED harness, first task on the detection track. NO check registration and NO check body. Add `internal/core/doctor_shipped_event_test.go` with two scenarios, BOTH RED, plus the declarations those tests need to compile: `FindingMissingShippedEvent DoctorFindingType = missing_shipped_event`, `FindingShippedUnarchivedResidue DoctorFindingType = shipped_unarchived_residue`, and `DoctorOptions.CheckShippedEventCompleteness bool`, off by default. Do NOT add an inert check registration: the absence of any registration is exactly what makes the harness fail for the right reason, because the option is set, the scan runs, and zero findings come back. `DoctorFinding` keeps its current shape (`internal/core/doctor.go:115-119`): there is no structured detail field and none is added, so finding detail is carried in `Description` as the existing findings do and the scenarios assert on `Description` content. This is the compile-then-fail pattern `.github/skills/harness-architect/SKILL.md` Step 4.3 prescribes, not a scaffold task. Scenario 1: an archived shipment with `archived_status: shipped` and no shipped event yields exactly one `missing_shipped_event` finding; the same fixture with the event present yields none; and an archived shipment whose `archived_status` is NOT shipped (for example abandoned) yields none, proving the check filters on `archived_status` rather than on archived. Scenario 2: a `status: shipped` unarchived shipment yields exactly one `shipped_unarchived_residue` finding whose `Description` records whether the shipped event is present and enumerates the archive candidates left behind alongside it; the scenario asserts the ACTUALLY RESOLVED artifact path produced by the routing registry for the shipped status rather than assuming `queue/`. The non-mutation guard that asserts byte-identical `.backlogit/logs` is green from the moment a check exists, so it lives in 143.006-T rather than here; doctor exit-code neutrality is asserted in 143.008-T because `core.Doctor` returns a report and an error, not an exit code. Execution posture: test-first (red phase).
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- The package compiles: `go test -run=^$ -count=1 ./internal/core/` succeeds.
- BOTH scenarios fail on this tree because no check is registered; if either passes, halt, the declarations carried behavior they were not supposed to carry and the `harness-ready` label cannot be applied.
- Scenario 1 includes the negative subtest for an archived shipment whose `archived_status` is not shipped.
- Scenario 2 asserts on `DoctorFinding.Description` content; `DoctorFinding` keeps its current shape and gains no detail field.
- The only production edits are two finding-type constants and one `DoctorOptions` field, all off by default and unreferenced by production code on this tree.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/core/doctor_shipped_event_test.go` (new), `internal/core/doctor.go` (two constants and one option field). `.backlogit/registry.yaml` routes neither status set to shipped, so the resolved path must be observed, not assumed. Verify with the named `-run` selector, not the whole package: the sibling durability harness 143.001-T may legitimately be red in `internal/core` at the same time.
<!-- END:implementation-notes -->
