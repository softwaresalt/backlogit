---
artifact_type: task
commit: 59269785c502ec268a9eaaf11c6e70b76e3cee0a
created_at: 2026-07-25T22:35:00.4361608Z
dependencies:
    - 126.001-T
id: 126.005-T
labels:
    - docs
    - policy
    - markdownlint
    - p-008
parent_id: 126-F
priority: medium
status: done
title: 'U5: Reconcile P-008 wording with the repo-wide _title-configured make md-lint gate'
updated_at: 2026-07-26T07:43:43.6919308Z
---

Domain: docs/policy. Reconcile policy P-008 (Markdown Conformance) in `.github/policies/workflow-policies.md` (~lines 159-182) with the provisioned REPO-WIDE reality from U1-U4. Four edits.

(a) STATEMENT: name the active rule set (MD001/MD025/MD041) and state that a file's title MAY be expressed as a leading `# H1` OR a frontmatter `title:` (MD041 credits the frontmatter title). Document that MD025 is configured with `front_matter_title` retargeted to a non-existent `_title` key so a frontmatter `title:` is NOT counted as a heading — a legitimate frontmatter `title:` + body `# H1` therefore does not double-count as two top-level headings.

(b) PRECONDITION: rewrite the current precondition (which wrongly asserts in present tense that the config already exists) to state that the workspace PROVIDES `.markdownlint.json` enabling exactly MD001/MD025/MD041 (`default:false`, MD025 `front_matter_title` = `^\s*_title\s*[:=]`), executed via the pinned `markdownlint-cli2@0.23.1` and reproducible with `make md-lint`.

(c) POSTCONDITION: replace the literal `markdownlint "**/*.md"` exits-0 postcondition with `make md-lint` exiting 0 with no violations anywhere in the tracked Markdown corpus (repo-wide). The universal "all markdown" statement is now genuinely TRUE — no scope/subset compromise is needed (that was the superseded Option-B artifact).

(d) VIOLATION ACTION: halt; fix the heading-hierarchy violation and re-run `make md-lint` before committing; do not suppress or disable the rules.

Acceptance criteria: (1) P-008 no longer claims a pre-existing config; it describes the U1-provisioned `.markdownlint.json` and `make md-lint`. (2) The postcondition describes repo-wide `make md-lint` exit-0, NOT a subset/scoped gate. (3) Exactly MD001/MD025/MD041 remain named; no rule added or removed. (4) The MD025 `_title` front_matter_title rationale is documented.

Verification: the file-specific markdown verification for `.github/policies/workflow-policies.md` MUST use `make md-lint` (the new gate) — docline EXCLUDES the entire `.github/` subtree (`internal/docline/policy.go` scopeExcludeDirs), so `make docs-lint` does NOT lint this file and would be a no-op verifier; the repo-wide md-lint gate covers `.github/policies/**` and exits 0. Depends on U1 (126.001-T).