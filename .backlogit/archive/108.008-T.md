---
archived_from: .backlogit/queue/108.008-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-18T17:15:26.4421541-07:00
custom_fields:
    harness_status: passing
dependencies:
    - 108.002-T
    - 108.003-T
id: 108.008-T
labels:
    - size
    - parity
    - cli
    - mcp
    - harness-ready
parent_id: 108-F
priority: medium
status: archived
title: 'SE-6: CLI/MCP read projection parity for size (cli+mcp)'
updated_at: 2026-07-19T20:27:10.885763Z
---

SE-6: CLI/MCP read projection parity for size (interface). Ensure custom_fields.size/provenance projects IDENTICALLY on the read pairs get<->get_item, queue view<->get_queue, shipment get<->get_shipment, shipment list<->list_shipments (size/provenance ride on custom_fields, which both transports already emit). Expose the SE-4 derived composition on the MCP read surfaces (get_item/get_shipment/get_queue) as a clearly non-persisted field. Parity-scope correction (Copilot G5): the derived COMPOSITION is exposed on MCP read surfaces ONLY in 099-S; CLI get JSON (internal/cli/get.go buildDetailMap) and queue JSON (internal/cli/queue_cmd.go / QueryQueue) are SEPARATE shapers that do not carry the composition, so this task does NOT claim CLI-JSON composition parity. The CLI-JSON composition gap is tracked as stash 387DE4BF. The cosmetic CLI human-column gap (list/shipment list/queue view table columns omit size) is separately tracked as stash D5FA1EE9. Both deferrals keep SE-6 within the 2-hour envelope. CLI/MCP size/provenance parity requires NO new CLI production code (Copilot cycle-3 H4): CLI get JSON already projects custom_fields.size verbatim -- internal/cli/get.go buildDetailMap copies the ENTIRE frontmatter map (including custom_fields) into the detail map, so size/provenance rides free on `get --json` with no code change; the cosmetic human-column table omission is the separately-tracked D5FA1EE9 gap. The single production change in SE-6 is therefore MCP-only: expose the derived composition on the MCP read surfaces in internal/mcp/tools.go. Tests: size/provenance projects identically on each named CLI/MCP read pair (proving the no-code-change CLI parity); get_item JSON exposes the derived composition on a sized feature; CLI --json exposes custom_fields.size (size/provenance parity), NOT composition. Depends on 108.002-T (SE-3a) and 108.003-T (SE-4). Files: internal/mcp/tools.go (single production file) plus parity tests.

<!-- BEGIN:implementation-notes -->
Harness (red): go test ./internal/mcp/ -run TestSE6MCPReadProjectionSizeCompositionHarness
<!-- END:implementation-notes -->