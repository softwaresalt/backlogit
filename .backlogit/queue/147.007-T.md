---
artifact_type: task
created_at: 2026-08-24T19:02:23.3342979Z
dependencies:
    - 147.006-T
    - 147.011-T
    - 147.021-T
id: 147.007-T
labels:
    - checkpoint
    - events
    - plan-u3b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U3b: ResolveCheckpoint conformance gate and named already-resolved exception'
updated_at: 2026-08-24T19:40:00.0000000Z
---

Plan unit **U3b**. Conformance gate on `ResolveCheckpoint` plus the named already-resolved exception.

Files: `internal/events/checkpoint_lifecycle.go`, `internal/events/checkpoint_lifecycle_test.go`.

After the U3 validity gate, add `CheckConformingTopLevelNamespace(data)` returning its typed error unchanged, then mutate and write.

**Named, accepted residual**: a document whose `status` is already `"resolved"` returns `nil` at the step-5 short-circuit and never reaches either new gate. This is deliberate — moving the gates ahead of it would turn a shipped idempotent no-op into a new error — but it means the exact fabricated skeletons produced by the *pre-fix* `ResolveCheckpoint` bypass both gates. Their discovery path is U6/U6b, not resolve.

**Granularity check before starting**: acceptance criterion 2 asserts across the U3b gate *and* the U6 discovery path. Count the functions modified across `checkpoint_lifecycle.go` and `checkpoint_lifecycle_test.go` for U3b combined with any still-open U6 work; if the total reaches 5, split the cross-unit assertion into a separate task rather than breaching the 2-Hour Rule.

<!-- BEGIN:acceptance-criteria -->
- A valid-but-non-conforming document is refused with `ErrCheckpointNonConforming` naming the keys, bytes unchanged
- An **invalid, already-resolved** document returns `nil` with bytes unchanged **and** U6 flags the same file `NeedsQuarantine: true` — both halves in one test so neither can be removed alone
- A document with `disposition: "abandoned"` still returns `ErrCheckpointCannotResolveAbandoned`
- Expected red: cases 1 and 2 fail
<!-- END:acceptance-criteria -->