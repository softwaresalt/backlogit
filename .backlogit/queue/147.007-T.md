---
artifact_type: task
created_at: 2026-08-24T19:02:23.3342979Z
dependencies:
    - 147.006-T
    - 147.011-T
    - 147.028-T
    - 147.037-T
    - 147.016-T
id: 147.007-T
labels:
    - checkpoint
    - events
    - plan-u3b
    - harness-exempt
    - merge-gate-dependent
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: active
title: 'U3b: resolve-verb conformance contract and named already-resolved residual'
updated_at: 2026-08-27T22:22:58.3720398Z
---

Plan unit **U3b** (partition 4). Verb-level contract pin for the resolve surface. **No production change.**

**HARD MERGE GATE backreference.** This task is one of the three units the `147.018-T` / U9b hard
merge gate constrains. A pull request containing this task MUST NOT merge unless the
`.github/instructions/backlogit.instructions.md` delta from `147.018-T` is present in the same
merge commit. The gate is owned and stated in `147.018-T` and in the plan's U9b
"Acceptance / merge gate" bullet; this backreference exists so the constraint is discoverable from
here. It constrains implementation units to each other inside one pull request and adds **no**
dependency edge — executable ordering is unchanged.

Files: `internal/events/checkpoint_lifecycle_conformance_guard_test.go` (new). Deliberately **not** `checkpoint_lifecycle_conformance_test.go` — that file is created earlier in the execution order by `147.042-T` / U3c, and `build-feature` Step 5's `verification-only` exception admits only *new* files a task names. A distinct guard file keeps this task inside the narrow exception instead of requiring an append to a pre-existing test file.

**Cycle-20 rewrite (Group-B P1).** Cycle 17 gave this task a production delta - "after the U3 validity gate, add `CheckConformingTopLevelNamespace(data)`" - that `147.037-T` / U14 had already made redundant. Once `ResolveCheckpoint` routes through `RewriteCheckpointFile`, the seam''s U13 implementation returns the raw `*CheckpointNonConformingError` before any marshal or write, and that error unwraps to `ErrCheckpointNonConforming`. A second conformance call inside the verb changes no observable behaviour and produces the same error. The delta is **withdrawn**, and with it the claim that this task carries a red assertion.

The conformance-refusal harness that the contract genuinely needs is **`147.042-T` / U3c**, which lands before `147.037-T` and fails against the pre-migration verb. What survives here is the verb-level contract pin, recorded as green validation after U14 and U6 have landed.

**Named, accepted residual (unchanged)**: a document whose `status` is already `"resolved"` returns `nil` at the step-5 short-circuit and never reaches the seam. This is deliberate - a non-writing terminal answer, and moving the gate ahead of it would turn a shipped idempotent no-op into a new error - but it means the exact fabricated skeletons produced by the *pre-fix* `ResolveCheckpoint` bypass the gate. Their discovery path is U6, not resolve.

**Test-lifecycle classification**: `harness-exempt: verification-only`. No harness test function is scaffolded. Both scenarios assert shipped behaviour once the prerequisites have landed, and they are committed as `TestU3bGuard_*` functions in this task''s own commit.

Verification: see the harness exemption contract below.

**Harness exemption contract (P-002.1 / P-002.3, machine-readable).** Static intake reads these
keys; Ship Step 4.1a runs `exempt_verification_command` once against the pre-work tree and MUST
observe it fail before this task may be claimed.

<!-- BEGIN:harness-exemption-contract -->

```text
harness_exemption_class: verification-only
harness_exemption_reason: There is no production change; both scenarios pin shipped behaviour delivered by 147.037-T / U14 and 147.011-T / U6, so neither can be red on this task's own delta.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command 'if (-not (Test-Path "internal/events/checkpoint_lifecycle_conformance_guard_test.go")) { exit 1 }; $o = go test -count=1 -v -run "^TestU3bGuard_" ./internal/events 2>&1; if ($LASTEXITCODE -ne 0 -or ($o -match "no tests to run") -or (@($o | Select-String -Pattern "^--- PASS: TestU3bGuard_").Count -lt 2)) { exit 1 }; Write-Output 'EXEMPT_VERIFY_OK:147.007-T'; exit 0'
exempt_precondition: must-fail-before-deliverable
```

<!-- END:harness-exemption-contract -->

**Why this command is a gate.** `go test -count=1 -run "^TestU3bGuard_" ./internal/events` on its
own exits **0** with `[no tests to run]` against the pre-work tree — the exact false green P-002.3
forbids. The command therefore requires the named file to exist and requires two named
`--- PASS: TestU3bGuard_` lines, so it cannot pass until both guards exist and pass.

**Baseline probe (cycle 23, worktree HEAD `e8b974e`)**: exit **1**, as declared. Cycle 26 renamed
the named file to `internal/events/checkpoint_lifecycle_conformance_guard_test.go`; the probe still
exits **1** against the pre-work tree, now on the guard file's absence rather than on the U3c
harness file's absence. Either way the `--- PASS: TestU3bGuard_` count is the binding half of the
gate, so the probe cannot false-green once `147.042-T` has created its own conformance file.

**Class delta surface (P-002.4)**: the new
`internal/events/checkpoint_lifecycle_conformance_guard_test.go` only. Zero non-test `*.go` files
may change; one that does is `EXEMPT_BEHAVIOR_NO_OWNER`. Creating this new test file is the narrow
`verification-only` exception in `build-feature` Step 5 — weakening, skipping, or renaming the
selector of any pre-existing assertion remains forbidden, and this task must not append to
`147.042-T`'s `checkpoint_lifecycle_conformance_test.go`.

<!-- BEGIN:acceptance-criteria -->
- An **invalid, already-resolved** document returns `nil` with bytes unchanged **and** U6 flags the same file `NeedsQuarantine: true` - both halves in one test so neither can be removed alone
- A document with `disposition: "abandoned"` still returns `ErrCheckpointCannotResolveAbandoned`
- Red harness: none. This task is `harness-exempt: verification-only`; the resolve-verb conformance RED lives in `147.042-T` / U3c and is turned green by `147.037-T` / U14
- Green-step guards (`TestU3bGuard_*`): the two scenarios above, landing after `147.037-T` and `147.011-T`
- Width: 1 file, 2 scenarios, no production change
<!-- END:acceptance-criteria -->