---
artifact_type: task
created_at: 2026-07-15T06:41:28.4831586Z
dependencies:
    - 108.001-T
    - 108.010-T
id: 108.002-T
labels:
    - size
    - provenance
    - core
parent_id: 108-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: 'SE-3a: Persist size provenance + estimate-history event (core)'
updated_at: 2026-07-18T18:27:27.735728-07:00
---

Introduce a NEW typed presence-aware entry point SetArtifactSizeWithProvenance(ctx, ws, id, SizeMutation{Size, Source *string, RulesetVersion *string, Actor ActorContext, OpID *string}) and KEEP the existing positional SetArtifactSize(ctx, ws, id, size string) as a THIN COMPATIBILITY WRAPPER that builds a SizeMutation (no provenance, actor from context) and delegates - so the repo stays buildable at every commit (Copilot F2). Existing callers (internal/cli/update.go:103, internal/mcp/tools.go:751, core tests) keep compiling on the wrapper; SE-5 migrates them to the typed path and removes the wrapper. Validate size_source and bounded size_ruleset_version at the seam (ErrValidation). Actor-context stamping: CLI=human, agent/MCP=agent; absent-on-read stays unknown, never rewritten. Sole-writer integrity for reserved sizing keys (size,size_source,size_ruleset_version,size_op_id): generic update carrying custom_fields merge-not-replaces them (closes updateArtifactUngated whole-map-replace hazard, artifacts.go:542-544); generic create rejects/strips them so an initial size is only authorable via the typed seam (never eventless) - a minimal targeted guard, NOT an emitter refactor. Append estimate-history event (new EventType constant, generic events.Event) event-before-write, fail-closed. Files: internal/core/artifact_size.go, internal/events/stream.go, minimal reserved-key guard in internal/core/artifacts.go. Depends on SE-1 (schema) and SE-7b (108.010-T lookup-time containment). <=2h, core persistence. See impl-plan SE-3a. NOTE: no codec bridge - custom_fields is the spike-selected durable carrier.