---
artifact_type: task
created_at: 2026-07-15T06:41:28.4831586Z
dependencies:
    - 108.001-T
id: 108.002-T
labels:
    - spike
    - investigate-first
    - size-estimation
parent_id: 108-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: Investigate size mutation/provenance/JSONL history durability, failure ordering, and containment boundary (2h max)
updated_at: 2026-07-15T17:51:35.1269894Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. Depends on the 108.001-T surface inventory. Investigate the durability questions a future typed provenance seam would have to answer: (1) core.SetArtifactSize accepts no provenance inputs and intentionally emits no mutation-hook event, so size_ruleset_version-only changes are silently untracked today; (2) item history is append-only JSONL via events.AppendEvent, best-effort warn-on-failure (internal/core/commits.go), which is a separate durability surface from YAML frontmatter provenance; (3) what write/append ordering and failure contract (fail-closed vs rollback) would guarantee exactly one history event per persisted provenance change, noting the overlap with the formal-gate spike's open partial-core-mutation-rollback question; (4) the UNRESOLVED workspace-containment boundary: SetArtifactSize resolves paths via FindArtifactPath (artifactSearchDirs + filepath.WalkDir) and writes via atomicfile.WriteFileAtomic WITHOUT calling SafeResolve, so size writes are NOT proven root-contained. Deliverable: durability/ordering findings and the named open questions (including the containment-seam question) as evidence, not decisions. No implementation.