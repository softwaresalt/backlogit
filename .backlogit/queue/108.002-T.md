---
artifact_type: task
created_at: 2026-07-15T06:41:28.4831586Z
custom_fields:
    harness_status: scaffolded
dependencies:
    - 108.001-T
    - 108.010-T
id: 108.002-T
labels:
    - size
    - provenance
    - core
    - harness-ready
parent_id: 108-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: 'SE-3a: Persist size provenance + estimate-history event (core)'
updated_at: 2026-07-19T19:36:52.2486683Z
---

Introduce a NEW typed presence-aware entry point SetArtifactSizeWithProvenance(ctx, ws, id, SizeMutation{Size, Source *string, RulesetVersion *string, Actor ActorContext}) and KEEP the existing positional SetArtifactSize(ctx, ws, id, size string) as a THIN COMPATIBILITY WRAPPER that builds a SizeMutation (no provenance, actor from context) and delegates - so the repo stays buildable at every commit (Copilot F2). Existing callers (internal/cli/update.go:103, internal/mcp/tools.go:751, core tests) keep compiling on the wrapper; SE-5 migrates them to the typed path and removes the wrapper. Validate size_source and bounded size_ruleset_version at the seam (ErrValidation). Actor-context stamping: CLI=human, agent/MCP=agent; absent-on-read stays unknown, never rewritten. Sole-writer integrity for reserved sizing keys (size,size_source,size_ruleset_version): a generic update carrying custom_fields merge-not-replaces them (closes updateArtifactUngated whole-map-replace hazard, artifacts.go:542-544). Generic create is MIGRATION-SAFE (Copilot G7): it PRESERVES an imported size when accompanied by provenance -- routing it through the size seam so the estimate-history event is recorded -- and rejects/strips only an UNPROVENANCED reserved size, so an initial size is never eventless (protects Protected invariant #1) while migration/import via cli/migrate.go (WithFields->CreateArtifact) neither loses nor fails on an already-sized item. Minimal targeted guard, NOT an emitter refactor. Append estimate-history event (new EventType constant, generic events.Event) event-before-write, fail-closed. Tests add: importing an already-sized item (migration path) preserves size + provenance and records the event. Files: internal/core/artifact_size.go, internal/events/stream.go, minimal reserved-key guard in internal/core/artifacts.go. Depends on SE-1 (schema) and SE-7b (108.010-T lookup-time containment). <=2h, core persistence. See impl-plan SE-3a. NOTE: no codec bridge - custom_fields is the spike-selected durable carrier.

<!-- BEGIN:implementation-notes -->
Harness (red): go test ./internal/core/ -run 'TestSE3aSetArtifactSizeWithProvenanceHarness|TestSE3aReservedSizingKeyGuardHarness'
<!-- END:implementation-notes -->