---
artifact_type: task
created_at: 2026-08-24T19:04:44.437763Z
dependencies:
    - 147.013-T
    - 147.030-T
    - 147.016-T
    - 147.031-T
id: 147.015-T
labels:
    - checkpoint
    - cli
    - plan-u8
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: active
title: 'U8: CLI surfaces the refusal with bounded offender keys'
updated_at: 2026-08-27T22:23:45.4315756Z
---

Plan unit **U8** (partition 4). CLI refusal surfacing with bounded offender keys.

Files: `internal/cli/checkpoint.go`, `internal/cli/checkpoint_test.go`.

**Cycle-17 change (gate finding H1).** This task no longer renders an executable remediation command. It surfaces the refusals on `backlogit checkpoint resolve` and `backlogit checkpoint abandon` as actionable operator messages that name the offending keys through **147.031-T / U1c''s `FieldPathsForDisplay()`** and state the required disposition verb read from the structured `RemediationIntent`. The bound, approval-gated command block printed underneath is owned by **147.039-T / U16**, which binds it to the resolved storage root and attaches the A4c approval, preimage, and no-clobber lines.

The offending-key list is only available for a valid-but-non-conforming target. A schema-invalid legacy document is refused by the 147.006-T / U3 validity gate before conformance runs, so no key list exists for it; that refusal prints the validation reason and the required verb instead.

Not in this task: the `checkpoint get` conformance projection (147.027-T / U8c - do not touch `newCheckpointGetCmd`); a JSON error envelope (stash `63E810D9`); a CLI assertion for `checkpoint resolve` on an already-abandoned document (stash `DBBA62AA`).

Red verification: `go test -count=1 -run '^TestU8_' ./internal/cli`.

<!-- BEGIN:acceptance-criteria -->
- `checkpoint resolve` on a schema-invalid legacy document exits non-zero, reports the `checkpoint_use_quarantine` class with the validation reason, names `quarantine` as the required verb, and prints **no** key list
- `checkpoint resolve` on a valid-but-non-conforming document exits non-zero and names the offending top-level keys in quoted, bounded form rendered from `FieldPathsForDisplay()`
- `checkpoint abandon` on that same document does likewise
- No case asserts a paste-runnable command; that assertion belongs to 147.039-T / U16
- Red harness (P-002/P-004 gate): one scaffolded `TestU8_` group covering all three cases, all failing
- Green-step guards: none. Every scenario is a red harness function
<!-- END:acceptance-criteria -->