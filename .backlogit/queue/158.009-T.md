---
artifact_type: task
created_at: 2026-09-11T01:29:16.7862206Z
custom_fields:
    harness_status: scaffolded
dependencies:
    - 158.001-T
id: 158.009-T
labels:
    - harness-ready
parent_id: 158-F
priority: high
status: queued
title: Compat corpus runner + adapters behavior (seq3 U1b)
updated_at: 2026-09-11T04:10:04.2667206Z
---

U1b (behavior; depends on 158.001-T declarations). Implement the deterministic corpus runner behavior and its decode-under-test adapters in internal/faultline/compatcorpus, owning ALL original 158.001-T behavior acceptance. Scope: Run() pure semantics (total sort by (EntryID,Adapter); adapter panic caught to a failing Result; twice-run byte-identical determinism); the three decode-under-test ParserAdapters (frontmatter, events_jsonl, scanner) whose declared strict-validation checks return the package sentinel errors; LoadCorpus/DefaultCorpus/DefaultAdapters; testdata/corpus fixtures + manifest.json (with a .gitattributes -text guard for CRLF/LF fixtures); sentinel outcomes asserted via errors.Is; Report + Report.JSON() under schema id compatcorpus.report/v1. No new exported declaration shape is introduced here (that is 158.001-T). Acceptance: Run(DefaultCorpus(),DefaultAdapters()) returns Failed==0 with every entry outcome matching its Expect/WantErr (errors.Is) / WantNormalized (bytes.Equal); Report.Deterministic==true; Report.JSON() validates the compatcorpus.report/v1 shape. Domain: tests. RED: go test ./internal/faultline/compatcorpus/ -run TestCorpusRunner -count=1 (fails until runner/adapters/fixtures implemented). GREEN: same command passes.

<!-- BEGIN:implementation-notes -->
Harness Manifest | Compilation: PASS (go test -run=^$ -count=1 ./...) | Red Phase: CONFIRMED | Harness Command: go test -count=1 -run '^TestCorpusRunner$' ./internal/faultline/compatcorpus | Red Evidence: TestCorpusRunner assertion failures report TODO implementation for DefaultCorpus and Run; fixtures gate strict sentinels, normalization, sorting, determinism, panic containment, and compatcorpus.report/v1.
<!-- END:implementation-notes -->