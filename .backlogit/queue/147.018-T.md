---
artifact_type: task
created_at: 2026-08-24T19:05:42.3137947Z
dependencies:
    - 147.017-T
id: 147.018-T
labels:
    - checkpoint
    - docs
    - harness
    - plan-u9b
    - merge-gate
    - harness-exempt
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U9b: agent instruction delta, quarantine-first disposition guidance (HARD MERGE GATE)'
updated_at: 2026-08-25T20:44:00Z
---

Plan unit **U9b**. Update `.github/instructions/backlogit.instructions.md` (requirement R10).

Files: `.github/instructions/backlogit.instructions.md`.

Granularity: one file, one skill domain (agent-facing documentation), no production functions, no test scenarios — the only gate is `go run ./cmd/backlogit --cwd . docs lint`. The acceptance list enumerates the required **content** of one instruction delta, not a scenario matrix.

**Rewritten in PR #377 cycle 16: this delta is quarantine-only.** Cycles 10-15 grew a two-entry-point hand-repair and post-quarantine restore runbook — rename archived evidence aside, copy preserved bytes back into the active directory, hand-edit JSON members in place, conditionally delete the restored copy on abort. The cycle-15 plan-review security lens rejected publishing it, and the rejection holds: that sequence performs rename, copy, in-place rewrite, and conditional delete against a directory this codebase does not open with real-root / no-follow semantics, with no handle-or-content compare-and-swap between the read that classified the file and the write that repairs it, and no adversarial coverage for a symlinked or concurrently-replaced path. This file carries `applyTo: '**'`, so publishing it as copy-and-run instructions would claim a safety property this release cannot guarantee.

**The runbook is withdrawn, not weakened.** It moves to stash `35A27CD0` together with the containment work that makes it safe: real-root / no-follow open, handle-or-content CAS, no-clobber destinations, and adversarial tests. Withholding it costs nothing this shipment needs — refusal plus verbatim-move is already a complete, reversible disposition. An operator may still make an explicit raw-token-aware survivor decision by hand, outside agent automation; this file simply stops claiming an automated path.

**What this task publishes.**

1. **The refusal contract.** `resolve` and `abandon` refuse a stored document that is schema-invalid or non-conforming; `quarantine` accepts it and moves the bytes verbatim into `archive/checkpoints/` with a `<filename>.disposition.json` sidecar. Within `status: "active"` exactly one verb accepts any given file.
2. **The offender-diagnosis step, with its exact source.** Offending members are read from `checkpoint get` — the `non_conforming_fields` projection declared by 147.012-T / U6b and surfaced by 147.022-T / U6c (MCP) and 147.027-T / U8c (CLI). It is atomic, per-file, and bounded. Explicitly forbid the two wrong sources: the decoded `checkpoint` object in the same payload, because the collapsing decode has already destroyed the duplicate members and they are not in it; and a whole-directory `checkpoint list` scan, which is neither atomic nor per-file.
3. **A diagnostic classification table.** Diagnostic, not a runbook — every row's disposition in this release is quarantine.

   | Reported offender | Meaning | Disposition in this release |
   |---|---|---|
   | `<key>` | a plain unmodeled top-level key | Quarantine. The class is theoretically relocatable, but automated relocation ships with `35A27CD0`. |
   | `duplicate:<key>` | two top-level members whose keys are `strings.EqualFold`-equal, at least one of which is modeled or collapses into a modeled slot | Quarantine. Any repair requires an explicit raw-token-aware operator selection of the surviving member, made outside agent automation. |
   | `duplicate:context.<key>` | an exact-duplicate `context` member, or a fold variant aliasing `shipment_id`, `feature_id`, `task_ids`, or `branch` (147.028-T / U2g) | Quarantine. Same operator-selection requirement. |
   | `duplicate:progress.<key>` | a duplicate or fold-variant member inside the modeled `progress` object (147.020-T / U2e) | Quarantine. No top-level container can relocate it. |

4. **The survivor-selection rule, with no spelling-based exception.** Every duplicate equivalence class that touches a modeled field requires an explicit, recorded, raw-token-aware operator selection of the surviving member, or quarantine. There is **no** automatic survivor and **no** "move the unmodeled variant and leave the modeled one in place" shortcut. Cycle 15 published exactly that shortcut for `duplicate:<key>` with one modeled member; it is removed. A spelling-based rule silently decides which of two on-disk values is authoritative — the information-destroying choice this feature exists to refuse.
5. **The scoped no-implicit-survivor invariant.** State it as: *within the stored-checkpoint administrative-disposition read and rewrite surfaces this work governs — `resolve`, `abandon`, `quarantine`, `list`, and `get` — no duplicate member is ever silently collapsed; a document containing one is refused or quarantined.* Cycle 15 asserted the rule "universally", at every level. That claim is **false at the create boundary**, which still parses caller-supplied bytes and re-marshals them and can still collapse duplicate `context` members before anything reaches disk. Create-boundary hardening is out of scope — it is 146-F's shipped surface and no pre-existing on-disk evidence is at risk there — and is deferred under stash `E429A031`. Say so, rather than publishing a universal guarantee the code does not provide.
6. **The operator-decision framing for quarantine itself.** Quarantining a checkpoint whose `status` is `active` moves live recovery state into the archive. It is a deliberate operator decision, not an automatic agent reflex, and **no automatic session-start repair, restore, or quarantine sweep of live checkpoints may be prescribed**. Session-start recovery reads `checkpoint list` and `checkpoint get`; any mutating disposition against `.backlogit/checkpoints/` is a destructive action requiring explicit operator approval immediately before execution.
7. **The recovery direction, stated without executable steps.** Quarantine is reversible in principle: the archived bytes and their sidecar are the verbatim pre-quarantine record, and an operator may restore them by hand. This release does **not** publish, automate, or verify that restore. Name `35A27CD0` as its home and state the two properties any future restore must carry — no-clobber on every destination, and evidence never left live under the same name in both `.backlogit/checkpoints/` and `archive/checkpoints/`, because `CleanupCheckpoints` calls `os.Remove(dst)` before its rename on Windows (`internal/events/checkpoint_lifecycle.go:238-242`) and would destroy the quarantined copy.
8. **The create-time scope repair.** Continuity Protocol §6 currently tells agents that a rejected create "means retry with the offending keys nested under context… there is nothing to recover". That is correct for **create** and wrong for a **stored legacy** document, whose keys are already on disk. Scope §6 explicitly to create.

**This task remains a HARD MERGE GATE.** A pull request containing 147.007-T / U3b, 147.008-T / U4, or 147.009-T / U5 MUST NOT merge without this file's delta present in the same merge commit. Shipping the refusal while the instruction file still tells agents to retry-and-nest strands every agent that hits it. If it cannot land in the same merge commit, **halt the merge** — do not defer it to a follow-up PR. This constrains the implementation units to each other inside one pull request; it is not a per-backlog-task merge requirement.

Must pass `go run ./cmd/backlogit --cwd . docs lint`.

**Test-lifecycle classification**: `harness-exempt: docs-only`. No Go test function is scaffolded, no RED is claimed, and there are no green-step guards; the gates are `go run ./cmd/backlogit --cwd . docs lint` and the merge-gate check above.
<!-- BEGIN:acceptance-criteria -->
- §6's create-time advice is explicitly scoped to create and no longer reads as universal
- A stored-document clause states that `resolve` may return `checkpoint_use_quarantine` or `checkpoint_non_conforming`, that `abandon` refuses the same class, and that the remedy is **quarantine** rather than retry
- The disjointness discriminator is stated as validity **and** top-level conformance
- The offender-diagnosis step names `checkpoint get`'s `non_conforming_fields` as the source and explicitly forbids reading offenders from the decoded `checkpoint` object or from a whole-directory `checkpoint list` scan
- The diagnostic classification table covers all four reported offender classes (`<key>`, `duplicate:<key>`, `duplicate:context.<key>`, `duplicate:progress.<key>`) and gives quarantine as the disposition for every row in this release
- No paste-runnable in-place repair, rename-aside, copy-back, or restore-abort procedure appears anywhere in the file; the withdrawn runbook is named as deferred to stash `35A27CD0` with its required safety properties (real-root/no-follow open, handle-or-content CAS, no-clobber destinations, adversarial tests)
- No mixed modeled/unmodeled auto-survivor rule appears. Every duplicate equivalence class touching a modeled field requires an explicit, recorded, raw-token-aware operator selection or quarantine, with no spelling-based implicit survivor
- The no-implicit-survivor invariant is **scoped** to the stored-checkpoint administrative-disposition read and rewrite surfaces (`resolve`, `abandon`, `quarantine`, `list`, `get`) and explicitly states that create-boundary hardening is deferred under stash `E429A031` — no universal claim appears
- The file states that quarantining an `active` checkpoint is a deliberate operator decision and that no automatic session-start repair, restore, or quarantine sweep of live checkpoints is prescribed
- The recovery direction is stated without executable steps: the archived bytes and sidecar are the verbatim record, restore is operator-performed and outside agent automation in this release
- The text states that creating a replacement checkpoint is **not** the repair path
- The wording matches plan unit U9b; 147.026-T / U10b no longer carries a restore row, so there is no third artifact to keep in sync
- `go run ./cmd/backlogit --cwd . docs lint` reports `valid: true, violation_count: 0`
- Merge gate: verified present in the same merge commit as 147.007-T, 147.008-T, and 147.009-T<!-- END:acceptance-criteria -->
