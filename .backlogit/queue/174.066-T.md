---
artifact_type: task
created_at: 2026-09-23T23:21:38.3373377Z
id: 174.066-T
parent_id: 174-F
priority: high
status: queued
title: Isolate shipped-event durability tests from real gate subprocess
updated_at: 2026-09-23T23:35:01.5019699Z
---

Test-infrastructure corrective (Wave 13, P-021 deferred from stash FF1F3AC7 / deliberation 068-DL). The authorized full suite `go test ./...` fails and times out in UNCHANGED internal/core durability tests because newShipDurabilityFixture inherits the real buildGateBroker (gate.ExecVersionRunner) from NewWorkspace (workspace.go:278) and shipWithWatchdog runs ShipShipment(context.Background()) unbounded, so gateShipmentCompletion -> Broker.Evaluate -> gate.ExecVersionRunner.Version spawns a real `autoharness version` subprocess that exhausts the 10m package deadline (first failure TestShipShipment_FailClosedShippedAppendSuppressesMoveStatusPostHook @ shipment_shipped_event_durability_test.go:316; timeout in TestShipShipment_ShippedEventAppendFailureLogsFixedShape/not_applied). Fix is TEST-ONLY via the PRE-EXISTING injection seam (public ws.GateBroker field + gate.VersionRunner/GateRunner interfaces + injectBroker/fakeVersion/fakeGateRunner helpers in gate_transition_test.go). NO production API change. This is P-021 C1 OUT OF SCOPE for the 174.064-T lock-order work (different contract surface) but blocks 174.064-T AC6 / the full-repository gate.

<!-- BEGIN:acceptance-criteria -->
AC1 (Inject a NOT-ENFORCED / fail-open fake broker - CORRECTED per correctness review): Override the durability fixture ws.GateBroker with a fake broker from the existing seam (injectBroker + fakeVersion + fakeGateRunner) configured so the gate is NOT ENFORCED / fail-open, so gateShipmentCompletion returns nil at its `!ev.Enforced` early return and the UNGATED ship path - the current default-config CI behavior for these fixture members, which are active and carry NO per-member gate-pass evidence - is preserved verbatim. Concretely: gate.EnabledAuto with a fakeVersion that reports the gate binary unavailable (version-probe error, yielding ev.Enforced==false), OR gate.EnabledFalse. Do NOT configure an ENFORCED+passing broker: because the fixture release-scope members are ungated, ev.Enforced==true makes gateShipmentCompletion call validateMemberGateEvidence and REFUSE the ship BEFORE it reaches the ws.shipmentEventAppend seam, breaking every durability assertion. Inject AFTER newShipDurabilityFixture/NewWorkspace construction and BEFORE shipWithWatchdog launches the ship goroutine. After the correction the real gate.ExecVersionRunner MUST be structurally unreachable in these scenarios. AC1 is the PRIMARY, load-bearing fix (removes the subprocess) and is REQUIRED for AC6; AC3's bounded context is defense-in-depth only.
AC2 (formalGateEnforced()==false precondition): the fixture MUST assert/guarantee no formal-gate enforcement (no BACKLOGIT_FORMAL_GATE_REQUIRED-style env, ws.Config formal-gate off) so the fail-open early return holds; otherwise the fail-open branch flips to a formal-gate refusal (ErrFormalGateRequired) and the ship is refused instead of returning nil.
AC3 (Bounded context - DEFENSE-IN-DEPTH, correctly bounded): propagate a bounded context (context.WithTimeout/WithDeadline + defer cancel) into ShipShipment and the ship goroutine in shipWithWatchdog instead of context.Background(). This is defense-in-depth: the load-bearing fix is AC1 (Broker.Evaluate already wraps the child process at TimeoutSeconds ~600s, so the real probe is 600s-bounded and merely LEAKS after the 90s watchdog). The bound MUST be (a) strictly LESS than the watchdog so the watchdog never fires first and leaks the goroutine, and (b) generous vs. real fixture work and UNOBSERVABLE on the happy path, so context.DeadlineExceeded is never injected into the append/compensation path and cannot silently mutate MutationPartialError.Class / CompensationState. Validate the bound against the pre-Evaluate work the ctx also threads through - in particular ws.headSHABounded(ctx) (a real git rev-parse capped ~5s) called BEFORE the `!ev.Enforced` return - not merely the append window.
AC4 (Preserve all assertions - by SEMANTICS): every existing shipped-event append / compensation-state / move-shipment-status POST-hook-suppression / fixed-shape-logging assertion MUST be preserved by SEMANTICS (the assert/require calls and their expected values), not necessarily the surrounding harness plumbing (context threading per AC3 is permitted). No assertion may be deleted, weakened, or made conditional.
AC5 (Close the wrong-path masking gap): TestShipShipment_FailClosedShippedAppendSuppressesMoveStatusPostHook currently asserts only require.Error + assert.False(movePostHookFired); a gate REFUSAL also yields an error and no move-status hook, so a misconfigured (enforced) broker could green it SPURIOUSLY while never reaching the shipped-append stage. This scenario MUST additionally prove the returned error is the shipped-event-append MutationPartialError (reuse requireShippedAppendPartial / errors.As), so a gate short-circuit cannot pass the suite.
AC6 (Deterministic no-real-broker proof - TYPE ASSERTION, both interfaces): deterministically prove NO real broker path is entered via a structural TYPE ASSERTION that ws.GateBroker.Version is the fake type (never gate.ExecVersionRunner) AND ws.GateBroker.Runner is the fake GateRunner (both seam interfaces gate.VersionRunner and gate.GateRunner) - NOT an invocation-count spy (gate.EnabledFalse short-circuits Broker.Evaluate before Probe/Version, so a "0 calls" spy passes vacuously). Any call-recording used as corroboration MUST be concurrency-safe (sync/atomic or mutex) and read ONLY after a channel receive from the ship goroutine, to avoid a -race hazard if the goroutine is leaked on watchdog fire.
AC7 (RED-before / GREEN-after; RED is a bounded timeout): the durability tests MUST be demonstrably failing BEFORE and passing deterministically AFTER. The pre-fix RED symptom is a HANG / watchdog t.Fatalf / package-deadline exhaustion (NOT a clean assertion failure); a BOUNDED reproduction (real broker under a short deadline proving the subprocess spawn / deadline breach) is an acceptable cheap deterministic RED demonstration - a full 10m run is NOT required. Targeted GREEN verification: go test -run TestShipShipment_ ./internal/core, recommended under -race; core and full-repository suites deferred to a later separately-authorized operation.
AC8 (No masking): NO broad timeout inflation, NO t.Skip, NO weakened assertions, NO production gate bypass. The gate still returns FAIL-OPEN (proceed) against the fake - whether gate.EnabledAuto evaluates to not-enforced or gate.EnabledFalse short-circuits at the kill-switch, gateShipmentCompletion returns nil via the same `!ev.Enforced` path - so the ship path is exercised exactly as in default-config CI; only the real subprocess is removed.
AC9 (Scope confinement / no NEW production API): changes confined to internal/core/shipment_shipped_event_durability_test.go and, only if strictly necessary, a sibling _test.go helper reusing the EXISTING seam. Introducing ANY new production (non _test.go) injection seam/API is OUT OF SCOPE BY DEFINITION - the existing seam (public ws.GateBroker + gate.VersionRunner/GateRunner interfaces + injectBroker/fakeVersion/fakeGateRunner helpers) suffices - and MUST cause Ship to HALT and classify, never be "bounded" into this task. Residual KNOWN limitation, explicitly OUT OF SCOPE: on a GENUINE lock regression where ShipShipment blocks in a lock path that does not observe ctx, neither AC3's bound nor the watchdog can join the goroutine, so t.Cleanup ws.Close()/TempDir removal can race the leaked goroutine; the full fix (cooperative ctx cancellation inside ShipShipment's lock path) is a PRODUCTION change and is NOT in scope. AC1 confines this hazard to the intended failure signal only.
AC10 (Circuit disposition): the same-operation full-suite circuit is OPEN. The post-fix go test ./... is a NEW separately-authorized final-gate operation at a NEW commit/phase, NOT a retry/probe of the pre-fix failing state; explicit operator authorization is required immediately before it and it MUST NOT be run within this task. If policy forbids a full-suite run even post-correction, the compliant alternative is targeted per-package verification (./internal/core plus the affected packages) under explicit authorization - never a skipped mandatory gate.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Seam locations (all pre-existing, test-only): fakeVersion (implements gate.VersionRunner: Version(ctx)(string,error)), fakeGateRunner (implements gate.GateRunner), fakeGitAllOK, and injectBroker(ws, enabled, runner, version) in internal/core/gate_transition_test.go; ws.GateBroker is a public *gate.Broker field on Workspace. Real wiring under test: NewWorkspace -> workspace.go:278 buildGateBroker -> gate.Broker{Version: gate.ExecVersionRunner{...}, Enabled, TimeoutSeconds}. The durability fixture inherits this real broker via setupShipmentWorkspace (shipment_test.go:27) -> NewWorkspace and never overrides it, so gateShipmentCompletion -> Broker.Evaluate -> Probe -> gate.ExecVersionRunner.Version spawns the real `autoharness version` subprocess.
CORRECTED broker configuration (from correctness review, P1): the durability fixture's two shipment members are active and ungated (never completed through UpdateArtifactWithGate), and formalGateEnforced() is false, so today gateShipmentCompletion takes the fail-open `!ev.Enforced` early return (returns nil) and the ungated ship proceeds to the shipped-append/compensation stage. To PRESERVE that behavior the injected fake broker MUST be NOT enforced / fail-open (gate.EnabledAuto + version-probe error, or gate.EnabledFalse). An ENFORCED+passing fake would instead run validateMemberGateEvidence and REFUSE the ungated members before the append seam, silently short-circuiting the ship and breaking every durability assertion. Gate coverage itself lives in the sibling shipment_gate*_test.go, not these durability tests, so a fail-open fake removes NO durability coverage; the ws.shipmentEventAppend failure-injection seam stays intact.
Fix shape: after the fixture builds ws, call injectBroker (or set ws.GateBroker directly) with fakeVersion + fakeGateRunner in a NOT-enforced mode; replace context.Background() in shipWithWatchdog with a bounded context (bound strictly < watchdog, generous vs. real work, unobservable on the happy path). Prove no real broker via a TYPE ASSERTION on ws.GateBroker.Version (and .Runner), not an invocation count. Close the post-hook-suppression masking gap by asserting the error is the shipped-event-append MutationPartialError (requireShippedAppendPartial/errors.As), so a gate refusal cannot green the scenario.
Ownership: P-021 C1 OUT OF SCOPE for 174.064-T lock-order canonicalization (that stack is item-log/artifact/global-lock, a different contract surface) but it BLOCKS 174.064-T AC6 / the full-repository gate, so it is a governed prerequisite for 155-S final readiness. Membership-as-gate: added to active 155-S; an unfinished member gates PR readiness, so no dependency-graph edge is added (fail-closed; a blocks edge onto already-done tasks would be a vacuous immediately-satisfied ordering). Do NOT reopen any completed task status. Do NOT touch the current uncommitted Ship implementation, the checkpoint checkpoint-20260923-231731.json, .gitattributes, 154-S, or PR #449.
<!-- END:implementation-notes -->