---
artifact_type: task
commit: 956ac8b2
created_at: 2026-08-24T19:05:02.1691474Z
dependencies:
    - 147.001-T
    - 147.030-T
    - 147.032-T
    - 147.002-T
    - 147.038-T
id: 147.016-T
labels:
    - checkpoint
    - tests
    - plan-u8b
    - harness-ready
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: done
title: 'U8b: cross-surface parity harness (RED) from one stored state'
$12026-08-28T00:05:25.0688307Z
---

Plan unit **U8b** (partition 3). The cross-surface parity harness, landing **before** every behavioural implementation.

Files: `internal/cli/checkpoint_parity_test.go` (new). No production change.

**Cycle-17 rewrite (gate finding H2).** Cycles 15 and 16 made this task depend on 147.022-T, 147.014-T, 147.024-T, 147.015-T, and 147.027-T - the very implementations it pins. A harness that depends on the implementations it pins can never be red against a pre-implementation state. The direction is inverted: this task now depends on **declarations only** and the partition-4 implementations depend on it.

`docs/compound/2026-07-03-cli-mcp-honest-fallback-map-and-registry-drift-test.md` Rule 3 requires a guard spanning CLI and MCP to exercise both surfaces **from the same stored state**. Add one shared fixture table - `legacy-shaped` (schema-invalid **and** non-conforming), `valid-but-non-conforming`, `conforming-active` - written once per case, then driven through the CLI command handler **and** the MCP handler **and** the `events` read layer against that same file. Shape differences (exit code + text vs JSON payload) are expected; the **classification** must not differ.

**Harness posture.** The file compiles against `events.GetCheckpointResult` / `CheckpointReadResult` (147.038-T / U15), `BoundedFieldPathSet` (147.030-T / U1b), `RemediationIntent` (147.032-T / U1d), and the shipped CLI and MCP handlers. Every symbol exists when it lands, so it compiles; every projection and refusal it asserts is unimplemented, so it fails on assertions.

**Already-green assertions relocated (cycle 20).** Cycle 16 claimed the `legacy-shaped` row''s `get` assertions were red. They are not: `GetCheckpoint` already runs `ValidateCheckpoint` and returns `ErrCheckpointInvalid`, `domainError` already maps that to `validation_failed`, and the CLI already exits non-zero. Cycle 17 kept them in the harness and merely excluded them from the red gate by description; the cycle-20 review raised that as a P1, because P-002/P-004 gate the harness commit. They now land with the parity implementation as `TestU8bGuard_*` functions and are **not committed in the harness step**.

Red verification: `go test -count=1 -run '^TestU8b_' ./internal/cli`.

**Persistent red across waves (cycle-32, P-002.6).** This task is a **red deliverable**: it completes in wave 4 with its harness landed and confirmed RED, and it stays red until every surface implementation it pins has landed. Its selector therefore stays in `open_red_deliverables` for waves 4 through 12 and closes at wave 13, when the last declared green-maker (`147.024-T` / U7c) completes. The wave convergence gate must not treat it as an unexpected failure before then, and the first unfiltered full-suite run after wave 3 is the wave-13 convergence gate. The mapping below is the machine-readable source Ship reads; it is not prose.

<!-- BEGIN:red-deliverable-contract -->

```text
red_deliverable: true
red_deliverable_reason: Cross-surface parity harness with no production change; its assertions turn green only after every surface implementation it pins has landed, so its TestU8b_ functions are red by design when it completes.
red_selector_command: go test -count=1 -run '^TestU8b_' ./internal/cli
green_maker_tasks: 147.006-T, 147.007-T, 147.008-T, 147.009-T, 147.011-T, 147.012-T, 147.013-T, 147.014-T, 147.015-T, 147.022-T, 147.024-T, 147.025-T, 147.027-T
green_maker_closes_wave: 13
```

<!-- END:red-deliverable-contract -->

<!-- BEGIN:acceptance-criteria -->
- `legacy-shaped` row: the single RED assertion is that `resolve` is refused with `checkpoint_use_quarantine` on all three surfaces; the three `get` assertions are green on landing and move to the green step as `TestU8bGuard_*` functions
- `valid-but-non-conforming` row: `NeedsQuarantine == true`, `RemediationIntent != nil`, non-empty `NonConformingFields.Paths`, MCP `conforming: false`, CLI `conforming: false`, and refusal of both `resolve` and `abandon` with `checkpoint_non_conforming` - all RED against the 147.038-T declaration state
- `conforming-active` row: the three `conforming: true` projections are RED until 147.012-T, 147.022-T, and 147.027-T land; accepted `abandon` is green on landing and moves to the green step as a `TestU8bGuard_` function
- Byte-identity postcondition applies only to refused mutation paths; the accepted `abandon` uses a fresh fixture copy and asserts the intended rewrite/archive outcome
- Red harness (P-002/P-004 gate): rows 1, 2, and 3 each produce concrete assertion failures against the partition-3 declaration state, and every scaffolded `TestU8b_` function fails
- Green-step guards (`TestU8bGuard_*`, landing with the parity implementation and never in the harness commit): the `legacy-shaped` row's three `get` refusal assertions, and the `conforming-active` row's accepted `abandon`. Cycle 19 committed these inside the harness and excluded them by description; cycle 20 relocates them because P-002/P-004 gate the harness commit, not the prose
<!-- END:acceptance-criteria -->