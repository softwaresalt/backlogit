---
artifact_type: task
created_at: 2026-08-24T19:01:46.4299917Z
dependencies:
    - 147.002-T
id: 147.005-T
labels:
    - checkpoint
    - events
    - tests
    - plan-u2d
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U2d: key-set parity and decision-anchored carrier guard'
updated_at: 2026-08-24T19:40:00.0000000Z
---

Plan unit **U2d**. Key-set derivation parity and the decision-anchored carrier guard.

Files: `internal/events/checkpoint_conformance_test.go`.

Assert `checkpointV1AllTopLevelKeys` equals `checkpointV1TopLevelKeys` unioned with `checkpointV1ReservedKeys`. Both derive from the same `modeledJSONTagKeys` call, so this guards drift in the **hand-written `checkpointV1ReservedKeys` literal**.

Add a **decision-anchored** guard that `CheckpointV1` declares no `json:"-"` map carrier, with a comment naming `docs/decisions/2026-08-24-checkpoint-toplevel-key-disposition-deliberation.md`. This is a "revisit the decision before changing this" marker, **not** a permanent ban.

Add the tag-coverage guard closing the `modeledJSONTagKeys` untagged-exported-field escape hatch.

Protected invariant **I1** (the checkpoint rewrite write-site enumeration) is **not** part of this unit — it is a distinct deliverable with its own file and is tracked separately as **147.021-T** so this unit keeps its declared green-on-landing posture and stays within one skill domain.

**Posture: regression guard — green on landing, no red phase.** The exemption from the two-step red rule is declared, not implied.

<!-- BEGIN:acceptance-criteria -->
- Set equality between the read-boundary and create-boundary key sets holds
- `CheckpointV1` declares no `json:"-"` map carrier
- Every exported field of `CheckpointV1` carries a non-empty `json:"..."` tag
<!-- END:acceptance-criteria -->