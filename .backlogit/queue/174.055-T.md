---
artifact_type: task
created_at: 2026-09-16T06:47:49.0904136Z
dependencies:
    - 174.044-T
id: 174.055-T
parent_id: 174-F
priority: high
status: queued
title: 'SCOPE-RED-A: release scope equals flat explicit manifest'
updated_at: 2026-09-16T06:47:49.0904136Z
---

Behavior RED harness pinning the **flat-manifest release-scope contract** (spec §0.5 / SBLK-R28; plan §0.4). Constructs shipments with explicit `custom_fields.items` manifests and asserts that the lifecycle release-scope derivation — `releaseScopeItemIDs` (`internal/core/shipment_lifecycle.go:1142`), the set consumed by member-evidence validation and completion — **equals the flat explicit manifest** with NO descendant expansion of a listed feature. The harness compiles against current code and is RED because today `releaseScopeItemIDs` expands descendants via `descendantItems` (`:1219`, BFS on `ParentID`, `IncludeArchived:true`); it turns GREEN only when SCOPE-IMPL-1 (`174.057-T`) flattens the derivation. It asserts BEHAVIOR only and introduces no production change.

Scenarios (<4):
1. **Task-only manifest** → release scope == exactly the listed task IDs (set equality; no extra IDs).
2. **Feature + one listed child** → release scope == `{feature, that child}`; a sibling descendant that is NOT listed — **including one whose status is `blocked`** — is absent (a listed feature does not expand to unlisted descendants).
3. **Explicitly-listed descendant** is present in scope (inclusion follows explicit listing, not hierarchy), even when its parent feature is also listed.

AC: (1) the harness FAILS against the current expanding derivation and PASSES after `174.057-T`; (2) asserts release-scope **set equality** against the explicit manifest; (3) asserts a listed feature does not pull in unlisted descendants; (4) asserts an explicitly-listed descendant is included. Domain: tests. <4 scenarios.

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Behavior RED harness pinning the flat-manifest release-scope contract (SBLK-R28); asserts releaseScopeItemIDs-derived release scope equals the flat explicit custom_fields.items manifest with no descendant expansion of a listed feature. It compiles against current code and its TestUReleaseScopeFlat_ functions are RED while the lifecycle derivation still expands descendants, turning green only after SCOPE-IMPL-1 (174.057-T) flattens the derivation.
red_selector_command: go test -count=1 -run '^TestUReleaseScopeFlat_' ./internal/core
green_maker_tasks: 174.057-T
green_maker_closes_wave: 8
```
<!-- END:red-deliverable-contract -->