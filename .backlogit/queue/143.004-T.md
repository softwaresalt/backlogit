---
artifact_type: task
created_at: 2026-08-17T05:10:23.9307321Z
id: 143.004-T
labels:
    - doctor
    - audit-log
    - core
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-16-shipment-shipped-event-audit-log-plan.md
status: queued
title: Add report-only doctor audit for missing shipped events
updated_at: 2026-08-17T05:10:23.9307321Z
---

Unit 4. Add FindingMissingShippedEvent DoctorFindingType = "missing_shipped_event" and DoctorOptions.CheckShippedEventCompleteness bool. In Doctor, gate a new check (off by default, advisory and exit-code-neutral like FindingOverArchivedCoveringFeature) that walks the FULL canonical artifact scan already used by Doctor (both queue/ and archive/, since a shipped-but-unarchived shipment stays under queue/ because shipped is not an archive-routed status), parsing each exact path raw Markdown once (extend artifactRef to carry archived_status), not a per-ID findArtifact second lookup. Flag a shipment when EITHER (a) it is archived with archived_status: shipped and its item JSONL log has no shipment_status_changed event with status == shipped, OR (b) it has status: shipped but is not archived (the indeterminate residue), anomalous regardless of event presence; record whether the shipped event is present in the finding detail. Carry the same "verify actual history; may be transient during an in-flight ship" caveat as FindingOverArchivedCoveringFeature. Never write or rewrite JSONL. Files: internal/core/doctor.go (+ internal/core/canonical_scan.go artifactRef field). Test-first.

<!-- BEGIN:acceptance-criteria -->
- A new advisory, off-by-default finding type and option exist; the check never changes the doctor exit code.
- Detection reads authoritative state from the full canonical raw-Markdown scan (queue and archive), not the DB projection, and covers both the archived and the shipped-but-unarchived states.
- The check never writes or rewrites historical JSONL (asserted by unchanged log bytes).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Mirror the 133-F CheckOverArchivedFeatures registration at doctor.go:522. Tests (three scenarios): archived archived_status: shipped with no shipped event -> one finding; the same with the shipped event present -> none; a status: shipped unarchived shipment -> one finding (residue). Assert zero JSONL bytes change (run with fix_orphans off).
<!-- END:implementation-notes -->