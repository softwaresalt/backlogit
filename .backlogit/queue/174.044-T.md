---
artifact_type: task
created_at: 2026-09-15T20:44:47.2552643Z
dependencies:
    - 174.052-T
    - 174.053-T
    - 174.043-T
id: 174.044-T
parent_id: 174-F
priority: high
status: queued
title: 'R6: Governed UnblockShipment + Claim under shared global lock'
updated_at: 2026-09-15T22:50:00.0000000Z
---

Implement UnblockShipment (filling the Rd 174.052-T stub) and make ClaimShipment share the SAME workspace-global lock. Target-aware unblock: to-queued leaves all members queued and preserves the snapshot for later claim; to-active restores the exact member snapshot under the lock (refuse on CAS/drift). Only ClaimShipment and unblock-to-active may create an active shipment; require --confirm and a free-active-slot check for unblock-to-active. Never leave active members under a queued shipment. Enable the governed `blocked -> {queued,active}` transitions here — this is the transition-table enablement deferred out of the declaration-only Rd task (174.052-T): the governed UnblockShipment path validates and performs `blocked -> queued` and `blocked -> active`, but it MUST NOT open the generic path — generic `MoveShipmentStatus` must remain UNABLE to persist a blocked transition (that ungoverned refusal is asserted by R2 174.040-T and enforced by R7a 174.045-T). Domain: code. AC: (1) turns unblock/claim tests of R1b (174.053-T, TestUR1B_) GREEN; (2) exact restore under lock with drift refusal; (3) no active shipment created outside claim/unblock; (4) the `blocked -> {queued,active}` transitions are enabled ONLY through this governed path — generic `MoveShipmentStatus` still refuses them (invariant preserved with R2/174.040-T + R7a/174.045-T). <5 functions, <4 scenarios. Depends R1b (174.053-T), Rd (174.052-T), R5 (174.043-T).