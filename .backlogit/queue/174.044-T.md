---
artifact_type: task
created_at: 2026-09-15T20:44:47.2552643Z
dependencies:
    - 174.052-T
    - 174.053-T
    - 174.043-T
    - 174.054-T
id: 174.044-T
parent_id: 174-F
priority: high
status: active
title: 'R6: Governed UnblockShipment + Claim under shared global lock'
updated_at: 2026-09-21T20:50:00.0000000Z
---

Implement UnblockShipment (filling the Rd 174.052-T stub) with the exact declared signature `UnblockShipment(ctx, ws, shipmentID string, opts UnblockOptions) (*models.Artifact, error)` and make ClaimShipment share the SAME workspace-global lock. Read `opts.Target` (must be `ShipmentQueued` or `ShipmentActive`; refuse otherwise), `opts.Confirm`, `opts.UnblockedBy`. Both unblock targets require `opts.Confirm==true` (a non-authoritative blocker-resolution confirmation per SBLK-R5; refuse when absent). Target-aware unblock: `Target==ShipmentQueued` leaves all members queued and preserves the snapshot for later claim; `Target==ShipmentActive` restores the exact member snapshot under the lock (refuse on CAS/drift). Only ClaimShipment and unblock-to-active may create an active shipment; `Target==ShipmentActive` additionally requires a free-active-slot check. Never leave active members under a queued shipment. Enable the governed `blocked -> {queued,active}` transitions here — this is the transition-table enablement deferred out of the declaration-only Rd task (174.052-T): the governed UnblockShipment path validates and performs `blocked -> queued` and `blocked -> active`, but it MUST NOT open the generic path — generic `MoveShipmentStatus` must remain UNABLE to persist a blocked transition. GUARD-BEFORE-EDGE (P-ordering): the generic `MoveShipmentStatus` top-level block/unblock refusal is already ACTIVE from R4g (174.054-T, a hard dependency of this task) BEFORE this task enables the `blocked -> {queued,active}` table edges, so no intermediate commit exposes an ungoverned unblock; UnblockShipment writes the edges through the U2c seam-private primitive beneath the choke point, never through the refusing top-level entry point. (Ungoverned refusal is asserted by R2 174.040-T, guaranteed pre-edge by R4g 174.054-T, and the broader write bypass routing enforced by R7a 174.045-T.) Domain: code. AC: (1) turns unblock/claim tests of R1b (174.053-T, TestUR1B_) GREEN; (2) exact restore under lock with drift refusal; (3) no active shipment created outside claim/unblock; (4) the `blocked -> {queued,active}` transitions are enabled ONLY through this governed path — generic `MoveShipmentStatus` still refuses them (invariant preserved by the R4g guard 174.054-T already active + R2/174.040-T + R7a/174.045-T). <5 functions, <4 scenarios. Depends R1b (174.053-T), Rd (174.052-T), R5 (174.043-T), R4g (174.054-T).

Wave-3 rev10 adversarial-consensus ownership clarification (2026-09-21): (i) the drift/CAS proof (exact-snapshot restore with refusal on CAS/drift) is a GREEN-owned proof of THIS task (R6), NOT a Wave-3 RED P1 of R1b (174.053-T); (ii) the DETERMINISTIC Claim/unblock CONTENTION proof (a competing governed operation deterministically contends on the shared workspace-global lock during Claim/unblock) is GREEN-owned HERE, post-implementation, paralleling the rev8 relocation of the recovery-contention proof to GREEN R10 (174.048-T) — it is NOT authored by the pre-implementation RED harness R1b (174.053-T), for which adversarial consensus found NO authentic RED-observable contention seam. Ownership clarified only; no dependency-graph, ID, status, priority, or shipment-membership change.