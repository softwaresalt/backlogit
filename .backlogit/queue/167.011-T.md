---
artifact_type: task
created_at: 2026-09-06T03:12:58.6271386Z
dependencies:
    - 167.003-T
id: 167.011-T
parent_id: 167-F
priority: critical
status: queued
title: Handle-relative item-log lock primitive
updated_at: 2026-09-06T03:12:58.6271386Z
---

Handle-relative item-log LOCK primitive (split out of 167.006 per 2-hour/single-domain rule). This is the LOCK primitive ONLY; it is acquired by the TRANSACTION (167.008) as the SINGLE acquirer (non-reentrant), in the order C-then-B (item-log lock C before artifact batch B, matching AssociateCommit), held together with B in BOTH Phase B (classification + 2b resume append) and Phase D (2d frontmatter write + append) across the atomic operations; the durable append primitive (167.007) runs UNDER it without re-acquiring. Tests cover Phase-B (2b resume) and Phase-D (2d) usage. Implement the cross-process handle-relative item-log lock used by the transaction: open the logs-directory HANDLE no-follow/reparse-safe, acquire the lock sidecar AND the JSONL RELATIVE to that verified handle (NOT LockItemLogCrossProcess, which re-derives + re-opens the sidecar from logsDir/itemID - stream.go:210-223,172-180 - and would NOT bind to the verified handle; reject non-regular/reparse-point targets). PLATFORM SPLIT (3-way, mirror internal/events/item_log_lock_*): _windows.go (//go:build windows), _unix.go (//go:build linux || darwin || freebsd || netbsd || openbsd || dragonfly), _other.go (//go:build !windows && !(linux || darwin || freebsd || netbsd || openbsd || dragonfly)) = unsupported-platform error. TDD (policy): the lock function signature is DECLARED with a panic body in 167.003 (gated by the source-shape harness); THIS task lands the BEHAVIOR harness (RED against the panic body; incl symlink/junction-swap) then the implementation (GREEN). Files internal/core/shipment_reconcile_lock*.go (+ cross-platform tests). Domain: code. Deps 167.003.