---
artifact_type: task
created_at: 2026-09-15T22:15:00.0000000Z
dependencies:
    - 174.039-T
id: 174.052-T
parent_id: 174-F
priority: high
status: queued
title: 'Rd: Core-lifecycle declaration-only stubs (compile-green)'
updated_at: 2026-09-15T22:15:00.0000000Z
---

Declaration-only compile-green stage for the stateful core lifecycle seam, inserted between the R1s source-shape RED harness (174.039-T) and the behavior RED harness (174.053-T). Land ONLY the observable production DECLARATIONS — no behavior: (1) a distinct `ShipmentBlocked ShipmentStatus = "blocked"` enum value on the `ShipmentStatus` Go type (internal/core/shipment.go), kept a DISTINCT Go type from member `models.StatusBlocked`; (2) `BlockShipment(...)` and `UnblockShipment(...)` function SIGNATURES as stubs returning a not-implemented sentinel (e.g. `blerrors.ErrNotImplemented`); (3) the `isValidShipmentTransition` blocked-transition SHAPE entries (active->blocked, blocked->{queued,active}). This is NOT a declaration-only harness exemption (P-002.1): the symbols are gated by the R1s source-shape harness (174.039-T) which is RED until these declarations land and turns GREEN when they do. Domain: code. AC: (1) turns the R1s source-shape harness (174.039-T, `^TestUR1S_`) GREEN — package declares `ShipmentBlocked`, `BlockShipment`, `UnblockShipment`, and the blocked transition shape; (2) stubs compile and return the not-implemented sentinel with NO lifecycle behavior (no lock, no intent/preimage, no member disposition); (3) `go build ./... && go vet ./internal/core` pass and the R1b behavior harness (174.053-T) and R2/R3 harnesses compile against these declarations. <5 functions (2 stub funcs + 1 enum const + isValidShipmentTransition shape edit), <4 scenarios. Depends R1s (174.039-T).
