---
artifact_type: task
created_at: 2026-07-23T18:51:54.8870941-07:00
id: 124.002-T
labels:
    - state-machine
    - transition-validation
    - tdd
    - bug
parent_id: 124-F
priority: high
references:
    - docs/exec-plans/2026-07-23-return-to-queued-transition-plan.md
    - docs/decisions/2026-07-23-return-to-queued-transition-deliberation.md
status: queued
title: Add blocked/active to queued transitions with TDD
updated_at: 2026-07-23T19:24:22.6670291-07:00
---

Unit 1 of the plan. Add `blocked -> queued` and `active -> queued` to the validated status-transition map in BOTH definition sites: `internal/config/defaults.go:508-514` (DefaultHooksConfig().Lifecycle.Transitions — the production-wired map per internal/core/workspace.go:114-118) and `internal/hooks/builtin_pre.go:16-24` (DefaultTransitions() — the empty-map fallback). Additive; no previously-valid transition becomes invalid. Execution posture: test-first (TDD red->green).

APPROACH:
1. (red) Add {blocked,queued} and {active,queued} rows to the AllDefaultTransitions table in internal/hooks/builtin_pre_test.go, plus a positive test that feeds config.DefaultHooksConfig().Lifecycle.Transitions into ValidateStatusTransition; confirm they fail.
2. (green) Edit internal/hooks/builtin_pre.go DefaultTransitions(): active gains queued, blocked gains queued.
3. (green) Apply identical edits to internal/config/defaults.go Lifecycle.Transitions.
4. Add the MANDATORY deep-equality sync-guard test (external _test package; no import cycle exists).
5. Run targeted suites + quality gates.

ACCEPTANCE CRITERIA:
- blocked->queued and active->queued are accepted by hooks.ValidateStatusTransition (red before, green after) in the AllDefaultTransitions table.
- A positive test feeds config.DefaultHooksConfig().Lifecycle.Transitions into ValidateStatusTransition and asserts both new transitions pass (proves the production-wired config-map edit, not just the hooks fallback).
- A still-forbidden transition (e.g. blocked->done) continues to error.
- A mandatory reflect.DeepEqual test pins hooks.DefaultTransitions() == config.DefaultHooksConfig().Lifecycle.Transitions (no weak containment fallback).
- go test ./internal/hooks/... ./internal/config/..., go vet ./..., golangci-lint run, gofmt -l . all pass.

OUT OF SCOPE: no other transitions (review->queued, done->queued); no schema/config-file surface changes; persisted hooks.yaml load-time normalization is Task 124.004-T; comment/doc alignment is Task 124.003-T.