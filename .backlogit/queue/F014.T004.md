---
artifact_type: task
created_at: 2026-04-03T23:45:26.0550087-07:00
id: F014.T004
labels:
    - mcp
    - cli
    - spike
parent_id: F014
priority: medium
status: queued
title: MCP and CLI integration for spike type
updated_at: 2026-04-03T23:45:26.0550087-07:00
---

Update MCP tool metadata and CLI output to recognize and surface the `spike` type.

In `internal/mcp/tools.go`:
- Verify `backlogit_create_item` accepts `artifact_type: "spike"` in its enum/description
- Verify `backlogit_list_types` includes `spike` in its output
- Verify `backlogit_get_wit_metadata` returns the spike type's fields including `spike_ref`

In `internal/cli/` commands:
- Verify `backlogit add --type spike` works
- Verify `backlogit list --type spike` filters correctly
- Verify `backlogit metadata catalog` includes spike

Files to modify: `internal/mcp/tools.go` (update tool descriptions/enums), CLI commands as needed

Verification: `backlogit_list_types` MCP call returns spike. `backlogit_get_wit_metadata type=spike` returns fields including `spike_ref`. `backlogit list --type spike` filters correctly.