---
artifact_type: task
created_at: 2026-08-24T19:00:54.9857915Z
dependencies:
    - 147.001-T
id: 147.002-T
labels:
    - checkpoint
    - events
    - plan-u2
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U2: conformance helper — unknown top-level key rule'
updated_at: 2026-08-24T19:00:54.9857915Z
---

Plan unit **U2**. Conformance helper skeleton plus the unknown top-level key rule.

Files: `internal/events/checkpoint_conformance.go` (new), `internal/events/checkpoint_conformance_test.go` (new).

Add `checkpointV1AllTopLevelKeys` — `modeledJSONTagKeys(reflect.TypeOf(CheckpointV1{}))` with **no** reserved-key subtraction — and exported `CheckConformingTopLevelNamespace(data []byte) error`. Reuse `decodeTopLevelEntries` and `isFoldKeyIn` unchanged; return `*backlogiterrors.CheckpointNonConformingError` directly so `errors.As` resolves in one hop. Perform **no** reserved-status-value check: `status: "abandoned"` is a legal read-boundary value.

Use a new file, not `checkpoint_strict.go`: the read boundary and the create boundary have deliberately different legal key sets and must not read as one mechanism.

<!-- BEGIN:acceptance-criteria -->
- A conforming V1 document returns nil
- A document with two unknown top-level keys returns the typed error with both keys sorted
- A document carrying all four `disposition*` fields **and** `status:"abandoned"` returns nil
- Expected red: cases 2 and 3 fail against the `return nil` stub
<!-- END:acceptance-criteria -->