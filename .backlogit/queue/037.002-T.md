---
artifact_type: task
created_at: 2026-04-21T09:22:31.7555601-07:00
id: 037.002-T
labels:
    - code
    - tdd
parent_id: 037-F
priority: medium
references:
    - docs/exec-plans/2026-04-21-mcp-merge-sync-plan.md
status: active
title: Manifest Walk and Population
updated_at: 2026-04-21T09:30:16.9648406-07:00
---

Unit 2: Implement manifest building from filesystem walk and integrate with Rehydrate.

Files: internal/db/manifest.go (extend), internal/db/rehydration.go (modify).

Functions:
- BuildManifest(workspacePath string) (map[string]FileEntry, error) — walks .backlogit/ tree, stats each file, classifies by kind, extracts artifact ID from frontmatter (first ~500 bytes, not full parse)
- RehydrateWithManifest(ctx, workspacePath, db) (int, map[string]FileEntry, error) — wraps existing Rehydrate logic but also builds and returns manifest from Phase 1 walk
- Existing Rehydrate delegates to RehydrateWithManifest and discards manifest (backward compat)

Pattern: Rehydrate Phase 1 collect walk in rehydration.go:53-79.
Depends on Unit 1 types. Test-first execution.

P2 advisory: Specify error handling for malformed frontmatter (skip file with slog.Warn, empty ItemID).

Verification:
- BuildManifest walks temp workspace with artifacts, stash, logs, config
- Artifact IDs extracted from frontmatter without full body parse
- RehydrateWithManifest returns same count as Rehydrate plus populated manifest
- Existing Rehydrate tests continue to pass
- go test -race ./internal/db/... passes