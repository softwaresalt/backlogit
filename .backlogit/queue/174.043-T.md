---
artifact_type: task
created_at: 2026-09-15T20:44:41.4573723Z
dependencies:
    - 174.052-T
    - 174.053-T
    - 174.042-T
    - 174.054-T
id: 174.043-T
parent_id: 174-F
priority: high
status: active
title: 'R5: Core governed BlockShipment'
updated_at: 2026-09-20T23:26:44.0055241Z
---

Implement BlockShipment (filling the Rd 174.052-T stub) with the exact declared signature `BlockShipment(ctx, ws, shipmentID string, opts BlockOptions) (*models.Artifact, error)` — read `opts.Reason` (required non-empty), `opts.BlockedBy`, `opts.ResumeCheckpointRef` into the governed metadata: acquire the WORKSPACE-GLOBAL lock and HOLD it across the whole operation: persist DURABLE INTENT + complete PREIMAGE snapshot FIRST, then disposition active/in-flight members to queued, then persist shipment status=blocked with governed metadata, then commit (or run compensation on failure). Guard member status mutations while the parent shipment is blocked (version/CAS). Enable the governed `active -> blocked` transition here — this is the transition-table enablement deferred out of the declaration-only Rd task (174.052-T): the governed BlockShipment path validates and performs `active -> blocked`, but it MUST NOT open the generic path — generic `MoveShipmentStatus` must remain UNABLE to persist a blocked transition. GUARD-BEFORE-EDGE (P-ordering): the generic `MoveShipmentStatus` top-level block/unblock refusal is already ACTIVE from R4g (174.054-T, a hard dependency of this task) BEFORE this task enables the `active -> blocked` table edge, so no intermediate commit exposes an ungoverned transition; BlockShipment writes the edge through the U2c seam-private primitive beneath the choke point, never through the refusing top-level entry point. (Ungoverned refusal is asserted by R2 174.040-T, guaranteed pre-edge by R4g 174.054-T, and the broader write bypass routing enforced by R7a 174.045-T.) Domain: code. AC: (1) turns core block tests of R1b (174.053-T, TestUR1B_) GREEN; (2) lock held through intent->disposition->persist->commit/compensation; (3) intent+preimage persisted before any mutation; (4) the `active -> blocked` transition is enabled ONLY through this governed path — generic `MoveShipmentStatus` still refuses it (invariant preserved by the R4g guard 174.054-T already active + R2/174.040-T + R7a/174.045-T). <5 functions, <4 scenarios. Depends R1b (174.053-T), Rd (174.052-T), R4 (174.042-T), R4g (174.054-T).