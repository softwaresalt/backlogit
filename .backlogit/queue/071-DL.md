---
artifact_type: deliberation
created_at: 2026-09-24T07:01:33.6138742Z
custom_fields:
    linked_stash_id: 4A0B7BCF
    linked_stash_kind: bug
    linked_stash_priority: high
    linked_stash_text: 'DEFERRED SCOPE EXPANSION: Restore the protected explicit Ship governed-lifecycle tool allowlist in .github/agents/_ship.agent.md that a generic renderer/template overwrote with a backlogit/* wildcard, failing the integration contract that requires explicit backlogit_block_shipment, backlogit_unblock_shipment, backlogit_normalize_blocked_shipment. HEAD has the correct explicit allowlist (incl. the trio and all intended tools); the uncommitted working-tree frontmatter regressed it to ''vscode/memory, backlogit/*, engram/*, ...''. C1 OUT OF SCOPE for all completed 174-F tasks: this is a harness/configuration surface (installed agent frontmatter + harness manifest drift/checksum record), not the Go source/test surface any done task owns, and moderate mutation authority. Minimal correction: restore the explicit workspace Ship allowlist (drop backlogit/*), preserve unrelated concurrent model-routing frontmatter edits, and update the .autoharness/harness-manifest.yaml _ship.agent.md checksum + drift record; touch registry/plugin template only if strictly necessary (else capture a separate upstream tune-preservation follow-up). Refs: integration contract tests/integration/shipment_155_harness_contract_test.go:37-46 (require.Contains trio); manifest entry .autoharness/harness-manifest.yaml _ship.agent.md checksum 99fa4692... drift_allowed:true; registry trio added by commit ac5ebd29; 174-F; 155-S; HEAD 46145ed5f3b68aea0c98d7b60610ece6df72174d; checkpoint checkpoint-20260924-065144.json. Requires deliberation. Kind bug/config. Provisional priority high. DISCOVERY-STATUS: drift root cause confirmed (drift_allowed protected explicit allowlist vs generic template wildcard render).'
id: 071-DL
priority: high
status: queued
title: Restore protected explicit Ship governed-lifecycle tool allowlist
updated_at: 2026-09-24T07:01:33.6138742Z
---

# Restore protected explicit Ship governed-lifecycle tool allowlist

## Problem Frame

<!-- BEGIN:problem-frame -->
A generic renderer/template overwrote the drift_allowed protected explicit Ship tool allowlist in _ship.agent.md with a backlogit/* wildcard, failing the integration contract that requires the explicit block/unblock/normalize lifecycle trio. Must restore the explicit allowlist and reconcile the harness manifest without regressing unrelated concurrent model-routing edits or changing the upstream template in this task.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
B1 (CHOSEN): restore the explicit workspace Ship allowlist (drop backlogit/*, keep the trio + all intended tools), preserve unrelated model-routing edits, update manifest checksum + drift record. | B2 (rejected here): modify the generic template/plugin so it reproduces the workspace-specific divergence — broader upstream scope; operator says do not change registry/plugin template in this task; capture a separate tune-preservation follow-up if warranted. | B3 (rejected): relax the integration contract to accept the wildcard — weakens the explicit governed-tool security contract and the P-015 flat-membership intent.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
B1: restore the explicit allowlist in the installed _ship.agent.md, exclude backlogit/*, update .autoharness/harness-manifest.yaml _ship checksum + drift record; add contract rejection of the wildcard only if needed. Do not touch registry/plugin template here.
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
Whether a separate upstream tune-preservation follow-up is warranted so the generic renderer stops overwriting the protected allowlist (capture separately if so). Exact model-routing edits to preserve are Ship implementation detail.
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
<!-- END:notes -->
