---
artifact_type: task
created_at: 2026-08-18T00:57:55.6780304Z
dependencies:
    - 143.007-T
    - 143.008-T
id: 143.009-T
labels:
    - doctor
    - mcp
    - registry
    - parity
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: queued
title: Expose the audit on MCP and record it in the registry
updated_at: 2026-08-18T01:45:00.0000000Z
---

# Expose the audit on MCP and record it in the registry

## Description

<!-- BEGIN:description -->
Unit 9. Add the `check_shipped_event_completeness` boolean parameter to the `backlogit_doctor` MCP tool and wire it in `handleDoctor`. Add `check_shipped_event_completeness` to the doctor operation params in `.autoharness/backlog-registry.yaml`. Read-only MCP exposure is justified by the verified precedent `check_partial_mutations` (`internal/mcp/tools.go:484`) and `check_workspace_root_conflict` (`:485`), both read-only doctor checks with dedicated MCP contract tests; do NOT cite `CheckGateEvidence`, which is CLI-only (`internal/cli/doctor.go:33`, `:129`, `:167`). Reconciling the two pre-existing drifted registry params and adding a repo-wide params-to-InputSchema parity invariant are explicitly DEFERRED to a closure follow-up: that invariant ranges over roughly forty operations whose current state this feature has not verified. The CLI-versus-MCP findings-equality assertion MUST live in `internal/cli/registry_parity_test.go`, the only package that sees both surfaces, because `internal/mcp` cannot import `internal/cli`; that file today asserts registry mapping and governed-mutation parity only, so this adds a new fixture. Execution posture: test-first.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- `backlogit_doctor` advertises `check_shipped_event_completeness` and `handleDoctor` wires it.
- The registry doctor params record the new parameter.
- MCP-side findings render for a seeded fixture, asserted in an `internal/mcp` contract test.
- A shared fixture yields identical findings through CLI and MCP, asserted from `internal/cli/registry_parity_test.go`.
- All three scenarios were written and observed failing before the parameter was wired; the red output is recorded in this task.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/mcp/tools.go`, `.autoharness/backlog-registry.yaml`, a new `internal/mcp` contract test mirroring `internal/mcp/doctor_partial_mutations_test.go`, and `internal/cli/registry_parity_test.go`.
<!-- END:implementation-notes -->
