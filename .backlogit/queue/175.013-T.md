---
artifact_type: task
created_at: 2026-09-18T00:57:59.561104Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.013-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U13 - errcheck remediation: residual closed package set'
updated_at: 2026-09-18T05:06:37.9867554Z
---

Remediate ALL golangci-lint errcheck findings across the COMPLETE, CLOSED residual package set — every module package not already owned by U3-U11 — so that NO errcheck package enumeration is deferred to Ship.

Closed residual package set (explicitly enumerated; owned by this unit): internal/canonical, internal/mcp, internal/hooks, internal/parser, internal/mdfront, internal/docline, internal/errors, internal/models, internal/jsonutil, internal/fsutil, internal/atomicfile, internal/release, internal/version, internal/gateevidence, internal/gateproof, internal/cli/format, internal/core/gate, internal/core/templates, internal/faultline/mutation, internal/faultline/parity, internal/faultline/compatcorpus, internal/faultline/analyzer/auditsuccess, internal/faultline/analyzer/errwrap, internal/faultline/analyzer/failopen, internal/faultline/analyzer/locktimeout, internal/faultline/analyzer/scannerdiscipline, cmd/backlogit, cmd/faultline-analyze, cmd/gen-docs, scripts, tests, tests/contract, internal/config, internal/faultline (top-level, EXCLUDING the two U3-owned files internal/faultline/testdata/parity_v1.golden.json and internal/faultline/evidence_conformance_test.go).

Explicitly EXCLUDED (owned elsewhere): internal/cli (U4), internal/db (U5), internal/telemetry (U6), internal/stash (U7), internal/events (U8), tests/integration (U9), internal/core top-level (U10). FILE-LEVEL exclusions only (NOT package-level): the two U3-owned files in internal/faultline top-level. internal/config and internal/faultline top-level are INCLUDED with file-level ownership. A package with zero errcheck findings closes as a no-op.

Immutable pre-handoff scope — NO Ship-time item creation (resolves the P1-1 finding): the OWNERSHIP of this unit is CLOSED and FROZEN at staging as the enumerated residual package set above. This unit creates NO subtasks and NO planning units at execution time, and it authorizes Ship to create NONE — every prior execution-time subtask-subdivision clause has been REMOVED from this unit in its entirety. Ship executes the remediation as a SINGLE width-isolated mechanical pass within this frozen closed set; it may not add backlog items after the shipment/DAG freeze.
Bound justification (evidence-backed, honest under the Stage role boundary): stored 149-S lint evidence records 50 errcheck findings REPOSITORY-WIDE; after U4-U10 take their packages, this residual set's share is a small, uniform, mechanical error-capture sweep (most enumerated packages carry zero or one finding and close as no-ops) well under the 2-hour rule. The <3-implementation-file heuristic is a proxy for the 2-hour rule; for this mechanical-remediation class the 2-hour rule is satisfied DIRECTLY by the small aggregate finding count, so the file-count proxy is explicitly waived for this unit with this recorded justification (see the plan "errcheck decomposition" record). Exact per-file counts are execution-verified by Ship because Stage's role boundary forbids running linters; that verification never creates a new planning unit.

Per-category strategy (same as U4-U10): named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation. No opportunistic cross-package helper.

Owned harness file (resolves the P1-2 harness-ownership finding): this unit exclusively owns and authorizes creation of the deterministic errcheck-regression harness `<owned-package>/errcheck_convergence_175_test.go` in EACH residual package that receives a behavior-changing fix. That filename segment is RESERVED to this unit in each owned residual package; no other unit may create that exact path in those packages, and it never collides with U11's `<pkg>/staticcheck_convergence_175_test.go`. These `*_test.go` harness files are EXCLUDED from the implementation-file inventory.

Acceptance criteria:
- Scope: the closed residual set above; edit only errcheck-flagged files within those packages (file-partition invariant). EXCLUDE every file listed in U11's owned handoff artifact `docs/closure/175-U11-overlap-inventory.md` (the fenced OVERLAP-INVENTORY payload); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and consumes that exact artifact BY PATH. If absent at execution, treat as a blocking U11-incomplete dependency violation. Ownership is decided by U11's up-front inventory, never by serialization order. A package with zero errcheck findings closes as a no-op for that package.
- No deferred planning (required): this unit creates NO subtasks/planning units and authorizes Ship to create NONE; the residual package set is frozen at staging; there is NO "subdivide at execution" clause.
- Owned harness (required): the reserved `<owned-package>/errcheck_convergence_175_test.go` per touched residual package drives any behavior-changing errcheck fix green; excluded from the implementation-file count.
- Verification (OWNED scope only): `golangci-lint run` over THIS unit's owned residual packages reports 0 errcheck for those packages; `gofmt -l` on touched files empty; touched packages still build and existing tests pass. REPOSITORY-WIDE zero-errcheck is NOT asserted here — it is the terminal U12 assertion once U4-U11, U13, and U14 have all landed. This unit verifies only its own residual scope.
- Existing test expectations: all current tests for touched packages pass unchanged; success-path behavior preserved.
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. The harness-architect authors a failing (RED) harness/test in the owned harness file that compiles before the fix and fails on an assertion (P-002/P-004), and implementation drives it green. NEW reachable failure-path behavior requires a failing (RED) test FIRST. There is NO mechanical/generic/closure-time harness exemption, exempt contract, or Principle II deviation for this task.
- Stage-role constraint (honest): exact per-file errcheck COUNTS are execution-verified by Ship because Stage's role boundary forbids running linters; Stage owns the complete package-to-unit assignment (this closed set), which is the planning decision required, and defers NO item creation.