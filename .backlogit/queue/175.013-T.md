---
artifact_type: task
created_at: 2026-09-18T00:57:59.561104Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.013-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U13 - errcheck remediation: residual closed package set'
updated_at: 2026-09-18T00:57:59.561104Z
---

Remediate ALL golangci-lint errcheck findings across the COMPLETE, CLOSED residual package set — every module package not already owned by U3-U11 — so that NO errcheck package enumeration or planning-unit creation is deferred to Ship.

Closed residual package set (explicitly enumerated; owned by this unit): internal/canonical, internal/mcp, internal/hooks, internal/parser, internal/mdfront, internal/docline, internal/errors, internal/models, internal/jsonutil, internal/fsutil, internal/atomicfile, internal/release, internal/version, internal/gateevidence, internal/gateproof, internal/cli/format, internal/core/gate, internal/core/templates, internal/faultline/mutation, internal/faultline/parity, internal/faultline/compatcorpus, internal/faultline/analyzer/auditsuccess, internal/faultline/analyzer/errwrap, internal/faultline/analyzer/failopen, internal/faultline/analyzer/locktimeout, internal/faultline/analyzer/scannerdiscipline, cmd/backlogit, cmd/faultline-analyze, cmd/gen-docs, scripts, tests, tests/contract, internal/config, internal/faultline (top-level, EXCLUDING the two U3-owned files internal/faultline/testdata/parity_v1.golden.json and internal/faultline/evidence_conformance_test.go).

Explicitly EXCLUDED (owned elsewhere): internal/cli (U4), internal/db (U5), internal/telemetry (U6), internal/stash (U7), internal/events (U8), tests/integration (U9), internal/core (U10). FILE-LEVEL exclusions only (NOT package-level): the two U3-owned files in internal/faultline top-level (internal/faultline/testdata/parity_v1.golden.json and internal/faultline/evidence_conformance_test.go). internal/config and internal/faultline top-level are NOW INCLUDED in this closed set — their prior package-level exclusion rested on 149-S evidence that Stage cannot substantiate without running linters (role boundary), so explicit file-level ownership replaces it. A package with zero errcheck findings closes as a no-op.

Per-category strategy (same as U4-U10): named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation. No opportunistic cross-package helper.

Acceptance criteria:
- Scope: the closed residual set above; edit only errcheck-flagged files within those packages (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order. A package with zero errcheck findings closes as a no-op for that package.
- Verification (zero-finding): with U4-U10 and this unit complete, `golangci-lint run` reports 0 errcheck repository-wide; `gofmt -l` on touched files empty; touched packages still build and existing tests pass.
- Existing test expectations: all current tests for touched packages pass unchanged; success-path behavior preserved.
- Test posture (Principle II): NEW reachable failure-path behavior requires a failing (RED) test FIRST; purely mechanical captures use the PRE-DECLARED mechanical harness-exemption contract recorded in the closure artifact at execution (not retrospective).
- Granularity / no deferred planning: package OWNERSHIP is fully closed by Stage. If the aggregate residual errcheck surface exceeds the 2-hour/<3-file bound at execution, subdivide into MECHANICAL execution subtasks (e.g., 175.013.001-ST per package) — execution decomposition of this already-owned unit, NOT a new planning unit. Ship creates NO new planning units.
- Stage-role constraint (honest): exact per-file errcheck COUNTS are execution-verified by Ship because Stage's role boundary forbids running linters; Stage owns the complete package-to-unit assignment (this closed set), which is the planning decision required.