---
artifact_type: task
created_at: 2026-09-18T00:57:59.561104Z
dependencies:
    - 175.001-T
id: 175.013-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/cli/registry_parity_test.go'
updated_at: 2026-09-18T06:44:49.4233618Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: errcheck
Package: internal/cli
Owned source file (exactly one): internal/cli/registry_parity_test.go
Findings (3) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L489: Error return value of `freshWS.Close` is not checked
- L538: Error return value of `freshWS.Close` is not checked
- L579: Error return value of `freshWS.Close` is not checked
Affected functions (3 distinct, < 5): observeGovernedState, observeGovernedCommentState, observeGovernedDependency
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/cli/errcheck_registry_parity_test_175_test.go
Ship's harness phase creates this harness JUST BEFORE this file task (NOT precommitted at Stage). It is a source-shape or command-backed test scoped to internal/cli/registry_parity_test.go ONLY: it asserts this file's errcheck / behavior contract without referencing any not-yet-existing symbol and without leaving any future-task red assertion in the full suite. It is EXCLUDED from the implementation-file inventory.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test function named `TestU175_013_ErrcheckRegistryParityTest` (deterministic `TestU175_013_` unit token; no other task uses this prefix). RED before the fix (the assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (explicit package, verbose, anchored, fail-closed, non-vacuous):
  go test ./internal/cli -run '^TestU175_013_' -v -count=1
Execution contract: run the command from the repo root; it echoes full test output and preserves the native `go test` exit status (nonzero => fail). After the green phase it is fail-closed and NON-VACUOUS: it MUST observe at least one line matching `^--- PASS: TestU175_013_` (a named PASS for this task's function); zero matching named-PASS lines is a FAILURE even if `go test` exits 0 (guards against a vacuous/absent selector). Before implementation the same command is RED via an assertion failure on `TestU175_013_ErrcheckRegistryParityTest`.

Fix strategy (constitution: returned errors MUST be handled; `_ =` / ignored-return-with-comment is NOT an allowed resolution): in this `_test.go` file, capture every flagged error return and FAIL the test on non-nil (`if err := x.Close(); err != nil { t.Fatalf("...: %v", err) }` or `require.NoError(t, err)`). A deferred close becomes a checked close that fails the test (`defer func(){ if err := x.Close(); err != nil { t.Errorf("...: %v", err) } }()`). No error return is discarded; no cross-file or cross-package helper is introduced; the edit is confined to this one file.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (3).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only-safe compile: verify the package's tests with go test ./internal/cli -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- The P-002.6 command `go test ./internal/cli -run '^TestU175_013_' -v -count=1` exits 0 AND emits at least one `--- PASS: TestU175_013_` line (non-vacuous), and golangci-lint run ./internal/cli reports 0 errcheck findings in internal/cli/registry_parity_test.go.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/cli/errcheck_registry_parity_test_175_test.go was RED (assertion on TestU175_013_ErrcheckRegistryParityTest) before the fix and GREEN after; no future-task red remains in the full suite.
- Every flagged error return is HANDLED (checked propagation/wrapping, named-return errors.Join, or test failure); NO `_ =` / ignored-return resolution is used.
- ONLY internal/cli/registry_parity_test.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.