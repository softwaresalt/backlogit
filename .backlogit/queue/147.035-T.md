---
artifact_type: task
created_at: 2026-08-25T16:56:03.0035358Z
dependencies:
    - 147.034-T
    - 147.004-T
id: 147.035-T
labels:
    - checkpoint
    - events
    - tests
    - plan-u12
    - seam
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U12: guarded rewrite seam contract harness'
updated_at: 2026-08-25T16:56:03.0035358Z
---

Plan unit **U12** (partition 2). The executable contract for the guarded rewrite seam. Harness-only; no production change.

Files: `internal/events/checkpoint_rewrite_contract_test.go` (new).

Compiles against the declaration `147.034-T` / U11 landed in an earlier wave, and fails on assertions because the seam carries no validity and no conformance precondition yet. This is the **behaviour** red for the seam: U11's own harness is source-shape and gates only the declared signature, so every behavioural obligation of the seam is observed failing here, before `147.036-T` / U13 implements it.

Verdict contract decided here: the seam returns the **raw verdict errors** - `ErrCheckpointCorrupt`, `ErrCheckpointInvalid`, and `*CheckpointNonConformingError`. It does **not** wrap them into `ErrCheckpointUseQuarantine` or `ErrCheckpointNonConforming`; that wrap differs per verb and per gate-ordering rule (I2) and stays with the caller. This keeps the seam a mechanism and leaves the contract with the verb.

**Test-lifecycle classification**: red harness only. All three scaffolded `TestU12_` functions fail, because the landed U11 seam performs no validity and no conformance check, so every case currently rewrites the file and returns `nil`. No green-step guards. `147.036-T` / U13 turns all three green.

Red verification: `go test -count=1 -run '^TestU12_' ./internal/events`.

<!-- BEGIN:acceptance-criteria -->
- The seam returns the **raw verdict errors** — `ErrCheckpointCorrupt`, `ErrCheckpointInvalid`, and `*CheckpointNonConformingError` — never wrapped into `ErrCheckpointUseQuarantine` or `ErrCheckpointNonConforming`; that wrap stays with the caller
- No production change: this task is harness-only, compiling against the declaration `147.034-T` / U11 landed in an earlier wave
- Red harness (P-002/P-004 gate): all three scaffolded `TestU12_` functions fail, because the landed U11 seam performs no validity and no conformance check and every case currently rewrites the file and returns `nil`
- Green-step guards: none. `147.036-T` / U13 turns all three functions green
- Width: 1 file, 3 scenarios, no production change
<!-- END:acceptance-criteria -->