---
artifact_type: task
created_at: 2026-08-24T19:05:20.4501692Z
dependencies:
    - 147.012-T
    - 147.016-T
    - 147.020-T
id: 147.017-T
labels:
    - checkpoint
    - docs
    - plan-u9
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U9: design doc records the total three-class disposition contract'
updated_at: 2026-08-24T19:40:00.0000000Z
---

Plan unit **U9**. Update `docs/design-docs/checkpoint-administrative-disposition.md` with the total classification.

Files: `docs/design-docs/checkpoint-administrative-disposition.md`.

The shipped doc states abandon and quarantine are "disjoint by design" over `{valid, malformed}`. That dichotomy is now a **trichotomy** over `status: "active"` documents:

| Class | abandon | resolve | quarantine |
|---|---|---|---|
| conforming + valid | accept | accept | refuse (`ErrCheckpointUseAbandon`) |
| valid but non-conforming | refuse (`ErrCheckpointNonConforming`) | refuse | **accept** |
| malformed (parse fails) | refuse (`ErrCheckpointUseQuarantine`) | refuse | accept |

Record explicitly that the classification is **total only over `status: "active"`** (I3 scoping, pinned by U5b), and that the invariant preserved is C2 — a document that cannot be trusted to round-trip is moved verbatim, never rewritten.

Must pass `backlogit docs lint`.

<!-- BEGIN:acceptance-criteria -->
- The disjointness claim is replaced by the three-class table
- The `status: "active"` scoping of totality is stated explicitly, referencing the resolved-state exclusion
- `backlogit docs lint` reports `valid: true, violation_count: 0`
<!-- END:acceptance-criteria -->