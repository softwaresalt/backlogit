---
artifact_type: task
created_at: 2026-08-24T19:05:20.4501692Z
dependencies:
    - 147.012-T
    - 147.016-T
    - 147.020-T
id: 147.017-T
labels:
    - checkpoint
    - docs
    - plan-u9
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U9: design doc records the total four-class disposition contract'
updated_at: 2026-08-24T20:30:00.0000000Z
---

Plan unit **U9**. Update `docs/design-docs/checkpoint-administrative-disposition.md` with the total classification.

Files: `docs/design-docs/checkpoint-administrative-disposition.md`.

The shipped doc states abandon and quarantine are "disjoint by design" over `{valid, malformed}`. That dichotomy is now a **four-class** classification over `status: "active"` documents:

| Class | abandon | resolve | quarantine |
|---|---|---|---|
| valid + conforming | accept | accept | refuse (`ErrCheckpointUseAbandon`) |
| valid but non-conforming | refuse (`ErrCheckpointNonConforming`) | refuse (`ErrCheckpointNonConforming`) | **accept** |
| parses but schema-invalid | refuse (`ErrCheckpointUseQuarantine`) | refuse (`ErrCheckpointUseQuarantine`) | accept |
| does not parse | refuse (`ErrCheckpointUseQuarantine`) | refuse (`ErrCheckpointUseQuarantine`) | accept |

Row 3 must be written out separately rather than folded into "malformed": it is the exact shape of the nine live legacy files — they parse cleanly and fail `ValidateCheckpoint` — so a table that only names parse failure is **not total** over active checkpoints and omits the very class this feature exists to handle.

Record explicitly that the classification is **total only over `status: "active"`** (I3 scoping, pinned by U5b), and that the invariant preserved is C2 — a document that cannot be trusted to round-trip is moved verbatim, never rewritten.

Must pass `backlogit docs lint`.

<!-- BEGIN:acceptance-criteria -->
- The disjointness claim is replaced by the four-class table
- The `parses but schema-invalid` class is named separately from `does not parse`, so the contract is total over active checkpoints
- The `status: "active"` scoping of totality is stated explicitly, referencing the resolved-state exclusion
- `backlogit docs lint` reports `valid: true, violation_count: 0`
<!-- END:acceptance-criteria -->