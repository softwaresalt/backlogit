---
artifact_type: task
created_at: 2026-09-17T23:59:28.9893922Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.005-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U5 - errcheck remediation: internal/db'
updated_at: 2026-09-18T00:54:43.3164059Z
---

Remediate ALL golangci-lint errcheck findings in internal/db using the per-category strategy (named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation with two-%w where a shared sentinel policy applies). No opportunistic cross-package helper.

Acceptance criteria:
- Scope (package/files): internal/db only; edit only errcheck-flagged files within internal/db. Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order.
- Verification (zero-finding): `golangci-lint run ./internal/db` reports 0 errcheck; `gofmt -l` on touched files empty; `go build ./internal/db` and existing internal/db tests still pass.
- Existing test expectations: all current internal/db tests pass unchanged; success-path behavior preserved (capture/propagation added only on failure paths).
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. The harness-architect authors a failing (RED) harness/test that compiles before the fix and fails on an assertion (P-002/P-004), and implementation drives it green. NEW reachable failure-path behavior requires a failing (RED) test FIRST. There is NO mechanical/generic/closure-time harness exemption, exempt contract, or Principle II deviation for this task.