---
artifact_type: task
created_at: 2026-07-28T18:34:31.5218505Z
custom_fields:
    source_stash_id: 7A965F8A
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'BUG: ArchiveItem re-persist drops modeled item_links (e.g. spike_ref) from artifact frontmatter. Observed: 123-F->120-F spike_ref link dropped during shipment 109-S archive; PR #309 restored it by hand. Root cause likely the archive load->persist path (SQLite GetItem fast-path projection omitting links, mirroring known archived_from/archived_status drop). Related to D04D63D0. Fix: preserve item_links on archive re-persist + regression test asserting frontmatter links survive ArchiveItem.'
id: 129.002-T
parent_id: 129-F
priority: medium
status: queued
title: Reload from Markdown before re-persist so links and provenance survive
updated_at: 2026-07-28T18:34:31.5218505Z
---

From stash 7A965F8A. In internal/core/shipment_lifecycle.go, make the re-persist path full-fidelity: REPLACE the lossy DB fast-path loadArtifact with a Markdown (findArtifact source-of-truth) load for the mutate-then-persist candidate in attachCommitToItems, set Commit/UpdatedAt on the reloaded artifact, then persistArtifact. Do NOT add a supplemental read (coexisting loads re-introduce the drop). Wrap reload error with fmt.Errorf(...%w...) + errors.Is(ErrNotFound). Mirrors MoveInQueue / serializer_provenance_hardening precedent. Preserves item_links (spike_ref) + archive provenance for every stamped member; keeps the index a rebuildable cache (no projection widening, no migration). Preserve the ship-gate Markdown fail-closed archived_status read. TEST-FIRST: RED test asserts item_links (spike_ref) survive re-persist on a STAMPED NON-ARCHIVED candidate — the reachable seam, since attachCommitToItems re-persists non-archived members while Unit 1 skips already-archived candidates. Assert BOTH populated AND empty/nil-links cases (omitempty guard). Do NOT assert "provenance survives re-persist" for an already-archived deliberation: that path is unreachable once Unit 1 skips archived candidates; archive-provenance-unchanged on skipped archived candidates is covered by Unit 1's assertion (c). Plan: docs/exec-plans/2026-07-28-archive-repersist-projection-drop-plan.md Unit 2.