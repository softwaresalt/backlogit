---
artifact_type: task
created_at: 2026-07-25T22:35:00.4361608Z
dependencies:
    - 126.001-T
id: 126.005-T
labels:
    - docs
    - policy
    - markdownlint
    - p-008
parent_id: 126-F
priority: medium
status: queued
title: 'U5: Reconcile P-008 wording in workflow-policies.md with provisioned config + scope'
updated_at: 2026-07-25T22:35:00.4361608Z
---

Domain: docs/policy. Reconcile policy P-008 (Markdown Conformance) in .github/policies/workflow-policies.md (~lines 159-182) with the provisioned reality from U1-U4. Three edits.

(a) CONFIG PREMISE: P-008 must reflect that .markdownlint.json now enables EXACTLY MD001, MD025, and MD041 (default:false, no other rule IDs) — the config provisioned in U1 (126.001-T). Do not add or imply any additional rules.

(b) PRECONDITION: rewrite the current precondition, which wrongly asserts in present tense that "The workspace has a .markdownlint.json config enabling MD001, MD025, MD041", so it no longer overstates that the config already exists as a background fact. State the actual provisioned setup: the workspace NOW PROVIDES .markdownlint.json (rules MD001/MD025/MD041, default:false) plus the U1 scope mechanism (the curated Day-1 ignore set — .markdownlintignore or an ignores array in the config), reproducible via make md-lint (markdownlint-cli2, available via npx).

(c) POSTCONDITION: replace the current literal markdownlint "**/*.md" exits-0 postcondition. A bare repo-wide **/*.md exit-0 gate CONTRADICTS the Option B scope decision (248 pre-existing violations remain in ignored dirs, so it can never be literally true today). Rewrite it as CONFIG-DRIVEN SCOPED enforcement: make md-lint (markdownlint-cli2 honoring .markdownlint.json plus the ignore set) exits 0 with no MD001/MD025/MD041 violations for Markdown in the enforced (non-ignored) scope; new or edited Markdown in non-ignored locations must pass before commit. Keep MD001/MD025/MD041 and the single-H1 / no-skipped-levels / first-line-H1 semantics named. Note the gate is advisory for its first green cycle then promoted to a required check (tracked follow-up stash 918BCDAF).

Acceptance criteria: (1) P-008 no longer claims a pre-existing config; it describes the U1-provisioned .markdownlint.json + ignore scope and make md-lint. (2) The postcondition describes scoped, config-driven enforcement, NOT a literal **/*.md exit-0. (3) Exactly MD001/MD025/MD041 remain named; no rule added or removed. (4) Precondition and postcondition match the files U1-U4 actually create.

Verification: make docs-lint (Go docline frontmatter gate) still passes for the edited workflow-policies.md; a manual read confirms the precondition and postcondition match provisioned reality; no docline frontmatter fields are changed. Depends on U1 (126.001-T) for the actual provisioned filenames and scope.