---
artifact_type: task
created_at: 2026-08-18T00:54:55.133539Z
dependencies:
    - 143.001-T
id: 143.002-T
labels:
    - shipment-lifecycle
    - audit-log
    - tests
    - red-harness
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: queued
title: Add RED harness for shipped-event durability
updated_at: 2026-08-18T00:54:55.133539Z
---

# Add RED harness for shipped-event durability

## Description

<!-- BEGIN:description -->
Unit 2. Test-only; NO production file. Add `internal/core/shipment_shipped_event_durability_test.go` with three table-driven scenarios, injecting through the per-workspace `ws.shipmentEventAppend` seam from 143.001-T. Real-filesystem injection was evaluated and REJECTED: a directory planted at the item log or lock path aborts earlier in `snapshotShipArtifacts` (`internal/core/shipment_lifecycle.go:156-164`, called at `:478`, before the transition at `:509`), leaving `shipSnapshots` empty so the rollback defer short-circuits at `:391`. Scenario 1 success ordering: active to shipped to archived persists AND the `shipment_status_changed` event with status shipped is present in `.backlogit/logs/{id}.jsonl` AND ordered before the archival records. Scenario 2 not-applied and untagged compensate honestly: subtests for a seam error wrapping `ErrWriteNotApplied`, a bare untagged `errors.New`, and a third where one release-scope item cannot be restored during compensation, which must yield `MutationPartialError` with `Class: not-applied` and `CompensationState: partially-compensated` naming the un-restored ID. Scenario 3 indeterminate never rolls back: a seam error wrapping `ErrWriteIndeterminate` on a fixture whose ship would roll up a non-member covering feature; the release scope is NOT rolled back, the covering feature is restored and re-read through `loadArtifact`, archival is halted, the shipment is left shipped-and-unarchived, and the error satisfies `errors.As` with `Class: indeterminate` and `FailedStep: blerrors.StepShippedEventAppend`. Execution posture: test-first (red phase).
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- Scenarios 2 and 3 fail on the 143.001-T tree, and fail from the ship path (assert the failure is not `snapshot release scope`).
- Scenario 1 passes: it characterizes already-correct baseline behavior and locks the ordering contract against regression.
- The file does not call `t.Parallel` and every fixture pins `Config.DurableWrites` explicitly.
- No production file is modified by this task.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/core/shipment_shipped_event_durability_test.go` (new). `t.Parallel` is forbidden because `ShipShipment` reads the package globals `persistArtifactWriteFn` (`internal/core/shipment.go:762`) and `mkdirDirSyncFn`, which other `internal/core` tests override.
<!-- END:implementation-notes -->
