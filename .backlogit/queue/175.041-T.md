---
artifact_type: task
created_at: 2026-09-19T01:30:53.539215Z
dependencies:
    - 175.031-T
    - 175.011-T
id: 175.041-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/cli/migrate.go [slice 2/2]'
updated_at: 2026-09-19T07:24:14.1507608Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck
Package: ./internal/cli
Owned source file(s) (1): internal/cli/migrate.go
Owned baseline occurrences (9) — line:column shown are DIAGNOSTIC baseline coordinates only, NOT durable identity (golangci-lint v2.13.2, unbounded invocation; diagnostic table in docs/decisions/2026-09-17-baseline-lint-inventory.md). Durable ownership is the line-stable group-multiset model in the task-lint-contract below (stable key = path + linter + normalized message; per-task occurrence multiplicity with ordered-slice expected residual):
- internal/cli/migrate.go:L54:C17 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/migrate.go:L56:C18 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/migrate.go:L58:C17 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/migrate.go:L60:C17 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/migrate.go:L77:C18 [errcheck] Error return value of `ws.Close` is not checked
- internal/cli/migrate.go:L83:C17 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/migrate.go:L93:C16 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/migrate.go:L96:C16 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/migrate.go:L103:C17 [errcheck] Error return value of `fmt.Fprintf` is not checked

Ownership identity (line-stable): this task owns an exact occurrence-COUNT slice of one or more stable groups in this shared file. Within each stable group the occurrences are truly indistinguishable and semantically interchangeable (identical path+linter+normalized message, identical mechanical fix), so ownership is enforced by required multiplicity decrease, NOT by line/column position. The sibling slices form an ordered chain (slice k/n) so that when this task's verifier runs, every earlier slice is already converged; the task-lint-contract records this slice's owned_count and the expected_group_residual_after_task (the count still owned by strictly-later slices). A multiline edit that shifts a still-queued sibling's line numbers cannot make its occurrence disappear, because the group is matched by stable fingerprint and counted by multiplicity, not by absolute line. Ownership is by stable group fingerprint + occurrence multiplicity, not by exact (path,line,column) identity and not by whole-file path.
Affected functions (1 distinct): internal/cli/migrate.go: newMigrateCommand

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/cli/lintfix_175_041_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_041_LintFix` (deterministic `TestU175_041_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/cli" -run "^TestU175_041_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_041_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_041_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return is NOT an allowed resolution): for every flagged call, capture the returned error and handle it -- checked propagation with `fmt.Errorf("context: %w", err)`, a named-return `errors.Join`, or (inside a test/benchmark) fail via the in-scope `*testing.T`/`*testing.B` receiver (`require.NoError`, `t.Fatalf`, `b.Fatalf`). A deferred close becomes a checked deferred close. No error return is discarded; no cross-file or cross-package helper is introduced; edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (owned source file(s) + one owned harness file).
- Implementation files = 1 (owned harness excluded).
- Owned baseline findings = 9 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/cli -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.041-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "identity_model": "line-stable-group-multiset/v1",
    "packages": [
      "./internal/cli"
    ],
    "owned_files": [
      "internal/cli/migrate.go"
    ],
    "owned_group_count": 3,
    "owned_occurrence_count": 9,
    "owned_groups": [
      {
        "group_fingerprint": "89c896ecaea0fe5470606e2ea5c9243c9483df91bd50cae92c6b918bd82f3d24",
        "path": "internal/cli/migrate.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `fmt.Fprintf` is not checked",
        "group_multiplicity": 12,
        "owned_count": 6,
        "owned_ordinals": [
          1,
          2,
          3,
          4,
          5,
          6
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 54,
            "column": 17
          },
          {
            "line": 58,
            "column": 17
          },
          {
            "line": 60,
            "column": 17
          },
          {
            "line": 93,
            "column": 16
          },
          {
            "line": 96,
            "column": 16
          },
          {
            "line": 103,
            "column": 17
          }
        ]
      },
      {
        "group_fingerprint": "865bdcd2aec82a0b068bdfd8b32d427350a37d6244b08570ba47e6a9d504df46",
        "path": "internal/cli/migrate.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `fmt.Fprintln` is not checked",
        "group_multiplicity": 3,
        "owned_count": 2,
        "owned_ordinals": [
          1,
          2
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 56,
            "column": 18
          },
          {
            "line": 83,
            "column": 17
          }
        ]
      },
      {
        "group_fingerprint": "0ac3ad39ef6de8512eb2da11728dc19d65b25592543ba752473b969c73a0b472",
        "path": "internal/cli/migrate.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `ws.Close` is not checked",
        "group_multiplicity": 2,
        "owned_count": 1,
        "owned_ordinals": [
          1
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 77,
            "column": 18
          }
        ]
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.041-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
