---
artifact_type: task
created_at: 2026-09-17T23:59:22.9623017Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.004-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U4 - errcheck remediation: internal/cli'
updated_at: 2026-09-18T00:54:31.0144733Z
---

Remediate ALL golangci-lint errcheck findings in internal/cli using the per-category strategy: named-return + errors.Join for a writable Close (defer func() { err = errors.Join(err, f.Close()) }()); the checked write/short-write helper for Write paths; justified `_ =` only for genuinely discardable returns (with an inline reason); `fmt.Errorf("...: %w", err)` for propagation (two-%w form where a shared sentinel policy classifies via errors.Is). No opportunistic cross-package helper (introduce a shared helper only when >=3 identical call sites exist AND via an explicit predecessor sub-unit).

Acceptance criteria:
- Scope (package/files): internal/cli only; edit only files within internal/cli that errcheck flags. Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order.
- Verification (zero-finding): `golangci-lint run ./internal/cli` reports 0 errcheck; `gofmt -l` on touched files is empty; `go build ./internal/cli` and existing internal/cli tests still pass (no regression).
- Existing test expectations: all current internal/cli tests continue to pass unchanged; success-path behavior preserved (fixes add capture/propagation only on failure paths).
- Test posture (Principle II): for any errcheck fix introducing genuinely NEW reachable failure-path behavior, add a failing (RED) test exercising the propagated error FIRST, then make it green. For purely mechanical captures with NO reachable behavior change, this task PRE-DECLARES (not retrospectively) a mechanical harness-exemption contract: the exempted call sites and the "no reachable behavior change" justification are recorded in the closure artifact at execution time.