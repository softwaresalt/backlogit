---
artifact_type: task
created_at: 2026-09-14T01:02:33.9338637Z
custom_fields:
    source_stash_id: 1E0C2251
dependencies:
    - 172.009-T
id: 172.005-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Apply CAS guard to RemoveArtifactLink via guarded without-DB-only-links persist
updated_at: 2026-09-14T01:02:33.9338637Z
---

U2.1 [1E0C2251]. Apply guardArchivedStatusUnchangedSince to RemoveArtifactLink (internal/core/artifacts.go:981), which persists via persistArtifactWithoutDBOnlyLinks (NOT persistArtifactWithGuard). Introduce a combined guarded + without-DB-only-links persist variant so BOTH stale-write guarding AND DB-only-link stripping are preserved. Snapshot preLockArchivedStatus before the findArtifact->persist window. Snapshot preLockArchivedStatus before the findArtifact->persist window; the snapshot AND the in-lock comparand MUST read archived_status from the Markdown source of truth via findArtifact, NOT the SQLite fast-path projection (loadArtifact/selectCols omits archived_status and returns zero, which would make the CAS compare zero==zero and admit the stale clobber). Do NOT route the early 'nothing removed' branch (direct db.RemoveLink, no artifact persist) through the guard. SCOPE (narrowed everywhere): this guard is deliberately narrowed to the archived_status field ONLY (the reconcile-to-shipped invariant); it is NOT a general whole-artifact stale-write guard. Any stale-write protection BEYOND archived_status must instead use revision/canonical-content CAS or reload-and-merge under lock (out of scope for this task). This Unit-2 work is INDEPENDENT of the lock-order Unit 1 (no dependency on U1.2); its only test-first predecessor is the U2.0 RED harness. Domain: code. Depends on U2.0 (172.009-T, RED harness written and observed failing first). AC: stale archived_status:shipped cannot be clobbered by RemoveArtifactLink; DB-only-link stripping still honored; early no-op branch unguarded; scope-narrowing to archived_status stated; the 172.009-T RED test covering RemoveArtifactLink transitions RED->GREEN.