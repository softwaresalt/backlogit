---
artifact_type: task
created_at: 2026-09-16T06:47:55.5023009Z
dependencies:
    - 174.044-T
id: 174.056-T
parent_id: 174-F
priority: high
status: active
title: 'SCOPE-RED-B: flat-manifest regression scenarios'
updated_at: 2026-09-20T23:28:20.4772793Z
---

Behavior RED **regression** harness for the flat-manifest edge cases (spec §0.5 / SBLK-R28; plan §0.4), kept tightly coupled to flat-manifest semantics. Compiles against current code and is RED until SCOPE-IMPL-2 (`174.058-T`) lands. Asserts BEHAVIOR only; no production change.

Scenarios (<4, exactly 3), each anchored to a surface `174.058-T` owns (member projection + ship/closure feature cleanup):
1. **Unlisted blocked descendant excluded from the projection + not orphaned on ship** — a descendant of a listed feature whose status is `blocked` and whose ID is NOT in the manifest is absent from `size_composition.members`, and shipping the manifest does NOT return-to-backlog / `clearParentID`-orphan it (`returnUnreleasedFeatureItems`). Membership is by explicit listing, not by status or hierarchy.
2. **Unlisted descendants excluded from the member projection** — descendants of a listed feature that are NOT in the manifest — including the `archived` `174.001-T…174.038-T` band archetype — are absent from `size_composition.members` (the projection lists explicit task members only, with no feature→child expansion). Archival is NOT the descoping mechanism — non-membership is. No archived/superseded task is restored to satisfy scope. (Ship/closure **archive-candidate** cleanup — the unlisted terminal-but-not-archived descendant and linked-deliberation expansions in `collectArchiveCandidateIDs` — is asserted separately by `174.060-T`.)
3. **Feature-only / zero-executable-task manifest** — a manifest containing only a covering feature (no listed task members) scopes to `{feature}`; ship/closure completes only listed members, completes/archives NO unlisted descendant, and **frees the single active slot** (P-001) rather than stranding it. This is the active-slot-safety case for zero executable tasks.

AC: (1) unlisted blocked descendant absent from `size_composition.members` and not orphaned on ship; (2) unlisted descendants (incl. archived) absent from the `size_composition.members` projection; archival not required to descope (ship/closure archive-candidate cleanup asserted in `174.060-T`); (3) feature-only manifest → scope == `{feature}` and the active slot is freed on ship/closure with no unlisted descendant completed/archived; (4) harness RED before `174.058-T`, GREEN after. Domain: tests. <4 scenarios.

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Behavior RED regression harness for flat-manifest edge cases (SBLK-R28); asserts unlisted blocked descendants and archived descendants are excluded from release scope and that a feature-only/zero-executable-task manifest scopes to the feature alone and frees the active slot on ship/closure. It compiles against current code and its TestUReleaseScopeRegression_ functions are RED while the projection/gate still expand hierarchy, turning green only after SCOPE-IMPL-2 (174.058-T).
red_selector_command: go test -count=1 -run '^TestUReleaseScopeRegression_' ./internal/core
green_maker_tasks: 174.058-T
green_maker_closes_wave: 9
```
<!-- END:red-deliverable-contract -->