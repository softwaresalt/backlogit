---
artifact_type: task
created_at: 2026-08-24T19:40:00.0000000Z
dependencies:
    - 147.005-T
id: 147.021-T
labels:
    - checkpoint
    - events
    - tests
    - plan-u2f
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U2f: protected invariant I1 — write-site enumeration or gated rewrite seam'
updated_at: 2026-08-24T20:30:00.0000000Z
---

Plan unit **U2f**. Land the executable form of protected invariant **I1**: every code path that rewrites an existing checkpoint file in place must be gated, not just the two named in the stash text.

Files: `internal/events/checkpoint_writesite_test.go` (new); under the fallback mechanism additionally `internal/events/checkpoint_lifecycle.go` (helper plus the `ResolveCheckpoint` call site), `internal/core/checkpoint_disposition.go` (the `AbandonCheckpoint` call site) and their two `_test.go` files.

Primary mechanism: a write-site enumeration test that walks the package sources for calls to `syncWriteFileAtomic` / `atomicfile.WriteFileAtomic` / `os.WriteFile` whose target resolves under the checkpoint directory, and asserts the resulting call-site set equals the allow-list audited in the plan. A new in-place rewrite path added later then fails that test rather than silently joining the ungated set.

Fallback if the enumeration proves impractical to implement reliably: a single **exported** gated helper `events.RewriteCheckpointFile(path string, cp *CheckpointV1, original []byte) error` that runs both gates internally and is the **only** function permitted to re-marshal an existing checkpoint. Either mechanism satisfies I1; a comment does not.

**The export is mandatory, not stylistic.** The seam has two consumers in two packages: `ResolveCheckpoint` in `internal/events` (147.007-T / U3b) and `AbandonCheckpoint` in `internal/core` (147.008-T / U4, `internal/core/checkpoint_disposition.go:~110-125`, today calling `atomicfile.WriteFileAtomic` directly inside its `MutationEnvelope`). Go cannot reference an unexported identifier across a package boundary, so an unexported `rewriteCheckpointFile` in `internal/events` would leave U4's rewrite permanently ungated — the exact hole I1 exists to close. If the fallback is chosen, the enumeration allow-list must also cover `internal/core`.

**Why this is separate from 147.005-T.** The fallback is a **production seam** that 147.007-T (U3b) and 147.008-T (U4) must route through, so the mechanism decision is taken here and consumed there. Bundling it into 147.005-T would put a possible production change inside a unit that declares itself green on landing with no red phase, and would take that unit to four scenarios across two skill domains.

Record the chosen mechanism in the task log before 147.007-T or 147.008-T starts.

<!-- BEGIN:acceptance-criteria -->
- The enumerated in-place rewrite call-site set equals the plan's audited allow-list
- A synthetic ungated rewrite site fails the assertion (under the fallback: `events.RewriteCheckpointFile` refuses a non-conforming document)
- Under the fallback the helper is exported and both call sites — `internal/events` and `internal/core` — route through it
- The chosen mechanism is recorded before 147.007-T or 147.008-T begins
- Expected red: case 2 fails until the chosen mechanism exists
<!-- END:acceptance-criteria -->