---
artifact_type: task
created_at: 2026-09-14T01:03:00.0883681Z
dependencies:
    - 172.009-T
    - 172.011-T
    - 172.012-T
id: 172.006-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Apply CAS guard to BulkUpdateStatus with per-item snapshot
updated_at: 2026-09-14T01:48:42.8097791Z
---

U2.2 [1E0C2251]. Apply guardArchivedStatusUnchangedSince (archived_status stale-write CAS only) to BulkUpdateStatus (internal/core/queue.go:404) via persistArtifactWithGuard with a per-item pre-lock preLockArchivedStatus snapshot read from the Markdown source of truth via findArtifact (NOT the SQLite fast-path projection loadArtifact/selectCols, which omits archived_status and returns zero — a DB-projection snapshot would compare zero==zero and admit the stale clobber). Behavior change to assert (BACKWARD-COMPATIBLE + ADDITIVE): the existing `BulkUpdateResult.Failed []string` field is PRESERVED and continues to be populated for every failed item (no signature change, no removal, no retype), so existing callers keep compiling and behaving unchanged; a per-item stale-write is ADDITIONALLY recorded in a NEW additive typed detail surface on BulkUpdateResult (e.g. `FailedDetails []BulkUpdateConflict{ID, Err, FromStatus, ToStatus}`) carrying the item id and the conflicting status transition, so NEW callers can distinguish a stale-write conflict from other per-item failures. The typed detail is added ALONGSIDE `Failed []string`, NOT as a replacement; never aborts or silently clobbers the batch. The BulkUpdateResult source shape and the additive typed-field design are declared first by the predecessor 172.011-T, and caller compatibility/adaptation is completed by 172.012-T, both BEFORE this behavior implementation. INDEPENDENT of the lock-order unit (no dependency on U1.x). Domain: code. Depends on U2.0 (172.009-T RED harness), 172.011-T (source-shape declaration), 172.012-T (caller compatibility). AC: concurrent BulkUpdateStatus cannot clobber archived_status:shipped; `Failed []string` remains populated and backward-compatible; per-item conflicts are ADDITIONALLY recorded in the new additive typed detail surface (additive, not a replacement).