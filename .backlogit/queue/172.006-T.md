---
artifact_type: task
created_at: 2026-09-14T01:03:00.0883681Z
dependencies:
    - 172.009-T
id: 172.006-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Apply CAS guard to BulkUpdateStatus with per-item snapshot
updated_at: 2026-09-14T01:48:42.8097791Z
---

U2.2 [1E0C2251]. Apply guardArchivedStatusUnchangedSince (archived_status stale-write CAS only) to BulkUpdateStatus (internal/core/queue.go:404) via persistArtifactWithGuard with a per-item pre-lock preLockArchivedStatus snapshot read from the Markdown source of truth via findArtifact (NOT the SQLite fast-path projection loadArtifact/selectCols, which omits archived_status and returns zero — a DB-projection snapshot would compare zero==zero and admit the stale clobber). Behavior change to assert: a per-item stale-write surfaces as a TYPED per-item conflict entry carrying the item id and the conflicting status transition (structured BulkUpdateResult failure entry, e.g. {ID, Err, FromStatus, ToStatus}), NOT a bare []string, so callers can distinguish a stale-write conflict from other per-item failures; never aborts or silently clobbers the batch. INDEPENDENT of the lock-order unit (no dependency on U1.x). Domain: code. Depends on U2.0 (RED harness). AC: concurrent BulkUpdateStatus cannot clobber archived_status:shipped; per-item conflicts recorded as typed entries.