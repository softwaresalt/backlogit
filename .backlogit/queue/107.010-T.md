---
artifact_type: task
created_at: 2026-07-15T20:40:49.7934785Z
id: 107.010-T
labels:
    - go
    - docline
    - test-first
    - extension
    - integration
    - red
parent_id: 107-F
priority: low
references:
    - tests/integration/docline_extension_compat_test.go
    - internal/docline/normalize.go
    - internal/docline/frontmatter.go
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
status: queued
title: 'RED-first integration regression: prove docline preserves unknown top-level extension keys (fails at HEAD)'
updated_at: 2026-07-15T20:40:49.7934785Z
---

RED-first integration regression (test-only, <=2h, width-isolated to tests/integration/docline_extension_compat_test.go; introduced for the PR #241 correction). Writes the FAILING integration assertions FIRST, before the 107.009-T production pass-through seam change. Proves representative backlogit-owned top-level extension keys (size, size_source, size_ruleset_version) added to an in-scope doc: (a) stay TOP-LEVEL and byte-unchanged through docline Normalize (never relocated under the docline namespace and never dropped during serialization); (b) are never defaulted, rewritten, validated, or emitted by docline; (c) survive a docs lint/migration round-trip without body mutation; (d) keep the transform idempotent (Normalize(Normalize(x)) == Normalize(x)). Against current HEAD these assertions FAIL for TWO reasons that the plan must state honestly: internal/docline/normalize.go folds every non-contract top-level key under the docline namespace via foldUnderDocline, AND internal/docline/frontmatter.go BaseFrontmatter.ToMap() only emits base-contract fields plus the docline namespace, so any unknown top-level key is dropped on serialization. This RED task captures that dual failure as executable evidence and is the dependency gate for the 107.009-T production seam change that turns it GREEN. Test-only: no production code changes here. Ownership stays explicit: top-level pass-through is DOCLINE base-contract behavior; size validation, defaults, provenance, and composition remain BACKLOGIT-owned and out of scope. Decoupled from formal-gate governance and the size spike (096-S).