---
artifact_type: task
created_at: 2026-07-15T20:40:49.7934785Z
dependencies:
    - 107.001-T
id: 107.010-T
labels:
    - go
    - docline
    - test-first
    - extension
    - integration
    - red
parent_id: 107-F
priority: low
references:
    - tests/integration/docline_extension_compat_test.go
    - internal/docline/normalize.go
    - internal/docline/frontmatter.go
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
status: queued
title: 'RED-first integration regression: prove docline preserves unknown top-level extension keys (fails at HEAD)'
updated_at: 2026-07-16T06:46:21.497281Z
---

RED-first integration regression (test-only, <=2h, width-isolated to tests/integration/docline_extension_compat_test.go; introduced for the PR #241 correction). This task DEPENDS ON 107.001-T so the live Git-tracked corpus RED (107.001-T) is observed FIRST; the extension assertions here are then observed failing, and both precede the 107.009-T production pass-through seam change. Writes the FAILING integration assertions before that seam change. Proves representative backlogit-owned top-level extension keys (size, size_source, size_ruleset_version) added to an in-scope doc: (a) stay TOP-LEVEL and remain SEMANTICALLY/deep-value equivalent through docline Normalize - the extension key/value graph is preserved at the top level and the document body bytes remain unchanged; docline reserializes YAML via the frontmatter codec (mdfront.Encode), which MAY canonicalize key ordering/quotes/scalar spelling/comments/anchors, so this task asserts semantic/deep-value equivalence, top-level placement, unchanged body bytes, and idempotent normalization, NOT raw frontmatter byte/lexical preservation (no raw-byte guarantee unless a future YAML-node/raw-byte design is explicitly chosen); (b) are preserved and re-emitted in place by docline as a semantically/deep-value equivalent key/value graph but never defaulted, rewritten in value, synthesized, validated, or semantically interpreted by docline; (c) survive a docs lint/migration round-trip without body mutation; (d) keep the transform idempotent (Normalize(Normalize(x)) == Normalize(x)). CORRECTED CURRENT-HEAD BEHAVIOR (honest causal sequence, replacing the earlier dropped-on-serialization claim): at current HEAD these assertions FAIL because internal/docline/normalize.go folds every unknown (non-contract, non-docline) top-level key INTO the docline namespace via foldUnderDocline, and internal/docline/frontmatter.go FromMap/ToMap PRESERVE that nested docline map (ToMap emits the docline key when non-empty). The extension values are therefore RELOCATED under docline and round-trip intact - they are NOT currently dropped. The failing assertion at HEAD is that the keys no longer stay TOP-LEVEL (Normalize moves them under docline). The absence of a top-level carrier on BaseFrontmatter is a LATENT drop, not a current one: it would occur ONLY if the fold in normalize.go were removed WITHOUT also adding the top-level carrier in frontmatter.go, because ToMap then emits only base-contract fields plus docline. This RED task captures the current relocation failure plus the latent-drop hazard as executable evidence and is the dependency gate for the 107.009-T production seam change (which must both stop the fold AND add the frontmatter.go carrier, coupled, to turn it GREEN). (e) FORWARD-ONLY / MIGRATION EVIDENCE (mandatory before asserting no migration is needed): inventory every git-tracked in-Scope doc for already-folded producer keys docline.size, docline.size_source, and docline.size_ruleset_version and record either ZERO matches (forward-only, no back-migration required) or a bounded migration requirement enumerating exact paths. Stage inventory at authoring time (git grep over docs, README.md, AGENTS.md) found ZERO tracked matches, so this task asserts a forward-only contract with no back-migration and RE-VERIFIES zero at execution. The one canonical derived-key location is TOP-LEVEL (size/size_source/size_ruleset_version at the top level, never docline.size*). Test-only: no production code changes here. Ownership stays explicit: top-level pass-through is DOCLINE base-contract behavior; size validation, defaults, provenance, and composition remain BACKLOGIT-owned (validated by backlogit's future derived contract, not docline) and out of scope. Decoupled from formal-gate governance and the size spike (096-S).