---
artifact_type: task
created_at: 2026-09-05T04:04:24.0933888Z
dependencies:
    - 156.004-T
id: 156.006-T
parent_id: 156-F
priority: high
status: queued
title: Fault-line evidence contract BEHAVIOR - canonical/decode/validate + golden (U4a-behavior)
updated_at: 2026-09-05T05:22:20.4533757Z
---

U4a-behavior (deps 156.004-T). Canonical()=thin wrapper over internal/canonical.Canonicalize via canonicalMap() (envelope struct->map[string]any MERGING the registered family payload CanonicalMap() under verified_evidence; integers-only per gateevidence.asInt; non-nil collections). DecodeAndValidate([]byte)(EvidenceArtifact,FamilyPayload,error): (0) BOUNDED DECODE (SEC-01) enforce MaxArtifactBytes/MaxJSONDepth + member/array/string budgets BEFORE token scan, exceed=>ErrMalformed(resource-limit); (1) RAW json.Decoder token-stream pre-scan over envelope AND nested verified_evidence rejecting duplicate members (case-sensitive OR case-fold-equal) and case-variant aliases before Unmarshal (encoding/json alone does last-value-wins + case-insensitive matching, so DisallowUnknownFields does NOT catch these); (2) two-phase peek schema_version outside knownVersions->typed error; (3) DisallowUnknownFields + io.EOF trailing-data; (4) family lookup by (SchemaVersion,NodeFamily) via version-aware registry->concrete payload; (5) envelope Validate(a,p) + family p.Validate() (Status-referencing CROSS-FIELD STATE MATRIX enforced at envelope level: ExpectedDivergence=true=>report_only; pass=>no divergent fields+ExpectedDivergence=false; report_only=>ExpectedDivergence=true+TrackedDefect+DivergentFields; fail=>DivergentFields non-empty+ExpectedDivergence=false). TYPED ERRORS: ErrMalformed (dup-key/case/DisallowUnknownFields/trailing), ErrUnknownVersion, ErrUnknownFamily, ErrValidation - so Classify (U4b) maps outcome 3 vs 4 deterministically via errors.Is. Conformance covers the state matrix (contradictory pass/report_only/fail combos) + each typed-error path + per-family raw-bytes golden. Skew: any envelope/payload change bumps version; N=1 accepts only v1 (v0/absent rejected); {N,N-1} at v2 via version-specific decoders; new golden PER FAMILY per version. Acceptance: Canonical byte-stable vs CHECKED-IN raw-bytes per-family GOLDEN (not round-trip DeepEqual); empty collections present-and-array (.([]any)); unknown/unstamped/trailing-data/duplicate-key/case-variant/unknown-family/oversized/deep-nested/contradictory-state fails closed typed; REGISTRY freeze conformance (duplicate/nil/post-freeze rejected). Files: internal/faultline/evidence.go + goldens + conformance. Domain: code.