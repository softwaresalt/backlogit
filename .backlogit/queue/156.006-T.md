---
artifact_type: task
created_at: 2026-09-05T04:04:24.0933888Z
dependencies:
    - 156.004-T
id: 156.006-T
parent_id: 156-F
priority: high
status: queued
title: Fault-line evidence contract BEHAVIOR - canonical/decode/validate + golden (U4a-behavior)
updated_at: 2026-09-05T23:33:37.7617008Z
---

U4a-behavior (deps 156.004-T). LANDS the concrete function BODIES for the 156.004-T contract (Canonical, DecodeAndValidate, Validate, RegisterFamily, knownFamilies, freeze - all require bodies so cannot live in the declaration task). Canonical()=thin wrapper over internal/canonical.Canonicalize via canonicalMap() (envelope struct->map[string]any MERGING the registered family payload CanonicalMap() under verified_evidence; integers-only per gateevidence.asInt; non-nil collections; SET-LIKE fields Applicability/Surfaces/DivergentFields normalized SORTED+DUP-FREE before canonicalize - Canonicalize preserves array order so unsorted sets would yield different digests and bypass the digest-keyed anti-replay; Validate also rejects duplicate set elements). DecodeAndValidate([]byte)(EvidenceArtifact,FamilyPayload,error): (0) BOUNDED DECODE (SEC-01) enforce MaxArtifactBytes/MaxJSONDepth + member/array/string budgets BEFORE token scan, exceed=>ErrMalformed(resource-limit); (1) RAW json.Decoder token-stream pre-scan: reject duplicate members (case-sensitive OR case-fold-equal, family-agnostic over envelope AND nested verified_evidence) + validate ENVELOPE exact-casing; nested verified_evidence keys are family/version-specific so their exact casing is validated AFTER family selection (step 4), not here (encoding/json alone does last-value-wins + case-insensitive matching, so DisallowUnknownFields does NOT catch these); (2) two-phase peek schema_version outside knownVersions->typed error; (3) DisallowUnknownFields + io.EOF trailing-data; (4) family lookup by (SchemaVersion,NodeFamily) via version-aware FROZEN registry->concrete payload type, THEN validate nested verified_evidence exact-casing against THAT family's declared tags, then decode FROM verified_evidence bytes; (5) Validate(a) (derives p from a.VerifiedEvidence - no separate-payload mismatch): envelope checks + family p.Validate(view EvidenceView) where view carries Status/Applicability/SchemaVersion/NodeFamily so the family enforces its OWN CROSS-FIELD STATE MATRIX (ExpectedDivergence=>report_only; pass=>no divergent fields+ExpectedDivergence=false; report_only=>ExpectedDivergence+TrackedDefect+DivergentFields; fail=>DivergentFields non-empty+ExpectedDivergence=false). TYPED ERRORS: ErrMalformed (dup-key/case/DisallowUnknownFields/trailing/bounded-decode), ErrUnknownVersion, ErrUnknownFamily, ErrValidation - so Classify (U4b) maps outcome 3 vs 4 deterministically via errors.Is. REGISTRY: RegisterFamily(...) error init-only (duplicate/nil/post-freeze->error, no last-writer-wins); FOUNDATION-OWNED per-version family MANIFEST (static map[int][]string); freeze() triggered by first of DecodeAndValidate/Canonical/Validate/knownFamilies, validates registered factories match manifest EXACTLY (missing/extra=>fail closed); ALL readers (incl knownFamilies+Validate) read frozen snapshot only (register-vs-read -race clean). Conformance covers the state matrix (contradictory pass/report_only/fail combos) + each typed-error path + TrackedDefect canonical-ID format (free-form/invalid rejected; S4 expected-divergence fixture pins TrackedDefect==166-F) + per-family raw-bytes golden + freeze (duplicate/nil/post-freeze/race). Skew: any envelope/payload change OR family-set change bumps version; each version has a CLOSED family set (v1={parity}); N=1 accepts only v1 (v0/absent rejected); {N,N-1} at v2 via version-specific decoders; new golden PER FAMILY per version. Acceptance: Canonical byte-stable vs CHECKED-IN raw-bytes per-family GOLDEN (not round-trip DeepEqual); empty collections present-and-array (.([]any)); unknown/unstamped/trailing-data/duplicate-key/case-variant/unknown-family/oversized/deep-nested/contradictory-state fails closed typed; REGISTRY freeze conformance (duplicate/nil/post-freeze rejected + manifest missing/extra rejected + race clean). Files: internal/faultline/evidence.go + goldens + conformance. Domain: code.