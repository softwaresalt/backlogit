---
artifact_type: task
created_at: 2026-09-14T03:40:00Z
id: 172.014-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: 'RED: AST harness asserts additive BulkUpdateResult typed conflict field'
updated_at: 2026-09-14T03:40:00Z
---

U2.decl-red [1E0C2251] source-shape AST RED harness (test-first predecessor to the production field declaration 172.011-T). Write a failing source-shape assertion (AST / type-level harness) that asserts the ADDITIVE typed conflict-detail surface exists on BulkUpdateResult ALONGSIDE the preserved `Failed []string`: a NEW `FailedDetails []BulkUpdateConflict` field whose element type `BulkUpdateConflict` exposes `{ID, Err, FromStatus, ToStatus}`, AND that `Failed []string` is UNCHANGED (still present, same name and type). Observe RED: the field and type do not yet exist, so the harness fails (compile-time absence or reflective/AST assertion failure) BEFORE the production field declaration (172.011-T) lands and turns it GREEN. This is a source-shape harness only; it does NOT assert BulkUpdateStatus populate behavior (that is the 172.009-T stale-write RED harness and the 172.010-T GREEN regression). Domain: tests. No deps (predecessor). <=2 scenarios. AC: the harness asserts (1) `BulkUpdateResult.FailedDetails []BulkUpdateConflict{ID,Err,FromStatus,ToStatus}` is present and (2) `Failed []string` is preserved unchanged; the harness is observed RED before 172.011-T lands.
