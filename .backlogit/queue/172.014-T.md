---
artifact_type: task
created_at: 2026-09-14T03:40:00Z
id: 172.014-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: 'RED: AST harness asserts additive BulkUpdateResult typed conflict field'
updated_at: 2026-09-14T03:40:00Z
---

U2.decl-red [1E0C2251] source-shape AST RED harness (test-first predecessor to the production field declaration 172.011-T). Write a failing source-shape assertion using a **source-only `go/parser` + `go/ast` harness that reads the production source text as data** (parses the relevant `.go` files into an AST and inspects the declarations) that asserts the ADDITIVE typed conflict-detail surface exists on BulkUpdateResult ALONGSIDE the preserved `Failed []string`: a NEW `FailedDetails []BulkUpdateConflict` field whose element type `BulkUpdateConflict` exposes `{ID, Err, FromStatus, ToStatus}`, AND that `Failed []string` is UNCHANGED (still present, same name and type). The harness MUST compile and run WITHOUT importing or referring to the not-yet-declared production symbols (`BulkUpdateResult.FailedDetails`, `BulkUpdateConflict`) — it operates on the parsed AST, not on type/reflect references — so it builds cleanly before 172.011-T lands. Observe RED as an **AST-assertion failure ONLY** (the parsed declarations do not yet contain the additive field/type): RED MUST NOT be a compile-time / missing-symbol build failure. The production field declaration (172.011-T) then lands the additive field/type and turns the AST assertion GREEN. This is a source-shape harness only; it does NOT assert BulkUpdateStatus populate behavior (that is the 172.009-T stale-write RED harness and the 172.010-T GREEN regression). Domain: tests. No deps (predecessor). <=2 scenarios. AC: the harness compiles and runs before 172.011-T; it parses the production source via go/parser/go/ast and asserts (1) `BulkUpdateResult.FailedDetails []BulkUpdateConflict{ID,Err,FromStatus,ToStatus}` is present and (2) `Failed []string` is preserved unchanged; the harness is observed RED by AST assertion failure (never a compile failure) before 172.011-T lands.
