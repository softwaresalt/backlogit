---
artifact_type: task
created_at: 2026-09-14T03:17:00Z
dependencies:
    - 172.001-T
    - 172.015-T
id: 172.013-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Declare no-op B/C barrier instrumentation seam in production
updated_at: 2026-09-14T03:40:00Z
---

U1.seam [FE440C62] NO-OP PRODUCTION seam declaration (makes the 172.015-T AST RED harness GREEN). Add the deterministic B/C lock-barrier instrumentation seam to production: expose injectable, controllable barrier hook points at BOTH the B (`lockArtifactMutations`) AND the C (item-log JSONL append) acquisition points in the artifact-mutation writers (per the 172.001-T canonical-order analysis), CENTRALIZED at the shared `lockArtifactMutations` entry and the shared item-log append helper (keeping the change to <=3 files, not duplicated per writer). The existing pre-B `persistArtifactPreLockHook` is INSUFFICIENT for two reasons: it fires BEFORE lock acquisition and so cannot hold a barrier AT the acquisition point, and NO C-acquisition seam exists at all; add a controllable hook AT the C acquisition point (and the paired controllable hook AT the B acquisition point) so a test can force the AB-BA interleaving DETERMINISTICALLY rather than relying on probabilistic `go test -race` repeats. Pin the seam's shape: where each hook injects, that it is enabled ONLY under test (off / no-op on production paths so NO production behavior changes), and how it preserves the causal item-log event-ordering invariant. The hooks are NO-OP by default; this task adds the seam ONLY and does NOT force any interleaving and does NOT change lock order (the barrier-driven RED harness 172.008-T drives the seam; the implementation 172.002-T/172.003-T changes the order). It turns the 172.015-T source-shape AST RED harness GREEN. This seam is the source-of-truth consumed by the barrier-driven RED harness (172.008-T), the lock-order implementation (172.002-T/172.003-T), and its GREEN regression (172.004-T). Domain: code. Depends on 172.001-T (canonical-order analysis) and 172.015-T (AST RED harness observed failing first). AC: production writers expose no-op controllable barrier hooks at BOTH the B and C acquisition points, enabled only under test with no production behavior change and causal item-log ordering preserved; the 172.015-T AST harness transitions RED->GREEN; no lock-order change in this task.
