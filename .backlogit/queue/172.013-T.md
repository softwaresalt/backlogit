---
artifact_type: task
created_at: 2026-09-14T03:17:00Z
dependencies:
    - 172.001-T
id: 172.013-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Declare deterministic B/C barrier instrumentation seam (source shape)
updated_at: 2026-09-14T03:17:00Z
---

U1.seam [FE440C62] source-shape/declaration (predecessor to the RED harness). Declare the deterministic B/C lock-barrier instrumentation seam: specify the injectable, controllable barrier hook at the B (lockArtifactMutations) and C (item-log JSONL append) acquisition points in the artifact-mutation writers (per the 172.001-T canonical-order analysis) that lets a test force the AB-BA interleaving DETERMINISTICALLY rather than relying on probabilistic `go test -race` repeats. Pin the seam's shape: where the barrier injects, how it is enabled ONLY under test (off / no-op on production paths so no production behavior changes), and how it preserves the causal item-log event-ordering invariant. This declaration is the source-of-truth seam consumed by the RED harness (172.008-T) and by the lock-order implementation (172.002-T / 172.003-T) and its GREEN regression (172.004-T). The RED harness depends on this declaration, and the implementation follows the RED evidence. Domain: docs/analysis. Depends on 172.001-T. AC: a declaration note specifies the barrier-injection points, the test-only enablement (no production behavior change), and the causal-ordering preservation; 172.008-T (RED harness) is declared to depend on this seam and the lock-order implementation follows the observed-RED evidence.
