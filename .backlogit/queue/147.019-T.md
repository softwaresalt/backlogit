---
artifact_type: task
created_at: 2026-08-24T19:06:01.0567287Z
dependencies:
    - 147.018-T
id: 147.019-T
labels:
    - checkpoint
    - verification
    - plan-u10
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U10: runtime verification in a scratch workspace (live checkpoints untouched)'
updated_at: 2026-08-24T20:30:00.0000000Z
---

Plan unit **U10**. Runtime verification in a scratch workspace.

Files: `.gitignore` (ignore rule for the scratch directory). No product code changes.

**Scratch containment (Constitution IV).** The scratch workspace is created **inside the repository working tree** at `docs/scratch/checkpoint-verification/` — never `%TEMP%`, never outside the cwd. Assert the resolved path is repo-root-relative **before the first write**, add the directory to the freeze-scope declaration, and add the `.gitignore` rule: there is no `docs/scratch/` rule today, and while `*.exe` already covers the built binary the copied fixtures are not covered.

**Binary under test.** Build from the branch HEAD — `go build -o docs/scratch/checkpoint-verification/backlogit-verify.exe ./cmd/backlogit` — and exercise that binary. The pinned repo-root `backlogit.exe` predates the change and must not be used.

The nine live legacy checkpoints under `.backlogit/checkpoints/` MUST NOT be used as verification fixtures and MUST NOT be mutated. Copy representative bytes into the scratch workspace instead.

Verify end to end:
1. `list` reports `needs_quarantine: true` with a paste-runnable, PowerShell-safe remediation command
2. `get` reports `conforming: false`
3. `resolve` refuses; `abandon` refuses
4. `quarantine` accepts and archives the bytes verbatim (byte-identical comparison)
5. the body-preserving repair path — entry point (b) of the single 147.018-T procedure: rename the archived bytes **and** their `.disposition.json` sidecar aside to `<filename>.quarantined-<disposition_at>` first, copy the preserved bytes back into the now-free active filename, hand-repair in place by moving the unmodeled top-level keys under `context.legacy_top_level`, confirm `conforming: true`, then `resolve` succeeds

Scratch teardown is `ActionRisk: destructive` (A4b) and requires operator approval; if approval is withheld the directory stays in place and is recorded as a cleanup follow-up. This is the documented Principle VII deviation recorded in the plan's Constitution Check.

<!-- BEGIN:acceptance-criteria -->
- The scratch path resolves under the repository root and the assertion runs before the first write
- The `.gitignore` rule for `docs/scratch/checkpoint-verification/` is added by this task
- The binary under test is built from the branch HEAD, not the pinned repo-root `backlogit.exe`
- All five verification steps pass in the scratch workspace
- **Every** file under `.backlogit/checkpoints/` is byte-unchanged, verified by enumerating the directory and comparing SHA-256 hashes before and after — not a count-pinned subset
- A session-start recovery sweep refuses **only** the nine enumerated legacy filenames and succeeds on every other file in that directory
- The quarantined archive copy is byte-identical to the pre-quarantine original
- At no point during the restore does one filename exist simultaneously under `.backlogit/checkpoints/` and `archive/checkpoints/`
- The renamed quarantined evidence pair (bytes plus `.disposition.json` sidecar) still exists after the restore and repair complete, and its bytes are byte-identical to the pre-restore archive copy
- The restored file recovers the original top-level values under `context.legacy_top_level` and keeps its original filename and `created_at`
<!-- END:acceptance-criteria -->