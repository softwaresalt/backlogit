---
artifact_type: task
created_at: 2026-08-24T19:06:01.0567287Z
dependencies:
    - 147.018-T
id: 147.019-T
labels:
    - checkpoint
    - verification
    - plan-u10
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U10: runtime verification in a scratch workspace (live checkpoints untouched)'
updated_at: 2026-08-24T19:06:01.0567287Z
---

Plan unit **U10**. Runtime verification in a scratch workspace.

Files: none in the repository tree — verification is performed against a **scratch workspace outside `.backlogit/`**.

The nine live legacy checkpoints under `.backlogit/checkpoints/` MUST NOT be used as verification fixtures and MUST NOT be mutated. Copy representative bytes into the scratch workspace instead.

Verify end to end:
1. `list` reports `needs_quarantine: true` with a paste-runnable remediation command
2. `get` reports `conforming: false`
3. `resolve` refuses; `abandon` refuses
4. `quarantine` accepts and archives the bytes verbatim (byte-identical comparison)
5. the body-preserving repair path (quarantine → create fresh conforming checkpoint with recovered fields under `context`) works as documented in U9b

Scratch teardown is a documented Principle VII deviation recorded in the plan's Constitution Check.

<!-- BEGIN:acceptance-criteria -->
- All five verification steps pass in a scratch workspace
- The nine live checkpoints under `.backlogit/checkpoints/` are byte-unchanged (verified by hash before and after)
- The quarantined archive copy is byte-identical to the pre-quarantine original
- The repair path recovers the original top-level values under `context`
<!-- END:acceptance-criteria -->