---
artifact_type: task
created_at: 2026-09-20T05:05:00.0000000Z
dependencies: []
id: 175.099-T
parent_id: 175-F
priority: high
references:
    - docs/decisions/2026-09-19-baseline-convergence-decomposition.md
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'RS-W(-1) prerequisite: bootstrap shared task-lint runner (runner-bootstrap chain source, STRICT PREDECESSOR of U1)'
updated_at: 2026-09-20T12:10:00.0000000Z
---

Runner-bootstrap PREREQUISITE member of the baseline-convergence feature `175-F` and the in-degree-zero SOURCE of the runner-bootstrap chain (`175.099-T` -> `175.100-T` -> `175.101-T`). It OWNS creation of exactly one shared runner, the canonical per-task runner `scripts/verify-task-lint.ps1`, which every baseline-remediation member's `<!-- BEGIN:task-lint-contract -->` block invokes. Single-domain (verification tooling: one PowerShell runner plus one Go harness). This task carries no bootstrap dependency; the baseline-lint runner-bootstrap task `175.100-T` depends on it.

## Why this task exists (bootstrap split, PR #449 cycle 9)

The prior aggregate bootstrap task owned three independent runners plus one harness (four files) and a seven-plus-scenario behavioral matrix, exceeding the repository task-granularity contract (fewer than 3 files / fewer than 4 scenarios) and concentrating the gate for all downstream shipments in one task. It is split into three dependency-ordered runner-specific tasks, one per runner, each owning at most two files and a bounded (at most three) behavioral scenario matrix. This member owns the per-task runner only.

## Owned deliverable surface (created by Ship during execution; NOT implemented in this planning PR)

- `scripts/verify-task-lint.ps1` — the canonical short per-task runner invoked by all 98 remediation members' `task_lint_cmd` (`-TaskId <id> -FeatureId 175-F`, with `-Phase Schema|PreCommit|PostCommit` lifecycle screens).
- `tests/runner_bootstrap_task_lint_175_099_test.go` — the EXCLUSIVELY OWNED Go harness (`TestU175_099_TaskLintRunnerBootstrap`, deterministic `TestU175_099_` token; no other member uses this path or prefix).

These are the ONLY two files this task may create (one implementation runner plus one owned harness). It creates NO Go source, NO workflow, and modifies NO existing script.

## Bootstrap harness (self-contained, non-vacuous; RED before / GREEN after)

BOOTSTRAP INVARIANT: the task-lint GATE for this member is the owned Go harness invoked as `go test ./tests -run ^TestU175_099_`, NEVER a direct invocation of `scripts/verify-task-lint.ps1` — using the runner this task CREATES as its own gate would be a circular self-dependency. The gate MUST COMPILE and its targeted test MUST EXECUTE under existing main-branch tooling BEFORE the runner exists: with the runner absent the harness COMPILES cleanly but its assertions FAIL when run, so `go test ./tests -run ^TestU175_099_` exits NONZERO (the expected RED phase). Only after `scripts/verify-task-lint.ps1` is implemented correctly does the same targeted test pass GREEN (exit 0). Compile success never implies test success.

The harness proves the following using ONLY existing main-branch tooling (`go test` + the built-in PowerShell 7 AST parser and fixture-driven runner invocation).

STATIC layer (files-present proof; RED before the runner is created):

1. `scripts/verify-task-lint.ps1` exists as a tracked, non-ignored, non-empty file under `scripts/`.
2. It PARSES CLEANLY under the built-in PowerShell AST parser (`[System.Management.Automation.Language.Parser]::ParseFile`) with zero parse errors.
3. It declares its required `param(...)` surface (AST param-block inspection): `TaskId` and `FeatureId`.

BEHAVIORAL layer (at most three scenario classes; consolidated table-driven; non-vacuous). After the runner exists the harness EXERCISES `verify-task-lint.ps1` (`-TaskId <id> -FeatureId 175-F`, `-Phase Schema|PreCommit|PostCommit`) as a controlled fixture-driven subject under the `line-stable-group-multiset/v1` identity model (ownership keys on `(path, linter, normalized_message)` fingerprint plus multiplicity, never on `line`/`column`):

- `green_success`: a compliant owned-target fixture for `<id>` -> native exit 0 and the runner's success marker.
- `red_owned_residual` (table-driven RED matrix): a retained owned lint finding; a line-stable shift-safety case where a still-owned occurrence shifts to a different line but owned residual stays above `expected_group_residual_after_task`; and an interchangeable-duplicate isolation case where a strictly-later slice's occupancy is resolved so owned residual falls below expected -> each native NONZERO, no success marker.
- `fail_closed_input` (table-driven fail-closed matrix): missing/empty `-TaskId`, an unknown task id, or a missing `-FeatureId` -> native NONZERO.

Non-vacuity evidence: `green_success` and `red_owned_residual` fixtures MUST yield DIFFERENT exit codes, so a no-op runner that only parses and declares params is rejected.

## Independence and safety posture

- INDEPENDENTLY COMPILABLE (RED before / GREEN after): the harness compiles under existing main-branch tooling with no dependency on the runner pre-existing (`go build ./...` and `go vet ./tests` succeed and the `TestU175_099_` binary builds); test execution is RED (nonzero) before the runner exists and GREEN after.
- NO RETROFIT of YAML BDD: this task authors one imperative PowerShell runner and one Go harness only; it introduces no BDD product spec and applies none of the deferred BDD scope.
- Governed-claim posture: claiming the parent shipment `176-S` runs `core.ClaimShipment` (`internal/core/shipment_lifecycle.go:46-98`), which activates the shipment AND every queued member and cascades status to the feature. The governed claim therefore dirties, under `.backlogit/`, the FULL claim footprint: the shipment manifest `.backlogit/queue/176-S.md`; every member status artifact `.backlogit/queue/175.099-T.md`, `.backlogit/queue/175.100-T.md`, `.backlogit/queue/175.101-T.md` (not this task alone); the feature `.backlogit/queue/175-F.md` (status cascade); and the append-only event/hook journals `.backlogit/hooks_queue.jsonl` and the events JSONL. All of these are outside the owned `scripts/` and `tests/` surface. FAIL closed on any dirty path outside the CLOSED union {the governed-claim footprint above} ∪ {`scripts/verify-task-lint.ps1`, `tests/runner_bootstrap_task_lint_175_099_test.go`}. NOTE: this shipment is not claimable for wave execution until the shipment-claim / wave-scheduler convergence prerequisite lands (readiness BLOCKED; see `docs/decisions/2026-09-20-shipment-claim-wave-scheduler-convergence-deliberation.md`).

## Acceptance criteria

- `scripts/verify-task-lint.ps1` exists as a tracked, non-ignored, non-empty file, and `tests/runner_bootstrap_task_lint_175_099_test.go` declares `TestU175_099_TaskLintRunnerBootstrap`.
- The EXECUTABLE non-vacuity wrapper (package `./tests`, anchored `^TestU175_099_`) is RED before the runner exists and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_099_` line; propagates native nonzero exit).
- Static layer: the harness proves the runner exists, is tracked, is non-empty, parses cleanly under the built-in PowerShell AST parser, and declares its required `param(...)` surface (`TaskId`, `FeatureId`).
- Behavioral layer (at most three scenario classes, non-vacuous): `green_success`, `red_owned_residual`, and `fail_closed_input` as above, with the success and failure fixtures yielding DIFFERENT exit codes.
- The GATE never becomes circular: the task-lint command runs only the Go harness (`go test ./tests -run ^TestU175_099_`) and is never a direct invocation of `scripts/verify-task-lint.ps1`.
- No Go source, workflow, or existing script is modified; the only created files are the one runner and the one harness.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$o = & go test \"./tests\" -run \"^TestU175_099_\" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match \"^--- PASS: TestU175_099_\" }).Count; if ($p -lt 1) { Write-Output \"NON-VACUITY-FAIL: zero named --- PASS: TestU175_099_ lines\"; exit 1 }; Write-Output \"TASK-LINT-OK:175.099-T:runner-bootstrap-task-lint\"; exit 0'",
  "lint_scope": {
    "kind": "runner-bootstrap-self-contained",
    "owned_runner_script": "scripts/verify-task-lint.ps1",
    "owned_file": "tests/runner_bootstrap_task_lint_175_099_test.go",
    "package": "./tests",
    "test_function": "TestU175_099_TaskLintRunnerBootstrap",
    "gate_must_not_be_runner": "scripts/verify-task-lint.ps1",
    "harness_invokes_runner_as_fixture_subject": true,
    "static_proofs": [
      "scripts/verify-task-lint.ps1 exists as a tracked, non-ignored, non-empty file under scripts/",
      "it parses cleanly under the built-in PowerShell AST parser with zero parse errors",
      "it declares its required param(...) surface: TaskId and FeatureId"
    ],
    "behavioral_scenarios": {
      "runner": "verify-task-lint.ps1",
      "invocation": "-TaskId <id> -FeatureId 175-F [-Phase Schema|PreCommit|PostCommit]",
      "identity_model": "line-stable-group-multiset/v1",
      "scenario_classes": {
        "green_success": "compliant owned-target fixture -> native exit 0 + success marker",
        "red_owned_residual": "table-driven RED matrix (retained owned finding; line-stable shift-safety where a still-owned occurrence shifts line yet residual stays above expected; interchangeable-duplicate isolation where a strictly-later slice's occupancy is resolved so owned residual falls below expected) -> native nonzero, no success marker",
        "fail_closed_input": "table-driven fail-closed matrix (missing/empty -TaskId, unknown task id, missing -FeatureId) -> native nonzero"
      },
      "non_vacuity": "green_success and red_owned_residual fixtures must yield different exit codes; ownership keys on the (path, linter, normalized_message) group fingerprint plus multiplicity, never on line/column"
    },
    "fixture_injectability_requirement": "the runner must accept inputs via its declared parameter surface and an execution working directory so the harness drives it against harness-authored deterministic fixtures without mutating live repository state",
    "safety": "harness constructs all fixtures in temp directories and invokes the runner via fixed argument vectors (no shell string, no untrusted-input interpolation)",
    "tooling": "existing main-branch go test + PowerShell 7 only; the runner this task creates is never used as this task's gate",
    "bootstrap_rationale": "This member OWNS creation of scripts/verify-task-lint.ps1, so its GATE (the owned Go harness) MUST NOT be a direct invocation of that runner (a circular self-dependency) and MUST compile with its targeted test executing before the runner exists (RED nonzero before, GREEN after; compile success never implies test success). It is the in-degree-zero source of the full feature graph."
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.099-T:runner-bootstrap-task-lint",
    "behavioral_discriminators": [
      "verify-task-lint.ps1: compliant-vs-noncompliant fixtures yield different exit codes; line-shift and interchangeable-duplicate fixtures are caught by fingerprint+multiplicity, not line/column"
    ],
    "rejects": "a no-op runner that only parses and declares params but never enforces owned-residual success/failure behavior"
  }
}
```

<!-- END:task-lint-contract -->
