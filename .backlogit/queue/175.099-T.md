---
artifact_type: task
created_at: 2026-09-20T05:05:00.0000000Z
dependencies: []
id: 175.099-T
parent_id: 175-F
priority: high
references:
    - docs/decisions/2026-09-19-baseline-convergence-decomposition.md
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'RS-W(-1) prerequisite: bootstrap shared baseline-convergence lint runners (STRICT PREDECESSOR of U1)'
updated_at: 2026-09-20T05:05:00.0000000Z
---

Runner-bootstrap PREREQUISITE of the entire baseline-convergence feature `175-F`. This is the STRICT PREDECESSOR of `175.001-T` (U1) and, transitively, of every baseline-remediation member `175.001-T`..`175.098-T`. It is the in-degree-zero source of the FULL feature graph; the 98-member remediation sub-DAG (whose own internal source is U1) is reachable only after this member lands. Single-domain (test/harness authoring plus PowerShell runner scaffolding — one skill domain: verification tooling).

## Why this task exists (bootstrap resolution, PR #449 cycle 2)

Every baseline-remediation member's `<!-- BEGIN:task-lint-contract -->` block invokes the canonical short runner `scripts/verify-task-lint.ps1`, and the feature additionally requires the intermediate-wave runner `scripts/verify-baseline-lint.ps1` and the terminal runner `scripts/verify-terminal-lint.ps1`. None of those three scripts exists on this branch, and no `175.*` remediation member owned creating them. Without an owner, the first replacement shipment `157-S` (U1) would have no executable task-lint command, and folding the runners into `157-S` would exceed that shipment's declared single-task ownership. This task closes that gap by OWNING creation of the three shared runner scripts, and it is chained ahead of the replacement baseline sequence via `176-S -> 157-S` (shipment edge) and `175.099-T -> 175.001-T` (task edge).

## Owned deliverable surface (created by Ship during execution; NOT implemented in this planning PR)

- `scripts/verify-task-lint.ps1` — the canonical short per-task runner invoked by all 98 remediation members' `task_lint_cmd` (`-TaskId <id> -FeatureId 175-F`, with `-Phase Schema|PreCommit|PostCommit` lifecycle screens).
- `scripts/verify-baseline-lint.ps1` — the intermediate-wave monotonicity runner (`-Inventory ... -InventorySha256 ... -Shipment <replacement-shipment-id> -FeatureId 175-F -TerminalTask 175.012-T`).
- `scripts/verify-terminal-lint.ps1` — the terminal zero-warning global runner (`-FeatureId 175-F`).

These are the ONLY three implementation files this task may create. It creates NO Go source, NO workflow, and modifies NO existing script; the three runners are net-new files under `scripts/`.

## Bootstrap harness (non-vacuous; MUST NOT invoke the runner it creates)

BOOTSTRAP INVARIANT (honest, non-vacuous): the verification of this task MUST NOT invoke `scripts/verify-task-lint.ps1` (or the other two runners' lint logic), because doing so would be a vacuous self-dependency on the very artifact this task creates. Instead this task EXCLUSIVELY OWNS one deterministic Go harness path `tests/runner_bootstrap_175_099_test.go` (exactly-one-owner; no other member may create it), declaring a single task-scoped Go test function `TestU175_099_RunnerBootstrap` (deterministic `TestU175_099_` unit token). The harness proves, using ONLY existing main-branch tooling (`go test` + the built-in PowerShell 7 AST parser — no missing script is executed), that:

1. Each of the three runner scripts exists as a tracked, non-ignored, non-empty file under `scripts/`.
2. Each runner script PARSES CLEANLY under the built-in PowerShell AST parser (`[System.Management.Automation.Language.Parser]::ParseFile`) with zero parse errors — a syntactic validity proof that requires no execution of the runner's lint body and no golangci-lint / inventory dependency.
3. Each runner declares its required parameter surface in its `param(...)` block (AST param-block inspection): `verify-task-lint.ps1` declares `TaskId` and `FeatureId`; `verify-baseline-lint.ps1` declares `Inventory`, `InventorySha256`, `Shipment`, `FeatureId`, `TerminalTask`; `verify-terminal-lint.ps1` declares `FeatureId`.

RED/GREEN DISCRIMINATOR: before the three scripts are created the harness fails RED (the existence assertion fails because the files are absent); after creation it passes GREEN. The harness executes no runner lint logic, so it is independently buildable and runnable under existing tooling at every point and never invokes a missing script.

P-002.6 SCOPED VERIFICATION COMMAND (EXECUTABLE non-vacuity wrapper; explicit package, anchored, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./tests" -run "^TestU175_099_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_099_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_099_ lines"; exit 1 }; Write-Output "TASK-LINT-OK:175.099-T:runner-bootstrap"; exit 0'

Wrapper contract: runs the explicit package (`./tests`) and anchored `^TestU175_099_` selector with `-v -count=1`; captures and re-emits combined `go test` output; preserves/propagates the native nonzero exit; counts `^--- PASS: TestU175_099_` lines and EXITS NONZERO when that count is < 1 (a zero-match/absent selector fails vacuously instead of passing). It uses ONLY `go test` and `pwsh`, both present on the existing main-branch toolchain (`go1.26.x`, PowerShell 7); it invokes NONE of the three runner scripts. `tests/runner_bootstrap_175_099_test.go` is a `*_test.go` file EXCLUDED from the implementation-file inventory (the implementation surface owned is the three `scripts/verify-*.ps1` runners).

## Independence and safety posture

- INDEPENDENTLY BUILDABLE: `go build ./...` and `go test ./tests -run ^TestU175_099_` succeed under existing main-branch tooling with no dependency on any of the three runners being pre-existing. The RED phase is an expected failing assertion, not a build/tooling break.
- NO RETROFIT of YAML BDD: this task authors imperative PowerShell runners and a Go harness only; it introduces no BDD product spec and applies none of the deferred BDD scope.
- CLEAN-TREE / governed-claim posture mirrors the other members: the governed claim dirties only this task's own status artifact `.backlogit/queue/175.099-T.md` and the append-only `.backlogit/hooks_queue.jsonl`; both are outside the owned `scripts/` and `tests/` surface. FAIL closed on any dirty path outside the CLOSED union {the two governed claim paths} ∪ {the three owned `scripts/verify-*.ps1` runners, the owned `tests/runner_bootstrap_175_099_test.go` harness}.

## Acceptance criteria

- The three runner scripts `scripts/verify-task-lint.ps1`, `scripts/verify-baseline-lint.ps1`, `scripts/verify-terminal-lint.ps1` exist as tracked, non-ignored, non-empty files.
- `tests/runner_bootstrap_175_099_test.go` declares `TestU175_099_RunnerBootstrap`; the EXECUTABLE non-vacuity wrapper (package `./tests`, anchored `^TestU175_099_`) is RED before the runners exist and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_099_` line; propagates native nonzero exit).
- The harness proves each runner script parses cleanly under the PowerShell AST parser and declares its required `param(...)` surface, WITHOUT executing any runner's lint body and WITHOUT invoking `scripts/verify-task-lint.ps1`.
- No Go source, workflow, or existing script is modified; the only created files are the three `scripts/verify-*.ps1` runners and the one `tests/runner_bootstrap_175_099_test.go` harness.
- Task lint contract (bootstrap, self-contained): the canonical, executable, task-scoped lint verifier is the `task_lint_cmd` in this task's `task-lint-contract` block below; it delegates to the owned Go harness and NEVER invokes the runner this task creates.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$o = & go test \"./tests\" -run \"^TestU175_099_\" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match \"^--- PASS: TestU175_099_\" }).Count; if ($p -lt 1) { Write-Output \"NON-VACUITY-FAIL: zero named --- PASS: TestU175_099_ lines\"; exit 1 }; Write-Output \"TASK-LINT-OK:175.099-T:runner-bootstrap\"; exit 0'",
  "lint_scope": {
    "kind": "runner-bootstrap-self-contained",
    "owned_file": "tests/runner_bootstrap_175_099_test.go",
    "package": "./tests",
    "owned_runner_scripts": [
      "scripts/verify-task-lint.ps1",
      "scripts/verify-baseline-lint.ps1",
      "scripts/verify-terminal-lint.ps1"
    ],
    "must_not_invoke": "scripts/verify-task-lint.ps1",
    "bootstrap_rationale": "This member OWNS creation of the three shared runners, so its verification MUST NOT invoke the runner it creates (a vacuous self-dependency). The harness proves the runner scripts exist, are tracked, are non-empty, parse cleanly via the built-in PowerShell AST parser, and declare their required param surface, using only existing main-branch tooling (go test + pwsh) and executing no runner lint body."
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.099-T:runner-bootstrap"
  }
}
```

<!-- END:task-lint-contract -->
