---
artifact_type: shipment
created_at: 2026-09-19T23:35:30.2745363Z
custom_fields:
    items:
        - 175.001-T
dependencies:
    - 176-S
id: 157-S
priority: high
status: queued
title: 'Baseline Convergence RS-W00: U1 line-ending source boundary'
updated_at: 2026-09-20T05:05:00.0000000Z
---

## Release sequencing contract

`157-S` (RS-W00, carrying U1 `175.001-T`) is the first wave of the replacement
baseline sequence, and it `blocks`-depends on the runner-bootstrap prerequisite
shipment `176-S` (RS-W(-1), carrying `175.099-T`). The replacement baseline
sequence MUST NOT begin until `176-S` has shipped: the shared lint runners
(`scripts/verify-task-lint.ps1`, `scripts/verify-baseline-lint.ps1`,
`scripts/verify-terminal-lint.ps1`) that every member's `task_lint_cmd` invokes
are owned and created by `175.099-T` in `176-S`. This is an explicit ordering
edge encoded in the `dependencies` frontmatter above; shipped-only readiness is
governed by the Orchestrator claim-routing policy consistent with the advisory
dependency semantics documented for `149-S`/`156-S`.

