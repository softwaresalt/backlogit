---
artifact_type: task
created_at: 2026-07-29T07:34:51.079012Z
id: 131.001-T
labels:
    - durability
    - filesystem
    - leaf-package
    - tests
parent_id: 131-F
priority: low
references:
    - docs/exec-plans/2026-07-29-fsutil-durable-mkdir-extraction-plan.md
status: queued
title: 'Create internal/fsutil leaf + FsyncDir with direct unit tests'
updated_at: 2026-07-29T07:34:51.079012Z
---

Test-first. Create the new stdlib-only leaf package internal/fsutil with doc.go, fsutil.go, fsutil_test.go. Scope of THIS task is the package skeleton plus the FsyncDir primitive only; MkdirAllDurable is added in follow-on tasks 131.004-T (creation path) and 131.005-T (retry/re-confirm). Port fsyncDirCore verbatim into FsyncDir(path string) error (open dir, Sync(), Close(), neutral error messages). Import stdlib only (fmt, os, path/filepath) — no internal/core, internal/events, or internal/errors. doc.go records the leaf-family convention: general fs primitives return neutral errors and callers map at the boundary; backlogit-specific writers (atomicfile) may self-classify.

## Acceptance Criteria

internal/fsutil imports stdlib only (no internal/core, internal/events, internal/errors). doc.go states the stdlib-only no-core/no-events contract AND the neutral-error convention. FsyncDir has DIRECT unit tests: (1) success on a real directory returns nil; (2) open-error on a non-existent/non-openable path returns an error naming the path. go test ./internal/fsutil/... passes; go vet, golangci-lint, gofmt clean.

## Execution Posture

test-first