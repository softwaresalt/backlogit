---
artifact_type: task
created_at: 2026-08-17T05:09:34.8504892Z
id: 143.001-T
labels:
    - shipment-lifecycle
    - audit-log
    - core
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-16-shipment-shipped-event-audit-log-plan.md
status: queued
title: Route shipped-event append through an error-returning writer scoped to the shipped transition
updated_at: 2026-08-17T05:09:34.8504892Z
---

Unit 1. In moveShipmentStatusWithHeadGuard (internal/core/shipment.go), route the shipment_status_changed emission through the error-returning appendItemEventErr(ctx, ws, ...) and return its error, but ONLY for the active-to-shipped transition (gate on newStatus == ShipmentShipped) so claim (queued-to-active) and abandon keep best-effort semantics. Parity via passing the same ws (no EventWriter parameter, no fresh writer). Introduce a per-ws append seam (mirroring ws.gateEvidenceAppend, e.g. ws.shipmentEventAppend) for test injection. Execution posture: test-first. NOT independently releasable; unsafe without the class-aware rollback in 143.002-T and must land with it.

<!-- BEGIN:acceptance-criteria -->
- The shipped transition emits shipment_status_changed through appendItemEventErr(ctx, ws, ...) whose error is returned to the caller.
- The change is scoped to newStatus == ShipmentShipped; claim and abandon semantics are unchanged and asserted so.
- The append is injectable through a per-ws seam, not a package-global var.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Files: internal/core/shipment.go plus one small per-ws seam field on Workspace and its default wiring. Lands the failing red assertion; broader scenarios are 143.003-T.
<!-- END:implementation-notes -->