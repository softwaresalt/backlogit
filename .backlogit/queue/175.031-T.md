---
artifact_type: task
created_at: 2026-09-18T05:50:50.8440574Z
dependencies:
    - 175.001-T
id: 175.031-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/mcp/contract_consistency_test.go'
updated_at: 2026-09-18T07:56:07.879024Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: errcheck
Package: internal/mcp
Owned source file (exactly one): internal/mcp/contract_consistency_test.go
Findings (1) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L196: Error return value of `(*github.com/softwaresalt/backlogit/internal/core.Workspace).Close` is not checked
Affected functions (1 distinct, < 5): TestMCP_ConcurrentEnsureWorkspace_NoRace
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/mcp/errcheck_contract_consistency_test_175_test.go
Ship's harness phase creates this harness JUST BEFORE this file task (NOT precommitted at Stage). It is a source-shape or command-backed test scoped to internal/mcp/contract_consistency_test.go ONLY: it asserts this file's errcheck / behavior contract without referencing any not-yet-existing symbol and without leaving any future-task red assertion in the full suite. It is EXCLUDED from the implementation-file inventory.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test function named `TestU175_031_ErrcheckContractConsistencyTest` (deterministic `TestU175_031_` unit token; no other task uses this prefix). RED before the fix (the assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/mcp" -run "^TestU175_031_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_031_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_031_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the task's explicit package (`./internal/mcp`) and anchored `^TestU175_031_` selector with `-v -count=1`; captures and re-emits the combined go test output; preserves and propagates the native `go test` nonzero exit; counts lines matching `^--- PASS: TestU175_031_` and EXITS NONZERO when that count is < 1 (so a zero-match/absent selector fails vacuously instead of passing). Before implementation the same command is RED via an assertion failure on `TestU175_031_ErrcheckContractConsistencyTest`; after the green phase it exits 0 only when at least one named PASS line is observed.

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return-with-comment is NOT an allowed resolution): in this `_test.go` file, capture every flagged error return and FAIL the test on non-nil (`if err := x.Close(); err != nil { t.Fatalf("...: %v", err) }` or `require.NoError(t, err)`). A deferred close becomes a checked close that fails the test (`defer func(){ if err := x.Close(); err != nil { t.Errorf("...: %v", err) } }()`). No error return is discarded; no cross-file or cross-package helper is introduced; the edit is confined to this one file.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only-safe compile: verify the package's tests with go test ./internal/mcp -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 AND (via its counted `--- PASS: TestU175_031_` guard) proves at least one named PASS (non-vacuous), and golangci-lint run ./internal/mcp reports 0 errcheck findings in internal/mcp/contract_consistency_test.go.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/mcp/errcheck_contract_consistency_test_175_test.go was RED (assertion on TestU175_031_ErrcheckContractConsistencyTest) before the fix and GREEN after; no future-task red remains in the full suite.
- Every flagged error return is HANDLED (checked propagation/wrapping, named-return errors.Join, or test failure); NO `_ =` / ignored-return resolution is used.
- ONLY internal/mcp/contract_consistency_test.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.