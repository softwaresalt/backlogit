---
artifact_type: feature
created_at: 2026-07-15T06:35:53.3003336Z
custom_fields:
    harness_status: pending
id: 107-F
labels:
    - docline
    - testing
    - documentation
priority: low
references:
    - docs/decisions/2026-07-14-docline-soft-key-regression-decision.md
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
status: queued
title: Guard tracked docline soft keys
updated_at: 2026-07-16T03:00:01.7376789Z
---

Guard committed docline soft keys through bounded contracts: live Git-tracked corpus with production Scope, hermetic value and tracked-versus-untracked fixtures, a RED-first integration regression (107.010-T, tests/integration/docline_extension_compat_test.go) that proves unknown top-level extension keys are RELOCATED under the docline namespace (folded and preserved as a nested map, not top-level) at current HEAD and inventories tracked docs for already-folded producer keys (docline.size*), and a production-contract codec task (107.009-T, gated behind the 107.010-T RED) that changes BOTH the docline normalization seam (internal/docline/normalize.go: stop folding non-contract top-level keys under docline) AND the serialization seam (internal/docline/frontmatter.go: BaseFrontmatter must carry and re-emit unknown top-level keys, since removing that fold without a top-level carrier would leave ToMap() emitting only base-contract fields plus docline, a LATENT top-level drop the coupled carrier closes) to preserve unknown top-level extension properties (e.g. backlogit-owned size, size_source, size_ruleset_version) IN PLACE instead of folding them under the docline namespace, updating the contradictory normalization unit tests (internal/docline/policy_test.go TestIsContractField stays valid and unchanged). The full base/extension correction is width-isolated across THREE concerns and their own tasks: 107.009-T (Go codec + unit tests, no schema/doc edits), 107.011-T (opens the docline base JSON schema schemas/docline/base-frontmatter-v1.schema.json so it ACCEPTS additional producer-owned top-level extension properties generally, with no declared/sanctioned-vs-legacy distinction and no enumeration of size fields, with test-first schema-validation coverage), and 107.012-T (rewrites docs/docline-frontmatter-authoring-guide.md to the inheritance model). This shipment therefore DOES include a bounded JSON-schema change (in 107.011-T): the earlier no-schema-change framing applied ONLY to the Go codec task and is corrected here. Together these prove docline validates/normalizes only its own base fields and re-emits/serializes caller-provided extension properties at the top level when present but never owns, synthesizes, defaults, validates, or semantically interprets extension semantics, whose ownership stays with backlogit's derived contract; preserve protected scratch state. Restaged cleanly from origin/main as its own shipment, decoupled from formal-gate governance and the size spike (096-S).