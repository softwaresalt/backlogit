---
artifact_type: task
created_at: 2026-07-18T17:14:41.6743627-07:00
dependencies:
    - 108.002-T
id: 108.006-T
labels:
    - size
    - crash-safety
    - core
parent_id: 108-F
priority: medium
status: queued
title: 'SE-3b: Best-effort append-before-write audit ordering (core persistence)'
updated_at: 2026-07-18T19:06:11.4439345-07:00
---

SE-3b: Best-effort append-before-write audit ordering at the size seam (core persistence). Option B2 descope (Copilot cycle-3 H1/H5): the crash-window exactly-once ambition is REMOVED at the root, not patched. The estimate-history event is a best-effort observability/audit trail, NOT a source of truth. Protocol (all under the already-held per-task lock, on existing atomicfile/JSONL rails, no new dependency): (1) append the intent event BEFORE the durable write (gate-evidence precedent internal/core/gate_evidence.go:34-58); (2) durable write = atomicfile.WriteFileAtomic(file) at artifact_size.go:79 THEN db.UpsertItem(index) at :90 (file-then-index ordering); the write path re-upserts the artifact into the SQLite index so a normal completion is index-consistent; (3) FAIL-CLOSED ON READ: the durable custom_fields.size is the SOLE source of truth; any orphan appended event left by a crash (append succeeded, write did not land) is simply IGNORED on read -- reads never trust an event that lacks a corresponding durable size. DROPPED entirely (deferred to stash 9D5BB492): OpID-based dedup, exactly-once, client-retry-reuses-same-OpID language, the size_op_id reserved key, the PrevOpID predecessor chain, and doctor reconciliation. No OpID transport ingress is added. Durability scope: PROCESS-crash observability only (G3) -- atomicfile.WriteFileAtomic and events.AppendEvent are both sync-free, so OS-crash/power-loss durability is OUT OF SCOPE for 099-S (tracked as stash 131CEAE4). Tests: the intent event is appended before the durable write; a forced write failure after a successful append leaves an orphan audit event that a subsequent read IGNORES (durable size unchanged, no shared-JSONL truncation, no duplicate replay); a normal size set re-upserts the SQLite index so the row is consistent. Now genuinely <=2h single-domain (internal/core/artifact_size.go). Depends on 108.002-T (SE-3a). Files: internal/core/artifact_size.go.