---
artifact_type: task
created_at: 2026-09-15T20:44:17.8023231Z
id: 174.039-T
parent_id: 174-F
priority: high
status: queued
title: 'R1s: Core-lifecycle SOURCE-SHAPE RED harness'
updated_at: 2026-09-15T22:15:00.0000000Z
---

SOURCE-SHAPE RED harness (P-002.1 source-shape harness for a declaration unit; NO declaration-only exemption) pinning the EXISTENCE and SHAPE of the shared blocked-shipment lifecycle seam BEFORE any declaration or behavior lands. Uses go/ast / go/parser to assert over the internal/core package source that it declares: (1) a distinct `ShipmentBlocked ShipmentStatus = "blocked"` enum value on the `ShipmentStatus` type, kept a DISTINCT Go type from `models.StatusBlocked`; (2) `BlockShipment(...)` and `UnblockShipment(...)` function signatures; (3) the `isValidShipmentTransition` blocked-transition shape (active->blocked, blocked->{queued,active}). The harness itself always compiles (it reads source as data); it is assertion-RED while the declarations are absent and turns GREEN the moment the declaration-only stub task (Rd 174.052-T) lands them. It asserts SHAPE ONLY — no lifecycle behavior. Domain: tests. AC: (1) source-shape assertions FAIL until the Rd declarations (174.052-T) land, then PASS; (2) assert `ShipmentBlocked` is a distinct type from `models.StatusBlocked`; (3) assert the block/unblock signatures and blocked transition shape are declared. <4 scenario groups.

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Core-lifecycle SOURCE-SHAPE RED harness pinning the existence/shape of the blocked-shipment seam with no production change; its go/ast shape assertions over ShipmentBlocked, BlockShipment, UnblockShipment, and the isValidShipmentTransition blocked shape turn green only after the declaration-only stub task (Rd 174.052-T) lands the declarations, so its TestUR1S_ functions are source-shape-red by design when it completes.
red_selector_command: go test -count=1 -run '^TestUR1S_' ./internal/core
green_maker_tasks: 174.052-T
green_maker_closes_wave: 2
```
<!-- END:red-deliverable-contract -->
