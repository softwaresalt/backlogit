---
artifact_type: task
created_at: 2026-09-19T06:48:04.4306599Z
dependencies:
    - 175.094-T
id: 175.096-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/core shipment_reconcile_fs_unix.go + shipment_reconcile_lock_unix.go (linux)'
updated_at: 2026-09-19T08:06:33.465246Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant). Its owned findings are Linux-only `errcheck` identities (GOOS=linux build surface); the owned source file(s) participate in the Linux go build/list only.

Linters: errcheck
Package(s): ./internal/core
Owned source file(s) (2): internal/core/shipment_reconcile_fs_unix.go, internal/core/shipment_reconcile_lock_unix.go
Owned baseline occurrences (2) — GOOS=linux; line:column shown are DIAGNOSTIC baseline coordinates only, NOT durable identity (golangci-lint v2.13.2, unbounded invocation). Durable ownership is the line-stable group-multiset model in the task-lint-contract below (stable key = path + linter + normalized message; per-task occurrence multiplicity with ordered-slice expected residual):
- internal/core/shipment_reconcile_fs_unix.go:L30:C21 [errcheck] Error return value of `dirFile.Close` is not checked
- internal/core/shipment_reconcile_lock_unix.go:L30:C21 [errcheck] Error return value of `dirFile.Close` is not checked
Supported GOOS for owned findings: linux (owned `*_unix.go` file(s) build under the linux constraint only).

Task-scoped lint verification runs the canonical harness runner (`scripts/verify-task-lint.ps1`); the full zero-warning repository golangci-lint run is mandatory only at terminal wave convergence (U12 / 175.012-T), not per task. Same-wave sibling harnesses may remain red until wave convergence; no later-wave harness is scaffolded early.

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =`/ignored-return is NOT an allowed resolution): for every flagged `Close`/return, capture and handle the error (checked deferred close with `fmt.Errorf("context: %w", err)` or an in-scope named-return `errors.Join`); edits are confined to the owned file(s); no cross-file or cross-package helper is introduced.

Test posture (Principle II / P-002.6): NORMAL harness-required task (no harness exemption). This task EXCLUSIVELY OWNS one deterministic Linux-tagged harness path: `internal/core/lintfix_175_096_unix_test.go` (exactly-one-owner; `//go:build linux`). It declares a single task-scoped Go test named `TestU175_096_LintFix` (deterministic `TestU175_096_` token). RED before the fix (failing assertion, compiling under GOOS=linux), GREEN after. P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests; run with GOOS=linux):
  pwsh -NoProfile -Command '$env:GOOS="linux"; $o = & go test "./internal/core" -run "^TestU175_096_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_096_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_096_ lines"; exit 1 }; exit 0'

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 3 (two owned source files + one owned Linux-tagged harness file).
- Owned baseline findings = 2 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized errcheck remediation confined to owned file(s).

Acceptance criteria:
- The task-scoped harness selector `^TestU175_096_` proves >= 1 named PASS under GOOS=linux (non-vacuous), and the canonical `task_lint_cmd` exits 0 with success marker `TASK-LINT-OK:175.096-T:finding-remediation` (no owned baseline finding remains in the owned identity slice).
- Edits are confined to the owned file(s); no owned finding of another task is modified.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.096-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "identity_model": "line-stable-group-multiset/v1",
    "packages": [
      "./internal/core"
    ],
    "owned_files": [
      "internal/core/shipment_reconcile_fs_unix.go",
      "internal/core/shipment_reconcile_lock_unix.go"
    ],
    "owned_group_count": 2,
    "owned_occurrence_count": 2,
    "owned_groups": [
      {
        "group_fingerprint": "a3e2da5bb9ab3ad069e0e91ec6a3c8e996de53f37e0f832c99f11963fd002192",
        "path": "internal/core/shipment_reconcile_fs_unix.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `dirFile.Close` is not checked",
        "group_multiplicity": 1,
        "owned_count": 1,
        "owned_ordinals": [
          1
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 30,
            "column": 21
          }
        ]
      },
      {
        "group_fingerprint": "2f7665e5c6f28e390cb20e5892062ae8ceb9be81c03ede9f20c163fc7d027742",
        "path": "internal/core/shipment_reconcile_lock_unix.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `dirFile.Close` is not checked",
        "group_multiplicity": 1,
        "owned_count": 1,
        "owned_ordinals": [
          1
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 30,
            "column": 21
          }
        ]
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.096-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
