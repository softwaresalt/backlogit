---
artifact_type: task
created_at: 2026-09-18T05:49:54.957431Z
dependencies:
    - 175.001-T
id: 175.024-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/core/workspace.go'
updated_at: 2026-09-18T19:31:51.6092221Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: errcheck
Package: internal/core
Owned source file (exactly one): internal/core/workspace.go
Findings (1) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L151: Error return value of `database.Close` is not checked
Affected functions (1 distinct, < 5): NewWorkspace
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/core/errcheck_workspace_175_test.go
Ship's harness phase scaffolds this harness together with its admitted ready wave -- every non-exempt member of the same ready_k wave is scaffolded together in ONE pass (per _ship.agent.md Step 2 and the harness-architect skill), leaving them all red; the harnesses are NOT precommitted at Stage. It is a source-shape or command-backed test scoped to internal/core/workspace.go ONLY: it asserts this file's errcheck / behavior contract without referencing any not-yet-existing symbol. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. It is EXCLUDED from the implementation-file inventory.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test function named `TestU175_024_ErrcheckWorkspace` (deterministic `TestU175_024_` unit token; no other task uses this prefix). RED before the fix (the assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/core" -run "^TestU175_024_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_024_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_024_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the task's explicit package (`./internal/core`) and anchored `^TestU175_024_` selector with `-v -count=1`; captures and re-emits the combined go test output; preserves and propagates the native `go test` nonzero exit; counts lines matching `^--- PASS: TestU175_024_` and EXITS NONZERO when that count is < 1 (so a zero-match/absent selector fails vacuously instead of passing). Before implementation the same command is RED via an assertion failure on `TestU175_024_ErrcheckWorkspace`; after the green phase it exits 0 only when at least one named PASS line is observed.

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return-with-comment is NOT an allowed resolution): capture every flagged unchecked error and resolve it by CHECKED PROPAGATION/WRAPPING (`return fmt.Errorf("...: %w", err)`), a NAMED-RETURN `errors.Join` for a deferred close (`defer func(){ err = errors.Join(err, x.Close()) }()` where the function has a named `err` return), or an EXISTING safe helper that already handles the error. Never assign a flagged return to `_`. No cross-file or cross-package helper is introduced; the edit is confined to this one file.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only-safe compile: verify the package's tests with go test ./internal/core -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 AND (via its counted `--- PASS: TestU175_024_` guard) proves at least one named PASS (non-vacuous), and the file-scoped lint verification (FSLV) command below exits 0 (no owned errcheck finding remains in internal/core/workspace.go).
- File-scoped lint verification (FSLV): the canonical, executable, task-scoped lint verifier is the `task_lint_cmd` inside this task's `<!-- BEGIN:task-lint-contract -->` block at the END of this file (single source of truth; the command is maintained ONLY there to eliminate dual-maintenance drift). Its contract: exit 0 = no owned errcheck finding remains in internal/core/workspace.go (this task is INDEPENDENTLY COMPLETABLE while same-wave sibling findings stay red); exit 1 = an owned finding still remains; distinct exits 91-97 are fail-closed schema/launch failures (empty stdout 91, malformed JSON 92, JSON null 93, non-object root 94, missing Issues 95, non-array Issues 96, native launch failure 97); any OTHER nonzero exit is a preserved native golangci-lint config/execution failure. The verifier is TASK-SCOPED ONLY: a bare repository-wide `golangci-lint run` is NOT run per task; full-repository lint is MANDATORY at Ship's wave-convergence gate (Step 4.6), and this task-scoped verifier does NOT weaken that terminal full lint.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/core/errcheck_workspace_175_test.go was RED (assertion on TestU175_024_ErrcheckWorkspace) before the fix and GREEN after. Within the same ready wave, sibling task-scoped red is EXPECTED and TOLERATED; this task's own scoped selector must turn GREEN when its fix lands, and full-suite green is required only at Ship's Step 4.6 wave-convergence gate (when the wave's open-red set is empty). No later-wave harness is scaffolded early.
- Every flagged error return is HANDLED (checked propagation/wrapping, named-return errors.Join, or test failure); NO `_ =` / ignored-return resolution is used.
- ONLY internal/core/workspace.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$ErrorActionPreference=\"Stop\"; $psi=[System.Diagnostics.ProcessStartInfo]::new(); $psi.FileName=\"golangci-lint\"; $psi.Arguments=\"run --output.json.path stdout --show-stats=false --issues-exit-code 0 ./internal/core\"; $psi.RedirectStandardOutput=$true; $psi.RedirectStandardError=$true; $psi.UseShellExecute=$false; $psi.WorkingDirectory=(Get-Location).Path; try { $p=[System.Diagnostics.Process]::Start($psi) } catch { Write-Output \"LINT-NATIVE-LAUNCH-FAIL\"; exit 97 }; $so=$p.StandardOutput.ReadToEndAsync(); $se=$p.StandardError.ReadToEndAsync(); $p.WaitForExit(); $out=$so.GetAwaiter().GetResult(); $err=$se.GetAwaiter().GetResult(); $ec=$p.ExitCode; if ($ec -ne 0) { Write-Output (\"LINT-NATIVE-FAIL:\"+$ec); if ($err) { Write-Output $err }; exit $ec }; $t=$out.Trim(); if ($t.Length -eq 0) { Write-Output \"LINT-SCHEMA-FAIL:empty-stdout\"; exit 91 }; try { $j=$t | ConvertFrom-Json -ErrorAction Stop } catch { Write-Output \"LINT-SCHEMA-FAIL:malformed-json\"; exit 92 }; if ($null -eq $j) { Write-Output \"LINT-SCHEMA-FAIL:json-null\"; exit 93 }; if (-not ($j -is [System.Management.Automation.PSCustomObject])) { Write-Output \"LINT-SCHEMA-FAIL:non-object-root\"; exit 94 }; if (-not ($j.PSObject.Properties.Name -contains \"Issues\")) { Write-Output \"LINT-SCHEMA-FAIL:missing-Issues\"; exit 95 }; if (-not ($j.Issues -is [System.Array])) { Write-Output \"LINT-SCHEMA-FAIL:Issues-not-collection\"; exit 96 }; $hits=@($j.Issues | Where-Object { [string]::Equals($_.FromLinter, \"errcheck\", [System.StringComparison]::Ordinal) -and [string]::Equals(((\"\" + $_.Pos.Filename) -replace \"\\\\\",\"/\"), \"internal/core/workspace.go\", [System.StringComparison]::Ordinal) }); if ($hits.Count -gt 0) { Write-Output (\"OWNED-FINDING-REMAINS:\"+$hits.Count); exit 1 }; Write-Output \"FSLV-OK:internal/core/workspace.go:errcheck:0-owned-findings\"; exit 0'",
  "lint_scope": {
    "kind": "file-scoped-go-lint",
    "package": "./internal/core",
    "linter": "errcheck",
    "owned_file": "internal/core/workspace.go"
  },
  "non_vacuity_evidence": {
    "mode": "structured-json-schema-validated",
    "success_marker": "FSLV-OK:internal/core/workspace.go:errcheck:0-owned-findings",
    "asserts": [
      "native golangci-lint launch failure preserved distinctly (exit 97)",
      "native golangci-lint nonzero exit preserved and propagated (passthrough)",
      "empty stdout is a hard failure (exit 91)",
      "malformed JSON is a hard failure (exit 92)",
      "JSON null root is a hard failure (exit 93)",
      "non-object JSON root is a hard failure (exit 94)",
      "missing Issues key is a hard failure (exit 95)",
      "non-array Issues is a hard failure (exit 96)",
      "schema validated before filtering; no unknown or vacuous success shape is accepted",
      "owned path and linter matched by ordinal exact identity; exit 1 if any owned finding remains",
      "read-only and task-scoped; no machine temp file, no Remove-Item cleanup; full repository golangci-lint run deferred to wave convergence"
    ]
  }
}
```

<!-- END:task-lint-contract -->
