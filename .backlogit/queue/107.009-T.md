---
artifact_type: task
created_at: 2026-07-15T06:37:49.5882466Z
dependencies:
    - 107.010-T
id: 107.009-T
labels:
    - go
    - docline
    - test-first
    - extension
    - normalization
parent_id: 107-F
priority: low
references:
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
    - internal/docline/normalize.go
    - internal/docline/frontmatter.go
    - internal/docline/normalize_test.go
status: queued
title: Preserve unknown top-level extension keys in docline normalization (production pass-through)
updated_at: 2026-07-16T02:15:11.7289526Z
---

EXECUTABLE production-contract task (<=2h, width-isolated to internal/docline normalization + serialization seam; corrected for PR #241/#242). Test-first: this task is GATED behind the RED integration regression 107.010-T (tests/integration/docline_extension_compat_test.go), which must be observed FAILING first; this production change turns it GREEN. CORRECTED CURRENT-HEAD BEHAVIOR (honest causal sequence, replacing the earlier dropped-on-serialization claim): at current HEAD internal/docline/normalize.go folds every non-contract, non-docline top-level key INTO the docline namespace (foldUnderDocline, driven by the CLOSED contractFields set in internal/docline/policy.go), and internal/docline/frontmatter.go FromMap/ToMap PRESERVE that nested docline map (ToMap emits the docline key when non-empty). So unknown top-level keys are currently RELOCATED under docline and round-trip intact - they are NOT dropped today; the failure is that they no longer stay TOP-LEVEL. The missing carrier on BaseFrontmatter is a LATENT drop that would occur ONLY if the fold were removed WITHOUT adding a top-level carrier, because ToMap then emits only base-contract fields plus docline. Honest scope: preserving unknown top-level extension keys IN PLACE at the top level REQUIRES a COUPLED change to BOTH seams - (1) stop the fold in internal/docline/normalize.go so non-contract top-level keys are not relocated under docline, AND (2) add a carrier to BaseFrontmatter in internal/docline/frontmatter.go so FromMap captures non-contract/non-docline top-level keys and ToMap re-emits them unchanged at the top level. Removing the fold without the carrier would convert today's relocation into the latent top-level drop, so the two edits must land together. Per the authoritative base/derived contract, docline is the BASE contract and owns only the base Markdown/frontmatter fields; it MUST preserve unknown top-level extension properties (e.g. the backlogit-owned size, size_source, size_ruleset_version) unchanged IN PLACE at the top level, validating/normalizing only its own base fields, and MUST NOT relocate extension fields under docline. Update the contradictory existing unit tests in internal/docline/normalize_test.go (TestNormalize_ContractSurfaceHoldsOnlyContractKeys and TestNormalize_FoldsLegacyTypeUnderDocline, which currently assert every top-level key is a contract field / that non-contract keys are folded away) to assert top-level pass-through. internal/docline/policy_test.go is NOT changed: TestIsContractField remains valid against current HEAD because the closed contract set still governs which base fields docline normalizes and no longer governs relocation of unknown keys. The size-extension regression proof (size/size_source/size_ruleset_version stay top-level and semantically/deep-value equivalent, with unchanged document body bytes and idempotent normalization, never validated/defaulted/emitted, surviving a lint/migration round-trip) lives in the RED task 107.010-T and is out of this task changed-file set. Body bytes and idempotency (Normalize(Normalize(x)) == Normalize(x)) preserved. Scope stays Go-only: this task changes internal/docline/normalize.go, internal/docline/frontmatter.go, and internal/docline/normalize_test.go plus their in-file comments/docs, and makes NO JSON-schema or authoring-guide edits; the correlated non-Go corrections are separate width-isolated sibling tasks in shipment 095-S: 107.011-T opens the docline base JSON schema (schemas/docline/base-frontmatter-v1.schema.json) to ACCEPT producer-owned top-level extension properties generally, and 107.012-T rewrites docs/docline-frontmatter-authoring-guide.md to the inheritance model. No CLI-distribution changes. Ownership stays explicit under the base/derived model: top-level pass-through is DOCLINE base-contract CODEC behavior (lossless semantic preservation); size SEMANTICS (validation, defaults, provenance, composition) are BACKLOGIT-owned and validated by backlogit's FUTURE derived contract, not by docline. Codec responsibility here is strictly LOSSLESS SEMANTIC PRESERVATION: docline retains unknown top-level keys as a SEMANTICALLY/deep-value equivalent key/value graph in place at the top level without interpreting, relocating, dropping, or validating them. Because the frontmatter codec (mdfront.Encode) reserializes YAML and MAY canonicalize key ordering/quotes/scalar spelling/comments/anchors, this is deep-value equivalence plus unchanged document body bytes and idempotent normalization, NOT raw frontmatter byte/lexical preservation unless a future YAML-node/raw-byte design is explicitly chosen. Extension-semantic validity is NOT docline's responsibility: the opened base schema (107.011-T) simply ACCEPTS extension properties without validating their semantics, and backlogit's future derived schema/validators own size validation. Preservation (docline codec) and extension-semantic validation (backlogit derived contract) are intentionally separate responsibilities - there is no missing selective codec migration and no base-schema enumeration of size fields. Decoupled from formal-gate governance and the size spike (096-S).