---
artifact_type: task
created_at: 2026-07-15T06:37:49.5882466Z
dependencies:
    - 107.001-T
id: 107.009-T
labels:
    - go
    - docline
    - test-first
    - extension
    - normalization
parent_id: 107-F
priority: low
references:
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
    - internal/docline/normalize.go
    - internal/docline/normalize_test.go
    - internal/docline/policy_test.go
    - tests/integration/docline_extension_compat_test.go
status: queued
title: Preserve unknown top-level extension keys in docline normalization (production pass-through)
updated_at: 2026-07-15T17:25:52.9052245Z
---

EXECUTABLE production-contract task (<=2h, width-isolated to internal/docline normalization; corrected for PR #241). The current production seam CONTRADICTS the authoritative base/extension contract: internal/docline/normalize.go folds every non-contract top-level key into the docline namespace (foldUnderDocline), and internal/docline/policy.go defines a CLOSED contractFields set that drives that relocation. Per the authoritative contract, docline owns only the base Markdown/frontmatter contract and MUST preserve unknown top-level extension properties (e.g. the backlogit-owned size, size_source, size_ruleset_version) unchanged IN PLACE at the top level, validating/normalizing only its own base fields, and MUST NOT relocate extension fields under docline. Change the production normalization seam so non-contract top-level keys pass through unchanged instead of being folded under docline (docline still reads/emits its own docline namespace and its base fields; body bytes and idempotency preserved). Update the contradictory existing normalization tests (internal/docline/normalize_test.go TestNormalize_ContractSurfaceHoldsOnlyContractKeys and TestNormalize_FoldsLegacyTypeUnderDocline, which currently assert every top-level key is a contract field / that non-contract keys are folded away). Add regression coverage using backlogit size extension examples proving size/size_source/size_ruleset_version stay top-level, unchanged, idempotent, and are never validated, defaulted, or emitted by docline, and survive a lint/migration round-trip without body mutation. Ownership stays explicit: top-level pass-through is DOCLINE base-contract behavior; size semantics (validation, defaults, provenance, composition) remain BACKLOGIT-owned and are out of scope here. Test-first: write the failing pass-through/regression assertions before changing the seam. No schema changes; no CLI-distribution changes; decoupled from formal-gate governance and the size spike (096-S).