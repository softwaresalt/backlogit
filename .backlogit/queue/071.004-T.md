---
artifact_type: task
created_at: 2026-06-30T22:50:49.0320349-07:00
dependencies:
    - 071.001-T
id: 071.004-T
labels:
    - size:M
    - phase-1
    - unit:U2
    - test-first
    - work-item:1-restrict-doctor-scope
parent_id: 071-F
priority: medium
references:
    - docs/exec-plans/2026-06-30-backlogit-deterministic-gates-slice-plan.md
status: queued
title: Wire doctor --target CLI flag with 5s timeout and exit-code contract
updated_at: 2026-06-30T22:50:49.0320349-07:00
---

Execution posture: TEST-FIRST. Depends on U1 (071.001-T). Scope: register --target string on the doctor cobra command (internal/cli/doctor.go). When set, run core.DoctorTarget in a GOROUTINE and select on ctx.Done() from context.WithTimeout(ctx,5s) — a plain wrapper does NOT interrupt synchronous I/O. Use a BUFFERED result channel (cap 1) so on timeout the still-running goroutine can send and exit (no leak); orphaned sidecar reclaimed by U4 60s stale-TTL. Map outcome to an explicit VERSIONED exit-code table via SilenceErrors + explicit os.Exit (Cobra RunE otherwise collapses to 1): 0 pass / 1 validation-fail / 2 timeout (errors.Is DeadlineExceeded) / 3 scope-or-IO / 4 busy(lock, U5). Honor --format json but emit a versioned target-mode schema with a mode:target discriminator. Help text documents: exit table, that autoharness subprocess timeout_seconds:5 (design 5) is the authoritative outer bound, and the Q1 escape hatch (backlogit move {id} --status blocked/queued — existing transition, no new command). ACCEPTANCE CRITERIA: valid file->exit 0; missing-required-field->exit 1; timeout->exit 2 (inject near-zero deadline vs blocked stub, non-vacuous); out-of-scope->exit 3; CLI test green.