---
artifact_type: task
created_at: 2026-09-06T00:58:27.4400083Z
dependencies:
    - 167.001-T
id: 167.002-T
parent_id: 167-F
priority: critical
status: queued
title: Distinct shipment_reconciled_shipped event + doctor-local recognition (U3)
updated_at: 2026-09-06T01:55:46.4682878Z
---

U3 (TDD: event schema + validator + doctor tests FIRST). Define EventShipmentReconciledShipped="shipment_reconciled_shipped", its DELTA SCHEMA, and the shared FULL-EVENT VALIDATOR + doctor recognition. U3 does NOT append the event (avoids an ordering cycle + the non-durable appendShipmentEventErr/appendFast fallback); the actual APPEND (dedicated always-fsync handle-relative appender + post-append IndexEvent) is implemented in 167.006-T (behavior). delta {before{status,archived_status}, after{archived_status:shipped}, reason, actor, second_approver, idempotency_key, evidence{merge_sha,closure_evidence,manifest_digest,member_terminal_ids,closure_content_hash,evidence_refs,evidence_digest}} (second_approver + evidence_refs always persisted, empty when unset). Doctor-LOCAL branch suppresses detectMissingShippedEvents ONLY on a FULLY-VALID event (RAW serialized item_id - no ReadAllEvents backfill - + stored key + target + evidence_digest + before/evidence fields + newline-terminated); bare/malformed/stale/duplicate/key-mismatched does NOT suppress; unreadable/path-unsafe log = presence-unknown, RETAIN finding; a fully-consistent forgery is not detectable (accepted residual, see trust model). Do NOT broaden shared shippedEventPresence (144-F guard 2 stays strict; negative test proves guard not weakened). Files internal/core/shipment_reconcile.go (event const+append), internal/core/doctor.go (recognition) + tests. Domain: code. See plan U3.