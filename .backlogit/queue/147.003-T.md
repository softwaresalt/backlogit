---
artifact_type: task
created_at: 2026-08-24T19:01:10.661472Z
dependencies:
    - 147.002-T
id: 147.003-T
labels:
    - checkpoint
    - events
    - plan-u2b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U2b: conformance helper — nested progress rule and open context namespace'
updated_at: 2026-08-24T19:01:10.661472Z
---

Plan unit **U2b**. Nested `progress` rule plus preservation of the open `context` namespace.

Files: `internal/events/checkpoint_conformance.go`, `internal/events/checkpoint_conformance_test.go`.

Recurse into every case-insensitive `progress` match using `unknownNestedProgressKeys`, reporting offending paths in the existing `progress.<key>` form. A `progress` value that is not a JSON object (null, scalar, array) is **not** a conformance failure — `ParseCheckpoint` already governs that — and must not panic.

The `context` namespace shipped open in 146-F. Sweeping it into refusal is the highest-risk regression in this plan.

<!-- BEGIN:acceptance-criteria -->
- An unknown nested key returns `progress.<key>`
- **Unmodeled `context` keys return nil** — permanent 146-F regression guard
- A non-object `progress` returns nil without panicking
- Expected red: cases 1 and 3 fail; case 2 passes trivially and is a guard
<!-- END:acceptance-criteria -->