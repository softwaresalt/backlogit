---
artifact_type: task
created_at: 2026-07-02T11:33:01.6465772-07:00
id: 073.001-T
labels:
    - hardening
    - go
    - core
    - harness-ready
parent_id: 073-F
priority: medium
references:
    - docs/exec-plans/2026-07-02-artifacts-write-nil-headerdef-hardening-plan.md
status: queued
title: Fail closed on nil header-def in CreateArtifact and UpdateArtifact
updated_at: 2026-07-02T11:33:01.6465772-07:00
---

Execution posture: TEST-FIRST (TDD). Scope: internal/core/artifacts.go (add shared helper requireHeaderDef + rewire the two call sites) and a new internal/core/artifacts_headerdef_test.go (package core_test). Fix: replace the 'if ws.HeaderDef != nil' guards at CreateArtifact (~line 224) and UpdateArtifact (~line 514) with a call to a new helper unc requireHeaderDef(ws *Workspace) error that returns fmt.Errorf('header definition not loaded; cannot validate artifact fields: %w', blerrors.ErrConfig) when ws.HeaderDef == nil. On the create path the helper MUST run BEFORE ApplyFieldDefaults/ValidateArtifactFields (both call headerDef.ResolveFieldSchema, which dereferences the nil receiver with no nil-guard -> would panic) -- this ordering is a load-bearing invariant, document it in the helper comment. blerrors is already imported in artifacts.go (zero import cost); ErrConfig is absent from domainError's validation case so it maps to MCP internal (500), never validation_failed (422) -- consistent with the local validateSizeValue / NewMetadataCatalog nil-HeaderDef convention and reconciling 072-S Option B (system/config fault, not user input). Both surfaces inherit the fix through the single shared core functions (CLI add/update/move; MCP create_item/update_item); create_item hard-maps via InternalError while update_item/move_item use domainError, but both converge to internal for this fault. Tests (3 scenarios, 1 file, reuse setupTestWorkspace): (1) TestCreateArtifact_NilHeaderDefFailsClosed -- loaded create succeeds, then ws.HeaderDef=nil create returns errors.Is(err, blerrors.ErrConfig)==true && errors.Is(err, blerrors.ErrValidation)==false, nil artifact, no file persisted; (2) TestUpdateArtifact_NilHeaderDefFailsClosed -- same fail-closed shape + on-disk title unchanged (guard precedes persistArtifact); (3) loaded-path regression (create+update succeed with schema present). Acceptance criteria: AC1 sentinel assertions fail before impl and pass after; AC2 no artifact mutation persisted on either nil path; AC3 loaded-path regression green; AC4 ErrConfig-wrap guarantees internal mapping structurally (no MCP test added); AC5 full quality-gate set (go test ./..., go vet ./..., golangci-lint run, gofmt -l .) passes whole-repo. 2 files, 3 production functions (helper + 2 edited blocks), 3 test scenarios -- within the 2-hour rule, Go-code single domain. Plan: docs/exec-plans/2026-07-02-artifacts-write-nil-headerdef-hardening-plan.md (see Decisions for the ErrConfig/internal-vs-validation rationale and the deferred advisories).