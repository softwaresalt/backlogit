---
artifact_type: task
created_at: 2026-07-15T18:46:05.9498467Z
dependencies:
    - 109.002-T
    - 109.003-T
id: 109.004-T
labels:
    - spike
    - investigate-first
    - size-estimation
    - documentation
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: Verify CLI/MCP response parity, synthesize spike findings, and record explicit proceed/pivot exit decision (2h max)
updated_at: 2026-07-16T00:21:43.4689099Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. Depends on 109.002-T and 109.003-T. (a) CLI/MCP PARITY inventory across TRUE command-to-tool pairs, covering REQUEST contract, RESPONSE shape, AND mutation validation/error, not response shape alone. (a1) READ/projection pairs a future size projection must populate identically: CLI queue view <-> MCP get_queue; CLI shipment get <-> MCP get_shipment; CLI shipment list <-> MCP list_shipments; CLI get <-> MCP get_item; CLI list <-> MCP list_items. For each, record REQUEST-CONTRACT parity (exact CLI flags vs MCP params, default filters, default sort, default grouping) and the shared-subset vs capability gaps for the queue and list surfaces, AND RESPONSE-SHAPE parity (field-for-field). (a2) PIN the CLI get --format json <-> MCP get_item comparison specifically and record an EXPLICIT decision on the body / dependencies_detail / commit_links context asymmetry (which context fields each surface returns and whether a future size projection must appear identically on both). (a3) MUTATION pair (MANDATORY evidence, not optional): the CURRENT true write pair CLI update --size <-> MCP update_item size (both route only size through core.SetArtifactSize today). Record current mutation validation/error parity as mandatory evidence across: invalid size value, unsupported artifact type, busy-lock/conflict (ErrTaskBusy), and workspace-open failures. Then, as a DECISION output (not implementation), inventory and decide the paired FUTURE CLI flags / MCP fields and validation/error semantics for size_source and size_ruleset_version across BOTH surfaces: flag/field names, accepted values, defaulting rules, rejection of unknown source/version, and identical error-SHAPE parity from CLI and MCP. (a4) Reference the 109.002-T generic rewrite-path inventory (models.ArtifactFromFrontmatter, core.WriteArtifactFile, core.SetArtifactSize, generic title/status/section updates) and confirm those paths preserve top-level size extensions so no later write drops them. (b) Synthesize the three prior investigations and record an explicit proceed/pivot/defer exit decision with confidence in the plan docline.conclusion. (c) The decision MUST include an explicit selection of the size mutation/event ordering and the rollback-versus-fail-closed policy, choosing among the LinkCommit warn-continue, AppendComment fail-surface, and gate-evidence fail-closed precedents inventoried in 109.002-T, as a first-class decision output. (d) Composition ratification is finalized here jointly with the 109.003-T composition investigation, not by the ownership inventory. (e) SIZE PLACEMENT CONSISTENCY (must be decided before any proceed): existing task size is physically stored under custom_fields.size (a NESTED location) via core.SetArtifactSize, while this spike proposes feature/shipment size as a TOP-LEVEL backlogit-owned extension. Record this inconsistency and require a CANONICAL-LOCATION decision (reconcile task custom_fields.size vs feature/shipment top-level size, including whether task size migrates to top-level or the two coexist with a documented rationale) as a first-class exit criterion. A proceed outcome is permitted ONLY if BOTH the 109.002-T containment boundary AND this canonical size-location question are resolved. A proceed decision is the sole authorization for a later, separately planned, harvested, and re-reviewed implementation (with plan-harden evaluated). Deliverable: recorded decision plus read request/response parity, mutation validation/error parity, rewrite-path preservation confirmation, canonical size-location decision, and exit-criteria confirmation. No implementation.