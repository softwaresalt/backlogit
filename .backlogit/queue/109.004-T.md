---
artifact_type: task
created_at: 2026-07-15T18:46:05.9498467Z
dependencies:
    - 109.002-T
    - 109.003-T
id: 109.004-T
labels:
    - spike
    - investigate-first
    - size-estimation
    - documentation
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: Verify CLI/MCP response parity, synthesize spike findings, and record explicit proceed/pivot exit decision (2h max)
updated_at: 2026-07-15T18:46:05.9498467Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. Depends on 109.002-T and 109.003-T. (a) CLI/MCP parity inventory across TRUE command-to-tool pairs. READ/projection pairs a future size projection must populate identically: CLI queue view <-> MCP get_queue; CLI shipment get <-> MCP get_shipment; CLI shipment list <-> MCP list_shipments; CLI get <-> MCP get_item; CLI list <-> MCP list_items. MUTATION pair: inventory the CURRENT true write pair CLI `update --size` <-> MCP `update_item` size field (both route only size through core.SetArtifactSize today) and record its write-shape parity as evidence. As a DECISION output (not an implementation), inventory and decide the paired FUTURE CLI flags / MCP fields and the validation/error semantics required for size_source and size_ruleset_version across BOTH surfaces: the future flag/field names, accepted values, defaulting rules, rejection of unknown source/version, and error-SHAPE parity (identical error contract from CLI and MCP). Record response-SHAPE parity (field-for-field) for reads AND write/validation/error parity for mutations as evidence and as explicit exit criteria. (b) Synthesize the three prior investigations and record an explicit proceed/pivot/defer exit decision with confidence in the plan docline.conclusion. (c) The decision MUST include an explicit selection of the size mutation/event ordering and the rollback-versus-fail-closed policy, choosing among the LinkCommit warn-continue, AppendComment fail-surface, and gate-evidence fail-closed precedents inventoried in 109.002-T, as a first-class decision output. (d) Composition ratification is finalized here jointly with the 109.003-T composition investigation, not by the ownership inventory. A proceed outcome is permitted ONLY if the 109.002-T containment boundary is resolved. A proceed decision is the sole authorization for a later, separately planned, harvested, and re-reviewed implementation (with plan-harden evaluated). Deliverable: recorded decision plus read parity, mutation/validation parity, and exit-criteria confirmation. No implementation.