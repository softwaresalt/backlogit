---
artifact_type: task
created_at: 2026-07-23T10:55:20.1745448-07:00
id: 122.001-T
labels:
    - mcp
    - cli-mcp-parity
    - list-items
parent_id: 122-F
priority: medium
references:
    - docs/exec-plans/2026-07-23-mcp-list-items-parity-plan.md
    - docs/decisions/2026-07-23-cli-mcp-list-items-parity-deliberation.md
status: queued
title: Add priority/owner params to backlogit_list_items (schema+handler+tests)
updated_at: 2026-07-23T10:55:20.1745448-07:00
---

TDD, single Go/MCP domain, ~8 LOC + tests, <3 files. Plan: docs/exec-plans/2026-07-23-mcp-list-items-parity-plan.md.

Steps:
1. RED: add a failing MCP-handler test in internal/mcp/*_test.go that invokes handleListItems with `priority` and `owner` arguments against a seeded workspace and asserts the filtered result set (table-driven; t.Run subtests). Confirm it fails (params currently ignored).
2. GREEN schema: in internal/mcp/tools.go:78-84 add `mcplib.WithString("priority", mcplib.Description("Filter by priority"))` and `mcplib.WithString("owner", mcplib.Description("Filter by owner (distinct from assigned_to)"))`, mirroring the existing four params.
3. GREEN handler: in handleListItems (internal/mcp/tools.go:476-488) add two reads mirroring the existing pattern: `if v, ok := request.Params.Arguments["priority"].(string); ok { filters.Priority = v }` and equivalent for `owner` -> filters.Owner.
4. Parity-lock test: add/extend a CLI/MCP request-contract parity test asserting the MCP list_items filter-param set equals the CLI `list` filter-flag set (compound: 2026-06-27-cli-mcp-catalog-parity, 2026-07-03-cli-mcp-registry-drift-test). Both-surfaces check (compound: 2026-05-07-mcp-cli-config-parity).
5. Docs/drift: regenerate any generated MCP tool-reference doc so "CLI Reference Drift" and "Docline frontmatter gate" CI jobs stay green (Ship regenerates during build).

Verified facts: data layer already supports both filters (internal/db/queries.go: QueryFilters.Owner :22 / .Priority :23; QueryItems WHERE clauses owner= :463-465, priority= :467-469). CLI reference: internal/cli/list.go:250 (--priority), :252 (--owner).

Verify: go test ./internal/mcp/... (new handler test + parity test green), go build ./cmd/backlogit, go vet ./..., golangci-lint run, gofmt -l ., CI drift + docline gates.