---
artifact_type: task
created_at: 2026-09-19T01:31:11.6260706Z
dependencies:
    - 175.031-T
id: 175.042-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/cli/telemetry.go [slice 1/3]'
updated_at: 2026-09-19T07:24:30.4735447Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck
Package: ./internal/cli
Owned source file(s) (1): internal/cli/telemetry.go
Owned baseline findings (13) from docs/decisions/2026-09-17-baseline-lint-inventory.md (golangci-lint v2.13.2, unbounded invocation):
- internal/cli/telemetry.go:L438:C15 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L459:C14 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L461:C14 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L462:C14 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L463:C15 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L464:C15 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L470:C15 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L473:C14 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L475:C14 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L476:C14 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/telemetry.go:L477:C15 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L478:C15 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/telemetry.go:L480:C15 [errcheck] Error return value of `fmt.Fprintf` is not checked

Slice ordering (shift-safety): this task owns the exact 13 highest-remaining-line identities of internal/cli/telemetry.go for slice 1/3. It is gated so higher-line slices converge before lower-line siblings, so fixing owned findings never shifts a still-queued sibling's line numbers. Ownership is by EXACT (path,line,column,linter) identity, not whole-file path.
Affected functions (2 distinct): internal/cli/telemetry.go: renderSchemaMarkdown, renderSchemaText

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/cli/lintfix_175_042_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_042_LintFix` (deterministic `TestU175_042_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/cli" -run "^TestU175_042_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_042_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_042_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return is NOT an allowed resolution): for every flagged call, capture the returned error and handle it -- checked propagation with `fmt.Errorf("context: %w", err)`, a named-return `errors.Join`, or (inside a test/benchmark) fail via the in-scope `*testing.T`/`*testing.B` receiver (`require.NoError`, `t.Fatalf`, `b.Fatalf`). A deferred close becomes a checked deferred close. No error return is discarded; no cross-file or cross-package helper is introduced; edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (owned source file(s) + one owned harness file).
- Implementation files = 1 (owned harness excluded).
- Owned baseline findings = 13 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/cli -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.042-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "packages": [
      "./internal/cli"
    ],
    "owned_files": [
      "internal/cli/telemetry.go"
    ],
    "owned_findings": [
      {
        "path": "internal/cli/telemetry.go",
        "line": 438,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 459,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 461,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 462,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 463,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 464,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 470,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 473,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 475,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 476,
        "column": 14,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 477,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 478,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/telemetry.go",
        "line": 480,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.042-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
