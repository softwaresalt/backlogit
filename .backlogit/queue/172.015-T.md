---
artifact_type: task
created_at: 2026-09-14T03:40:00Z
dependencies:
    - 172.001-T
id: 172.015-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: 'RED: AST harness asserts B/C barrier seam hook points exist'
updated_at: 2026-09-14T03:40:00Z
---

U1.seam-red [FE440C62] source-shape AST RED harness (test-first predecessor to the no-op production seam 172.013-T). Write a failing source-shape assertion (AST / type-level harness) that asserts the artifact-mutation writers expose controllable barrier hook points at BOTH the B (`lockArtifactMutations`) AND the C (item-log JSONL append) acquisition points identified by the 172.001-T canonical-order analysis. Observe RED: only a pre-B hook (`persistArtifactPreLockHook`) exists today and NO C-acquisition seam exists, so the assertion that BOTH a controllable B and a controllable C hook point are present fails BEFORE the no-op production seam declaration (172.013-T) adds the missing C hook (and the paired B hook) and turns it GREEN. This is a source-shape harness only; it does NOT force any interleaving (that is the barrier-driven behavior RED harness 172.008-T). Domain: tests. Depends on 172.001-T (canonical-order analysis names the B/C acquisition points). <=2 scenarios. AC: the harness asserts a controllable B AND a controllable C barrier hook point exist on the production writers and is observed RED (C seam absent; existing pre-B `persistArtifactPreLockHook` is insufficient) before 172.013-T lands.
