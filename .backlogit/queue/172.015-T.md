---
artifact_type: task
created_at: 2026-09-14T03:40:00Z
dependencies:
    - 172.001-T
id: 172.015-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: 'RED: AST harness asserts B/C barrier seam hook points exist'
updated_at: 2026-09-14T03:40:00Z
---

U1.seam-red [FE440C62] source-shape AST RED harness (test-first predecessor to the no-op production seam 172.013-T). Write a failing source-shape assertion using a **source-only `go/parser` + `go/ast` harness that reads the production source text as data** (parses the artifact-mutation writer `.go` files into an AST and inspects the declarations) that asserts the writers expose controllable barrier hook points at BOTH the B (`lockArtifactMutations`) AND the C (item-log JSONL append) acquisition points identified by the 172.001-T canonical-order analysis. The harness MUST compile and run WITHOUT importing or referring to the not-yet-declared B/C hook symbols (the missing controllable C-acquisition hook, and the paired B hook 172.013-T adds) — it operates on the parsed AST, not on symbol/type/reflect references to hooks that do not exist yet — so it builds cleanly before 172.013-T lands. Observe RED as an **AST-shape-assertion failure ONLY**: only a pre-B hook (`persistArtifactPreLockHook`) is present in the parsed source today and NO C-acquisition seam exists, so the assertion that BOTH a controllable B and a controllable C hook point are present fails on AST shape — RED MUST NOT be a compile-time / missing-symbol build failure. The no-op production seam declaration (172.013-T) then adds the missing C hook (and the paired B hook) and turns the AST assertion GREEN. This is a source-shape harness only; it does NOT force any interleaving (that is the barrier-driven behavior RED harness 172.008-T). Domain: tests. Depends on 172.001-T (canonical-order analysis names the B/C acquisition points). <=2 scenarios. AC: the harness compiles and runs before 172.013-T; it parses the production source via go/parser/go/ast and asserts a controllable B AND a controllable C barrier hook point exist on the production writers; it is observed RED by AST shape assertion failure (never a compile failure — C seam absent in the parsed AST; existing pre-B `persistArtifactPreLockHook` is insufficient) before 172.013-T lands.
