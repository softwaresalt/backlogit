---
artifact_type: task
created_at: 2026-09-26T04:10:07.8354243Z
dependencies:
    - 174.082-T
id: 174.080-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - mcp-adapter
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: queued
title: Route MCP normalize through guarded path and require actor
updated_at: 2026-09-26T04:13:26.714277Z
---

Wave 20 work unit U20C4 (unit token U20C4, P-002.6 prefix ^TestU20C4_) for the 155-S final-review remediation. Findings ADV-B and ADV-C (adversarial, confirmed in scope under P-021 C1/C3): (B) internal/mcp/tools.go selects the target-guarded normalize recovery path only when workspace initialization fails, so a warm server takes ordinary auto-recovery and can be blocked by a later poison journal; (C) the normalize tool schema leaves by optional although core rejects an empty actor.

Fix (internal/mcp/tools.go): handleNormalizeBlockedShipment keeps its current order (workspace or diagnostic-workspace acquisition, then the id and snapshot_ref checks); right after those it rejects strings.TrimSpace(by) == "" with ValidationFailed("by is required"); it then ALWAYS calls core.NormalizeBlockedShipmentForRecovery, passing the workspace from requireWorkspace when that succeeds and the diagnostic workspace otherwise. The recoveryRemediation branch to core.NormalizeBlockedShipment is removed. RegisterTools marks by as mcplib.Required(). Tool name, other parameters and error mapping are unchanged. The target-shipment guard is preserved (hardened at aggregate scope by 174.082-T).

Domain: MCP adapter (Go). Class: NORMAL harness-required. Width note: the Required() edit changes the handler's own advertised tool contract in the same file for the same tool; it is not a data-schema change paired with an API handler. Files: implementation 1 (internal/mcp/tools.go: RegisterTools normalize schema, handleNormalizeBlockedShipment); harness 1 (internal/mcp/shipment_normalize_u20c4_harness_test.go, package mcp, no t.Parallel). Wave W2. Depends on 174.082-T.

Authoritative contract: plan Wave 20 sections 20.3.4, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=10m -v -run '^TestDiagnosticToolsRemainAvailableWithPoisonedLifecycleJournal$' ./internal/mcp",
    "go test -count=1 -timeout=10m -v -run '^(TestRegistryParity_|TestUR8_)' ./internal/cli",
    "go test -count=1 -timeout=10m -v -run '^(TestNormalizeBlockedShipmentForRecovery_|TestU20C6_)' ./internal/core"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly two top-level functions in internal/mcp/shipment_normalize_u20c4_harness_test.go; no other function in the repository matches ^TestU20C4_. Both dispatch through the registered tool (s.InvokeTool or the package's callToolForTest equivalent), never the handler directly.
  - TestU20C4_WarmServerNormalizeUsesTargetGuardedRecoveryPath: build the blocked-shipment fixture in the shape of newPoisonedNormalizationWorkspace with the unrelated poison moved aside; warm the server (a workspace-requiring read tool succeeds), then register t.Cleanup(func(){ require.NoError(t, server.Workspace.Close()) }); restore the poison and invoke normalize with by. Expect not IsError; poison still present and byte-identical; core.NewWorkspace on the root still fails with an error naming the poison (contains unrelated-poison.json or the shipment-operation journal validation failure), ruling out lock contention. RED at 3a240fe0: the warm path runs ordinary auto-recovery and fails on the poison.
  - TestU20C4_NormalizeToolRequiresActor: ToolDefs() for backlogit_normalize_blocked_shipment has InputSchema.Required containing id, snapshot_ref and by; an invocation without by is IsError with error code validation_failed and a message containing 'by is required' (handler-specific, distinct from core's actor message); inline row: the same for by: '   ' (whitespace only); shipment file bytes and shipment event count unchanged. RED at 3a240fe0: by is not in Required and the message is core's actor message.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C4_' ./internal/mcp -> exactly 2 top-level PASS when GREEN.
  - RED at 3a240fe091422e96d22b75b9af0854f66b60340f (deliverable absent) as above; compile check go test -run '^$' -count=1 ./... passes; '[no tests to run]' or 'no test files' is a halt. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready.
AC2 (Fix): normalize always routes through core.NormalizeBlockedShipmentForRecovery (warm and cold); by is schema-required and whitespace-only by is rejected with ValidationFailed('by is required') after the id/snapshot_ref checks; tool name, other params and error mapping unchanged.
AC3 (Regression, exact counts, fail closed): command 1 exactly 1 top-level PASS; command 2 exactly 13 (TestRegistryParity_ 10 + TestUR8_ 3); command 3 exactly 6 (TestNormalizeBlockedShipmentForRecovery_ 4 + TestU20C6_ 2). Zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC4 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/mcp/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/mcp/tools.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope.
SHAs: one harness commit per task (HARNESS_COMMIT, staged by exact path); HARNESS_BASE_SHA = its parent; START_SHA = git rev-parse HEAD at claim. Captures under logs/diagnostics/174080-*, never staged. H10 t.Parallel audit before scaffolding.
Wave W2 = {174.078-T, 174.080-T}; production files disjoint. Edge: 174.080-T depends on 174.082-T (the aggregate guard must be GREEN before the warm path routes through it). Wave convergence records FULL_SUITE_OPERATOR_DEFERRED: wave 2. No go test ./... with tests selected. No push until W3 convergence and the W3 pre-push gate are GREEN.
The MCP server's cached workspace handle must be closed in t.Cleanup before TempDir removal (Windows).
Size: 2 files, 2 production declarations. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage.
Documented deviations: EW-1, QG-1, QG-2, TG-1, TG-2. Related deferred captures (do not implement): 09D06A75 (missing MCP reopen tool, scan consolidation, NormalizeBlockedShipment doc comment), 9900D0DD (uniform refusal format).
<!-- END:implementation-notes -->