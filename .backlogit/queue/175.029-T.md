---
artifact_type: task
created_at: 2026-09-19T01:27:33.5799739Z
dependencies:
    - 175.011-T
id: 175.029-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (staticcheck): internal/gateevidence/formal.go'
updated_at: 2026-09-19T07:20:43.0000435Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: staticcheck
Package: ./internal/gateevidence
Owned source file(s) (1): internal/gateevidence/formal.go
Owned baseline findings (1) from docs/decisions/2026-09-17-baseline-lint-inventory.md (golangci-lint v2.13.2, unbounded invocation):
- internal/gateevidence/formal.go:L223:C2 [staticcheck] QF1003: could use tagged switch on schema
Affected functions (1 distinct): internal/gateevidence/formal.go: envelopeFromEvent

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/gateevidence/lintfix_175_029_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_029_LintFix` (deterministic `TestU175_029_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/gateevidence" -run "^TestU175_029_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_029_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_029_ lines"; exit 1 }; exit 0'

Fix strategy (staticcheck): apply the exact staticcheck-suggested rewrite named in each finding (QF1012/QF1011/QF1003 quickfix or ST1008 result-ordering) directly in source. Do NOT suppress with `//nolint`; do NOT alter observable behavior beyond the mechanical rewrite. Edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (owned source file(s) + one owned harness file).
- Implementation files = 1 (owned harness excluded).
- Owned baseline findings = 1 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/gateevidence -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.029-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "packages": [
      "./internal/gateevidence"
    ],
    "owned_files": [
      "internal/gateevidence/formal.go"
    ],
    "owned_findings": [
      {
        "path": "internal/gateevidence/formal.go",
        "line": 223,
        "column": 2,
        "linter": "staticcheck",
        "message": "QF1003: could use tagged switch on schema"
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.029-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
