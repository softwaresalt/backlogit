---
artifact_type: task
created_at: 2026-06-30T22:51:20.6640496-07:00
dependencies:
    - 071.007-T
id: 071.008-T
labels:
    - size:S
    - phase-2
    - unit:U8
    - test-first
    - work-item:4-mutation-cli
parent_id: 071-F
priority: medium
references:
    - docs/exec-plans/2026-06-30-backlogit-deterministic-gates-slice-plan.md
status: queued
title: Add backlogit update --size CLI flag routing to SetArtifactSize
updated_at: 2026-06-30T22:51:20.6640496-07:00
---

Execution posture: TEST-FIRST. Depends on U7 (071.007-T). Scope: register --size string on internal/cli/update.go. When set, route to core.SetArtifactSize (NOT the generic updates map — a bare updates[size] is dropped by UpdateArtifact's type-switch). --size is MUTUALLY EXCLUSIVE with other frontmatter-mutating flags (--status/--priority/etc.): if combined, error BEFORE any write, because those flags route through the generic UpdateArtifact->WriteArtifactFile rebuild path and running both would double-write and negate body preservation. Single-purpose: --size performs the body-preserving size mutation only. On a BUSY task lock (concurrent doctor --target/mutation holds it), surface the SAME non-zero busy exit code as the doctor table (4) — do not block — so the autoharness sizing hook sees deterministic contention behavior. Emit a clear enum error for out-of-enum values. ACCEPTANCE CRITERIA: (1) backlogit update {id} --size M persists and backlogit get {id} shows size:M with on-disk body unchanged (E2E body-preservation smoke); (2) --size XXL exits non-zero with an enum error; (3) --size M --status done (combined) errors before writing (mutual-exclusion guard); CLI test green + live dogfood on a scratch task.