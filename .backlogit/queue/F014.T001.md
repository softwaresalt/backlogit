---
artifact_type: task
created_at: 2026-04-03T23:45:25.8984747-07:00
id: F014.T001
labels:
    - config
    - spike
parent_id: F014
priority: medium
status: queued
title: Add spike type to config and header-def
updated_at: 2026-04-03T23:45:25.8984747-07:00
---

Add the `spike` artifact type to `config.yaml` and `header-def.yaml` with prefix `SP`, level 1 placement alongside `feature`, and a `spike_ref` custom field.

In `.backlogit/config.yaml`:
- Add `spike` entry under `artifact_types` with `prefix: SP`, `name_format: "{prefix}{NNN}"`, and `allowed_children: []`
- Add `spike` to `queue_layout.levels[0].types` alongside `feature`

In `.backlogit/header-def.yaml`:
- Add `spike` type block with `prefix: SP`, `id_format: "{prefix}{NNN}"`, and a `spike_ref` field of type `string` (optional, holds the ID of the linked feature/task/epic)

In `internal/config/defaults.go`:
- Update the default config and header-def templates to include `spike`

Files to modify: `.backlogit/config.yaml`, `.backlogit/header-def.yaml`, `internal/config/defaults.go`

Verification: `backlogit sync` succeeds after config changes. Config loader validates the new type without errors.