---
artifact_type: task
created_at: 2026-07-16T00:15:17.7525726Z
dependencies:
    - 107.010-T
id: 107.011-T
labels:
    - schema
    - docline
    - extension
    - test-first
parent_id: 107-F
priority: low
references:
    - schemas/docline/base-frontmatter-v1.schema.json
    - internal/docline/validate.go
    - docs/exec-plans/2026-07-14-docline-soft-key-regression-guard-plan.md
status: queued
title: Open docline base schema for declared producer-owned top-level extension fields (test-first)
updated_at: 2026-07-16T00:15:17.7525726Z
---

EXECUTABLE schema-contract task (<=2h, width-isolated to the docline base schema family; introduced for the PR #241 correction). Test-first and GATED behind the RED-first regression 107.010-T. The authoritative base contract is open/extensible so derived producers (backlogit) may carry top-level extension fields, but schemas/docline/base-frontmatter-v1.schema.json currently declares top-level additionalProperties:false, which contradicts that design and rejects the planned producer-owned top-level extension keys (size, size_source, size_ruleset_version). Open the base schema for producer-owned top-level extension fields using the LEAST-PERMISSIVE compatible mechanism justified by current schema/test patterns (for example declaring the sanctioned producer extension properties as optional typed top-level fields, or a scoped patternProperties/definitions namespace) rather than a blanket additionalProperties:true, so DECLARED producer extensions validate at the top level while truly-unknown/legacy arbitrary keys stay distinguishable from sanctioned extensions. Test-first: write the FAILING schema-validation coverage first (declared producer extension keys validate top-level; the chosen mechanism still distinguishes sanctioned extensions from legacy arbitrary keys), then change the schema to turn it green. Keep the hand-rolled Go validator internal/docline/validate.go consistent with the schema contract (it does not enforce additionalProperties today, so no Go behavior regression is introduced here). Coordinate coherently with the codec pass-through task 107.009-T (preserves unknown top-level keys in place) and the authoring-guide task 107.012-T (documents the inheritance model); the Schema-CLI-Docs Coupling Reviewer must confirm the three stay consistent. Ownership stays explicit: docline owns only the base contract and never validates/defaults/emits extension semantics; size domain semantics remain backlogit-owned and out of scope. No CLI-distribution changes.