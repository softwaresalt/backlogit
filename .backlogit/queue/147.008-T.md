---
artifact_type: task
created_at: 2026-08-24T19:02:41.2322178Z
dependencies:
    - 147.004-T
    - 147.028-T
    - 147.016-T
    - 147.044-T
id: 147.008-T
labels:
    - checkpoint
    - core
    - plan-u4
    - merge-gate-dependent
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: active
title: 'U4: AbandonCheckpoint conformance gate placed before the already-abandoned short-circuit'
updated_at: 2026-08-27T22:23:05.7270347Z
---

Plan unit **U4**. Conformance gate on `AbandonCheckpoint`.

**HARD MERGE GATE backreference.** This task is one of the three units the `147.018-T` / U9b hard
merge gate constrains. A pull request containing this task MUST NOT merge unless the
`.github/instructions/backlogit.instructions.md` delta from `147.018-T` is present in the same
merge commit. The gate is owned and stated in `147.018-T` and in the plan's U9b
"Acceptance / merge gate" bullet; this backreference exists so the constraint is discoverable from
here. It constrains implementation units to each other inside one pull request and adds **no**
dependency edge — executable ordering is unchanged.

Files: `internal/core/checkpoint_disposition.go`, `internal/core/checkpoint_disposition_test.go`.

Call `events.CheckConformingTopLevelNamespace(data)` **immediately after `ValidateCheckpoint` and BEFORE the already-abandoned short-circuit** (`~:78-80`), returning its typed error unchanged.

Placement matters twice:
* *Before the short-circuit* — a non-conforming file carrying `disposition: "abandoned"` would otherwise return `nil` (reported success, no write) from abandon while quarantine accepts it and list/get report `needs_quarantine: true`: three surfaces disagreeing about one file. This is "authenticate before filter" applied literally.
* *Before `appendCheckpointDispositionAudit`* — the shipped ordering guarantee is audit-then-mutate, so a refusal must leave no audit event and no byte change.

It is a non-writing refusal, so nothing is lost by refusing earlier.

<!-- BEGIN:acceptance-criteria -->
- A valid-but-non-conforming active document is refused with `ErrCheckpointNonConforming` naming the keys
- The disposition audit JSONL is byte-unchanged after that refusal
- A non-conforming **already-abandoned** document returns `ErrCheckpointNonConforming` rather than `nil`
- The existing "conforming active document abandons successfully" test stays green
- Red harness (P-002/P-004 gate): all three scaffolded `TestU4_` functions fail
- Green-step guards: none. Every scenario is a red harness function
<!-- END:acceptance-criteria -->