---
artifact_type: task
created_at: 2026-09-20T05:05:00.0000000Z
dependencies:
    - 175.099-T
id: 175.100-T
parent_id: 175-F
priority: high
references:
    - docs/decisions/2026-09-19-baseline-convergence-decomposition.md
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'RS-W(-1) prerequisite: bootstrap shared baseline-lint runner (depends on 175.099-T)'
updated_at: 2026-09-20T12:10:00.0000000Z
---

Runner-bootstrap PREREQUISITE member of the baseline-convergence feature `175-F`, dependency-ordered after the task-lint runner-bootstrap task `175.099-T` (`175.100-T depends_on 175.099-T`). It OWNS creation of exactly one shared runner, the intermediate-wave monotonicity runner `scripts/verify-baseline-lint.ps1`, invoked once per active replacement shipment in RS-W00..RS-W12 order. Single-domain (verification tooling: one PowerShell runner plus one Go harness). The terminal-lint runner-bootstrap task `175.101-T` depends on this member.

## Why this task exists (bootstrap split, PR #449 cycle 9)

The prior aggregate bootstrap task owned three runners plus one harness (four files) with a seven-plus-scenario matrix, exceeding the repository task-granularity contract. It is split into three dependency-ordered runner-specific tasks; this member owns the intermediate-wave baseline-lint runner only.

## Owned deliverable surface (created by Ship during execution; NOT implemented in this planning PR)

- `scripts/verify-baseline-lint.ps1` — the intermediate-wave remaining-baseline monotonicity runner (`-Inventory ... -InventorySha256 ... -Shipment <replacement-shipment-id> -FeatureId 175-F -TerminalTask 175.012-T`).
- `tests/runner_bootstrap_baseline_lint_175_100_test.go` — the EXCLUSIVELY OWNED Go harness (`TestU175_100_BaselineLintRunnerBootstrap`, deterministic `TestU175_100_` token; no other member uses this path or prefix).

These are the ONLY two files this task may create (one implementation runner plus one owned harness). It creates NO Go source, NO workflow, and modifies NO existing script.

## Bootstrap harness (self-contained, non-vacuous; RED before / GREEN after)

BOOTSTRAP INVARIANT: the task-lint GATE for this member is the owned Go harness invoked as `go test ./tests -run ^TestU175_100_`, NEVER a direct invocation of the runner this task creates. The gate MUST COMPILE and its targeted test MUST EXECUTE under existing main-branch tooling BEFORE `scripts/verify-baseline-lint.ps1` exists: the harness COMPILES cleanly but its assertions FAIL when run (RED, nonzero), and passes GREEN (exit 0) only after the runner is implemented correctly. Compile success never implies test success.

The harness proves the following using ONLY existing main-branch tooling (`go test` + the built-in PowerShell 7 AST parser and fixture-driven runner invocation).

STATIC layer (files-present proof; RED before the runner is created):

1. `scripts/verify-baseline-lint.ps1` exists as a tracked, non-ignored, non-empty file under `scripts/`.
2. It PARSES CLEANLY under the built-in PowerShell AST parser with zero parse errors.
3. It declares its required `param(...)` surface: `Inventory`, `InventorySha256`, `Shipment`, `FeatureId`, `TerminalTask`.

BEHAVIORAL layer (at most three scenario classes; consolidated table-driven; non-vacuous). After the runner exists the harness EXERCISES `verify-baseline-lint.ps1` as a controlled fixture-driven subject under the `line-stable-group-multiset/v1` identity model (remaining-baseline monotonicity evaluated as an expected residual multiset per stable `group_fingerprint`; `line`/`column` never compared):

- `green_success`: a fixture inventory whose actual SHA-256 equals `-InventorySha256` and whose remaining baseline is monotone -> native exit 0.
- `red_integrity_monotonicity` (table-driven RED matrix): a tampered inventory whose actual content hash does NOT match `-InventorySha256`; a shifted-descendant where a completed-owner occurrence moved line but was not resolved so group residual exceeds expected; a missing case where an owned occurrence was resolved by no completed owner; and a new-fingerprint / reordered-across-distinguishable-groups case -> each native NONZERO.
- `fail_closed_input` (table-driven fail-closed matrix): a missing inventory file or malformed inventory content -> native NONZERO.

Non-vacuity evidence: matched-hash monotone and tampered/non-monotone fixtures MUST yield DIFFERENT exit codes, so a no-op runner is rejected.

## Independence and safety posture

- INDEPENDENTLY COMPILABLE (RED before / GREEN after): the harness compiles under existing main-branch tooling with no dependency on the runner pre-existing; test execution is RED (nonzero) before the runner exists and GREEN after.
- NO RETROFIT of YAML BDD: one imperative PowerShell runner and one Go harness only.
- Governed-claim posture: the governed claim dirties only this task's own status artifact `.backlogit/queue/175.100-T.md` and the append-only `.backlogit/hooks_queue.jsonl`; both are outside the owned `scripts/` and `tests/` surface. FAIL closed on any dirty path outside the CLOSED union {the two governed claim paths} ∪ {`scripts/verify-baseline-lint.ps1`, `tests/runner_bootstrap_baseline_lint_175_100_test.go`}.

## Acceptance criteria

- `scripts/verify-baseline-lint.ps1` exists as a tracked, non-ignored, non-empty file, and `tests/runner_bootstrap_baseline_lint_175_100_test.go` declares `TestU175_100_BaselineLintRunnerBootstrap`.
- The EXECUTABLE non-vacuity wrapper (package `./tests`, anchored `^TestU175_100_`) is RED before the runner exists and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_100_` line; propagates native nonzero exit).
- Static layer: the harness proves the runner exists, is tracked, is non-empty, parses cleanly, and declares its required `param(...)` surface (`Inventory`, `InventorySha256`, `Shipment`, `FeatureId`, `TerminalTask`).
- Behavioral layer (at most three scenario classes, non-vacuous): `green_success`, `red_integrity_monotonicity`, and `fail_closed_input` as above, with matched-hash monotone and tampered/non-monotone fixtures yielding DIFFERENT exit codes.
- The GATE never becomes circular: the task-lint command runs only the Go harness (`go test ./tests -run ^TestU175_100_`) and is never a direct invocation of `scripts/verify-baseline-lint.ps1`.
- No Go source, workflow, or existing script is modified; the only created files are the one runner and the one harness.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$o = & go test \"./tests\" -run \"^TestU175_100_\" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match \"^--- PASS: TestU175_100_\" }).Count; if ($p -lt 1) { Write-Output \"NON-VACUITY-FAIL: zero named --- PASS: TestU175_100_ lines\"; exit 1 }; Write-Output \"TASK-LINT-OK:175.100-T:runner-bootstrap-baseline-lint\"; exit 0'",
  "lint_scope": {
    "kind": "runner-bootstrap-self-contained",
    "owned_runner_script": "scripts/verify-baseline-lint.ps1",
    "owned_file": "tests/runner_bootstrap_baseline_lint_175_100_test.go",
    "package": "./tests",
    "test_function": "TestU175_100_BaselineLintRunnerBootstrap",
    "gate_must_not_be_runner": "scripts/verify-baseline-lint.ps1",
    "harness_invokes_runner_as_fixture_subject": true,
    "static_proofs": [
      "scripts/verify-baseline-lint.ps1 exists as a tracked, non-ignored, non-empty file under scripts/",
      "it parses cleanly under the built-in PowerShell AST parser with zero parse errors",
      "it declares its required param(...) surface: Inventory, InventorySha256, Shipment, FeatureId, TerminalTask"
    ],
    "behavioral_scenarios": {
      "runner": "verify-baseline-lint.ps1",
      "invocation": "-Inventory <path> -InventorySha256 <hash> -Shipment <replacement-shipment-id> -FeatureId 175-F -TerminalTask 175.012-T",
      "identity_model": "line-stable-group-multiset/v1",
      "scenario_classes": {
        "green_success": "fixture inventory whose actual sha256 equals -InventorySha256 and whose remaining baseline is monotone (expected residual multiset per group_fingerprint) -> native exit 0",
        "red_integrity_monotonicity": "table-driven RED matrix (tampered inventory whose actual content hash != -InventorySha256; shifted-descendant where a completed-owner occurrence moved line but was not resolved so group residual exceeds expected; missing where an owned occurrence was resolved by no completed owner; new-fingerprint / reordered-across-distinguishable-groups) -> native nonzero",
        "fail_closed_input": "table-driven fail-closed matrix (missing inventory file; malformed inventory content) -> native nonzero"
      },
      "non_vacuity": "matched-hash monotone fixture and tampered/non-monotone fixtures must yield different exit codes; comparison keys on group_fingerprint plus multiplicity, never on exact line/column"
    },
    "fixture_injectability_requirement": "the runner must accept inputs via its declared parameter surface and an execution working directory so the harness drives it against harness-authored deterministic fixtures without mutating live repository state",
    "safety": "harness constructs all fixtures in temp directories and invokes the runner via fixed argument vectors (no shell string, no untrusted-input interpolation)",
    "tooling": "existing main-branch go test + PowerShell 7 only; the runner this task creates is never used as this task's gate",
    "bootstrap_rationale": "This member OWNS creation of scripts/verify-baseline-lint.ps1; its GATE is its owned Go harness (never a direct invocation of the runner it creates) and MUST compile with its targeted test executing before the runner exists (RED nonzero before, GREEN after). It is dependency-ordered after the task-lint runner-bootstrap task 175.099-T."
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.100-T:runner-bootstrap-baseline-lint",
    "behavioral_discriminators": [
      "verify-baseline-lint.ps1: matched-vs-tampered inventory sha256 fixtures yield different exit codes; a shifted-descendant occurrence is caught by fingerprint+multiplicity, not line/column"
    ],
    "rejects": "a no-op runner that only parses and declares params but never enforces inventory integrity or remaining-baseline monotonicity"
  }
}
```

<!-- END:task-lint-contract -->
