---
artifact_type: task
created_at: 2026-09-06T02:07:30.646237Z
dependencies:
    - 167.006-T
    - 167.002-T
    - 167.011-T
id: 167.007-T
parent_id: 167-F
priority: critical
status: queued
title: Durable event appender (fsync + IndexEvent)
updated_at: 2026-09-06T02:09:09.4922086Z
---

Durable event appender PRIMITIVE (deps 167.006 writer + 167.011 item-log lock primitive + 167.002 event schema). The item-log lock is acquired by the TRANSACTION (167.008), NOT here (single acquirer, non-reentrant); this primitive runs UNDER the caller's already-held item-log lock and MUST NOT re-acquire it. Implement the always-fsync reconciliation append that runs under the held lock: logs-dir handle (no-follow/reparse-safe) -> raw-bytes partial-tail check (newline-less trailing line => indeterminate, never concatenate) -> handle-relative fsync WRITE primitive (NOT EventWriter.AppendEvent, which re-derives path/reacquires path-lock/opens by path) -> post-append bldb.IndexEvent (item_log_entries query visibility; index failure does not un-durable the JSONL) -> exact re-read+validate via the shared full validator. Classify append errors (ErrWriteNotApplied/ErrWriteIndeterminate); no second append after indeterminate. TDD (policy): the fsync append primitive is DECLARED with a panic body in 167.003 (gated by the source-shape harness); THIS task lands the BEHAVIOR harness (RED against the panic body; partial-line, durable_writes-off, index-visibility, symlink-swap) then the implementation (GREEN). Files internal/core/shipment_reconcile.go (appender) + tests. Domain: code. See plan U1. Deps 167.006 + 167.011 + 167.002.