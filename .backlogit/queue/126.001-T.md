---
artifact_type: task
created_at: 2026-07-25T22:31:43.7516025Z
id: 126.001-T
labels:
    - config
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U1: Add .markdownlint.json (exactly MD001/MD025/MD041) + verified scope mechanism'
updated_at: 2026-07-25T22:31:43.7516025Z
---

Domain: config. Create .markdownlint.json with default:false and exactly the three rules MD001, MD025, MD041 enabled (no other rule IDs). Provide the scope/ignore mechanism aligned to CI's existing .backlogit/** exclusion. The Day-1 ignore set MUST cover EVERY currently-violating tracked path so `make md-lint` is provably exit-0 on Day 1 (empirically re-verified 2026-07-25 with a default:false + MD001/MD025/MD041 run: dirty tracked dirs = .backlogit/, .autoharness/, .github/skills/, plugin/, docs/archive/, docs/closure/, docs/compound/, docs/decisions/, docs/design-docs/, docs/exec-plans/, docs/memory/, docs/research/, docs/reviews/; plus ephemeral .copilot/, .copilot-tracking/, logs/ and defensive node_modules/), AND the two root file offenders README.md and AGENTS.md (both MD025) MUST be ignored at FILE granularity because they sit in an otherwise-clean repo root. STRAGGLER RULE: because the ignore list is at directory granularity, any dirty file inside an otherwise-linted directory must be either file-ignored or fixed so `make md-lint` stays genuinely exit-0 Day 1 — re-run the linter over the whole tree and confirm exit 0 BEFORE U4 lands. Honest framing: Day-1 the gate effectively enforces only the currently-CLEAN corpus (e.g. clean docs/* subdirs, .github non-skills, tests/, cmd/, internal/) and is fail-closed for new/edited files there; ignored dirs graduate into scope per the plan's future-widening roadmap (which must stay consistent with this ignore set). VERIFIED SCOPE MECHANISM (decided, plan-review Architecture P2): an empirical run on the inventoried cli2 v0.23.1 confirmed a bare .markdownlintignore is NOT honored by cli2 (a listed file is still linted), whereas the ignores glob array inside .markdownlint-cli2.jsonc IS honored (excluded as expected); cli2 also auto-reads .markdownlint.json for RULE config. Provision the Day-1 dirty-bucket list as the ignores array in .markdownlint-cli2.jsonc (NOT .markdownlintignore), plus globs:["**/*.md"] there, auto-discovered by a bare pinned markdownlint-cli2@0.23.1 invocation. Equivalent alternative: pass the ignore set as negated CLI globs in the Makefile target — the config-file approach is primary. Files: .markdownlint.json (rules) and .markdownlint-cli2.jsonc (globs + ignores).