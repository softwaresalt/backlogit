---
artifact_type: feature
created_at: 2026-09-05T04:40:50.3906755Z
custom_fields:
    harness_status: pending
id: 166-F
priority: high
status: queued
title: Cross-surface gate-payload parity tracked defect (report-only owner)
updated_at: 2026-09-05T04:40:50.3906755Z
---

STANDALONE tracked-defect owner (NOT a member of any shipment, NOT a descendant of any shipped feature — deliberately outside the 138-S/156-F hierarchy so ShipShipment descendant expansion never sweeps it and the completion gate never rejects it). Durable owner for the KNOWN cross-surface drift the S4 fault-line harness allowlists as a GREEN report-only expected divergence: the CLI `--json` gate-error payload (`gateJSONPayload`) OMITS the `remediation` and `retry_after_ms` fields that the MCP gate-error surface emits. The S4 U2/U3 expected-divergence scenario pins THIS id (166-F) as its `TrackedDefect`, giving the divergence concrete, queryable ownership (not a free-form string). Future production remediation: add `remediation`/`retry_after_ms` to the CLI `--json` gate-error payload to match MCP, add field-parity tests on both surfaces, then flip the S4 expected-divergence scenario from report-only to fail-closed. Supersedes the mis-parented 156.007-T (archived).
