---
artifact_type: task
created_at: 2026-09-17T04:09:39.3015122Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 170.003-T
id: 170.017-T
parent_id: 170-F
priority: high
status: queued
title: Token nonce atomic check-then-consume + audit
updated_at: 2026-09-17T04:09:39.3015122Z
---

Token nonce atomic check-then-consume + consume audit (behavior). Carved from 170.003-T per plan-review cycle-2 item 6. Complements 170.003-T (durable append+fsync nonce record, replay-reject, prune). This task owns the ATOMIC check-then-consume seam: the presence check and the consume are a single atomic operation so two concurrent verifications of the same nonce cannot both succeed, and each successful consume persists a consume audit event. Domain: core (internal/core). TDD: RED harness first including a concurrency probe. Depends on 170.003-T. Executable scenarios (3): (a) a first consume of a fresh nonce succeeds and persists a consume audit; (b) two concurrent consumes of the same nonce yield exactly one success and one typed replay rejection (atomicity); (c) a consume-audit-write failure surfaces a typed error without leaving the nonce ambiguously half-consumed. Build/test: go build ./cmd/backlogit; go test ./internal/core/ -run TestTokenNonceAtomicConsumeAudit. AC: (1) RED test first, observed failing; (2) check-then-consume is atomic (exactly one of two concurrent consumes succeeds); (3) each consume persists a consume audit; (4) consume-audit failure is typed and leaves no half-consumed state; (5) tests GREEN.