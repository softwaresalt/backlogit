---
artifact_type: task
created_at: 2026-06-29T18:36:23.276387-07:00
custom_fields:
    source_stash_id: 2797E9F8
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: 'Refactor internal/db logging to dependency-inject a *slog.Logger into Rehydrate/warnOnDuplicateSourceIDs (and related db functions) so tests can capture log output without mutating the global slog default via slog.SetDefault. Optional design improvement raised in PR #132 Copilot review (066.004-T). Not a flakiness defect: go test isolates packages in separate processes and the existing test is sequential with t.Cleanup restore. Tracking as a future test-ergonomics/observability cleanup across the db package.'
id: 070.002-T
parent_id: 070-F
priority: low
status: queued
title: Dependency-inject slog.Logger into internal/db Rehydrate path
updated_at: 2026-06-29T18:37:22.8598542-07:00
---

Refactor internal/db logging to inject a *slog.Logger into Rehydrate / warnOnDuplicateSourceIDs (and related db functions) so tests can capture log output without mutating the global slog default via slog.SetDefault (see internal/cli/root.go). Optional design improvement from PR #132 Copilot review (066.004-T); test-ergonomics/observability cleanup, not a flakiness defect.

<!-- BEGIN:acceptance-criteria -->
- Rehydrate / warnOnDuplicateSourceIDs (and related internal/db functions) accept an injected *slog.Logger instead of relying on the global slog default.
- A db test captures duplicate-source-id warning output via the injected logger without calling slog.SetDefault.
- Existing internal/db tests (066_rehydrate_dup_warning_test.go and rehydration tests) pass; no production behavior change.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Thread a *slog.Logger (parameter, or a small logger field/option) through Rehydrate/warnOnDuplicateSourceIDs and update callers, including the internal/cli/root.go wiring that currently calls slog.SetDefault. Pure ergonomics/observability refactor, not a flakiness fix (go test isolates packages; existing test uses t.Cleanup restore). Source: PR #132 Copilot review (066.004-T). ~2h, single domain (Go code).
<!-- END:implementation-notes -->