---
artifact_type: feature
created_at: 2026-07-25T22:30:53.2226982Z
custom_fields:
    harness_status: pending
    source_deliberation_id: 054-DL
    source_stash_id: B3D30415
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'Provision the markdownlint tooling that policy P-008 already assumes. .github/policies/workflow-policies.md (P-008) states as a precondition that "The workspace has a .markdownlint.json config enabling MD001, MD025, MD041" and postconditions `markdownlint "**/*.md"` exits 0 — but the repo ships NO .markdownlint.json, no Makefile md-lint target, and no markdownlint CI job (only make docs / make docs-lint exist). So the P-008 markdown heading-hierarchy gate is not reproducible or CI-enforced; agents fall back to markdownlint environment defaults. Fix: add .markdownlint.json enabling exactly MD001/MD025/MD041, a Makefile target (e.g. md-lint) invoking markdownlint-cli2 (or equivalent), and a CI job so Ship and CI apply the same rules. Surfaced by PR #297 Copilot review of the 125-F docs-consistency plan (thread on U6 markdown-structure lint).'
id: 126-F
labels:
    - chore
    - tooling
    - ci
    - markdownlint
    - p-008
    - tech-debt
priority: medium
status: queued
title: 'Provision markdownlint (P-008) tooling: config, Makefile target, CI job, policy reconciliation'
updated_at: 2026-07-25T22:30:53.2226982Z
---

Nature: chore/tech-debt (top-level artifact created as feature since the workspace WIT config has no chore type). Provision the markdownlint tooling that policy P-008 assumes but the repo lacked, taking the REPO-WIDE doctor-to-compliance direction (this supersedes the earlier scoped-ignore approach recorded in deliberation 054-DL). Deliverables: a `.markdownlint.json` enabling exactly MD001/MD025/MD041 with MD025's `front_matter_title` retargeted to a non-existent `_title` key (so a frontmatter `title:` is not counted as a heading — eliminating all 229 frontmatter-`title:`/body-`# H1` MD025 double-counts with zero file edits, while MD041 stays default and still credits `title:`); a `.markdownlint-cli2.jsonc` runner config with `gitignore: true` so the gate lints exactly the version-controlled corpus (identical to CI's checkout, excluding local scratch); remediation of the ~21 real structural violations (20 SKILL.md files missing a leading `# H1` → MD041, and 1 H3→H4 skip → MD001); paired `scripts/md-lint.{sh,ps1}` invoking a pinned markdownlint-cli2, delegated from a `Makefile` `md-lint` target and a `make.ps1` case (operator-facing files stay npx/npm-free per the tests/integration retired-wrapper guard); a repo-wide, SHA-pinned, blocking `md-lint` CI job in `ci.yml`; characterization guards for the config invariant and the repo-wide job; and a P-008 wording reconciliation. Empirical (1839 tracked files): 250 violations (MD001=1/MD025=229/MD041=20) with the default rule set → 21 with the `_title` config alone (zero edits) → 0 repo-wide after the ~21 remediations. The 20 SKILL.md files are generated from external autoharness templates not present in this repo, so the fix is applied to the generated files directly and the required repo-wide gate catches any future regeneration drift; an upstream template fix is a tracked follow-up. Planned in docs/exec-plans/2026-07-25-markdownlint-p008-provisioning-plan.md (deliberation 054-DL). Surfaced by PR #297 Copilot review of the 125-F docs-consistency plan.