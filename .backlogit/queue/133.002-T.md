---
artifact_type: task
created_at: 2026-08-01T04:56:33.6194786Z
id: 133.002-T
labels:
    - tests
    - ship
    - cascade
parent_id: 133-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-shipshipment-partial-feature-archive-cascade-plan.md
status: active
title: Update three existing ship tests to membership contract
updated_at: 2026-08-01T05:56:54.8488308Z
---

Domain: tests (plan Unit 1a). Execution posture: characterization-first (test-first) — land red before Unit 2. Update the three existing ShipShipment tests that currently encode the 114-S bug (children-only manifest that archives/closes the covering feature) to the explicit-membership contract. Sibling of Unit 1b; both must be landed (red) before Unit 2.

Tests to update:
1. TestShipShipment_QueuePathAbsentAfterShip (internal/core/025_archive_harness_test.go:93): real target is the archive-deletion bug, not feature archival. Change its manifest to feature-inclusive ([]string{feature.ID, task.ID}) so archive-deletion assertions hold with the feature legitimately a member (validateShipmentItemIDs accepts feature IDs; NormalizeShipmentItems is pass-through).
2. TestShipShipment_CleansReleasedFeatureScope (internal/core/shipment_test.go:149): partial scenario (feature + 2 tasks, ships only releasedTask, futureTask returned). Flip assertions to the correct contract — covering feature AND its linked deliberation remain in .backlogit/queue/ (not archived, not done); releasedTask archived; futureTask returned to queued with parent_id cleared. (futureTask staying non-terminal keeps ComputeParentStatus from rolling up, so this test does NOT exercise the third/rollup seam — that is Unit 1b's body-planned test.)
3. TestShipShipment_SkipsAlreadyArchivedLinkedDeliberation (internal/core/shipment_repersist_test.go:46): change its manifest to feature-inclusive so the linkedDeliberationIDs collection path is still exercised and the skip-already-archived assertion stays meaningful under membership gating.

## Acceptance Criteria
- All three tests updated to the explicit-membership contract; every existing ShipShipment test that asserted covering-feature archival on a children-only manifest is enumerated and updated; none left silently regressed.
- Assertions are filesystem/Markdown-based (queue dir + status via findArtifact), distinguishing "covering feature still in queue / not done" from "manifest items archived" (per the re-persist learning) to avoid a false-green.
- Tests compile under `go test ./internal/core/...` (they may fail red until Unit 2 lands the fix).