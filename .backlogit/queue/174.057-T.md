---
artifact_type: task
created_at: 2026-09-16T06:48:01.6735892Z
dependencies:
    - 174.055-T
    - 174.045-T
    - 174.051-T
id: 174.057-T
parent_id: 174-F
priority: high
status: queued
title: 'SCOPE-IMPL-1: flatten release-scope derivation to explicit membership'
updated_at: 2026-09-16T06:48:01.6735892Z
---

Flatten the lifecycle **release-scope derivation** to flat explicit membership (spec §0.5 / SBLK-R28; plan §0.4). Green-maker for `174.055-T` (`TestUReleaseScopeFlat_`).

Change the `releaseScope` derivation at `internal/core/shipment_lifecycle.go:547-550` so the release set **equals the flat explicit manifest** — `explicitScope = uniqueNonEmptyStrings(NormalizeShipmentItems(shipment))` — instead of `releaseScopeItemIDs(...)` expanding descendants via `descendantItems`. Consumers that receive `releaseScope` **as a parameter** then flatten transitively with no change to their own logic: the member-evidence set / `validateMemberGateEvidence` (via the `releaseScope` argument to `gateShipmentCompletion`), `completeReleaseScope`, and the snapshot/rollback-lock ID set (`rollbackIDs`/`snapshotShipArtifacts`).

**Consumers that re-expand independently are OUT OF SCOPE here and handled by `174.058-T`:** `returnUnreleasedFeatureItems` (`:735`) and the covering-feature descendant loop inside `collectArchiveCandidateIDs` (`:800`) call `descendantItems(featureID)` directly and are NOT flattened by changing line 550 — do not rely on this task to neutralize them.

Preserve parent-first ordering as manifest ORDERING (deterministic order from the manifest), never as expansion. Introduce **no public/exported API** — `NormalizeShipmentItems` unchanged; internal behavior change only (no new source-shape seam). Remove descendant expansion from the lifecycle release-scope path; retain `descendantItems` for the callers `174.058-T` handles. Do NOT restore any archived/superseded task.

AC: (1) `174.055-T` (`TestUReleaseScopeFlat_`) passes; (2) release scope == flat explicit manifest; (3) a listed feature does not expand to unlisted descendants in the derivation/evidence/completion/rollback set; (4) parent-first manifest order preserved as ordering; (5) no new exported symbol and existing `blocked`-lifecycle tests remain green. Domain: code. ≤5 functions.