---
artifact_type: task
created_at: 2026-09-06T04:19:41.8237507Z
dependencies:
    - 167.006-T
    - 167.011-T
id: 167.013-T
parent_id: 167-F
priority: high
status: queued
title: CI Windows test-matrix coverage for platform-split handle code
updated_at: 2026-09-06T04:19:41.8237507Z
---

CI Windows test-matrix coverage (config/ci domain) for the #423 platform-split handle code. The 167.006-T atomic writer and 167.011-T item-log lock introduce Windows-specific reparse-safe handle syscalls (_windows.go) that the current CI does NOT exercise: .github/workflows/ci.yml runs tests ONLY on ubuntu-latest (lines ~88-121), and release.yml merely cross-builds Windows binaries without running tests. Add a windows-latest job (or a windows entry in the test matrix) to .github/workflows/ci.yml that runs go test ./... (at minimum the internal/core reconcile handle/lock tests) on Windows, so the Windows-specific security-sensitive handle paths are actually executed, not build-only-verified. Keep the ubuntu job unchanged. Acceptance: ci.yml has a Windows test job; the reconcile writer/lock cross-platform tests run and pass on Windows in CI. Domain: ci/config (width-isolated from the Go implementation tasks). Verifies 167.006-T + 167.011-T. Deps 167.006 + 167.011 (the code the Windows job must exercise must exist first).