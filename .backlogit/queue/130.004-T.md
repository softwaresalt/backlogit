---
artifact_type: task
created_at: 2026-07-28T22:41:44.1804723Z
id: 130.004-T
labels:
    - durable-writes
    - core
    - filesystem
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: queued
title: U4 - re-fsync existing dir in core mkdirAllDurable durable retry
updated_at: 2026-07-28T22:41:44.1804723Z
---

internal/core/durable_fs.go mkdirAllDurable: when durable and the target dir already exists, UNCONDITIONALLY re-fsync its parent (idempotent) instead of the os.Stat early-return, so a previously failed dirent flush is re-attempted; wrap the re-fsync failure with percent-w around blerrors.ErrWriteNotApplied for parity with the events variant. Keep durable=false exactly os.MkdirAll. Fix the stale doc-comment cross-reference (currently says it mirrors the U5 events durable-mkdir; the counterpart is U3). Shared function consumed at runtime by U1 and U2, so add a cross-consumer happy-path regression (existing dir plus fsync ok yields nil, no false indeterminate). Seams: mkdirDirSyncEnabled and mkdirDirSyncFn (no t.Parallel). Test-first (durable_fs_test.go). Acceptance criteria: (a) mkdir ok, parent fsync fails -> ErrWriteNotApplied-wrapped error; retry re-attempts; (b) durable-off -> plain MkdirAll, seam not invoked; (c) existing durable dir plus fsync ok -> nil. Plan unit U4.