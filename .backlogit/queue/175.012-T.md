---
artifact_type: task
created_at: 2026-09-18T00:00:12.8030757Z
dependencies:
    - 175.002-T
    - 175.003-T
    - 175.004-T
    - 175.005-T
    - 175.006-T
    - 175.007-T
    - 175.008-T
    - 175.009-T
    - 175.010-T
    - 175.011-T
    - 175.013-T
    - 175.014-T
    - 175.015-T
    - 175.016-T
    - 175.017-T
    - 175.018-T
    - 175.019-T
    - 175.020-T
    - 175.021-T
    - 175.022-T
    - 175.023-T
    - 175.024-T
    - 175.025-T
    - 175.026-T
    - 175.027-T
    - 175.028-T
    - 175.029-T
    - 175.030-T
    - 175.031-T
    - 175.032-T
    - 175.033-T
    - 175.034-T
    - 175.035-T
    - 175.036-T
    - 175.037-T
    - 175.038-T
    - 175.039-T
id: 175.012-T
labels:
    - harness-exempt
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U12 - Repository convergence verification (terminal sink; unblocks 149-S)
updated_at: 2026-09-18T16:46:15.8385396Z
---

Verification-only P-002.1 harness-exempt unit -- the SOLE member of the plan's closed harness-exempt set. Terminal sink of the file-owned lint DAG.

DEPENDENCY CONTRACT (corrected): U12 DIRECTLY depends on exactly 37 immediate predecessors -- the 36 file-owned lint tasks PLUS U14 (175.014-T). It depends TRANSITIVELY (not as an immediate edge) on U1 (175.001-T) and U40 (175.040-T): every lint task depends on U1, and U14 depends on U40 which depends on U1. U12 therefore has NO direct edge to U1 or U40; the machine DAG records only the 37 immediate edges. U12 is the sole terminal sink (in-degree 0 as a dependency target). It records green evidence for behavior delivered by its prerequisites, commits ONLY its named evidence artifact, and adds NO new red assertion, so it scaffolds no red harness. It owns NO code guards; its sole deliverable is the named evidence artifact.

The exempt_verification_command is a single-quoted pwsh -Command payload (literal child payload -- no premature outer-shell expansion; AST-validated with zero single quotes inside). It FIRST fails closed when not running Windows-native (`if(-not $IsWindows){exit 19}`) BEFORE any gate, so golangci-lint run / go build / go vet / go test include the four windows-build-tagged files owned by U19-U22 that the Ubuntu lint CI would silently exclude; this preserves U14's dedicated Windows workflow semantics unchanged and broadens no implementation scope. It then EXECUTES the four mandatory gates directly (never trusts hand-authored claims): go test ./..., go vet ./..., golangci-lint run, and gofmt -l . -- FAILS on any nonzero exit (distinct exits 2-5) and SEPARATELY FAILS when gofmt -l . stdout is non-empty (exit 6). Only after all four gates pass does it validate the evidence artifact, including the repository docs lint on the artifact (exit 20 on any docline violation or nonzero/unparseable lint), the `VERIFIED-PARENT: <40-hex>` body line against `git rev-parse HEAD^` (exit 21), and the current commit's changed-path set being EXACTLY the evidence artifact via `git diff --name-only HEAD^ HEAD` (exit 22) -- all with fail-closed native-command handling (any nonzero git/lint exit fails). This binds the evidence to the exact verified content without impossible self-reference: the artifact records its PARENT (the implementation-content commit), never its own commit SHA.

Gate-marker validation (EXACT UNIQUE LINES, not substring Contains): each of the four gate markers (go test ./...: GREEN / go vet ./...: GREEN / golangci-lint run: GREEN / gofmt -l .: GREEN) MUST appear as an EXACT full trimmed line EXACTLY ONCE in the evidence artifact, compared with ORDINAL equality ([string]::Equals on the trimmed line with [System.StringComparison]::Ordinal -- NOT the culture-aware -ceq operator) so capitalization drift OR any culture-ignorable code point (e.g. trailing soft hyphen U+00AD) in any marker fails. A missing marker, a substring-only match, a CASE-DRIFTED marker, a culture-ignorable-codepoint-drifted marker (e.g. trailing soft hyphen U+00AD), or a DUPLICATE marker line all fail (exit 8). This closes the prior substring-Contains gap where a tampered or partial marker could pass.

//nolint inventory validation (machine-readable, FULL IDENTITY): the evidence artifact carries a SINGLE inventory block delimited by EXACTLY ONE `# BEGIN NOLINT-INVENTORY` and one `# END NOLINT-INVENTORY` line, each matched by EXACT FULL TRIMMED-LINE ORDINAL EQUALITY ([string]::Equals(trimmed line, literal delimiter, [System.StringComparison]::Ordinal) -- NOT the culture-aware -ceq operator and NOT a substring -match) so a near-miss like `## BEGIN ...` or a delimiter carrying trailing text is NOT counted; duplicate or missing delimiters, or a block whose END precedes its BEGIN, fail (exit 9). Only rows STRICTLY INSIDE that single block are parsed; rows outside the block are ignored, and there is no second block. Each row MUST have EXACTLY three columns path:line | linter-set | justification -- fewer or MORE than three columns fail (exit 10). An empty path fails (exit 11); an empty normalized linter set fails (exit 12); an empty justification fails (exit 13); a DUPLICATE path:line row fails (exit 14).
The command then enumerates every actual //nolint directive in the TRACKED source. It obtains the tracked Go path set by launching `git ls-files -z -- "*.go"` as a redirected child process and reading its standard-output BYTE STREAM continuously (ProcessStartInfo/RedirectStandardOutput, copying BaseStream into a MemoryStream), then splitting the raw bytes on the NUL (0x00) byte and STRICT throw-on-invalid UTF-8 decoding each segment (a fail-closed `System.Text.UTF8Encoding` constructed with throwOnInvalidBytes, so a malformed-UTF-8 path segment routes to exit 18 BEFORE any file read or inventory comparison instead of being silently mapped to the U+FFFD replacement character) -- so embedded-newline path bytes survive (the prior line-oriented native-text adapter that could drop newline-bearing Git path bytes before NUL splitting is removed) and a nonzero git exit fails (exit 18). It scans Go lexical states before accepting a token, so directive-shaped text inside interpreted strings, raw strings, rune literals, or block comments is excluded. A line-comment token is accepted ONLY when it starts with explicit directive syntax (//nolint optionally followed by :linters, then whitespace or end-of-line), so non-directive tokens such as //nolinting, //nolintfoo, or later text inside an ordinary comment are also excluded. The scanner first self-checks interpreted-string, raw-string, block-comment, and real-line-comment cases; a path-enumeration, read, scanner, or self-check failure exits 18. It parses EACH accepted directive's ACTUAL linter list (//nolint:a,b -> ORDINAL sorted-unique set {a,b} via a deterministic StringComparer.Ordinal normalizer, so case drift in a linter name is a real difference; bare //nolint -> ALL), and compares FULL IDENTITY path:line + normalized linter set (NOT path:line alone): a directive MISSING from the inventory fails (exit 15); a row whose linter set is WRONG/CHANGED/EXTRA/insufficient relative to the actual directive fails (exit 16); a STALE/EXTRA inventory row absent from the tree fails (exit 17). The linter-set comparison is order-insensitive via ORDINAL sorted-unique normalization on both sides, and all path-key maps and identity comparisons are ORDINAL (StringComparer.Ordinal dictionaries; [string]::Equals with [System.StringComparison]::Ordinal -- NOT the culture-aware -ceq/-cne operators, which treat culture-ignorable code points such as soft hyphen U+00AD as equal), so capitalization drift OR a culture-ignorable code point in a path key or linter set is a real, detectable difference. Distinct auditable exit codes make every negative state diagnosable; the positive marker EXEMPT_VERIFY_OK:175.012-T prints ONLY on exit 0 after every check passes.

Negative-state proof (validated at Stage against 21 synthetic gate/inventory scenarios plus 8 lexical-exclusion checks plus 3 ordinal-comparator self-checks -- linter-set case-fold rejection, ordinal-dict case-variant miss, and a soft-hyphen U+00AD culture-ignorable-codepoint rejection proving the ordinal comparator rejects what culture-aware -ceq would false-green, plus a strict-UTF8 path-decode self-check (a synthetic malformed-byte path segment REJECTED to exit 18 while a valid embedded-LF path segment decodes intact) -- and a 42-directive scanner-vs-git authoritative match before commit, plus 4 Windows/Docline/content-binding gate simulations for exits 19-22 asserted by positive and negative docs-lint and git rev-parse/diff runs, ALL PASS): gate failure -> exit 2-6; absent artifact -> exit 7; tampered/duplicate/CASE-DRIFTED gate marker -> exit 8; duplicate/missing/inverted/CASE-DRIFTED delimiters -> exit 9; wrong column count (2 or 4 columns) -> exit 10; empty path -> exit 11; empty linter set -> exit 12; empty justification -> exit 13; duplicate row -> exit 14; missing row or CASE-DRIFTED path key (case-variant path treated as distinct -> inventory miss) -> exit 15; wrong/extra/CASE-DRIFTED linter set -> exit 16; stale row -> exit 17; directive-shaped text inside interpreted strings, raw strings, and block comments excluded while a real line-comment directive is retained, byte-stream NUL enumeration preserves embedded-newline path bytes while strict throw-on-invalid UTF-8 decoding rejects a malformed-UTF-8 path segment to exit 18, a soft-hyphen U+00AD appended to a gate marker is REJECTED by the ordinal comparator (culture-aware -ceq would false-green it), and native git failure is caught -> scanner self-check passes (otherwise exit 18); not running Windows-native -> exit 19; invalid/absent Docline closure frontmatter or nonzero/unparseable docs lint -> exit 20; missing/duplicate/mismatched VERIFIED-PARENT vs git rev-parse HEAD^ -> exit 21; final-commit changed-path set not exactly the evidence artifact (git diff --name-only HEAD^ HEAD) -> exit 22; clean pass (including order-insensitive multi-linter) -> exit 0 with the success marker.

Deliverable (evidence artifact -- verification-only delta surface, under docs/closure/): docs/closure/175-baseline-convergence-convergence-evidence.md, GENERATED FROM OBSERVED gate outputs (never hand-authored). U12's FINAL task commit contains ONLY this evidence artifact (no other changed path), so the artifact's recorded parent SHA is exactly the implementation-content HEAD the four gates verify. It MUST carry VALID Docline closure frontmatter -- `title`, `source`, `doc_type: closure`, `chunk_strategy`, `schema_version` -- validated by the repository docs lint (`backlogit docs lint --path <artifact> --format json`; violation_count 0 and valid true), a single `VERIFIED-PARENT: <40-hex>` body line equal to the evidence commit's parent (`git rev-parse HEAD^`, i.e. the implementation-content commit), the four exact gate marker lines, and the single NOLINT-INVENTORY block:
- go test ./...: GREEN
- go vet ./...: GREEN
- golangci-lint run: GREEN
- gofmt -l .: GREEN
- a RAW marker-delimited block (NOT a Markdown code fence) delimited by the literal plain-text lines # BEGIN NOLINT-INVENTORY and # END NOLINT-INVENTORY: every nonempty trimmed line STRICTLY BETWEEN those two delimiter lines is parsed as one row path:line | linter-set | justification (exactly three |-separated columns), each linter set matching the actual directive and each justification NONEMPTY. No Markdown code-fence line (three backticks) may appear inside the BEGIN..END interval -- a fence line there parses as a malformed one-column row and fails (exit 10). If a Markdown fence is used purely for rendering, the # BEGIN / # END delimiters MUST sit INSIDE the fence and the fence lines OUTSIDE the parsed row interval, so a fence line is never part of the interval; the command and this deliverable wording therefore cannot conflict.

Must-fail-before-deliverable: on the pre-work tree the four gates are RED (the errcheck/staticcheck/gofmt debt is unremediated) so the command exits nonzero; even once green it still exits nonzero until the evidence artifact exists, records all four EXACT marker lines, AND its NOLINT-INVENTORY matches the tree by FULL IDENTITY. It also exits nonzero when not running Windows-native (19), when the artifact lacks valid Docline closure frontmatter or fails docs lint (20), when its VERIFIED-PARENT line does not equal git rev-parse HEAD^ (21), or when the final commit changes any path other than the evidence artifact (22). Only then does it exit 0 and print EXEMPT_VERIFY_OK:175.012-T.

Acceptance criteria:
- Scope (files): only the evidence artifact under docs/closure/ (verification-only delta surface -- no non-test *.go file and no repository-configuration file is changed). The final task commit contains ONLY this artifact, so git diff --name-only HEAD^ HEAD is exactly that one path.
- Runnable gate execution: the exempt_verification_command itself runs the four gates, fails on any nonzero exit, and fails on non-empty gofmt output -- never relying on a test-selector exit status or string-only evidence.
- Windows-native + Docline + content-binding: the command fails closed with exit 19 when not run Windows-native (so the four windows-build-tagged U19-U22 files are included by golangci-lint run / go vet / go test, which the Ubuntu lint CI would exclude; U14's dedicated Windows workflow semantics are preserved unchanged); the evidence artifact carries valid Docline closure frontmatter (title, source, doc_type: closure, chunk_strategy, schema_version) verified by the repository docs lint (exit 20 on any violation or nonzero/unparseable lint); a single VERIFIED-PARENT: <40-hex> line equals git rev-parse HEAD^ (exit 21); and git diff --name-only HEAD^ HEAD equals exactly the evidence artifact (exit 22) -- all with fail-closed native-command handling.
- Gate markers validated as EXACT UNIQUE LINES via ordinal [string]::Equals (not substring Contains, not culture-aware -ceq): missing, substring-only, duplicate, case-drifted, or culture-ignorable-codepoint-drifted (e.g. trailing soft hyphen U+00AD) marker fails.
- Inventory FULL IDENTITY: exactly three columns; nonempty path/linter-set/justification; single BEGIN/END block whose delimiters are matched by EXACT FULL TRIMMED-LINE ORDINAL EQUALITY via [string]::Equals with [System.StringComparison]::Ordinal (not the culture-aware -ceq operator, not substring); rows outside the block ignored; byte-stream NUL-split tracked-file enumeration (embedded-newline-safe, strict throw-on-invalid UTF-8 path decoding that fails closed to exit 18 on malformed bytes, fails on nonzero git exit) plus lexical-state-aware scanning accepts only line-comment tokens that begin with EXPLICIT //nolint directive syntax, excluding directive-shaped text inside interpreted strings, raw strings, rune literals, block comments, and ordinary comment text; every actual //nolint directive matched by path:line + ORDINAL normalized linter set; wrong/empty/changed/extra linter, duplicate, missing, stale, or scanner-failure states all rejected with distinct exits.
- Must-fail-before-deliverable: fails on the pre-work tree, on an absent or incomplete artifact, and on any inventory/marker mismatch; passes only when everything is green and exact.
- Dependencies: 37 IMMEDIATE edges (36 lint tasks + U14); U1 and U40 transitive only (no direct edge). Sole terminal sink; unblocks 149-S. Sole member of the closed harness-exempt set.
- Posture: runtime-verification, verification-only harness-exempt (owns no code guards; only the named evidence artifact).

<!-- BEGIN:harness-exemption-contract -->
```text
harness_exemption_class: verification-only
harness_exemption_reason: Executes and records green evidence for the four mandatory gates delivered by the 36 file-owned lint tasks plus U14 (transitively U40 and U1); commits only its named evidence artifact and adds no new red assertion.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command 'if(-not $IsWindows){exit 19}; & go test ./...; if($LASTEXITCODE -ne 0){exit 2}; & go vet ./...; if($LASTEXITCODE -ne 0){exit 3}; & golangci-lint run; if($LASTEXITCODE -ne 0){exit 4}; $gf = & gofmt -l .; if($LASTEXITCODE -ne 0){exit 5}; if($gf){exit 6}; $f = "docs/closure/175-baseline-convergence-convergence-evidence.md"; if(-not (Test-Path $f)){exit 7}; $lines = Get-Content $f; $dl = & backlogit docs lint --path $f --format json 2>$null; if($LASTEXITCODE -ne 0){exit 20}; $dj=$null; try{$dj=$dl | ConvertFrom-Json}catch{exit 20}; if($null -eq $dj -or $dj.violation_count -ne 0 -or $dj.valid -ne $true){exit 20}; $vp=@($lines | Where-Object { $_.Trim() -match "^VERIFIED-PARENT: [0-9a-f]{40}$" }); if($vp.Count -ne 1){exit 21}; $recParent=($vp[0].Trim() -split " ")[1]; $actualParent = & git rev-parse HEAD^; if($LASTEXITCODE -ne 0){exit 21}; if(-not [string]::Equals($actualParent.Trim(), $recParent, [System.StringComparison]::Ordinal)){exit 21}; $chg=@(& git diff --name-only HEAD^ HEAD); if($LASTEXITCODE -ne 0){exit 22}; if($chg.Count -ne 1){exit 22}; if(-not [string]::Equals(($chg[0].Trim() -replace "\\","/"), $f, [System.StringComparison]::Ordinal)){exit 22}; $markers = @("go test ./...: GREEN","go vet ./...: GREEN","golangci-lint run: GREEN","gofmt -l .: GREEN"); foreach($m in $markers){ if(@($lines | Where-Object { [string]::Equals($_.Trim(), $m, [System.StringComparison]::Ordinal) }).Count -ne 1){exit 8} }; $bi=@(); $ei=@(); for($i=0;$i -lt $lines.Count;$i++){ $tl=$lines[$i].Trim(); if([string]::Equals($tl, "# BEGIN NOLINT-INVENTORY", [System.StringComparison]::Ordinal)){$bi += $i}; if([string]::Equals($tl, "# END NOLINT-INVENTORY", [System.StringComparison]::Ordinal)){$ei += $i} }; if($bi.Count -ne 1 -or $ei.Count -ne 1){exit 9}; if($ei[0] -le $bi[0]){exit 9}; $rows=@(); for($i=$bi[0]+1;$i -lt $ei[0];$i++){ $t=$lines[$i].Trim(); if($t){$rows += $t} }; function Norm([string]$s){ $u=[System.Collections.Generic.SortedSet[string]]::new([System.StringComparer]::Ordinal); foreach($x in ($s -split ",")){ if($x){[void]$u.Add($x)} }; return ($u -join ",") }; function Get-ActualNolint([string]$gp,[string]$src){ $i=0; $ln=1; while($i -lt $src.Length){ $c=[int][char]$src[$i]; if($c -eq 10){$ln++;$i++;continue}; if($c -eq 34 -or $c -eq 39){$q=$c;$i++;$esc=$false;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};if($esc){$esc=$false;$i++;continue};if($d -eq 92){$esc=$true;$i++;continue};$i++;if($d -eq $q){break}};continue}; if($c -eq 96){$i++;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};$i++;if($d -eq 96){break}};continue}; if($c -eq 47 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 47){$sl=$ln;$j=$i;while($j -lt $src.Length -and [int][char]$src[$j] -ne 10){$j++};$cm=$src.Substring($i,$j-$i);$mt=[regex]::Match($cm,"^//nolint(?::([A-Za-z0-9_,-]+))?(?:\s|$)");if($mt.Success){if($mt.Groups[1].Success){$ss=Norm $mt.Groups[1].Value}else{$ss="ALL"};[pscustomobject]@{Key=$gp+":"+$sl;Set=$ss}};$i=$j;continue}; if($c -eq 47 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 42){$i+=2;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};if($d -eq 42 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 47){$i+=2;break};$i++};continue}; $i++} }; $nl=[char]10;$dq=[char]34;$bt=[char]96;$probe="package p"+$nl+"var a = "+$dq+"//nolint:gosec fixture"+$dq+$nl+"var b = "+$bt+"//nolint:errcheck raw"+$bt+$nl+"/* //nolint:unused */"+$nl+"//nolint:staticcheck reason"+$nl+"//nolint:GoSec,errcheck mixed"+$nl;$pr=@(Get-ActualNolint "probe.go" $probe);if($pr.Count -ne 2){exit 18};if(-not [string]::Equals($pr[0].Key, "probe.go:5", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[0].Set, "staticcheck", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[1].Key, "probe.go:6", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[1].Set, "GoSec,errcheck", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals((Norm "gosec,GOSEC,gosec"), "GOSEC,gosec", [System.StringComparison]::Ordinal)){exit 18};$cs=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal);$cs["a.go:1"]="x";if($cs.ContainsKey("A.go:1")){exit 18};if(-not [string]::Equals("Alpha", "Alpha", [System.StringComparison]::Ordinal)){exit 18};if([string]::Equals("Alpha", "alpha", [System.StringComparison]::Ordinal)){exit 18};$sh=[char]0x00AD;if([string]::Equals(("go test ./...: GREEN"+$sh), "go test ./...: GREEN", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals(("go test ./...: GREEN"+$sh), ("go test ./...: GREEN"+$sh), [System.StringComparison]::Ordinal)){exit 18};$u8=[System.Text.UTF8Encoding]::new($false,$true);$badp=[byte[]](0x61,0xff,0x2e,0x67,0x6f);$rej=$false;try{[void]$u8.GetString($badp)}catch{$rej=$true};if(-not $rej){exit 18};$okp=$u8.GetString([byte[]](0x61,0x0a,0x62,0x2e,0x67,0x6f));if(-not [string]::Equals($okp, ("a"+$nl+"b.go"), [System.StringComparison]::Ordinal)){exit 18};$psi=[System.Diagnostics.ProcessStartInfo]::new();$psi.FileName="git";$psi.ArgumentList.Add("ls-files");$psi.ArgumentList.Add("-z");$psi.ArgumentList.Add("--");$psi.ArgumentList.Add("*.go");$psi.RedirectStandardOutput=$true;$psi.UseShellExecute=$false;$proc=[System.Diagnostics.Process]::Start($psi);$mem=[System.IO.MemoryStream]::new();$proc.StandardOutput.BaseStream.CopyTo($mem);$proc.WaitForExit();if($proc.ExitCode -ne 0){exit 18};$bytes=$mem.ToArray();$act=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal);$st=0;for($b=0;$b -le $bytes.Length;$b++){ if($b -eq $bytes.Length -or $bytes[$b] -eq 0){ if($b -gt $st){ try{$gp=$u8.GetString($bytes,$st,$b-$st)}catch{exit 18}; try{$src=[System.IO.File]::ReadAllText($gp)}catch{exit 18}; foreach($a in @(Get-ActualNolint $gp $src)){ if($act.ContainsKey($a.Key)){exit 18}; $act[$a.Key]=$a.Set } }; $st=$b+1 } }; $inv=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal); foreach($row in $rows){ $c = $row -split "\|"; if($c.Count -ne 3){exit 10}; $k=$c[0].Trim(); $l=Norm $c[1].Trim(); $j=$c[2].Trim(); if(-not $k){exit 11}; if(-not $l){exit 12}; if(-not $j){exit 13}; if($inv.ContainsKey($k)){exit 14}; $inv[$k]=$l }; foreach($k in $act.Keys){ if(-not $inv.ContainsKey($k)){exit 15}; if(-not [string]::Equals($inv[$k], $act[$k], [System.StringComparison]::Ordinal)){exit 16} }; foreach($k in $inv.Keys){ if(-not $act.ContainsKey($k)){exit 17} }; Write-Output "EXEMPT_VERIFY_OK:175.012-T"; exit 0'
exempt_precondition: must-fail-before-deliverable
```
<!-- END:harness-exemption-contract -->