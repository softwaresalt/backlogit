---
artifact_type: task
created_at: 2026-09-18T00:00:12.8030757Z
dependencies:
    - 175.002-T
    - 175.003-T
    - 175.004-T
    - 175.005-T
    - 175.006-T
    - 175.007-T
    - 175.008-T
    - 175.009-T
    - 175.010-T
    - 175.011-T
    - 175.013-T
    - 175.014-T
id: 175.012-T
labels:
    - harness-exempt
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U12 - Repository convergence verification (unblocks 149-S)
updated_at: 2026-09-18T00:00:12.8030757Z
---

Verification only — P-002.1 `verification-only` harness-exempt unit. This task
records green evidence for behavior already delivered by its prerequisites (the
four mandatory quality gates go green as U1–U11, U13, and U14 land); it commits
ONLY green guards and its evidence artifact and adds NO new red assertion, so it
scaffolds no red harness. It is a member of the plan's closed harness-exempt set
(see the "Harness-Exempt Set (closed)" section of
`docs/exec-plans/2026-09-17-baseline-convergence-plan.md`); U12 is the SOLE member.

Confirm and capture evidence that `go test ./...`, `go vet ./...`,
`golangci-lint run`, and `gofmt -l .` are ALL green, and record the full
enumerated list of any `//nolint` suppressions and their justifications.

Deliverable (evidence artifact — verification-only class delta surface, under
`docs/closure/`): `docs/closure/175-baseline-convergence-convergence-evidence.md`.
The artifact MUST record each of the four gates as green using these EXACT marker
strings (one per gate) plus the nolint enumeration section header, which are the
strings the `exempt_verification_command` asserts:

- `go test ./...: GREEN`
- `go vet ./...: GREEN`
- `golangci-lint run: GREEN`
- `gofmt -l .: GREEN`
- `//nolint enumeration:` (section header, followed by the full enumerated list)

Must-fail-before-deliverable: before the evidence artifact exists (or before it
records all five required strings) the `exempt_verification_command` exits non-zero
and is the required pre-work failure; after the deliverable lands with all required
strings it exits 0 and prints its unique `EXEMPT_VERIFY_OK:175.012-T` marker. The
command asserts the declared evidence-manifest rows/scalars and does NOT rely on a
test selector's exit status.

Acceptance criteria:
- Scope (files): only the evidence artifact under `docs/closure/` (verification-only
  class delta surface — no non-test `*.go` file and no repository-configuration file
  is changed).
- Evidence: `docs/closure/175-baseline-convergence-convergence-evidence.md` exists
  and records all four mandatory gates green with the exact marker strings above and
  the full enumerated `//nolint` suppression list with justifications.
- Exempt verification: the `exempt_verification_command` fails on the pre-work tree
  (artifact absent) and passes (exit 0 + `EXEMPT_VERIFY_OK:175.012-T` marker) only
  after the deliverable lands.
- Posture: runtime-verification. Terminal sink node (unblocks 149-S).

<!-- BEGIN:harness-exemption-contract -->
```text
harness_exemption_class: verification-only
harness_exemption_reason: Records green evidence for the four mandatory gates delivered by U1-U11/U13/U14; commits only green guards and the evidence artifact and adds no new red assertion.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command "$f='docs/closure/175-baseline-convergence-convergence-evidence.md'; if(-not (Test-Path $f)){ exit 1 }; $c=Get-Content $f -Raw; foreach($m in @('go test ./...: GREEN','go vet ./...: GREEN','golangci-lint run: GREEN','gofmt -l .: GREEN','//nolint enumeration:')){ if(-not $c.Contains($m)){ exit 1 } }; Write-Output 'EXEMPT_VERIFY_OK:175.012-T'; exit 0"
exempt_precondition: must-fail-before-deliverable
```
<!-- END:harness-exemption-contract -->