---
artifact_type: task
created_at: 2026-09-18T00:00:12.8030757Z
dependencies:
    - 175.003-T
    - 175.004-T
    - 175.005-T
    - 175.006-T
    - 175.007-T
    - 175.008-T
    - 175.009-T
    - 175.010-T
    - 175.013-T
    - 175.014-T
    - 175.015-T
    - 175.016-T
    - 175.017-T
    - 175.018-T
    - 175.019-T
    - 175.020-T
    - 175.021-T
    - 175.023-T
    - 175.024-T
    - 175.025-T
    - 175.026-T
    - 175.027-T
    - 175.028-T
    - 175.029-T
    - 175.030-T
    - 175.032-T
    - 175.033-T
    - 175.034-T
    - 175.035-T
    - 175.036-T
    - 175.037-T
    - 175.038-T
    - 175.039-T
    - 175.042-T
    - 175.045-T
    - 175.046-T
    - 175.047-T
    - 175.048-T
    - 175.049-T
    - 175.050-T
    - 175.051-T
    - 175.052-T
    - 175.053-T
    - 175.054-T
    - 175.055-T
    - 175.056-T
    - 175.057-T
    - 175.058-T
    - 175.059-T
    - 175.060-T
    - 175.061-T
    - 175.062-T
    - 175.063-T
    - 175.064-T
    - 175.065-T
    - 175.066-T
    - 175.067-T
    - 175.069-T
    - 175.070-T
    - 175.071-T
    - 175.072-T
    - 175.073-T
    - 175.074-T
    - 175.075-T
    - 175.076-T
    - 175.078-T
    - 175.079-T
    - 175.080-T
    - 175.081-T
    - 175.082-T
    - 175.083-T
    - 175.084-T
    - 175.085-T
    - 175.086-T
    - 175.087-T
    - 175.088-T
    - 175.089-T
    - 175.090-T
    - 175.091-T
    - 175.092-T
    - 175.093-T
    - 175.095-T
    - 175.096-T
    - 175.097-T
    - 175.098-T
id: 175.012-T
labels:
    - harness-exempt
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U12 - Repository convergence verification (terminal sink; unblocks 149-S)
updated_at: 2026-09-19T07:15:52.7315252Z
---

Verification-only P-002.1 harness-exempt unit -- the SOLE member of the plan's closed harness-exempt set and the terminal-convergence sink of the 497-identity supported-platform baseline-convergence DAG (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface).

DEPENDENCY CONTRACT: U12 DIRECTLY depends on exactly 85 immediate predecessors -- the 84 finding-remediation frontier tasks PLUS U14 (175.014-T). It depends TRANSITIVELY (not as an immediate edge) on U1 (175.001-T) and U40 (175.040-T): every finding-remediation task depends on U1, and U14 depends on U40 which depends on U1. U12 therefore has NO direct edge to U1 or U40; the machine DAG records only the 85 immediate edges. U12 is the sole terminal sink (no member depends on it having any successor). It records green evidence for behavior delivered by its prerequisites, commits ONLY its named evidence artifact, and adds NO new red assertion, so it scaffolds no red harness. It owns NO code guards; its sole deliverable is the named evidence artifact.

CANONICAL RUNTIME ADOPTION: the task-scoped lint surface is validated by the canonical harness runner (`scripts/verify-task-lint.ps1 -TaskId 175.012-T -FeatureId 175-F`) under the `no-go-lint-surface` scope kind. The runner owns the read-only schema screen (`-Phase Schema`) and the evidence+governed-claim CONTENT-BINDING claim-union: `-Phase PreCommit` asserts the uncommitted working set (unstaged + staged + untracked) set-equals `owned_paths` (the evidence artifact) plus `governed_claim_paths` (`.backlogit/queue/175.012-T.md`, `.backlogit/hooks_queue.jsonl`); `-Phase PostCommit` asserts the HEAD commit changed exactly `owned_paths` while the working set still holds exactly `governed_claim_paths`. The runner emits `TASK-LINT-OK:175.012-T:terminal-convergence` on success. The evidence+governed-claim content-binding lives entirely in the canonical runner's `-Phase` claim-union; U12 declares no separate evidence-to-commit self-binding of its own and does not duplicate the runner's content-binding logic.

The `exempt_verification_command` (harness-exemption-contract) is the P-002.3 must-fail-before-deliverable DELIVERABLE-EVIDENCE probe, distinct from the lint claim-union. It is a single-quoted pwsh -Command payload (literal child payload -- no premature outer-shell expansion; AST-validated with zero single quotes inside). It FIRST fails closed when not running Windows-native (`if(-not $IsWindows){exit 19}`) BEFORE any gate, so `golangci-lint run` / `go build` / `go vet` / `go test` include the four windows-build-tagged files owned by U19-U22 that an Ubuntu-only lint CI would silently exclude; this preserves U14's dedicated Windows workflow semantics unchanged and broadens no implementation scope. It then EXECUTES the four mandatory gates directly (never trusts hand-authored claims): `go test ./...`, `go vet ./...`, `golangci-lint run`, and `gofmt -l .` -- FAILS on any nonzero exit (distinct exits 2-5) and SEPARATELY FAILS when `gofmt -l .` stdout is non-empty (exit 6). Only after all four gates pass does it validate the evidence artifact's existence (exit 7) and the repository docs lint on the artifact (exit 20 on any Docline violation or nonzero/unparseable lint), then the gate-marker and NOLINT-inventory content checks below. The evidence+governed-claim commit sequencing is NOT re-derived here; it is enforced by the canonical runner's `-Phase PreCommit`/`-Phase PostCommit` claim-union at claim time and Ship's post-commit rerun.

Gate-marker validation (EXACT UNIQUE LINES, not substring Contains): each of the four gate markers (`go test ./...: GREEN` / `go vet ./...: GREEN` / `golangci-lint run: GREEN` / `gofmt -l .: GREEN`) MUST appear as an EXACT full trimmed line EXACTLY ONCE in the evidence artifact, compared with ORDINAL equality (`[string]::Equals` on the trimmed line with `[System.StringComparison]::Ordinal` -- NOT the culture-aware -ceq operator) so capitalization drift OR any culture-ignorable code point (e.g. a trailing soft hyphen U+00AD) in any marker fails. A missing marker, a substring-only match, a CASE-DRIFTED marker, a culture-ignorable-codepoint-drifted marker, or a DUPLICATE marker line all fail (exit 8).

//nolint inventory validation (machine-readable, FULL IDENTITY): the evidence artifact carries a SINGLE inventory block delimited by EXACTLY ONE `# BEGIN NOLINT-INVENTORY` and one `# END NOLINT-INVENTORY` line, each matched by EXACT FULL TRIMMED-LINE ORDINAL EQUALITY (`[string]::Equals(trimmed line, literal delimiter, [System.StringComparison]::Ordinal)` -- NOT the culture-aware -ceq operator and NOT a substring -match) so a near-miss like `## BEGIN ...` or a delimiter carrying trailing text is NOT counted; duplicate or missing delimiters, or a block whose END precedes its BEGIN, fail (exit 9). Only rows STRICTLY INSIDE that single block are parsed; each row MUST have EXACTLY three columns `path:line | linter-set | justification` -- fewer or MORE than three columns fail (exit 10). An empty path fails (exit 11); an empty normalized linter set fails (exit 12); an empty justification fails (exit 13); a DUPLICATE `path:line` row fails (exit 14).
The command then enumerates every actual //nolint directive in the TRACKED source. It obtains the tracked Go path set by launching `git ls-files -z -- "*.go"` as a redirected child process and reading its standard-output BYTE STREAM continuously (ProcessStartInfo/RedirectStandardOutput copying BaseStream into a MemoryStream), splitting the raw bytes on the NUL (0x00) byte and STRICT throw-on-invalid UTF-8 decoding each segment (a fail-closed `System.Text.UTF8Encoding` constructed with throwOnInvalidBytes, routing a malformed-UTF-8 path segment to exit 18 BEFORE any file read) -- so embedded-newline path bytes survive and a nonzero git exit fails (exit 18). It scans Go lexical states before accepting a token, so directive-shaped text inside interpreted strings, raw strings, rune literals, or block comments is excluded; a line-comment token is accepted ONLY when it starts with explicit `//nolint` directive syntax. It parses EACH accepted directive's ACTUAL linter list (`//nolint:a,b` -> ORDINAL sorted-unique set {a,b}; bare `//nolint` -> ALL), and compares FULL IDENTITY `path:line` + normalized linter set: a directive MISSING from the inventory fails (exit 15); a row whose linter set is WRONG/CHANGED/EXTRA/insufficient fails (exit 16); a STALE/EXTRA inventory row absent from the tree fails (exit 17). All path-key maps and identity comparisons are ORDINAL, so capitalization drift OR a culture-ignorable code point in a path key or linter set is a real, detectable difference. Distinct auditable exit codes make every negative state diagnosable; the positive marker `EXEMPT_VERIFY_OK:175.012-T` prints ONLY on exit 0 after every check passes.

Negative-state proof (validated at Stage against the synthetic gate/inventory scenarios plus lexical-exclusion checks plus ordinal-comparator self-checks -- linter-set case-fold rejection, ordinal-dict case-variant miss, and a soft-hyphen U+00AD culture-ignorable-codepoint rejection proving the ordinal comparator rejects what culture-aware -ceq would false-green -- plus a strict-UTF8 path-decode self-check and a scanner-vs-git authoritative match before commit): gate failure -> exit 2-6; absent artifact -> exit 7; tampered/duplicate/CASE-DRIFTED gate marker -> exit 8; duplicate/missing/inverted delimiters -> exit 9; wrong column count -> exit 10; empty path -> exit 11; empty linter set -> exit 12; empty justification -> exit 13; duplicate row -> exit 14; missing row -> exit 15; wrong/extra/CASE-DRIFTED linter set -> exit 16; stale row -> exit 17; scanner / strict-UTF8 / native-git failure -> exit 18; not running Windows-native -> exit 19; invalid/absent Docline closure frontmatter or nonzero/unparseable docs lint -> exit 20; clean pass -> exit 0 with the success marker. The evidence+governed-claim commit sequencing (pre-commit and post-commit) is proven separately by the canonical runner's `-Phase` claim-union, not by this probe.

Deliverable (evidence artifact -- verification-only delta surface, under docs/closure/): `docs/closure/175-baseline-convergence-convergence-evidence.md`, GENERATED FROM OBSERVED gate outputs (never hand-authored). U12's FINAL task commit contains ONLY this evidence artifact (no other changed path); the canonical `-Phase PostCommit` claim-union proves that committed set equals the evidence artifact while the governed-claim paths remain the only dirty working-tree state. The artifact MUST carry VALID Docline closure frontmatter -- `title`, `source`, `doc_type: closure`, `chunk_strategy`, `schema_version` -- validated by the repository docs lint (`backlogit docs lint --path <artifact> --format json`; violation_count 0 and valid true), the four exact gate marker lines, and the single NOLINT-INVENTORY block:
- go test ./...: GREEN
- go vet ./...: GREEN
- golangci-lint run: GREEN
- gofmt -l .: GREEN
- a RAW marker-delimited block (NOT a Markdown code fence) delimited by the literal plain-text lines `# BEGIN NOLINT-INVENTORY` and `# END NOLINT-INVENTORY`: every nonempty trimmed line STRICTLY BETWEEN those two delimiter lines is parsed as one row `path:line | linter-set | justification` (exactly three |-separated columns), each linter set matching the actual directive and each justification NONEMPTY. No Markdown code-fence line may appear inside the BEGIN..END interval.

Must-fail-before-deliverable: on the pre-work tree the four gates are RED (the errcheck/staticcheck/gofmt debt is unremediated) so the command exits nonzero; even once green it still exits nonzero until the evidence artifact exists, records all four EXACT marker lines, AND its NOLINT-INVENTORY matches the tree by FULL IDENTITY. It also exits nonzero when not running Windows-native (19) or when the artifact lacks valid Docline closure frontmatter or fails docs lint (20). Only then does it exit 0 and print `EXEMPT_VERIFY_OK:175.012-T`.

Acceptance criteria:
- Scope (files): only the evidence artifact under docs/closure/ (verification-only delta surface -- no non-test *.go file and no repository-configuration file is changed). The canonical `-Phase PostCommit` claim-union proves the final task commit changes exactly that one path.
- Canonical task-lint contract: the `no-go-lint-surface` `task-lint-contract` names the evidence artifact as `owned_paths` and the two governed claim paths as `governed_claim_paths`; the runner's `-Phase Schema/PreCommit/PostCommit` phases own the schema screen and the evidence+governed-claim content-binding; success marker `TASK-LINT-OK:175.012-T:terminal-convergence`.
- Runnable gate execution: the `exempt_verification_command` runs the four gates, fails on any nonzero exit, and fails on non-empty gofmt output -- never relying on a test-selector exit status or string-only evidence.
- Windows-native + Docline: the command fails closed with exit 19 when not run Windows-native (so the four windows-build-tagged U19-U22 files are included by `golangci-lint run` / `go vet` / `go test`); the evidence artifact carries valid Docline closure frontmatter verified by the repository docs lint (exit 20 on any violation or nonzero/unparseable lint).
- Gate markers validated as EXACT UNIQUE LINES via ordinal `[string]::Equals` (not substring Contains, not culture-aware -ceq): missing, substring-only, duplicate, case-drifted, or culture-ignorable-codepoint-drifted marker fails (exit 8).
- Inventory FULL IDENTITY: exactly three columns; nonempty path/linter-set/justification; single BEGIN/END block matched by exact ordinal equality; byte-stream NUL-split tracked-file enumeration (embedded-newline-safe, strict throw-on-invalid UTF-8 decoding to exit 18) plus lexical-state-aware scanning; every actual //nolint directive matched by `path:line` + ORDINAL normalized linter set; wrong/empty/changed/extra linter, duplicate, missing, stale, or scanner-failure states all rejected with distinct exits.
- Must-fail-before-deliverable: fails on the pre-work tree, on an absent or incomplete artifact, and on any inventory/marker mismatch; passes only when everything is green and exact.
- Dependencies: 85 IMMEDIATE edges (84 remediation frontier + U14); U1 and U40 transitive only (no direct edge). Sole terminal sink; unblocks 149-S. Sole member of the closed harness-exempt set.
- Posture: runtime-verification, verification-only harness-exempt (owns no code guards; only the named evidence artifact).

<!-- BEGIN:harness-exemption-contract -->
```text
harness_exemption_class: verification-only
harness_exemption_reason: Executes and records green evidence for the four mandatory gates delivered by the finding-remediation tasks plus U14 (transitively U40 and U1); commits only its named evidence artifact and adds no new red assertion.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command 'if(-not $IsWindows){exit 19}; & go test ./...; if($LASTEXITCODE -ne 0){exit 2}; & go vet ./...; if($LASTEXITCODE -ne 0){exit 3}; & golangci-lint run; if($LASTEXITCODE -ne 0){exit 4}; $gf = & gofmt -l .; if($LASTEXITCODE -ne 0){exit 5}; if($gf){exit 6}; $f = "docs/closure/175-baseline-convergence-convergence-evidence.md"; if(-not (Test-Path $f)){exit 7}; $lines = Get-Content $f; $dl = & backlogit docs lint --path $f --format json 2>$null; if($LASTEXITCODE -ne 0){exit 20}; $dj=$null; try{$dj=$dl | ConvertFrom-Json}catch{exit 20}; if($null -eq $dj -or $dj.violation_count -ne 0 -or $dj.valid -ne $true){exit 20}; $markers = @("go test ./...: GREEN","go vet ./...: GREEN","golangci-lint run: GREEN","gofmt -l .: GREEN"); foreach($m in $markers){ if(@($lines | Where-Object { [string]::Equals($_.Trim(), $m, [System.StringComparison]::Ordinal) }).Count -ne 1){exit 8} }; $bi=@(); $ei=@(); for($i=0;$i -lt $lines.Count;$i++){ $tl=$lines[$i].Trim(); if([string]::Equals($tl, "# BEGIN NOLINT-INVENTORY", [System.StringComparison]::Ordinal)){$bi += $i}; if([string]::Equals($tl, "# END NOLINT-INVENTORY", [System.StringComparison]::Ordinal)){$ei += $i} }; if($bi.Count -ne 1 -or $ei.Count -ne 1){exit 9}; if($ei[0] -le $bi[0]){exit 9}; $rows=@(); for($i=$bi[0]+1;$i -lt $ei[0];$i++){ $t=$lines[$i].Trim(); if($t){$rows += $t} }; function Norm([string]$s){ $u=[System.Collections.Generic.SortedSet[string]]::new([System.StringComparer]::Ordinal); foreach($x in ($s -split ",")){ if($x){[void]$u.Add($x)} }; return ($u -join ",") }; function Get-ActualNolint([string]$gp,[string]$src){ $i=0; $ln=1; while($i -lt $src.Length){ $c=[int][char]$src[$i]; if($c -eq 10){$ln++;$i++;continue}; if($c -eq 34 -or $c -eq 39){$q=$c;$i++;$esc=$false;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};if($esc){$esc=$false;$i++;continue};if($d -eq 92){$esc=$true;$i++;continue};$i++;if($d -eq $q){break}};continue}; if($c -eq 96){$i++;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};$i++;if($d -eq 96){break}};continue}; if($c -eq 47 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 47){$sl=$ln;$j=$i;while($j -lt $src.Length -and [int][char]$src[$j] -ne 10){$j++};$cm=$src.Substring($i,$j-$i);$mt=[regex]::Match($cm,"^//nolint(?::([A-Za-z0-9_,-]+))?(?:\s|$)");if($mt.Success){if($mt.Groups[1].Success){$ss=Norm $mt.Groups[1].Value}else{$ss="ALL"};[pscustomobject]@{Key=$gp+":"+$sl;Set=$ss}};$i=$j;continue}; if($c -eq 47 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 42){$i+=2;while($i -lt $src.Length){$d=[int][char]$src[$i];if($d -eq 10){$ln++};if($d -eq 42 -and $i+1 -lt $src.Length -and [int][char]$src[$i+1] -eq 47){$i+=2;break};$i++};continue}; $i++} }; $nl=[char]10;$dq=[char]34;$bt=[char]96;$probe="package p"+$nl+"var a = "+$dq+"//nolint:gosec fixture"+$dq+$nl+"var b = "+$bt+"//nolint:errcheck raw"+$bt+$nl+"/* //nolint:unused */"+$nl+"//nolint:staticcheck reason"+$nl+"//nolint:GoSec,errcheck mixed"+$nl;$pr=@(Get-ActualNolint "probe.go" $probe);if($pr.Count -ne 2){exit 18};if(-not [string]::Equals($pr[0].Key, "probe.go:5", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[0].Set, "staticcheck", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[1].Key, "probe.go:6", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals($pr[1].Set, "GoSec,errcheck", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals((Norm "gosec,GOSEC,gosec"), "GOSEC,gosec", [System.StringComparison]::Ordinal)){exit 18};$cs=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal);$cs["a.go:1"]="x";if($cs.ContainsKey("A.go:1")){exit 18};if(-not [string]::Equals("Alpha", "Alpha", [System.StringComparison]::Ordinal)){exit 18};if([string]::Equals("Alpha", "alpha", [System.StringComparison]::Ordinal)){exit 18};$sh=[char]0x00AD;if([string]::Equals(("go test ./...: GREEN"+$sh), "go test ./...: GREEN", [System.StringComparison]::Ordinal)){exit 18};if(-not [string]::Equals(("go test ./...: GREEN"+$sh), ("go test ./...: GREEN"+$sh), [System.StringComparison]::Ordinal)){exit 18};$u8=[System.Text.UTF8Encoding]::new($false,$true);$badp=[byte[]](0x61,0xff,0x2e,0x67,0x6f);$rej=$false;try{[void]$u8.GetString($badp)}catch{$rej=$true};if(-not $rej){exit 18};$okp=$u8.GetString([byte[]](0x61,0x0a,0x62,0x2e,0x67,0x6f));if(-not [string]::Equals($okp, ("a"+$nl+"b.go"), [System.StringComparison]::Ordinal)){exit 18};$psi=[System.Diagnostics.ProcessStartInfo]::new();$psi.FileName="git";$psi.ArgumentList.Add("ls-files");$psi.ArgumentList.Add("-z");$psi.ArgumentList.Add("--");$psi.ArgumentList.Add("*.go");$psi.RedirectStandardOutput=$true;$psi.UseShellExecute=$false;$proc=[System.Diagnostics.Process]::Start($psi);$mem=[System.IO.MemoryStream]::new();$proc.StandardOutput.BaseStream.CopyTo($mem);$proc.WaitForExit();if($proc.ExitCode -ne 0){exit 18};$bytes=$mem.ToArray();$act=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal);$st=0;for($b=0;$b -le $bytes.Length;$b++){ if($b -eq $bytes.Length -or $bytes[$b] -eq 0){ if($b -gt $st){ try{$gp=$u8.GetString($bytes,$st,$b-$st)}catch{exit 18}; try{$src=[System.IO.File]::ReadAllText($gp)}catch{exit 18}; foreach($a in @(Get-ActualNolint $gp $src)){ if($act.ContainsKey($a.Key)){exit 18}; $act[$a.Key]=$a.Set } }; $st=$b+1 } }; $inv=[System.Collections.Generic.Dictionary[string,string]]::new([System.StringComparer]::Ordinal); foreach($row in $rows){ $c = $row -split "\|"; if($c.Count -ne 3){exit 10}; $k=$c[0].Trim(); $l=Norm $c[1].Trim(); $j=$c[2].Trim(); if(-not $k){exit 11}; if(-not $l){exit 12}; if(-not $j){exit 13}; if($inv.ContainsKey($k)){exit 14}; $inv[$k]=$l }; foreach($k in $act.Keys){ if(-not $inv.ContainsKey($k)){exit 15}; if(-not [string]::Equals($inv[$k], $act[$k], [System.StringComparison]::Ordinal)){exit 16} }; foreach($k in $inv.Keys){ if(-not $act.ContainsKey($k)){exit 17} }; Write-Output "EXEMPT_VERIFY_OK:175.012-T"; exit 0'
exempt_precondition: must-fail-before-deliverable
```
<!-- END:harness-exemption-contract -->

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.012-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "no-go-lint-surface",
    "owned_paths": [
      "docs/closure/175-baseline-convergence-convergence-evidence.md"
    ],
    "governed_claim_paths": [
      ".backlogit/queue/175.012-T.md",
      ".backlogit/hooks_queue.jsonl"
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.012-T:terminal-convergence"
  }
}
```

<!-- END:task-lint-contract -->
