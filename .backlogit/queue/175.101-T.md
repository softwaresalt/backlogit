---
artifact_type: task
created_at: 2026-09-20T05:05:00.0000000Z
dependencies:
    - 175.100-T
id: 175.101-T
parent_id: 175-F
priority: high
references:
    - docs/decisions/2026-09-19-baseline-convergence-decomposition.md
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'RS-W(-1) prerequisite: bootstrap shared terminal-lint runner (runner-bootstrap sink; U1 depends on this)'
updated_at: 2026-09-20T12:10:00.0000000Z
---

Runner-bootstrap PREREQUISITE member of the baseline-convergence feature `175-F` and the TERMINAL SINK of the runner-bootstrap chain, dependency-ordered after the baseline-lint runner-bootstrap task `175.100-T` (`175.101-T depends_on 175.100-T`). It OWNS creation of exactly one shared runner, the terminal zero-warning global runner `scripts/verify-terminal-lint.ps1`. U1 (`175.001-T`) depends on this member (`175.001-T depends_on 175.101-T`), so this task is the last of the three prerequisite bootstrap tasks to ship. Single-domain (verification tooling: one PowerShell runner plus one Go harness).

## Why this task exists (bootstrap split, PR #449 cycle 9)

The prior aggregate bootstrap task owned three runners plus one harness (four files) with a seven-plus-scenario matrix, exceeding the repository task-granularity contract. It is split into three dependency-ordered runner-specific tasks; this member owns the terminal-lint runner only and closes the bootstrap chain.

## Owned deliverable surface (created by Ship during execution; NOT implemented in this planning PR)

- `scripts/verify-terminal-lint.ps1` — the terminal zero-warning global runner (`-FeatureId 175-F`), screening BOTH supported GOOS surfaces (`windows`, `linux`) via an in-process `GOOS` override on a single host and emitting the deterministic success marker `TERMINAL-LINT-OK:175-F` only after both surfaces prove zero residual warnings.
- `tests/runner_bootstrap_terminal_lint_175_101_test.go` — the EXCLUSIVELY OWNED Go harness (`TestU175_101_TerminalLintRunnerBootstrap`, deterministic `TestU175_101_` token; no other member uses this path or prefix).

These are the ONLY two files this task may create (one implementation runner plus one owned harness). It creates NO Go source, NO workflow, and modifies NO existing script.

## Bootstrap harness (self-contained, non-vacuous; RED before / GREEN after)

BOOTSTRAP INVARIANT: the task-lint GATE for this member is the owned Go harness invoked as `go test ./tests -run ^TestU175_101_`, NEVER a direct invocation of the runner this task creates. The gate MUST COMPILE and its targeted test MUST EXECUTE under existing main-branch tooling BEFORE `scripts/verify-terminal-lint.ps1` exists: the harness COMPILES cleanly but its assertions FAIL when run (RED, nonzero), and passes GREEN (exit 0) only after the runner is implemented correctly. Compile success never implies test success.

The harness proves the following using ONLY existing main-branch tooling (`go test` + the built-in PowerShell 7 AST parser and fixture-driven runner invocation).

STATIC layer (files-present proof; RED before the runner is created):

1. `scripts/verify-terminal-lint.ps1` exists as a tracked, non-ignored, non-empty file under `scripts/`.
2. It PARSES CLEANLY under the built-in PowerShell AST parser with zero parse errors.
3. It declares its required `param(...)` surface: `FeatureId`.

BEHAVIORAL layer (at most three scenario classes; consolidated table-driven; non-vacuous). After the runner exists the harness EXERCISES `verify-terminal-lint.ps1` (`-FeatureId 175-F`) as a controlled fixture-driven subject, independently proving BOTH supported GOOS surfaces (`windows`, `linux`):

- `green_success`: a fixture clean on BOTH the `windows` and `linux` surfaces -> native exit 0 AND the exact `TERMINAL-LINT-OK:175-F` marker.
- `red_single_surface` (table-driven RED matrix): a residual warning injected ONLY under `GOOS=windows` (e.g. on a `_windows.go` path) and, separately, ONLY under `GOOS=linux` (e.g. on a `_unix.go` path) -> each native NONZERO, no success marker; proving each surface is independently analyzed.
- `fail_closed_input` (table-driven fail-closed matrix): a missing `-FeatureId` or a missing required precondition input -> native NONZERO.

Non-vacuity evidence: the both-surfaces-clean fixture and EACH single-surface residual fixture MUST yield DIFFERENT exit codes; only the both-clean case emits `TERMINAL-LINT-OK:175-F`, so a host-only single-surface implementation is rejected. This bootstrap fixture set and U12's executable terminal gate agree on the runner CLI (`scripts/verify-terminal-lint.ps1 -FeatureId 175-F`), the `TERMINAL-LINT-OK:175-F` success marker, and fail-closed-on-either-surface failure semantics.

## Independence and safety posture

- INDEPENDENTLY COMPILABLE (RED before / GREEN after): the harness compiles under existing main-branch tooling with no dependency on the runner pre-existing; test execution is RED (nonzero) before the runner exists and GREEN after.
- NO RETROFIT of YAML BDD: one imperative PowerShell runner and one Go harness only.
- Governed-claim posture: claiming the parent shipment `176-S` runs `core.ClaimShipment` (`internal/core/shipment_lifecycle.go:46-98`), which activates the shipment AND every queued member and cascades status to the feature. The governed claim therefore dirties, under `.backlogit/`, the FULL claim footprint: the shipment manifest `.backlogit/queue/176-S.md`; every member status artifact `.backlogit/queue/175.099-T.md`, `.backlogit/queue/175.100-T.md`, `.backlogit/queue/175.101-T.md` (not this task alone); the feature `.backlogit/queue/175-F.md` (status cascade); and the append-only event/hook journals `.backlogit/hooks_queue.jsonl` and the events JSONL. All of these are outside the owned `scripts/` and `tests/` surface. FAIL closed on any dirty path outside the CLOSED union {the governed-claim footprint above} ∪ {`scripts/verify-terminal-lint.ps1`, `tests/runner_bootstrap_terminal_lint_175_101_test.go`}. NOTE: this shipment is not claimable for wave execution until the shipment-claim / wave-scheduler convergence prerequisite lands (readiness BLOCKED; see `docs/decisions/2026-09-20-shipment-claim-wave-scheduler-convergence-deliberation.md`).

## Acceptance criteria

- `scripts/verify-terminal-lint.ps1` exists as a tracked, non-ignored, non-empty file, and `tests/runner_bootstrap_terminal_lint_175_101_test.go` declares `TestU175_101_TerminalLintRunnerBootstrap`.
- The EXECUTABLE non-vacuity wrapper (package `./tests`, anchored `^TestU175_101_`) is RED before the runner exists and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_101_` line; propagates native nonzero exit).
- Static layer: the harness proves the runner exists, is tracked, is non-empty, parses cleanly, and declares its required `param(...)` surface (`FeatureId`).
- Terminal-runner cross-surface proof (at most three scenario classes, non-vacuous): `green_success` (both surfaces clean -> native exit 0 + exact `TERMINAL-LINT-OK:175-F`), `red_single_surface` (windows-only and linux-only injected residual each -> native nonzero, no marker), and `fail_closed_input` (missing `-FeatureId` -> native nonzero); the both-clean and each single-surface fixture yield DIFFERENT exit codes, so a host-only single-surface implementation is rejected.
- The GATE never becomes circular: the task-lint command runs only the Go harness (`go test ./tests -run ^TestU175_101_`) and is never a direct invocation of `scripts/verify-terminal-lint.ps1`.
- No Go source, workflow, or existing script is modified; the only created files are the one runner and the one harness.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$o = & go test \"./tests\" -run \"^TestU175_101_\" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match \"^--- PASS: TestU175_101_\" }).Count; if ($p -lt 1) { Write-Output \"NON-VACUITY-FAIL: zero named --- PASS: TestU175_101_ lines\"; exit 1 }; Write-Output \"TASK-LINT-OK:175.101-T:runner-bootstrap-terminal-lint\"; exit 0'",
  "lint_scope": {
    "kind": "runner-bootstrap-self-contained",
    "owned_runner_script": "scripts/verify-terminal-lint.ps1",
    "owned_file": "tests/runner_bootstrap_terminal_lint_175_101_test.go",
    "package": "./tests",
    "test_function": "TestU175_101_TerminalLintRunnerBootstrap",
    "gate_must_not_be_runner": "scripts/verify-terminal-lint.ps1",
    "harness_invokes_runner_as_fixture_subject": true,
    "success_marker": "TERMINAL-LINT-OK:175-F",
    "static_proofs": [
      "scripts/verify-terminal-lint.ps1 exists as a tracked, non-ignored, non-empty file under scripts/",
      "it parses cleanly under the built-in PowerShell AST parser with zero parse errors",
      "it declares its required param(...) surface: FeatureId"
    ],
    "behavioral_scenarios": {
      "runner": "verify-terminal-lint.ps1",
      "invocation": "-FeatureId 175-F",
      "surfaces": [
        "windows",
        "linux"
      ],
      "scenario_classes": {
        "green_success": "fixture clean on BOTH windows and linux surfaces -> native exit 0 + TERMINAL-LINT-OK:175-F marker",
        "red_single_surface": "table-driven RED matrix (residual warning injected only under GOOS=windows e.g. a _windows.go path; residual warning injected only under GOOS=linux e.g. a _unix.go path) -> native nonzero, no success marker; proves each surface independently analyzed",
        "fail_closed_input": "table-driven fail-closed matrix (missing -FeatureId; missing required precondition input) -> native nonzero"
      },
      "non_vacuity": "both-surfaces-clean fixture and each single-surface residual fixture must yield different exit codes; only the both-clean case emits TERMINAL-LINT-OK:175-F; a host-only single-surface implementation fails at least one RED scenario"
    },
    "fixture_injectability_requirement": "the runner must accept inputs via its declared parameter surface and an execution working directory (with an in-process GOOS override) so the harness drives it against harness-authored deterministic fixtures without mutating live repository state",
    "safety": "harness constructs all fixtures in temp directories and invokes the runner via fixed argument vectors (no shell string, no untrusted-input interpolation)",
    "tooling": "existing main-branch go test + PowerShell 7 only; the runner this task creates is never used as this task's gate",
    "bootstrap_rationale": "This member OWNS creation of scripts/verify-terminal-lint.ps1; its GATE is its owned Go harness (never a direct invocation of the runner it creates) and MUST compile with its targeted test executing before the runner exists (RED nonzero before, GREEN after). It is the terminal sink of the runner-bootstrap chain (dependency-ordered after the baseline-lint runner-bootstrap task 175.100-T) and the task on which U1 (175.001-T) depends. Its CLI, TERMINAL-LINT-OK:175-F success marker, and fail-closed-on-either-surface semantics agree with U12's executable terminal gate."
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.101-T:runner-bootstrap-terminal-lint",
    "behavioral_discriminators": [
      "verify-terminal-lint.ps1: both-surfaces-clean vs windows-only-residual and vs linux-only-residual fixtures yield different exit codes; emits TERMINAL-LINT-OK:175-F only when both windows and linux surfaces are clean"
    ],
    "rejects": "a no-op or host-only single-surface runner that only parses and declares params but never independently analyzes both GOOS surfaces"
  }
}
```

<!-- END:task-lint-contract -->
