---
artifact_type: task
created_at: 2026-04-03T23:45:25.9625271-07:00
id: F014.T002
labels:
    - db
    - spike
parent_id: F014
priority: medium
status: queued
title: DB schema, rehydration, and template support for spike
updated_at: 2026-04-03T23:45:25.9625271-07:00
---

Ensure the DB schema, rehydration engine, and template system handle the new `spike` artifact type and its `spike_ref` custom field.

The `spike` type should be storable in the existing `items` table (the `artifact_type` column already accepts any string). The `spike_ref` field maps through the existing `custom_fields` JSON column or through `header-def` dynamic column extension via `ApplySchemaExtensions`.

In `internal/db/rehydration.go`:
- Verify that spike Markdown files under `queue/` are picked up by the directory walker and indexed correctly
- Verify `spike_ref` is captured in `custom_fields` during frontmatter parsing

In `internal/config/defaults.go`:
- Ensure the default template directory includes a `spike.md` template

In `.backlogit/templates/`:
- Create a `spike.md` template with frontmatter fields: `id`, `title`, `status`, `artifact_type: spike`, `spike_ref`, and a body section for research notes

Files to modify: `internal/db/rehydration.go` (verify), `internal/config/defaults.go`, `.backlogit/templates/spike.md`

Verification: Create a spike via CLI or MCP, run `backlogit sync`, query `SELECT * FROM items WHERE artifact_type='spike'` and confirm the row exists with `spike_ref` in custom_fields.