---
artifact_type: task
created_at: 2026-09-17T23:59:48.407223Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.008-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U8 - errcheck remediation: internal/events'
updated_at: 2026-09-18T00:55:35.589109Z
---

Remediate ALL golangci-lint errcheck findings in internal/events using the per-category strategy (named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation). No opportunistic cross-package helper.

Acceptance criteria:
- Scope (package/files): internal/events only; edit only errcheck-flagged files within internal/events. Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order.
- Verification (zero-finding): `golangci-lint run ./internal/events` reports 0 errcheck; `gofmt -l` on touched files empty; `go build ./internal/events` and existing internal/events tests still pass.
- Existing test expectations: all current internal/events tests pass unchanged; success-path behavior preserved.
- Test posture (Principle II): NEW reachable failure-path behavior requires a failing (RED) test FIRST; purely mechanical captures use the PRE-DECLARED mechanical harness-exemption contract recorded in the closure artifact at execution (not retrospective).