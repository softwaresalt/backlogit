---
artifact_type: task
created_at: 2026-09-19T01:21:12.3931056Z
dependencies:
    - 175.001-T
id: 175.009-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck+staticcheck): internal/cli/jsonrpc_test.go (+1)'
updated_at: 2026-09-19T07:14:58.4932366Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck, staticcheck
Package: ./internal/cli
Owned source file(s) (2): internal/cli/jsonrpc_test.go, internal/cli/status_cmd.go
Owned baseline findings (6) from docs/decisions/2026-09-17-baseline-lint-inventory.md (golangci-lint v2.13.2, unbounded invocation):
- internal/cli/jsonrpc_test.go:L126:C9 [staticcheck] QF1011: could omit type func() error from declaration; it will be inferred from the right-hand side
- internal/cli/status_cmd.go:L30:C18 [errcheck] Error return value of `ws.Close` is not checked
- internal/cli/status_cmd.go:L38:C15 [errcheck] Error return value of `fmt.Fprintf` is not checked
- internal/cli/status_cmd.go:L40:C17 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/status_cmd.go:L45:C16 [errcheck] Error return value of `fmt.Fprintln` is not checked
- internal/cli/status_cmd.go:L47:C16 [errcheck] Error return value of `fmt.Fprintf` is not checked
Affected functions (2 distinct): internal/cli/jsonrpc_test.go: TestExecute_FunctionExists; internal/cli/status_cmd.go: newStatusCommand

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/cli/lintfix_175_009_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_009_LintFix` (deterministic `TestU175_009_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/cli" -run "^TestU175_009_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_009_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_009_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck + staticcheck): handle every flagged errcheck return (checked propagation/wrapping or test-receiver failure; never `_ =`) AND apply each staticcheck-suggested rewrite (QF/ST) exactly. Do NOT suppress with `//nolint`. Edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 3 (owned source file(s) + one owned harness file).
- Implementation files = 2 (owned harness excluded).
- Owned baseline findings = 6 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/cli -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.009-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "packages": [
      "./internal/cli"
    ],
    "owned_files": [
      "internal/cli/jsonrpc_test.go",
      "internal/cli/status_cmd.go"
    ],
    "owned_findings": [
      {
        "path": "internal/cli/jsonrpc_test.go",
        "line": 126,
        "column": 9,
        "linter": "staticcheck",
        "message": "QF1011: could omit type func() error from declaration; it will be inferred from the right-hand side"
      },
      {
        "path": "internal/cli/status_cmd.go",
        "line": 30,
        "column": 18,
        "linter": "errcheck",
        "message": "Error return value of `ws.Close` is not checked"
      },
      {
        "path": "internal/cli/status_cmd.go",
        "line": 38,
        "column": 15,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      },
      {
        "path": "internal/cli/status_cmd.go",
        "line": 40,
        "column": 17,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/status_cmd.go",
        "line": 45,
        "column": 16,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintln` is not checked"
      },
      {
        "path": "internal/cli/status_cmd.go",
        "line": 47,
        "column": 16,
        "linter": "errcheck",
        "message": "Error return value of `fmt.Fprintf` is not checked"
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.009-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
