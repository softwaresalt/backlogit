---
artifact_type: task
created_at: 2026-09-17T23:59:54.7154757Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.009-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U9 - errcheck remediation: tests/integration'
updated_at: 2026-09-18T00:56:02.6919328Z
---

Remediate ALL golangci-lint errcheck findings in tests/integration. Test-layer policy: prefer require.NoError / explicit assertions over %w propagation; a justified //nolint:errcheck is allowed ONLY for genuinely non-load-bearing test cleanup (with an inline reason). Prefer t.Setenv over a discarded os.Setenv where a *testing.T is in scope.

Acceptance criteria:
- Scope (package/files): tests/integration only; edit only errcheck-flagged files within tests/integration. Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order.
- Verification (zero-finding): `golangci-lint run ./tests/integration` reports 0 errcheck; `gofmt -l` on touched files empty; `go test ./tests/integration` still builds and passes.
- Existing test expectations: all current tests/integration tests pass unchanged; assertion additions must not weaken or skip existing coverage.
- Test posture (Principle II): a test-layer change that adds a genuinely new assertion of reachable behavior confirms it fails before the fix where applicable; purely mechanical cleanup captures (require.NoError / justified //nolint) use the PRE-DECLARED mechanical harness-exemption contract recorded in the closure artifact at execution (not retrospective).