---
artifact_type: task
created_at: 2026-09-17T23:59:54.7154757Z
dependencies:
    - 175.001-T
id: 175.009-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (staticcheck): internal/cli/jsonrpc_test.go'
updated_at: 2026-09-18T05:55:20.3400929Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: staticcheck
Package: internal/cli
Owned source file (exactly one): internal/cli/jsonrpc_test.go
Findings (1) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L126: QF1011: could omit type func() error from declaration; it will be inferred from the right-hand side
Affected functions (1 distinct, < 5): TestExecute_FunctionExists
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/cli/staticcheck_jsonrpc_test_175_test.go
Ship's harness phase creates this harness JUST BEFORE this file task (NOT precommitted at Stage). It is a source-shape or command-backed test scoped to internal/cli/jsonrpc_test.go ONLY: it asserts this file's staticcheck / behavior contract without referencing any not-yet-existing symbol and without leaving any future-task red assertion in the full suite. It is EXCLUDED from the implementation-file inventory.
Red-before-green selector (targeted, this file only): go test ./internal/cli -run "^Test_staticcheck_Conv175_jsonrpc_test$" -count=1 -- RED (assertion failure, not a build error) before the fix, GREEN after.

Fix strategy: Omit the redundant func() error type from the flagged declaration; let the type infer from the right-hand side.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only file: verify with go test ./internal/cli -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- go test ./internal/cli -run=^$ -count=1 (test-only-safe compile of this package's tests; NOT go build on a test file) passes, and golangci-lint run ./internal/cli reports 0 staticcheck findings in internal/cli/jsonrpc_test.go.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/cli/staticcheck_jsonrpc_test_175_test.go was RED (assertion) before the fix via Test_staticcheck_Conv175_jsonrpc_test and GREEN after; no future-task red remains in the full suite.
- ONLY internal/cli/jsonrpc_test.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption, NO Principle II deviation. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.