---
artifact_type: task
created_at: 2026-09-17T23:59:54.7154757Z
dependencies:
    - 175.001-T
id: 175.009-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (staticcheck): internal/cli/jsonrpc_test.go'
updated_at: 2026-09-18T07:47:51.5729849Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: staticcheck
Package: internal/cli
Owned source file (exactly one): internal/cli/jsonrpc_test.go
Findings (1) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L126: QF1011: could omit type func() error from declaration; it will be inferred from the right-hand side
Affected functions (1 distinct, < 5): TestExecute_FunctionExists
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/cli/staticcheck_jsonrpc_test_175_test.go
Ship's harness phase creates this harness JUST BEFORE this file task (NOT precommitted at Stage). It is a source-shape or command-backed test scoped to internal/cli/jsonrpc_test.go ONLY: it asserts this file's staticcheck / behavior contract without referencing any not-yet-existing symbol and without leaving any future-task red assertion in the full suite. It is EXCLUDED from the implementation-file inventory.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test function named `TestU175_009_StaticcheckJsonrpcTest` (deterministic `TestU175_009_` unit token; no other task uses this prefix). RED before the fix (the assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/cli" -run "^TestU175_009_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_009_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_009_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the task's explicit package (`./internal/cli`) and anchored `^TestU175_009_` selector with `-v -count=1`; captures and re-emits the combined go test output; preserves and propagates the native `go test` nonzero exit; counts lines matching `^--- PASS: TestU175_009_` and EXITS NONZERO when that count is < 1 (so a zero-match/absent selector fails vacuously instead of passing). Before implementation the same command is RED via an assertion failure on `TestU175_009_StaticcheckJsonrpcTest`; after the green phase it exits 0 only when at least one named PASS line is observed.

Fix strategy (staticcheck QF1011 at internal/cli/jsonrpc_test.go L126 in TestExecute_FunctionExists): REMOVE ONLY the redundant explicit `func() error` type from the declaration so the type is inferred from the right-hand side (e.g. `var f func() error = g` becomes `f := g`). No behavior change, no other edit, no ignored return.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only-safe compile: verify the package's tests with go test ./internal/cli -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 AND (via its counted `--- PASS: TestU175_009_` guard) proves at least one named PASS (non-vacuous), and golangci-lint run ./internal/cli reports 0 staticcheck findings in internal/cli/jsonrpc_test.go.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/cli/staticcheck_jsonrpc_test_175_test.go was RED (assertion on TestU175_009_StaticcheckJsonrpcTest) before the fix and GREEN after; no future-task red remains in the full suite.
- The flagged staticcheck finding(s) (L126: QF1011: could omit type func() error from declaration; it will be inferred from the right-hand side) are resolved by a BEHAVIOR-PRESERVING change; golangci-lint run ./internal/cli reports 0 staticcheck findings in internal/cli/jsonrpc_test.go; and NO new errcheck / unchecked-return finding is introduced (no `_ =` / ignored return).
- ONLY internal/cli/jsonrpc_test.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.