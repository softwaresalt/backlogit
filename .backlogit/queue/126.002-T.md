---
artifact_type: task
created_at: 2026-07-25T22:32:31.9788451Z
dependencies:
    - 126.001-T
id: 126.002-T
labels:
    - build
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U2: Add Makefile + make.ps1 md-lint targets delegating to scripts/md-lint.{sh,ps1}'
updated_at: 2026-07-25T22:32:31.9788451Z
---

Domain: build/config. Add a `md-lint` target to the `Makefile` (and add `md-lint` to `.PHONY`) plus a `md-lint` case to `make.ps1` (and to its `-Target` ValidateSet + usage doc) so Ship, CI, and humans reproduce the exact P-008 gate on both platforms. The Node tool invocation MUST NOT live in the `Makefile`, `ci.yml`, or any other operator-facing file: the `tests/integration` retired-wrapper guard (`TestActivePluginDocsDoNotReferenceRetiredNPMWrapper`) forbids the substrings `npx `/`npm install` in `Makefile`/`ci.yml`/README and friends. Follow the repo's established paired-script convention (`scripts/search.{sh,ps1}`, `scripts/*_lock.{sh,ps1}`): add `scripts/md-lint.sh` (bash, LF line endings) and `scripts/md-lint.ps1`, each running a bare, version-pinned `npx --yes markdownlint-cli2@0.23.1 "**/*.md"` (cli2 auto-discovers `.markdownlint.json` + `.markdownlint-cli2.jsonc`); `scripts/` is not in the guard's file set. The `Makefile` target runs `bash scripts/md-lint.sh`; the `make.ps1` case runs `scripts/md-lint.ps1`. Verification: `make md-lint`, `bash scripts/md-lint.sh`, and `make.ps1 md-lint` each exit 0 (0 violations repo-wide). Depends on U1 (126.001-T).