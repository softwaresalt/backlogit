---
artifact_type: task
created_at: 2026-09-17T23:59:01.1120137Z
id: 175.001-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U1 - Harden .gitattributes line endings + renormalize (STRICT PREDECESSOR)
updated_at: 2026-09-18T19:24:16.5937293Z
---

Config unit and STRICT PREDECESSOR of every executable lint task (175.002-T..175.011-T, 175.013-T, 175.015-T..175.039-T) and of the line-ending guard-script task U40 (175.040-T). Scope: `.gitattributes` line-ending hardening + a pathspec-scoped WORKING-TREE REFRESH of the FROZEN tracked CRLF `*.go`/`*.json` set (with a pathspec-scoped `git add --renormalize` of that same set for index hygiene). Single-domain (config/line-ending migration).

EXACT BASELINE SCOPE (corrected): the frozen set is 548 tracked paths — 518 `*.go` + 30 `*.json` — whose WORKING TREE is CRLF at baseline, established from the live read-only `git ls-files --eol` count. Evidence over tracked `*.go`/`*.json`: **indexCRLF=0** (every target path is already `i/lf`) and **worktreeCRLF=548** (`w/crlf`: 518 Go + 30 JSON). BASELINE INDEX EXPECTATION (recorded): indexCRLF=0 — the index blobs are ALREADY LF; the problem is 548 CRLF *working-tree* paths, NOT CRLF index blobs. All plan/feature/deliberation/memory/task/checkpoint counts use 548 (518 Go + 30 JSON).

FROZEN SET IS THE WORKTREE REFRESH/VERIFICATION SET (corrected contract): the frozen 548 paths are the exact set to REFRESH in the working tree and VERIFY as LF. Because indexCRLF=0, `git add --renormalize` over this set produces NO content-staged diff for the already-LF index blobs; therefore renormalize MUST NOT require all 548 source paths to appear staged. The staged source subset MAY BE EMPTY or a SUBSET of the frozen 548, and MUST contain NO path OUTSIDE the frozen set. `.gitattributes` is staged separately. FAIL closed on any staged path OUTSIDE the frozen set (a staged EXTRA); DO NOT fail merely because an index-LF path produces no staged diff (a missing/no-diff path WITHIN the frozen set is expected and allowed).

2-HOUR RULE vs FILE-COUNT HEURISTIC: this is ONE homogeneous, purely MECHANICAL line-ending migration whose PRIMARY bound — human-equivalent effort well under 2 hours — is met by a single pathspec-scoped command sequence (`.gitattributes` edit + one scoped worktree refresh + one scoped `git add --renormalize` + verification). The `<3 files` guideline is a HEURISTIC PROXY for effort, DELIBERATELY exceeded here because a byte-only EOL migration touches all 548 frozen paths with ZERO semantic change. This is NOT a waiver of the 2-hour rule and NOT a harness/granularity waiver.

SAFETY POSTURE (Freeze-scope + Careful mode): this unit runs under BOTH `Freeze-scope` and `Careful mode`.
- Freeze-scope: the migration set is FROZEN to exactly the 548 enumerated paths (plus `.gitattributes`); no path outside the frozen set may be staged or refreshed, and the set is captured once and reused for refresh, staging, and verification.
- Careful mode: each mutating step declares ProposedAction (the exact command), ActionRisk (working-tree mutation over 548 tracked files; index already LF; reversible via `git restore`), an explicit approval checkpoint, a rollback plan (frozen-set-scoped `git restore --worktree --source=HEAD --pathspec-from-file=<temp> --pathspec-file-nul` plus `git reset` of any staged `.gitattributes`), and a recorded ActionResult (staged path set + verification outcome) before proceeding.
- IMMEDIATE OPERATOR-ONLY APPROVAL (blocking, unchanged): MANDATORY, explicitly-recorded OPERATOR-ONLY approval of the renormalization recorded BEFORE any `git add --renormalize` staging or any worktree refresh (NOT merely before the commit). Ship may prepare/present the proposed `.gitattributes` rules, the enumerated 548-path set, and its verification evidence but CANNOT authorize; authorization is the operator's alone.
- CLEAN-TREE PRECONDITION (blocking, claim-aware): Ship claims this task (status -> active) AFTER the wave harness commit, so a governed claim legitimately dirties a BOUNDED, ENUMERATED set of backlog-state paths before U1 begins; the precondition therefore CANNOT demand a porcelain empty except `.gitattributes`. Before staging/refreshing anything: (1) SNAPSHOT the governed claim-generated allow-set — the EXACT, CLOSED, empirically-proven 2-path TRACKED set the claim mutates in this workspace: (a) this task's OWN status artifact `.backlogit/queue/175.001-T.md` (status -> active + `updated_at` bump), modified IN PLACE — verified there is NO per-status relocation in this workspace (`done`/`blocked` items live in `queue/` alongside `queued`; the queue layout `root_dir` is `queue` for every status, so the claim is a same-path content edit, NEVER a rename or add+delete pair); and (b) the append-only hook/event byproduct `.backlogit/hooks_queue.jsonl` that backlogit produces on the claim transition (the post-update `HookUpdateArtifact` event append). These are the ONLY two TRACKED, NON-IGNORED paths the claim can dirty — verified via `git check-ignore`: backlogit's SQLite index `.backlogit/backlogit.db{,-shm,-wal}` and the `.backlogit/.locks|review|runtime` runtime state are ALL gitignored and never appear in porcelain, and `.backlogit/stash.jsonl` is NOT touched by a task status transition. This CLOSED 2-path allow-set SUPERSEDES the prior open-ended "at minimum" language and is the COMPLETE governed set; (2) assert `git status --porcelain` contains NOTHING outside that governed allow-set PLUS the intended `.gitattributes` edit — FAIL closed and halt on ANY other index/worktree change, including any BROADER `.backlogit/**` dirt not in the enumerated allow-set (clean-tree safety is NOT waived and broad `.backlogit/**` dirt is NOT tolerated); (3) preserve the snapshotted governed paths byte-for-byte through the pathspec-scoped line-ending refresh/renormalize (they are `.md`/`.jsonl`, excluded from the frozen 548 `*.go`/`*.json` set, so the EOL operations never touch them) and re-verify they are unchanged after the migration.

Changes:
- Add explicit `eol=lf` rules for `*.go` and `*.json`.
- The byte-exact golden JSON fixture MUST remain governed by `eol=lf` (LF-normalize its committed bytes and stage under `eol=lf`). The previously-allowed `-text` branch for the golden is REMOVED — do NOT pin the golden as `-text`. The golden is part of the fixed `*.json` set and is checked by U40 (`175.040-T`) and the U14 (`175.014-T`) workflow guard.
- Pin true binaries (`*.exe`, `*.db`, images, intentionally-binary fixtures) as `-text`.
- NUL-SAFE PATHSPEC CONSTRUCTION (corrected): enumerate `git ls-files -z --eol -- '*.go' '*.json'`; each NUL-separated record has the shape `<metadata>\t<path>` (metadata `i/... w/... attr/...` then a TAB then the path). Extract ONLY the PATH FIELD AFTER THE TAB and write those paths (NUL-separated) to the temp pathspec file. NEVER write the full `i/... w/... attr/...<TAB>path` records as pathspecs. Filter to the frozen 548 (the worktree-CRLF set).
- PATHSPEC-SCOPED WORKTREE REFRESH (PRIMARY op; replaces whole-tree checkout): refresh ONLY the frozen set from the INDEX using the DEFAULT index source: `git restore --worktree --pathspec-from-file=<temp> --pathspec-file-nul` (NO `--source`; the default source is the index, which is already LF). `--source=:` is INVALID and MUST NOT appear anywhere. This rewrites the 548 working-tree files to the index's LF bytes. Do NOT use `git checkout-index -f -a` or any whole-tree removal/re-checkout.
- PATHSPEC-SCOPED RENORMALIZE (index hygiene; MAY stage nothing): stage `.gitattributes` separately (`git add -- .gitattributes`), then `git add --renormalize --pathspec-from-file=<temp> --pathspec-file-nul` over the frozen set. Because indexCRLF=0 this typically stages NO source content; that is EXPECTED. FAIL closed only if a staged path lies OUTSIDE the frozen set (plus `.gitattributes`).

COMMITTED-FILE BOUND (truthful): U1 operationally REFRESHES/VERIFIES 548 working-tree paths, but because the baseline index is already LF (indexCRLF=0) the expected IMPLEMENTATION DIFF committed is `.gitattributes` ONLY (plus the separately-committed U1-owned harness file). Any source/index EOL diff, if present, is a SUBSET of the frozen 548 and is byte-canonicalization verified (EOL-only). U1 does NOT claim 548 staged file modifications.

Safety checks (corrected):
- Assert `git ls-files --eol` shows `w/lf` (working tree) for all 548 tracked `.go`/`.json` ONLY AFTER the scoped worktree refresh; `i/lf` (index) holds at baseline AND after (indexCRLF=0); `w/lf` under `eol=lf` for the golden; `-text` for declared binaries.
- Content-identity (EOL-only proof) over any staged subset: do NOT use `git diff --cached -w`. For each staged text path EXCLUDING `.gitattributes` and declared binaries, compare base vs index blob after canonicalizing ONLY CRLF->LF: `B = git cat-file blob HEAD:<path>`, `I = git cat-file blob :<path>`; replace every CRLF with LF (leaving all other bytes, including any bare CR, untouched); require `SHA-256(canon(B)) == SHA-256(canon(I))`. Equal hashes prove ONLY line-ending bytes changed.
- Binary identity: do NOT treat `git diff --cached --numstat` `-`/`-` as byte-identity proof. Require each declared binary ABSENT from the staged diff, OR compare pre/post blob hashes (`git rev-parse :<path>`) and confirm identical.

Test posture (Principle II / P-002.6): NORMAL harness-required task — no harness exemption (no `harness-exempt` label, no exemption contract). U1 EXCLUSIVELY OWNS one deterministic harness path: `tests/lineending_baseline_175_test.go` (exactly-one-owner; no other unit may create it). It declares a single task-scoped Go test function named `TestU175_001_BaselineEol` (deterministic `TestU175_001_` unit token). P-002.6 SCOPED VERIFICATION COMMAND (EXECUTABLE non-vacuity wrapper; explicit package, anchored, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./tests" -run "^TestU175_001_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_001_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_001_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the explicit package (`./tests`) and anchored `^TestU175_001_` selector with `-v -count=1`; captures and re-emits combined go test output; preserves/propagates the native nonzero exit; counts `^--- PASS: TestU175_001_` lines and EXITS NONZERO when that count is < 1 (a zero-match/absent selector fails vacuously instead of passing). Execution contract: run from repo root. The harness enumerates the frozen 548-path set (`git ls-files -- '*.go' '*.json'` filtered to the migration set, excluding declared binaries) and asserts EVERY path's WORKING-TREE bytes are LF — this is the RED discriminator: the 548 CRLF working-tree paths fail the LF assertion BEFORE the `.gitattributes` hardening + scoped worktree refresh, and pass GREEN AFTER. It ADDITIONALLY verifies index blobs are LF (a stable invariant already true at baseline given indexCRLF=0). After the green phase the command is NON-VACUOUS: it MUST observe at least one `^--- PASS: TestU175_001_` line; zero matching named-PASS lines is a FAILURE even if `go test` exits 0. The harness makes no caller-index mutation and is source-shape/command-backed. It is a `*_test.go` file EXCLUDED from the implementation-file inventory (the sole implementation surface owned is `.gitattributes`).

Note on workflow ownership: U1 changes NO workflow content. `.github/workflows/ci.yml` stays BYTE-IDENTICAL. The persistent line-ending guard is split into the guard-script task U40 (`175.040-T`, owning `scripts/check-line-endings.ps1`) and the workflow-integration task U14 (`175.014-T`, owning `.github/workflows/line-endings.yml`).

Acceptance criteria:
- `.gitattributes` declares `eol=lf` for `*.go`/`*.json`, LF-preserving `eol=lf` for the golden JSON (NO `-text` branch), and `-text` for all true binaries.
- After the scoped worktree refresh, `git ls-files --eol` reports `w/lf` (working tree) for all 548 tracked `.go`/`.json` (518 Go + 30 JSON); `i/lf` (index) holds at baseline and after (indexCRLF=0 recorded).
- The staged set contains NO path OUTSIDE the frozen 548 + `.gitattributes` (FAIL on any staged EXTRA); it MAY be empty or a subset (NO failure for an index-LF path that produces no staged diff). Staging used `git add --renormalize --pathspec-from-file=<temp> --pathspec-file-nul` (NOT `git add --renormalize .`); `.gitattributes` staged separately. The governed claim-generated backlog-state paths (`.backlogit/queue/175.001-T.md`, `.backlogit/hooks_queue.jsonl`) are NEVER added to U1's staged set and are NOT part of U1's committed implementation diff — they are preserved as-is, outside U1's frozen-548 EOL scope.
- The NUL pathspec file contains ONLY path fields (extracted AFTER THE TAB from `git ls-files -z --eol`), never full `i/... w/... attr/...` eol records.
- The worktree refresh is pathspec-scoped to the frozen 548 set using `git restore --worktree --pathspec-from-file=... --pathspec-file-nul` with NO `--source` (default index source); `--source=:` does not appear; no whole-tree checkout/removal.
- Committed implementation diff is `.gitattributes` only (index already LF); any staged EOL subset is EOL-only verified by normalized-blob SHA-256; every declared binary proven byte-identical.
- Freeze-scope + Careful mode observed: frozen 548-path set; each mutating step records ProposedAction/ActionRisk/approval/rollback/ActionResult; operator-only approval recorded BEFORE any renormalize staging or refresh; clean-tree precondition enforced; rollback is frozen-set-scoped `git restore --worktree --source=HEAD --pathspec-from-file=... --pathspec-file-nul`.
- 2-hour bound: one pathspec-scoped mechanical command sequence; the `<3 files` heuristic documented as exceeded (not waived) with the 548-file (518 Go + 30 JSON) evidence.
- Harness (P-002.6): `tests/lineending_baseline_175_test.go` declares `TestU175_001_BaselineEol`; the EXECUTABLE non-vacuity wrapper (package `./tests`, anchored `^TestU175_001_`) is RED before and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_001_` line; propagates native nonzero exit). `ci.yml` remains byte-identical.
- Task lint contract (harness-file-scoped): the canonical, executable, task-scoped lint verifier is the `task_lint_cmd` in this task's `<!-- BEGIN:task-lint-contract -->` block at the END of this file. It runs golangci-lint (in-memory `ProcessStartInfo` stdout capture; no temp file, no `Remove-Item`) over package `./tests`, validates the JSON schema before filtering (fail-closed exits 91-97; native launch/exit failures preserved distinctly), and matches U1's OWNED harness path `tests/lineending_baseline_175_test.go` by ordinal exact identity across ALL linters (any-linter), exiting 1 if any owned finding remains. It is TASK-SCOPED ONLY; the full-repository `golangci-lint run` is MANDATORY at Ship's wave-convergence gate and is NOT weakened by this per-task verifier.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$ErrorActionPreference=\"Stop\"; $psi=[System.Diagnostics.ProcessStartInfo]::new(); $psi.FileName=\"golangci-lint\"; $psi.Arguments=\"run --output.json.path stdout --show-stats=false --issues-exit-code 0 ./tests\"; $psi.RedirectStandardOutput=$true; $psi.RedirectStandardError=$true; $psi.UseShellExecute=$false; $psi.WorkingDirectory=(Get-Location).Path; try { $p=[System.Diagnostics.Process]::Start($psi) } catch { Write-Output \"LINT-NATIVE-LAUNCH-FAIL\"; exit 97 }; $so=$p.StandardOutput.ReadToEndAsync(); $se=$p.StandardError.ReadToEndAsync(); $p.WaitForExit(); $out=$so.GetAwaiter().GetResult(); $err=$se.GetAwaiter().GetResult(); $ec=$p.ExitCode; if ($ec -ne 0) { Write-Output (\"LINT-NATIVE-FAIL:\"+$ec); if ($err) { Write-Output $err }; exit $ec }; $t=$out.Trim(); if ($t.Length -eq 0) { Write-Output \"LINT-SCHEMA-FAIL:empty-stdout\"; exit 91 }; try { $j=$t | ConvertFrom-Json -ErrorAction Stop } catch { Write-Output \"LINT-SCHEMA-FAIL:malformed-json\"; exit 92 }; if ($null -eq $j) { Write-Output \"LINT-SCHEMA-FAIL:json-null\"; exit 93 }; if (-not ($j -is [System.Management.Automation.PSCustomObject])) { Write-Output \"LINT-SCHEMA-FAIL:non-object-root\"; exit 94 }; if (-not ($j.PSObject.Properties.Name -contains \"Issues\")) { Write-Output \"LINT-SCHEMA-FAIL:missing-Issues\"; exit 95 }; if (-not ($j.Issues -is [System.Array])) { Write-Output \"LINT-SCHEMA-FAIL:Issues-not-collection\"; exit 96 }; $hits=@($j.Issues | Where-Object { [string]::Equals(((\"\" + $_.Pos.Filename) -replace \"\\\\\",\"/\"), \"tests/lineending_baseline_175_test.go\", [System.StringComparison]::Ordinal) }); if ($hits.Count -gt 0) { Write-Output (\"OWNED-FINDING-REMAINS:\"+$hits.Count); exit 1 }; Write-Output \"FSLV-OK:tests/lineending_baseline_175_test.go:any-linter:0-owned-findings\"; exit 0'",
  "lint_scope": {
    "kind": "harness-file-scoped-go-lint",
    "package": "./tests",
    "linter": null,
    "owned_file": "tests/lineending_baseline_175_test.go"
  },
  "non_vacuity_evidence": {
    "mode": "structured-json-schema-validated",
    "success_marker": "FSLV-OK:tests/lineending_baseline_175_test.go:any-linter:0-owned-findings",
    "asserts": [
      "native golangci-lint launch failure preserved distinctly (exit 97)",
      "native golangci-lint nonzero exit preserved and propagated (passthrough)",
      "empty stdout is a hard failure (exit 91)",
      "malformed JSON is a hard failure (exit 92)",
      "JSON null root is a hard failure (exit 93)",
      "non-object JSON root is a hard failure (exit 94)",
      "missing Issues key is a hard failure (exit 95)",
      "non-array Issues is a hard failure (exit 96)",
      "schema validated before filtering; no unknown or vacuous success shape is accepted",
      "owned harness path matched by ordinal exact identity across all linters; exit 1 if any owned finding remains",
      "read-only and task-scoped; no machine temp file, no Remove-Item cleanup; full repository golangci-lint run deferred to wave convergence"
    ]
  }
}
```

<!-- END:task-lint-contract -->
