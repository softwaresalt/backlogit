---
artifact_type: task
created_at: 2026-09-17T02:01:13.0636646Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 170.001-T
id: 170.012-T
parent_id: 170-F
priority: high
references:
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
status: queued
title: Authorization-token parser
updated_at: 2026-09-17T03:13:26.8253099Z
---

Authorization-token parser (behavior) over the 170.001-T declared shape. Reject malformed token input and duplicate-member JSON via a raw json.Decoder token scan (mirroring the #423 validator, before canonicalization). Duplicate-member rejection MUST reject BOTH exact-byte duplicate JSON object members AND decoder-equivalent duplicate members (keys colliding case-insensitively / after Unicode-escape decoding onto the same target field, matching Go encoding/json case-insensitive field matching) at EVERY object nesting depth (plan-review cycle-1 item 13). Domain: core (internal/core). TDD: RED behavior harness first. Depends on 170.001-T. Executable scenarios (3): (a) a well-formed token parses into the declared struct; (b) malformed token input is rejected with a typed error; (c) exact and case-fold/decoder-equivalent duplicate members at top level and nested depth are each rejected (negative tests at multiple depths). Build/test: go build ./cmd/backlogit; go test ./internal/core/ -run TestAuthorizationTokenParser. AC: (1) RED test first; (2) a well-formed token parses; (3) malformed token input is rejected; (4) exact and decoder-equivalent case-fold duplicate members are rejected at every object depth, proven by negative tests at nested depth; (5) tests GREEN.