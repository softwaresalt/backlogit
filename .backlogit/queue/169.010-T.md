---
artifact_type: task
created_at: 2026-09-17T02:00:38.2646812Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 169.001-T
id: 169.010-T
parent_id: 169-F
priority: high
references:
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
status: queued
title: Attestation envelope parser (reject malformed/duplicate-member JSON)
updated_at: 2026-09-17T03:12:17.4084482Z
---

Attestation envelope parser (behavior). Implement the envelope parser rejecting malformed JSON and duplicate-member JSON via a raw json.Decoder token scan (mirroring the #423 shipment_reconcile_event validator, before canonicalization), over the 169.001-T declared shape. Duplicate-member rejection MUST reject BOTH exact-byte duplicate JSON object members AND decoder-equivalent duplicate members (keys that collide case-insensitively / after Unicode-escape decoding onto the same target field, matching Go encoding/json case-insensitive field matching) at EVERY object nesting depth, not only the top level (plan-review cycle-1 item 13). Domain: core (internal/core). TDD: RED behavior harness first. Depends on 169.001-T. Executable scenarios (3): (a) a well-formed envelope parses; (b) malformed JSON is rejected with a typed error; (c) exact-duplicate AND case-fold/decoder-equivalent duplicate members at top level and at a nested depth are each rejected (negative tests at multiple depths). Build/test: go build ./cmd/backlogit; go test ./internal/core/ -run TestAttestationEnvelopeParser. AC: (1) RED test first; (2) a well-formed envelope parses; (3) malformed JSON is rejected; (4) exact and decoder-equivalent case-fold duplicate members are rejected at every object depth, proven by negative tests at nested depth; (5) tests GREEN.