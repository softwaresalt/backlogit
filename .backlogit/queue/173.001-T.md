---
artifact_type: task
created_at: 2026-09-14T01:05:23.2654812Z
dependencies:
    - 173.006-T
id: 173.001-T
parent_id: 173-F
priority: high
references:
    - docs/exec-plans/2026-09-13-shipment-claim-scheduler-reconciliation-plan.md
status: queued
title: Persist scheduler-baseline marker on claim activation via gated setArtifactStatus seam
updated_at: 2026-09-14T01:05:23.2654812Z
---

U1 [CC0EBB59]. Persist an additive marker as a KEY inside the custom_fields map on EVERY claim-activated manifest item, written atomically within the same setArtifactStatus activation write (inside activatedIDs rollback tracking). custom_fields SEMANTICS (explicit): `omitempty` applies to the custom_fields MAP FIELD as a whole (a nil or empty map is omitted from frontmatter); individual map KEYS do NOT have omitempty semantics. Activation ADDS the marker key (lazily initializing a nil map to a one-entry map). Marker removal (U3) DELETES the key and, when that empties the map, MUST restore the map to nil so the field serializes away again (no lingering empty `custom_fields: {}`). Seam = a gated off-by-default option threaded into setArtifactStatus so ONLY claim-driven activation writes the marker; other callers stay byte-identical. MARKER CONTRACT (PINNED): the marker is the single custom_fields key `scheduler_baseline_claim` with value = activating shipment ID (non-empty string); presence => claim-activated. This exact key is the stable cross-workspace contract (used by 173.002/173.006/173.007/173.005). PARENT-CASCADE SAFETY: setArtifactStatus cascades to parent via cascadePersistedParentStatuses; write the marker ONLY on activated manifest items and do NOT let the cascade propagate it onto parents. Domain: code (shipment_lifecycle.go + seam). Depends on U0a (173.006-T, RED). AC: marker key `scheduler_baseline_claim` set exactly on StatusQueued->StatusActive claim transitions; nil map lazily initialized on first marker; non-claim setArtifactStatus callers byte-identical; parents do not receive the marker via cascade; ignoring consumers unaffected.