---
artifact_type: task
created_at: 2026-09-18T00:00:00.6968916Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.010-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U10 - errcheck remediation: internal/core'
updated_at: 2026-09-18T05:06:18.8651183Z
---

Remediate ALL golangci-lint errcheck findings in internal/core using the per-category strategy (named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation). No opportunistic cross-package helper. internal/core is the largest residual error-returning surface outside the U4-U9 packages, so it is assigned its own bounded unit.

Immutable pre-handoff scope — NO Ship-time item creation (resolves the P1-1 finding): the OWNERSHIP of this unit is CLOSED and FROZEN at staging: internal/core top-level (NOT sub-packages internal/core/gate or internal/core/templates, which belong to U13's closed residual set). This unit creates NO subtasks and NO planning units at execution time, and it authorizes Ship to create NONE — every prior execution-time subtask-subdivision clause has been REMOVED from this unit. Ship executes the remediation as a SINGLE width-isolated mechanical pass within this frozen scope; it may not add backlog items after the shipment/DAG freeze.
Bound justification (evidence-backed, honest under the Stage role boundary): stored 149-S lint evidence records 50 errcheck findings REPOSITORY-WIDE across all owning units; internal/core's residual share is therefore a small, uniform, mechanical error-capture sweep well under the 2-hour rule. The <3-implementation-file heuristic is a proxy for the 2-hour rule; for this specific mechanical-remediation class the 2-hour rule is satisfied DIRECTLY by the small aggregate finding count, so the file-count proxy is explicitly waived for this unit with this recorded justification (see the plan "errcheck decomposition" record). Exact per-file counts are execution-verified by Ship because Stage's role boundary forbids running linters; that verification never creates a new planning unit.

Owned harness file (resolves the P1-2 harness-ownership finding): this unit exclusively owns and authorizes creation of the deterministic errcheck-regression harness `internal/core/errcheck_convergence_175_test.go`. That filename segment is RESERVED to this unit in internal/core; no other unit may create that exact path, and it never collides with U11's `internal/core/staticcheck_convergence_175_test.go`. This `*_test.go` harness file is EXCLUDED from the implementation-file inventory.

Acceptance criteria:
- Scope (package/files): internal/core top-level only (NOT internal/core/gate or internal/core/templates, owned by U13); edit only errcheck-flagged files within internal/core (file-partition invariant). EXCLUDE every file listed in U11's owned handoff artifact `docs/closure/175-U11-overlap-inventory.md` (the fenced OVERLAP-INVENTORY payload); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and consumes that exact artifact BY PATH. If absent at execution, treat as a blocking U11-incomplete dependency violation. Ownership is decided by U11's up-front inventory, never by serialization order.
- No deferred planning (required): this unit creates NO subtasks/planning units and authorizes Ship to create NONE; the scope is frozen at staging; there is NO "subdivide at execution" clause.
- Owned harness (required): the reserved `internal/core/errcheck_convergence_175_test.go` RED-first regression drives any behavior-changing errcheck fix green; excluded from the implementation-file count.
- Verification (owned scope): `golangci-lint run ./internal/core` reports 0 errcheck; `gofmt -l` on touched files empty; `go build ./internal/core` and existing internal/core tests still pass. Repository-wide zero-errcheck is asserted by terminal U12, not here.
- Existing test expectations: all current internal/core tests pass unchanged; success-path behavior preserved.
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. The harness-architect authors a failing (RED) harness/test in the owned harness file that compiles before the fix and fails on an assertion (P-002/P-004), and implementation drives it green. NEW reachable failure-path behavior requires a failing (RED) test FIRST. There is NO mechanical/generic/closure-time harness exemption, exempt contract, or Principle II deviation for this task.