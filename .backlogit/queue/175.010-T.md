---
artifact_type: task
created_at: 2026-09-18T00:00:00.6968916Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.010-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U10 - errcheck remediation: internal/core'
updated_at: 2026-09-18T00:57:30.5098151Z
---

Remediate ALL golangci-lint errcheck findings in internal/core using the per-category strategy (named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation). No opportunistic cross-package helper. internal/core is the largest residual error-returning surface outside the U4-U9 packages, so it is assigned its own bounded unit rather than left to an open-ended execution-time sweep.

Acceptance criteria:
- Scope (package/files): internal/core only (NOT sub-packages internal/core/gate or internal/core/templates, which belong to U13's closed residual set); edit only errcheck-flagged files within internal/core. Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file in U11's recorded staticcheck-owned set — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and reads U11's recorded owned-file set to exclude it. Ownership is decided by U11's up-front inventory, never by serialization order.
- Verification (zero-finding): `golangci-lint run ./internal/core` reports 0 errcheck; `gofmt -l` on touched files empty; `go build ./internal/core` and existing internal/core tests still pass.
- Existing test expectations: all current internal/core tests pass unchanged; success-path behavior preserved.
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. The harness-architect authors a failing (RED) harness/test that compiles before the fix and fails on an assertion (P-002/P-004), and implementation drives it green. NEW reachable failure-path behavior requires a failing (RED) test FIRST. There is NO mechanical/generic/closure-time harness exemption, exempt contract, or Principle II deviation for this task.
- Granularity / no deferred planning: if internal/core's errcheck surface exceeds the 2-hour/<3-file bound at execution, subdivide into MECHANICAL execution subtasks under this task (e.g., 175.010.001-ST) split by file — execution decomposition of an already-owned unit, NOT a new planning unit. Ship creates NO new planning units.