---
artifact_type: task
created_at: 2026-09-06T02:39:30.0081254Z
dependencies:
    - 167.002-T
    - 167.003-T
id: 167.010-T
parent_id: 167-F
priority: critical
status: queued
title: Shared total state classifier (pure, read-only)
updated_at: 2026-09-06T03:18:18.1193984Z
---

Shared TOTAL state classifier (pure, read-only) — extracted so both U2 (167.001-T conflict re-confirm) and the U1 transaction (167.008-T routing) consume ONE classifier and no dependency cycle exists. TDD: RED table-driven classifier test FIRST, then implement. classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, incomingCompleteEventDigest) -> ordered TOTAL mutually-exclusive branch. The classifier receives the request idempotency key AND the incoming complete-event digest as PLAIN INPUTS (both are MATERIALIZED by the transaction BEFORE calling the classifier - see 167.008: the incoming digest is computed under lock from U2 evidence binding = closure content-hash + merge + manifest-digest); the classifier itself performs NO evidence I/O and NO write and NO lock (callers hold the lock), so it takes no dependency on 167.001 (no cycle). Branches: 0 unreadable/path-unsafe=indeterminate; 0b malformed JSONL line=indeterminate; 1a-c reconcile-event-present (1b no_op requires FULLY-VALID single event + stored frontmatter key == reqIdempotencyKey + persisted event digest == incomingCompleteEventDigest; 1c conflict incl same-key/different-digest); 2a-d reconcile-absent (2b resume-append-only requires stored key == reqIdempotencyKey + persisted prepared-event present+parseable+digest-matching; 2c/2d conflict/normal-repair-2d). Uses the shared FULL-EVENT VALIDATOR + event constant from 167.002 (U3) and the request/result structs from 167.003; the shared validator rejects duplicate object members via a RAW TOKEN-LEVEL scan before canonicalization (see 167.002); composes the reused shippedEventPresence path-safety/readability contract WITHOUT broadening it (144-F guard 2 stays strict). Every (log-readability x frontmatter-state x key x digest x event-presence/validity) input lands in exactly one branch (table-driven totality/exclusivity test required). Files internal/core/shipment_reconcile_classifier.go + test. Domain: code. Deps 167.002 (event const/validator) + 167.003 (structs). See plan U1 state-classifier section + Dependency Graph.