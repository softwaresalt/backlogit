---
artifact_type: task
commit: 1c5dd79a
created_at: 2026-08-25T03:46:00Z
dependencies:
    - 147.001-T
    - 147.016-T
id: 147.029-T
labels:
    - checkpoint
    - mcp
    - plan-u7e
    - harness-ready
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: done
title: 'U7e: domainError maps ErrCheckpointCannotResolveAbandoned to validation_failed'
$12026-08-28T00:16:22.5648913Z
---

Plan unit **U7e** (partition 4). One `domainError` row: `ErrCheckpointCannotResolveAbandoned` -> `validation_failed`.

Files: `internal/mcp/errors.go`, `internal/mcp/checkpoint_disposition_test.go`.

**Sole ownership (cycle-17 restatement).** This task owns **one** `domainError` row and nothing else. It does **not** own, retain, or reinstate any `ErrCheckpointUseQuarantine` or `ErrCheckpointNonConforming` mapping. Cycle 16 **deleted** both as unreachable - they were never "moved here" - and cycle 17 removed every remaining sentence in the plan that described them as relocated or as a "safety net". No unit in this plan may add either row.

Reachability audit that produced the deletion:

* `ErrCheckpointUseQuarantine` - **unreachable.** The only checkpoint handlers routing through `domainError` are `handleGetCheckpoint` (`internal/mcp/tools.go:1205`) and `handleResolveCheckpoint` (`:1224`). Get never produces it, and after 147.025-T / U7d resolve reroutes every `QuarantineIsRemedy` match to `checkpointDispositionError`.
* `ErrCheckpointNonConforming` - **unreachable.** Same two handlers; get returns a result carrying `conforming: false` rather than this error.
* `ErrCheckpointCannotResolveAbandoned` - **reachable.** `QuarantineIsRemedy` does not match it, so it still falls through to `domainError` and still surfaces as `default: InternalError` - a 500 on a shipped agent-facing path.

Add the one explicit case and its row in the mapping-table doc comment (`internal/mcp/errors.go:127-144`). Placement is unconstrained: the sentinel is a bare `errors.New` value that wraps nothing, so it can collide with no other case. Group it with the other checkpoint rows for readability.

The MCP handler-level runtime assertion lives in 147.041-T / U10c. The CLI twin is out of scope and recorded as follow-up stash `DBBA62AA`.

Red verification: `go test -count=1 -run '^TestU7e_' ./internal/mcp`.

<!-- BEGIN:acceptance-criteria -->
- `domainError` maps `fmt.Errorf("resolve %s: %w", filename, ErrCheckpointCannotResolveAbandoned)` to `validation_failed` rather than `internal`, asserted against a realistically wrapped error and never a bare sentinel
- The mapping-table doc comment carries the corresponding row
- No `ErrCheckpointUseQuarantine` or `ErrCheckpointNonConforming` case is added to `domainError` by this task or any other
- Red harness (P-002/P-004 gate): the single scaffolded `TestU7e_` function fails - `domainError` currently falls to `default: InternalError` for this sentinel
- Green-step guards: none. Width: 2 files, 1 scenario
<!-- END:acceptance-criteria -->