---
artifact_type: shipment
created_at: 2026-09-18T00:05:17.1218206Z
custom_fields:
    items: []
dependencies:
    - 169-S
id: 156-S
priority: high
status: queued
title: Repository baseline convergence
updated_at: 2026-09-19T07:41:18.8837686Z
---

> ⛔ **SUPERSEDED — DO NOT CLAIM.** This single-shipment packaging of `175-F` was
> abandoned with PR #448 (closed without merge) and is replaced by 13 wave-aligned
> shipments `157-S`..`169-S` (RS-W00..RS-W12). Membership was cleared to `items: []`
> (historical 99-member manifest preserved in git history and on branch
> `stage/baseline-convergence-main`).
> Rationale: `docs/decisions/2026-09-19-baseline-convergence-decomposition.md`.
>
> **Disposition (governed, not a hard tool guard).** The `156-S depends_on 169-S`
> edge is an ADVISORY ordering signal only — it is **not** a hard non-claim guard.
> backlogit applies dependencies only while filtering queue results
> (`internal/core/queue.go` `filterByResolvedDependencies`), and direct
> `core.ClaimShipment` (`internal/core/shipment_lifecycle.go`) transitions a queued
> shipment `queued -> active` with **no** dependency check; queue filtering also
> treats any no-longer-blocking terminal status of `169-S` (the 6-status cascade
> done/accepted/archived/shipped/abandoned/rejected) as satisfying the edge. This
> shipment is therefore held non-claimable by GOVERNANCE, not by the edge: (1) this
> DO-NOT-CLAIM supersession record, which the Orchestrator's claim-routing MUST
> honor and never claim `156-S`; and (2) the empty manifest, so a claim would
> activate no work. A true tool-level non-claimable status (a governed
> `queued -> abandoned/archived` disposition reachable without activation) is not
> available in Stage scope today; that core enforcement — plus claim-time dependency
> validation and shipped-only readiness — is captured as explicit prerequisite work
> in stash `6434A4D7` (deferred, planning-only; no Go changes in this PR).

# Repository baseline convergence

`156-S` is **superseded** and is retained in the queue solely as immutable
historical provenance. Its current manifest is **empty** (`items: []`), it carries
no current members and makes no current delivery, and it **must never be claimed**
(the DO-NOT-CLAIM banner above records the governed disposition). This is its one
and only current state; every packaging statement that follows is past-tense
Git-history provenance, not a current claim.

**Historical packaging (Git-history provenance; past tense).** Before
supersession, `156-S` was the abandoned single-shipment packaging of the
baseline-convergence release unit (feature `175-F`): a covering release unit that
was intended to turn all four mandatory quality gates green by fixing the shared
line-ending root cause and converging the complete supported-platform
golangci-lint v2.13.2 baseline. Its historical manifest held 99 members — feature
`175-F` plus its 98 executable tasks (`175.001-T` through `175.098-T`); that
99-member manifest was cleared to `items: []` and now survives only in Git history
and on branch `stage/baseline-convergence-main`. The historical executable
dependency DAG carried 98 task members, 184 acyclic `blocks` edges, and 13 bounded
waves, with `175.001-T` (U1, baseline-control) as the historical unique source and
`175.012-T` (U12, terminal-convergence) as the historical unique terminal sink; the
Linux-only tasks `175.095-T` through `175.098-T` historically remediated the 8
Linux-only findings and gated terminal convergence alongside the Windows-primary
frontier. The runner-bootstrap prerequisite tasks (`175.099-T`, `175.100-T`,
`175.101-T`, shipment `176-S`) were never part of this historical manifest; they
were introduced later under the accepted decomposition. The current active
packaging supersedes all of the above — see the banner above and `175-F`.

**Historical relationships (past tense).** `156-S` historically unblocked shipment
`149-S` and historically sourced deferred-scope-expansion stash entries `92F79833`
and `4DB1DFF1`. Under the accepted decomposition, `149-S` was rewired to depend on
the terminal replacement shipment `169-S` (RS-W12, carrying U12 `175.012-T`), not
on `156-S`; that ordering is advisory and governed by claim-routing policy (see
`149-S` and stash `6434A4D7`). The operator authorization permitting exact
unfinished-owned baseline findings to persist until terminal convergence —
originally scoped to `156-S` and extended to the replacement sequence
`157-S`..`169-S` — is recorded at
`docs/decisions/2026-09-17-baseline-convergence-authorization.md`.
