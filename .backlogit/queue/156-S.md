---
artifact_type: shipment
created_at: 2026-09-18T00:05:17.1218206Z
custom_fields:
    items: []
dependencies:
    - 169-S
id: 156-S
priority: high
status: queued
title: Repository baseline convergence
updated_at: 2026-09-19T07:41:18.8837686Z
---

> ⛔ **SUPERSEDED — DO NOT CLAIM.** This single-shipment packaging of `175-F` was
> abandoned with PR #448 (closed without merge) and is replaced by 13 wave-aligned
> shipments `157-S`..`169-S` (RS-W00..RS-W12). Membership was cleared to the
> replacement shipments (historical 99-member manifest preserved in git history and
> on branch `stage/baseline-convergence-main`). Guarded by `156-S depends_on 169-S`.
> Rationale: `docs/decisions/2026-09-19-baseline-convergence-decomposition.md`.

# Repository baseline convergence

Shipment `156-S` delivers the covering baseline-convergence release unit
(feature `175-F`) that turns all four mandatory quality gates green by fixing the
shared line-ending root cause and converging the complete supported-platform
golangci-lint v2.13.2 baseline. Membership is 99 members: feature `175-F` plus its
98 executable tasks (`175.001-T` through `175.098-T`).

The executable dependency DAG has 98 task members, 184 acyclic `blocks` edges, and
13 bounded waves, with `175.001-T` (U1, baseline-control) as the unique source and
`175.012-T` (U12, terminal-convergence) as the unique terminal sink. The Linux-only
tasks `175.095-T` through `175.098-T` remediate the 8 Linux-only findings and gate
terminal convergence alongside the Windows-primary frontier.

This shipment unblocks shipment `149-S` (which remains `queued` and depends on
`156-S`) and sources deferred-scope-expansion stash entries `92F79833` and
`4DB1DFF1`. The shipment-scoped operator authorization permitting exact
unfinished-owned baseline findings to persist until terminal convergence is
recorded at `docs/decisions/2026-09-17-baseline-convergence-authorization.md`.
