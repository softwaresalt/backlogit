---
artifact_type: task
created_at: 2026-09-18T05:51:26.3619995Z
dependencies:
    - 175.001-T
id: 175.035-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/stash/jsonl_test.go'
updated_at: 2026-09-18T05:51:26.3619995Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: errcheck
Package: internal/stash
Owned source file (exactly one): internal/stash/jsonl_test.go
Findings (1) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L124: Error return value of `f.Close` is not checked
Affected functions (1 distinct, < 5): TestMigrateStashMDToJSONL_Success
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/stash/errcheck_jsonl_test_175_test.go
Ship's harness phase creates this harness JUST BEFORE this file task (NOT precommitted at Stage). It is a source-shape or command-backed test scoped to internal/stash/jsonl_test.go ONLY: it asserts this file's errcheck / behavior contract without referencing any not-yet-existing symbol and without leaving any future-task red assertion in the full suite. It is EXCLUDED from the implementation-file inventory.
Red-before-green selector (targeted, this file only): go test ./internal/stash -run "^Test_errcheck_Conv175_jsonl_test$" -count=1 -- RED (assertion failure, not a build error) before the fix, GREEN after.

Fix strategy: Capture and handle every flagged unchecked error return in this file. A deferred Close() in a function with a (possibly named) error return becomes a checked close (for example defer func(){ err = errors.Join(err, x.Close()) }()); a fmt.Fprint*/os.Remove on a non-critical path is either propagated with fmt.Errorf("...: %w", err) or assigned to _ ONLY with an inline justification when the value is genuinely discardable. In _test.go files, check the returned error and fail the test (if err := x.Close(); err != nil { t.Fatalf(...) } or require.NoError). No cross-file or cross-package helper is introduced; the edit is confined to this one file.

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only file: verify with go test ./internal/stash -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- go test ./internal/stash -run=^$ -count=1 (test-only-safe compile of this package's tests; NOT go build on a test file) passes, and golangci-lint run ./internal/stash reports 0 errcheck findings in internal/stash/jsonl_test.go.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/stash/errcheck_jsonl_test_175_test.go was RED (assertion) before the fix via Test_errcheck_Conv175_jsonl_test and GREEN after; no future-task red remains in the full suite.
- ONLY internal/stash/jsonl_test.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption, NO Principle II deviation. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.