---
artifact_type: task
created_at: 2026-09-18T06:53:40.938767Z
dependencies:
    - 175.001-T
id: 175.040-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U40 - Line-ending guard script (scripts/check-line-endings.ps1)
updated_at: 2026-09-18T19:37:48.7945051Z
---

NORMAL harness-required line-ending GUARD-SCRIPT task, split out of the former single workflow task so both U40 and U14 (`175.014-T`) stay within the <4-scenario bound. This task exclusively OWNS the reusable guard script `scripts/check-line-endings.ps1` and its owned harness `tests/lineendings_data_guard_175_test.go`. Depends on U1 (`175.001-T`): the guard is authored after the line-ending baseline lands. It is consumed by the U14 workflow (`.github/workflows/line-endings.yml` invokes `scripts/check-line-endings.ps1`), and feeds the U12 (`175.012-T`) terminal verification transitively through U14.

STAGE-TIME CONSTRAINT: do NOT create or edit `scripts/check-line-endings.ps1` or any file during staging. This is a planning task that authorizes Ship to add the script + harness at execution time.

Owned implementation file (exactly one): `scripts/check-line-endings.ps1`.
Owned harness file (exactly one, RESERVED): `tests/lineendings_data_guard_175_test.go`.

GUARD-SCRIPT specification (`scripts/check-line-endings.ps1` — fail-closed, non-vacuous over a FIXED tracked set): the script enumerates a FIXED, committed set of tracked `*.go` and `*.json` paths via `git ls-files -- '*.go' '*.json'` (NOT an attribute-driven `git ls-files --eol` query, which would go vacuous if `.gitattributes` were deleted or narrowed). It asserts THREE properties INDEPENDENTLY and exits nonzero (fail-closed) if ANY assertion fails, echoing the offending paths:
  1. `.gitattributes` CONTRACT: the required `.gitattributes` rules are present and normalize `*.go`/`*.json` (and the golden JSON) to `eol=lf`; a missing/removed/narrowed attribute is a hard failure, so deleting attributes cannot make the guard pass.
  2. WORKING-TREE EOL: every enumerated path's on-disk bytes contain NO `\r\n` (no CRLF, no mixed).
  3. STORED/INDEX BLOB normalization: each enumerated path's index/committed blob is LF-normalized independently of `.gitattributes` (hash the LF-normalized bytes and compare to the stored blob), so an LF worktree over a CRLF-committed blob still fails.
Because the three assertions are independent, narrowing or deleting `.gitattributes` FAILS assertion 1 rather than silently disabling assertions 2 and 3. The script is designed to be invoked both locally and by the U14 CI workflow.

Test posture (Principle II / P-002.6): NORMAL harness-required. The owned harness `tests/lineendings_data_guard_175_test.go` declares a task-scoped Go test function prefixed `TestU175_040_` (deterministic unit token; e.g. `TestU175_040_DataGuard`). It builds a DISPOSABLE Git repository under `t.TempDir()` and performs ALL git operations there, making NO mutation to the caller/working-repository index. It EXTRACTS and EXECUTES the committed `scripts/check-line-endings.ps1` against the temp repo (it does not re-implement the guard), proving the SHIPPED script.
P-002.6 SCOPED VERIFICATION COMMAND (EXECUTABLE non-vacuity wrapper; explicit package, anchored, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./tests" -run "^TestU175_040_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_040_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_040_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the explicit package (`./tests`) and anchored `^TestU175_040_` selector with `-v -count=1`; captures and re-emits combined go test output; preserves/propagates the native nonzero exit; counts `^--- PASS: TestU175_040_` lines and EXITS NONZERO when that count is < 1 (zero-match/absent selector fails vacuously instead of passing).
Execution contract: run from repo root; echo full output; preserve native `go test` exit status. RED before the script exists (assertion failure), GREEN after. NON-VACUOUS: after green it MUST observe at least one `^--- PASS: TestU175_040_` line; zero matching named-PASS lines is a FAILURE even if `go test` exits 0. WAVE CONSISTENCY: this harness is scaffolded together with its admitted ready wave (per `_ship.agent.md` Step 2 and the harness-architect skill), not just before this file task; sibling task-scoped red within the SAME wave is EXPECTED and TOLERATED until wave convergence — only this task's own anchored `^TestU175_040_` selector must turn GREEN when its fix lands, and unfiltered full-suite green is required only at Ship's Step 4.6 wave-convergence gate.

SCENARIOS (exactly THREE; attribute-contract validation is part of EACH data-state setup/precondition, NOT a fourth standalone scenario), table-grouped if helpful:
  1. CRLF/mixed WORKTREE red: a tracked path checked out with CRLF (or mixed) bytes -> guard FAILS (assertion 2), with the required `.gitattributes` present as precondition.
  2. CRLF committed BLOB red: a tracked path whose index/committed blob is CRLF while the worktree may be LF -> guard FAILS (assertion 3), with the required `.gitattributes` present as precondition.
  3. Fully LF green: worktree LF + index LF + required `.gitattributes` present -> guard PASSES (all three assertions hold).

Bounds:
- Modified files <= 2 (one implementation script + one owned harness).
- Implementation files < 3 (exactly 1: `scripts/check-line-endings.ps1`; owned harness excluded).
- Test scenarios < 4 (exactly 3).
- Human effort < 2 hours.

Acceptance criteria:
- `scripts/check-line-endings.ps1` enumerates the FIXED tracked `*.go`/`*.json` set and asserts the three independent properties (`.gitattributes` contract, worktree EOL, index/blob normalization), failing closed on any violation; removing/narrowing `.gitattributes` FAILS assertion 1 (non-vacuous).
- Owned harness `tests/lineendings_data_guard_175_test.go` uses a `t.TempDir()` disposable repo, mutates no caller index, extracts+executes the committed script, and covers exactly the three scenarios (CRLF/mixed worktree red, CRLF committed blob red, LF green) with attribute-contract validation embedded in each setup.
- P-002.6 command the P-002.6 executable non-vacuity wrapper (package `./tests`, anchored `^TestU175_040_`, fails on zero matched tests) is RED before and GREEN after, fail-closed and non-vacuous (>=1 named `--- PASS: TestU175_040_` line).
- Depends on U1 (`175.001-T`); consumed by U14 (`175.014-T`).
- NORMAL harness-required: NO harness-exempt label, NO exemption contract, NO generic exemption.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -Command '$ErrorActionPreference=\"Stop\"; $psi=[System.Diagnostics.ProcessStartInfo]::new(); $psi.FileName=\"golangci-lint\"; $psi.Arguments=\"run --output.json.path stdout --show-stats=false --issues-exit-code 0 ./tests\"; $psi.RedirectStandardOutput=$true; $psi.RedirectStandardError=$true; $psi.UseShellExecute=$false; $psi.WorkingDirectory=(Get-Location).Path; try { $p=[System.Diagnostics.Process]::Start($psi) } catch { Write-Output \"LINT-NATIVE-LAUNCH-FAIL\"; exit 97 }; $so=$p.StandardOutput.ReadToEndAsync(); $se=$p.StandardError.ReadToEndAsync(); $p.WaitForExit(); $out=$so.GetAwaiter().GetResult(); $err=$se.GetAwaiter().GetResult(); $ec=$p.ExitCode; if ($ec -ne 0) { Write-Output (\"LINT-NATIVE-FAIL:\"+$ec); if ($err) { Write-Output $err }; exit $ec }; $t=$out.Trim(); if ($t.Length -eq 0) { Write-Output \"LINT-SCHEMA-FAIL:empty-stdout\"; exit 91 }; try { $j=$t | ConvertFrom-Json -ErrorAction Stop } catch { Write-Output \"LINT-SCHEMA-FAIL:malformed-json\"; exit 92 }; if ($null -eq $j) { Write-Output \"LINT-SCHEMA-FAIL:json-null\"; exit 93 }; if (-not ($j -is [System.Management.Automation.PSCustomObject])) { Write-Output \"LINT-SCHEMA-FAIL:non-object-root\"; exit 94 }; if (-not ($j.PSObject.Properties.Name -contains \"Issues\")) { Write-Output \"LINT-SCHEMA-FAIL:missing-Issues\"; exit 95 }; if (-not ($j.Issues -is [System.Array])) { Write-Output \"LINT-SCHEMA-FAIL:Issues-not-collection\"; exit 96 }; $hits=@($j.Issues | Where-Object { [string]::Equals(((\"\" + $_.Pos.Filename) -replace \"\\\\\",\"/\"), \"tests/lineendings_data_guard_175_test.go\", [System.StringComparison]::Ordinal) }); if ($hits.Count -gt 0) { Write-Output (\"OWNED-FINDING-REMAINS:\"+$hits.Count); exit 1 }; Write-Output \"FSLV-OK:tests/lineendings_data_guard_175_test.go:any-linter:0-owned-findings\"; exit 0'",
  "lint_scope": {
    "kind": "harness-file-scoped-go-lint",
    "package": "./tests",
    "linter": null,
    "owned_file": "tests/lineendings_data_guard_175_test.go"
  },
  "non_vacuity_evidence": {
    "mode": "structured-json-schema-validated",
    "success_marker": "FSLV-OK:tests/lineendings_data_guard_175_test.go:any-linter:0-owned-findings",
    "asserts": [
      "native golangci-lint launch failure preserved distinctly (exit 97)",
      "native golangci-lint nonzero exit preserved and propagated (passthrough)",
      "empty stdout is a hard failure (exit 91)",
      "malformed JSON is a hard failure (exit 92)",
      "JSON null root is a hard failure (exit 93)",
      "non-object JSON root is a hard failure (exit 94)",
      "missing Issues key is a hard failure (exit 95)",
      "non-array Issues is a hard failure (exit 96)",
      "schema validated before filtering; no unknown or vacuous success shape is accepted",
      "owned harness path matched by ordinal exact identity across all linters; exit 1 if any owned finding remains",
      "read-only and task-scoped; no machine temp file, no Remove-Item cleanup; full repository golangci-lint run deferred to wave convergence"
    ]
  }
}
```

<!-- END:task-lint-contract -->
