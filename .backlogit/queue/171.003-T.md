---
artifact_type: task
created_at: 2026-09-07T00:09:19.9499759Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 171.002-T
id: 171.003-T
parent_id: 171-F
priority: high
references:
    - docs/exec-plans/2026-09-06-checkpoint-create-schema-reject-plan.md
status: queued
title: 'CLI: add migration-only --allow-legacy-import flag to checkpoint create'
updated_at: 2026-09-07T00:09:19.9499759Z
---

U3. Add a migration-only --allow-legacy-import boolean flag to newCheckpointCreateCmd in
internal/cli/checkpoint.go; pass WithAllowLegacyImport() into core when set; keep --state-dump
required; update long-help to state default-reject behavior AND convey the same compact opt-in
scope the MCP tool description carries (accepts only in-window legacy shapes; the narrow upgrade
window; future/unsupported/wrong-typed/malformed and records needing remap/identity CANNOT be
rescued and remain rejected; import upgrades to valid V1, never verbatim) so both governed-pair
surfaces present symmetric in-surface decision context (remove the 'written verbatim'
advertisement). The CLI error-mapping layer appends the --allow-legacy-import remediation ONLY to
ErrCheckpointSchemaRejected (legacy-eligible); it MUST NOT append the flag to
ErrCheckpointSchemaUnsupported (enabling the flag cannot help).

Acceptance criteria: CLI create without flag rejects a schema-less dump (non-zero exit,
ErrCheckpointSchemaRejected preserved through %w, no file); with --allow-legacy-import an
in-window dump is imported and readable; an out-of-window legacy dump still fails
(ErrCheckpointSchemaUnsupported, no flag remediation); flag defaults false. Use real
cli.NewRootCommand() dispatch. `go test ./internal/cli/...` green.