---
artifact_type: task
created_at: 2026-09-15T22:15:00.0000000Z
dependencies:
    - 174.052-T
id: 174.053-T
parent_id: 174-F
priority: high
status: queued
title: 'R1b: Core-lifecycle BEHAVIOR RED harness'
updated_at: 2026-09-15T23:15:00.0000000Z
---

BEHAVIOR RED harness for the shared blocked-shipment lifecycle, added AFTER the declaration-only stubs (174.052-T) so it COMPILES against the declared `ShipmentBlocked`/`BlockShipment`/`UnblockShipment` symbols and fails on BEHAVIOR (not compile) until implementation lands. It exercises the EXACT declared API: `BlockShipment(ctx, ws, id, core.BlockOptions{Reason, BlockedBy, ResumeCheckpointRef})` and `UnblockShipment(ctx, ws, id, core.UnblockOptions{Target, Confirm, UnblockedBy})`, asserting the returned `(*models.Artifact, error)`. Covers the behavioral contract previously conflated into R1: transitions active->blocked and blocked->{queued,active}; governed metadata blocked_reason/blocked_at/blocked_by/resume_checkpoint_ref (sourced from `BlockOptions`); DURABLE INTENT + complete PREIMAGE snapshot persisted BEFORE any member/shipment mutation; member disposition (active/in-flight members captured in a machine-readable snapshot, returned to queued on block); target-aware unblock semantics (`UnblockOptions.Target==ShipmentQueued` leaves members queued + preserves snapshot; `Target==ShipmentActive` requires `Confirm==true` and restores the exact snapshot); single-active-shipment invariant excludes blocked. Domain: tests. AC: (1) tests COMPILE against the R1s/Rd declarations (exact signatures + option structs) and FAIL on behavior until the R5/R6 GREEN tasks land; (2) assert intent+preimage precede any mutation; (3) assert never-active-members-under-queued-shipment. <4 scenario groups. Depends Rd (174.052-T).

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Core-lifecycle BEHAVIOR RED harness; it compiles against the declaration-only stubs (Rd 174.052-T) and its transition/metadata/intent+preimage/disposition/target-aware-unblock behavioral assertions turn green only after core BlockShipment (R5 174.043-T) and UnblockShipment+Claim (R6 174.044-T) land, so its TestUR1B_ functions are behavior-red by design when it completes.
red_selector_command: go test -count=1 -run '^TestUR1B_' ./internal/core
green_maker_tasks: 174.043-T, 174.044-T
green_maker_closes_wave: 6
```
<!-- END:red-deliverable-contract -->
