---
artifact_type: task
created_at: 2026-09-14T01:00:45.2300701Z
custom_fields:
    source_stash_id: FE440C62
dependencies:
    - 172.001-T
    - 172.008-T
id: 172.002-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Reorder ArchiveItem so lock B is never held while acquiring lock C
updated_at: 2026-09-14T01:00:45.2300701Z
---

U1.2 [FE440C62]. Eliminate the B->C inversion in ArchiveItem (internal/core/archive.go). Preferred: move the best-effort item-log (C) JSONL append out of the B-held region so the two locks never overlap in B->C order, WITHOUT widening the C critical section and PRESERVING the existing non-fatal append-failure semantics (warn+skip, archive still succeeds). If C is instead acquired before B, U1.1 note must record the widened critical section + bounded-contention trade-off. Moving/reordering the C acquisition MUST PRESERVE the item's causal item-log event ordering (the observable append sequence for a given item is unchanged); this is proven by the deterministic-barrier RED harness (U1.0 172.008-T) observed failing first, then passing. Domain: code. Depends on U1.1 (172.001-T) and the RED harness U1.0 (172.008-T, written and observed failing before this change). AC: ArchiveItem no longer holds B while acquiring C; causal item-log event ordering preserved; non-fatal append semantics preserved; the 172.008-T barrier test transitions RED->GREEN.