---
artifact_type: task
created_at: 2026-08-22T01:04:15.149468Z
dependencies:
    - 146.004-T
    - 146.005-T
    - 146.008-T
    - 146.009-T
id: 146.006-T
labels:
    - stage-harvested
    - unit-U2
    - domain-code
parent_id: 146-F
priority: high
references:
    - docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md
    - docs/decisions/2026-08-21-success-shaped-evidence-loss-deliberation.md
status: queued
title: Preserve unmodeled checkpoint context keys through the create round-trip
updated_at: 2026-08-22T01:04:15.149468Z
---

Plan unit U2. Give the Extra carrier U0a declared its behavior. Add custom UnmarshalJSON (pointer receiver) routing unmodeled keys into Extra, and MarshalJSON (VALUE receiver, because Context is a value field and a pointer receiver would be silently skipped for non-addressable values) re-emitting modeled keys followed by sorted Extra keys flattened into the same context object. Replace U0a Keys() stub with Keys() ([]string, error). Both MarshalJSON and Keys() delegate to one unexported emit() (keys, body, err) so neither swallows an error. emit() MUST marshal the plainContext shadow through jsonutil.MarshalReadable and append each Extra raw message verbatim; json.Marshal MUST NOT appear in emit(), because HTML escaping must stay disabled. Modeled-key set derived by reflection via a package-level var initializer, no init() and no panic, compared case-insensitively.