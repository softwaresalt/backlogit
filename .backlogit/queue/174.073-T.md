---
artifact_type: task
created_at: 2026-09-24T18:34:47.7430646Z
id: 174.073-T
labels:
    - wave-18
    - corrective
    - test-infrastructure
    - 155-S
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - .backlogit/queue/072-DL.md
status: queued
title: Route size/complexity core_test fixtures through recovery-free seam
updated_at: 2026-09-24T18:36:09.9736204Z
---

Corrective work unit (Wave 18 / U18). This is a test-only Go change. It routes the fixture named by DEFERRED SCOPE EXPANSION 62C6A469 (setupSizeWorkspace, internal/core/artifact_size_test.go) and its construction-identical sibling (setupComplexityWorkspace, internal/core/artifact_complexity_test.go) through the EXISTING recovery-free seam core.NewWorkspaceWithoutRecoveryForTest, so these 13 recovery-agnostic tests no longer pay unrelated shipment-recovery startup cost. Template loading is retained. Root cause (evidence-bounded, 072-DL): three full-suite captures failed ONLY on the internal/core cumulative 10m package deadline, at about 59/69/79% of the 881-test run order. Each time the running test was 0-1s old and sat in a different normal I/O stack (the post-wave17 stack was setupSizeWorkspace -> NewWorkspace -> config.LoadTemplates -> os.ReadFile). No stack showed a per-test hang or lock wait. HONEST SUFFICIENCY: this task alone is NOT expected to clear the package deadline. The residual is DEFERRED SCOPE EXPANSION 11BE840F, and the slog capture leak is D8EF5443. Both are out of scope (P-021 C4). Authoritative contract: Wave 18 section of docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md (plan review rev20 attempt 2: PASS). Source stash 62C6A469 (SPLIT disposition); deliberation 072-DL.



<!-- BEGIN:acceptance-criteria -->
AC1 (Implementation boundary, exactly 2 files / 4 functions, all package core_test): (1) NEW helper newRecoveryFreeFixtureWorkspace(ctx context.Context, root string) (*core.Workspace, error) in internal/core/artifact_size_test.go. It ONLY calls core.NewWorkspaceWithoutRecoveryForTest(ctx, root) and wraps any error with fmt.Errorf('new recovery-free fixture workspace: %w', err); it does NOT call config.WriteDefaults. (2) setupSizeWorkspace and (3) setupComplexityWorkspace replace ONLY their core.NewWorkspace call with the helper. TempDir, queue MkdirAll, config.WriteDefaults, require.NoError, t.Cleanup(ws.Close), golden file and db.UpsertItem seed stay unchanged. (4) NEW harness TestRecoveryFreeFixture_SkipsShipmentRecoveryButLoadsTemplates (no t.Parallel, no slog/env mutation). NO change to internal/core/workspace_test_seam_test.go, to any non-_test.go file, or to NewWorkspace/newWorkspace/NewDiagnosticWorkspace, recovery, locking, the template loader, timeouts, partitioning, CI or Makefile.
AC2 (RED harness evidence; harness-architect owns this phase): (a) Characterization extract: add the helper TEMPORARILY calling core.NewWorkspace (a transition-only state) and reroute both fixtures. Selector S2 must be GREEN with exactly 13 top-level PASS. (b) Add the harness. Arrange: t.TempDir root with .backlogit/queue plus config.WriteDefaults; require.NoFileExists and NoDirExists on <root>/.backlogit/ops; then write <root>/.backlogit/ops as a REGULAR FILE (confirm against shipmentOpsRootForWorkspace in internal/core/shipment_ops.go). Use distinct recoveryCtx/cancelRecovery and isolatedCtx/cancelIsolated (context.WithTimeout 2s each, defer cancel) and distinct recoveryWS/recoveryErr and isolatedWS/isolatedErr. Negative control: recoveryWS, recoveryErr := core.NewWorkspace(recoveryCtx, root); if recoveryWS != nil register a Close cleanup; require.Error, require.ErrorContains(recoveryErr, 'recover shipment operations'), require.ErrorIs(recoveryErr, blerrors.ErrValidation), require.Nil(recoveryWS). Positive: isolatedWS, isolatedErr := newRecoveryFreeFixtureWorkspace(isolatedCtx, root); require.NoError; t.Cleanup(func(){ require.NoError(t, isolatedWS.Close()) }); require.NotEmpty(isolatedWS.Templates). (c) Compile check: go test -run '^$' -count=1 ./internal/core must exit 0. S1 must FAIL on the positive assertion with the recovery error. Capture this to logs/diagnostics/174073-red-harness.txt plus .metadata.json. Record Compilation: PASS / Red Phase: CONFIRMED (scoped), then apply harness-ready. If RED cannot be reproduced, or the extract makes S2 non-GREEN, STOP and return to Stage; do not weaken the fingerprint.
AC3 (GREEN; build-feature owns this phase): change ONLY the helper body to call core.NewWorkspaceWithoutRecoveryForTest. Targeted selectors (single-quoted, PowerShell-safe; count only unindented top-level '--- PASS: Test' lines; any SKIP/FAIL or unresolved name = FAIL):
  S1: go test ./internal/core -run '^TestRecoveryFreeFixture_SkipsShipmentRecoveryButLoadsTemplates$' -count=1 -timeout=2m -v -> exactly 1 PASS.
  S2: go test ./internal/core -run '^(TestSetArtifactSize_PersistsAndPreservesIndexColumns|TestSetArtifactSize_RejectsInvalidValueBeforeWrite|TestSetArtifactSize_GoldenBodyPreserved|TestSetArtifactSize_Idempotent|TestSetArtifactSize_BusyLockReturnsErrTaskBusy|TestSetArtifactComplexity_PersistsAndPreservesBody|TestSetArtifactComplexity_RejectsInvalidValueBeforeWrite|TestSetArtifactComplexity_EmptyClearsField|TestSetArtifactComplexity_EmptyRejectsNonTask|TestSetArtifactComplexity_RejectsNonTaskEvenWithCustomSchema|TestSetArtifactComplexity_EmptyRequiresComplexitySchema|TestSetArtifactComplexity_GenericUpdatePreservesComplexity|TestSetArtifactComplexity_GenericCreateRejectsComplexity)$' -count=1 -timeout=2m -v -> exactly 13 PASS. It is exact-name anchored because the prefix also matches the out-of-scope TestSetArtifactSize_PreservesTopLevelDocline. BusyLock must still assert core.ErrTaskBusy.
  Capture GREEN output under logs/diagnostics/174073-*.
AC4 (Negative controls: recovery and template behavior stay tested where intended):
  S3: go test ./internal/core -run '^(TestNewWorkspace_RecoversPendingReturnBlockedJournal|TestNewWorkspace_RemovesWriterTempResidueAndRecoversValidJournal|TestNewWorkspaceWithoutRecoveryForTest_IsolatesRecoveryState)$' -count=1 -timeout=2m -v -> exactly 3 PASS. Those tests keep explicit core.NewWorkspace or seam coverage.
  The harness negative control itself proves recovery still engages on a seeded root, and require.NotEmpty(Templates) proves template loading is retained.
  Optional non-gating sanity: go test ./internal/config -run '^(TestLoadTemplates_|TestWriteDefaults_)' -count=1 -v.
AC5 (Pinned lint/build/format gates, Go 1.24.0; START_SHA = the literal output of git rev-parse HEAD recorded before the first edit, never an angle-bracket placeholder):
  go vet ./...
  golangci-lint v1.64.8: go run github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.8 run --timeout=5m --new-from-rev=START_SHA ./internal/core/... -> 0 new findings in the two target files.
  go build ./cmd/backlogit
  gofmt -l 'internal/core/artifact_size_test.go' 'internal/core/artifact_complexity_test.go' -> empty output.
  git diff --check START_SHA HEAD -- 'internal/core/artifact_size_test.go' 'internal/core/artifact_complexity_test.go' -> clean.
  On a gate failure, stop without retrying under a different command form and record the evidence.
AC6 (Scope and reroute inventory on the task commit):
  git diff --name-only START_SHA HEAD -- internal cmd lists EXACTLY the two target files. Only .backlogit/** metadata for this task, logs/diagnostics/174073-* and docs/memory/** may change outside that filter. Unrelated dirty files must be neither staged nor committed.
  Select-String -CaseSensitive -SimpleMatch over the two files: 'core.NewWorkspace(' gives EXACTLY 1 match (the harness negative control), and 'newRecoveryFreeFixtureWorkspace(' gives EXACTLY 4 (definition + 3 calls). Counts include comments.
AC7 (Circuit / stop for authorization): do NOT run go test ./... (including -run '^$' ./... forms) or an unselected go test ./internal/core in this task. After the task commit, STOP. Any next go test ./... needs a NEW, separate, explicit operator authorization immediately before the run. That authorization request MUST cite a recorded operator disposition of 11BE840F, or an explicit known-risk waiver. The recommended run form is go test -json ./... captured to logs/diagnostics/. The 155-S zero-P0/P1 stop-gate and final standard + adversarial review then apply.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Signatures: setupSizeWorkspace @ internal/core/artifact_size_test.go:46 (core.NewWorkspace at :54). setupComplexityWorkspace @ internal/core/artifact_complexity_test.go:37 (core.NewWorkspace at :45). Seam: NewWorkspaceWithoutRecoveryForTest @ internal/core/workspace_test_seam_test.go -> newWorkspace(ctx, root, false). newWorkspace loads templates (workspace.go ~171) BEFORE the autoRecover-gated recoverPendingShipmentOperations (workspace.go ~283-286), which wraps 'recover shipment operations: %w'. A non-directory ops root is rejected with ErrValidation (shipment_ops.go ~87-105). Harness naming precedent: workspace_no_recovery_regression_test.go:17-42. Negative-control locations: shipment_test.go:1244, shipment_p1_lifecycle_remediation_test.go:61, workspace_no_recovery_regression_test.go:17. Safety modes: investigate-first + freeze-scope. Posture: characterization-first (extract), then test-first (harness RED), then GREEN. Membership: governed member of active 155-S; no blocks edge (membership-as-gate). Provenance: 174.071-T (seam author) is informational. Domain: Go test infrastructure. Constitution: documented deviations (scoped lint/format; P-004 compile check narrowed to ./internal/core) per the plan's Wave 18 Constitution Check. Out of scope (P-021 C4): 11BE840F (package runtime budget: timeout/partition/consolidation/caching), D8EF5443 (slog/log capture restore leak), and any other core_test fixture including docline_codec_roundtrip_test.go.
<!-- END:implementation-notes -->