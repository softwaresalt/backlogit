---
artifact_type: task
created_at: 2026-09-03T08:49:44.0749741Z
id: 156.004-T
parent_id: 156-F
priority: high
status: queued
title: Fault-line evidence contract DECLARATIONS + shape harness (U4a-decl)
updated_at: 2026-09-05T05:21:59.1187195Z
---

U4a-declarations. NEW standalone leaf internal/faultline (only intentional internal deps: internal/canonical + optionally internal/errors; no detector/harness/core/events). Declare SHARED ENVELOPE EvidenceArtifact{SchemaVersion int `json:"schema_version"`,ProducingTask `json:"producing_task"`,ProducingCommit `json:"producing_commit"`,NodeFamily `json:"node_family"`,Applicability []string `json:"applicability"`,ValidatorIdentity `json:"validator_identity"`,VerifiedEvidence json.RawMessage `json:"verified_evidence"`,Status `json:"status"`} (all outer tags explicit snake_case; VerifiedEvidence is a DISCRIMINATED node-family payload, NOT one parity struct for all producers). FamilyPayload interface{CanonicalMap() map[string]any; Validate() error} + VERSION-AWARE family REGISTRY keyed by composite (SchemaVersion,NodeFamily): map[int]map[string]func()FamilyPayload + RegisterFamily(version,name,factory)/knownFamilies(version) so decode/Validate select allowed family set + concrete payload shape from the artifact SchemaVersion (flat versionless map cannot; {N,N-1} needs per-version shapes). TYPED ERROR CATEGORIES from DecodeAndValidate: ErrMalformed (duplicate key/case-variant/DisallowUnknownFields/trailing-data), ErrUnknownVersion, ErrUnknownFamily, ErrValidation (known-version/family but invariant fails) so Classify distinguishes OutcomeUnknownOrMalformed vs OutcomeValidationFailed via errors.Is. v1 ONLY family: exported const NodeFamilyParity="parity" (S4) with ParityEvidence{ScenarioID string `json:"scenario_id"`; Dimension string `json:"dimension"`; Surfaces []string `json:"surfaces"`; DivergentFields []string `json:"divergent_fields"`; ExpectedDivergence bool `json:"expected_divergence"`; TrackedDefect string `json:"tracked_defect"`; Detail string `json:"detail"`} (snake_case; Surfaces/DivergentFields non-nil empty; TrackedDefect empty only when ExpectedDivergence=false). Later producers register own typed payload+golden (RepresentationEvidence,RedGreenEvidence,...) - no Detail overloading. EXACT API signatures: func (a EvidenceArtifact) Canonical() ([]byte,error); func DecodeAndValidate(data []byte) (EvidenceArtifact,FamilyPayload,error); func Validate(a EvidenceArtifact, p FamilyPayload) error. Validate INVARIANTS split by OWNER (absent JSON decodes to zero even under DisallowUnknownFields, reject zero-valued-but-shape-valid): p.Validate() (family, INTRA-payload, NO Status): ParityEvidence ScenarioID/Dimension non-empty; Surfaces non-empty each in {cli,mcp,internal}; ExpectedDivergence=>TrackedDefect non-empty concrete ID. Validate(a,p) (envelope, ONLY place Status-referencing rules live): SchemaVersion in knownVersions; ProducingTask/ProducingCommit/ValidatorIdentity non-empty; NodeFamily in knownFamilies(SchemaVersion); Applicability non-empty each in {cli,mcp,internal}; Status in {pass,report_only,fail}; then p.Validate(); then CROSS-FIELD STATE MATRIX: (a) ExpectedDivergence=true=>Status=report_only; (b) Status=pass=>DivergentFields empty AND ExpectedDivergence=false; (c) Status=report_only=>ExpectedDivergence=true AND TrackedDefect non-empty AND DivergentFields non-empty; (d) Status=fail=>DivergentFields non-empty AND ExpectedDivergence=false. EvidenceSchemaVersion const + knownVersions. BOUNDED DECODE consts (SEC-01): MaxArtifactBytes/MaxJSONDepth + object-member/array/string budgets declared; exceed=>ErrMalformed(resource-limit). REGISTRY LIFECYCLE (SEC-02): RegisterFamily init-only returns error on duplicate (version,name)/nil factory (no last-writer-wins); freeze() immutable snapshot before first decode; decode/Canonical read frozen snapshot only. Source-shape (declaration) harness FIRST per harness-architect asserts only the DECLARATION shape (types/fields/JSON tags/API signatures exist, via AST/compile) - it does NOT assert a serialization golden (that requires behavior; the checked-in raw-bytes per-family golden lives in 156.006-T). State-matrix + typed-error conformance also in 156.006-T. Behavior in 156.006-T. Acceptance: declarations+registry+signatures+red declaration-shape harness (no serialization golden here). Files: internal/faultline/evidence.go. Domain: code.