---
artifact_type: task
created_at: 2026-09-03T08:49:44.0749741Z
id: 156.004-T
parent_id: 156-F
priority: high
status: queued
title: Fault-line evidence contract DECLARATIONS + shape harness (U4a-decl)
updated_at: 2026-09-05T05:56:24.0805304Z
---

U4a-declarations. NEW standalone leaf internal/faultline (only intentional internal deps: internal/canonical + optionally internal/errors; no detector/harness/core/events). Declare SHARED ENVELOPE EvidenceArtifact{SchemaVersion int `json:"schema_version"`,ProducingTask `json:"producing_task"`,ProducingCommit `json:"producing_commit"`,NodeFamily `json:"node_family"`,Applicability []string `json:"applicability"`,ValidatorIdentity `json:"validator_identity"`,VerifiedEvidence json.RawMessage `json:"verified_evidence"`,Status `json:"status"`} (all outer tags explicit snake_case; VerifiedEvidence is a DISCRIMINATED node-family payload, NOT one parity struct for all producers). FamilyPayload interface{CanonicalMap() map[string]any; Validate(view EvidenceView) error} where EvidenceView is a FOUNDATION-OWNED read-only view of envelope Status/Applicability/SchemaVersion/NodeFamily (so each family enforces its OWN cross-envelope invariants without foundation importing producer pkgs / reversing deps). VERSION-AWARE family REGISTRY keyed by composite (SchemaVersion,NodeFamily): map[int]map[string]func()FamilyPayload. VERSION UNIQUELY IDENTIFIES FAMILY SET: each EvidenceSchemaVersion has a COMPLETE CLOSED family set defined by a FOUNDATION-OWNED per-version MANIFEST (static map[int][]string in internal/faultline, binary-INDEPENDENT - not derived from linked packages); knownFamilies(version)=manifest set; freeze() validates registered factories match the manifest EXACTLY per version (missing factory OR extra/unmanifested registration => fail closed), so two binaries cannot freeze different sets for a version. Adding/removing a family REQUIRES a version bump (v1 set = {parity} forever, foundation-owned so always complete); later producer targets a NEW version superset, never mutates v1. TYPED ERROR CATEGORIES from DecodeAndValidate: ErrMalformed (duplicate key/case-variant/DisallowUnknownFields/trailing-data/bounded-decode), ErrUnknownVersion, ErrUnknownFamily, ErrValidation (known-version/family but invariant fails) so Classify distinguishes OutcomeUnknownOrMalformed vs OutcomeValidationFailed via errors.Is. v1 ONLY family: exported const NodeFamilyParity="parity" (S4) with ParityEvidence{ScenarioID string `json:"scenario_id"`; Dimension string `json:"dimension"`; Surfaces []string `json:"surfaces"`; DivergentFields []string `json:"divergent_fields"`; ExpectedDivergence bool `json:"expected_divergence"`; TrackedDefect string `json:"tracked_defect"`; Detail string `json:"detail"`} (snake_case; Surfaces/DivergentFields non-nil empty; TrackedDefect empty only when ExpectedDivergence=false, and when non-empty MUST match canonical backlog-ID regex ^\d{3}(\.\d{3})*-[A-Z]{1,2}$ - free-form rejected, closing the 166-F allowlist gap). Family payload TYPES + their RegisterFamily calls live in the SHARED foundation pkg internal/faultline (or always-linked internal/faultline/families) - NOT a producer's separately-linked pkg - so any consumer importing the foundation links the complete manifest set (no import-every-producer gap). Later producers register own typed payload+golden (RepresentationEvidence,RedGreenEvidence,...) at a NEW version - no Detail overloading. EXACT API signatures: func (a EvidenceArtifact) Canonical() ([]byte,error); func DecodeAndValidate(data []byte) (EvidenceArtifact,FamilyPayload,error) (envelope+payload BOTH from same bytes); func Validate(a EvidenceArtifact) error (DERIVES payload from a.VerifiedEvidence - no separate-payload mismatch vector); func RegisterFamily(version int,name string,factory func()FamilyPayload) error; func knownFamilies(version int) []string. Validate INVARIANTS: ENVELOPE (Validate(a)): SchemaVersion in knownVersions; ProducingTask/ProducingCommit/ValidatorIdentity non-empty; NodeFamily in knownFamilies(SchemaVersion); Applicability non-empty each in {cli,mcp,internal}; Status in {pass,report_only,fail}. FAMILY envelope-aware (p.Validate(view)): ParityEvidence ScenarioID/Dimension non-empty; Surfaces non-empty each in {cli,mcp,internal}; PLUS CROSS-FIELD STATE MATRIX vs view.Status: (a) ExpectedDivergence=>Status=report_only; (b) Status=pass=>DivergentFields empty AND ExpectedDivergence=false; (c) Status=report_only=>ExpectedDivergence AND TrackedDefect AND DivergentFields; (d) Status=fail=>DivergentFields non-empty AND ExpectedDivergence=false. Validate(a) derives p from a.VerifiedEvidence then calls p.Validate(view). EvidenceSchemaVersion const + knownVersions. BOUNDED DECODE consts (SEC-01): MaxArtifactBytes/MaxJSONDepth + object-member/array/string budgets declared; exceed=>ErrMalformed(resource-limit). REGISTRY LIFECYCLE (SEC-02+freeze completeness): RegisterFamily init-only returns error on duplicate (version,name)/nil/post-freeze (no last-writer-wins); freeze() triggered by first of DecodeAndValidate/Canonical/Validate/knownFamilies; ALL readers (incl knownFamilies+Validate) read frozen snapshot only (no register-vs-read race). Source-shape (declaration) harness FIRST per harness-architect asserts only the DECLARATION shape (types/fields/JSON tags/API signatures exist, via AST/compile) - NOT a serialization golden (behavior; per-family golden in 156.006-T). State-matrix + typed-error + freeze conformance in 156.006-T. Behavior in 156.006-T. Acceptance: declarations+registry+signatures+red declaration-shape harness (no serialization golden here). Files: internal/faultline/evidence.go. Domain: code.