---
artifact_type: task
created_at: 2026-09-03T08:49:44.0749741Z
id: 156.004-T
parent_id: 156-F
priority: high
status: queued
title: Fault-line evidence contract DECLARATIONS + shape harness (U4a-decl)
updated_at: 2026-09-05T04:04:12.6467413Z
---

U4a-declarations. NEW standalone leaf internal/faultline (only intentional internal deps: internal/canonical + optionally internal/errors; no detector/harness/core/events). Declare SHARED ENVELOPE EvidenceArtifact{SchemaVersion int `json:"schema_version"`,ProducingTask `json:"producing_task"`,ProducingCommit `json:"producing_commit"`,NodeFamily `json:"node_family"`,Applicability []string `json:"applicability"`,ValidatorIdentity `json:"validator_identity"`,VerifiedEvidence json.RawMessage `json:"verified_evidence"`,Status `json:"status"`} (all outer tags explicit snake_case; VerifiedEvidence is a DISCRIMINATED node-family payload, NOT one parity struct for all producers). FamilyPayload interface{CanonicalMap() map[string]any; Validate() error} + family REGISTRY map[string]func()FamilyPayload keyed by NodeFamily + RegisterFamily(name,factory)/knownFamilies(). v1 ONLY family: exported const NodeFamilyParity="parity" (S4) with ParityEvidence{ScenarioID string `json:"scenario_id"`; Dimension string `json:"dimension"`; Surfaces []string `json:"surfaces"`; DivergentFields []string `json:"divergent_fields"`; ExpectedDivergence bool `json:"expected_divergence"`; TrackedDefect string `json:"tracked_defect"`; Detail string `json:"detail"`} (snake_case; Surfaces/DivergentFields non-nil empty; TrackedDefect empty only when ExpectedDivergence=false). Later producers register own typed payload+golden (RepresentationEvidence,RedGreenEvidence,...) - no Detail overloading. EXACT API signatures: func (a EvidenceArtifact) Canonical() ([]byte,error); func DecodeAndValidate(data []byte) (EvidenceArtifact,FamilyPayload,error); func Validate(a EvidenceArtifact, p FamilyPayload) error. Validate INVARIANTS (absent JSON decodes to zero even under DisallowUnknownFields, reject zero-valued-but-shape-valid): SchemaVersion in knownVersions; ProducingTask/ProducingCommit/ValidatorIdentity non-empty; NodeFamily in knownFamilies(); Applicability non-empty each in {cli,mcp,internal}; Status in {pass,report_only,fail}; PLUS family Validate (ParityEvidence: ScenarioID/Dimension non-empty; Surfaces non-empty each in {cli,mcp,internal}; ExpectedDivergence=>TrackedDefect non-empty concrete ID; Status!=pass=>DivergentFields non-empty). EvidenceSchemaVersion const + knownVersions. Source-shape (declaration) harness FIRST per harness-architect asserts exact serialized envelope+ParityEvidence shape against checked-in per-family golden. Behavior in 156.006-T. Acceptance: declarations+registry+signatures+red shape harness. Files: internal/faultline/evidence.go. Domain: code.