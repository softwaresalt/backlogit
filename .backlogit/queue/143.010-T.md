---
artifact_type: task
created_at: 2026-08-18T00:58:07.1096515Z
dependencies:
    - 143.004-T
    - 143.009-T
id: 143.010-T
labels:
    - mcp
    - audit-log
    - recovery
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: queued
title: Scope MCP recovery guidance to the producing step
updated_at: 2026-08-18T00:58:07.1096515Z
---

# Scope MCP recovery guidance to the producing step

## Description

<!-- BEGIN:description -->
Unit 10. `mutationPartialRecovery` (`internal/mcp/errors.go:165-176`) is keyed on `Class` ONLY and is reached from `domainError` (`:116-119`), `handleTrackCommit` (`internal/mcp/tools.go:1595`, dispatch `:1613-1616`), and `checkpointDispositionError` (`internal/mcp/errors.go:229`, dispatch `:230-233`). Repointing the indeterminate case would mis-advise all three. Instead pass `err.FailedStep`, already in scope at `internal/mcp/errors.go:153` and `:156`, and branch on `blerrors.StepShippedEventAppend`, leaving the class-only default text untouched. The shipped-event guidance must be DUAL-SURFACE, naming both `check_shipped_event_completeness` (MCP) and `--check-shipped-event-completeness` (CLI), and must carry the reconcile-before-archiving instruction. Also correct the structured `Retryable` field: `internal/mcp/errors.go:155` sets `Retryable` to `Class == not-applied`, so the new not-applied plus partially-compensated result would advertise itself as safe to retry while release-scope items remain un-restored; gate `Retryable` on class AND compensation state. Keep the single dispatch site in `handleShipShipment`: `gateErrorResult` runs first but cannot match `MutationPartialError` (`internal/mcp/gate_errors.go:22-38`), so add a regression guard rather than restructuring the dispatch. Execution posture: test-first.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- Guidance branches on `FailedStep`, not on `Class` alone, and the class-only default text is unchanged.
- The shipped-event guidance names both the MCP parameter and the CLI flag and carries the reconcile-before-archiving instruction.
- A partially-compensated result is never reported as `Retryable: true`.
- `internal/mcp/error_mapping_test.go:200` (`track_commit` guidance contains `check_partial_mutations`) still passes.
- A regression guard asserts the indeterminate result is not consumed by `gateErrorResult`.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/mcp/errors.go` plus an `internal/mcp` contract test. Halt if `error_mapping_test.go:200` goes red: the guidance change has leaked out of the shipped-event producer.
<!-- END:implementation-notes -->
