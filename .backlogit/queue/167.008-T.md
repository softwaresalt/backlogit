---
artifact_type: task
created_at: 2026-09-06T02:07:36.8045547Z
dependencies:
    - 167.007-T
    - 167.001-T
    - 167.010-T
id: 167.008-T
parent_id: 167-F
priority: critical
status: queued
title: Reconciliation transaction (classifier + orchestration + rollback)
updated_at: 2026-09-06T04:43:05.6831198Z
---

Reconciliation transaction (deps 167.007 appender + 167.001 preconditions + 167.010 classifier). Implement ReconcileShipmentToShipped as a TWO-PHASE protocol that satisfies BOTH "slow evidence I/O never held under the artifact lock batch" AND "replay reads no closure/git": PHASE A (no locks) - read the shipment manifest to compute the member id set (NormalizeShipmentItems); compute the cheap REQUEST-IDENTITY digest (request scalars ONLY: idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH - NO file read). PHASE B (sorted batch #1) - acquire the SINGLE globally-sorted lockArtifactMutations({shipment} union members) batch (identical ordering to every caller incl ReconcileArchivedLifecycle, shipment.go:447-471 - NO inversion; never lock shipment-then-members sequentially); IDENTITY/LOCATION gate (reload Markdown/handle: artifact_type==shipment + status==archived + real-path confinement); classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, reqRequestIdentityDigest) using frontmatter+log ONLY (NO evidence I/O). Non-2d branches (no_op/resume/conflict/indeterminate) finalize and RETURN under batch #1 - resume appends the persisted prepared Event verbatim; a renamed/deleted closure still returns no_op (replay NEVER reads closure). PHASE C (no locks, branch 2d ONLY) - RELEASE batch #1, run SLOW delivery-evidence I/O (git MERGE-COMMIT>=2 parents + reachability under boundedHelperTimeout, closure open+content-hash) with NO locks held; build the full evidence_digest + complete-event digest + the persist-ready canonical FULL events.Event. PHASE D (sorted batch #2) - re-acquire the SAME sorted batch, RE-CLASSIFY under lock (TOCTOU: must STILL be 2d - a concurrent completion re-routes to no_op/conflict), re-check every member terminal from Markdown, then in-place write via the 167.006 writer setting archived_status:shipped + idempotency_key + shipment_reconciliation_request_identity_digest (scalar) + shipment_reconciliation_prepared_event (canonical FULL events.Event) + shipment_reconciliation_event_digest (full wrapper+delta) ALL in the SAME atomic write; snapshot+rollback file+index (ErrWriteNotApplied restore / ErrWriteIndeterminate = indeterminate NO restore NO append); durable append via 167.007 (item-log lock = distinct sidecar namespace, nested last). Routing branches (0 unreadable/path-unsafe=indeterminate; 0b malformed line=indeterminate; 1a-c reconcile-present incl 1b no-op requires stored key==reqKey AND persisted request-identity digest==reqRequestIdentityDigest, 1c conflict; 2a-d reconcile-absent incl 2b resume-append-only requires stored key==reqKey AND persisted request-identity digest match AND persisted prepared Event present+parseable+event-digest-matching, else indeterminate; 2d normal-repair only when NO leftover reconciliation metadata, else torn=indeterminate). DryRun runs the full precondition eval + prints planned before/after but makes NO reconciliation writes (archive file/index/event log unchanged; lock sidecars excepted). Result/CLI outcomes exactly reconciled/no_op/conflict/indeterminate. TDD (per policy): behavior harness lands against the 167.003 panic-body declaration, RED, then implement. Table-driven state-ladder + injected failures + concurrency + same-key/different-request-identity replay + missing/modified prepared-payload resume + two-phase TOCTOU re-route. Files internal/core/shipment_reconcile.go (transaction) + tests. Domain: code. See plan U1. Deps 167.007 + 167.001 + 167.010.