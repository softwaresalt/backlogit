---
artifact_type: task
created_at: 2026-09-06T02:07:36.8045547Z
dependencies:
    - 167.007-T
    - 167.001-T
    - 167.010-T
    - 167.014-T
    - 167.016-T
    - 167.017-T
id: 167.008-T
parent_id: 167-F
priority: critical
status: queued
title: Reconciliation transaction (classifier + orchestration + rollback)
updated_at: 2026-09-06T04:43:05.6831198Z
---

Reconciliation transaction (deps 167.007 appender + 167.001 preconditions + 167.010 classifier). Implement ReconcileShipmentToShipped as a TWO-PHASE protocol that satisfies BOTH "slow evidence I/O never held under the artifact lock batch" AND "replay reads no closure/git": PHASE A - acquire lockShipmentMembership(shipment) HELD ACROSS ALL PHASES (blocks AddItemToShipment-path manifest changes); read the shipment manifest to compute the member id set (NormalizeShipmentItems); compute the cheap REQUEST-IDENTITY digest (request scalars ONLY: idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH - NO file read). PHASE B (acquire C then B, order C->B matching AssociateCommit) - acquire the item-log lock C, then the sorted artifact batch B (lockArtifactMutations({shipment} union members), shipment.go:447-471); IDENTITY/LOCATION gate (reload Markdown/handle: artifact_type==shipment + status==archived + real-path confinement); classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, reqRequestIdentityDigest) using frontmatter+log ONLY (NO evidence I/O). Routing: NON-APPEND branches (no_op/conflict/indeterminate) finalize and RETURN (release B, C, A); 2b RESUME runs HERE under the held C+B (re-read log confirm no reconcile/shipped event + append the persisted prepared Event verbatim, then release B, C, A); 2d NORMAL-REPAIR releases B AND C (keeps membership lock A) and continues to Phase C for slow evidence I/O. PHASE C (branch 2d ONLY, only membership lock A held) - SLOW delivery-evidence I/O (git MERGE-COMMIT>=2 parents + reachability, closure open+content-hash) with NO artifact lock B and NO item-log lock C held; build the full evidence_digest + complete-event digest + persist-ready canonical FULL events.Event. PHASE D (branch 2d, HOLD C+B together in the order C-then-B matching AssociateCommit) - acquire the item-log lock C (167.011, SOLE acquirer, non-reentrant) THEN the sorted artifact batch B; RE-READ manifest under B and assert the member set EQUALS the Phase-A set (UpdateArtifact can change custom_fields.items under B without A, artifacts.go:514-518,604-606 -> mismatch = indeterminate); member terminality; authoritative re-classify (still 2d); snapshot via 167.016; write frontmatter via the 167.006 writer (archived_status:shipped + idempotency_key + shipment_reconciliation_request_identity_digest + shipment_reconciliation_prepared_event canonical FULL events.Event + shipment_reconciliation_event_digest) AND durable append via the 167.007 primitive (validate-before-index) ATOMICALLY UNDER the held C+B (log re-read + write + append all under C+B - NO gap, NO clobber; 167.007 does NOT re-acquire C); ErrWriteNotApplied rolls back file+index via 167.016 UNDER the held C+B / ErrWriteIndeterminate = indeterminate NO restore NO append; release B, then C, then membership lock A. THE TRANSACTION HOLDS B AND C TOGETHER ONLY IN THE C->B ORDER (matching AssociateCommit - NO new inversion); the residual deadlock vs ArchiveItem's B->C (archive.go:99,292) is the PRE-EXISTING inversion captured as HARD PREREQUISITE 763A1152 (a global blocking B/C order must be established across ArchiveItem/AssociateCommit/reconcile). A "never hold both" alternative was rejected (releasing B before append lets a concurrent AssociateCommit/ArchiveItem overwrite archived_status in the gap, and the rollback then erases a valid update or needs B-under-C = deadlock). A CONCURRENCY TEST vs concurrent AssociateCommit AND ArchiveItem on the same shipment is REQUIRED. The 2b RESUME path runs under the Phase-B held C+B (C-then-B): re-read log (confirm no reconcile/shipped event) + append the persisted prepared Event verbatim under C+B, then release B, C, A. Routing branches (0 unreadable/path-unsafe=indeterminate; 0b malformed line=indeterminate; 1a-c reconcile-present incl 1b no-op requires stored key==reqKey AND persisted request-identity digest==reqRequestIdentityDigest, 1c conflict; 2a-d reconcile-absent incl 2b resume-append-only requires stored key==reqKey AND persisted request-identity digest match AND persisted prepared Event present+parseable+event-digest-matching, else indeterminate; 2d normal-repair only when NO leftover reconciliation metadata, else torn=indeterminate). DryRun runs the full precondition eval + prints planned before/after but makes NO reconciliation writes (archive file/index/event log unchanged; lock sidecars excepted). Result/CLI outcomes exactly reconciled/no_op/conflict/indeterminate. TDD (per policy): behavior harness lands against the 167.003 panic-body declaration, RED, then implement. Table-driven state-ladder + injected failures + concurrency + same-key/different-request-identity replay + missing/modified prepared-payload resume + two-phase TOCTOU re-route. Files internal/core/shipment_reconcile.go (transaction) + tests. Domain: code. See plan U1. Deps 167.007 + 167.001 + 167.010 + 167.014 + 167.016 + 167.017 (lock-order coherence prerequisite - blocks deadlock/clobber-safe implementation).