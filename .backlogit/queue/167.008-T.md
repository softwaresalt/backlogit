---
artifact_type: task
created_at: 2026-09-06T02:07:36.8045547Z
dependencies:
    - 167.007-T
    - 167.001-T
    - 167.010-T
    - 167.014-T
    - 167.016-T
id: 167.008-T
parent_id: 167-F
priority: critical
status: queued
title: Reconciliation transaction (classifier + orchestration + rollback)
updated_at: 2026-09-06T04:43:05.6831198Z
---

Reconciliation transaction (deps 167.007 appender + 167.001 preconditions + 167.010 classifier). Implement ReconcileShipmentToShipped as a TWO-PHASE protocol that satisfies BOTH "slow evidence I/O never held under the artifact lock batch" AND "replay reads no closure/git": PHASE A - acquire lockShipmentMembership(shipment) HELD ACROSS ALL PHASES (blocks AddItemToShipment-path manifest changes); read the shipment manifest to compute the member id set (NormalizeShipmentItems); compute the cheap REQUEST-IDENTITY digest (request scalars ONLY: idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH - NO file read). PHASE B (sorted artifact batch #1, nested in the membership lock) - acquire lockArtifactMutations({shipment} union members) (identical ordering to every caller incl ReconcileArchivedLifecycle, shipment.go:447-471 - NO inversion; never lock shipment-then-members sequentially); IDENTITY/LOCATION gate (reload Markdown/handle: artifact_type==shipment + status==archived + real-path confinement); classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, reqRequestIdentityDigest) using frontmatter+log ONLY (NO evidence I/O). Non-2d branches (no_op/resume/conflict/indeterminate) finalize and RETURN (release batch, then membership lock). PHASE C (branch 2d ONLY) - RELEASE artifact batch #1 (KEEP the membership lock), run SLOW delivery-evidence I/O (git MERGE-COMMIT>=2 parents + reachability, closure open+content-hash) with NO artifact locks held; build the full evidence_digest + complete-event digest + the persist-ready canonical FULL events.Event. PHASE D - re-acquire the SAME sorted batch; RE-READ the manifest UNDER the held shipment ARTIFACT lock and assert the member set EQUALS the Phase-A set - ANY change => FAIL CLOSED to indeterminate (lockShipmentMembership alone is INSUFFICIENT because UpdateArtifact can replace custom_fields.items under the ARTIFACT lock without the membership lock, artifacts.go:514-518,604-606, so this member-set equality check is REQUIRED); re-classify (TOCTOU: still 2d, else re-route), re-check every member terminal from Markdown, then - ACQUIRE THE ITEM-LOG LOCK BEFORE THE FINAL HISTORY RECLASSIFY (the classify->write-frontmatter->append decision reads the event log; a concurrent append between the read and the append would persist archived_status:shipped from stale history then add a duplicate/conflicting marker - so hold the item-log lock across the final reclassify + frontmatter write + event append, making the sequence atomic w.r.t. event-log changes) - re-classify, in-place write via the 167.006 writer setting archived_status:shipped + idempotency_key + shipment_reconciliation_request_identity_digest (scalar) + shipment_reconciliation_prepared_event (canonical FULL events.Event) + shipment_reconciliation_event_digest (full wrapper+delta) ALL in the SAME atomic write; snapshot+rollback via the 167.016 primitive (ErrWriteNotApplied restore / ErrWriteIndeterminate = indeterminate NO restore, NO append); durable append via 167.007 UNDER the same item-log lock; release item-log lock, artifact batch, then membership lock. The 2b RESUME path likewise acquires the item-log lock BEFORE its final log re-read (confirm no reconcile/shipped event) + verbatim prepared-Event append, closing the same decision-to-append gap. Routing branches (0 unreadable/path-unsafe=indeterminate; 0b malformed line=indeterminate; 1a-c reconcile-present incl 1b no-op requires stored key==reqKey AND persisted request-identity digest==reqRequestIdentityDigest, 1c conflict; 2a-d reconcile-absent incl 2b resume-append-only requires stored key==reqKey AND persisted request-identity digest match AND persisted prepared Event present+parseable+event-digest-matching, else indeterminate; 2d normal-repair only when NO leftover reconciliation metadata, else torn=indeterminate). DryRun runs the full precondition eval + prints planned before/after but makes NO reconciliation writes (archive file/index/event log unchanged; lock sidecars excepted). Result/CLI outcomes exactly reconciled/no_op/conflict/indeterminate. TDD (per policy): behavior harness lands against the 167.003 panic-body declaration, RED, then implement. Table-driven state-ladder + injected failures + concurrency + same-key/different-request-identity replay + missing/modified prepared-payload resume + two-phase TOCTOU re-route. Files internal/core/shipment_reconcile.go (transaction) + tests. Domain: code. See plan U1. Deps 167.007 + 167.001 + 167.010 + 167.014 + 167.016.