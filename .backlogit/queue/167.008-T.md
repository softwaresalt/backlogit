---
artifact_type: task
created_at: 2026-09-06T02:07:36.8045547Z
dependencies:
    - 167.007-T
    - 167.001-T
    - 167.010-T
id: 167.008-T
parent_id: 167-F
priority: critical
status: queued
title: Reconciliation transaction (classifier + orchestration + rollback)
updated_at: 2026-09-06T02:24:37.1695836Z
---

Reconciliation transaction (deps 167.007 appender + 167.001 preconditions + 167.010 classifier). Implement ReconcileShipmentToShipped orchestration: acquire locks membership -> shipment artifact FIRST, then an IDENTITY/LOCATION GATE UNDER LOCK (reload Markdown/handle, validate artifact_type==shipment + status==archived + real-path confinement under .backlogit/archive/ under the held locks - closes the TOCTOU window; runs on EVERY invocation before request-identity materialization + classification so no_op branch 1b and resume branch 2b cannot act on a mis-identified/mis-located target); then MATERIALIZE the cheap REQUEST-IDENTITY digest UNDER LOCK (hash over request scalars only: idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH - NO external file read, so replay never depends on the closure still existing) -> CALL the shared classifier classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, reqRequestIdentityDigest) from 167.010-T (does NOT redefine it) to route -> branch 2d only: FULL delivery-evidence verification via U2 part B (closure open+content-hash + merge reachability) + member locks(sorted)+terminality re-check -> write. Routing branches (0 unreadable/path-unsafe=indeterminate; 0b malformed line=indeterminate; 1a-c reconcile-present incl 1b no-op requires stored key==reqKey AND persisted request-identity digest==reqRequestIdentityDigest (NO closure re-read), 1c same-key/different-request-identity=conflict; 2a-d reconcile-absent incl 2b resume-append-only requires stored key==reqKey AND persisted shipment_reconciliation_request_identity_digest==incoming request-identity digest (so changed actor/reason/approver/evidence-refs/closure-path cannot reach resume) AND the persisted shipment_reconciliation_prepared_event to be PRESENT + PARSEABLE + matching shipment_reconciliation_event_digest before appending - a missing/unparseable/modified prepared payload = indeterminate (never a blind append) - and a successful append-only recovery returns outcome reconciled). in-place edit via the 167.006 writer setting archived_status:shipped + idempotency_key + shipment_reconciliation_request_identity_digest (scalar, for replay identity) + shipment_reconciliation_prepared_event (canonical payload) + shipment_reconciliation_event_digest (full, content-hash-bound) - ALL persisted in the SAME atomic write so no_op/resume can match after the initial repair; snapshot+rollback file+index (upsert-or-delete by row presence); frontmatter-write classification (clean / ErrWriteNotApplied restore / ErrWriteIndeterminate=indeterminate no-rollback no-append); append via 167.007; DryRun no writes. Result/CLI outcomes are exactly reconciled/no_op/conflict/indeterminate (resume is NOT a distinct external outcome - a completed append-only recovery reports reconciled). TDD: RED behavior harness first; table-driven state-ladder + injected failures + concurrency + same-key/different-payload replay + missing/modified prepared-payload resume. Files internal/core/shipment_reconcile.go (transaction) + tests. Domain: code. See plan U1. Deps 167.007 + 167.001 + 167.010.