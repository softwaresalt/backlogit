---
artifact_type: task
created_at: 2026-09-06T02:07:36.8045547Z
dependencies:
    - 167.007-T
    - 167.001-T
    - 167.010-T
    - 167.014-T
    - 167.016-T
id: 167.008-T
parent_id: 167-F
priority: critical
status: queued
title: Reconciliation transaction (classifier + orchestration + rollback)
updated_at: 2026-09-06T04:43:05.6831198Z
---

Reconciliation transaction (deps 167.007 appender + 167.001 preconditions + 167.010 classifier). Implement ReconcileShipmentToShipped as a TWO-PHASE protocol that satisfies BOTH "slow evidence I/O never held under the artifact lock batch" AND "replay reads no closure/git": PHASE A - acquire lockShipmentMembership(shipment) HELD ACROSS ALL PHASES (blocks AddItemToShipment-path manifest changes); read the shipment manifest to compute the member id set (NormalizeShipmentItems); compute the cheap REQUEST-IDENTITY digest (request scalars ONLY: idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH - NO file read). PHASE B (sorted artifact batch #1, nested in the membership lock) - acquire lockArtifactMutations({shipment} union members) (identical ordering to every caller incl ReconcileArchivedLifecycle, shipment.go:447-471 - NO inversion; never lock shipment-then-members sequentially); IDENTITY/LOCATION gate (reload Markdown/handle: artifact_type==shipment + status==archived + real-path confinement); classifyShipmentReconcileState(log, frontmatter, reqIdempotencyKey, reqRequestIdentityDigest) using frontmatter+log ONLY (NO evidence I/O). Phase B is a ROUTING classify: NON-APPEND branches (no_op/conflict/indeterminate) finalize and RETURN (release B, then membership lock A); APPEND branches (2d normal-repair, 2b resume) release B and continue to Phase C/D. PHASE C (branch 2d ONLY, only membership lock A held) - SLOW delivery-evidence I/O (git MERGE-COMMIT>=2 parents + reachability, closure open+content-hash) with NO artifact lock B and NO item-log lock C held; build the full evidence_digest + complete-event digest + persist-ready canonical FULL events.Event. PHASE D-write (branch 2d ONLY, B only) - acquire B; RE-READ manifest under B and assert the member set EQUALS the Phase-A set (UpdateArtifact can change custom_fields.items under B without A, artifacts.go:514-518,604-606 -> mismatch = indeterminate); re-check member terminality from Markdown; authoritatively re-classify (still 2d); snapshot via 167.016; write frontmatter via the 167.006 writer setting archived_status:shipped + idempotency_key + shipment_reconciliation_request_identity_digest (scalar) + shipment_reconciliation_prepared_event (canonical FULL events.Event) + shipment_reconciliation_event_digest (full wrapper+delta) ALL in the SAME atomic write; a frontmatter-write failure rolls back HERE via 167.016 (ErrWriteNotApplied restore / ErrWriteIndeterminate = indeterminate NO restore); release B. PHASE E-append (2d + 2b resume, C only) - acquire the item-log lock C (167.011, SOLE acquirer, non-reentrant); RE-READ the event log UNDER C (authoritative append idempotency/conflict decision: reconcile/shipped event now present -> done/no_op; conflicting normal shipped event -> conflict; else append the prepared Event); durable append via the 167.007 primitive UNDER the held C (167.007 does NOT re-acquire; validate-before-index) - the log re-read and append are BOTH under C so decide->append is atomic; release C; release membership lock A. THE TRANSACTION NEVER HOLDS B AND C SIMULTANEOUSLY (B released before C acquired), so it is deadlock-safe against the PRE-EXISTING blocking-lock B<->C inversion between ArchiveItem/reconcile (B->C, archive.go:99,292) and AssociateCommit (C->B, commits.go:101-131 -> shipment.go:839-844) - captured as latent-deadlock follow-up 763A1152; the frontmatter/append gap is covered by idempotency resume. Frontmatter writes are serialized by B (a concurrent AssociateCommit persistArtifact, also under B, cannot clobber Phase-D's write). A CONCURRENCY TEST vs a concurrent AssociateCommit AND a concurrent ArchiveItem on the same shipment MUST prove NO deadlock. The 2b RESUME path likewise acquires the item-log lock BEFORE its final log re-read (confirm no reconcile/shipped event) + verbatim prepared-Event append, closing the same decision-to-append gap. Routing branches (0 unreadable/path-unsafe=indeterminate; 0b malformed line=indeterminate; 1a-c reconcile-present incl 1b no-op requires stored key==reqKey AND persisted request-identity digest==reqRequestIdentityDigest, 1c conflict; 2a-d reconcile-absent incl 2b resume-append-only requires stored key==reqKey AND persisted request-identity digest match AND persisted prepared Event present+parseable+event-digest-matching, else indeterminate; 2d normal-repair only when NO leftover reconciliation metadata, else torn=indeterminate). DryRun runs the full precondition eval + prints planned before/after but makes NO reconciliation writes (archive file/index/event log unchanged; lock sidecars excepted). Result/CLI outcomes exactly reconciled/no_op/conflict/indeterminate. TDD (per policy): behavior harness lands against the 167.003 panic-body declaration, RED, then implement. Table-driven state-ladder + injected failures + concurrency + same-key/different-request-identity replay + missing/modified prepared-payload resume + two-phase TOCTOU re-route. Files internal/core/shipment_reconcile.go (transaction) + tests. Domain: code. See plan U1. Deps 167.007 + 167.001 + 167.010 + 167.014 + 167.016.