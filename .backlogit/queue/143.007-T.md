---
artifact_type: task
created_at: 2026-08-18T00:56:57.5001772Z
dependencies:
    - 143.006-T
id: 143.007-T
labels:
    - doctor
    - audit-log
    - core
    - stash-0115F71F
parent_id: 143-F
priority: medium
references:
    - docs/exec-plans/2026-08-17-shipment-shipped-event-audit-log-plan.md
    - docs/decisions/2026-08-17-shipment-shipped-event-audit-log-decision.md
status: queued
title: Implement the report-only doctor reconciliation audit
updated_at: 2026-08-18T00:56:57.5001772Z
---

# Implement the report-only doctor reconciliation audit

## Description

<!-- BEGIN:description -->
Unit 7. Replace the inert registration with the real check. The check MUST read `archived_status`, not just `status`: `ArchiveItem` sets `status: archived` and stores the pre-archive status in `archived_status` (`internal/core/archive.go:223`), so an archived shipped shipment presents as `status: archived`. Neither `artifactRef` (`internal/core/canonical_scan.go:21`) nor the doctor-local struct (`internal/core/doctor.go:230-236`, `:253-260`) carries `archived_status` today, so both must be extended or the check must re-read the shipment frontmatter from the exact path already in `refs`; without that filter the audit false-positives on shipments archived from abandoned or any other status. Scan queue and archive. For `shipped_unarchived_residue` also enumerate the shipment's release-scope items left terminal-but-unarchived, because the indeterminate branch returns before `collectArchiveCandidateIDs` (`internal/core/shipment_lifecycle.go:515`) and strands the whole archive set, not the shipment alone. Carry the verify-the-actual-history caveat wording from `internal/core/doctor.go:545`. Never write, synthesize, or rewrite JSONL. Execution posture: test-first.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- The check filters on `archived_status` and does not flag shipments archived from a non-shipped status.
- Detection reads authoritative state from the canonical raw-Markdown scan across queue and archive, not the DB projection.
- The `shipped_unarchived_residue` finding enumerates the stranded release-scope items.
- The check never writes or rewrites JSONL, asserted by unchanged log bytes.
- All three 143.006-T scenarios are green.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Files: `internal/core/doctor.go`, `internal/core/canonical_scan.go`. Widening `artifactRef` is mandatory, not conditional; the struct is shared with the create-time uniqueness guard, so the added field must be additive and unused elsewhere.
<!-- END:implementation-notes -->
