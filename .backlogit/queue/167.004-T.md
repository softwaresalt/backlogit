---
artifact_type: task
created_at: 2026-09-06T00:58:39.5232361Z
dependencies:
    - 167.008-T
id: 167.004-T
parent_id: 167-F
priority: critical
status: queued
title: CLI shipment reconcile-shipped + dry-run (U4)
updated_at: 2026-09-06T02:59:30.1896823Z
---

U4 (TDD: CLI tests FIRST). newShipmentReconcileShippedCmd under the shipment group (internal/cli/shipment.go): backlogit shipment reconcile-shipped <id>; flags --reason/--actor/--idempotency-key/--merge-sha/--closure-evidence (required), --second-approver/--evidence-ref (optional), --dry-run, and --confirm <token> (ENFORCED operator-authorization gate, required for any non-dry-run mutation). Because CLI-only is a surface boundary NOT an authorization boundary in an autonomous harness (self-asserted --actor/--second-approver do NOT prove operator intent), the mutating path mirrors the s12 U2 / repair_member_evidence / force_cli_only break-glass: proceeds ONLY with an enforced operator-confirmation signal (interactive-TTY confirmation OR an explicit --confirm token matching the required value), DENIED BY DEFAULT for non-interactive/agent invocation without it (typed ErrOperatorConfirmationRequired). --dry-run + inspection need no confirmation (no writes). Builds ShipmentShippedReconcileRequest, calls core.ReconcileShipmentToShipped. --dry-run runs the full precondition eval + prints planned before/after + planned event, makes NO writes (exit 0). Exit codes via internal/cli.ExitCodeFor (typed reject -> non-zero governance/validation; success/no_op -> 0; indeterminate -> distinct non-zero). CLI-only, no MCP tool. Tests via fresh cobra cmd + SetArgs/SetOut (no os.Args/os.Stdout): required-flag omission errors; a non-dry-run invocation WITHOUT the operator-confirmation signal is DENIED (ErrOperatorConfirmationRequired, non-zero, no writes); dry-run zero file changes + no confirmation needed; success (with confirmation) + a reject exit code. Files internal/cli/shipment.go + test. Domain: code. See plan U4. Deps 167.008 (transaction).