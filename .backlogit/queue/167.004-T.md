---
artifact_type: task
created_at: 2026-09-06T00:58:39.5232361Z
dependencies:
    - 167.008-T
id: 167.004-T
parent_id: 167-F
priority: critical
status: queued
title: CLI shipment reconcile-shipped + dry-run (U4)
updated_at: 2026-09-06T04:02:32.6083423Z
---

U4 (TDD: CLI tests FIRST). newShipmentReconcileShippedCmd under the shipment group (internal/cli/shipment.go): backlogit shipment reconcile-shipped <id>; flags --reason/--actor/--idempotency-key/--merge-sha/--closure-evidence (required), --second-approver/--evidence-ref (optional), --dry-run, and --confirm <token> (explicit-confirmation guard, required for any non-dry-run mutation). CONFIRMATION PREDICATE (deterministic): --confirm must EXACTLY equal the operation-and-shipment-specific phrase "reconcile-shipped <shipment-id>" (e.g. reconcile-shipped 048-S) - a fixed, shipment-scoped literal so a stray/copy-pasted token for another shipment does not confirm; OR, when stdin is an interactive TTY, the command prompts and requires the operator to type that same exact phrase. A non-dry-run mutation with neither the matching --confirm token NOR an interactive-TTY confirmation is DENIED (typed ErrConfirmationRequired, non-zero, no writes). TRUTHFUL FRAMING: this is NOT machine-authenticated (an autonomous caller can supply the phrase or allocate a TTY; backlogit has no external operator trust anchor) - it is an explicit-confirmation GUARD against silent/accidental invocation + a durable audit event for attribution; authenticated approval is deferred (866FDC8C - broadened to explicitly track authenticated invocation authorization). The #423 issue's "operator-authorized" wording is CONSCIOUSLY REVISED to confirmation-only for v1 (confirmation + attribution, not machine-authenticated authorization); genuine authenticated operator authorization is tracked SEPARATELY in 866FDC8C, not silently dropped. --dry-run + inspection need no confirmation (no reconciliation writes - the shipment archive file/index row/event log are unchanged; lock sidecars are excepted). Builds ShipmentShippedReconcileRequest, calls core.ReconcileShipmentToShipped. --dry-run runs the full precondition eval + prints planned before/after + planned event, makes NO writes (exit 0). Exit codes via internal/cli.ExitCodeFor (typed reject -> non-zero governance/validation; success/no_op -> 0; indeterminate -> distinct non-zero). CLI-only, no MCP tool. Tests via fresh cobra cmd + SetArgs/SetOut (no os.Args/os.Stdout): required-flag omission errors; a non-dry-run invocation with NO --confirm and non-TTY stdin is DENIED (ErrConfirmationRequired, no writes); a --confirm token that does NOT equal the shipment-specific phrase is DENIED; a MATCHING --confirm token permits the mutation; dry-run zero RECONCILIATION writes (archive file/index/event log unchanged; lock sidecars excepted) + no confirmation needed; success + a reject exit code. Files internal/cli/shipment.go + test. Domain: code. See plan U4. Deps 167.008 (transaction).