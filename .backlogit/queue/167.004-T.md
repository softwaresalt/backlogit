---
artifact_type: task
created_at: 2026-09-06T00:58:39.5232361Z
dependencies:
    - 167.008-T
id: 167.004-T
parent_id: 167-F
priority: critical
status: queued
title: CLI shipment reconcile-shipped + dry-run (U4)
updated_at: 2026-09-06T03:19:37.4758761Z
---

U4 (TDD: CLI tests FIRST). newShipmentReconcileShippedCmd under the shipment group (internal/cli/shipment.go): backlogit shipment reconcile-shipped <id>; flags --reason/--actor/--idempotency-key/--merge-sha/--closure-evidence (required), --second-approver/--evidence-ref (optional), --dry-run, and --confirm <token> (explicit-confirmation guard, required for any non-dry-run mutation). TRUTHFUL FRAMING: this is NOT a machine-authenticated operator-authorization boundary - an autonomous caller can supply --confirm or allocate a TTY, and backlogit has no external operator trust anchor. The --confirm/TTY gate is an EXPLICIT-CONFIRMATION GUARD (prevents silent/accidental non-interactive invocation) + the durable audit event provides ATTRIBUTABLE provenance; genuine operator-authenticated approval is the deferred residual (866FDC8C). Describe the surface as CLI-only + explicit-confirmation-gated + audited + acknowledged, NOT authenticated. --dry-run + inspection need no confirmation (no writes). Builds ShipmentShippedReconcileRequest, calls core.ReconcileShipmentToShipped. --dry-run runs the full precondition eval + prints planned before/after + planned event, makes NO writes (exit 0). Exit codes via internal/cli.ExitCodeFor (typed reject -> non-zero governance/validation; success/no_op -> 0; indeterminate -> distinct non-zero). CLI-only, no MCP tool. Tests via fresh cobra cmd + SetArgs/SetOut (no os.Args/os.Stdout): required-flag omission errors; a non-dry-run invocation WITHOUT the confirmation guard is DENIED (ErrConfirmationRequired, non-zero, no writes); dry-run zero file changes + no confirmation needed; success (with confirmation) + a reject exit code. Files internal/cli/shipment.go + test. Domain: code. See plan U4. Deps 167.008 (transaction).