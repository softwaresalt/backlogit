---
artifact_type: task
created_at: 2026-09-19T01:41:34.0554715Z
dependencies:
    - 175.044-T
id: 175.076-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/db/telemetry_schema_test.go (+1)'
updated_at: 2026-09-19T07:33:51.7082586Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck
Package: ./internal/db
Owned source file(s) (2): internal/db/telemetry_schema_test.go, internal/db/schema_gen_test.go
Owned baseline findings (7) from docs/decisions/2026-09-17-baseline-lint-inventory.md (golangci-lint v2.13.2, unbounded invocation):
- internal/db/telemetry_schema_test.go:L23:C35 [errcheck] Error return value of `sqliteDB.Close` is not checked
- internal/db/telemetry_schema_test.go:L39:C13 [errcheck] Error return value of `rows1.Close` is not checked
- internal/db/telemetry_schema_test.go:L43:C13 [errcheck] Error return value of `rows2.Close` is not checked
- internal/db/telemetry_schema_test.go:L95:C18 [errcheck] Error return value of `rows.Close` is not checked
- internal/db/schema_gen_test.go:L151:C35 [errcheck] Error return value of `database.Close` is not checked
- internal/db/schema_gen_test.go:L191:C35 [errcheck] Error return value of `database.Close` is not checked
- internal/db/schema_gen_test.go:L208:C18 [errcheck] Error return value of `rows.Close` is not checked
Affected functions (6 distinct): internal/db/telemetry_schema_test.go: TestEnsureTelemetrySchema_CreatesTablesIdempotently, TestTelemetryTablesQueryableViaGate, setupTelemetryDB; internal/db/schema_gen_test.go: TestEnsureSchemaWithExtensions_FreshSchemaIncludesComplexity, columnExists, setupSchemaGenTestDB

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/db/lintfix_175_076_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_076_LintFix` (deterministic `TestU175_076_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/db" -run "^TestU175_076_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_076_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_076_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return is NOT an allowed resolution): for every flagged call, capture the returned error and handle it -- checked propagation with `fmt.Errorf("context: %w", err)`, a named-return `errors.Join`, or (inside a test/benchmark) fail via the in-scope `*testing.T`/`*testing.B` receiver (`require.NoError`, `t.Fatalf`, `b.Fatalf`). A deferred close becomes a checked deferred close. No error return is discarded; no cross-file or cross-package helper is introduced; edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 3 (owned source file(s) + one owned harness file).
- Implementation files = 2 (owned harness excluded).
- Owned baseline findings = 7 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/db -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.076-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "packages": [
      "./internal/db"
    ],
    "owned_files": [
      "internal/db/schema_gen_test.go",
      "internal/db/telemetry_schema_test.go"
    ],
    "owned_findings": [
      {
        "path": "internal/db/schema_gen_test.go",
        "line": 151,
        "column": 35,
        "linter": "errcheck",
        "message": "Error return value of `database.Close` is not checked"
      },
      {
        "path": "internal/db/schema_gen_test.go",
        "line": 191,
        "column": 35,
        "linter": "errcheck",
        "message": "Error return value of `database.Close` is not checked"
      },
      {
        "path": "internal/db/schema_gen_test.go",
        "line": 208,
        "column": 18,
        "linter": "errcheck",
        "message": "Error return value of `rows.Close` is not checked"
      },
      {
        "path": "internal/db/telemetry_schema_test.go",
        "line": 23,
        "column": 35,
        "linter": "errcheck",
        "message": "Error return value of `sqliteDB.Close` is not checked"
      },
      {
        "path": "internal/db/telemetry_schema_test.go",
        "line": 39,
        "column": 13,
        "linter": "errcheck",
        "message": "Error return value of `rows1.Close` is not checked"
      },
      {
        "path": "internal/db/telemetry_schema_test.go",
        "line": 43,
        "column": 13,
        "linter": "errcheck",
        "message": "Error return value of `rows2.Close` is not checked"
      },
      {
        "path": "internal/db/telemetry_schema_test.go",
        "line": 95,
        "column": 18,
        "linter": "errcheck",
        "message": "Error return value of `rows.Close` is not checked"
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.076-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
