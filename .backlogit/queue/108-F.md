---
artifact_type: feature
created_at: 2026-07-15T06:39:32.6780535Z
custom_fields:
    harness_status: pending
    source_stash_id: D7B1B33D
    source_stash_kind: feature
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: Extend optional size estimation to feature and shipment artifacts. Define level-specific size semantics for feature versus shipment; add size_source and size_ruleset_version metadata plus estimate-history behavior; expose derived composition/rollups without persisting derived values as though they were human-authored estimates.
dependencies:
    - 109.004-T
    - 110-F
id: 108-F
labels:
    - size-estimation
    - schema
    - cli
    - blocked-on-spike
    - gated
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: blocked
title: Size estimation for feature and shipment implementation (blocked; needs recorded proceed spike outcome + Stage restaging)
updated_at: 2026-07-16T02:19:29.0591323Z
---

BLOCKED, UNMANIFESTED future-implementation placeholder for extending optional size estimation to feature and shipment artifacts (harvested from stash D7B1B33D; traceability preserved here). It has NO shipment home: it is a member of no shipment manifest and carries no shipment membership until a later Stage restaging. This feature is authored status: blocked and stays blocked as the durable gate, mirroring the 106-F formal-gate pattern. It depends on THREE necessary-but-not-sufficient edges: (1) 109.004-T (the size-extension architecture spike exit decision); (2) 107.009-T (the docline top-level extension pass-through PRODUCTION CODEC task in shipment 095-S); and (3) 107.011-T (opening the docline base JSON schema to ACCEPT producer-owned top-level extension properties, shipment 095-S). The 107.009-T and 107.011-T edges are genuine cross-shipment prerequisites: the top-level derived size fields (size, size_source, size_ruleset_version) are backlogit-owned extension keys at the TOP LEVEL of the derived contract. CURRENT-HEAD docline behavior (honest causal sequence): docline normalization currently FOLDS unknown top-level keys under the docline namespace, and FromMap/ToMap preserve that nested map, so the keys are RELOCATED (moved off the top level) rather than dropped today; a top-level DROP is only LATENT (it would occur if the fold were removed without a top-level carrier on BaseFrontmatter). Separately, the base schema currently REJECTS unknown top-level keys via additionalProperties:false. So until the codec preserves unknown top-level keys IN PLACE at the top level (107.009-T, coupling the fold-stop with a top-level carrier) AND the base schema is opened to ACCEPT producer-owned top-level extension properties (107.011-T), any future feature/shipment size provenance cannot survive a docline lint/migration round-trip at the top level or validate against the open base contract. Why the base contract matters even though backlogit needs its OWN round-trip implementation: docline codec/schema conformance is NECESSARY BUT NOT SUFFICIENT. Backlogit's own .backlogit artifact mutation paths (models.ArtifactFromFrontmatter, core.WriteArtifactFile, core.SetArtifactSize, and the generic title/status/section update rewrites) are the actual writers of size provenance; the current generic paths demonstrably DROP unknown top-level fields (the enumerated-key models.ArtifactFromFrontmatter mapping has no top-level extension carrier; core.WriteArtifactFile re-emits only struct-backed fields; only nested custom_fields survives), so they must be bridged and proven to preserve top-level extensions independently. The docline base contract only guarantees the docline lint/ingestion seam will not relocate or reject the fields, not that backlogit's read/modify/write cycle round-trips them. CURRENT-VS-TARGET (honest): current .backlogit artifacts are parsed and written by backlogit's own models/core codec, NOT automatically by internal/docline.Scope or internal/docline.Normalize; the base/extension inheritance model is the TARGET contract for interoperable ingestion (graphtor/engram), not proof that current backlog writers already use the docline codec. The 107.009-T and 107.011-T edges are base-contract prerequisites that do NOT by themselves provide backlog-artifact round-trip preservation; 109.002-T (the generic rewrite-path inventory), 109.007-T (the concrete inheritance-bridge and per-path preserve/drop classification and canonical size-location evidence), and 109.004-T (the final inheritance-bridge SELECTION and size-location decision) must resolve the backlog writers separately. All three edges are NECESSARY-BUT-NOT-SUFFICIENT: completing them only lifts dependency-based queue suppression and is outcome-agnostic; none auto-transitions this feature (the only permitted transition from blocked is an explicit blocked->active move) and none enforces a proceed outcome. The child tasks (108.001-T define size/provenance schema; 108.002-T persist provenance + estimate-history; 108.003-T expose render-time composition; 108.004-T document contract + verify CLI/MCP parity) are preserved as blocked implementation placeholders and are NOT authorized. No implementation may be activated until (a) 109.004-T records an explicit proceed decision (with the containment boundary, canonical size-location, and inheritance-bridge selection resolved) and (b) a later Stage restaging moves this feature blocked->active, gives it a shipment home, and re-harvests bounded <=2h width-isolated units. The existing task shapes may be re-scoped or superseded by the spike outcome.

## Reconciliation (Model A - 2026-07-16)

SUPERSEDING NOTE (authoritative; overrides any top-level-extension / open-base-schema framing above). The docline extension-ownership fork is DECIDED as Model A (deliberation 052-DL, feature 110-F; operator 2026-07-16): backlogit-owned properties (size, size_source, size_ruleset_version) nest UNDER the docline map as docline.backlogit.* (e.g. docline.backlogit.size); the docline base top level stays additionalProperties:false. The rejected Model-B approach (keep extension keys TOP-LEVEL, open the base schema, stop folding under docline) MUST NOT be implemented; its tasks 107.009/010/011/012-T are archived (terminal). This feature's prerequisites are now 109.004-T (spike exit decision) and 110-F (the Model-A extension contract: convention + nested ext-schema); the former 107.009-T/107.011-T top-level edges are removed. Because Model A stores size NESTED, the top-level round-trip DROP problem no longer applies - the only surviving bridge requirement is that backlogit's own writers preserve the nested docline.backlogit subtree, which the generic custom_fields path already preserves. No implementation is authorized until 110-F lands, 109.004-T records an explicit proceed decision, and a later Stage restaging moves this feature blocked->active with a shipment home.