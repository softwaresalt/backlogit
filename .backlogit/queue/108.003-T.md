---
artifact_type: task
created_at: 2026-07-15T06:41:32.8971867Z
dependencies:
    - 108.001-T
id: 108.003-T
labels:
    - spike
    - investigate-first
    - size-estimation
parent_id: 108-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: Investigate feature/shipment structured-composition membership, dedup, missing, and ruleset ownership (2h max)
updated_at: 2026-07-15T17:51:40.009695Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. Depends on the 108.001-T surface inventory. Investigate a candidate structured composition for derived feature/shipment sizing WITHOUT ratifying it as a contract. Because no canonical XS-XL aggregation ruleset exists (grep of internal/, schemas/, header-def), summing categorical values would invent arithmetic; the candidate shape is a render-only structured response: an XS-XL count histogram, an unsized/missing count, and the exact de-duplicated canonical member IDs. Investigate membership rules: feature = direct children by parent_id; shipment = manifest expansion de-duplicating the union of {feature, its child tasks} so a manifest listing both counts each work item once. Investigate missing/legacy handling (absent size increments unsized, never a default bucket; absent size_source reads as unknown/legacy, never rewritten as human) and the ruleset-version ownership question (who owns/versions any future ruleset; null until owned). Deliverable: membership/dedup/missing/ruleset findings as an investigation feeding 108.004-T, never persisted as authored. No implementation.