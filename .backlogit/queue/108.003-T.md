---
artifact_type: task
created_at: 2026-07-15T06:41:32.8971867Z
custom_fields:
    harness_status: scaffolded
dependencies:
    - 108.001-T
id: 108.003-T
labels:
    - size
    - composition
    - core
    - harness-ready
parent_id: 108-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: 'SE-4: Computed-on-read size composition rollups (core)'
updated_at: 2026-07-19T19:37:13.5100607Z
---

SE-4: Computed-on-read size composition rollups (core aggregation). Add internal/core/size_composition.go with a SizeComposition function returning a never-persisted structure: XS-XL count histogram, unsized count, de-duplicated canonical-members array. Feature membership = direct children by parent_id (internal/core/hierarchy.go). Shipment membership = NormalizeShipmentItems (internal/core/shipment.go:545-571), which returns ONLY the explicit custom_fields.items verbatim and does NOT expand a feature into its children (G4); therefore this unit MUST add explicit feature->children expansion: when a normalized shipment member is a feature, walk its child tasks (parent_id) and include their sizes. De-dup via uniqueNonEmptyStrings so a manifest listing a feature AND its explicit child tasks counts each item once. Membership resolution disambiguation (Copilot cycle-3 H6): an EXISTING artifact that has no size increments the `unsized` count; an UNRESOLVED manifest id (ErrNotFound -- the id resolves to no artifact) is WARN+SKIPPED and is NOT counted in the histogram or in `unsized` (optionally surfaced as a separate skipped/unresolved count); other resolution errors are warn+skipped likewise (DeriveCoveringFeature precedent). Comparator XS<S<M<L<XL mirrors the priority CASE ordered-enum (internal/core/queue.go:183-191). ruleset_version=null. Never writes to disk. Tests: (1) an existing-but-unsized child increments unsized (+1) AND a dangling manifest id (ErrNotFound) is skipped -- NOT counted, unsized unchanged by the dangling id, warn emitted; (2) a shipment whose manifest lists ONLY a feature expands to that feature children and counts their sizes (G4); (3) a shipment listing a feature PLUS its explicit child tasks de-dups so each item is counted once (G4); (4) the function never persists. Depends on 108.001-T (SE-1). Files: internal/core/size_composition.go.

<!-- BEGIN:implementation-notes -->
Harness (red): go test ./internal/core/ -run TestSE4SizeCompositionHarness
<!-- END:implementation-notes -->