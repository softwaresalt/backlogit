---
artifact_type: task
created_at: 2026-09-15T20:44:24.1253102Z
dependencies:
    - 174.052-T
id: 174.040-T
parent_id: 174-F
priority: high
status: active
title: 'R2: Writer/bypass BEHAVIOR RED harness'
updated_at: 2026-09-21T20:20:00.0000000Z
---

BEHAVIOR RED harness for the governed writer boundary and bypass closure. Depends on the declaration-only stubs (Rd 174.052-T) so it COMPILES against the declared block/unblock symbols AND the declaration-only `blerrors.ErrShipmentBlockedRequiresEnvelope` sentinel (also declared by Rd 174.052-T) and fails on BEHAVIOR (not compile). Targets public WriteArtifactFile boundary, the private lower writer, generic move/update, MoveShipmentStatus, bulk/cascade status updates, and create-as-active/generic activation paths. Asserts every such path funnels through the governed writer/envelope and that only ClaimShipment and unblock-to-active may create an active shipment (all other create-as-active/activation paths refuse). For the exported generic `MoveShipmentStatus` block/unblock refusal specifically, it asserts the top-level refusal returns `blerrors.ErrShipmentBlockedRequiresEnvelope` for BOTH directions (the sentinel is DECLARED by Rd 174.052-T so this harness compiles; that assertion portion is turned GREEN early by the R4g pre-edge guard 174.054-T which lands the guard BEHAVIOR, before the R5/R6 table edges land). Domain: tests. AC: (1) tests COMPILE against the Rd declarations and FAIL on behavior until rewiring lands; (2) governed-refusal asserted for each bypass path (MoveShipmentStatus block/unblock refusal via the R4g guard); (3) create-as-active refused outside claim/unblock; (4) POSITIVE NON-SHIPMENT CONTROLS for every POLYMORPHIC/GENERIC guarded surface that can legitimately receive a non-shipment artifact — public `WriteArtifactFile` boundary, private lower writer, generic move/update, bulk/cascade status updates, and create-as-active/generic activation paths — assert that an ordinary non-shipment artifact (task/member) status transition on that SAME surface is PRESERVED (succeeds unchanged), proving each guard scopes strictly on `ArtifactType=="shipment"` and no over-broad, surface-specific guard breaks ordinary task/member transitions; these controls MUST cover every polymorphic/generic guarded surface enumerated here, not only `UpdateArtifact`. (5) SHIPMENT-SPECIFIC SAME-SURFACE CONTROLS for the shipment-only `MoveShipmentStatus` entry point — which is inherently shipment-scoped and cannot legitimately receive a non-shipment artifact, so NO non-shipment success path is asserted or required through it: assert (a) shipment-only type enforcement — where the surface is callable with a non-shipment ID, a non-shipment ID is REJECTED / fails closed; (b) refusal of governed `blocked`/unblock bypasses (top-level `blerrors.ErrShipmentBlockedRequiresEnvelope` refusal for BOTH directions via the R4g guard 174.054-T); and (c) PRESERVATION of every otherwise-allowed shipment status transition on this surface EXCEPT that create-as-active/activation remains restricted to `ClaimShipment` and confirmed unblock-to-active per the plan (i.e., `queued→active` via `MoveShipmentStatus` is NOT an allowed activation path). <4 scenario groups (positive and same-surface controls are added as assertions within the existing bypass-path scenario groups; no new group). Depends Rd (174.052-T).

Wave-3 verification-ownership amendment (2026-09-21): AC (4) added to close cycle-4 review P1 finding 2 — non-shipment positive controls previously covered only `UpdateArtifact`, leaving surface-specific over-broad guards able to break ordinary task/member transitions undetected. Task ID, dependencies, priority, status, shipment membership, and the red-deliverable-contract (green_maker_tasks/close-wave) are UNCHANGED.

Wave-3 fresh-review contract correction (2026-09-21): the positive-non-shipment-control matrix in AC (4) is NARROWED to the POLYMORPHIC/GENERIC surfaces that can legitimately receive a non-shipment artifact (public `WriteArtifactFile`, private lower writer, generic move/update, bulk/cascade, create-as-active/generic activation). `MoveShipmentStatus` is REMOVED from that success-path matrix because it is a shipment-only entry point through which no non-shipment artifact can flow — a literal non-shipment success path there is impossible, substituting `UpdateArtifact` does not satisfy the literal contract, and permitting `queued→active` via `MoveShipmentStatus` to manufacture such a path would conflict with activation being restricted to claim/confirmed unblock. New AC (5) instead requires the CORRECT shipment-specific SAME-SURFACE controls for `MoveShipmentStatus` (shipment-only type enforcement, governed blocked/unblock bypass refusal, and preservation of allowed shipment transitions except claim/unblock-restricted activation). All other rev8 corrections are unchanged. Task ID, dependencies, priority, status, shipment membership, and the red-deliverable-contract (green_maker_tasks/close-wave) remain UNCHANGED.

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Writer/bypass BEHAVIOR RED harness pinning the governed-writer boundary and bypass closure; it compiles against the declaration-only stubs (Rd 174.052-T) and its assertions turn green across waves — the MoveShipmentStatus block/unblock refusal portion via the R4g pre-edge guard (174.054-T)@wave4, and the governed writer core (R4 174.042-T) plus the two remaining bypass-closure halves (R7a 174.045-T write-paths and R7b 174.051-T create-as-active/activation)@wave7 — so its TestUR2_ functions are behavior-red by design when it completes.
red_selector_command: go test -count=1 -run '^TestUR2_' ./internal/core
green_maker_tasks: 174.054-T, 174.042-T, 174.045-T, 174.051-T
green_maker_closes_wave: 7
```
<!-- END:red-deliverable-contract -->