---
artifact_type: task
created_at: 2026-09-15T20:44:24.1253102Z
dependencies:
    - 174.052-T
id: 174.040-T
parent_id: 174-F
priority: high
status: active
title: 'R2: Writer/bypass BEHAVIOR RED harness'
updated_at: 2026-09-20T23:26:17.3168612Z
---

BEHAVIOR RED harness for the governed writer boundary and bypass closure. Depends on the declaration-only stubs (Rd 174.052-T) so it COMPILES against the declared block/unblock symbols AND the declaration-only `blerrors.ErrShipmentBlockedRequiresEnvelope` sentinel (also declared by Rd 174.052-T) and fails on BEHAVIOR (not compile). Targets public WriteArtifactFile boundary, the private lower writer, generic move/update, MoveShipmentStatus, bulk/cascade status updates, and create-as-active/generic activation paths. Asserts every such path funnels through the governed writer/envelope and that only ClaimShipment and unblock-to-active may create an active shipment (all other create-as-active/activation paths refuse). For the exported generic `MoveShipmentStatus` block/unblock refusal specifically, it asserts the top-level refusal returns `blerrors.ErrShipmentBlockedRequiresEnvelope` for BOTH directions (the sentinel is DECLARED by Rd 174.052-T so this harness compiles; that assertion portion is turned GREEN early by the R4g pre-edge guard 174.054-T which lands the guard BEHAVIOR, before the R5/R6 table edges land). Domain: tests. AC: (1) tests COMPILE against the Rd declarations and FAIL on behavior until rewiring lands; (2) governed-refusal asserted for each bypass path (MoveShipmentStatus block/unblock refusal via the R4g guard); (3) create-as-active refused outside claim/unblock. <4 scenario groups. Depends Rd (174.052-T).

<!-- BEGIN:red-deliverable-contract -->
```text
red_deliverable: true
red_deliverable_reason: Writer/bypass BEHAVIOR RED harness pinning the governed-writer boundary and bypass closure; it compiles against the declaration-only stubs (Rd 174.052-T) and its assertions turn green across waves — the MoveShipmentStatus block/unblock refusal portion via the R4g pre-edge guard (174.054-T)@wave4, and the governed writer core (R4 174.042-T) plus the two remaining bypass-closure halves (R7a 174.045-T write-paths and R7b 174.051-T create-as-active/activation)@wave7 — so its TestUR2_ functions are behavior-red by design when it completes.
red_selector_command: go test -count=1 -run '^TestUR2_' ./internal/core
green_maker_tasks: 174.054-T, 174.042-T, 174.045-T, 174.051-T
green_maker_closes_wave: 7
```
<!-- END:red-deliverable-contract -->