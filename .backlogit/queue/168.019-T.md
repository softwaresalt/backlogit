---
artifact_type: task
created_at: 2026-09-17T04:07:53.8017773Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 168.017-T
id: 168.019-T
parent_id: 168-F
priority: high
status: queued
title: Trust-anchor denied-mutation audit-write-failure + recovery
updated_at: 2026-09-17T04:07:53.8017773Z
---

Trust-anchor DENIED-mutation audit-write-failure + recovery reconciliation (behavior). Carved from 168.017-T per plan-review cycle-2 item 4 (denied-path audit semantics + 3-scenario width). Complements 168.017-T (successful-path outcomes). A DENIED mutation (external-pin rejection) makes NO config/status change but MUST still persist a denial audit event (F2); if that denial audit-write fails, the denial itself stands (fail-closed preserved, no state change) and an audit-gap recovery marker is written so the recovery pass re-emits the missing denial audit. Write outcome for this path: ErrAuditNotPersisted (state definitively unchanged, audit missing) is RETRY-SAFE for the audit only; never a state retry. Compensation/retry: recovery pass re-emits the denial audit idempotently keyed on the rejected request; no state compensation is needed because no state changed. Domain: core (internal/core). TDD: RED behavior harness first with an injected denial-audit-write failure. Depends on 168.017-T. Executable scenarios (3): (a) a denied mutation persists a denial audit event on the success path; (b) an injected denial-audit-write failure yields ErrAuditNotPersisted with an audit-gap recovery marker and NO state change; (c) the recovery pass re-emits the missing denial audit idempotently. Build/test: go build ./cmd/backlogit; go test ./internal/core/ -run TestTrustAnchorDeniedAuditRecovery. AC: (1) RED test first, observed failing (compiling harness); (2) a denied mutation persists a denial audit; (3) a denial-audit-write failure returns ErrAuditNotPersisted with an audit-gap marker and no state change; (4) the recovery pass re-emits the missing denial audit idempotently; (5) tests GREEN.