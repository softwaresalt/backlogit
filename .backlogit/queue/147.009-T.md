---
artifact_type: task
created_at: 2026-08-24T19:02:58.3719697Z
dependencies:
    - 147.008-T
id: 147.009-T
labels:
    - checkpoint
    - core
    - plan-u5
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U5: widen QuarantineCheckpoint classification (deadlock closure)'
updated_at: 2026-08-24T19:02:58.3719697Z
---

Plan unit **U5**. Widen `QuarantineCheckpoint` classification to close the deadlock.

Files: `internal/core/checkpoint_disposition.go`, `internal/core/checkpoint_disposition_test.go`.

**This unit is mandatory, not optional.** Refusing on both mutation verbs without widening quarantine strands valid-but-non-conforming files with no disposition path at all.

Extend the in-memory classification so `validTarget` is `parse OK && validate OK && conformance OK`. Only a target satisfying all three is refused with `ErrCheckpointUseAbandon`. The verbatim `moveNoReplace` path, audit-before-move ordering, sidecar upsert, and `MutationEnvelope` compensation are unchanged.

The paired accept/refuse assertion is the single highest-value assertion in the plan. If it cannot be made to pass, **halt** rather than weakening it.

<!-- BEGIN:acceptance-criteria -->
- Expressed as one table so the paired assertions cannot be removed independently
- A valid-but-non-conforming **active** document is **accepted** by quarantine and **refused** by abandon in the same table row
- A fully conforming active document is refused by quarantine with `ErrCheckpointUseAbandon`
- A quarantined non-conforming document's archived bytes are byte-identical to the original
- Expected red: row 1's accept-half fails
- **State-conflict regression guards (already-green, pinned from retired U5b)**: a `status:"resolved"` conforming document is refused by `AbandonCheckpoint` with `errors.Is(err, ErrCheckpointNotActive)` and by `QuarantineCheckpoint` with `errors.Is(err, ErrCheckpointUseAbandon)` — both are pre-existing shipped behaviour that this feature does not change, colocated so a future edit cannot silently decouple the two surfaces' state-class agreement. These rows are green on landing and do not contribute to U5's red gate; they pin the out-of-scope state-conflict class named in the Decisions and Rationale (I3 table, row 3) as a tested invariant rather than an assumed one
<!-- END:acceptance-criteria -->