---
artifact_type: task
created_at: 2026-08-24T19:02:58.3719697Z
dependencies:
    - 147.008-T
id: 147.009-T
labels:
    - checkpoint
    - core
    - plan-u5
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U5: widen QuarantineCheckpoint classification (deadlock closure)'
updated_at: 2026-08-24T19:02:58.3719697Z
---

Plan unit **U5**. Widen `QuarantineCheckpoint` classification to close the deadlock.

Files: `internal/core/checkpoint_disposition.go`, `internal/core/checkpoint_disposition_test.go`.

**This unit is mandatory, not optional.** Refusing on both mutation verbs without widening quarantine strands valid-but-non-conforming files with no disposition path at all.

Extend the in-memory classification so `validTarget` is `parse OK && validate OK && conformance OK`. Only a target satisfying all three is refused with `ErrCheckpointUseAbandon`. The verbatim `moveNoReplace` path, audit-before-move ordering, sidecar upsert, and `MutationEnvelope` compensation are unchanged.

The paired accept/refuse assertion is the single highest-value assertion in the plan. If it cannot be made to pass, **halt** rather than weakening it.

Red verification: `go test -count=1 -run '^TestU5_' ./internal/core`.

<!-- BEGIN:acceptance-criteria -->
- Expressed as one table so the paired assertions cannot be removed independently. **Two independent scenarios** (PR #377 cycle-16 width normalization)
- **Scenario 1** — a valid-but-non-conforming **active** document is **accepted** by quarantine and **refused** by abandon in the same table row, and the archived bytes are **byte-identical to the pre-quarantine original** as a postcondition of that same row
- **Scenario 2** — a **conforming active** document is refused by quarantine with `ErrCheckpointUseAbandon`
- Expected red: scenario 1's accept-half fails; its byte-identity postcondition is green on landing. Scenario 2 is a declared regression guard. P-004 is satisfied by scenario 1
- Byte-identity is **not** a separate scenario. Cycle 15 counted it as one, which took this task to four effective flows. It is the accept-half's postcondition and is strictly stronger asserted on the row that produced the archive
- The `status: "resolved"` state-conflict rows are **withdrawn** (see below) and must not be reintroduced here
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
**Withdrawn in PR #377 cycle 16: the `status: "resolved"` state-conflict guards.** Cycle 10 absorbed three regression rows from retired `147.010-T` / U5b into this task, and cycle 15 re-presented them as extra rows of scenario 2. They assert the **out-of-scope state-conflict class** the origin decision excluded, on a second document state this task's production delta does not touch, and counting them kept this task at four effective flows in every review cycle from 10 through 15.

They are removed and recorded as follow-up stash `6FA45E69`. Nothing in the plan now depends on them: I3 table row 3 stays documented as pre-existing behaviour, and `147.017-T` / U9's design-doc rewrite still carries the `active`-only scope qualifier. Do not re-add `status: "resolved"` rows to this task to "restore coverage" — that coverage belongs to a unit that owns the state-conflict class.
<!-- END:implementation-notes -->