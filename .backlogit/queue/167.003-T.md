---
artifact_type: task
created_at: 2026-09-06T00:58:33.454741Z
id: 167.003-T
parent_id: 167-F
priority: critical
status: queued
title: Atomic core primitive DECLARATIONS + AST harness (U1 decl)
updated_at: 2026-09-06T04:41:33.1839773Z
---

U1 DECLARATIONS (source-shape harness FIRST, declaration SECOND per workflow-policies.md:123-143; ROOT task - no deps). Step 1: commit a RED go/ast SOURCE-SHAPE harness (inspects source, does NOT call the symbols, so it compiles against the declaration-free tree) that FAILS on the declaration-free tree. Step 2: add the ShipmentShippedReconcileRequest/ShipmentShippedReconcileResult STRUCT types (+ the branch enum + any interface) AND the SIGNATURES of every reconcile function (classifyShipmentReconcileState, ReconcileShipmentToShipped, the handle-relative atomic writer, the item-log lock, the fsync append primitive) with MINIMAL panic("not implemented") bodies - turning the source-shape harness GREEN. Per policy the panic bodies are DECLARATIONS gated by their preceding shape harness (NOT production stubs admitted with no failing test, and NOT a stub-first behavior pattern). Each function's BEHAVIOR harness (RED against the panic body) + real implementation land in a STRICTLY LATER wave in its OWNER task: classifier 167.010-T, ReconcileShipmentToShipped 167.008-T, writer 167.006-T, item-log lock 167.011-T, fsync appender 167.007-T. The function SIGNATURES reference ONLY this task's own request/result/branch types + stdlib (context.Context, *Workspace, []byte, error) - the U3 event const and reconcile.trusted_refs config are used only inside the later-landed bodies, NOT the signatures - so this task stays a dependency-free ROOT (no cycle). Does NOT declare the U2 error sentinels (167.001-T) or the EventShipmentReconciledShipped const (167.002-T) - no duplicate declarations. Files internal/core/shipment_reconcile.go (declarations). Domain: code. See plan U1 decomposition. ROOT task (no dependencies).