---
artifact_type: task
created_at: 2026-09-14T01:52:56.2811241Z
id: 173.007-T
labels:
    - harness-ready
parent_id: 173-F
priority: high
references:
    - docs/exec-plans/2026-09-13-shipment-claim-scheduler-reconciliation-plan.md
status: queued
title: 'RED harness: claim-marker external read surface'
updated_at: 2026-09-14T06:48:30.7479857Z
---

U0b [CC0EBB59] RED harness + characterization (predecessor). Write the read-surface tests BEFORE U2, asserting the pinned `scheduler_baseline_claim` key on BOTH transports independently (different code paths / freshness). TWO are genuine RED (they fail pre-impl because the marker is not yet produced): (1) the marker appears in the frontmatter-backed CLI `get --format json` projection for a claim-activated item; (2) it appears identically in the DB-backed MCP `get_item` projection for the same item. TWO are PASSING characterization/regression baselines, NOT required RED: (3) the marker is absent for an organic-active item (organic items carry no marker before AND after); (4) a consumer ignoring the marker (absent-marker consume path) is unchanged. Domain: tests. No deps. <=4 scenarios. AC: the marker-projection scenarios (1) and (2) fail against the pre-implementation read path (genuine RED) and prove the marker materializes on both the CLI (frontmatter) and MCP (DB) surfaces so they cannot silently drift; the absence/ignore scenarios (3) and (4) pass as characterization/regression baselines both before and after and MUST NOT be treated as required RED.

<!-- BEGIN:red-deliverable-contract -->

```text
red_deliverable: true
red_deliverable_reason: Read-surface RED harness asserting the scheduler_baseline_claim marker on both transports independently - scenario (1) the marker appears in the frontmatter-backed CLI get --format json projection and scenario (2) it appears identically in the DB-backed MCP get_item projection for a claim-activated item, both failing pre-implementation because the marker is not yet produced; the absence and ignore scenarios (3) and (4) are passing characterization baselines, not required RED. Green-makers 173.001-T (U1 persists the marker) and 173.002-T (U2 verifies both projections plus the same-operation index-freshness upsert) turn it green in a later wave; no in-wave step balances it green.
red_selector_command: go test -count=1 -run '^TestU0b_ClaimMarkerReadSurface$' ./internal/cli ./internal/mcp
green_maker_tasks: 173.001-T, 173.002-T
green_maker_closes_wave: 3
```

<!-- END:red-deliverable-contract -->