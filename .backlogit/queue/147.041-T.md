---
artifact_type: task
created_at: 2026-08-25T16:56:59.8182986Z
dependencies:
    - 147.026-T
    - 147.033-T
    - 147.022-T
    - 147.025-T
    - 147.029-T
    - 147.027-T
id: 147.041-T
labels:
    - checkpoint
    - verification
    - plan-u10c
    - harness-exempt
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U10c: runtime verification of context-duplicate parity and abandoned-resolve'
updated_at: 2026-08-25T20:44:00Z
---

Plan unit **U10c** (partition 5). Owns the runtime coverage that cycles 14-16 left orphaned.

Files: none beyond appending to `docs/closure/2026-08-checkpoint-disposition-runtime-verification.md`. Runs inside the scratch workspace 147.019-T / U10 created.

Created in PR #377 cycle 17 to close gate finding **H3**. Two behaviours sat in a plan table row that no unit owned, so no task carried them, no dependency scheduled them, and nothing forced them to run.

147.019-T / U10''s six-point execution contract applies unchanged, including the cycle-17 step-5 operation-class split: in-place rewrite versus archive move versus declared intentional collision.

Rows:

1. **Exact-duplicate `context` member, cross-surface** - refused by `resolve` **and** `abandon` with `checkpoint_non_conforming` naming `duplicate:context.<key>` in a raw bounded `unknown_fields` array; reported `needs_quarantine: true` by `list` **and** `get` with a matching `non_conforming_fields.paths` list; **accepted** by `quarantine`; bytes SHA-identical across both refusals.
2. **Fold variant aliasing a modeled field, plus the open-namespace guard** - `shipment_id` + `Shipment_Id` refused the same way; a sibling carrying distinct unmodeled `foo` + `Foo` **resolves successfully** with both keys surviving; a fixture whose only routing member is spelled `Context` with an inner exact duplicate is refused, pinning 147.033-T / U2h at runtime.
3. **Abandoned-resolve handler mapping** - invoking `backlogit_resolve_checkpoint` on an already-abandoned document returns `validation_failed`, **not** `"error":"internal"`, proving 147.029-T / U7e''s row reaches the handler.

Teardown of `docs/scratch/checkpoint-verification/` is owned by this task and runs only after all three rows pass.

**Test-lifecycle classification**: `harness-exempt: verification-only`. No Go test function is scaffolded, no RED is claimed, and there are no green-step guards; the gate is the recorded runtime evidence appended to the tracked closure artifact.