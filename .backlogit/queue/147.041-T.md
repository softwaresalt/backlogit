---
artifact_type: task
created_at: 2026-08-25T16:56:59.8182986Z
dependencies:
    - 147.026-T
    - 147.033-T
    - 147.022-T
    - 147.025-T
    - 147.029-T
    - 147.027-T
id: 147.041-T
labels:
    - checkpoint
    - verification
    - plan-u10c
    - harness-exempt
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U10c: runtime verification of context-duplicate parity and abandoned-resolve'
updated_at: 2026-08-25T20:44:00Z
---

Plan unit **U10c** (partition 5). Owns the runtime coverage that cycles 14-16 left orphaned.

Files: none beyond appending to `docs/closure/2026-08-checkpoint-disposition-runtime-verification.md`. Runs inside the scratch workspace 147.019-T / U10 created.

Created in PR #377 cycle 17 to close gate finding **H3**. Two behaviours sat in a plan table row that no unit owned, so no task carried them, no dependency scheduled them, and nothing forced them to run.

147.019-T / U10''s six-point execution contract applies unchanged, including the cycle-17 step-5 operation-class split: in-place rewrite versus archive move versus declared intentional collision.

Rows:

1. **Exact-duplicate `context` member, cross-surface** - refused by `resolve` **and** `abandon` with `checkpoint_non_conforming` naming `duplicate:context.<key>` in a raw bounded `unknown_fields` array; reported `needs_quarantine: true` by `list` **and** `get` with a matching `non_conforming_fields.paths` list; **accepted** by `quarantine`; bytes SHA-identical across both refusals.
2. **Fold variant aliasing a modeled field, plus the open-namespace guard** - `shipment_id` + `Shipment_Id` refused the same way; a sibling carrying distinct unmodeled `foo` + `Foo` **resolves successfully** with both keys surviving; a fixture whose only routing member is spelled `Context` with an inner exact duplicate is refused, pinning 147.033-T / U2h at runtime.
3. **Abandoned-resolve handler mapping** - invoking `backlogit_resolve_checkpoint` on an already-abandoned document returns `validation_failed`, **not** `"error":"internal"`, proving 147.029-T / U7e''s row reaches the handler.

Teardown of `docs/scratch/checkpoint-verification/` is owned by this task and runs only after all three rows pass.

**Test-lifecycle classification**: `harness-exempt: verification-only`. No Go test function is scaffolded, no RED is claimed, and there are no green-step guards; the gate is the recorded runtime evidence appended to the tracked closure artifact.

**Harness exemption contract (P-002.1 / P-002.3, machine-readable).** Static intake reads these
keys; Ship Step 4.1a runs `exempt_verification_command` once against the pre-work tree and MUST
observe it fail before this task may be claimed.

<!-- BEGIN:harness-exemption-contract -->

```text
harness_exemption_class: verification-only
harness_exemption_reason: This unit executes and records runtime evidence for behaviour delivered by its prerequisites; it lands no production or test code, so there is nothing on its own delta that an assertion could fail against.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command '$f = "docs/closure/2026-08-checkpoint-disposition-runtime-verification.md"; if (-not (Test-Path $f)) { exit 1 }; $t = Get-Content $f; if (@($t | Select-String -Pattern "^evidence_row: unit=U10c row=\d+ filename=\S+ sha256=[0-9a-f]{64} state=\S+ destination=\S+ outcome=(refused|accepted|unchanged)$").Count -lt 3) { exit 1 }; foreach ($s in @("^evidence_scalar: unit=U10c abandoned_resolve_code=validation_failed$","^evidence_scalar: unit=U10c teardown_completed=true$")) { if (-not ($t | Select-String -Pattern $s -Quiet)) { exit 1 } }; exit 0'
exempt_precondition: must-fail-before-deliverable
```

<!-- END:harness-exemption-contract -->

**Durable evidence manifest (cycle 23).** This unit appends its own block to the same tracked
closure artifact `docs/closure/2026-08-checkpoint-disposition-runtime-verification.md`, using the
grammar 147.019-T / U10 establishes, scoped to `unit=U10c`:

```text
evidence_row: unit=U10c row=<n> filename=<bare-filename> sha256=<64-hex> state=<classified-state> destination=<bare-destination|none> outcome=<refused|accepted|unchanged>
evidence_scalar: unit=U10c abandoned_resolve_code=validation_failed
evidence_scalar: unit=U10c teardown_completed=true
```

Exactly the three rows above are required — the cross-surface exact-duplicate `context` member,
the fold-variant aliasing row with its open-namespace guard, and the abandoned-resolve handler
mapping. `abandoned_resolve_code=validation_failed` pins row 3''s discriminating result (it must
not be `internal`), and `teardown_completed=true` records that the scratch workspace teardown this
task owns ran only after all three rows passed.

**Why this command is a gate.** The probe fails while the artifact is absent, while the `U10c`
block is under-populated, and when either declared scalar is missing — so a created-but-empty
file, a partial run, or a teardown performed before the rows pass cannot satisfy it.

**Baseline probe (cycle 23, worktree HEAD `e8b974e`)**: exit **1**, as declared —
`docs/closure/2026-08-checkpoint-disposition-runtime-verification.md` does not exist.

**Class delta surface (P-002.4)**: `docs/closure/2026-08-checkpoint-disposition-runtime-verification.md`
only. Zero non-test `*.go` files may change — one that does is `EXEMPT_BEHAVIOR_NO_OWNER`.