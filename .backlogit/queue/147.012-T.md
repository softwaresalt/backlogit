---
artifact_type: task
created_at: 2026-08-24T19:03:48.1151581Z
dependencies:
    - 147.011-T
    - 147.030-T
    - 147.016-T
    - 147.038-T
    - 147.032-T
id: 147.012-T
labels:
    - checkpoint
    - events
    - plan-u6b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U6b: GetCheckpoint reports conformance and agrees with ListCheckpoints'
updated_at: 2026-08-25T17:29:35.9702315Z
---

Plan unit **U6b** (partition 4). `GetCheckpoint` agrees with `ListCheckpoints`.

Files: `internal/events/checkpoint_lifecycle.go`, `internal/events/checkpoint_lifecycle_test.go`.

**Cycle-17 change: the carrier is declared by 147.038-T / U15, not here.** This task is the **production delta only** - it populates `Conforming`, `NeedsQuarantine`, `RemediationIntent`, and `NonConformingFields` on the `CheckpointReadResult` U15 declared. Moving the declaration into partition 3 is what lets 147.016-T / U8b compile and fail honestly before any implementation lands.

`valid` retains its existing meaning (schema-valid) and is **not** repurposed; conformance is a distinct field so no existing consumer''s contract silently changes. Schema-invalid documents keep returning `ErrCheckpointInvalid` **unwrapped** - a read is not a rewrite and there is nothing to refuse.

**Offender projection is raw.** `NonConformingFields` is the `BoundedFieldPathSet` recovered with `errors.As` from the conformance verdict and produced by 147.030-T / U1b''s `BoundedFieldPaths()` - never from the raw `Fields` slice and never re-capped here. Its `Paths` carry raw offender paths; quoting belongs only to 147.031-T / U1c''s human rendering.

**Remediation is structured**: the result carries `RemediationIntent` (147.032-T / U1d), not a command string. Nothing in `internal/events` renders a shell command.

Red verification: `go test -count=1 -run '^TestU6b_' ./internal/events`.

<!-- BEGIN:acceptance-criteria -->
- A valid-but-non-conforming file returns `valid: true, conforming: false, needs_quarantine: true`, a `RemediationIntent` naming verb `quarantine` and approval class `A4c`, and a `NonConformingFields.Paths` slice naming the offenders in raw, unquoted form
- A conforming file returns `conforming: true`, a nil `RemediationIntent`, and an empty `NonConformingFields.Paths` with `Truncated: false`
- The file is byte-unchanged after get
- Red harness (P-002/P-004 gate): two scaffolded `TestU6b_` functions (cases 1 and 2), which fail because the 147.038-T declaration leaves every projected field zero-valued
- Green-step guards (`TestU6bGuard_*`, landing with the implementation): case 3, which pins `GetCheckpoint`'s read-only contract
<!-- END:acceptance-criteria -->