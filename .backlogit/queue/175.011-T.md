---
artifact_type: task
created_at: 2026-09-18T00:00:06.6921888Z
dependencies:
    - 175.001-T
id: 175.011-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U11 - staticcheck remediation
updated_at: 2026-09-18T05:00:40.7786663Z
---

Remediate ALL golangci-lint staticcheck findings AND serve as the staticcheck/errcheck overlap-inventory owner. U11 runs IMMEDIATELY AFTER U1 (depends on `175.001-T` ONLY) and BEFORE every errcheck unit (U4-U10, U13), so overlap ownership is determined BEFORE any errcheck code edit begins.

Exactly-one-owner contract (resolves Copilot cycle-2 exactly-one-owner + suppressed-U11-criterion findings):
- FIRST run `golangci-lint run` and enumerate (a) the specific staticcheck check IDs and (b) the COMPLETE set of files staticcheck flags.
- U11 EXCLUSIVELY OWNS every staticcheck-flagged file for ALL of its findings — both staticcheck findings AND any errcheck findings in that same file — EXCEPT the two U3-owned files (`internal/faultline/testdata/parity_v1.golden.json` and `internal/faultline/evidence_conformance_test.go`), which are pre-assigned to U3 and therefore excluded from U11's inventory. Consequently NO file is ever owned by both U11 and an errcheck unit: a staticcheck-flagged file is owned by U11 alone; a non-flagged file stays with its package/residual errcheck unit.
- Ownership is decided UP FRONT by this inventory. U11 does NOT acquire ownership of a file merely because a dependency serializes it, and an errcheck unit never edits a U11-owned file merely because it runs after U11. Serialization is NOT an ownership grant.

Owned handoff artifact (resolves the P1-3 overlap-handoff finding) — an EXPLICIT U11-OWNED SHIP DELIVERABLE, generated at U11 execution time, NOT pre-existing during Stage:
- Exact repository path (reserved to U11): `docs/closure/175-U11-overlap-inventory.md`.
- Provenance: this file DOES NOT EXIST during staging and MUST NOT be treated as pre-existing; Ship generates it while executing U11. If the path is absent at U11 start that is the expected RED state.
- Stable schema: a docline-valid markdown file (frontmatter `title`, `source`, `doc_type: closure`, `chunk_strategy: h1-h2-h3`, `schema_version: "1.0"`) whose machine-readable payload is a SINGLE fenced ```text block delimited by the exact markers `# BEGIN OVERLAP-INVENTORY` and `# END OVERLAP-INVENTORY`. Between the markers: one repository-relative POSIX path per line (forward slashes, exact case, no leading `./`, no trailing whitespace), the COMPLETE set of staticcheck-flagged files with the two U3-owned files removed.
- Normalized path convention: repo-root-relative, forward-slash separators, LF line terminators, NFC, no duplicates.
- Stable ordering: byte-ascending (C locale / ordinal) sort of the path strings, so the artifact is reproducible and diff-stable across runs.
- Deterministic generation command (documented in the artifact body): `golangci-lint run --out-format=json | jq -r ''.Issues[] | select(.FromLinter=="staticcheck") | .Pos.Filename'' | grep -vxF -e internal/faultline/testdata/parity_v1.golden.json -e internal/faultline/evidence_conformance_test.go | sort -u` — the emitted list is written verbatim between the markers.
- Ownership: U11 EXCLUSIVELY generates and owns this artifact; no other unit writes it.
- Consumption contract: every errcheck leaf (U4-U10, U13) reads the fenced payload of `docs/closure/175-U11-overlap-inventory.md` and EXCLUDES every path listed from its own scope. An errcheck unit that finds the artifact absent must treat that as a blocking dependency violation (U11 not complete), never as "no overlap".

Owned harness file (resolves the P1-2 harness-ownership finding): U11 exclusively owns and authorizes creation of the deterministic staticcheck-regression harness `<pkg>/staticcheck_convergence_175_test.go` in EACH staticcheck-flagged package that receives a behavior-relevant fix (e.g. an SA1019/SA4006 fix). The filename segment `staticcheck_convergence_175_test.go` is RESERVED to U11 in every package; no other unit may create that exact path, and it never collides with an errcheck unit's `errcheck_convergence_175_test.go` in the same package. These `*_test.go` harness files are EXCLUDED from the <3 implementation-file inventory (which counts only the staticcheck-flagged non-test files U11 edits).

Fix strategy: choose fix-vs-nolint per category — SA1019 (deprecated API) and SA4006 (unused write) are behavior-relevant and must NOT be uniformly suppressed. `//nolint:staticcheck` only with a justified inline reason. Any errcheck finding inside a U11-owned file is fixed with the SAME per-category errcheck strategy as U4-U10 (named-return + errors.Join for a writable Close, checked write/short-write helper, justified `_ =` for genuinely discardable returns, `fmt.Errorf("...: %w", err)` propagation).

Acceptance criteria:
- Ordering: U11 depends on U1 (`175.001-T`) ONLY and runs before U4-U10/U13; the errcheck units depend on U11 (not the reverse). U12 remains the terminal sink.
- Scope (files): U11 owns exactly the staticcheck-flagged files (expected small), minus the two U3-owned files. It fixes EVERY finding (staticcheck + errcheck) in those files, so no errcheck unit ever needs to touch them.
- Owned handoff artifact (deterministic Ship deliverable): `docs/closure/175-U11-overlap-inventory.md` is generated during U11 execution at the exact reserved path, with the schema/ordering/normalization/generation-command above; it is NOT present during Stage; downstream errcheck leaves consume it by exact path and exclude every listed file.
- Owned harness (required): the reserved `<pkg>/staticcheck_convergence_175_test.go` RED-first regression drives each behavior-relevant staticcheck fix green; the harness path is exclusive to U11 and excluded from the implementation-file count.
- Exactly-one-owner: no file is edited by both U11 and an errcheck unit; serialization-as-ownership is explicitly disallowed.
- Verification (zero-finding): `golangci-lint run` reports 0 staticcheck; `gofmt -l` on touched files empty; affected packages still build and existing tests pass.
- Existing test expectations: all current tests for touched files pass unchanged; SA1019/SA4006 fixes preserve behavior.
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. Where a staticcheck OR errcheck fix in a U11-owned file changes reachable behavior, add a failing (RED) test FIRST in the owned harness; the harness-architect owns authoring the failing harness/test that compiles before the fix and fails on an assertion (P-002/P-004), driven green by the fix. Mechanical suppressions/renames/captures claim NO mechanical/generic/closure-time harness exemption, exempt contract, or Principle II deviation.