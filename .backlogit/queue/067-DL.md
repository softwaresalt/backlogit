---
artifact_type: deliberation
created_at: 2026-09-23T19:25:41.0287402Z
custom_fields:
    linked_stash_id: 18E587A0
    linked_stash_kind: bug
    linked_stash_priority: high
    linked_stash_text: |-
        DEFERRED SCOPE EXPANSION
        Expansion: add a path-specific .gitattributes LF-materialization entry so internal/faultline/testdata/parity_v1.golden.json checks out LF on Windows (core.autocrlf=true), unblocking the mandatory local full suite whose pre-existing test TestU4aBehaviorCanonicalByteStable byte-compares a.Canonical() LF output to that golden.
        C1 rationale (OUT OF SCOPE): fixing the fixture byte-stability / line-ending materialization contract is NOT completing the exact change already authorized for 155-S (shipment blocked-lifecycle status + 174.064-T lock-order). It is a DIFFERENT contract surface owned by the Faultline golden-parity harness task 156.006-T (Fault-line evidence contract BEHAVIOR - canonical/decode/validate + golden), not 174-F lock-order work. same-file / same-PR / same-subsystem do not make it in-scope; it fails the same-contract-surface test.
        Refs: C1-owner=156.006-T (archived); faultline-feature=156-F (archived); sibling-corrective=174.064-T; covering-feature=174-F (active); shipment=155-S (active); checkpoint=.backlogit/checkpoints/checkpoint-20260923-191544.json (active, ship, quality-gate-blocked); branch-HEAD=3554e3c132feb441c798247b44071429776d5656; failing-test=internal/faultline/evidence_conformance_test.go:TestU4aBehaviorCanonicalByteStable; golden=internal/faultline/testdata/parity_v1.golden.json.
        requires deliberation: yes
        kind: bug (environment/config line-ending materialization); provisional priority: high
        Discovery: environment-only full-suite blocker (WAVE_RED_DELIVERABLE / mandatory local full suite; no Linux CI evidence; no waiver). Not a product regression; Windows core.autocrlf=true materializes fixture CRLF while repo blobs are LF and .gitattributes carries only `* text=auto`. Smallest root cause: add `internal/faultline/testdata/parity_v1.golden.json text eol=lf`.
id: 067-DL
priority: high
status: queued
title: Fixture LF materialization contract for Faultline parity golden (155-S full-suite unblock)
updated_at: 2026-09-23T19:25:41.0287402Z
---

# Fixture LF materialization contract for Faultline parity golden (155-S full-suite unblock)

## Problem Frame

<!-- BEGIN:problem-frame -->
Environment-only blocker for 155-S final readiness. Pre-existing test internal/faultline/evidence_conformance_test.go:TestU4aBehaviorCanonicalByteStable byte-compares a.Canonical() LF output to internal/faultline/testdata/parity_v1.golden.json. Repo blobs are LF, but Windows core.autocrlf=true materializes the fixture as CRLF because .gitattributes carries only * text=auto. Test/fixture/.gitattributes are unchanged by 155-S; no Linux CI evidence and no waiver exist, and the local full suite is mandatory. The same-operation circuit is OPEN: no test re-run or fourth attempt is permitted before a reviewed correction and explicit later operation authorization. P-021 C1: OUT OF SCOPE for 174-F lock-order work; the fixture byte-stability contract is owned by archived Faultline task 156.006-T under archived feature 156-F.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
1. CHOSEN - path-specific .gitattributes entry internal/faultline/testdata/parity_v1.golden.json text eol=lf (repo-controlled, deterministic LF materialization; no test weakening; smallest blast radius). 2. REJECTED - normalize/strip CRLF inside the test before comparing: weakens the byte-stability contract the test exists to enforce (owned by 156.006-T), not a config fix. 3. REJECTED - global * text eol=lf: broad line-ending change touching unrelated fixtures/scripts; out of scope. 4. REJECTED - waiver / skip mandatory full suite: no waiver exists and local full suite is mandatory; non-compliant.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
Create the smallest corrective task adding the path-specific eol=lf materialization contract for the implicated Faultline golden (scoped to the implicated testdata path, not global). TDD/verification: git check-attr eol on the path resolves lf; working-tree byte assertion (no CRLF) after normalization; targeted TestU4aBehaviorCanonicalByteStable passes; canonical fixture contract preserved (golden bytes unchanged in-repo, only checkout EOL fixed). OWNERSHIP: 156-F/156.006-T are ARCHIVED (terminal); hosting live queued work under an archived feature would soft-reopen closed scope and risk hierarchical-ID ambiguity, so the corrective is parented to the ACTIVE covering feature 174-F with explicit C1 provenance to 156.006-T recorded, and added as an explicit single-task prerequisite member of active 155-S (governed AddItemToShipment; active not mutation-blocked).
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
(a) Scope eol=lf to only the implicated golden path vs the whole faultline testdata golden set -- decide NARROW (implicated path(s) only) to avoid broad change; implementer audits adjacent goldens but fixes only paths with a proven materialization defect. (b) Circuit disposition: after the correction commit changes repository state, the post-fix local full-suite invocation is a NEW separately-authorized final-gate operation at a new commit/workflow phase, NOT a retry/probe of the open pre-fix circuit; explicit operator authorization is still required immediately before that post-fix run if policy demands it.
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
Membership gates 155-S final readiness via dependency/order; do not reopen completed statuses; do not touch .gitattributes/fixture/test/source/checkpoint now (planning only). Branch HEAD 3554e3c1; active checkpoint checkpoint-20260923-191544.json untouched.
<!-- END:notes -->
