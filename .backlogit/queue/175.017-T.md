---
artifact_type: task
created_at: 2026-09-19T01:23:37.7586509Z
dependencies:
    - 175.002-T
id: 175.017-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/core/archive_durable_write_test.go (+1)'
updated_at: 2026-09-19T07:17:19.3623901Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (156-S / feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck
Package: ./internal/core
Owned source file(s) (2): internal/core/archive_durable_write_test.go, internal/core/size_compositions_batch_test.go
Owned baseline occurrences (5) — line:column shown are DIAGNOSTIC baseline coordinates only, NOT durable identity (golangci-lint v2.13.2, unbounded invocation; diagnostic table in docs/decisions/2026-09-17-baseline-lint-inventory.md). Durable ownership is the line-stable group-multiset model in the task-lint-contract below (stable key = path + linter + normalized message; per-task occurrence multiplicity with ordered-slice expected residual):
- internal/core/archive_durable_write_test.go:L77:C36 [errcheck] Error return value of `workspace.Close` is not checked
- internal/core/size_compositions_batch_test.go:L37:C16 [errcheck] Error return value of `ws.Close` is not checked
- internal/core/size_compositions_batch_test.go:L86:C16 [errcheck] Error return value of `ws.Close` is not checked
- internal/core/size_compositions_batch_test.go:L109:C16 [errcheck] Error return value of `ws.Close` is not checked
- internal/core/size_compositions_batch_test.go:L131:C16 [errcheck] Error return value of `ws.Close` is not checked
Affected functions (5 distinct): internal/core/archive_durable_write_test.go: setupDurableArchiveWorkspace; internal/core/size_compositions_batch_test.go: TestSizeCompositions_EmptyInput, TestSizeCompositions_MatchesPerArtifact, TestSizeCompositions_ShipmentDanglingAndFeatureMember, setupBatchComposition

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/core/lintfix_175_017_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_017_LintFix` (deterministic `TestU175_017_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/core" -run "^TestU175_017_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_017_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_017_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return is NOT an allowed resolution): for every flagged call, capture the returned error and handle it -- checked propagation with `fmt.Errorf("context: %w", err)`, a named-return `errors.Join`, or (inside a test/benchmark) fail via the in-scope `*testing.T`/`*testing.B` receiver (`require.NoError`, `t.Fatalf`, `b.Fatalf`). A deferred close becomes a checked deferred close. No error return is discarded; no cross-file or cross-package helper is introduced; edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 3 (owned source file(s) + one owned harness file).
- Implementation files = 2 (owned harness excluded).
- Owned baseline findings = 5 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/core -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.017-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "identity_model": "line-stable-group-multiset/v1",
    "packages": [
      "./internal/core"
    ],
    "owned_files": [
      "internal/core/archive_durable_write_test.go",
      "internal/core/size_compositions_batch_test.go"
    ],
    "owned_group_count": 2,
    "owned_occurrence_count": 5,
    "owned_groups": [
      {
        "group_fingerprint": "d601fb0e2676e078b939122509ec2c207aefec4732f1555467ff63057147f9ce",
        "path": "internal/core/archive_durable_write_test.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `workspace.Close` is not checked",
        "group_multiplicity": 1,
        "owned_count": 1,
        "owned_ordinals": [
          1
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 77,
            "column": 36
          }
        ]
      },
      {
        "group_fingerprint": "505967ec894c03e4ce106c539f7c58e8bea8c57444ada4d4b2de25a635f76d6e",
        "path": "internal/core/size_compositions_batch_test.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `ws.Close` is not checked",
        "group_multiplicity": 4,
        "owned_count": 4,
        "owned_ordinals": [
          1,
          2,
          3,
          4
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 37,
            "column": 16
          },
          {
            "line": 86,
            "column": 16
          },
          {
            "line": 109,
            "column": 16
          },
          {
            "line": 131,
            "column": 16
          }
        ]
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.017-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
