---
artifact_type: task
created_at: 2026-09-14T03:15:00Z
id: 172.011-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Declare BulkUpdateResult source shape + additive typed conflict detail design
updated_at: 2026-09-14T03:15:00Z
---

U2.decl [1E0C2251] source-shape declaration (predecessor). Declare the CURRENT source shape of BulkUpdateResult (the existing `Failed []string` field and how BulkUpdateStatus populates it today) and specify the ADDITIVE typed conflict-detail surface to be added ALONGSIDE it (e.g. `FailedDetails []BulkUpdateConflict{ID, Err, FromStatus, ToStatus}`), pinning: (a) `Failed []string` is PRESERVED unchanged - no signature change, no removal, no retype - for backward compatibility; (b) the typed detail is a NEW additive field, never a replacement for `Failed []string`; (c) the exact field/struct names and the deterministic mapping from a per-item stale-write (ErrShipmentConflict) to a typed conflict entry (id + FromStatus/ToStatus). This declaration is the source-of-truth contract consumed by the caller-compatibility task (172.012-T) and by the behavior implementation (172.006-T) and its GREEN regression (172.010-T), all of which land AFTER this declaration. Domain: docs/analysis. No deps (predecessor). AC: a declaration note enumerates the current BulkUpdateResult shape, the PRESERVED `Failed []string` field, and the additive typed detail struct + stale-write mapping, with backward compatibility explicitly pinned; this task changes no code behavior.
