---
artifact_type: task
created_at: 2026-09-14T03:15:00Z
dependencies:
    - 172.014-T
id: 172.011-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Declare additive BulkUpdateResult typed conflict field in production
updated_at: 2026-09-14T03:40:00Z
---

U2.decl [1E0C2251] PRODUCTION source-shape declaration (makes the 172.014-T AST RED harness GREEN). Add the ADDITIVE typed conflict-detail surface to BulkUpdateResult in production: a NEW `FailedDetails []BulkUpdateConflict` field and the `BulkUpdateConflict{ID, Err, FromStatus, ToStatus}` type, ADDED ALONGSIDE the existing `Failed []string`, which is PRESERVED unchanged (no signature change, no removal, no retype) for backward compatibility. This task declares the field and type ONLY: it does NOT populate `FailedDetails` and does NOT change BulkUpdateStatus behavior (that is 172.006-T). It turns the 172.014-T source-shape AST RED harness GREEN by making the field and type present. Pin the exact field and struct names, and DOCUMENT — as the source-of-truth contract, NOT implement here — the intended mapping from a per-item stale-write (ErrShipmentConflict) to a typed conflict entry (id + FromStatus/ToStatus) that the behavior task 172.006-T will populate; this contract is consumed by the caller-adaptation task (172.012-T), the behavior implementation (172.006-T), and its GREEN regression (172.010-T), all of which land AFTER this declaration. Domain: code. Depends on 172.014-T (AST RED harness observed failing first). AC: `BulkUpdateResult` exposes the additive `FailedDetails []BulkUpdateConflict{ID,Err,FromStatus,ToStatus}` field with `Failed []string` preserved unchanged; the 172.014-T AST harness transitions RED->GREEN; no BulkUpdateStatus behavior change and `FailedDetails` is not yet populated in this task.
