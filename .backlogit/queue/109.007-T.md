---
artifact_type: task
created_at: 2026-07-16T01:33:34.8569026Z
dependencies:
    - 109.001-T
    - 109.002-T
id: 109.007-T
labels:
    - spike
    - investigate-first
    - size-estimation
parent_id: 109-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-size-estimation-feature-shipment-plan.md
status: queued
title: Gather inheritance-bridge, generic rewrite-path preservation, and canonical size-location evidence (2h max)
updated_at: 2026-07-16T01:33:34.8569026Z
---

Research/decision task, strictly time-boxed to 2h, investigation only. Depends on 109.001-T and 109.002-T. Extracted from the former oversized 109.004-T (PR #242 split) as the inheritance-bridge + rewrite-path + size-location EVIDENCE half; width-isolated to the backlog-writer bridge and size-location questions. (a) CONCRETE INHERITANCE-BRIDGE evidence: record which docline base-contract invariants (which base fields, schema constraints, and codec preservation behaviors) the .backlogit backlog writers must IMPLEMENT or REUSE to honor the TARGET inheritance model, WITHOUT assuming direct reuse of internal/docline.Scope or internal/docline.Normalize. Current .backlogit artifacts are parsed/written by backlogit own models/core codec, not automatically by docline; the 107.009-T and 107.011-T base-contract edges are NECESSARY but do NOT themselves provide backlog-artifact round-trip preservation, so this bridge evidence resolves separately how backlog writers achieve it. (b) GENERIC BACKLOG REWRITE-PATH preservation confirmation: consume the 109.002-T generic rewrite-path inventory (models.ArtifactFromFrontmatter, core.WriteArtifactFile, core.SetArtifactSize, generic title/status/section updates) and confirm those paths preserve top-level size extensions so no later write drops them. Because the frontmatter codec (mdfront.Encode) reserializes YAML and MAY canonicalize key ordering, quote style, scalar spelling, comments, and anchors, preservation is asserted HONESTLY as: the extension key/value graph stays SEMANTICALLY / deep-value equivalent and TOP-LEVEL; document body bytes remain unchanged; normalization is idempotent. It is NOT a claim of raw frontmatter byte/lexical preservation unless a future YAML-node/raw-byte design is explicitly chosen. (c) CANONICAL SIZE-LOCATION evidence: record that task size is physically stored under custom_fields.size (a NESTED location) via core.SetArtifactSize while this spike proposes feature/shipment size as a TOP-LEVEL backlogit-owned extension, and gather the reconciliation options (migrate task size to top-level vs coexist with documented rationale) as EVIDENCE. Deliverable: inheritance-bridge decision-input, rewrite-path semantic-preservation confirmation, and canonical size-location evidence feeding the 109.004-T synthesis/exit decision. The final canonical-location decision and proceed/pivot/defer selection are made in 109.004-T. No implementation, no code/schema/CLI changes.