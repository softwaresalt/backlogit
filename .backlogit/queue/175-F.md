---
artifact_type: feature
created_at: 2026-09-17T23:58:13.3595381Z
custom_fields:
    harness_status: pending
id: 175-F
labels:
    - baseline-convergence
    - tech-debt
    - deferred-scope-expansion
priority: high
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
    - docs/decisions/2026-09-17-baseline-convergence-deliberation.md
status: queued
title: 'Repository baseline convergence: line-ending, lint, and CRLF golden-fixture green'
updated_at: 2026-09-18T05:08:40.319032Z
---

Covering release unit that makes all four mandatory quality gates — go test ./..., go vet ./..., golangci-lint run, and gofmt -l . — all green (go vet kept green throughout) by fixing the shared line-ending root cause (.gitattributes text=auto -> CRLF on Windows checkout) plus residual gofmt, 50 errcheck, and 6 staticcheck findings, and the U4a CRLF golden-fixture failure, and installs a persistent CI line-ending guard (U14, a NEW always-running `.github/workflows/line-endings.yml` with `pull_request` + protected-branch `push`, a `windows-latest` checkout, and a `git ls-files --eol` working-tree guard — leaving `.github/workflows/ci.yml` byte-for-byte untouched) so line endings cannot silently re-drift. errcheck remediation owns a CLOSED, FROZEN package->unit assignment fixed at staging: U4-U10 for the named packages and U13 for the fully-enumerated residual package set — now including internal/config and internal/faultline top-level with file-level exclusions for the U3-owned files. There is NO open-ended sweep and NO Ship-time planning-unit or subtask creation: every unit's scope is frozen before handoff and Ship creates NO backlog items after the shipment/DAG freeze (the prior execution-time subdivision clauses on U10/U13 are removed). U11 runs first as the staticcheck/errcheck overlap-inventory owner and produces the owned Ship-deliverable handoff artifact `docs/closure/175-U11-overlap-inventory.md` (deterministic, byte-sorted, not pre-existing at Stage) that every errcheck unit consumes by exact path to exclude overlapped files. Every normal harness-required unit (U3-U11, U13, U14) exclusively owns its deterministic `*_test.go` harness file(s), separate from the implementation-file inventory. Unblocks shipment 149-S. Sources deferred-scope-expansion stash 92F79833 + 4DB1DFF1. Harness-exempt set (closed): U12 (`175.012-T`) is the SOLE harness-exempt unit — P-002.1 `verification-only` with the required label, canonical contract block, closed-exempt-set membership, and a runnable `exempt_verification_command` that itself executes go test/go vet/golangci-lint/gofmt and fails on any nonzero exit or non-empty gofmt output before validating its evidence artifact; U1-U11, U13, and U14 are all NORMAL harness-required units (no generic/mechanical/closure-time exemption).