---
artifact_type: feature
created_at: 2026-09-17T23:58:13.3595381Z
custom_fields:
    harness_status: pending
id: 175-F
labels:
    - baseline-convergence
    - tech-debt
    - deferred-scope-expansion
priority: high
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
    - docs/decisions/2026-09-17-baseline-convergence-deliberation.md
status: queued
title: 'Repository baseline convergence: line-ending, lint, and CRLF golden-fixture green'
updated_at: 2026-09-18T19:23:58.3345931Z
---

Covering release unit that makes all four mandatory quality gates — `go test ./...`, `go vet ./...`, `golangci-lint run`, and `gofmt -l .` — all green (go vet kept green throughout) by fixing the shared line-ending root cause (`.gitattributes text=auto` -> CRLF on Windows checkout) and the full enumerated lint debt, and installs a persistent CI line-ending guard so line endings cannot silently re-drift.

IMMUTABLE FILE-OWNED LINT DAG (no aggregate waiver): the release unit is decomposed into a FROZEN set of 40 executable tasks fixed BEFORE handoff. There is NO frozen-package aggregate, NO `<3-file proxy waived` clause, NO exact counts deferred to Ship, NO dynamic scope subtraction, NO U11 runtime inventory, and NO Ship-time planning-unit or subtask creation — Ship creates NO backlog items after the shipment/DAG freeze. The 40 tasks are:
- U1 (`175.001-T`): the sole homogeneous mechanical line-ending migration (`.gitattributes` hardening + a path-scoped WORKTREE REFRESH/VERIFICATION of the 548 tracked CRLF *working-tree* paths — 518 `*.go` + 30 `*.json`, confirmed by live read-only `git ls-files --eol`; baseline `indexCRLF=0` so the index is already LF and the frozen 548 is the worktree refresh/verification set, the staged source subset may be empty or a subset with no path outside it, and the expected implementation diff is `.gitattributes` only). Its `<3 files` heuristic is documented as exceeded (not waived) because a byte-only EOL migration touches all 548 working-tree files with zero semantic change; the 2-hour effort bound holds. U1 owns exactly one harness (`TestU175_001_...`).
- 36 FILE-OWNED lint tasks, exactly ONE flagged source/test file each, covering every flagged file in the golangci-lint v2.13.2 baseline. Each task lists its exact linter, source file, finding lines/messages, package, exclusive harness path, scoped red selector (`TestU175_<NNN>_...`), <5 affected functions, and <4 scenarios; it modifies at most 2 files (its one flagged file + its one reserved harness file) and no other implementation file.
- U40 (`175.040-T`): line-ending guard SCRIPT task. Owns exactly `scripts/check-line-endings.ps1` plus `tests/lineendings_data_guard_175_test.go` (harness `TestU175_040_...`). Depends on U1. The script performs fixed-set `.gitattributes` contract + worktree EOL + index/blob normalization checks; 3 scenarios (CRLF/mixed worktree red, CRLF committed blob red, fully LF green; attribute-contract validation is part of each data-state precondition, not a fourth scenario).
- U14 (`175.014-T`): line-ending WORKFLOW integration task. Owns ONLY the NEW always-running `.github/workflows/line-endings.yml` plus `tests/lineendings_workflow_175_test.go` (harness `TestU175_014_...`). Depends on U40; the workflow invokes `scripts/check-line-endings.ps1`. Security criteria: `permissions: contents: read`, full-SHA checkout pin, `persist-credentials: false`, safe concurrency group/cancel. 3 scenarios (missing/malformed workflow invocation red, insecure/skippable workflow contract red, valid secure always-run workflow green). `.github/workflows/ci.yml` remains BYTE-IDENTICAL — neither U14 nor U1 changes any ci.yml content.
- U12 (`175.012-T`): terminal verification-only sink.

EXACT LINT EVIDENCE (golangci-lint v2.13.2, baseline HEAD, 56 issues; captured read-only and recorded in `docs/decisions/2026-09-17-baseline-lint-inventory.md`): errcheck = 50 findings across 31 files / 9 packages; staticcheck = 6 findings across 5 files / 4 packages. The staticcheck and errcheck file sets have NO overlap, PROVEN from the inventory — so NO U11 overlap-inventory artifact and NO overlap edge are needed (all prior U11 artifact/overlap language is removed). A separate residual-gofmt task is unnecessary: the 518 gofmt-listed Go files are exactly the 518 tracked CRLF Go files, so U1's line-ending migration resolves gofmt wholesale.

DAG (frozen, acyclic, 75 edges): every one of the 36 lint tasks depends on U1 (36 edges); U40 depends on U1 (1 edge); U14 depends on U40 (1 edge). U12 is the SOLE terminal sink and depends on exactly 37 immediate predecessors — all 36 lint tasks plus U14 (37 outgoing edges); its dependency on U1 and U40 is TRANSITIVE only (no direct edge to either). 36 + 1 + 1 + 37 = 75 edges. U12 has in-degree 0 as a dependency target (terminal). The machine dependency graph is unambiguous and acyclic.

HARNESS CONTRACTS: every task except U12 (that is U1 + 36 lint tasks + U40 + U14 = 39 normal tasks) is NORMAL harness-required and exclusively owns exactly one deterministic `*_test.go` harness path (exactly-one-owner; no overlap), separate from its implementation-file inventory, with a genuine targeted red-before-green selector whose test function starts `TestU175_<NNN>_` and whose scoped verification command is an EXECUTABLE non-vacuity PowerShell wrapper around `go test ./<pkg> -run '^TestU175_<NNN>_' -v -count=1` that re-emits combined output, preserves/propagates the native nonzero exit, and fails fail-closed (exit nonzero) when zero matching `--- PASS: TestU175_<NNN>_` lines are observed. No generic/mechanical/closure-time exemption and no retrospective waiver exists for any normal task. Ship's harness phase scaffolds every non-exempt member of the admitted ready wave together (per `_ship.agent.md` Step 2 and the harness-architect skill), not one-harness-just-before-one-task; sibling task-scoped red within that same wave is EXPECTED and TOLERATED until wave convergence. Each task's OWN `^TestU175_<NNN>_` selector must turn green when that task completes; full-suite green is required only at wave convergence; no later-wave harness may be scaffolded early. The harnesses are NOT pre-committed at Stage. This wording correction does not serialize tasks and does not change the frozen 40-task / 41-member / 75-edge DAG.

TASK-LINT CONTRACTS (task-scoped lint per member; full repository lint deferred to wave convergence): every one of the 40 tasks carries a canonical `<!-- BEGIN:task-lint-contract -->` block with a frozen `task_lint_cmd`, a closed `lint_scope`, and `non_vacuity_evidence`, per the installed `_ship.agent.md` Step 3 item 4 / Step 4.1 contract. The 36 file-owned lint tasks use an in-memory file-scoped lint verifier (FSLV) that launches golangci-lint v2.13.2 as a child process (`System.Diagnostics.ProcessStartInfo`, in-memory stdout/stderr capture — NO machine temp files, NO `Remove-Item` cleanup), runs `--output.json.path stdout --show-stats=false --issues-exit-code 0`, preserves native launch/exit failure distinctly, treats empty stdout / malformed JSON / JSON `null` / non-object root / missing or non-collection `Issues` as hard failures, validates schema before filtering, and matches the task-owned path + linter name by ORDINAL exact identity — failing if any owned finding remains, task-scoped only. U1, U40, and U14 use a harness-path FSLV (any-linter over `./tests`, filtered to the task's owned `*_175_test.go` harness path, `null` linter). U12 owns no lintable Go surface and carries a dual-mode `positive-no-go-surface-proof` `task_lint_cmd` that proves its owned delta is the single non-Go `docs/closure/` evidence artifact in BOTH the pre-commit and post-commit sequencing positions (fail-closed on any owned `.go` path, ambiguous mode, wrong delta, or malformed/native-git failure). Each member's `task_lint_cmd` is task-scoped ONLY; the full repository `golangci-lint run` is DEFERRED to mandatory wave convergence and is NEVER used as a per-task proxy. Same-wave sibling harnesses may remain red until wave convergence; no later-wave harness or lint scaffolding is created early.

Harness-exempt set (closed, single member): U12 (`175.012-T`) is the SOLE harness-exempt unit — P-002.1 `verification-only` with the required `harness-exempt` label, canonical contract block, `harness_owner: none`, `must-fail-before-deliverable` precondition, and a runnable single-quoted `exempt_verification_command` that itself executes `go test ./...`, `go vet ./...`, `golangci-lint run`, and `gofmt -l .`, fails on any nonzero exit or non-empty gofmt output, then validates its evidence artifact (gate markers as EXACT UNIQUE LINES) and a machine-readable full-identity `//nolint` suppression inventory (rejecting missing/extra/duplicate/empty-justification/stale rows and delimiter tampering, with lexical-state-aware scanning, a continuous NUL-delimited byte-stream tracked-file enumeration, strict throw-on-invalid UTF-8 path decoding, and ordinal-identity comparison, all with distinct auditable exits), then fails closed when not Windows-native (19), when the evidence artifact lacks valid Docline closure frontmatter or fails docs lint (20), and through a DUAL-MODE content-binding gate that succeeds under BOTH build-feature's pre-commit exemption-completion check and Ship's post-commit Step 4.3 rerun (VERIFIED-PARENT vs `git rev-parse HEAD` in pre-commit mode / `git rev-parse HEAD^` in post-commit mode — exit 21; post-commit `git diff --name-only HEAD^ HEAD` exactly the evidence artifact — exit 22; ambiguous mode, malformed porcelain record, or any non-allowlisted dirty tracked/untracked path — exit 23), before printing its success marker. U12 also carries a canonical `task-lint-contract` block (dual-mode `positive-no-go-surface-proof`). U12 owns ONLY its named evidence artifact under `docs/closure/` (the prior unowned "green guards" wording is removed).

Shipment 156-S membership: 41 members (this feature 175-F + 40 tasks). Unblocks shipment 149-S. Sources deferred-scope-expansion stash 92F79833 + 4DB1DFF1.
