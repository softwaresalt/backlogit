---
artifact_type: feature
created_at: 2026-09-17T23:58:13.3595381Z
custom_fields:
    harness_status: pending
id: 175-F
labels:
    - baseline-convergence
    - tech-debt
    - deferred-scope-expansion
priority: high
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
    - docs/decisions/2026-09-17-baseline-convergence-deliberation.md
status: queued
title: 'Repository baseline convergence: line-ending, lint, and CRLF golden-fixture green'
updated_at: 2026-09-18T06:16:32.227284Z
---

Covering release unit that makes all four mandatory quality gates — `go test ./...`, `go vet ./...`, `golangci-lint run`, and `gofmt -l .` — all green (go vet kept green throughout) by fixing the shared line-ending root cause (`.gitattributes text=auto` -> CRLF on Windows checkout) and the full enumerated lint debt, and installs a persistent CI line-ending guard so line endings cannot silently re-drift.

IMMUTABLE FILE-OWNED LINT DAG (no aggregate waiver): the release unit is decomposed into a FROZEN set of 39 executable tasks fixed BEFORE handoff. There is NO frozen-package aggregate, NO `<3-file proxy waived` clause, NO exact counts deferred to Ship, NO dynamic scope subtraction, NO U11 runtime inventory, and NO Ship-time planning-unit or subtask creation — Ship creates NO backlog items after the shipment/DAG freeze. The 39 tasks are:
- U1 (`175.001-T`): the sole homogeneous mechanical line-ending migration (`.gitattributes` hardening + path-scoped renormalize of the 518 tracked `*.go`/`*.json` files). Its `<3 files` heuristic is documented as exceeded (not waived) because a byte-only EOL migration touches all 518 files with zero semantic change; the 2-hour effort bound holds.
- 36 FILE-OWNED lint tasks, exactly ONE flagged source/test file each, covering every flagged file in the golangci-lint v2.13.2 baseline. Each task lists its exact linter, source file, finding lines/messages, package, exclusive harness path, scoped red selector, <5 affected functions, and <4 scenarios; it modifies at most 2 files (its one flagged file + its one reserved harness file) and no other implementation file.
- U14 (`175.014-T`): owns ONLY the NEW always-running `.github/workflows/line-endings.yml` (dedicated EOL guard). `.github/workflows/ci.yml` remains BYTE-IDENTICAL — U14 and U1 change no ci.yml content.
- U12 (`175.012-T`): terminal verification-only sink.

EXACT LINT EVIDENCE (golangci-lint v2.13.2, baseline HEAD, 56 issues; captured read-only and recorded in `docs/decisions/2026-09-17-baseline-lint-inventory.md`): errcheck = 50 findings across 31 files / 9 packages; staticcheck = 6 findings across 5 files / 4 packages. The staticcheck and errcheck file sets have NO overlap, PROVEN from the inventory — so NO U11 overlap-inventory artifact and NO overlap edge are needed (all prior U11 artifact/overlap language is removed). A separate residual-gofmt task is unnecessary: the 518 gofmt-listed Go files are exactly the 518 tracked CRLF Go files, so U1's line-ending migration resolves gofmt wholesale.

DAG (frozen, acyclic, 74 edges): every one of the 36 lint tasks depends on U1, and U14 depends on U1 (37 edges into U1). U12 is the SOLE terminal sink and depends on every other executable task — all 36 lint tasks plus U14 (37 outgoing edges); its dependency on U1 is transitive. U12 has in-degree 0 as a dependency target of nothing (terminal). The machine dependency graph is unambiguous and acyclic.

HARNESS CONTRACTS: every task except U12 is NORMAL harness-required and exclusively owns exactly one deterministic `*_test.go` harness path (exactly-one-owner; no overlap), separate from its implementation-file inventory, with a genuine targeted red-before-green selector. No generic/mechanical/closure-time exemption and no retrospective waiver exists for any normal task. Ship's harness phase creates each harness just before its file task; the harnesses are NOT pre-committed at Stage.

Harness-exempt set (closed, single member): U12 (`175.012-T`) is the SOLE harness-exempt unit — P-002.1 `verification-only` with the required `harness-exempt` label, canonical contract block, `harness_owner: none`, `must-fail-before-deliverable` precondition, and a runnable single-quoted `exempt_verification_command` that itself executes `go test ./...`, `go vet ./...`, `golangci-lint run`, and `gofmt -l .`, fails on any nonzero exit or non-empty gofmt output, then validates its evidence artifact and a machine-readable `//nolint` suppression inventory (rejecting missing/extra/duplicate/empty-justification/stale rows) before printing its success marker. U12 owns ONLY its named evidence artifact under `docs/closure/` (the prior unowned "green guards" wording is removed).

Unblocks shipment 149-S. Sources deferred-scope-expansion stash 92F79833 + 4DB1DFF1.
