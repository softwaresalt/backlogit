---
artifact_type: task
commit: 349abaa5
created_at: 2026-08-25T16:56:21.2285601Z
dependencies:
    - 147.036-T
    - 147.042-T
id: 147.037-T
labels:
    - checkpoint
    - events
    - plan-u14
    - seam
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: done
title: 'U14: migrate ResolveCheckpoint onto the guarded seam'
$12026-08-28T00:50:19.2721088Z
---

Plan unit **U14** (partition 2). Replaces the `ResolveCheckpoint` in-place rewrite site with a seam call.

Files: `internal/events/checkpoint_lifecycle.go`, `internal/events/checkpoint_migration_test.go` (new).

`ResolveCheckpoint` (`internal/events/checkpoint_lifecycle.go:178`) stops calling `syncWriteFileAtomic` directly and calls `RewriteCheckpointFile` with its existing mutation closure.

**Scope narrowed in cycle 37.** Through cycle 36 this unit also migrated `AbandonCheckpoint` in `internal/core`, which made it a three-file task — not *fewer than three*, and therefore outside the 2-Hour Rule heuristic this plan has already enforced against `147.021-T` and `147.014-T`. The abandon-site migration is now `147.044-T` / U14b. The two migrations touch disjoint packages and share no code, so each is independently verifiable at two files.

**This task introduces no new verb-facing sentinel and changes no ordering.** Its only claim is that the write mechanism moved. After it lands, `ResolveCheckpoint` refuses untrustworthy documents with the seam's raw verdict errors, so the verb's own `errors.Is` sentinel assertion still fails until 147.006-T / U3 lands - which is what keeps that unit honestly red.

**Test-lifecycle classification (cycle-20, rescoped cycle-37)**: red harness plus one green-step guard, and this task additionally turns a prior unit's harness green. One scaffolded `TestU14_` function - the structural assertion that `internal/events/checkpoint_lifecycle.go` contains **no** direct atomic-write call whose target resolves under the checkpoint directory **and** does contain a call to `RewriteCheckpointFile` inside `ResolveCheckpoint` - fails before the migration. Both halves are required: removal alone would be satisfied by routing through any other helper, which is not this task's contract. The shipped-accept-path assertion is green on landing and is committed with the migration as `TestU14Guard_ResolveAcceptPathUnchanged`, never in the harness commit.

**Cross-unit obligation**: this migration is what delivers the resolve verb's conformance refusal contract, because the seam returns the raw `*CheckpointNonConformingError` before any write and that error unwraps to `ErrCheckpointNonConforming`. The failing harness for that contract is `147.042-T` / U3c, which MUST be `harness-ready` and red before this task is claimed - enforced by the edge `147.037-T -> 147.042-T`. Completing this task turns `TestU3c_ResolveRefusesValidNonConformingDocument` green.

Red verification: `go test -count=1 -run '^TestU14_' ./internal/events`; the U3c gate is `go test -count=1 -run '^TestU3c_' ./internal/events`, which must go from failing to passing across this task.

<!-- BEGIN:acceptance-criteria -->
- `ResolveCheckpoint` stops calling `syncWriteFileAtomic` directly and calls `RewriteCheckpointFile` with its existing mutation closure; no new verb-facing sentinel and no ordering change
- Red harness (P-002/P-004 gate): the single scaffolded `TestU14_` function fails before migration — the structural assertion that `internal/events/checkpoint_lifecycle.go` contains no direct atomic-write call under the checkpoint directory **and** contains a `RewriteCheckpointFile` call inside `ResolveCheckpoint`; removal alone is not sufficient, because routing through any other helper would satisfy it
- Green-step guard (`TestU14Guard_ResolveAcceptPathUnchanged`): `ResolveCheckpoint` on a conforming active document still resolves and the resulting bytes are unchanged from the pre-migration expectation; green on landing, committed with the migration
- Ordering: completing this task turns `147.042-T` / U3c's `TestU3c_ResolveRefusesValidNonConformingDocument` from failing to passing, enforced by the edge `147.037-T -> 147.042-T`
- Scope: the `AbandonCheckpoint` migration is `147.044-T` / U14b, not this task
- Width: 2 files, 2 scenarios
<!-- END:acceptance-criteria -->