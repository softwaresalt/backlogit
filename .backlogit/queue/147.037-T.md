---
artifact_type: task
created_at: 2026-08-25T16:56:21.2285601Z
dependencies:
    - 147.036-T
id: 147.037-T
labels:
    - checkpoint
    - events
    - core
    - plan-u14
    - seam
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U14: migrate ResolveCheckpoint and AbandonCheckpoint onto the guarded seam'
updated_at: 2026-08-25T16:56:21.2285601Z
---

Plan unit **U14** (partition 2). Replaces the two in-place rewrite sites with seam calls.

Files: `internal/events/checkpoint_lifecycle.go`, `internal/core/checkpoint_disposition.go`, `internal/events/checkpoint_migration_test.go` (new).

`ResolveCheckpoint` (`internal/events/checkpoint_lifecycle.go:178`) and `AbandonCheckpoint` (`internal/core/checkpoint_disposition.go:105-118`) stop calling `syncWriteFileAtomic` / `atomicfile.WriteFileAtomic` directly and call `RewriteCheckpointFile` with their existing mutation closure.

**This task introduces no new verb-facing sentinel and changes no ordering.** Its only claim is that the write mechanism moved. After it lands, both verbs refuse untrustworthy documents with the seam''s raw verdict errors, so each verb''s own `errors.Is` sentinel assertion still fails until 147.006-T / U3, 147.007-T / U3b, and 147.008-T / U4 land - which is what keeps those units honestly red.

Red verification: `go test -count=1 -run '^TestU14_' ./internal/events ./internal/core`.