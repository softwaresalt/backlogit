---
artifact_type: feature
created_at: 2026-07-10T00:21:44.8821969-07:00
custom_fields:
    harness_status: pending
    source_stash_id: 17C25A87
    source_stash_kind: spike
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: |-
        Investigate why this repo's GitHub Actions spend is ~2x the next most expensive repo.

        Goal: identify the top cost drivers in GitHub Actions billing for softwaresalt/backlogit and recommend concrete reductions. Produce a findings artifact (spike) ranking cost by workflow/job with evidence.

        Investigation angles:
        - Billing/usage breakdown: use the org/repo Actions usage report (Settings -> Billing -> Actions, or `gh api` usage endpoints) to attribute minutes by workflow and runner OS.
        - Runner OS multiplier: confirm which runners each job uses. macOS = 10x, Windows = 2x, Linux = 1x billing multiplier. Any macOS/Windows jobs (e.g., release.yml build matrix, test matrix) are prime suspects.
        - Trigger frequency + fan-out: ci.yml runs on push(main) AND pull_request. The Go `test` matrix (Go 1.23 + 1.24 = 2 runs) with `go test -race` (slow) + coverage is likely the biggest driver; cli-reference-drift and release.yml (build binaries) add more.
        - Verify the recent cost-gating (#189, job-level dorny/paths-filter that skips heavy Go steps on docs/backlog-only changes) is actually effective and that required checks still report (the dark-mode run just exercised this). Quantify how many recent runs actually skipped heavy work vs ran full.
        - Redundant/duplicate runs: push-to-main AND the PR both trigger CI (double-running the same commits); concurrency cancellation config; re-runs; matrix over-provisioning.
        - Release workflow: release.yml builds cross-platform binaries (linux/macos/windows x64/arm64) — check if it runs more often than needed or on non-release events.

        Likely quick wins to evaluate: drop redundant push+PR double runs (e.g., only run heavy jobs on PR, or use merge_group), trim the Go matrix, gate `-race`/coverage to fewer runs, ensure macOS/Windows jobs are minimized, confirm paths-filter coverage.

        Deliverable: spike findings artifact in docs/decisions or docs/spikes ranking cost drivers with a recommended reduction plan; link follow-up feature/tasks if a fix is warranted.
id: 090-F
priority: high
status: queued
title: Reduce GitHub Actions CI cost
updated_at: 2026-07-10T00:23:32.8316916-07:00
---

Stage-only feature harvested from stash 17C25A87 and the GitHub Actions cost spike. Ship should implement config-only CI workflow changes that reduce billable Actions minutes while preserving branch-protection required-check satisfiability and P-009 merge-commit compatibility. Scope is .github/workflows/ci.yml, .github/workflows/cli-reference-drift.yml, .github/workflows/release.yml, and only documented local lint enforcement if CI lint is removed.

<!-- BEGIN:dod -->
* Shipment manifest contains the feature and all six CI-cost tasks
* Stash 17C25A87 is harvested and traceable to the spike findings
* Downstream Ship can implement the tasks without re-triaging the spike
<!-- END:dod -->

<!-- BEGIN:goals -->
* Reduce redundant GitHub Actions billable minutes from duplicate PR and push validation
* Preserve branch-protection required-check satisfiability for every PR type
* Keep all staged work config-only for the downstream Ship phase
<!-- END:goals -->