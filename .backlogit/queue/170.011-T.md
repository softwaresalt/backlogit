---
artifact_type: task
created_at: 2026-09-17T02:00:58.0143366Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 170.011-T
labels:
    - docs-only
    - harness-exempt
parent_id: 170-F
priority: high
references:
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
    - 167.008-T
status: queued
title: 'Design: request-identity-digest contract + operator pre-commit issuance flow'
updated_at: 2026-09-17T05:02:28.5232095Z
---

Design/declaration (blocking predecessor): request-identity-digest contract + operator pre-commit issuance flow. DESIGN OUTPUT PATH (exact) -- FORWARD REFERENCE until produced by THIS task: docs/design-docs/2026-09-16-866fdc8c-request-identity-digest-contract.md. Define the request-identity-digest field set, canonical byte encoding, and operator pre-commit token-issuance flow so issuance and verification are fully specified before Feature C implementation. Domain: docs (design-doc). Blocks 170.001-T and 170.002-T. Producer linkage: request_identity_digest is produced by the reconcile/event path in the locked 167.008-T critical section (shipped via 148-S) as frontmatter field shipment_reconciliation_request_identity_digest (internal/core/shipment_reconcile_classifier.go, shipment_reconcile_event.go); this task references 167.008-T as the authoritative producer, byte-for-byte aligned, no independent re-canonicalization. Digest/lock sequencing DESCRIBED against ACTUAL code (cycle-3 item 12): the request-identity digest is computed lock-free/pre-lock from the NORMALIZED reconcile delta in shipment_reconcile_transaction.go (~line 76) BEFORE any membership or persistence lock; Phase-A membership locking is acquired next; persistence locks are acquired inside the critical section. prepareShipmentReconcileEvidence (internal/core/shipment_reconcile_evidence.go:186) recomputes the digest under the Phase-A membership lock; because that recomputation is DETERMINISTIC over the same normalized reconcile delta it yields a byte-identical digest and introduces NO drift. The design DOCUMENTS this current authoritative producer/recompute behavior accurately rather than mandating a code change: the cycle-2 de-dup behavior task 170.015-T is WITHDRAWN (cycle-3 item 12) as outside the authorized digest-design scope, since the recompute is drift-free by determinism. This docs task claims no behavior no task owns; it describes existing behavior only. AC: (1) the design doc is written at the exact path and enumerates the exact request-identity-digest input fields and order, byte-for-byte aligned with the 167.008-T producer; (2) it specifies a canonical deterministic byte encoding matching the producer (no re-canonicalization); (3) it describes the actual pre-lock computation, Phase-A membership locking, in-critical-section persistence-lock sequencing, AND the deterministic under-lock recomputation at shipment_reconcile_evidence.go:186, documenting that its determinism over the normalized delta makes it drift-free (no behavior change mandated, no removal task); (4) it specifies the operator pre-commit issuance flow (who mints, what is signed incl. not_before/issued-at, TTL and nonce handling); (5) it is referenced by 170.001-T and 170.002-T and references the 167.008-T producer; (6) markdownlint passes.

<!-- BEGIN:harness-exemption-contract -->
```text
harness_exemption_class: docs-only
harness_exemption_reason: Produces a design-doc only; no production or test code is changed.
harness_owner: none
exempt_verification_command: pwsh -NoProfile -Command "if (Test-Path docs/design-docs/2026-09-16-866fdc8c-request-identity-digest-contract.md) { exit 0 } else { exit 1 }"
exempt_precondition: must-fail-before-deliverable
```
<!-- END:harness-exemption-contract -->