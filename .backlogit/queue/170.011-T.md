---
artifact_type: task
created_at: 2026-09-17T02:00:58.0143366Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 170.011-T
labels:
    - docs-only
    - p002.1-harness-exempt
parent_id: 170-F
priority: high
references:
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
    - 167.008-T
status: queued
title: 'Design: request-identity-digest contract + operator pre-commit issuance flow'
updated_at: 2026-09-17T03:14:34.1979714Z
---

Design/declaration (blocking predecessor): request-identity-digest contract + operator pre-commit issuance flow. DESIGN OUTPUT PATH (exact): docs/design-docs/2026-09-16-866fdc8c-request-identity-digest-contract.md. Define the request-identity-digest field set, canonical byte encoding, and operator pre-commit token-issuance flow so issuance and verification are fully specified before Feature C implementation. Domain: docs (design-doc). Blocks 170.001-T and 170.002-T. Producer linkage: request_identity_digest is produced by the reconcile/event path in the locked 167.008-T critical section (shipped via 148-S) as the frontmatter field shipment_reconciliation_request_identity_digest (internal/core/shipment_reconcile_classifier.go, shipment_reconcile_event.go); this task references 167.008-T as the authoritative producer and layers the token issuance/verification contract on top (byte-for-byte aligned, no independent re-canonicalization). Digest/lock sequencing (plan-review cycle-1 item 6): the request-identity digest is computed/normalized from the NORMALIZED reconcile delta as PRE-LOCK, pure, deterministic work BEFORE any membership or persistence lock is acquired; Phase-A shipment membership locking is acquired next; persistence (artifact) locks are acquired inside the critical section; the pre-computed digest is threaded into the prepared event UNDER the lock but is NEVER recomputed or re-canonicalized under the lock, preventing digest drift and membership/persistence lock-ordering inversion. Cross-feature decoupling: 169-F does NOT consume this token-issuance contract (169.003-T keeps attestation out of request_identity_digest), so this task stays in 151-S with no ordering edge into 150-S. AC: (1) the design doc is written at docs/design-docs/2026-09-16-866fdc8c-request-identity-digest-contract.md and enumerates the exact request-identity-digest input fields and their order, byte-for-byte aligned with the 167.008-T producer; (2) it specifies a canonical deterministic byte encoding matching the producer (no re-canonicalization); (3) it specifies the pre-lock digest computation, Phase-A membership locking, and in-critical-section persistence-lock sequencing; (4) it specifies the operator pre-commit issuance flow (who mints, what is signed, TTL and nonce handling); (5) it is referenced by 170.001-T and 170.002-T and references the 167.008-T producer; (6) markdownlint passes.