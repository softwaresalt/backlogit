---
artifact_type: task
created_at: 2026-09-17T02:00:58.0143366Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 170.011-T
labels:
    - docs-only
    - p002.1-harness-exempt
parent_id: 170-F
priority: high
references:
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
    - 167.008-T
status: queued
title: 'Design: request-identity-digest contract + operator pre-commit issuance flow'
updated_at: 2026-09-17T04:22:13.7652652Z
---

Design/declaration (blocking predecessor): request-identity-digest contract + operator pre-commit issuance flow. DESIGN OUTPUT PATH (exact): docs/design-docs/2026-09-16-866fdc8c-request-identity-digest-contract.md (this task PRODUCES that file; downstream tasks treat it as a forward reference until produced). Define the request-identity-digest field set, canonical byte encoding, and operator pre-commit token-issuance flow so issuance and verification are fully specified before Feature C implementation. Domain: docs (design-doc). Blocks 170.001-T and 170.002-T. Producer linkage: request_identity_digest is produced by the reconcile/event path in the locked 167.008-T critical section (shipped via 148-S) as frontmatter field shipment_reconciliation_request_identity_digest (internal/core/shipment_reconcile_classifier.go, shipment_reconcile_event.go); this task references 167.008-T as the authoritative producer, byte-for-byte aligned, no independent re-canonicalization. Digest/lock sequencing corrected to ACTUAL code (cycle-2 item 3): the request-identity digest is computed lock-free/pre-lock from the NORMALIZED reconcile delta in shipment_reconcile_transaction.go (~line 76) BEFORE any membership or persistence lock; Phase-A membership locking is acquired next; persistence locks are acquired inside the critical section. HOWEVER the current code STILL redundantly recomputes the digest under the Phase-A membership lock inside prepareShipmentReconcileEvidence (internal/core/shipment_reconcile_evidence.go:186); the design specifies the TARGET of a single pre-lock computation threaded through with NO under-lock recomputation, and behavior task 170.015-T removes that redundant recomputation and threads the precomputed digest. This docs task therefore does not itself claim the drift-free behavior as already-implemented; 170.015-T owns that behavior. Cross-feature decoupling: 169-F does NOT consume this contract, so this task stays in 151-S. AC: (1) the design doc is written at the exact path and enumerates the exact request-identity-digest input fields and order, byte-for-byte aligned with the 167.008-T producer; (2) it specifies a canonical deterministic byte encoding matching the producer (no re-canonicalization); (3) it specifies the TARGET single pre-lock digest computation, Phase-A membership locking, and in-critical-section persistence-lock sequencing, and identifies the current under-lock recomputation at shipment_reconcile_evidence.go:186 as removed by behavior task 170.015-T; (4) it specifies the operator pre-commit issuance flow (who mints, what is signed incl. not_before/issued-at, TTL and nonce handling); (5) it is referenced by 170.001-T and 170.002-T and references the 167.008-T producer; (6) markdownlint passes.