---
artifact_type: task
created_at: 2026-09-25T00:45:08.5760281Z
dependencies:
    - 174.075-T
id: 174.076-T
labels:
    - wave-19r
    - 155-S
    - harness-metadata
    - harness-required
    - p002-4-reclassified
    - harness-ready
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - .backlogit/queue/073-DL.md
status: queued
title: Reconcile harness-manifest checksums for budget surfaces
updated_at: 2026-09-25T21:00:09.4350818Z
---

Wave 19R work unit 3 of 3 (unit token U19R3) for the 155-S minimal unblock. It reconciles .autoharness/harness-manifest.yaml for the two artifacts 174.075-T changed:
  - sets the LF-normalized lowercase SHA-256 checksum of the entries for .github/agents/_ship.agent.md (line ~373) and .github/policies/workflow-policies.md (line ~125);
  - appends one sentence to each entry's existing single drift_reason.
Domain: harness metadata (configuration). Class: NORMAL harness-required task (NOT harness-exempt): D8 (plan rev22.4) plus D9 (plan rev22.5, P-002.6 unit-prefix selector). Files: implementation 1 (.autoharness/harness-manifest.yaml); harness 1 (tests/integration/harness_manifest_budget_reconciliation_test.go, function TestU19R3_ManifestBudgetReconciliation). Depends on 174.075-T.

Why non-exempt: Ship's P-002.4 path pass halted EXEMPT_DELTA_EXCEEDS_CLASS (logs/diagnostics/174076-p0024-delta-report.json). docs-only admits only Markdown, instruction, prompt, and agent artifacts, and no other exempt class admits a YAML configuration file. P-002.4 already names the non-exempt route with its own red harness, so no policy text changes and no waiver is taken.

Why rev22.5: Ship halted on the rev22.4 exact-name selector, which had no unit prefix and so violated P-002.6 task-scoped command requirement 4 (-run '^TestU<unit>_'). The harness function is renamed TestU19R3_ManifestBudgetReconciliation (test-only rename, no assertion change). The task-scoped harness_cmd is: go test -count=1 -timeout=5m -v -run '^TestU19R3_' ./tests/integration

Authoritative contract: this task's green-regression-contract block, acceptance-criteria, and implementation-notes (rev22.5). They supersede plan section 19R.5.3 and the rev22.4 selector. Deliberation 073-DL option O12; source stash 11BE840F (DEFERRED SCOPE EXPANSION, consumed by this wave). Wave 19R closed exempt set: {174.075-T} only.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=5m -v -run '^TestActivePluginDocsDoNotReferenceRetiredNPMWrapper$' ./tests/integration"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Checksums): Set each entry's checksum: value to the lowercase SHA-256 of the file's UTF-8 content with CRLF normalized to LF, computed over the COMMITTED (HEAD) content of each file. Expected values: workflow-policies d6e67d03bb872709a3edd5faf7daf7f44a62042b0019862cee83c709cf164778; _ship.agent.md 5467a6af712d67364009d21e65da7c6f109f3312b64df2504677dd2da839eca6. No other entry changes, and no key is duplicated.
AC2 (Drift reason): Amend each entry's existing drift_reason in place by appending: 073-DL rev22: explicit governed full-suite budget `go test -timeout=30m ./...` (Steps 4.6/5, P-002.6, P-004). Do not write 'npx ' anywhere in the manifest; TestActivePluginDocsDoNotReferenceRetiredNPMWrapper forbids it.
AC3 (Harness; P-002/P-004/P-002.6):
  - The harness is the single function TestU19R3_ManifestBudgetReconciliation in tests/integration/harness_manifest_budget_reconciliation_test.go. It was introduced in edb8d88be551e84d439354aeb685693755994766 under the rev22.4 name and is renamed by a test-only harness-architect commit that changes only the function identifier (no assertion, subtest, import, or constant change).
  - No other function in the repository may match ^TestU19R3_.
  - harness_cmd (task-scoped; satisfies all six P-002.6 requirements): go test -count=1 -timeout=5m -v -run '^TestU19R3_' ./tests/integration
  - RED re-verification after the rename, against the HEAD manifest with the deliverable set aside:
    - go vet ./tests/integration exits 0 (compile check per plan D2);
    - harness_cmd exits 1, with exactly one top-level '--- FAIL: TestU19R3_ManifestBudgetReconciliation' line and zero top-level '--- PASS:' lines;
    - both subtests (workflow-policies, ship-agent) fail on assertions (c) and (d), not on a build, import, parse, or panic;
    - '[no tests to run]' or 'no test files' at any exit code is a halt (vacuous), not RED.
  - harness-ready is retained. The harness manifest is re-recorded against the new selector (Compilation: PASS, Red Phase: CONFIRMED, rename commit SHA).
AC4 (Green): after the deliverable is restored byte-exactly, harness_cmd exits 0 with exactly one top-level '--- PASS: TestU19R3_ManifestBudgetReconciliation' line and no FAIL or SKIP. Exit 0 with zero PASS lines, '[no tests to run]', or 'no test files' is a failure. go vet ./tests/integration exits 0.
AC5 (Checksum currency; acceptance-evidence probe): the checksum probe in the implementation notes exits 0 and prints CHECKSUM_PROBE_OK:174.076-T on the deliverable, and exits 1 with no marker against the HEAD manifest.
  - Run it under the approved byte-exact _ship.agent.md backup / HEAD-swap / restore procedure, because the working-tree tools line is operator-owned.
  - It is NOT harness_cmd and NOT a green_regression_cmds entry. It is a read-only PowerShell content check, not a scoped Go test command, so the P-002.6 scoped-command requirements do not apply to it, and it must not be added to the green-regression block.
AC6 (Regression): the single green_regression_cmds entry in the green-regression-contract block exits 0 with exactly one top-level '--- PASS: TestActivePluginDocsDoNotReferenceRetiredNPMWrapper' line and no FAIL or SKIP. Zero PASS lines or '[no tests to run]' is a failure.
AC7 (Scope):
  - Harness commits: edb8d88be551e84d439354aeb685693755994766 and the rename commit each change exactly tests/integration/harness_manifest_budget_reconciliation_test.go.
  - Implementation: git diff --name-only {claim_baseline_sha}..HEAD lists exactly .autoharness/harness-manifest.yaml, plus Ship bookkeeping (.backlogit/**, docs/memory/**). Here claim_baseline_sha is the HEAD captured at the Step 4.1a re-claim, immediately after the rename commit.
  - Whole W3 re-entry: git diff --name-only ccd277239110f9e0533a4394bb42ea57bde22b7b..HEAD lists exactly tests/integration/harness_manifest_budget_reconciliation_test.go and .autoharness/harness-manifest.yaml, plus bookkeeping (.backlogit/**, docs/memory/**). ccd277239110f9e0533a4394bb42ea57bde22b7b is the Stage rev22.5 plan commit; the Stage task-file commit after it is .backlogit bookkeeping.
  - .github/agents/_ship.agent.md, .github/policies/workflow-policies.md, and every other pre-existing dirty or untracked file are never staged.
AC8 (Fail-closed): any deviation halts Ship and returns the task to Stage, with no in-session relaxation, renaming beyond D9, or selector substitution. W1/W2/W3 commits stay local and unpushed until W3 closes.
AC9 (Then STOP): After W3 closes, Ship pushes and then STOPS. Per 174.073-T AC7 and plan 19R.8, it requests ONE operator authorization for exactly one governed full-suite run: go test -timeout=30m ./... 2>&1 | Tee-Object -FilePath logs/diagnostics/155-s-go-test-governed-30m-<yyyymmdd-hhmm>.txt. W3 records FULL_SUITE_OPERATOR_DEFERRED: wave 3 (19R.7 D1; 174.073-T AC7).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
#### rev22.5 (2026-09-25, Stage): P-002.6 unit-prefix correction

* **State at amendment:**
  * HEAD edb8d88be551e84d439354aeb685693755994766 holds the harness (test file only), assertion-RED under its rev22.4 name.
  * The deliverable manifest is restored in the working tree (SHA-256 28d7424a2969e5beffe654824f35779cda6bc8f9ac29fa23bc6c4cfcf494c306); the backup is logs/diagnostics/174076-manifest-deliverable-backup.yaml.
  * Stage returned the task to queued (harness-ready kept) so that the rename and RED re-verification happen before the claim, in standard order.
* **Wave inputs for W3 = {174.076-T}:**
  * sibling_red_selectors = [];
  * open_red_selectors = [] (174.074-T was closed by 174.075-T);
  * red_deliverable = false;
  * there is no exempt input;
  * green_regression_cmds = the one-entry array from the block. Re-freeze it at resume (Step 3 item 4), because the block is new.
* **Harness specification (unchanged except the name):**
  * one top-level function, TestU19R3_ManifestBudgetReconciliation, with two subtests, workflow-policies and ship-agent;
  * yaml.v3 parse of .autoharness/harness-manifest.yaml, then per entry:
    * (a) exactly one artifact has the path;
    * (b) drift_allowed is true;
    * (c) the AC2 sentence appears exactly once in drift_reason;
    * (d) the checksum matches ^[0-9a-f]{64}$ and differs from the pre-reconciliation value (workflow-policies d941c1c78f33a46c0afd9e743ccd606d3da4316cada7260871708c4b91d9290b; ship-agent d6b046f8d2299e8b146df95994fa7d5d6472c28eec56335a8e8988fbf242a3d9);
  * no checksum-currency recomputation (plan D7/D8 over-coupling bar).

**Ship sequence:**
1. **Resume.** Resume from the Ship checkpoint. Re-run Step 3 item 4 for 174.076-T and freeze green_regression_cmds from the block.
2. **Set aside the deliverable.** Confirm the working manifest and logs/diagnostics/174076-manifest-deliverable-backup.yaml both hash to 28d7424a…c306. Run git restore --source=HEAD --worktree -- .autoharness/harness-manifest.yaml, then confirm git diff --quiet HEAD -- .autoharness/harness-manifest.yaml exits 0. This is the operator-approved backup + hash-check pattern.
3. **harness-architect: rename.** Change only the function identifier to TestU19R3_ManifestBudgetReconciliation.
   * Check with git diff: exactly one line removed and one added, both being the func declaration.
   * Run gofmt -l on the file (no output expected), then go vet ./tests/integration (exit 0).
   * Confirm that no other function matches ^TestU19R3_ (repo grep: exactly 1 hit).
4. **harness-architect: RED re-verification.** Run harness_cmd and expect the AC3 RED result.
   * Commit the test file only, for example: test(core): anchor manifest reconciliation harness to U19R3.
   * Re-record the harness manifest on the task (Compilation: PASS / Red Phase: CONFIRMED / rename commit SHA). harness-ready stays.
5. **Re-claim.** Step 4.1a: move to active and capture claim_baseline_sha = HEAD (the rename commit).
6. **Restore the deliverable.** Copy-Item the backup back over .autoharness/harness-manifest.yaml and verify SHA-256 = 28d7424a…c306.
7. **Green gates:**
   * AC4: harness_cmd gives 1 PASS; go vet ./tests/integration exits 0.
   * AC6: the green-regression command gives 1 PASS.
   * AC5: the checksum probe prints CHECKSUM_PROBE_OK:174.076-T, run under the approved _ship.agent.md swap. The restored _ship.agent.md LF-SHA256 must be a627c45a0fbd2386e6e5b1316961f0f99626de9933532b4f6d30a3c52f39aedb, and the tools line must be the only diff.
   * Lint/format are scoped to the test file (plan D3); the compile check is scoped by D2.
8. **Close.** Commit the manifest only, for example: chore(core): reconcile harness manifest checksums for budget surfaces. Run the AC7 scope checks, then track, complete, and archive. Push, then STOP (AC9).

**Checksum probe (AC5 acceptance evidence; read-only; not a gate command):**

$m = Get-Content '.autoharness/harness-manifest.yaml'; foreach ($f in @('.github/agents/_ship.agent.md', '.github/policies/workflow-policies.md')) { $t = [IO.File]::ReadAllText((Resolve-Path $f)).Replace([string][char]13 + [char]10, [string][char]10); $h = [BitConverter]::ToString([Security.Cryptography.SHA256]::Create().ComputeHash([Text.Encoding]::UTF8.GetBytes($t))).Replace('-', '').ToLower(); $i = [Array]::IndexOf($m, '  - path: ' + [char]34 + $f + [char]34); if ($i -lt 0) { exit 1 }; $j = $i + 1; $ok = 0; $ck = 0; $dr = 0; $dok = 0; while ($j -lt $m.Count -and -not $m[$j].StartsWith('  - path:')) { $l = $m[$j].Trim(); if ($l.StartsWith('checksum:')) { $ck++ }; if ($l.StartsWith('drift_reason:')) { $dr++; if ($l.Contains('073-DL rev22')) { $dok++ } }; if ($l -eq ('checksum: ' + [char]34 + $h + [char]34)) { $ok++ }; $j++ }; if ($ok -ne 1 -or $ck -ne 1 -or $dr -ne 1 -or $dok -ne 1) { exit 1 } }; Write-Output 'CHECKSUM_PROBE_OK:174.076-T'; exit 0
<!-- END:implementation-notes -->
