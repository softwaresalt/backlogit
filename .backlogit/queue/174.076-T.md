---
artifact_type: task
created_at: 2026-09-25T00:45:08.5760281Z
dependencies:
    - 174.075-T
id: 174.076-T
labels:
    - wave-19r
    - 155-S
    - harness-metadata
    - harness-required
    - p002-4-reclassified
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - .backlogit/queue/073-DL.md
status: queued
title: Reconcile harness-manifest checksums for budget surfaces
updated_at: 2026-09-25T20:40:04.8495662Z
---

Wave 19R work unit 3 of 3 for the 155-S minimal unblock. It reconciles .autoharness/harness-manifest.yaml for the two artifacts 174.075-T changed:
  - sets the LF-normalized lowercase SHA-256 checksum of the entries for .github/agents/_ship.agent.md (line ~373) and .github/policies/workflow-policies.md (line ~125);
  - appends one sentence to each entry's existing single drift_reason.
Domain: harness metadata (configuration). Class: NORMAL harness-required task (NOT harness-exempt) since the 2026-09-25 Stage AC6 amendment (plan Wave 19R rev22.4, decision D8, which supersedes D7 for this task). Files: implementation 1 (.autoharness/harness-manifest.yaml); harness 1 (tests/integration/harness_manifest_budget_reconciliation_test.go, scaffolded by harness-architect in W3). Depends on 174.075-T.

Why reclassified: Ship's P-002.4 path pass halted EXEMPT_DELTA_EXCEEDS_CLASS (logs/diagnostics/174076-p0024-delta-report.json). The docs-only class delta surface is 'Markdown, instruction, prompt, and agent artifacts only'; the YAML manifest is outside it, and no other exempt class admits it (verification-only bars configuration files; covered-by needs a harness-ready predecessor owner and admits only production files). Rather than change P-002.4 text or waive it, the task now satisfies P-002 the ordinary way: harness-ready after a compiling, assertion-red harness (P-002 precondition, P-004 red phase). This is the same non-exempt route P-002.4 already prescribes for repository-configuration files ('a separate, non-exempt ... task with its own red harness or probe'). No policy text changes.

Authoritative contract: this task's acceptance-criteria and implementation-notes sections (rev22.4). They supersede the rev22.3 docs-only exemption contract in plan section 19R.5.3. Deliberation 073-DL option O12; source stash 11BE840F (DEFERRED SCOPE EXPANSION, consumed by this wave). The harness-exemption contract block was removed on purpose: this task is no longer a member of the Wave 19R closed exempt set, which is now {174.075-T} only.

<!-- BEGIN:acceptance-criteria -->
AC1 (Checksums): Set each entry's checksum: value to the lowercase SHA-256 of the file's UTF-8 content with CRLF normalized to LF, computed over the COMMITTED (HEAD) content of each file. Expected values: workflow-policies d6e67d03bb872709a3edd5faf7daf7f44a62042b0019862cee83c709cf164778; _ship.agent.md 5467a6af712d67364009d21e65da7c6f109f3312b64df2504677dd2da839eca6. No other entry changes, and no key is duplicated.
AC2 (Drift reason): Amend each entry's existing drift_reason in place by appending: 073-DL rev22: explicit governed full-suite budget `go test -timeout=30m ./...` (Steps 4.6/5, P-002.6, P-004). Do not write 'npx ' anywhere in the manifest; TestActivePluginDocsDoNotReferenceRetiredNPMWrapper forbids it.
AC3 (Harness first, assertion-red; P-002/P-004): harness-architect scaffolds exactly ONE test function, TestHarnessManifestBudgetReconciliation, in the new file tests/integration/harness_manifest_budget_reconciliation_test.go, per the implementation notes. Red is confirmed against the HEAD manifest content, with the uncommitted deliverable set aside (implementation notes, steps 1-3):
  - go vet ./tests/integration exits 0;
  - go test ./tests/integration -run '^TestHarnessManifestBudgetReconciliation$' -count=1 -timeout=5m -v exits 1, with exactly one top-level '--- FAIL: TestHarnessManifestBudgetReconciliation' line and zero top-level '--- PASS:' lines;
  - both subtests (workflow-policies, ship-agent) fail on assertions (c) and (d), not on a build, import, or YAML parse error.
  The harness commit contains only the test file. harness-ready is then applied through backlogit, and the harness manifest records Compilation: PASS and Red Phase: CONFIRMED.
AC4 (Green): after the deliverable is restored byte-exactly, the same selector exits 0 with exactly 1 top-level '--- PASS:' line and no FAIL or SKIP; go vet ./tests/integration exits 0.
AC5 (Checksum probe, exact currency): the checksum probe in the implementation notes exits 0 and prints CHECKSUM_PROBE_OK:174.076-T on the deliverable. Run it under the approved byte-exact _ship.agent.md backup / HEAD-swap / restore procedure, because the working-tree tools line is operator-owned. Against the HEAD manifest it exits 1 with no marker; this was already observed on 2026-09-25 at Ship Step 4.1a and build-feature Step 0, under the former marker name EXEMPT_VERIFY_OK.
AC6 (Regression): go test ./tests/integration -run '^TestActivePluginDocsDoNotReferenceRetiredNPMWrapper$' -count=1 -timeout=5m -v gives exactly 1 top-level '--- PASS:' line and no FAIL or SKIP.
AC7 (Scope):
  - the harness commit diff is exactly tests/integration/harness_manifest_budget_reconciliation_test.go;
  - the implementation commit diff is exactly .autoharness/harness-manifest.yaml;
  - git diff --name-only 1a0641757b66094f5ba4d531d0d4bdeb53674f76 HEAD lists exactly those two files, plus Ship bookkeeping (.backlogit/**, docs/memory/**).
  .github/agents/_ship.agent.md, .github/policies/workflow-policies.md, and every other pre-existing dirty or untracked file are never staged.
AC8 (Fail-closed): any deviation halts Ship and returns the task to Stage, with no in-session relaxation. Deviations include: red that is not assertion-red, a PASS/FAIL count mismatch, a probe failure, a backup SHA-256 mismatch, or an unexpected path. The W1/W2 commits stay local and unpushed until W3 closes.
AC9 (Then STOP): After W3 closes, Ship pushes and then STOPS. Per 174.073-T AC7 and plan 19R.8, it requests ONE operator authorization for exactly one governed full-suite run: go test -timeout=30m ./... 2>&1 | Tee-Object -FilePath logs/diagnostics/155-s-go-test-governed-30m-<yyyymmdd-hhmm>.txt. W3 records FULL_SUITE_OPERATOR_DEFERRED: wave 3 (19R.7 D1; 174.073-T AC7) unchanged.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
#### rev22.4 (2026-09-25, Stage AC6 return): 174.076-T as a normal harness-required task

* **Status:** returned to queued so Ship re-enters W3 through P-002.6 static intake. It carries neither harness-ready nor harness-exempt, so Ship runs harness-architect for the W3 ready set, which is {174.076-T}.
* **Deliverable already exists.** Ship's validated manifest edit is uncommitted in the working tree (SHA-256 of the file at Stage review time is recorded in the Stage decision comment). It already satisfies AC1 and AC2, and it passed the probe and the regression selector on 2026-09-25. Nothing is re-authored. It is only set aside for the red phase and then restored.

**Harness specification (harness-architect writes exactly this):**
* File: tests/integration/harness_manifest_budget_reconciliation_test.go.
* Package: integration_test.
* Imports: only os, path/filepath, regexp, strings, testing, gopkg.in/yaml.v3 (already in go.mod), and testify require/assert.
* Uses the existing testRepoRoot(t) helper.
* One top-level function: TestHarnessManifestBudgetReconciliation.
  * Read .autoharness/harness-manifest.yaml and yaml.Unmarshal it into a struct with Artifacts []{Path `yaml:path`, Checksum `yaml:checksum`, DriftAllowed bool `yaml:drift_allowed`, DriftReason `yaml:drift_reason`}. require.NoError: yaml.v3 rejects a duplicate mapping key, so parse success also proves AC1's no-duplicate rule.
  * Two t.Run subtests, workflow-policies (path .github/policies/workflow-policies.md) and ship-agent (path .github/agents/_ship.agent.md), each asserting:
    * (a) exactly one artifact has the path;
    * (b) drift_allowed is true;
    * (c) strings.Count(drift_reason, S) == 1, where S is the exact AC2 sentence;
    * (d) checksum matches ^[0-9a-f]{64}$ AND is not the pre-reconciliation value: workflow-policies d941c1c78f33a46c0afd9e743ccd606d3da4316cada7260871708c4b91d9290b; ship-agent d6b046f8d2299e8b146df95994fa7d5d6472c28eec56335a8e8988fbf242a3d9.
* **Deliberately NOT asserted: checksum currency against the target files.** Plan D7 rejected a permanent currency test as over-coupling, because every future edit of either file would fail CI. The working-tree _ship.agent.md also carries the operator-owned tools line (LF SHA-256 a627c45a...), so a currency test would be red in this workspace and in the governed full suite. Exact currency is proven by the AC5 task-gate probe instead.
* Stage verified the design read-only with python yaml:
  * HEAD manifest: both entries have 0 sentence occurrences and the stale checksums above, so the test goes red on (c) and (d);
  * working-tree manifest: 1 occurrence each, with checksums equal to the LF SHA-256 of the HEAD blobs, so the test goes green.

**Ship execution sequence:**
1. **Set aside the deliverable.** Copy-Item .autoharness/harness-manifest.yaml to logs/diagnostics/174076-manifest-deliverable-backup.yaml (logs/ is git-ignored; confirm with git check-ignore -v). Record both SHA-256 values; they must be equal.
2. **Restore the HEAD manifest.** Run git restore --source=HEAD --worktree -- .autoharness/harness-manifest.yaml, then confirm git diff --quiet HEAD -- .autoharness/harness-manifest.yaml exits 0. This is an overwrite with a hash-verified backup, the same shape as the approved _ship.agent.md swap. P-002.5 / Principle VII screening applies: if Ship's screen routes it to operator approval, halt and request approval. Do not proceed without a verified backup.
3. **Scaffold and confirm red (harness-architect).**
   * Scaffold the harness.
   * Run go vet ./tests/integration: exit 0.
   * Run the red selector (AC3): exit 1, assertion-red.
   * Optional corroboration: the checksum probe exits 1 with no marker.
   * Commit the test file only, for example: test(core): add harness-manifest budget reconciliation harness.
   * Apply harness-ready through backlogit and record Compilation: PASS / Red Phase: CONFIRMED.
4. **Claim and restore.** Move the task to active and capture START/baseline SHA = HEAD. Copy the backup back over .autoharness/harness-manifest.yaml, then verify its SHA-256 equals the recorded backup SHA-256.
5. **Green gates.**
   * The AC3 selector passes: 1 top-level PASS.
   * go vet ./tests/integration exits 0.
   * The AC6 regression selector passes: 1 PASS.
   * The AC5 checksum probe passes, under the approved _ship.agent.md backup / HEAD-swap / restore procedure. Verify the restored _ship.agent.md LF-SHA256 is a627c45a0fbd2386e6e5b1316961f0f99626de9933532b4f6d30a3c52f39aedb and the tools line is the only diff.
   * Scoped lint/format is limited to the new test file (D3).
6. **Commit and close.** Commit the manifest only, for example: chore(core): reconcile harness manifest checksums for budget surfaces. Then run the AC7 scope check, track the commit, complete and archive the task, push, and STOP (AC9).

**Checksum probe (AC5 task gate; formerly the exempt_verification_command, now with the marker renamed):**

$m = Get-Content '.autoharness/harness-manifest.yaml'; foreach ($f in @('.github/agents/_ship.agent.md', '.github/policies/workflow-policies.md')) { $t = [IO.File]::ReadAllText((Resolve-Path $f)).Replace([string][char]13 + [char]10, [string][char]10); $h = [BitConverter]::ToString([Security.Cryptography.SHA256]::Create().ComputeHash([Text.Encoding]::UTF8.GetBytes($t))).Replace('-', '').ToLower(); $i = [Array]::IndexOf($m, '  - path: ' + [char]34 + $f + [char]34); if ($i -lt 0) { exit 1 }; $j = $i + 1; $ok = 0; $ck = 0; $dr = 0; $dok = 0; while ($j -lt $m.Count -and -not $m[$j].StartsWith('  - path:')) { $l = $m[$j].Trim(); if ($l.StartsWith('checksum:')) { $ck++ }; if ($l.StartsWith('drift_reason:')) { $dr++; if ($l.Contains('073-DL rev22')) { $dok++ } }; if ($l -eq ('checksum: ' + [char]34 + $h + [char]34)) { $ok++ }; $j++ }; if ($ok -ne 1 -or $ck -ne 1 -or $dr -ne 1 -or $dok -ne 1) { exit 1 } }; Write-Output 'CHECKSUM_PROBE_OK:174.076-T'; exit 0

**Unchanged from rev22.3:**
* the checksum method (174.072-T AC3 precedent);
* the drift-reason sentence;
* the npx regression selector;
* the D1 full-suite deferral;
* the push-after-W3 rule;
* the AC9 stop.

**Removed:**
* the docs-only class, the harness-exempt label, and the exemption contract block;
* the D7 classification, which is superseded for this task by D8 in plan rev22.4.
<!-- END:implementation-notes -->
