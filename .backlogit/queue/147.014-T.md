---
artifact_type: task
created_at: 2026-08-24T19:04:27.7497239Z
dependencies:
    - 147.012-T
    - 147.013-T
id: 147.014-T
labels:
    - checkpoint
    - mcp
    - docs
    - plan-u7b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U7b: MCP tool descriptions state the refusal contract'
updated_at: 2026-08-24T19:04:27.7497239Z
---

Plan unit **U7b**. Update the five MCP checkpoint tool descriptions.

Files: `internal/mcp/tools.go` (descriptions at `:176-224`), `internal/mcp/tools_test.go`.

Tool descriptions are the **only** contract most agents ever read; they are the primary context surface. Leaving them stale means an agent selects `resolve` for a legacy file, receives a refusal it was never told about, and burns a retry cycle.

Exact replacement strings (current text captured verbatim in the plan):

| Line | Tool | Delta |
|---|---|---|
| `:178` | `list_checkpoints` | append: ` A summary with needs_quarantine true is not safely rewritable; use quarantine_checkpoint, not resolve_checkpoint or abandon_checkpoint.` |
| `:189` | `get_checkpoint` | append: ` Returns conforming false when the document carries unmodeled top-level keys; such a document cannot be resolved or abandoned.` |
| `:196` | `resolve_checkpoint` | append: ` Refuses with checkpoint_use_quarantine when the stored document is schema-invalid or carries unmodeled top-level keys.` |
| `:211` | `abandon_checkpoint` | append: ` Also refuses when the document carries unmodeled top-level keys.` |
| `:220` | `quarantine_checkpoint` | replace `malformed checkpoint file` → `checkpoint file that cannot be safely rewritten (malformed, schema-invalid, or carrying unmodeled top-level keys)` |

<!-- BEGIN:acceptance-criteria -->
- A table-driven test over the five tool names asserts each description contains its required substring
- `quarantine_checkpoint` no longer says only `malformed`
- The test reads registered descriptions from the built tool set, not from a duplicated literal
- Expected red: all five rows fail
<!-- END:acceptance-criteria -->