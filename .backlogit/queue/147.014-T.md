---
artifact_type: task
created_at: 2026-08-24T19:04:27.7497239Z
dependencies:
    - 147.012-T
    - 147.022-T
    - 147.023-T
    - 147.016-T
id: 147.014-T
labels:
    - checkpoint
    - mcp
    - docs
    - plan-u7b
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U7b: MCP read-surface tool descriptions state the conformance contract'
updated_at: 2026-08-25T17:31:03.957274Z
---

Plan unit **U7b** (partition 4). MCP read-surface tool descriptions - exact replacement strings.

Files: `internal/mcp/tools.go` (descriptions at `:178` and `:189`, one line after each `mcplib.NewTool` name literal at `:177` and `:188`), `internal/mcp/tools_test.go`; `.autoharness/backlog-registry.yaml` and `internal/cli/registry_parity_test.go` re-run only if the registry carries description text for these two tools.

**Cycle-17 correction (gate finding H6): tool names are the registered `backlogit_*` identifiers**, verified against the `mcplib.NewTool` literals at `internal/mcp/tools.go:177`, `:188`, `:195`, `:209`, and `:218`. A description that names a bare `quarantine_checkpoint` tells an agent to call a tool that is not registered.

| Line | Registered tool | Delta |
|---|---|---|
| `:178` | `backlogit_list_checkpoints` | append: ` A summary with needs_quarantine true is not safely rewritable; use backlogit_quarantine_checkpoint, not backlogit_resolve_checkpoint or backlogit_abandon_checkpoint. Such a summary is returned regardless of the status, agent, shipment_id, feature_id, and max_age filters, so a filtered result can contain rows that do not match the filter. The accompanying remediation_intent is a structured record of the required disposition, not a runnable command.` |
| `:189` | `backlogit_get_checkpoint` | append: ` For a schema-valid document, returns conforming false when it carries unmodeled top-level keys; such a document cannot be resolved or abandoned. non_conforming_fields carries raw offender paths with explicit truncation counts. A schema-invalid document is refused before any conformance verdict is produced.` |

The `backlogit_get_checkpoint` qualifier is load-bearing: `GetCheckpoint` returns `ErrCheckpointInvalid` before any conformance result exists, so an unqualified promise would describe a verdict the read path cannot produce for the nine legacy files. The filter sentence is why this task depends on 147.023-T / U6d.

Red verification: `go test -count=1 -run '^TestU7b_' ./internal/mcp`.

<!-- BEGIN:acceptance-criteria -->
- A table-driven assertion over the two registered read descriptions, read from the **built tool set** rather than a duplicated literal, each row keyed by its registered `backlogit_*` name and asserting its required substring
- No description references a bare, unregistered tool name
- The registry-parity / fallback-map drift test is re-run and stays green, with `.autoharness/backlog-registry.yaml` updated in the same commit if it carries description text for these tools
- Expected red: both rows of case 1 fail; the drift test is a declared regression guard
<!-- END:acceptance-criteria -->