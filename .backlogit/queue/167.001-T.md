---
artifact_type: task
created_at: 2026-09-06T00:58:21.212265Z
dependencies:
    - 167.010-T
    - 167.002-T
    - 167.012-T
id: 167.001-T
parent_id: 167-F
priority: critical
status: queued
title: Preconditions + shipment-bound evidence verification (U2)
updated_at: 2026-09-06T03:35:19.5642938Z
---

U2 (TDD: table-driven precondition tests FIRST). reconcile.trusted_refs config schema/loader is SPLIT OUT to 167.012-T (width-isolation); this task is CORE EVIDENCE CODE only. TWO tiers, both invoked by the U1 transaction under the held membership+shipment locks: (A) REQUEST-IDENTITY MATERIALIZATION (cheap, NO external file read) - compute the request-identity digest = hash over request SCALARS only (idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH), invoked BEFORE classification so the classifier can compare replay identity WITHOUT depending on the closure still existing on disk (a renamed/deleted closure after a successful repair still returns no_op). (B) DELIVERY-EVIDENCE VERIFICATION + WRITE-PATH PRECONDITION GATE (branch 2d only): (1) id is a SHIPMENT resolved UNDER .backlogit/archive/ (also checked in the transaction identity/location gate under lock); (2) status==archived AND archived_status in supportedLegacyShippedPreStates={ShipmentActive} v1 else ErrUnsupportedLegacyPreState; (3) no conflicting history via the SHARED state classifier from 167.010-T (reaching here = classifier branch 2d), re-confirmed under lock; (4) manifest NON-EMPTY + EVERY member terminal (done) via NormalizeShipmentItems + a member-status read that RELOADS EACH LOCKED MEMBER FROM ITS MARKDOWN FILE (authoritative), NOT loadArtifact/SQLite (index-first shipment.go:789-808, would miss an out-of-band non-terminal edit after last sync; mirror shipment_lifecycle.go:1077-1089); empty manifest REJECTED; NO operator descoping v1; (5) MergeSHA validated isGitObjectName BEFORE git, boundedHelperTimeout, reachable from a TRUSTED REF from WorkspaceConfig.reconcile.trusted_refs (field owned by 167.012; default repo default branch); (6) ClosureEvidencePath real-path contained + opened no-follow/reparse-safe + handle-validated (fstat/final-path regular non-empty), MUST REFERENCE this shipment id (lenient-but-bound - real 048-S shipments-list/body shape), SHIPMENT-SCOPED id->merge association cross-checked against --merge-sha (another shipment SHA REJECTED - negative test); compute the FULL evidence_digest = hash(shipment-id + merge-sha + manifest-digest + closure CONTENT-HASH + ordered EvidenceRefs) and the complete-event digest over (reason/actor/second_approver/idempotency_key/evidence_digest) - these are computed on the WRITE path and persisted; the closure CONTENT-HASH is NEVER recomputed on replay. SecondApprover when supplied MUST differ from Actor (equal=rejected). EvidenceRefs each non-empty (path entries real-path contained), persisted in the U3 event + folded into evidence_digest; second_approver persisted. Persist BOTH shipment_reconciliation_request_identity_digest (for replay identity, scalar) AND shipment_reconciliation_event_digest (full, content-hash-bound, for audit). Typed sentinels ErrShipmentReconcileConflict/ErrShipmentReconcileEvidence/ErrUnsupportedLegacyPreState; fail closed on ambiguity/partial/conflict; each failure performs NO write. Files internal/core/shipment_reconcile_preconditions.go + test. Domain: code. See plan U2. Deps 167.010 (classifier) + 167.002 (event/validator) + 167.012 (trusted_refs config).