---
artifact_type: task
created_at: 2026-09-06T00:58:21.212265Z
dependencies:
    - 167.010-T
    - 167.002-T
id: 167.001-T
parent_id: 167-F
priority: critical
status: queued
title: Preconditions + shipment-bound evidence verification (U2)
updated_at: 2026-09-06T01:55:31.9179355Z
---

U2 (TDD: table-driven precondition tests FIRST). Precondition gate invoked by U1 under lock, after the idempotency short-circuit: (1) id is a SHIPMENT (artifact_type shipment) resolved UNDER .backlogit/archive/; (2) status==archived AND archived_status in supportedLegacyShippedPreStates={ShipmentActive} v1 else ErrUnsupportedLegacyPreState; (3) no conflicting history via the SHARED state classifier from 167.010-T (reaching here = classifier branch 2d), re-confirmed under lock; (4) manifest NON-EMPTY + EVERY member terminal (done) via NormalizeShipmentItems + a member-status read that RELOADS EACH LOCKED MEMBER FROM ITS MARKDOWN FILE (authoritative), NOT loadArtifact/SQLite (index-first shipment.go:789-808, would miss an out-of-band non-terminal edit after last sync; mirror shipment_lifecycle.go:1077-1089); empty manifest REJECTED; NO operator descoping v1; (5) MergeSHA validated by isGitObjectName BEFORE git, boundedHelperTimeout, reachable from a TRUSTED REF (NEW config WorkspaceConfig.reconcile.trusted_refs - schema+loader+validation+config tests in THIS unit; default = repo default branch when unset; NOT the pre-task base_ref); (6) ClosureEvidencePath real-path contained + opened no-follow/reparse-safe + handle-validated (fstat/final-path regular non-empty); the closure MUST REFERENCE this shipment id (lenient-but-bound - accepts the real 048-S shape: shipments-list frontmatter OR body reference); authoritative MergeSHA from --merge-sha (reachability-checked in 5); SHIPMENT-SCOPED ID->merge extraction (the real combined 047-S/048-S closure names MULTIPLE shipments + SHAs, so associate THIS shipment id with its OWN merge via the shipment-scoped section/mapping and cross-check --merge-sha against THAT; another shipment's SHA is REJECTED - negative test); FULL manifest verified against the shipment OWN manifest (NormalizeShipmentItems + member-terminal in 4), NOT required in the closure; unrelated closure that never names the shipment REJECTED. SecondApprover when supplied MUST differ from Actor (equal=rejected). EvidenceRefs each non-empty (path entries real-path contained). Files internal/core/shipment_reconcile_preconditions.go + internal/config schema/loader for reconcile.trusted_refs + tests. evidence_digest = hash(shipment-id + merge-sha + manifest-digest + closure content-hash + ordered EvidenceRefs). (7) optional EvidenceRefs: each non-empty, path entries real-path contained under workspace, persisted in the U3 event (evidence_refs) + folded into evidence_digest (replay with different refs = mismatch); SecondApprover when supplied is a non-empty self-asserted approver persisted in the event (second_approver). Typed sentinels ErrShipmentReconcileConflict/ErrShipmentReconcileEvidence/ErrUnsupportedLegacyPreState; fail closed on ambiguity/partial/conflict; each failure performs NO write. Files internal/core/shipment_reconcile_preconditions.go + test. Domain: code. See plan U2.