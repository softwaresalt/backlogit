---
artifact_type: task
created_at: 2026-09-06T00:58:21.212265Z
dependencies:
    - 167.002-T
    - 167.012-T
id: 167.001-T
parent_id: 167-F
priority: critical
status: queued
title: Preconditions + shipment-bound evidence verification (U2)
updated_at: 2026-09-06T05:15:41.9619048Z
---

U2 DELIVERY-EVIDENCE (TDD: table-driven evidence tests FIRST; the identity/pre-state/manifest-terminality PRECONDITION GATE is SPLIT OUT to 167.014-T, and reconcile.trusted_refs config to 167.012-T, per 2-hour/width-isolation). This task = the EVIDENCE domain only (git + FS + hashing), producing the request-identity digest, the delivery-evidence result, and the persisted digests. TWO tiers used by the U1 two-phase transaction: (A) REQUEST-IDENTITY MATERIALIZATION (cheap, NO file read) - the request-identity digest = hash over request SCALARS only using a DOMAIN-SEPARATED CANONICAL encoding (each field length-prefixed and field-tagged, NOT bare concatenation, so distinct tuples cannot collide by shifting field boundaries): idempotency_key + merge_sha + reason + actor + second_approver + ordered evidence_refs + closure PATH; computed in transaction PHASE A, compared by the classifier in PHASE B, so a same-key REPLAY never depends on the closure still existing (renamed/deleted closure still returns no_op). (B) DELIVERY-EVIDENCE VERIFICATION (branch 2d ONLY; slow I/O in PHASE C with NO artifact batch held - the membership lock keeps the manifest stable; re-checked in PHASE D): (5) MergeSHA validated isGitObjectName BEFORE git, boundedHelperTimeout, reachable from a TRUSTED REF from WorkspaceConfig.reconcile.trusted_refs (field owned by 167.012; default repo default branch) AND MUST be a TRUE MERGE COMMIT (>=2 parents via git rev-list --parents / cat-file - merge-base --is-ancestor accepts any ancestor and P-009 mandates merge-commit delivery; negative test: reachable NON-merge commit REJECTED); (6) ClosureEvidencePath real-path contained + opened no-follow/reparse-safe + handle-validated (fstat/final-path regular non-empty), MUST REFERENCE this shipment id via a DETERMINISTIC DELIVERY-MERGE grammar keyed on the ROLE ANNOTATION in the real 048-S fixture (e.g. "Merged as PR #101 (feature), commit ac8847..., and PR #102 (post-merge closure), commit 0cf49a..."): the delivery merge is the (feature)-annotated commit; a (post-merge closure)/(closure)-annotated commit is NOT the delivery merge (no literal delivery-merge label exists in the fixture, so a label-only grammar would REJECT the real fixture); cross-check --merge-sha against the (feature) commit; negative tests: (i) supplying the (post-merge closure) SHA 0cf49a as --merge-sha when delivery is ac8847 is REJECTED, (ii) another shipment SHA REJECTED. Compute evidence_digest = hash(shipment-id + merge-sha + manifest-digest + closure CONTENT-HASH + ordered EvidenceRefs); the closure CONTENT-HASH is NEVER recomputed on replay. (7) EvidenceRefs each non-empty (path entries real-path contained), persisted in the U3 event + folded into evidence_digest. Build the persist-ready CANONICAL FULL events.Event (wrapper + delta) and the complete-event digest over that full Event. Persist (via 167.008 write) BOTH shipment_reconciliation_request_identity_digest (scalar replay identity) AND shipment_reconciliation_event_digest (full-event, content-hash-bound audit). Typed sentinel ErrShipmentReconcileEvidence; fail closed; NO write on failure. Files internal/core/shipment_reconcile_evidence.go + test. Domain: code. See plan U2. Deps 167.002 (event/validator) + 167.012 (trusted_refs config).