---
artifact_type: task
created_at: 2026-09-06T00:58:21.212265Z
dependencies:
    - 167.010-T
    - 167.002-T
    - 167.012-T
id: 167.001-T
parent_id: 167-F
priority: critical
status: queued
title: Preconditions + shipment-bound evidence verification (U2)
updated_at: 2026-09-06T03:18:51.880108Z
---

U2 (TDD: table-driven precondition tests FIRST). reconcile.trusted_refs config schema/loader is SPLIT OUT to 167.012-T (width-isolation: config domain); this task is CORE EVIDENCE CODE only. Two responsibilities, both invoked by the U1 transaction under lock: (A) EVIDENCE MATERIALIZATION -> incoming complete-event digest, invoked BEFORE classification so the classifier can compare replay identity: validate MergeSHA (isGitObjectName BEFORE git, boundedHelperTimeout, reachable from a TRUSTED REF drawn from WorkspaceConfig.reconcile.trusted_refs [field owned by 167.012]; default repo default branch); ClosureEvidencePath real-path contained + opened no-follow/reparse-safe + handle-validated (fstat/final-path regular non-empty), MUST REFERENCE this shipment id (lenient-but-bound - real 048-S shipments-list/body shape), SHIPMENT-SCOPED id->merge extraction cross-checked against --merge-sha (another shipment SHA REJECTED - negative test); compute evidence_digest = hash(shipment-id + merge-sha + manifest-digest + closure content-hash + ordered EvidenceRefs) and the complete-event digest over (reason/actor/second_approver/idempotency_key/evidence_digest). (B) WRITE-PATH PRECONDITION GATE (branch 2d only): (1) id is a SHIPMENT resolved UNDER .backlogit/archive/ (the cheap identity/location subset also runs in the transaction pre-gate before classification); (2) status==archived AND archived_status in supportedLegacyShippedPreStates={ShipmentActive} v1 else ErrUnsupportedLegacyPreState; (3) no conflicting history via the SHARED state classifier from 167.010-T (reaching here = classifier branch 2d), re-confirmed under lock; (4) manifest NON-EMPTY + EVERY member terminal (done) via NormalizeShipmentItems + a member-status read that RELOADS EACH LOCKED MEMBER FROM ITS MARKDOWN FILE (authoritative), NOT loadArtifact/SQLite (index-first shipment.go:789-808, would miss an out-of-band non-terminal edit after last sync; mirror shipment_lifecycle.go:1077-1089); empty manifest REJECTED; NO operator descoping v1; (5)/(6) merge+closure evidence as materialized in (A). SecondApprover when supplied MUST differ from Actor (equal=rejected). EvidenceRefs each non-empty (path entries real-path contained), persisted in the U3 event (evidence_refs) + folded into evidence_digest (replay with different refs = mismatch); second_approver persisted in the event. Typed sentinels ErrShipmentReconcileConflict/ErrShipmentReconcileEvidence/ErrUnsupportedLegacyPreState; fail closed on ambiguity/partial/conflict; each failure performs NO write. Files internal/core/shipment_reconcile_preconditions.go + test. Domain: code. See plan U2. Deps 167.010 (classifier) + 167.002 (event/validator) + 167.012 (trusted_refs config).