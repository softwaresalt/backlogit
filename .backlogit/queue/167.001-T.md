---
artifact_type: task
created_at: 2026-09-06T00:58:21.212265Z
id: 167.001-T
parent_id: 167-F
priority: critical
status: queued
title: Preconditions + shipment-bound evidence verification (U2)
updated_at: 2026-09-06T01:14:25.6046912Z
---

U2 (TDD: table-driven precondition tests FIRST). Precondition gate invoked by U1 under lock, after the idempotency short-circuit: (1) id is a SHIPMENT (artifact_type shipment) resolved UNDER .backlogit/archive/; (2) status==archived AND archived_status in supportedLegacyShippedPreStates={ShipmentActive} v1 else ErrUnsupportedLegacyPreState; (3) no conflicting history via the SHARED state classifier (reaching here = classifier branch 2d), re-confirmed under lock; (4) manifest NON-EMPTY + EVERY member terminal (done) via NormalizeShipmentItems + member read; empty manifest REJECTED; NO operator descoping v1; (5) MergeSHA validated by isGitObjectName BEFORE git, boundedHelperTimeout, reachable from a CONFIGURED TRUSTED REF (allowlist; default repo default branch) - not merely any object; (6) ClosureEvidencePath real-path contained + opened no-follow/reparse-safe + handle-validated (fstat/final-path regular non-empty), STRUCTURED record enumerating shipment-id + same MergeSHA + FULL manifest set matching NormalizeShipmentItems (any id/SHA/manifest mismatch fails closed); evidence_digest = hash(shipment-id + merge-sha + manifest-digest + closure content-hash + ordered EvidenceRefs). (7) optional EvidenceRefs: each non-empty, path entries real-path contained under workspace, persisted in the U3 event (evidence_refs) + folded into evidence_digest (replay with different refs = mismatch); SecondApprover when supplied is a non-empty self-asserted approver persisted in the event (second_approver). Typed sentinels ErrShipmentReconcileConflict/ErrShipmentReconcileEvidence/ErrUnsupportedLegacyPreState; fail closed on ambiguity/partial/conflict; each failure performs NO write. Files internal/core/shipment_reconcile_preconditions.go + test. Domain: code. See plan U2.