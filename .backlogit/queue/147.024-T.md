---
artifact_type: task
created_at: 2026-08-24T21:15:00.0000000Z
dependencies:
    - 147.013-T
    - 147.025-T
id: 147.024-T
labels:
    - checkpoint
    - mcp
    - docs
    - plan-u7c
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U7c: MCP mutation-surface tool descriptions state the refusal contract'
updated_at: 2026-08-24T21:15:00.0000000Z
---

Plan unit **U7c**. Update the **three mutation-surface** MCP checkpoint tool descriptions.

Files: `internal/mcp/tools.go` (descriptions at `:193-224`), `internal/mcp/tools_test.go`. If `.autoharness/backlog-registry.yaml` carries description text for these three tools, update it in the same commit and re-run `internal/cli/registry_parity_test.go`.

**Scope note.** Split out of 147.014-T (U7b), which retains the two read-surface rows. Five description rows in one task exceeded the four-scenario limit and mixed a read contract with a refusal contract.

An agent that selects `resolve` for a legacy file and receives a refusal it was never told about burns a retry cycle; these three descriptions are where that refusal is announced.

Exact replacement strings (current text captured verbatim in the plan):

| Line | Tool | Delta |
|---|---|---|
| `:196` | `resolve_checkpoint` | append: ` Refuses a stored document it cannot safely rewrite rather than replacing it: checkpoint_use_quarantine when the document is schema-invalid, checkpoint_non_conforming when it carries unmodeled top-level keys. Use quarantine_checkpoint instead.` |
| `:211` | `abandon_checkpoint` | append: ` Also refuses when the document carries unmodeled top-level keys.` |
| `:220` | `quarantine_checkpoint` | replace `malformed checkpoint file` → `checkpoint file that cannot be safely rewritten (malformed, schema-invalid, or carrying unmodeled top-level keys)` |

The `resolve_checkpoint` row promises two **codes**, which only reach that surface once 147.025-T (U7d) routes the handler through `checkpointDispositionError`; that is why this task depends on it as well as on the 147.013-T mapping. This table is reproduced verbatim in plan unit U7c; the two must not drift.

<!-- BEGIN:acceptance-criteria -->
- A table-driven test over the three mutation tool names asserts each description contains its required substring
- The `resolve_checkpoint` description distinguishes `checkpoint_use_quarantine` (schema-invalid) from `checkpoint_non_conforming` (unmodeled top-level keys), matching the U7 mapping in 147.013-T and the routing in 147.025-T
- `quarantine_checkpoint` no longer says only `malformed`
- Expected red: all three rows fail
<!-- END:acceptance-criteria -->