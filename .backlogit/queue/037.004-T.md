---
artifact_type: task
created_at: 2026-04-21T09:22:32.3218273-07:00
id: 037.004-T
labels:
    - code
    - tdd
    - mcp
parent_id: 037-F
priority: medium
references:
    - docs/exec-plans/2026-04-21-mcp-merge-sync-plan.md
status: active
title: Server Manifest Integration
updated_at: 2026-04-21T09:30:17.6565466-07:00
---

Unit 4: Wire manifest and merge_sync tool into the MCP server.

Files: internal/mcp/server.go (modify), internal/mcp/tools.go (modify).

Changes to Server struct:
- Add `manifest map[string]db.FileEntry` field
- Add `manifestMu sync.RWMutex` (separate from workspaceMu, per D5/R10)

Modify ensureWorkspace: after NewWorkspace succeeds, call db.RehydrateWithManifest and store returned manifest (one-time baseline, R1).

Register backlogit_merge_sync tool with dry_run boolean parameter.

handleMergeSync handler:
1. requireWorkspace (existing pattern)
2. manifestMu.RLock → snapshot manifest → RUnlock
3. Call db.MergeSync(ctx, storagePath, ws.DB, snapshot, dryRun)
4. If not dry-run: manifestMu.Lock → update manifest → Unlock
5. Return MergeSyncResult as JSON

P2 advisory: Document lock ordering (workspaceMu before manifestMu, never hold both). Add tool to metadata catalog.

Pattern: ensureWorkspace mutex (server.go:106-128), handleSyncIndex (tools.go:720-729).
Depends on Units 2+3.

Verification:
- Tool registered and discoverable via MCP tool list
- Handler returns structured JSON matching MergeSyncResult schema
- Manifest populated after first workspace init
- Concurrent handleMergeSync calls serialize via manifestMu
- Dry-run flag respected
- go test -race ./internal/mcp/... passes