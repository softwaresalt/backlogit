---
artifact_type: task
created_at: 2026-09-14T01:05:52.745215Z
dependencies:
    - 173.001-T
    - 173.007-T
id: 173.002-T
parent_id: 173-F
priority: high
references:
    - docs/exec-plans/2026-09-13-shipment-claim-scheduler-reconciliation-plan.md
status: queued
title: 'Read accessor: distinguish claim-activated from organic-active'
updated_at: 2026-09-14T06:00:45.2443306Z
---

U2 [CC0EBB59]. Expose the claim-activated-vs-organic distinction through the ALREADY-SUPPORTED generic custom_fields projection (verified: CLI get --format json buildDetailMap copies all frontmatter keys; MCP backlogit_get_item via scanArtifactRow rehydrates custom_fields from the JSON column persisted by UpsertItem) — NOT via an internal/core-only Go accessor and NOT via a new bespoke marker-specific projection field (a divergent side channel is forbidden). U2 is primarily VERIFICATION that the pinned `scheduler_baseline_claim` key materializes identically on both the CLI (frontmatter-backed) and MCP (DB-backed) read paths, plus an optional thin read-only helper explicitly labeled non-contract. INDEX FRESHNESS: the claim-activation write must upsert custom_fields into the SQLite index in the same operation so the DB-backed MCP surface is not stale vs the CLI surface. Authoritative transports: `backlogit get --format json` and MCP `backlogit_get_item` (both yield custom_fields.scheduler_baseline_claim); SQL query is secondary (json_extract on the opaque blob). Record-only claim mode dropped (universal marker). Domain: code. Depends on U1 (173.001-T) and U0b (173.007-T, RED). AC: the pinned marker is present in BOTH CLI get --format json and MCP get_item projections for a claim-activated item and absent for an organic-active item; no bespoke projection field and no internal-core-only accessor as contract; no state mutation.