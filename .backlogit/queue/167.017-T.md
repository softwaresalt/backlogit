---
artifact_type: task
created_at: 2026-09-06T07:07:14.1856067Z
custom_fields:
    source_stash_id: 763A1152
id: 167.017-T
parent_id: 167-F
priority: critical
status: queued
title: Lock-order coherence + reload-under-lock (deadlock prerequisite for reconcile)
updated_at: 2026-09-06T07:07:14.1856067Z
---

DEADLOCK PREREQUISITE for the reconcile transaction (167.008) - harvested from stash 763A1152. Establish coherent cross-path locking so the C->B reconcile is deadlock-safe AND clobber-safe: (1) GLOBAL BLOCKING LOCK ORDER - choose ONE global order for the blocking artifact-mutation lock (B) and item-log lock (C) and refactor the divergent EXISTING path so ALL blocking B/C acquirers agree: ArchiveItem/ReconcileArchivedLifecycle (B->C, archive.go:99,292; archive_reconcile.go:117,281), AssociateCommit (C->B, commits.go:101-131 -> shipment.go:839-844), AND ShipShipment (holds its outer artifact batch B until return - shipment_lifecycle.go:614, deferred releaser :469-474 - then acquires item-log C via attachCommitToItems->LinkCommit :1106 and archiveItems->ArchiveItem archive.go:292, i.e. B->C), plus LinkCommit itself, and the new reconcile; both use blocking locks (LockItemLogCrossProcess is not a try-lock) so today NO single order is safe. (2) RELOAD-UNDER-LOCK for snapshot-before-lock writers - AssociateCommit READS the full artifact BEFORE acquiring its locks (commits.go:60-102), so even with a coherent order its STALE pre-lock snapshot can OVERWRITE a completed reconciliation after locks are released; require AssociateCommit (and any snapshot-before-lock artifact writer) to RE-READ/RE-VALIDATE the artifact AFTER acquiring its locks (read-under-lock or compare-and-swap on the on-disk state) so a stale snapshot cannot clobber a concurrent write. (3) concurrency tests over the EXISTING paths ONLY - ArchiveItem, ReconcileArchivedLifecycle, AssociateCommit, ShipShipment, and LinkCommit - proving the new global order + reload-under-lock neither deadlocks nor clobbers among THEM (this task is ordered BEFORE 167.008 and does not depend on 167.003, so the new reconcile symbol does not yet exist here; the three-way reconcile-vs-ArchiveItem-vs-AssociateCommit race test stays with 167.008, which already mandates it). This is a CROSS-CUTTING change to EXISTING core paths, gating 167.008; it MUST be completed before the reconcile transaction is implemented deadlock/clobber-safe. Domain: code (concurrency). Blocks 167.008.