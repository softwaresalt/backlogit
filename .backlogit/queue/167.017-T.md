---
artifact_type: task
created_at: 2026-09-06T07:07:14.1856067Z
custom_fields:
    source_stash_id: 763A1152
id: 167.017-T
parent_id: 167-F
priority: critical
status: queued
title: Stable item-log lock identity migration (deadlock prereq 1/4)
updated_at: 2026-09-06T08:12:55.5614697Z
---

DEADLOCK PREREQUISITE part 1/4 (stable item-log lock identity) - split from the original 763A1152 cross-cutting fix per the 2-hour/width-isolation rule (siblings: 167.018 order refactor, 167.019 reload/CAS, 167.020 concurrency suite). Migrate LockItemLogCrossProcess (stream.go:167-180 recomputes the sidecar PATHNAME and opens it through the CURRENT directory, so after a logs-dir swap two lockers open DIFFERENT inodes and do NOT mutually exclude) AND all its callers (AssociateCommit commits.go:102, ArchiveItem archive.go:292, LinkCommit, ShipShipment) to a STABLE, NON-SWAPPABLE lock IDENTITY: the itemID (caller-controlled, reached BEFORE the identity/location gate since C is acquired first) MUST be ENCODED into a SINGLE safe filename component (as artifactMutationLockPath does - NOT joined raw, because a mere containment check still permits a contained multi-component id like x/y where x is a symlink that escapes the lock root; reject or encode away path separators so no traversal/symlink escape is possible); the lock helper MUST open the sidecar HANDLE-BOUND through a validated .backlogit/.locks/itemlog directory HANDLE with no-follow/reparse-safe semantics (NOT by pathname - task_lock.go:71-72 / task_lock_unix.go:13-18 open by path, so replacing the lock DIRECTORY after validation would let a second caller lock a DIFFERENT inode while the first holds the old one; bind all lock ops to the verified directory handle). the canonical lock sidecar lives in a stable locks root under a DISTINCT item-log-lock NAMESPACE (e.g. .backlogit/.locks/itemlog/<itemID> - NOT the same key as lockShipmentMembership, which uses .backlogit/.locks/<shipmentID> -> .<shipmentID>.lock via task_lock.go:50-72; a shipment item has itemID==shipmentID, so a shared root keyed only by item identity would ALIAS the membership lock A and collapse the A/C distinction). The item-log lock C MUST be a resource DISTINCT from the membership lock A. NOT relative to the swappable logs directory. TDD: a test proves two lockers mutually exclude on the same item INCLUDING after a logs-dir swap. Single domain (lock-identity migration). Domain: code (concurrency). Blocks 167.018/167.011.