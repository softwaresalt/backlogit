---
artifact_type: task
created_at: 2026-09-06T07:07:14.1856067Z
id: 167.017-T
parent_id: 167-F
priority: critical
status: queued
title: Lock-order coherence + reload-under-lock (deadlock prerequisite for reconcile)
updated_at: 2026-09-06T07:07:14.1856067Z
---

DEADLOCK PREREQUISITE for the reconcile transaction (167.008) - harvested from stash 763A1152. Establish coherent cross-path locking so the C->B reconcile is deadlock-safe AND clobber-safe: (1) GLOBAL BLOCKING LOCK ORDER - choose ONE global order for the blocking artifact-mutation lock (B) and item-log lock (C) and refactor the divergent EXISTING path so all of ArchiveItem/ReconcileArchivedLifecycle (currently B->C, archive.go:99,292; archive_reconcile.go:117,281), AssociateCommit (currently C->B, commits.go:101-131 -> shipment.go:839-844), and the new reconcile agree; both use blocking locks (LockItemLogCrossProcess is not a try-lock) so today NO single order is safe. (2) RELOAD-UNDER-LOCK for snapshot-before-lock writers - AssociateCommit READS the full artifact BEFORE acquiring its locks (commits.go:60-102), so even with a coherent order its STALE pre-lock snapshot can OVERWRITE a completed reconciliation after locks are released; require AssociateCommit (and any snapshot-before-lock artifact writer) to RE-READ/RE-VALIDATE the artifact AFTER acquiring its locks (read-under-lock or compare-and-swap on the on-disk state) so a stale snapshot cannot clobber a concurrent write. (3) a same-shipment concurrency test proving reconcile vs ArchiveItem vs AssociateCommit neither deadlocks nor clobbers. This is a CROSS-CUTTING change to EXISTING core paths, gating 167.008; it MUST be completed before the reconcile transaction is implemented deadlock/clobber-safe. Domain: code (concurrency). Blocks 167.008.