---
artifact_type: task
created_at: 2026-07-28T22:40:15.8018739Z
id: 130.002-T
labels:
    - durable-writes
    - core
    - dependencies
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: queued
title: U2 - explicit indeterminate reconciliation for dependency callers
updated_at: 2026-07-28T22:40:15.8018739Z
---

internal/core/dependencies.go AddDependency and RemoveDependency: distinguish the two persistArtifact error classes. For ErrWriteNotApplied keep the DB-edge rollback (file untouched). For ErrWriteIndeterminate do NOT roll back the DB edge (MD likely persisted, rollback diverges MD from the live index); surface it. Model on artifact_references.go applyCrossRefWriteFn. Seam: these callers use persistArtifact(relocate=false), so resolveArtifactPersistPaths returns currentPath==targetPath and BOTH the cross-directory source-dir fsync block AND mkdirAllDurable are skipped; the mkdirDirSyncFn seam CANNOT fire here. The only durable write on this path is WriteArtifactFileWithOptions, which has no existing core seam, so Ship MUST ADD a package-core persistArtifactWriteFn seam (a package-global wrapping WriteArtifactFileWithOptions, mirroring applyCrossRefWriteFn / sizeSeamAtomicWrite) and inject the wrapped ErrWriteIndeterminate through it; the test also asserts persistArtifact propagates ErrWriteIndeterminate on the relocate=false write as a precondition. No dependency on U4 (mkdirAllDurable is not reached). Test-first (no t.Parallel; seam is a package global). Acceptance criteria (<=3 scenarios): (a) indeterminate persist -> DB edge NOT rolled back, surfaced, and the persistArtifact-propagation precondition holds; (b) not-applied persist -> DB edge rolled back; (c) durable-off unchanged. Plan unit U2.