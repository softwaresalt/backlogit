---
artifact_type: task
created_at: 2026-07-28T22:40:15.8018739Z
dependencies:
    - 130.004-T
id: 130.002-T
labels:
    - durable-writes
    - core
    - dependencies
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: queued
title: U2 - explicit indeterminate reconciliation for dependency callers
updated_at: 2026-07-28T22:40:15.8018739Z
---

internal/core/dependencies.go AddDependency and RemoveDependency: distinguish the two persistArtifact error classes. For ErrWriteNotApplied keep the DB-edge rollback (file untouched). For ErrWriteIndeterminate do NOT roll back the DB edge (MD likely persisted, rollback diverges MD from the live index); surface it. Model on artifact_references.go applyCrossRefWriteFn. Seam CORRECTED: the relocate=false path skips the persistArtifact source-dir fsync seam, so inject via the atomicfile write seam or a new persistArtifact write seam, and assert persistArtifact propagates ErrWriteIndeterminate on the relocate=false write as a precondition. Test-first. Acceptance criteria: (a) indeterminate persist -> DB edge NOT rolled back, surfaced; (b) not-applied persist -> DB edge rolled back; (c) durable-off unchanged; (d) persistArtifact-propagation precondition holds. Depends on U4. Plan unit U2.