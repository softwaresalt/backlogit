---
artifact_type: task
created_at: 2026-09-06T22:31:59.6781726Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 168.003-T
    - 168.012-T
id: 168.005-T
parent_id: 168-F
priority: high
references:
    - docs/exec-plans/2026-09-13-866fdc8c-trust-boundary-split-decided-plan.md
    - docs/exec-plans/2026-09-16-866fdc8c-decomposition-correction-plan.md
status: queued
title: 'Trust-anchor mutation core: config+status mutation, pin-policy enforcement, audit event'
updated_at: 2026-09-17T03:45:54.7384993Z
---

Core mutation logic for add/rotate/revoke: deterministically edits config + status only and enforces the external-pin policy (a mutation introducing an unpinned fingerprint is rejected fail-closed), and persists a durable audit event ATOMICALLY with the state change (audit stays in-core for transactional atomicity, faithful to stash 71F5C21F). NO CLI parsing (CLI wiring is 168.013-T). The write-outcome error taxonomy (ErrWriteNotApplied vs ErrWriteIndeterminate), the durable recovery marker, retry/compensation boundaries, and injected-failure/recovery tests are carved into 168.017-T (plan-review cycle-1 items 3 and 9). Deterministic ordering: validate -> enforce pin policy -> mutate config -> mutate status -> persist audit event, with add/rotate/revoke sharing ONE parametrized mutation path. Domain: core (internal/core). TDD: RED behavior harness first. Depends on 168.003-T (preserved) and 168.012-T. Executable scenarios (3): (a) add/rotate/revoke each mutate config+status via the shared parametrized path; (b) an unpinned-fingerprint mutation is rejected fail-closed AND the denied mutation persists a durable audit event; (c) a successful mutation persists a durable audit event atomically with the state change. Build/test: go build ./cmd/backlogit; go test ./internal/core/ -run TestTrustAnchorMutationCore. AC: (1) RED test first; (2) add/rotate/revoke mutate config+status deterministically in fixed order; (3) an unpinned-fingerprint mutation is rejected fail-closed at the pin gate before any state change AND the DENIED mutation persists a durable audit event (SECURITY cycle-1 F2: denials are audited, not only successes); (4) each successful mutation persists a durable audit event atomically; (5) the core DECLARES the base WriteOutcome result type it returns; write-outcome taxonomy/recovery semantics are delegated to 168.017-T; (6) tests GREEN post-impl.