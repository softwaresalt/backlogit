---
artifact_type: task
created_at: 2026-08-29T00:43:18.326726Z
custom_fields:
    source_stash_id: F89CADB7
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: 'Review follow-up (129-S / 146-F, PR #373, deferred at review-fix cycle limit): CheckpointContext.Extra is an exported field and can be constructed directly (bypassing UnmarshalJSON''s guarantee that every stored json.RawMessage came from a real decode). CheckpointContext.emit() (used by both MarshalJSON and Keys()) appends each Extra value verbatim without validating it is well-formed JSON, so a directly-constructed CheckpointContext with a nil or malformed json.RawMessage in Extra makes MarshalJSON() return invalid JSON bytes with a NIL error, and Keys() likewise reports success despite the same emit() result being unserializable. Fix: validate each Extra raw value (e.g. json.Valid(v)) in emit() before appending, and return an error naming the offending key if invalid. Add a regression test constructing a CheckpointContext directly with a malformed Extra value and asserting MarshalJSON/Keys() both return an error rather than silently producing broken JSON. Deferred past the 3-cycle review-fix limit (circuit-breaker.instructions.md).'
dependencies:
    - 148.005-T
id: 148.004-T
labels:
    - harness-ready
parent_id: 148-F
priority: low
status: queued
title: 'U5: Validate CheckpointContext.Extra in emit()'
updated_at: 2026-08-29T00:45:42.0456684Z
---

Add json.Valid check for each Extra value in emit(). Return error naming offending key. Both MarshalJSON() and Keys() propagate. Regression test: directly-constructed CheckpointContext with malformed Extra. Source stash: F89CADB7.