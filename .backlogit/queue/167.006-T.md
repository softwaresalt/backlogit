---
artifact_type: task
created_at: 2026-09-06T01:26:46.0732034Z
dependencies:
    - 167.003-T
id: 167.006-T
parent_id: 167-F
priority: critical
status: queued
title: Handle-relative atomic archive-file writer primitive
updated_at: 2026-09-06T03:36:06.2537706Z
---

Handle-relative atomic archive-file WRITER primitive (item-log LOCK split out to 167.011-T per 2-hour/single-domain rule; deps 167.003 declarations). Implement: the HANDLE-RELATIVE atomic archive-file writer - open .backlogit/archive/ dir handle no-follow/reparse-safe, temp-create + atomic rename through the verified handle (openat/renameat-style POSIX; reparse-safe handle ops Windows), real-path containment + regular-file via handle. DURABILITY (required because this writer PRECEDES an always-fsync audit append - atomic rename guarantees VISIBILITY, not crash PERSISTENCE): fsync the temp file BEFORE rename, then fsync the archive DIRECTORY handle AFTER rename so the rename itself is durable; classify write outcome as clean / ErrWriteNotApplied (proven-not-written, pre-rename failure) / ErrWriteIndeterminate (rename may have committed). TDD (policy): the writer function signature is DECLARED with a panic body in 167.003 (gated by the source-shape harness); THIS task lands the BEHAVIOR harness (RED against the panic body; incl symlink/junction-swap, pre-rename fsync-failure => ErrWriteNotApplied, post-rename dir-flush-failure classification) then the implementation (GREEN). PLATFORM SPLIT: OS-specific handle syscalls (POSIX openat/renameat vs Windows reparse-safe handle ops) CANNOT co-exist in one cross-platform file — mirror the EXACT existing 3-way scheme (internal/events/item_log_lock_windows.go //go:build windows; item_log_lock_unix.go //go:build linux || darwin || freebsd || netbsd || openbsd || dragonfly; item_log_lock_other.go //go:build !windows && !(linux || darwin || freebsd || netbsd || openbsd || dragonfly) = unsupported-platform error). A bare !windows tag would wrongly select Plan 9 / other targets lacking openat/renameat/x/sys/unix. Files: internal/core/shipment_reconcile_fs.go (shared portable seam + exported API), internal/core/shipment_reconcile_fs_windows.go (//go:build windows), internal/core/shipment_reconcile_fs_unix.go (//go:build linux || darwin || freebsd || netbsd || openbsd || dragonfly), internal/core/shipment_reconcile_fs_other.go (unsupported-platform fallback), + cross-platform tests. Domain: code. See plan U1 decomposition. Deps 167.003.