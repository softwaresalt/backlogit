---
artifact_type: task
created_at: 2026-09-06T01:26:46.0732034Z
dependencies:
    - 167.003-T
id: 167.006-T
parent_id: 167-F
priority: critical
status: queued
title: Handle-relative filesystem primitives (writer + lock)
updated_at: 2026-09-06T02:08:55.9046699Z
---

Handle-relative filesystem primitives (deps 167.003 declarations). Implement: (a) HANDLE-RELATIVE atomic archive-file writer - open .backlogit/archive/ dir handle no-follow/reparse-safe, temp-create + atomic rename through the verified handle (openat/renameat-style POSIX; reparse-safe handle ops Windows), real-path containment + regular-file via handle; (b) HANDLE-RELATIVE item-log lock - acquire the sidecar + JSONL relative to the verified logs-dir handle (NOT LockItemLogCrossProcess, which re-derives the sidecar path). TDD: RED harness first (incl symlink/junction-swap tests). PLATFORM SPLIT: OS-specific handle syscalls (POSIX openat/renameat vs Windows reparse-safe handle ops) CANNOT co-exist in one cross-platform file — mirror the existing build-tagged pairs (internal/atomicfile/atomicfile_windows.go + atomicfile_other.go; internal/events/item_log_lock_windows.go + item_log_lock_unix.go). Files: internal/core/shipment_reconcile_fs.go (shared portable seam + exported API), internal/core/shipment_reconcile_fs_windows.go (//go:build windows) + internal/core/shipment_reconcile_fs_unix.go (//go:build !windows) for the writer + lock syscall implementations, + cross-platform tests. Domain: code. See plan U1 decomposition. Deps 167.003.