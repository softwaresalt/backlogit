---
artifact_type: deliberation
created_at: 2026-09-23T23:21:18.6441126Z
custom_fields:
    linked_stash_id: FF1F3AC7
    linked_stash_kind: bug
    linked_stash_priority: high
    linked_stash_text: |-
        DEFERRED SCOPE EXPANSION
        Expansion: isolate the internal/core shipped-event durability tests from the REAL autoharness gate/version subprocess by injecting the pre-existing fake gate broker (ws.GateBroker with gate.VersionRunner/GateRunner fakes) and propagating a bounded context into the ShipShipment test goroutine, so gateShipmentCompletion no longer spawns gate.ExecVersionRunner and exhausts the package deadline.
        C1 rationale (OUT OF SCOPE): the authorized full suite `go test ./...` fails in UNCHANGED internal/core tests because newShipDurabilityFixture inherits the real buildGateBroker (ExecVersionRunner) from NewWorkspace and shipWithWatchdog calls ShipShipment(context.Background()) unbounded; the real `autoharness version` subprocess (stack: gate.ExecVersionRunner.Version -> gate.Probe -> Broker.Evaluate -> gateShipmentCompletion -> ShipShipment) consumes the 10m package deadline. This is a TEST-INFRASTRUCTURE gate-isolation defect, NOT completion of the 174.064-T lock-order canonicalization (whose stack is item-log/artifact/global-lock, not the gate/version subprocess). It fails P-021 C1 for 174.064-T (different contract surface) yet blocks 174.064-T AC6 / the full-repository gate.
        Refs: failing-symbols=TestShipShipment_FailClosedShippedAppendSuppressesMoveStatusPostHook (internal/core/shipment_shipped_event_durability_test.go:316), TestShipShipment_ShippedEventAppendFailureLogsFixedShape/not_applied (10m package timeout); terminal-stack=gate.ExecVersionRunner.Version -> gate.Probe -> Broker.Evaluate -> gateShipmentCompletion -> ShipShipment; seam=ws.GateBroker / injectBroker / fakeVersion / fakeGateRunner (gate_transition_test.go), real-wiring=workspace.go:278 buildGateBroker. related=174.064-T; covering-feature=174-F (active); shipment=155-S (active); checkpoint=.backlogit/checkpoints/checkpoint-20260923-231731.json (active); branch-HEAD=1656477cd51115433cd3165ba8e6f4afd5811ca6; diagnostics=logs/diagnostics/155-s-go-test-final-20260923.txt.
        requires deliberation: yes
        kind: bug (test-infrastructure / gate isolation); provisional priority: high
        Production-API note: NO production change required; the injection seam (public ws.GateBroker field + gate.VersionRunner/GateRunner interfaces + injectBroker test helper) pre-exists. If any production seam turns out to be missing, it must be classified and bounded, not silently expanded.
id: 068-DL
priority: high
status: queued
title: Isolate internal/core durability tests from real gate/version subprocess
updated_at: 2026-09-23T23:21:18.6441126Z
---

# Isolate internal/core durability tests from real gate/version subprocess

## Problem Frame

<!-- BEGIN:problem-frame -->
Authorized full-suite go test ./... fails/timeouts in UNCHANGED internal/core durability tests: newShipDurabilityFixture inherits the real buildGateBroker (gate.ExecVersionRunner) from NewWorkspace (workspace.go:278) and shipWithWatchdog runs ShipShipment(context.Background()) unbounded, so gateShipmentCompletion -> Broker.Evaluate -> gate.ExecVersionRunner.Version spawns a real autoharness version subprocess that exhausts the 10m package deadline. First failure TestShipShipment_FailClosedShippedAppendSuppressesMoveStatusPostHook @ line 316; timeout in TestShipShipment_ShippedEventAppendFailureLogsFixedShape/not_applied.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
A) Inject the PRE-EXISTING fake gate broker (ws.GateBroker via injectBroker + fakeVersion/fakeGateRunner) into the durability fixture and propagate a bounded context into the ship goroutine; test-only, no production change. B) Inflate package/test timeouts or skip the durability tests under full suite; rejected (masks the defect, violates no-timeout-inflation/no-skip). C) Add a new production injection API for the version runner; rejected because the seam (public ws.GateBroker field + gate.VersionRunner/GateRunner interfaces + injectBroker helper) already exists, so a production change would be unnecessary scope expansion.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
Option A. One minimal test-harness corrective task under active covering feature 174-F (queued/high), governed-added to active 155-S. Scope is confined to internal/core/shipment_shipped_event_durability_test.go plus the pre-existing seam: inject a passing fake broker so no gate.ExecVersionRunner subprocess is spawned, propagate a bounded context into the ShipShipment test goroutine (not sole reliance on the package deadline), preserve every shipped-event append/compensation/post-hook/fixed-shape assertion, deterministically prove no ExecVersionRunner invocation, RED-before/GREEN-after. No production API change; no timeout inflation, skips, weakened assertions, or gate bypass.
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
Whether gate.EnabledMode should be On or Warn for the injected broker (must be whichever preserves the existing ship-completion path so the durability assertions still run); confirmed the seam requires zero production change.
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
P-021 C1 OUT OF SCOPE for 174.064-T lock-order (different contract surface: gate/version subprocess isolation, not artifact/global lock ordering) but blocks 174.064-T AC6 / full-repository gate. Membership-as-gate: an unfinished member of active 155-S gates PR readiness, so no dependency-graph change (fail-closed). Circuit disposition: the same-operation full-suite circuit is OPEN; the post-fix full-suite run is a NEW separately-authorized final-gate operation at a new commit/phase, requiring explicit operator authorization; do not run now.
<!-- END:notes -->
