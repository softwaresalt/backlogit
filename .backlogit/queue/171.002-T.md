---
artifact_type: task
created_at: 2026-09-07T00:09:14.3791103Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 171.001-T
id: 171.002-T
parent_id: 171-F
priority: high
references:
    - docs/exec-plans/2026-09-06-checkpoint-create-schema-reject-plan.md
status: queued
title: 'Core: reject non-V1 by default; legacy import upgrades to valid V1'
updated_at: 2026-09-07T00:09:14.3791103Z
---

U2 (behavior harness + core; strictly later wave than 171.001-T's declaration). Land the
semantics-matrix BEHAVIOR harness (red) against the landed declaration in
internal/events/checkpoint_create_schema_test.go (reuse assertNoCheckpointWritten — confirm
t.Helper() + empty-dir assertion; no t.Parallel() on global-seam tests), then invert
CreateCheckpoint BEHAVIOR (body only — the widened signature already landed in 171.001-T) in
internal/events/memory.go to turn it green.

Require schema_version==1 by default; reject BEFORE syncWriteFileAtomicHook, choosing the
sentinel from the SAME Upgrade-window predicate on BOTH paths: ErrCheckpointSchemaRejected for a
legacy shape that IS in the window (default-path "enable opt-in" advice is then actionable),
ErrCheckpointSchemaUnsupported for an out-of-window legacy shape and for
unsupported/future/wrong-typed/ambiguous, malformed-input error for malformed JSON. Classify
schema_version with a json.Decoder.Token() STREAM SCAN (reuse objectMemberKeys in
checkpoint_schema.go) capturing the raw token via json.RawMessage — NOT json.Number, and NOT an
int zero-value probe or struct/map decode (those collapse duplicate keys last-wins) — so a
present-but-wrong-typed value OR a DUPLICATED top-level schema_version member is rejected, not
collapsed to 0. The scan is CASE-FOLD-AWARE (strings.EqualFold, matching encoding/json and
checkClosedSchemaNamespace at internal/events/checkpoint_strict.go:166-170) so a valid
SCHEMA_VERSION:1 is recognized as V1 (not misclassified schema-less) and fold-equivalent
duplicates are rejected as ambiguous — cover this case in the U2 tests. In-window vs
out-of-window is decided by the COMPLETE upgraded-V1 pre-write DRY-RUN (coerce schema_version→1;
default created_at/updated_at/status; closed top-level + nested-progress namespace checks;
dup-key; full ValidateCheckpoint; Context.Keys(); canonical marshal; post-marshal size check;
second secret scan from checkStateDumpSecrets) — NOT a field-level identity+namespace predicate
alone, because a schema-less object with valid agent/session_id/phase can still fail via
wrong-typed context, invalid nested progress, duplicate context members, or canonical growth past
64 KiB. The default path emits
ErrCheckpointSchemaRejected (actionable) ONLY when that dry-run would succeed;
ErrCheckpointSchemaUnsupported for structural/validation dry-run failures (identity, namespace,
dup-key, ValidateCheckpoint, or Context.Keys() failure — opt-in cannot help); or the native size
error from checkStateDumpSize when the dry-run fails at the post-marshal size guard (preserved
so the caller knows to reduce the payload). When WithAllowLegacyImport() is set AND the dry-run succeeds,
upgrade: write the canonical V1 the dry-run validated. A legacy shape whose dry-run fails (needing
consumer→agent remap, identity synthesis, top-level relocation, carrying bad context/progress,
or canonical growth past 64 KiB)
is fail-closed rejected — no file; ErrCheckpointSchemaUnsupported for structural failures;
native size error type for post-marshal size failures (preserve the native error so
remediation is actionable) — identity (agent/session_id) is NEVER synthesized. Never write verbatim. Preserve size+secret guards and V1-branch
dup-key/closed-namespace; keep TestSyncWriteFileAtomic_NoPreRemoveInAST green.

Update existing tests that assumed schema-less/verbatim success to the new reject-by-default
contract (so go test ./... stays green): internal/events/memory_test.go
(TestCreateCheckpoint_WritesFile), internal/events/checkpoint_readpath_test.go
(TestCreateCheckpoint_LegacyDumpWrittenVerbatim), internal/events/checkpoint_u1_harness_test.go
(TestCreateCheckpoint_U1_ValidLegacyJSON_PassesThrough),
internal/cli/checkpoint_create_shape_test.go:12-82 (also pin arrays/scalars/null as unsupported
NON-OBJECT inputs that reject with no file), tests/contract/checkpoint_tools_test.go:219-237, and
any legacy-corpus fixture relied on for verbatim success. Add cases for a schema-less object with
valid identity but wrong-typed context / invalid nested progress / duplicate context members →
default ErrCheckpointSchemaUnsupported and opt-in fail-closed reject.

Acceptance criteria: default rejects an in-window missing/0 (ErrCheckpointSchemaRejected) and an
out-of-window/unsupported/future/wrong-typed (ErrCheckpointSchemaUnsupported) and malformed
(malformed-input) — each no-file; opt-in upgrades an in-window record to a valid V1 readable by
GetCheckpoint/ListCheckpoints without quarantine; opt-in fail-closed rejects an out-of-window
legacy shape; valid V1 unchanged; size/secret/dup-key/closed-namespace regressions still fire;
default-to-canonical growth past 64 KiB (in-window input passing the first size check whose
canonical form exceeds the limit) → native size-limit error, no file on both paths;
pre-classification Unicode-escaped secret (input with \u0067hp_<token> or similar in a context
value, caught by the first checkStateDumpSecrets call before schema classification, which uses
decoded-token matching and catches escaped prefixes) → native secret error, no file on any path;
PRODUCER-level errors.Is/errors.As sentinel assertions on the core return value here.
`go test ./internal/events/...` green.