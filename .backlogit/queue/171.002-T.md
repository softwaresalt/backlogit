---
artifact_type: task
created_at: 2026-09-07T00:09:14.3791103Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 171.001-T
id: 171.002-T
parent_id: 171-F
priority: high
references:
    - docs/exec-plans/2026-09-06-checkpoint-create-schema-reject-plan.md
status: queued
title: 'Core: reject non-V1 by default; legacy import upgrades to valid V1'
updated_at: 2026-09-07T00:09:14.3791103Z
---

U2 (behavior harness + core; strictly later wave than 171.001-T's declaration). Land the
semantics-matrix BEHAVIOR harness (red) against the landed declaration in
internal/events/checkpoint_create_schema_test.go (reuse assertNoCheckpointWritten — confirm
t.Helper() + empty-dir assertion; no t.Parallel() on global-seam tests), then invert
CreateCheckpoint BEHAVIOR (body only — the widened signature already landed in 171.001-T) in
internal/events/memory.go to turn it green.

Require schema_version==1 by default; reject BEFORE syncWriteFileAtomicHook, choosing the
sentinel from the SAME Upgrade-window predicate on BOTH paths: ErrCheckpointSchemaRejected for a
legacy shape that IS in the window (default-path "enable opt-in" advice is then actionable),
ErrCheckpointSchemaUnsupported for an out-of-window legacy shape and for
unsupported/future/wrong-typed/ambiguous, malformed-input error for malformed JSON. Classify
schema_version with a json.Decoder.Token() STREAM SCAN (reuse objectMemberKeys in
checkpoint_schema.go) capturing the raw token via json.RawMessage — NOT json.Number, and NOT an
int zero-value probe or struct/map decode (those collapse duplicate keys last-wins) — so a
present-but-wrong-typed value OR a DUPLICATED top-level schema_version member is rejected, not
collapsed to 0. When WithAllowLegacyImport() is set AND the dump is in the UPGRADE WINDOW (legacy
shape AND agent∈{ship,stage}, non-empty session_id, non-empty phase present AND NO foreign
top-level members; created_at/updated_at/status defaultable-when-absent, validated only if
present), upgrade: coerce schema_version→1, default timestamps+status, run the SAME V1 path
(closed-namespace, dup-key, ValidateCheckpoint, canonical marshal), write only if valid. A legacy
shape OUTSIDE the window (needing consumer→agent remap, identity synthesis, or top-level
relocation) is fail-closed rejected as ErrCheckpointSchemaUnsupported, no file — identity
(agent/session_id) is NEVER synthesized. Never write verbatim. Preserve size+secret guards and
V1-branch dup-key/closed-namespace; keep TestSyncWriteFileAtomic_NoPreRemoveInAST green.

Update existing tests that assumed schema-less/verbatim success to the new reject-by-default
contract: internal/events/memory_test.go (TestCreateCheckpoint_WritesFile),
internal/events/checkpoint_readpath_test.go (TestCreateCheckpoint_LegacyDumpWrittenVerbatim),
internal/events/checkpoint_u1_harness_test.go (TestCreateCheckpoint_U1_ValidLegacyJSON_PassesThrough),
and any legacy-corpus fixture relied on for verbatim success.

Acceptance criteria: default rejects an in-window missing/0 (ErrCheckpointSchemaRejected) and an
out-of-window/unsupported/future/wrong-typed (ErrCheckpointSchemaUnsupported) and malformed
(malformed-input) — each no-file; opt-in upgrades an in-window record to a valid V1 readable by
GetCheckpoint/ListCheckpoints without quarantine; opt-in fail-closed rejects an out-of-window
legacy shape; valid V1 unchanged; size/secret/dup-key/closed-namespace regressions still fire;
PRODUCER-level errors.Is/errors.As sentinel assertions on the core return value here.
`go test ./internal/events/...` green.