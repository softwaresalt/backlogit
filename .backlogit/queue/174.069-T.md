---
artifact_type: task
created_at: 2026-09-24T05:47:37.1888605Z
id: 174.069-T
parent_id: 174-F
priority: high
status: queued
title: Default GateBroker-nil in shared ShipShipment test fixtures to isolate gate subprocess
updated_at: 2026-09-24T05:53:33.0262295Z
---

Corrective work unit (Wave 15) — one CLASS-LEVEL, test-only correction closing the full-suite timeout root cause: 11 of 34 internal/core ShipShipment tests inherit the real default GateBroker (ExecVersionRunner/ExecRunner) via setupShipmentWorkspace or external setupTestWorkspace and spawn a real gate version subprocess. Established as a NEW corrective task (not same-contract completion of done 174.066-T, which isolated only the shipped-event durability family) because it changes SHARED shipment test infrastructure across multiple fixture families. Test-infrastructure domain only; scope internal/core test files only; NO production/config/default/API change. Source stash DB48A817; deliberation 069-DL.

<!-- BEGIN:acceptance-criteria -->
AC1 (Shared test-only helper): add a test-only helper disableExecGateForTest(t *testing.T, ws ...) that SETS ws.GateBroker=nil (removing the real ExecVersionRunner/ExecRunner default) and STRUCTURALLY ASSERTS ws.GateBroker==nil after the call. Helper lives in a shared internal/core test file (gate_transition_test.go alongside newGateTestWorkspace). Test-only; no production symbol added/changed.
AC2 (Ordinary shipment fixtures default nil): setupShipmentWorkspace (internal/core/shipment_test.go) calls disableExecGateForTest by DEFAULT so every ordinary shipment fixture yields GateBroker==nil, with the structural assertion. The latest failing test internal/core/shipment_test.go:TestShipShipment_RestoresNonMemberFeatureEvenWhenShipFailsAfterRollup MUST no longer reach a real gate version subprocess.
AC3 (Gate fixtures default nil + explicit fake re-injection): newGateTestWorkspace (internal/core/gate_transition_test.go) also defaults GateBroker==nil via the helper. Every INTENTIONAL gate test that requires broker behavior MUST then EXPLICITLY inject its fake gate broker/runner types AFTER the default and assert NO real Exec runners (ExecVersionRunner/ExecRunner) are present. Behavioral gate coverage is preserved — intentional gate tests remain behaviorally covered, not silently disabled.
AC4 (Durability fixture unchanged unless consolidating): the shipped-event durability family (internal/core/shipment_shipped_event_durability_test.go) REMAINS an explicit fail-open fake fixture; its current structural regression MUST still pass. Touch this file ONLY if consolidating structural assertions into the shared helper; otherwise leave it UNTOUCHED. Do NOT weaken any durability fail-open assertion.
AC5 (External-package archive wrapper): for external package test internal/core/025_archive_harness_test.go, add a LOCAL workspace wrapper that clears and asserts ws.GateBroker==nil for the ShipShipment fixture used there (name the actual ShipShipment caller, e.g. TestShipShipment_QueuePathAbsentAfterShip, rather than the archive-only ArchiveItem fixtures which do not call ShipShipment). Do NOT modify the broad shared core_test.setupTestWorkspace (out of scope; would over-reach other packages).
AC6 (Inventory regression): add a regression test that PROVES no ordinary ShipShipment fixture/caller retains the real exec gate seam — assert the ExecVersionRunner/ExecRunner default is ABSENT (i.e. GateBroker==nil, OR only fake seams for the intentionally-injected gate tests) across the inventoried ordinary ShipShipment fixtures. Assert on absence-of-real-Exec-types, NOT strict nil-ness, so the durability fail-open fake (which re-injects a fake broker via setupShipmentWorkspace, AC4) is not false-flagged. This locks the invariant so a newly added ShipShipment test cannot silently re-inherit the real broker. Intentional gate tests remain exempt via their explicit fake injection. Land the regression in a shared authorized internal/core test file (gate_transition_test.go alongside the shared helper).
AC7 (No weakening / no production change): NO skipped tests, NO timeout inflation, NO weakened assertions, NO production/config/default/API change, NO production gate bypass. Scope is confined to internal/core test files: gate_transition_test.go (shared helper/newGateTestWorkspace), shipment_test.go (setupShipmentWorkspace), 025_archive_harness_test.go (local external wrapper), and shipment_shipped_event_durability_test.go ONLY if consolidating (AC4). Any need to touch a production symbol -> HALT and classify.
AC8 (RED-before / GREEN-after + targeted verification): the inventory regression (AC6) and the corrected fixtures MUST make the previously-timing-out ordinary ShipShipment tests deterministic without the real subprocess; targeted GREEN via go test -run over the ShipShipment, gate, durability, and archive suites plus -race for the contention-relevant ones. Do NOT run go test ./... within this task.
AC9 (Shared circuit / full-suite disposition): the same-operation full-suite circuit is OPEN. After this corrective commit lands, the full suite runs ONCE as a NEW separately-authorized final-gate operation (explicit operator authorization immediately before it); compliant alternative is targeted per-package verification. Then the shared 155-S final review/stop-gate (zero P0/P1) applies.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Root cause: default core workspace GateBroker carries the real ExecVersionRunner/ExecRunner, which spawns a gate version subprocess during ShipShipment. 11 of 34 internal/core ShipShipment tests inherit that default via setupShipmentWorkspace or external setupTestWorkspace; the latest full-suite failure (shipment_test.go:TestShipShipment_RestoresNonMemberFeatureEvenWhenShipFailsAfterRollup) times out the package on the real subprocess. Gate/durability families that explicitly inject fakes are already safe; gate tests use newGateTestWorkspace but rely on every caller remembering injection, so the fixture DEFAULT is unsafe.
Class-level correction (test-only): one disableExecGateForTest(t,ws) sets/asserts ws.GateBroker=nil, called by default in setupShipmentWorkspace and newGateTestWorkspace. Intentional gate tests explicitly inject fakes afterward and assert no Exec runners. External 025_archive_harness_test.go gets a LOCAL archive-workspace wrapper that nils/asserts the broker (do NOT modify broad core_test.setupTestWorkspace). Durability fail-open fake fixture/assertions preserved; touched only if consolidating. Inventory regression enforces the invariant.
Scope: internal/core test files only (gate_transition_test.go, shipment_test.go, 025_archive_harness_test.go, durability test only if consolidating). No production/config/default/API change. P-021 C1 OUT OF SCOPE re 174.066-T: this is shared cross-family test infrastructure, not same-contract completion of the durability-only isolation.
Membership: governed member of active 155-S; no dependency edge (membership-as-gate). Runs before 155-S final review/PR readiness. Provenance 174.066-T (done) informational.
<!-- END:implementation-notes -->