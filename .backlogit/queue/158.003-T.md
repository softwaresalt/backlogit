---
artifact_type: task
created_at: 2026-09-03T08:50:15.099967Z
custom_fields:
    harness_status: scaffolded
id: 158.003-T
labels:
    - harness-ready
parent_id: 158-F
priority: high
status: queued
title: Analyzer framework scaffolding + FL001 declaration (seq3 U3a-scaffold)
updated_at: 2026-09-11T02:18:31.3628764Z
---

U3a-scaffold (shared analyzer prerequisite; FL001 discipline BEHAVIOR split to 158.010-T per governing finding P-002.1/P-002.6). Own the minimum analyzer declaration + shared build scaffolding that every later analysistest harness (158.004-T..158.007-T and 158.010-T) needs in order to compile: (1) promote golang.org/x/tools to a DIRECT require pinned to the ALREADY-SELECTED v0.39.0 via `go get golang.org/x/tools@v0.39.0` then `go mod tidy` — a promotion of an existing pruned transitive requirement, NOT an upgrade/downgrade; newly recorded // indirect entries (x/mod, x/sync) at their already-selected versions are expected; HALT on any VERSION MOVE of any existing requirement (e.g. x/text v0.32.0 must not change). (2) cmd/faultline-analyze/main.go: a single multichecker.Main(...) call that EXPLICITLY enumerates pre-reserved, alphabetically-sorted slots FL001..FL005 (no reflection, no parent init registry); FL001 is wired LIVE against the declared scannerdiscipline.Analyzer var, and FL002..FL005 are authored as pre-reserved COMMENTED placeholder slots so the file compiles referencing FL001 only. The FL002-FL005 slots are filled later by the SINGLE serialized integration task 158.011-T (NOT by each analyzer task), so main.go is never edited by parallel wave tasks. (3) Makefile `check` target — `go build ./cmd/faultline-analyze` then `go test ./internal/faultline/analyzer/...` — plus its .PHONY entry. (4) the FL001 analyzer subpackage internal/faultline/analyzer/scannerdiscipline DECLARATION only: `var Analyzer *analysis.Analyzer` with Name "FL001scannerdiscipline" and a compilable skeleton run func carrying NO discipline logic. (5) a source-shape harness (TestScaffoldShape) asserting the scaffolding + declarations exist and the multichecker binary builds. NO analyzer discipline behavior and NO analysistest want-fixtures here (those are 158.010-T). Acceptance: `go list -m golang.org/x/tools` prints v0.39.0; `go build ./cmd/faultline-analyze` builds; the scaffolding source-shape harness passes; no existing dependency version moved. Domain: code+build. RED/GREEN: go build ./cmd/faultline-analyze && go test ./internal/faultline/analyzer/scannerdiscipline/ -run TestScaffoldShape -count=1.

<!-- BEGIN:implementation-notes -->
Harness Manifest | Compilation: PASS (go test -run=^$ -count=1 ./...) | Red Phase: CONFIRMED | Harness Command: go test -count=1 -run '^TestScaffoldShape$' ./internal/faultline/analyzer/scannerdiscipline | Red Evidence: TestScaffoldShape assertion failures for absent Analyzer, multichecker, x/tools, and Makefile check shapes.
<!-- END:implementation-notes -->