---
artifact_type: task
created_at: 2026-09-03T08:50:04.8786642Z
custom_fields:
    harness_status: scaffolded
id: 158.001-T
labels:
    - harness-ready
parent_id: 158-F
priority: high
status: active
title: Compat corpus package declarations (seq3 U1a decl-only)
updated_at: 2026-09-11T02:20:38.9910152Z
---

U1a (declaration-only prerequisite; behavior split to 158.009-T per governing finding P-002.1/P-002.6 — a single task may not bundle declarations with the behavior harness that cannot compile until those declarations exist). Author the real, compilable exported DECLARATIONS of package internal/faultline/compatcorpus ONLY: the Category and Expectation const sets; the sentinel errors ErrTruncated, ErrMalformed, ErrDuplicateKey, ErrCaseFoldCollision, ErrOldVersion, ErrTokenTooLong, ErrInvalidPath, ErrUnclosedFront; the types Entry, DecodeResult, ParserAdapter (interface), Result, Report; and the compilable signatures/method set Run, DefaultCorpus, DefaultAdapters, LoadCorpus, and Report.JSON (stub bodies that compile — e.g. panic("not implemented") — carrying NO runner/adapter/fixture behavior). No corpus fixtures, no adapter logic, no report serialization behavior here. RED harness is a source-shape go/ast test (TestCompatcorpusDeclShape) that parses the package source with go/parser + go/ast and asserts every contracted symbol is declared with the contracted shape (names, kinds, signatures) — it executes no behavior. Acceptance: the package compiles; the go/ast source-shape harness passes; every contracted symbol is declared with the correct shape. No dependency on golang.org/x/tools. Domain: code. RED/GREEN: go test ./internal/faultline/compatcorpus/ -run TestCompatcorpusDeclShape -count=1.

<!-- BEGIN:implementation-notes -->
Harness Manifest | Compilation: PASS (go test -run=^$ -count=1 ./...) | Red Phase: CONFIRMED | Harness Command: go test -count=1 -run '^TestCompatcorpusDeclShape$' ./internal/faultline/compatcorpus | Red Evidence: TestCompatcorpusDeclShape assertion failures for absent declaration shapes.
<!-- END:implementation-notes -->