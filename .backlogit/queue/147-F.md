---
artifact_type: feature
created_at: 2026-08-24T19:00:08.4266989Z
custom_fields:
    harness_status: pending
    source_stash_id: D3CE9E81
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Preserve or refuse on unmodeled top-level keys in checkpoint disposition rewrites. core.AbandonCheckpoint (internal/core/checkpoint_disposition.go) and events.ResolveCheckpoint perform the same parse -> mutate -> re-marshal round-trip on a pre-existing file, so unmodeled TOP-LEVEL keys are still dropped on abandon and resolve even after 146-F / 129-S lands. U2 fixes the nested `context` loss as a side effect; the top level needs either a raw top-level carrier or an explicit refusal-to-mutate policy, which is a distinct design decision from the create boundary. Residual exposure: all nine live files under .backlogit/checkpoints/ carry such keys. Provenance: scope-boundary follow-up 1 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md (Scope boundaries and recorded follow-ups); parent work feature 146-F, shipment 129-S; external source 3C7AAC71. NOT release-blocking for 129-S.'
id: 147-F
priority: high
status: queued
title: Refuse to rewrite checkpoints carrying unmodeled top-level keys
updated_at: 2026-08-26T03:25:06.4293597Z
---

Checkpoint `abandon` and `resolve` parse a stored checkpoint, mutate the decoded struct, and re-marshal it. Any top-level JSON key that is not modeled on `CheckpointV1` is silently dropped by that round trip. Nested `context` preservation already shipped in 146-F; the top level did not.

Deliberation `docs/decisions/2026-08-24-checkpoint-toplevel-key-disposition-deliberation.md` decided **Option B — refuse to mutate** rather than preserve, and widened the decision on two grounds discovered empirically:

* `ResolveCheckpoint` has **no validity gate at all**. On a legacy document it replaces the whole file with a fabricated skeleton (`schema_version: 0`, empty `agent`, zero `created_at`, empty `context`), destroying every byte of evidence. Both Stage and Ship session-start recovery call it. This is the higher-severity half of the defect.
* A naive refusal would deadlock: `QuarantineCheckpoint` refuses any target that parses and validates, so a valid-but-non-conforming file would have no disposition path at all. Quarantine classification must widen in the same change.

Plan: `docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md`.

**Plan-review state: FAIL — pending fresh local review, push blocked.** Current gate is
`cycle: 31`, `dispatch_mode: single-agent-declared-degradation`
(`TOOL_DEGRADED: reviewer-subagent-dispatch`), `decision: FAIL`,
`pending: fresh-local-review-required`, severity counts P0=0, P1=6 (all remediated in-pass), P2=0,
P3=0, topology **SOUND** (42 tasks, 104 executable edges, 43 shipment members, 18 waves,
unchanged), `operator_authorization: approved`, `push_allowed: no`, `restage_recommendation: none`.
Cycle 31 closed the six PR #377 P1 threads that cycles 29 and 30 had acknowledged and deliberately
HELD. They reduce to three roots: **(R1)** four staged tasks still instructed "land the declaration
stub so the package compiles, then the harness, then implement" — the same NON-NEGOTIABLE Principle
II carve-out the `declaration-only` withdrawal was meant to close, and unschedulable besides, since
`harness-architect` no longer fabricates the missing declaration; the lifecycle is now **two steps,
harness first**, with a declaration → behaviour-harness → implementation split for any seam whose
body would absorb real behaviour (`147.034-T` / U11, whose stub carried real
read/parse/mutate/marshal/atomic-replace behaviour). **(R2)** the P-002.6 wave state machine had no
disposition for a member that becomes `blocked`, so `core.ReturnBlockedItem` could move a task out
of `queued` without it becoming `done` and the loop would report completion over an unfinished set;
the scheduler now tracks an **immutable manifest member set `M`** with a total status partition,
completion defined as `terminal = M`, and a new `WAVE_MEMBER_BLOCKED` halt carrying transitive
dependency impact and the `return_blocked` record. **(R3)** the wave order was unsatisfiable for
`|ready_k| > 1` — the normal case, and true of 11 of this unit's 18 waves — because each
`build-feature` invocation required the full suite against its siblings' still-red harnesses;
per-task loops are now **task-scoped** with an explicit closed tolerated-red set, and the full suite
is **relocated, not skipped**, to a new mandatory Ship Step 4.6 wave convergence gate. Policy
`workflow-policies.md` 1.18.0 → **1.19.0**. Re-simulated at this HEAD: 18 waves / 0 stalls / 0
compile-order violations; a blocked-member injection halts with `WAVE_MEMBER_BLOCKED` and produces
no false completion; the sibling-red wave progresses 2/2 under scoped loops where the old model
progressed 0/2. `go test ./...` exit 0, no production Go touched. **The gate is FAIL because the
corrected contract has been reviewed only by the session that authored it** — a fresh local review
of the cycle-31 changes is required before push, and §1.9 Check 3 remains blocking.

Cycle 29's `ADVISORY` (severity counts P0=0, P1=0, P2=3, P3=2) is now superseded for gate-decision
purposes; its remediation content — the `declaration-only` withdrawal, new policy P-002.6, the
one-snapshot-per-wave recipe, and its bounded fixes — stands unmodified as history, and cycle 31
builds on it rather than reversing it. Cycle 24's `ADVISORY` (severity counts P0=0, P1=0, P2=5,
P3=0) is likewise superseded for
gate-decision purposes; its own five-finding remediation (B1-B5) stands unmodified as history, as
does cycle 29's remediation content (the withdrawal, the waves, and the three bounded fixes) —
only the gate-state role is superseded here. Cycle 21's `FAIL` (severity counts P0=0, P1=1, P2=1,
P3=2, `restage_recommendation: confirmatory-review-of-cycle-21-fixes`) remains superseded in the
same way; its root-cause remediation stands unmodified as history. The cycles 1-13 `PASS`, the
`cycle: 14` `FAIL`, the `cycle: 15` `FAIL`, the `cycle: 16` `FAIL` (severity counts P0=1, P1=7,
P2=3, P3=2, `restage_recommendation: formal-decomposition`), the `cycle: 17` `FAIL`, the
`cycle: 18` `ADVISORY`, and the `cycle: 20` `FAIL` (severity counts inherited from the PR #377
Copilot review: P0=0, P1=17, P2=3, P3=0) are all historical and superseded. The recorded next
action is to push branch `chore/stage-130-s` and reconcile PR #377; this is **not** merge approval,
not a shipment claim, and not authorization for Ship to begin implementation.



**Cycle 22 is a bounded prompt/policy remediation pass, not a gate.** It generalized the cycle-21
Ship ready-selection adapter into the global P-002 consumer contract
(`.github/policies/workflow-policies.md` P-002.1/P-002.2, `.github/agents/.ship.agent.md` Step 2a,
`.github/skills/harness-architect/SKILL.md` Step 1a, `.github/skills/build-feature/SKILL.md`),
reworded the universal "no behaviour implementation exempt" claim so U13 reads as the explicit
edge-backed carve-out, added discoverable HARD MERGE GATE backreferences, normalized the remaining
stale current-gate pointers, and corrected the present-tense red-harness count to planned
ownership. It changed no topology, no dependency edge, and no shipment membership. The current
gate state is now `cycle: 24` `ADVISORY` (superseding `cycle: 21` `FAIL`); cycle 22 remains
remediation evidence and does **not** itself claim PASS.

**Cycle 23 is a bounded prompt/policy remediation pass, not a gate.** A fresh review of the cycle-22
contract found two P1 defects and both are corrected at the root. (1) **Deadlock**: the
`covered-by` owner's red evidence was checked at Ship Step 2, which runs *before* harness
generation, so `147.036-T` / U13 could never be admitted and shipment `130-S` would halt on its own
gate. P-002.1 is now split into a **static intake** (fields, class, reason, closed-contract
membership, owner dependency edge and type, declared commands) and a **claim-time** re-evaluation
at the new Ship **Step 4.1a**, which solely owns `EXEMPT_OWNER_NOT_RED`; `harness-architect` now
explicitly scaffolds `covered-by` owners and must not raise that code. (2) **False green**: exempt
dispatch could pass vacuously — `go test -run <selector>` exits 0 with `[no tests to run]`, a
declaration compile check passes before the declaration exists, and three runtime-evidence units
(`147.019-T`, `147.026-T`, `147.041-T`) carried no executable command at all. All ten exempt tasks
now carry a canonical, greppable `harness-exemption-contract` block with identical key grammar, and
every command is a real gate that was **observed failing at HEAD `e8b974e`** before any deliverable
exists. New P-002.3 (must-fail-before-deliverable probe, false-green signals, per-class command
authoring) and P-002.4 (objective behaviour classification by changed-file delta surface) close the
contract; `build-feature` gains a narrow exception letting `verification-only` and
`declaration-only` deliverables create the new test/evidence files they name while still forbidding
any weakening of an existing assertion. Topology, dependency edges, and shipment membership are
unchanged. The current gate state is now `cycle: 24` `ADVISORY` (superseding `cycle: 21` `FAIL`);
cycle 23 remains remediation evidence and does **not** itself claim PASS.

**Cycle 24 is a bounded prompt/policy/backlog-artifact remediation pass, not a gate.** Run in a
dedicated worktree at HEAD `35aac6c0` on branch `chore/cycle-24-remediation`. It closes a P-003 gap
cycle 23 undercounted — nine queued tasks (`147.031-T`, `147.033-T`, `147.034-T`, `147.035-T`,
`147.036-T`, `147.037-T`, `147.039-T`, `147.040-T`, `147.041-T`), not two, carried no
`acceptance-criteria` block — by authoring each block against that task's own existing contract,
with no invented scope and none exceeding three scenarios; all 42 queued tasks (43 with the
archived `147.010-T`) now carry one. It folds three P2 advisories into the exempt-execution
contract: every `exempt_verification_command` now MUST print an `EXEMPT_VERIFY_OK:{task_id}`
marker before its own `exit 0`, so a pass requires the exact marker and not exit 0 alone (new halt
code `EXEMPT_MARKER_MISSING`); new **P-002.5** statically screens every exempt/harness-owner
command for a Constitution Principle VII destructive-operation pattern before Ship or
`build-feature` ever executes it, rejecting and routing any match to Principle VII approval instead
(new halt code `EXEMPT_COMMAND_DESTRUCTIVE`); and `147.036-T`'s and the plan's present-tense claim
that U12 "carries `harness-ready`" is corrected to "must carry `harness-ready` at claim time —
eligible only after harness generation runs". The workflow-policy `Version` header, stale at
`1.0.0` since the registry's creation, now reads `1.16.0` to match the Amendment Log's own last
row, which is extended in place to also describe this cycle's additions. The not-yet-created
closure-evidence filename is renamed from `docs/closure/2026-08-checkpoint-disposition-runtime-verification.md`
to `docs/closure/2026-08-24-checkpoint-disposition-runtime-verification.md` for `YYYY-MM-DD`
convention alignment across every live reference; historical cycle-17/23 mentions of the prior name
are left unmodified. All ten `exempt_verification_command` values, now carrying their marker, were
re-executed against this worktree and still exit 1 with no marker printed — the declared baseline
is unchanged. Topology, dependency edges, and shipment membership are unchanged (42/104/43,
acyclic, ready roots `{147.001-T, 147.032-T}`). The current gate state is now `cycle: 24`
`ADVISORY` (superseding `cycle: 21` `FAIL`); this cycle-24 remediation paragraph remains evidence,
not a gate — the gate itself is the separate formal `cycle: 24` confirmatory review below.

**The cycle-21 confirmatory review is complete, formally recorded as `cycle: 24`, `decision:
ADVISORY`.** This is a distinct, later pass from the cycle-24 remediation paragraph immediately
above — it is the independent review that cycle 21's own remediation queue named
`required before push`, run against the cumulative state of cycles 21 through 24. It re-confirmed
the 42-task / 104-executable-edge / 43-shipment-member topology and the ten-unit `harness-exempt`
set **SOUND** and unchanged, found no P0 and no P1, and fixed five P2 findings in the same pass
under `operator_authorization: approved`: **B1** reordered Ship's claim-time sequence
(`.github/agents/.ship.agent.md`) so the harness-exempt gate (still named Step 4.1a) runs before
the renamed Step 4.1b claims the task and sets status `active` — a halt at the gate can no longer
strand a task `active`; **B2** explicitly declared plugin bundle P-002 contract parity out of
scope / pre-existing (this plan's Documented deviations, a new `.autoharness/drift-ignore` note,
and follow-up stash `633818E1`), per the pre-existing 101-F plugin-bundle structural-verification
decision, without editing `plugin/`; **B3** extended `.autoharness/drift-ignore`'s re-apply
obligation to cover cycle 24's `EXEMPT_VERIFY_OK` marker mandate, P-002.5, and the two new halt
codes; **B4** persisted canonical `stage-pr377-cycle-21` and `stage-pr377-cycle-24` entries to
`.backlogit/memories.json` via the worktree-bound `memory save` CLI; **B5** refreshed the plan's
stale illustrative live-corpus count while preserving the nine-file expected-refusal set and the
closure artifact's pre-merge recapture requirement verbatim. `push_allowed: yes`. This
authorization is scoped to pushing the already-reviewed branch and letting PR #377 checks run; it
is explicitly **not** merge approval, not a shipment claim, and not authorization for Ship to begin
implementation. Full record: the plan's `## Plan Review` `cycle: 24` section.

**Cycle 29 (PR #377 review-thread remediation, prompt/plan artifacts only).** The six threads that
remained unresolved on HEAD `6a822ceb` after the cycle 26-28 fix budget was spent are addressed
here. Three were coupled and are decided together. (1) **`declaration-only` is withdrawn** from the
global P-002.1 vocabulary: the class let observable production surface — a serialized
`remediation_intent` field on `CheckpointSummary` (U1d) and an exported `GetCheckpointResult`
wrapper (U15) — land with no observed failing test, which is a carve-out from NON-NEGOTIABLE
Constitution Principle II. `147.032-T` and `147.038-T` lose the `harness-exempt` label and their
contract blocks and become ordinary harness-required tasks; the closed exempt set drops from ten to
**eight**. Their red is a **source-shape** harness that parses the production file with `go/parser`
and asserts the declared shape via `go/ast`, so it compiles before the declaration exists (not a
build error) and fails on an assertion (not a tautology) — both verified compiling-and-red at
worktree HEAD `6a822ceb`. **No Principle II deviation is recorded for either unit.** (2) **New
policy P-002.6 introduces dependency-aware harness waves**: Ship Step 2 becomes a per-wave
procedure driven from a new Step 4.0 wave-admission step, Step 3 builds the wave schedule, and Step
4 loops over waves. This replaces the cycle-26 declaration-prerequisite early pass, which was only
ever one wave and could not satisfy the U8b → U15 → U1b chain the reviewer named. The 42-task /
104-edge DAG schedules in **18 waves with 0 stalls and 0 compile-order violations**; U1d lands in
wave 1, U1b in wave 2, U15 in wave 3, and U8b's harness is scaffolded in wave 4 against the landed
carrier — no waiver required. All waves share one branch, one worktree, and one PR (P-016), and the
Ship circuit breakers count across the whole sequence. (3) **U10 / `147.019-T` no longer commits a
`.gitignore` rule**: its scratch workspace moves to the already-ignored
`.copilot/scratch/checkpoint-verification/` (`git check-ignore -v` → `.gitignore:5:.copilot/`), so
the unsatisfiable `EXEMPT_DELTA_EXCEEDS_CLASS`-versus-dirty-tree squeeze disappears without adding a
task or widening `verification-only` — which P-002.4 now explicitly forbids from admitting
configuration files. Three bounded fixes complete the set: P-002.3's must-fail signal list no longer
counts an unmarked exit 0 as the required pre-work failure (it is `EXEMPT_MARKER_MISSING`, a halt at
both gates); `build-feature`'s completion gate diffs the baseline against the **working tree**
because it runs before its own commit, and an empty delta is now a halt rather than a vacuous pass;
and `.autoharness/drift-ignore`'s cycle-24 note is corrected to state that the `EXEMPT_VERIFY_OK`
marker applies to `exempt_verification_command` only, never to `harness_owner_command`, which U13
leaves unmarked by design. Topology is unchanged at 42 tasks / 104 executable edges / 43 shipment
members. **This is not merge approval and not a shipment claim**; the §1.9 readiness gate remains
**FAIL on Check 3** pending fresh local review. Full record: the plan's `## Plan Review` cycle-29
section.

**Cycle 29's formal Plan Review — the fresh local review this cycle's own gate state named
required before push — supersedes it for gate-decision purposes.** Run at canonical worktree HEAD
`ef8bd954`, `dispatch_mode: single-agent-declared-degradation`
(`TOOL_DEGRADED: reviewer-subagent-dispatch`), it re-confirmed the 42-task / 104-edge / 43-member
topology and the 18-wave schedule **SOUND** and found three P2 findings, all fixed (or, for one,
recorded as an authorized advisory exception) in the same pass: **P2-1** — `build-feature`'s
Behavioral Constraints bullet still named a missing `EXEMPT_VERIFY_OK:{task_id}` marker as a
P-002.3 false-green signal, the exact conflation this cycle's own normative fix had withdrawn;
split into two sibling constraints. **P2-2** — new P-002.6 defined wave admission and requery as an
ordering rule but not how the snapshot behind them is computed; added a one-snapshot-per-wave
recipe (SQL join when `features.sql_query: true`, one-call `list --json` fallback otherwise) and a
new `WAVE_SNAPSHOT_UNRELIABLE` halt code; `workflow-policies.md` 1.17.0 → 1.18.0. **P2-3** — this
shipment's cycle 26-30 remediation arc has run past the global §1.8 three-review-fix-cycle budget;
recorded as an operator-authorized one-shipment advisory exception, with every circuit-breaker
policy file left unmodified. Two P3 items were investigated: a PowerShell shell-quoting artifact in
`.backlogit/memories.json` (`-run='^\$'`) was corrected; a possible U1d fail-count discrepancy was
investigated and found not to be a confident defect, so left unchanged. `decision: ADVISORY`,
`operator_authorization: approved`, `push_allowed: yes` — **this is autonomous completion of this
bounded cycle, not merge approval**; it does not itself resolve the §1.9 readiness gate's Check 3.
Full record: the plan's `## Plan Review` `cycle: 29` section (the formal review block, distinct
from the remediation narrative in the paragraph immediately above).


**Cycle 17 executed that formal decomposition.** All eight cycle-16 blockers are dispositioned as
closed in the plan appendix "PR #377 plan remediation, cycle 17 — formal decomposition": the P0
unsafe ambient-cwd remediation command is replaced by a structured `RemediationIntent` with
CLI-only, workspace-bound, A4c-gated rendering (U1d, U16); U8b becomes a partition-3 harness that
precedes every implementation (U15, U8b); a bounded runtime unit U10c owns the orphaned coverage;
machine offender arrays carry raw paths with structured truncation metadata (U1b, U1c); the stale
U7/U7e ownership claims and the non-registered MCP tool names are corrected; the `AbandonCheckpoint`
`%v` wrap is fixed rather than deviated from (U17); and I1 enforcement moves from an AST enumeration
to a guarded rewrite seam (U11-U14), with U2f retained only as a supplemental regression guard. The
fresh, independent `cycle: 17` review that decomposition required confirmed the five partitions,
dependency graph, and topology architecturally **sound**, but returned `decision: FAIL` on three P1
synchronization defects (a stale runtime-verification recap block, a Constitution Check Principle II
narrative, and two unmaterialized follow-up stash IDs plus one incorrect stash reference); all three
were fixed in the cycle-18 remediation session (materialized as stash `F350503F`, `A12BBAFA`, and
the `3A33E404` correction).

**Cycle 18 was the fresh, independent re-review of that fix, and it is now superseded.** It found no
P0 and no P1 (severity counts P0=0, P1=0, P2=1, P3=5: a stale "current gate state" pointer chain, an
off-by-one U8b dependent count corrected from seventeen to eighteen, three residual ambient
`backlogit docs lint` references replaced with `go run ./cmd/backlogit --cwd . docs lint`, and an
optional partition-preamble softening plus three added learning citations), reconfirmed the 40-task
/ 98-executable-edge / 41-member topology **SOUND** with ready roots exactly `{147.001-T,
147.032-T}`, and returned `decision: ADVISORY`.

**Cycle 20 supersedes it.** A fresh current-head review of PR #377 at `3bcff086` found CI 6/6 green
and the Copilot review fresh on that exact commit, but **20 unresolved Copilot threads — 17 P1 and
3 P2** — against artifacts cycle 18 had declared clean. Cycle 18's `push_allowed: yes` and
`operator_authorization: approved` are therefore withdrawn for gate purposes. The cycle-20
remediation session resolved all four root causes rather than the quoted samples:

* **A (14 P1)** — green regression guards were being committed inside the red harness. The
  "declared regression guards" device and the cycle-17 narrowed-red-selector device are withdrawn
  plan-wide and replaced by one three-step lifecycle: declaration step (no test function) → red
  harness step (only functions that fail) → green step (implementation plus `TestU<unit>Guard_`
  guards). P-004 quantifies over every scaffolded harness test function and gates the harness
  commit, not the selector. **(Superseded by cycle 31: the leading "declaration step" is withdrawn.
  The lifecycle is two steps — red harness first, green step second — and no production stub may be
  committed ahead of a unit's own harness. See the cycle-31 gate state above.)**
* **B (3 P1)** — `147.032-T` / U1d and `147.038-T` / U15 are now `harness-exempt:
  declaration-only`, each naming the downstream unit whose harness genuinely fails; the cycle-8
  "every unit needs a failing assertion" rule that forced their fabricated REDs is withdrawn.
  `147.007-T` / U3b loses its redundant production delta (U14's seam migration already delivers the
  resolve-verb conformance refusal) and becomes `harness-exempt: verification-only`; the contract's
  failing harness is the new `147.042-T` / U3c, which lands before U14.
* **C (1 P2)** — the published `remediation_intent` sentence on `backlogit_list_checkpoints` was
  false for parse-failure and schema-invalid summaries. Closed by making the code total with the
  new `147.043-T` / U6e rather than by weakening the description.
* **D (2 P2)** — `147.021-T`'s halt path now names `return_blocked` explicitly, and the archived
  `147.010-T` ownership note no longer claims U5 holds guards cycle 16 withdrew to stash
  `6FA45E69`.

Cycle 20 also adds protected invariant **I4** (declaration → harness → implementation monotonicity)
and closes the `harness-exempt` set to ten enumerated units *(superseded by cycle 29: the set is
now **eight** — U1d and U15 left it when `declaration-only` was withdrawn)*. **This is not merge approval and not a
push authorization.** The remediation is complete but unreviewed: a fresh local plan review of the
cycle-20 doctrine, the two new units, the six new edges, and the recomputed topology must pass
before this branch is presented as ready. It remains a Stage-owned planning branch and is not yet a
Ship handoff or a shipment claim.

The work is organised into five DAG partitions, which are execution phases inside the single
`130-S` release unit, not separate release units: (1) foundation diagnostics/conformance,
(2) guarded rewrite seam, (3) declarations and genuine RED harness order, (4) implementation plus
MCP/CLI/instruction contracts, (5) runtime `U10`/`U10b`/`U10c` and closure.

**P-016 is a Ship execution precondition**: exactly one active implementation worktree may exist
for this release unit. At decomposition time an unrelated linked worktree existed on another
branch; it is not a containment violation and not a plan defect, but it must be finished or removed
by its owner before Ship claims `130-S`.

Task inventory: 42 tasks, `147.001-T` … `147.043-T` (excluding archived `147.010-T` / U5b), one per plan implementation unit. Four were appended during PR #377 Copilot review cycle 3 to satisfy the mandatory granularity limits — `147.023-T` (U6d), `147.024-T` (U7c), `147.025-T` (U7d), `147.026-T` (U10b) — and one during cycle 4, `147.027-T` (U8c), which carries the CLI `checkpoint get` conformance projection that 147.016-T's parity table required and no unit owned. `147.010-T` (U5b) was archived in cycle 10 because its production delta contradicted the origin decision's scope exclusion of the state-conflict class; cycle 16 then **withdrew** the state-conflict regression rows that cycle 10 had absorbed into `147.009-T` (U5), recording them as follow-up stash `6FA45E69`. Cycle 14 added `147.028-T` (U2g, context-member duplicate detection read boundary) and `147.029-T` (U7e, domainError mappings); cycle 16 narrowed U7e from three mappings to the one that is reachable and added `147.030-T` (U1b). Cycle 17 added eleven tasks — `147.031-T` (U1c), `147.032-T` (U1d), `147.033-T` (U2h), `147.034-T` (U11), `147.035-T` (U12), `147.036-T` (U13), `147.037-T` (U14), `147.038-T` (U15), `147.039-T` (U16), `147.040-T` (U17), `147.041-T` (U10c) — and retitled five: `147.021-T`, `147.030-T`, `147.016-T`, `147.029-T`, `147.015-T`. Cycle 20 added two — `147.042-T` (U3c, the resolve-verb conformance harness that must be red before `147.037-T`) and `147.043-T` (U6e, `RemediationIntent` on the parse-failure and schema-invalid branches) — and retitled `147.007-T` (U3b) from an implementation unit to a verification-only contract pin. No existing task ID has ever been renumbered, and no task was archived in cycle 17 or cycle 20; the plan's PR #377 remediation sections are the authoritative record of every split, narrowing, and retirement.

Topology (cycle 20): 42 queued tasks, 104 queued-to-queued executable edges, 43 members in shipment `130-S`, ready set exactly `{147.001-T, 147.032-T}` — two roots, `U1` (typed-error declaration) and `U1d` (remediation-intent declaration), unchanged because both cycle-20 tasks are interior nodes. Graph verified acyclic by Kahn topological sort, 42/42 nodes ordered. Historical total edges 105 (104 executable plus the archived `147.010-T -> 147.009-T`); schedule against the 104 only.

Test-lifecycle state (cycle 21, wording corrected cycle 22): 32 tasks are **planned to own** a red harness — each names the `TestU<unit>_` functions its harness step must land, and none has been scaffolded yet, because Ship has not run `harness-architect` against this shipment. **Cycle 29 correction**: 34 tasks are now planned to own a red harness — the 32 above plus `147.032-T` (U1d) and `147.038-T` (U15), which lost the withdrawn `declaration-only` exemption and now carry **source-shape** (`go/ast`) harnesses that compile before their declarations exist and fail on assertions (`TestU1d_*`, `TestU15_*`; both verified compiling-and-red at worktree HEAD `6a822ceb`), plus their own `harness-ready` obligation. The remaining **8** are `harness-exempt` under the closed set enumerated in the plan's Documented deviations — `147.036-T` (`covered-by 147.035-T`), `147.007-T` and `147.021-T` (`verification-only`), `147.017-T` and `147.018-T` (`docs-only`), and `147.019-T`, `147.026-T`, `147.041-T` (`verification-only`). All eight carry the `harness-exempt` label in frontmatter (cycle 21 applied seven that were missing it: `147.017-T`, `147.018-T`, `147.019-T`, `147.021-T`, `147.026-T`, `147.036-T`, `147.041-T`), and cycle 23 added a canonical `harness-exemption-contract` body block to all then-ten (now eight; U1d's and U15's blocks were removed in cycle 29) — five keys in identical order (`harness_exemption_class`, `harness_exemption_reason`, `harness_owner`, `exempt_verification_command`, `exempt_precondition`), plus `harness_owner_command` on `147.036-T` alone. Every `exempt_verification_command` was executed at HEAD `e8b974e` and observed **failing**, satisfying `exempt_precondition: must-fail-before-deliverable`. Every behaviour-changing task is backed by a failing harness observed red before its implementation lands, carried on the task itself except for the single explicitly allowed carve-out `147.036-T` / U13, whose red harness is owned by its declared prerequisite `147.035-T` / U12 (protected invariant I4). Cycle 24 added a mandatory positive-completion marker (`EXEMPT_VERIFY_OK:{task_id}`) to all then-ten `exempt_verification_command` values (eight remain after cycle 29; the marker is required on `exempt_verification_command` only and never on `harness_owner_command`, which is validated by its named `--- PASS:` count) — re-executed at this cycle's worktree HEAD and still observed failing, marker absent — and corrected `147.036-T`'s owner-status wording from "carries `harness-ready`" to "must carry `harness-ready` at claim time — eligible only after harness generation runs".

**Ship ready-selection query (cycle 21 adapter, generalized into global policy cycle 22).** Ship's ready-queue filter for this shipment MUST treat a `147.0xx-T` task as harness-satisfied — eligible for build-feature dispatch without a scaffolded red harness — when its `labels` field contains `harness-ready`, **or** contains `harness-exempt` and passes the fail-closed P-002.1 evaluation. Equivalent selection query, applied only after P-002.1 has validated each exempt task: `SELECT id FROM items WHERE parent_id = '147-F' AND status = 'queued' AND (labels LIKE '%harness-ready%' OR labels LIKE '%harness-exempt%')`. Cycle 21 recorded this as a shipment-local P-002 enforcement adapter because the global policy had no vocabulary for `harness-exempt`; cycle 22 closed that gap in `.github/policies/workflow-policies.md` (P-002.1 and P-002.2) and `.github/agents/.ship.agent.md` (Step 2a), so this paragraph now restates a general contract rather than carrying a local exception. **Cycle 23 split the evaluation**: the query above admits a task to the *queue* after **static intake** only; every exempt task additionally passes Ship **Step 4.1a** at claim time, which owns the `covered-by` owner red-evidence check and the P-002.3 must-fail-before-deliverable probe. Queue admission is not build admission. It remains **not** a waiver of Constitution Principle II. Full rule and rationale: the plan's Documented deviations section, "Ship ready-selection contract".

**HARD MERGE GATE (owner `147.018-T` / U9b).** A pull request containing `147.007-T` (U3b), `147.008-T` (U4), or `147.009-T` (U5) MUST NOT merge unless the `.github/instructions/backlogit.instructions.md` delta from `147.018-T` is present in the same merge commit. The three dependent tasks carry a `merge-gate-dependent` label and a backreference to this gate; `147.018-T` carries the `merge-gate` label and states the gate in full. This constrains implementation units to each other inside one pull request — it adds no dependency edge and leaves executable ordering unchanged.

Nine live checkpoints under `.backlogit/checkpoints/` carry unmodeled top-level keys and are schema-invalid. They are **out of the mutation boundary** for this work and are enumerated in the plan so their post-merge refusals are read as correct behaviour rather than a regression.
