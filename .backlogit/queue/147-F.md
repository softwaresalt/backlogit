---
artifact_type: feature
created_at: 2026-08-24T19:00:08.4266989Z
custom_fields:
    harness_status: pending
    source_stash_id: D3CE9E81
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Preserve or refuse on unmodeled top-level keys in checkpoint disposition rewrites. core.AbandonCheckpoint (internal/core/checkpoint_disposition.go) and events.ResolveCheckpoint perform the same parse -> mutate -> re-marshal round-trip on a pre-existing file, so unmodeled TOP-LEVEL keys are still dropped on abandon and resolve even after 146-F / 129-S lands. U2 fixes the nested `context` loss as a side effect; the top level needs either a raw top-level carrier or an explicit refusal-to-mutate policy, which is a distinct design decision from the create boundary. Residual exposure: all nine live files under .backlogit/checkpoints/ carry such keys. Provenance: scope-boundary follow-up 1 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md (Scope boundaries and recorded follow-ups); parent work feature 146-F, shipment 129-S; external source 3C7AAC71. NOT release-blocking for 129-S.'
id: 147-F
priority: high
status: queued
title: Refuse to rewrite checkpoints carrying unmodeled top-level keys
updated_at: 2026-08-24T21:29:00.9915369Z
---

Checkpoint `abandon` and `resolve` parse a stored checkpoint, mutate the decoded struct, and re-marshal it. Any top-level JSON key that is not modeled on `CheckpointV1` is silently dropped by that round trip. Nested `context` preservation already shipped in 146-F; the top level did not.

Deliberation `docs/decisions/2026-08-24-checkpoint-toplevel-key-disposition-deliberation.md` decided **Option B — refuse to mutate** rather than preserve, and widened the decision on two grounds discovered empirically:

* `ResolveCheckpoint` has **no validity gate at all**. On a legacy document it replaces the whole file with a fabricated skeleton (`schema_version: 0`, empty `agent`, zero `created_at`, empty `context`), destroying every byte of evidence. Both Stage and Ship session-start recovery call it. This is the higher-severity half of the defect.
* A naive refusal would deadlock: `QuarantineCheckpoint` refuses any target that parses and validates, so a valid-but-non-conforming file would have no disposition path at all. Quarantine classification must widen in the same change.

Plan: `docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md`.

**Plan-review state: FAIL (gate pending re-review) — current gate is `cycle: 16`,
`dispatch_mode: single-agent-declared-degradation` (`TOOL_DEGRADED: reviewer-subagent-dispatch`),
`decision: FAIL`.** The cycles 1-13 `PASS`, the `cycle: 14` `FAIL`, and the `cycle: 15` `FAIL` are
all historical and superseded by the `cycle: 16` record. Cycle 16 returned 1 P0 and 7 P1 blocking
findings (severity counts P0=1, P1=7, P2=3, P3=2) and
`restage_recommendation: formal-decomposition`.

**Cycle 17 executed that formal decomposition.** All eight blockers are dispositioned as closed in
the plan appendix "PR #377 plan remediation, cycle 17 — formal decomposition": the P0 unsafe
ambient-cwd remediation command is replaced by a structured `RemediationIntent` with CLI-only,
workspace-bound, A4c-gated rendering (U1d, U16); U8b becomes a partition-3 harness that precedes
every implementation (U15, U8b); a bounded runtime unit U10c owns the orphaned coverage; machine
offender arrays carry raw paths with structured truncation metadata (U1b, U1c); the stale U7/U7e
ownership claims and the non-registered MCP tool names are corrected; the `AbandonCheckpoint` `%v`
wrap is fixed rather than deviated from (U17); and I1 enforcement moves from an AST enumeration to
a guarded rewrite seam (U11-U14), with U2f retained only as a supplemental regression guard.
**The cycle-16 gate is not cleared by this remediation.** A fresh, independent plan review must be
dispatched against the decomposed plan before implementation begins. **Do not push this branch and
do not hand this shipment to Ship until that review passes.**

The work is organised into five DAG partitions, which are execution phases inside the single
`130-S` release unit, not separate release units: (1) foundation diagnostics/conformance,
(2) guarded rewrite seam, (3) declarations and genuine RED harness order, (4) implementation plus
MCP/CLI/instruction contracts, (5) runtime `U10`/`U10b`/`U10c` and closure.

**P-016 is a Ship execution precondition**: exactly one active implementation worktree may exist
for this release unit. At decomposition time an unrelated linked worktree existed on another
branch; it is not a containment violation and not a plan defect, but it must be finished or removed
by its owner before Ship claims `130-S`.

Task inventory: 40 tasks, `147.001-T` … `147.041-T` (excluding archived `147.010-T` / U5b), one per plan implementation unit. Four were appended during PR #377 Copilot review cycle 3 to satisfy the mandatory granularity limits — `147.023-T` (U6d), `147.024-T` (U7c), `147.025-T` (U7d), `147.026-T` (U10b) — and one during cycle 4, `147.027-T` (U8c), which carries the CLI `checkpoint get` conformance projection that 147.016-T's parity table required and no unit owned. `147.010-T` (U5b) was archived in cycle 10 because its production delta contradicted the origin decision's scope exclusion of the state-conflict class; cycle 16 then **withdrew** the state-conflict regression rows that cycle 10 had absorbed into `147.009-T` (U5), recording them as follow-up stash `6FA45E69`. Cycle 14 added `147.028-T` (U2g, context-member duplicate detection read boundary) and `147.029-T` (U7e, domainError mappings); cycle 16 narrowed U7e from three mappings to the one that is reachable and added `147.030-T` (U1b). Cycle 17 added eleven tasks — `147.031-T` (U1c), `147.032-T` (U1d), `147.033-T` (U2h), `147.034-T` (U11), `147.035-T` (U12), `147.036-T` (U13), `147.037-T` (U14), `147.038-T` (U15), `147.039-T` (U16), `147.040-T` (U17), `147.041-T` (U10c) — and retitled five: `147.021-T`, `147.030-T`, `147.016-T`, `147.029-T`, `147.015-T`. No existing task ID was renumbered and no task was archived in cycle 17; the plan's PR #377 remediation sections are the authoritative record of every split, narrowing, and retirement.

Topology (cycle 17): 40 queued tasks, 98 queued-to-queued executable edges, 41 members in shipment `130-S`, ready set exactly `{147.001-T, 147.032-T}` — two roots, `U1` (typed-error declaration) and `U1d` (remediation-intent declaration). Graph verified acyclic by Kahn topological sort, 40/40 nodes ordered. Historical total edges 99 (98 executable plus the archived `147.010-T -> 147.009-T`); schedule against the 98 only.

Nine live checkpoints under `.backlogit/checkpoints/` carry unmodeled top-level keys and are schema-invalid. They are **out of the mutation boundary** for this work and are enumerated in the plan so their post-merge refusals are read as correct behaviour rather than a regression.