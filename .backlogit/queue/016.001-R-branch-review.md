---
artifact_type: review
created_at: 2026-04-06T21:44:08.865385-07:00
id: 016.001-R
status: queued
title: Branch review
updated_at: 2026-04-06T23:40:11.2188522-07:00
---

## Summary

| Severity | Count | Action |
|---|---|---|
| P0 | 0 | None |
| P1 | 2 | Fixed during review remediation |
| P2 | 1 | Fixed during review remediation |
| P3 | 0 | None |

## Findings

### P1 · Migration script rewrites the wrong log root

*Files:* `scripts/migrate-ids.go`, `internal/core/workspace.go`

The migration script updated `logs/` at the repo root, but runtime writers and rehydration use `.backlogit/logs/`. The remediation switched the migration to the canonical workspace log root and added regression coverage against real `.backlogit/logs` data.

### P1 · Migration script leaves deliberation and stash references stale

*Files:* `scripts/migrate-ids.go`, `.backlogit/queue/016-F.md`, `.backlogit/queue/.stash.md`

The migration only rewrote top-level `id`, `parent_id`, `dependencies`, and `archived_from`. The remediation now rewrites mapped IDs across Markdown frontmatter string values, artifact bodies, and `.stash.md`, with regression coverage for deliberation references and stash links.

### P2 · Legacy-parent fallback can mint mixed-format child IDs

*Files:* `internal/core/hierarchy.go`

`numericParentPath` swallowed `ParseHierarchicalID` errors and fell back to the raw parent ID. The remediation now surfaces an explicit error instead of silently minting mixed-format child IDs during transition states.

## Learnings Applied

* Shipment review focused on persisted-state consistency across Markdown, SQLite, and JSONL because this branch changes all three layers at once.

## Residual Work

* None. The verified findings from this review were remediated in the working tree and validated before PR handoff.

## Logged Follow-Up Items

* `001-B` Migrate item logs under `.backlogit/logs` - closed after remediation
* `002-B` Rewrite deliberation and stash links during ID migration - closed after remediation
* `003-B` Reject mixed-format child IDs from legacy parent fallback - closed after remediation