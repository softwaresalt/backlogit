---
artifact_type: task
created_at: 2026-09-24T07:02:08.2566712Z
id: 174.072-T
parent_id: 174-F
priority: high
status: queued
title: Restore explicit Ship governed-lifecycle tool allowlist and reconcile harness manifest
updated_at: 2026-09-24T07:09:01.1277933Z
---

Corrective work unit (Wave 17B) — restore the protected explicit Ship governed-lifecycle tool allowlist that a generic renderer/template overwrote with backlogit/*, failing the integration contract requiring explicit backlogit_block_shipment/backlogit_unblock_shipment/backlogit_normalize_blocked_shipment. HEAD holds the correct explicit allowlist; the uncommitted working-tree _ship.agent.md regressed it to a wildcard. Harness/configuration surface (installed agent frontmatter + harness manifest drift/checksum), moderate mutation authority — distinct from any Go source/test task. P-021 C1 OUT OF SCOPE for all completed 174-F tasks. Restore explicit allowlist, preserve unrelated concurrent model-routing edits, reconcile manifest checksum/drift; do NOT change registry/plugin template here. Source stash 4A0B7BCF; deliberation 071-DL.

<!-- BEGIN:acceptance-criteria -->
AC1 (Restore explicit allowlist as SCALAR string, drop wildcard in place): the .github/agents/_ship.agent.md tools frontmatter MUST remain a scalar comma-separated string (the contract test does shipFrontmatter["tools"].(string) then require.Contains — a YAML list/flow-sequence would break the type assertion). Replace the drifted backlogit/* token IN PLACE with the explicit governed backlogit tools of the HEAD form, including the required lifecycle trio backlogit_block_shipment, backlogit_unblock_shipment, backlogit_normalize_blocked_shipment AND every other intended backlogit tool at HEAD. No backlogit/* wildcard may remain.
AC2 (Preserve unrelated edits + exact HEAD tokens): restore ONLY the tools scalar line; preserve all concurrent unrelated model-routing frontmatter edits (model_tier / model_provider / model_family / reasoning_effort, etc.). Restore the memory token to EXACTLY the HEAD token form (do not leave a mismatched vscode/memory if HEAD used memory). Do not touch other unrelated tokens (the eight read-only graphtor verbs and engram/* stay as HEAD had them; engram/* enumeration is out of scope here).
AC3 (Reconcile manifest, LF-normalized checksum): update the .autoharness/harness-manifest.yaml entry for .github/agents/_ship.agent.md — recompute its checksum over LF-normalized content (per the manifest's EOL-normalized convention; a naive CRLF recompute on this Windows checkout will not match the verifier) so it matches the restored file, and preserve/refresh drift_allowed: true + a drift_reason that documents the deliberately explicit, no-wildcard governed allowlist (incl. the lifecycle trio; do not auto-revert).
AC4 (Contract GREEN + MANDATORY wildcard rejection): tests/integration/shipment_155_harness_contract_test.go (require.Contains of the trio) MUST pass, AND a require.NotContains(shipTools, "backlogit/*") assertion MUST be added to lock the invariant. This is mandatory (not only-if-needed): require.Contains alone would still pass if a future render keeps the trio and re-adds backlogit/* alongside it, which is exactly the drift being protected against.
AC5 (No template change here; MANDATORY separate follow-up): do NOT modify the registry, plugin, or upstream agent template in this task (registry already has the trio via commit ac5ebd29). Because the generic renderer already overwrote the protected allowlist once, capturing a SEPARATE upstream tune-preservation follow-up (stash) is MANDATORY, not conditional, so the protected divergence is not silently re-drifted on the next render.
AC6 (Verification): run verify-workspace / harness drift verification and the integration contract test GREEN; confirm the recomputed LF-normalized manifest checksum matches the restored file; confirm no backlogit/* remains in the Ship allowlist. Do NOT run go test ./... within this task; targeted contract + verify only.
AC7 (Circuit / full-suite disposition): shared with 174.071-T — after BOTH corrective tasks land, one NEW separately-authorized full-suite run (explicit operator authorization immediately before), then the 155-S zero-P0/P1 stop-gate and final standard + adversarial review over the changed surfaces.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Evidence: HEAD _ship.agent.md tools line is the correct explicit allowlist (incl. trio); working-tree regressed to 'vscode/memory, backlogit/*, engram/*, ...'. Manifest entry .autoharness/harness-manifest.yaml path .github/agents/_ship.agent.md checksum 99fa4692befa977a4606a27b650cc9b08da9463833190be447993e2160ebbb0b, drift_allowed:true (line ~373). Contract require.Contains trio @ tests/integration/shipment_155_harness_contract_test.go:37-46. Registry trio added by commit ac5ebd29 (no registry change needed). Action risk: moderate mutation authority on harness/config surface — Ship/tune applies; Stage only plans. Membership: governed member of active 155-S; no blocks edge. Files: .github/agents/_ship.agent.md + .autoharness/harness-manifest.yaml (+ integration contract only if a wildcard-rejection assertion is added).
<!-- END:implementation-notes -->