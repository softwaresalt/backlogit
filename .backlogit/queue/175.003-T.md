---
artifact_type: task
created_at: 2026-09-17T23:59:16.3521277Z
dependencies:
    - 175.001-T
id: 175.003-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U3 - Golden-fixture LF normalization + U4a green (fixes 92F79833)
updated_at: 2026-09-17T23:59:16.3521277Z
---

Freeze internal/faultline/testdata/parity_v1.golden.json as byte-exact LF. Add a REQUIRED non-vacuity assertion in evidence_conformance_test.go comparing Canonical() output byte-for-byte to on-disk LF bytes (never regenerated-and-trusted). Cite the go-analysistest non-vacuity compound doc. Forbid -update regeneration in CI.

Acceptance criteria:
- Scope (files): only `internal/faultline/testdata/parity_v1.golden.json` (freeze as byte-exact LF) and `internal/faultline/evidence_conformance_test.go` (add the non-vacuity assertion). No other file is owned by this unit. These two files are PRE-ASSIGNED to U3 and are EXPLICITLY EXCLUDED from U11's staticcheck overlap inventory, so no other unit (including U11) ever edits them — exactly-one-owner holds by construction. Because U11 and the errcheck units exclude these two files, U3 owns EVERY finding in them (test correctness, gofmt, and any errcheck/staticcheck), so no finding in them is orphaned. U3 depends on U1 (`175.001-T`) only.
- Targeted test: `go test -run '^TestU4aBehaviorCanonicalByteStable$' ./internal/faultline` passes on a Windows checkout.
- Non-vacuity / canonical-byte assertion (REQUIRED): the test asserts the golden is non-empty AND compares `EvidenceArtifact.Canonical()` output byte-for-byte against the on-disk LF bytes (never a regenerated-and-trusted value). Cite `docs/compound/test-failures/go-analysistest-absolute-path-and-non-vacuity-2026-09-11.md` (same package `internal/faultline`, shipment 140-S).
- gofmt on owned files: `gofmt -l` on the touched `evidence_conformance_test.go` is empty.
- No silent `-update` rewrite: confirm no `-update`/golden-regeneration behavior can silently rewrite the canonical fixture outside the test contract; `-update` regeneration is forbidden in CI.
- Existing test expectations: all current internal/faultline tests pass; behavior preserved (test/fixture-only change).