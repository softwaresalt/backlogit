---
artifact_type: task
created_at: 2026-09-19T01:19:16.2636527Z
dependencies:
    - 175.001-T
id: 175.003-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (staticcheck): internal/canonical/canonical.go'
updated_at: 2026-09-19T07:13:13.6453314Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: staticcheck
Package: ./internal/canonical
Owned source file(s) (1): internal/canonical/canonical.go
Owned baseline occurrences (1) — line:column shown are DIAGNOSTIC baseline coordinates only, NOT durable identity (golangci-lint v2.13.2, unbounded invocation; diagnostic table in docs/decisions/2026-09-17-baseline-lint-inventory.md). Durable ownership is the line-stable group-multiset model in the task-lint-contract below (stable key = path + linter + normalized message; per-task occurrence multiplicity with ordered-slice expected residual):
- internal/canonical/canonical.go:L228:C5 [staticcheck] QF1012: Use fmt.Fprintf(...) instead of WriteString(fmt.Sprintf(...))
Affected functions (1 distinct): internal/canonical/canonical.go: encodeString

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/canonical/lintfix_175_003_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_003_LintFix` (deterministic `TestU175_003_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/canonical" -run "^TestU175_003_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_003_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_003_ lines"; exit 1 }; exit 0'

Fix strategy (staticcheck): apply the exact staticcheck-suggested rewrite named in each finding (QF1012/QF1011/QF1003 quickfix or ST1008 result-ordering) directly in source. Do NOT suppress with `//nolint`; do NOT alter observable behavior beyond the mechanical rewrite. Edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (owned source file(s) + one owned harness file).
- Implementation files = 1 (owned harness excluded).
- Owned baseline findings = 1 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/canonical -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.003-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "identity_model": "line-stable-group-multiset/v1",
    "packages": [
      "./internal/canonical"
    ],
    "owned_files": [
      "internal/canonical/canonical.go"
    ],
    "owned_group_count": 1,
    "owned_occurrence_count": 1,
    "owned_groups": [
      {
        "group_fingerprint": "a2c4932a14cc002ef8a3c717db2a270128ada826ad6a31650e337a2d36748d0d",
        "path": "internal/canonical/canonical.go",
        "linter": "staticcheck",
        "normalized_message": "QF1012: Use fmt.Fprintf(...) instead of WriteString(fmt.Sprintf(...))",
        "group_multiplicity": 1,
        "owned_count": 1,
        "owned_ordinals": [
          1
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 228,
            "column": 5
          }
        ]
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.003-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
