---
artifact_type: task
created_at: 2026-09-17T23:59:16.3521277Z
dependencies:
    - 175.001-T
id: 175.003-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: U3 - Golden-fixture LF normalization + U4a green (fixes 92F79833)
updated_at: 2026-09-18T05:07:57.7923643Z
---

Freeze internal/faultline/testdata/parity_v1.golden.json as byte-exact LF and install a GENUINELY RED-BEFORE-GREEN CRLF regression guard in evidence_conformance_test.go. The guard compares `EvidenceArtifact.Canonical()` output byte-for-byte to on-disk LF bytes (never regenerated-and-trusted). Cite the go-analysistest non-vacuity compound doc. Forbid -update regeneration in CI.

Genuine red-before-green contract (resolves the P2 "U3 already green after U1" finding):
- The problem: U1's on-disk renormalization would already leave the golden as LF, so an assertion that merely re-checks the on-disk golden is green-on-arrival (vacuous) once U1 has run — it is NOT a genuine red-before-green harness.
- The fix: U3 owns a SYNTHETIC in-memory CRLF regression sub-case that is red-before-green INDEPENDENTLY of U1's file renormalization. The test constructs a CRLF-corrupted copy of the golden IN MEMORY (`bytes.ReplaceAll(golden, []byte("\n"), []byte("\r\n"))`) and asserts that (a) `Canonical()` emits LF-only bytes and (b) a byte-for-byte comparison DETECTS and REJECTS the synthetic CRLF blob (the comparison returns not-equal / the guard fails on CRLF). This sub-case does NOT read any file U1 touched, so U1 landing does not make it green — the guard is genuinely RED before U3's normalization-assertion logic exists and GREEN only after U3 installs it. Authoring order: the RED synthetic-CRLF assertion is added and observed failing BEFORE the guard predicate is implemented (P-002/P-004), then driven green.
- U1 gate preserved: U3 renormalizes NO tracked file and does NOT re-run U1's renormalization; it freezes the already-normalized on-disk golden and adds test-only logic. U1's operator-only renormalization approval gate is untouched (U3 authors test/fixture assertions only, never a repository-wide renormalization).

Acceptance criteria:
- Scope (files): only `internal/faultline/testdata/parity_v1.golden.json` (freeze as byte-exact LF) and `internal/faultline/evidence_conformance_test.go` (add the synthetic-CRLF red-before-green regression guard + the on-disk non-vacuity assertion). No other file is owned by this unit. These two files are PRE-ASSIGNED to U3 and are EXPLICITLY EXCLUDED from U11's staticcheck overlap inventory, so no other unit (including U11) ever edits them — exactly-one-owner holds by construction. Because U11 and the errcheck units exclude these two files, U3 owns EVERY finding in them (test correctness, gofmt, and any errcheck/staticcheck), so no finding in them is orphaned. U3 depends on U1 (`175.001-T`) only.
- Genuine RED-before-green (required): the synthetic in-memory CRLF sub-case FAILS before U3's normalization-assertion logic exists and PASSES only after it is installed; its red state does NOT depend on U1 having-not-run (it operates on a synthetic in-memory CRLF blob, not the on-disk golden U1 renormalizes). This satisfies P-002/P-004 non-vacuity; it is NOT green-on-arrival.
- Targeted test: `go test -run '^TestU4aBehaviorCanonicalByteStable$' ./internal/faultline` passes on a Windows checkout, and the synthetic-CRLF regression sub-case is part of that (or a sibling reserved) test and demonstrably red before green.
- Non-vacuity / canonical-byte assertion (REQUIRED): the test asserts the golden is non-empty AND compares `Canonical()` output byte-for-byte against the on-disk LF bytes (never a regenerated-and-trusted value) AND rejects the synthetic CRLF blob. Cite `docs/compound/test-failures/go-analysistest-absolute-path-and-non-vacuity-2026-09-11.md` (same package `internal/faultline`, shipment 140-S).
- gofmt on owned files: `gofmt -l` on the touched `evidence_conformance_test.go` is empty.
- No silent `-update` rewrite: confirm no `-update`/golden-regeneration behavior can silently rewrite the canonical fixture outside the test contract; `-update` regeneration is forbidden in CI.
- U1 gate preserved (required): U3 renormalizes no tracked file and does not invoke a repository-wide renormalization; U1's operator-only renormalization approval gate is untouched.
- Existing test expectations: all current internal/faultline tests pass; behavior preserved (test/fixture-only change).
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed. The synthetic-CRLF regression guard IS the required RED harness for this unit.