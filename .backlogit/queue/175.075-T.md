---
artifact_type: task
created_at: 2026-09-19T01:41:09.8065505Z
dependencies:
    - 175.044-T
id: 175.075-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/db/rehydration_link_validation_harness_042_test.go (+1)'
updated_at: 2026-09-19T07:33:34.3063948Z
---

NORMAL harness-required, file-owned baseline-lint remediation task under the baseline-convergence release unit (feature 175-F). This task owns an exact, disjoint slice of the governed 497-identity supported-platform baseline (Windows-primary 489 findings + 8 Linux-only additions across the windows+linux supported surface) and modifies only the file(s) it owns (file-partition / exactly-one-owner invariant).

Linters: errcheck
Package: ./internal/db
Owned source file(s) (2): internal/db/rehydration_link_validation_harness_042_test.go, internal/db/schema.go
Owned baseline occurrences (8) — line:column shown are DIAGNOSTIC baseline coordinates only, NOT durable identity (golangci-lint v2.13.2, unbounded invocation; diagnostic table in docs/decisions/2026-09-17-baseline-lint-inventory.md). Durable ownership is the line-stable group-multiset model in the task-lint-contract below (stable key = path + linter + normalized message; per-task occurrence multiplicity with ordered-slice expected residual):
- internal/db/rehydration_link_validation_harness_042_test.go:L61:C22 [errcheck] Error return value of `database.Close` is not checked
- internal/db/rehydration_link_validation_harness_042_test.go:L90:C22 [errcheck] Error return value of `database.Close` is not checked
- internal/db/rehydration_link_validation_harness_042_test.go:L127:C22 [errcheck] Error return value of `database.Close` is not checked
- internal/db/rehydration_link_validation_harness_042_test.go:L159:C22 [errcheck] Error return value of `database.Close` is not checked
- internal/db/schema.go:L89:C18 [errcheck] Error return value of `rows.Close` is not checked
- internal/db/schema.go:L166:C18 [errcheck] Error return value of `rows.Close` is not checked
- internal/db/schema.go:L194:C18 [errcheck] Error return value of `rows.Close` is not checked
- internal/db/schema.go:L238:C18 [errcheck] Error return value of `rows.Close` is not checked
Affected functions (8 distinct): internal/db/rehydration_link_validation_harness_042_test.go: TestMergeSync_InvalidLinkType_NotIndexed, TestRehydrate_InvalidLinkType_NotIndexed, TestRehydrate_MixedLinkTypes_OnlyValidIndexed, TestRehydrate_ValidLinkType_Indexed; internal/db/schema.go: introspectColumns, introspectIndexColumns, introspectIndexes, listTables

Owned harness file (RESERVED to this task, uniquely named, non-colliding): internal/db/lintfix_175_075_test.go
Ship's harness phase scaffolds this harness with its admitted ready wave (per _ship.agent.md Step 2 and the harness-architect skill), leaving same-wave siblings red. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. No later-wave harness is scaffolded early.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test named `TestU175_075_LintFix` (deterministic `TestU175_075_` token; no other task uses this prefix). RED before the fix (assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/db" -run "^TestU175_075_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_075_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_075_ lines"; exit 1 }; exit 0'

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return is NOT an allowed resolution): for every flagged call, capture the returned error and handle it -- checked propagation with `fmt.Errorf("context: %w", err)`, a named-return `errors.Join`, or (inside a test/benchmark) fail via the in-scope `*testing.T`/`*testing.B` receiver (`require.NoError`, `t.Fatalf`, `b.Fatalf`). A deferred close becomes a checked deferred close. No error return is discarded; no cross-file or cross-package helper is introduced; edits are confined to the owned file(s).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 3 (owned source file(s) + one owned harness file).
- Implementation files = 2 (owned harness excluded).
- Owned baseline findings = 8 (<= 16, contract-bounded).
- Human effort ~< 2 hours: mechanical/localized lint remediation confined to owned file(s).
Test-only-safe compile: verify with `go test ./internal/db -run=^$ -count=1` (compiles _test.go without running the full suite), never `go build`.

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 and proves >= 1 named PASS (non-vacuous), and the task-lint contract command below exits 0 (no owned baseline finding remains in the owned file(s)/identity slice).
- Task-scoped lint verification: the canonical executable verifier is the `task_lint_cmd` inside this task's `task-lint-contract` block below.

<!-- BEGIN:task-lint-contract -->

```json
{
  "task_lint_cmd": "pwsh -NoProfile -File scripts/verify-task-lint.ps1 -TaskId 175.075-T -FeatureId 175-F",
  "lint_scope": {
    "kind": "bounded-finding-set-go-lint",
    "identity_model": "line-stable-group-multiset/v1",
    "packages": [
      "./internal/db"
    ],
    "owned_files": [
      "internal/db/rehydration_link_validation_harness_042_test.go",
      "internal/db/schema.go"
    ],
    "owned_group_count": 2,
    "owned_occurrence_count": 8,
    "owned_groups": [
      {
        "group_fingerprint": "276ea6bf25701fca7c286851b93a708d7d0bbdcb5ec4f99c3c78d773b0164db0",
        "path": "internal/db/rehydration_link_validation_harness_042_test.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `database.Close` is not checked",
        "group_multiplicity": 4,
        "owned_count": 4,
        "owned_ordinals": [
          1,
          2,
          3,
          4
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 61,
            "column": 22
          },
          {
            "line": 90,
            "column": 22
          },
          {
            "line": 127,
            "column": 22
          },
          {
            "line": 159,
            "column": 22
          }
        ]
      },
      {
        "group_fingerprint": "a5b7679248cca26580a26d00ce957308e737adcdd21e5a85054bc862fc7c17d5",
        "path": "internal/db/schema.go",
        "linter": "errcheck",
        "normalized_message": "Error return value of `rows.Close` is not checked",
        "group_multiplicity": 4,
        "owned_count": 4,
        "owned_ordinals": [
          1,
          2,
          3,
          4
        ],
        "expected_group_residual_after_task": 0,
        "diagnostic_baseline_coordinates": [
          {
            "line": 89,
            "column": 18
          },
          {
            "line": 166,
            "column": 18
          },
          {
            "line": 194,
            "column": 18
          },
          {
            "line": 238,
            "column": 18
          }
        ]
      }
    ]
  },
  "non_vacuity_evidence": {
    "success_marker": "TASK-LINT-OK:175.075-T:finding-remediation"
  }
}
```

<!-- END:task-lint-contract -->
