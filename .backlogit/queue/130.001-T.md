---
artifact_type: task
created_at: 2026-07-28T22:39:37.5093336Z
dependencies:
    - 130.004-T
id: 130.001-T
labels:
    - durable-writes
    - core
    - archive
parent_id: 130-F
priority: medium
references:
    - docs/exec-plans/2026-07-28-durable-writes-second-layer-hardening-plan.md
status: queued
title: U1 - reconcile ErrWriteIndeterminate in UnarchiveItem non-git restore
updated_at: 2026-07-28T22:39:37.5093336Z
---

internal/core/archive.go UnarchiveItem non-git branch: on ErrWriteIndeterminate from the restore content write (replaceFileWithOptions), do NOT early-return leaving archive copy plus restored file (duplicate). Complete-or-surface per commit-then-surface: remove the archive copy and DB-upsert so FS and DB agree, then surface IsWriteIndeterminate wrapped. Keep the ErrWriteNotApplied early-return (file untouched, safe). Combined-failure invariant (Copilot review): track whether the restore write was indeterminate; if the subsequent DB upsert ALSO fails, do NOT call restoreArchiveAfterUnarchiveFailure (archive.go ~786-793) - it removes the restored file and restores the archive copy, rolling back a possibly-applied write and violating never-roll-back-indeterminate. Instead surface errors.Join(ErrWriteIndeterminate, upsertErr) and leave the restored file in place; the existing rollback path is retained ONLY for the clean / not-applied restore write. Seam: replaceFileWriteFn; the test override MUST write the restored bytes and THEN return wrapped ErrWriteIndeterminate to reproduce the file-present state. Test-first (archive_durable_write_test.go; no t.Parallel). Acceptance criteria (<=3 scenarios): (a) indeterminate restore write -> exactly one canonical file, archive removed, DB row present, IsWriteIndeterminate true, no rollback; AND the combined sub-case (restore indeterminate + upsert fails) does NOT invoke restoreArchiveAfterUnarchiveFailure and keeps the restored file (both signals surfaced); (b) not-applied -> unchanged safe-abort; (c) durable-off unchanged. Depends on U4. Plan unit U1.