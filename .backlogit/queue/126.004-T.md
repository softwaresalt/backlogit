---
artifact_type: task
created_at: 2026-07-25T22:34:10.9755526Z
dependencies:
    - 126.001-T
    - 126.002-T
    - 126.003-T
id: 126.004-T
labels:
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U4: Add always-reporting md-lint job + md_touched classifier to ci.yml'
updated_at: 2026-07-25T22:34:10.9755526Z
---

Domain: CI/config. Add a 'md-lint' job to .github/workflows/ci.yml that runs markdownlint-cli2 (via make md-lint or npx) over the U1 scope. Wire md-file detection by adding a md_touched output to the existing changes/paths-filter classification (prefer folding md_touched into the existing classify dorny step rather than a 4th dorny invocation — Architecture P3); pattern md_touched: ['**/*.md'] (single pattern is quantifier-invariant under the existing predicate-quantifier: 'every'). Gate the expensive step behind needs.changes.outputs.md_touched != 'false' (fail-safe). Job condition if: always() && !cancelled(); SHA-pin actions/setup-node to a full 40-char SHA (repo convention, F013); quote node-version. No trigger-level path filters (do NOT add on.pull_request.paths). Keep permissions least-privilege (contents: read). Ships advisory-first (not a required check yet). Depends on U1 (126.001-T), U2 (126.002-T), U3 (126.003-T).