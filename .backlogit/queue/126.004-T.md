---
artifact_type: task
created_at: 2026-07-25T22:34:10.9755526Z
dependencies:
    - 126.001-T
    - 126.002-T
    - 126.003-T
id: 126.004-T
labels:
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U4: Add always-reporting md-lint job + md_touched classifier to ci.yml'
updated_at: 2026-07-25T22:34:10.9755526Z
---

Domain: CI/config. Add a 'md-lint' job to .github/workflows/ci.yml that runs markdownlint-cli2 (via make md-lint or npx) over the U1 scope. Wire md-file detection by adding a md_touched output to the existing changes/paths-filter classification (prefer folding md_touched into the existing classify dorny step rather than a 4th dorny invocation — Architecture P3); pattern md_touched must match markdown AND the gate's own control files so the job cannot fail open: use a SINGLE brace-alternation glob (ONE pattern — quantifier-invariant under the existing predicate-quantifier: 'every') covering '**/*.md', .markdownlint.json, .markdownlint.jsonc, .markdownlint-cli2.jsonc, .markdownlintignore, Makefile, make.ps1, and .github/workflows/ci.yml (the md-lint job's own file). Do NOT express this as a multi-pattern positive list: under predicate-quantifier: 'every' multiple disjoint positive patterns are constant-false (no single file matches all patterns) = silent fail-open (the 089-S trap; see docs/compound/github-actions/dorny-paths-filter-every-quantifier-semantics-2026-07-04.md). Gate the expensive step behind needs.changes.outputs.md_touched != 'false' (fail-safe). AC: a PR that changes ONLY .markdownlint.json / .markdownlintignore, ONLY the Makefile/make.ps1, or ONLY ci.yml (no .md file) MUST still run the md-lint job (md_touched != 'false') and exercise the gate. Job condition if: always() && !cancelled(); SHA-pin actions/setup-node to a full 40-char SHA (repo convention, F013); quote node-version. No trigger-level path filters (do NOT add on.pull_request.paths). Keep permissions least-privilege (contents: read). Ships advisory-first (not a required check yet). Depends on U1 (126.001-T), U2 (126.002-T), U3 (126.003-T).