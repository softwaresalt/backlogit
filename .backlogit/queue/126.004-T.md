---
artifact_type: task
created_at: 2026-07-25T22:34:10.9755526Z
dependencies:
    - 126.001-T
    - 126.002-T
    - 126.003-T
id: 126.004-T
labels:
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U4: Add repo-wide blocking md-lint job (SHA-pinned) to ci.yml'
updated_at: 2026-07-25T22:34:10.9755526Z
---

Domain: CI/config. Add a standalone `md-lint` job to `.github/workflows/ci.yml` that runs the P-008 gate REPO-WIDE and BLOCKING. The job has NO `needs`/`if` gating and NO trigger-level `paths:`/`paths-ignore:` — it runs on every PR and always reports, so it is eligible as a required status check. Steps: SHA-pinned `actions/checkout` (with `persist-credentials: false`) + SHA-pinned `actions/setup-node` (resolve the v-tag to a full 40-char SHA per F013; set `node-version: "20"` QUOTED to avoid the YAML int-vs-string trap) + `run: make md-lint`. Keep `permissions: contents: read` (least privilege). Drop ALL Option-B scoped machinery: NO `md_touched` output on the `changes` job, NO brace-alternation path glob, NO per-directory rollout list, NO advisory-first posture. The repo is empirically 0-violation repo-wide (U1 config + the ~21 remediations), so the gate is green from its first run and is blocking immediately — no advisory cycle. Note: `.github/workflows/ci.yml` is in the `cli_reference` paths filter, so editing it re-runs the CLI-drift job (harmless, no CLI docs change). Depends on U1 (126.001-T), U2 (126.002-T), U3 (126.003-T).