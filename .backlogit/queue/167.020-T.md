---
artifact_type: task
created_at: 2026-09-06T08:13:15.1295681Z
dependencies:
    - 167.017-T
    - 167.019-T
    - 167.016-T
    - 167.021-T
id: 167.020-T
parent_id: 167-F
priority: critical
status: queued
title: Reconcile-scoped concurrency proof harness
updated_at: 2026-09-06T18:54:20.806628Z
---

Reconcile-scoped concurrency HARNESS + proof. Delivers a RED concurrency test HARNESS that COMPILES against the 167.003 panic-body declaration of ReconcileShipmentToShipped and is therefore RED until 167.008 implements the transaction - its GREEN verification lands WITH 167.008 (this task delivers the compiling, failing harness + the full test matrix; harness-first per workflow policy). Proves the reconcile transaction (167.008) is deadlock-free and clobber-free against the co-acquirers it actually contends with on the SAME shipment item: concurrent AssociateCommit and ArchiveItem (and ShipShipment / LinkCommit / SetArtifactSizeWithProvenance on that item) racing reconcile A->C->B acquisition. Assertions: (1) no unbreakable deadlock (A and B are BOUNDED-wait, task_lock.go:96/111-127 -> ErrGateInProgress after 3s; only the in-process C mutex is indefinite, stream.go:125, and reconcile holds exactly ONE C-mutex acquired first, so it is never a node in a C-mutex wait cycle); (2) no clobber under stable lock identity (167.017) + reload/CAS (167.019) + snapshot/rollback (167.016); (3) same-key/different-request-identity replay, rollback-on-ErrWriteNotApplied, two-phase TOCTOU member-set re-route. NOTE: the existing cascade/adopt paths are INDEPENDENTLY SAFE and need no coverage here - ArchiveItem archives descendants BEFORE taking the parent C-lock (archive.go:119-124 run before line 292, so parent-C and child-C are never held together) and lockAdoptionEventLogs SORTS ids before acquisition (shipment_lifecycle.go:1332-1333); there is NO cross-item C-mutex cycle to prove. Single domain (concurrency tests). Domain: tests. Deps 167.016 + 167.017 + 167.019 + 167.021. Blocks 167.008 (harness-first).