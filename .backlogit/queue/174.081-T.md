---
artifact_type: task
created_at: 2026-09-26T04:10:46.6440947Z
dependencies:
    - 174.078-T
id: 174.081-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - core-lifecycle
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: queued
title: Treat commit-journal failure after committed evidence as indeterminate
updated_at: 2026-09-26T04:13:53.4156976Z
---

Wave 20 work unit U20C5 (unit token U20C5, P-002.6 prefix ^TestU20C5_) for the 155-S final-review remediation. Finding ADV-A (adversarial, confirmed in scope under P-021 C1/C3): committed evidence is appended before the journal records the committed phase; a journal write failure then compensates, allowing contradictory committed and compensated terminal events.

Fix (commit tails of BlockShipment and UnblockShipment in internal/core/shipment.go): if the journal committed write fails after the terminal committed event is durable, do NOT call compensate. Return an error whose %w chain carries blerrors.ErrWriteIndeterminate only, rendering the journal cause with %v and keeping the existing labels verbatim: persist block shipment commit and persist unblock shipment commit. The journal stays intent; recovery later finalizes it as committed through the existing terminal-evidence branch. The returned artifact stays nil (existing error signature; agents re-read state).

Domain: core (Go). Class: NORMAL harness-required. Files: implementation 1 (internal/core/shipment.go: BlockShipment and UnblockShipment commit tails); harness 1 (internal/core/shipment_commit_journal_u20c5_harness_test.go, package core, no t.Parallel). Wave W3. Depends on 174.078-T (RED is defined on the U20C2-green tree; reuses the U20C3 obstruction helper by name).

Authoritative contract: plan Wave 20 sections 20.3.5, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=15m -v -run '^(TestUR1B_|TestUR3_|TestUR10_|TestP021|TestU20C2_|TestU20C3_)' ./internal/core",
    "go test -count=1 -timeout=10m -v -run '^TestUR8_' ./internal/cli"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly one top-level function TestU20C5_CommitJournalFailureAfterCommittedEvidenceDoesNotCompensate in internal/core/shipment_commit_journal_u20c5_harness_test.go, table-driven over op in {block, unblock (target queued)} (TG-1: one scenario); no other function in the repository matches ^TestU20C5_.
  - Injection: the operation's final governed artifact write goes through the real writer, then shipmentOpsRoot(root) is obstructed inside the same seam call using the U20C3 obstruction helper BY NAME (never copied). For block the shipment write is last; for unblock to queued the fixture snapshot needs no member restore write, otherwise arm on the last member write.
  - After the failed call: returned artifact nil; IsWriteIndeterminate true and message contains the op label (persist block shipment commit / persist unblock shipment commit); errors.Is false for ErrValidation, ErrShipmentConflict, ErrWriteNotApplied, ErrNotFound; errors.As into *blerrors.MutationPartialError false; no correlated compensated lifecycle event and no compensated status event; shipment in the committed target state (for block, the envelope is canonical).
  - After removing the obstruction: recovery NoError and journal committed; Doctor clean, no conflict; for block validatePersistedBlockedShipmentEnvelope NoError.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C5_' ./internal/core -> exactly 1 top-level PASS when GREEN.
  - RED on the U20C2-green tree (after 174.078-T is GREEN): compensate runs and appends a contradictory terminal compensated event. Compile check go test -run '^$' -count=1 ./... passes; '[no tests to run]' or 'no test files' is a halt. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready.
AC2 (Fix): a commit-journal failure after durable committed evidence never compensates; ErrWriteIndeterminate-only %w chain; labels verbatim; journal left intent for recovery.
AC3 (Regression, exact counts, fail closed): command 1 exactly 15 top-level PASS (TestUR1B_ 3 + TestUR3_ 5 + TestUR10_ 1 + TestP021 3 + TestU20C2_ 1 + TestU20C3_ 2); command 2 exactly 3 (TestUR8_). Zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC4 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/core/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/core/shipment.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
AC5 (W3 pre-push gate, plan 20.4, after W3 convergence): ^TestU20C[12356]_ ./internal/core exactly 8; ^TestU20C4_ ./internal/mcp exactly 2; regression union core exactly 41, cli (TestRegistryParity_|TestUR8_) exactly 13, mcp diagnostic exactly 1; git status --porcelain shows no modified or untracked path under internal/** or cmd/**. Only then push once.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope.
SHAs: one harness commit per task (HARNESS_COMMIT, staged by exact path); HARNESS_BASE_SHA = its parent; START_SHA = git rev-parse HEAD at claim. Captures under logs/diagnostics/174081-*, never staged. H10 t.Parallel audit before scaffolding.
Wave W3 = {174.081-T}. Edge: 174.081-T depends on 174.078-T. Wave convergence records FULL_SUITE_OPERATOR_DEFERRED: wave 3. No go test ./... with tests selected (the 3a240fe0 authorization is consumed).
Push: Wave 20 harness commits are designed-RED; Ship pushes ONCE only after W3 convergence and the W3 pre-push gate are GREEN, then runs the final gate (plan 20.5): stash get FA6AE139, 9900D0DD, 09D06A75, 8AF55264, 7D8717B1 (halt to Stage if any missing); request one NEW operator authorization for a single governed go test -timeout=30m ./...; one standard + one adversarial review with the five captures pre-dispositioned; a second NOT READY halts to the operator.
Size: 2 files, 2 production declarations. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage.
Documented deviations: EW-1, QG-1, QG-2, TG-1, TG-2. Related deferred captures (do not implement): 9900D0DD (nil artifact on indeterminate return).
<!-- END:implementation-notes -->