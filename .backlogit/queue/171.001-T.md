---
artifact_type: task
created_at: 2026-09-07T00:09:08.5296098Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 171.001-T
parent_id: 171-F
priority: high
references:
    - docs/exec-plans/2026-09-06-checkpoint-create-schema-reject-plan.md
status: queued
title: Source-shape AST harness + declare create-time opt-in and two sentinels
updated_at: 2026-09-07T00:09:08.5296098Z
---

U1 (declaration task; harness-first ordering per workflow-policies.md:123-143 and
harness-architect/SKILL.md:221-234). Land the create-time vocabulary ONLY — NO behavior. A
source-shape (go/ast) harness lands FIRST and gates the declaration; NO production stub is
written; the declaration lands second and turns the harness green. The semantics-matrix
BEHAVIOR harness is NOT in this task — it is owned by 171.002-T in a strictly later wave.

Declarations to land: a CreateCheckpointOption type + WithAllowLegacyImport() constructor in a
new internal/events/checkpoint_create_options.go, AND the SIGNATURE-ONLY widening of
CreateCheckpoint in internal/events/memory.go to CreateCheckpoint(ctx,dir,dump,opts
...CreateCheckpointOption) — declaration only, body/behavior UNCHANGED (no opts branch here;
that is 171.002-T), not a stub (Go has no overloading, so the signature must live in memory.go;
3-arg callers compile unchanged; zero-option call is strict-by-default). TWO surface-neutral
sentinels in internal/errors/checkpoint_errors.go: ErrCheckpointSchemaRejected (legacy-eligible;
surfaces append the --allow-legacy-import/allow_legacy_import remediation) and
ErrCheckpointSchemaUnsupported (unsupported/future/wrong-typed/ambiguous/non-upgradable;
rejected on BOTH paths; NO opt-in remediation). Two-%w discriminator when a cause is preserved.
Reuse the existing malformed-input error for malformed JSON.

Source-shape harness (internal/events/checkpoint_create_shape_test.go): a go/ast test that
parses checkpoint_create_options.go, memory.go, and checkpoint_errors.go and asserts the shapes
are present (CreateCheckpointOption type, WithAllowLegacyImport constructor, the variadic
CreateCheckpoint signature parsed from memory.go, both sentinel identifiers). Shape only — no
behavior, no file I/O — so it gates the signature U1 actually lands.

Files (3 small declaration files): internal/errors/checkpoint_errors.go,
internal/events/checkpoint_create_options.go (new), internal/events/memory.go (signature-only;
U2 owns the body). memory.go touch is trivial; unit stays within the 2-hour envelope.

Acceptance criteria: source-shape harness is red before the declaration and green after; NO
production stub precedes the harness; no behavior test in this task; `go build ./...` and
`go vet ./internal/events/...` pass.