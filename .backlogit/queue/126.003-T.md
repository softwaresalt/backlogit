---
artifact_type: task
created_at: 2026-07-25T22:33:21.7229115Z
id: 126.003-T
labels:
    - tests
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U3: Add md-lint config + repo-wide job characterization guards (markdownlint_gate_test.go)'
updated_at: 2026-07-25T22:33:21.7229115Z
---

Domain: tests. Add characterization guards for the provisioned gate in `tests/integration/markdownlint_gate_test.go` (reuse the existing `readCIWorkflow`/`findRepoRoot`/`workflowPaths` helpers; keep the test hermetic — do NOT shell out to Node). (1) Config invariant (the crux standing guard): assert `.markdownlint.json` enables EXACTLY `{ default:false, MD001:true, MD025:{ front_matter_title:"^\s*_title\s*[:=]" }, MD041:true }` — the assertion MUST fail if `default` is not false, if any of MD001/MD025/MD041 is missing, if any additional rule ID is enabled, if MD025 is not the `_title` object (guards against a revert to the default `title:` match that would silently re-break all 229 files), or if MD041 is not plain `true` (guards the MD041 default-options guardrail); and assert the runner config `.markdownlint-cli2.jsonc` carries only scope/runner options (e.g. `gitignore:true`) and NO rule-altering keys (`config`, `customRules`, `overrides`, `default`, or bare `MD###`), since markdownlint-cli2 MERGES it over `.markdownlint.json` — a rule could otherwise be silently enabled/overridden there while the `.markdownlint.json` checks still pass (proves the EFFECTIVE merged rule set is exactly MD001/MD025/MD041). (2) Repo-wide job: assert `ci.yml` defines a `md-lint` job, that the `changes` job exposes NO `md_touched` output (the scoped classifier is gone — the gate is repo-wide), that the job pins `actions/setup-node` to a full 40-char SHA (guards step PRESENCE, since TestAllActionsUseSHAPins only checks SHA format on existing steps), and that it runs `make md-lint`. The empirical 0-violation check itself is enforced at CI runtime by the repo-wide `md-lint` job (U4). GREEN once U1/U4 land. Do NOT assert an advisory posture or a required branch-protection context (branch protection is external).