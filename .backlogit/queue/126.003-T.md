---
artifact_type: task
created_at: 2026-07-25T22:33:21.7229115Z
id: 126.003-T
labels:
    - tests
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U3: Extend ci_compliance_test.go with md-lint job assertions (test-first RED->GREEN)'
updated_at: 2026-07-25T22:33:21.7229115Z
---

Domain: tests (test-first). Extend tests/integration/ci_compliance_test.go with t.Run subtests asserting: ci.yml defines a 'md-lint' job with name set and if: always() && !cancelled(); the changes job exposes a md_touched output consumed via needs.changes.outputs.md_touched != 'false'; the md_touched filter is a SINGLE brace-alternation glob (one pattern, quantifier-invariant under the existing predicate-quantifier: 'every') whose alternatives include '**/*.md' AND the actual markdownlint config filenames (.markdownlint.json, .markdownlint-cli2.jsonc) AND Makefile AND make.ps1 AND the workflow file .github/workflows/ci.yml, so a PR that changes ONLY the config or the Makefile (no .md) still triggers the md-lint job (fail-open guard, F4); assert md_touched is NOT a multi-pattern positive list (multiple disjoint positive patterns are constant-false under 'every' = silent fail-open, the 089-S trap); no trigger-level paths:/paths-ignore: is introduced. Add a DEDICATED assertion that resolves wf.Jobs[md-lint], locates its actions/setup-node step, and asserts a 40-char SHA — genuinely RED today (job absent) and guards step PRESENCE, since TestAllActionsUseSHAPins only validates SHA format on existing steps (plan-review Go P2). Reuse readCIWorkflow; add a findSetupNodeStep-style helper (none exists). Avoid asserting an unquoted node-version scalar (YAML int-vs-string trap, F013). REQUIRED (not optional): assert the .markdownlint.json rule set is exactly { default:false, MD001:true, MD025:true, MD041:true } — the assertion MUST fail if default is not false, if any of MD001/MD025/MD041 is missing, OR if any additional rule ID is enabled (catches BOTH accidental extra defaults AND missing/renamed rules). This is a mandatory standing guard for the plan's headline exactly-three-rules invariant (Constitution P2), not budget-optional. Do NOT assert md-lint as a required branch-protection context (ships advisory-first). Independent unit (RED naturally); U4 turns it GREEN.