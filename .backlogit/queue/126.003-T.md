---
artifact_type: task
created_at: 2026-07-25T22:33:21.7229115Z
id: 126.003-T
labels:
    - tests
    - ci
    - markdownlint
parent_id: 126-F
priority: medium
status: queued
title: 'U3: Extend ci_compliance_test.go with md-lint job assertions (test-first RED->GREEN)'
updated_at: 2026-07-25T22:33:21.7229115Z
---

Domain: tests (test-first). Extend tests/integration/ci_compliance_test.go with t.Run subtests asserting: ci.yml defines a 'md-lint' job with name set and if: always() && !cancelled(); the changes job exposes a md_touched output consumed via needs.changes.outputs.md_touched != 'false'; no trigger-level paths:/paths-ignore: is introduced. Add a DEDICATED assertion that resolves wf.Jobs[md-lint], locates its actions/setup-node step, and asserts a 40-char SHA — genuinely RED today (job absent) and guards step PRESENCE, since TestAllActionsUseSHAPins only validates SHA format on existing steps (plan-review Go P2). Reuse readCIWorkflow; add a findSetupNodeStep-style helper (none exists). Avoid asserting an unquoted node-version scalar (YAML int-vs-string trap, F013). Optionally assert the .markdownlint.json rule set (exactly MD001/MD025/MD041, default:false) as a standing invariant guard (Constitution P2) if it stays within the scenario budget. Do NOT assert md-lint as a required branch-protection context (ships advisory-first). Independent unit (RED naturally); U4 turns it GREEN.