---
artifact_type: task
created_at: 2026-09-07T03:16:37.2512736Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 171.004-T
    - 171.003-T
id: 171.006-T
parent_id: 171-F
priority: high
references:
    - docs/exec-plans/2026-09-06-checkpoint-create-schema-reject-plan.md
status: queued
title: MCP tool opt-in param, schema/description, registry and parity fixtures
updated_at: 2026-09-07T03:16:37.2512736Z
---

U6. Expose the opt-in on the MCP surface consistently (depends on 171.004-T's sentinel→MCP
error mapping). Add an optional allow_legacy_import boolean parameter to the
backlogit_create_checkpoint tool schema; parse it in handleCreateCheckpoint
(internal/mcp/tools.go) and pass WithAllowLegacyImport() into core. Update the tool
description: (a) 'default changed: non-V1 dumps now rejected', (b) compact opt-in scope
(accepts only legacy shapes in the Upgrade window — absent or integer-literal 0 schema_version
that is otherwise V1-valid; future/unsupported/wrong-typed/malformed and records needing
remap/identity remain rejected; import upgrades to valid V1, never verbatim), (c) drop 'written
verbatim with no schema validation'. Advertise the new parameter in the governed operation
registry (.autoharness/backlog-registry.yaml — the create_checkpoint entry currently lists only
state_dump + a CLI command without the opt-in) and update the existing governed parity
fixtures / tool-metadata so generated metadata and registry-driven parity tests expose the new
parameter on both surfaces.

Files (≤2 production + config): internal/mcp/tools.go, .autoharness/backlog-registry.yaml;
governed parity fixtures / tool-metadata (tests/contract/checkpoint_tools_test.go or
internal/mcp/checkpoint_create_strict_test.go) plus the existing governed-parity fixture.

Acceptance criteria: MCP create without the param rejects a schema-less dump, writes no file,
and surfaces the bounded U4-mapped error code (parity on error CLASS, not just accept/reject);
with allow_legacy_import:true an in-window dump is imported and readable; an out-of-window
legacy dump fails as ErrCheckpointSchemaUnsupported; default false. Registry-driven parity
fixtures dispatch the REGISTERED handler (callToolForTest), separate t.TempDir() per surface;
generated tool metadata includes allow_legacy_import. `go test ./internal/mcp/... ./tests/contract/...` green.