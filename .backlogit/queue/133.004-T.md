---
artifact_type: task
created_at: 2026-08-01T04:57:15.3560717Z
dependencies:
    - 133.002-T
    - 133.003-T
id: 133.004-T
labels:
    - code
    - ship
    - cascade
    - core
parent_id: 133-F
priority: medium
references:
    - docs/exec-plans/2026-07-31-shipshipment-partial-feature-archive-cascade-plan.md
status: queued
title: Neutralize all three covering-feature seams on manifest membership
updated_at: 2026-08-01T04:57:15.3560717Z
---

Domain: code (plan Unit 2). Execution posture: test-first (make Unit 1a + Unit 1b green). In internal/core/shipment_lifecycle.go, neutralize all THREE covering-feature mutation seams using the explicit manifest membership set (explicitScope = NormalizeShipmentItems, resolved at :158 before any mutation):
1. featureIDs loop (:191): only call setArtifactStatus(featureID, StatusDone, "feature released") when featureID in explicitScope; else leave pre-ship status + queue location untouched.
2. collectArchiveCandidateIDs (:292): gate the ENTIRE per-feature block (feature append, linkedDeliberationIDs append, descendant sweep) on membership. Add explicitScope (a set) to the signature; update the single ShipShipment callsite. Shipped manifest items are still archived by the existing releaseScope-terminal branch at the top of the collector (unchanged).
3. Transitive parent-status rollup (the third seam): completeReleaseScope (:171/:254) marks shipped children done via setArtifactStatus, which unconditionally cascades (cascadePersistedParentStatuses :519 -> ComputeParentStatus harness_status.go:82) and rolls a covering feature to done + relocates it when all its RECORDED children are done. PRIMARY approach: snapshot each featureScopeRoots(explicitScope) feature's status + queue-file location at :158 (pre-mutation); after completeReleaseScope + the featureIDs loop, RESTORE any featureID NOT in explicitScope that the cascade moved to done/relocated. ALTERNATIVE: a cascade-suppression variant threading a non-member boundary set so the upward rollup stops at a non-member covering feature (touches cascadePersistedParentStatuses/setArtifactStatus — shared, higher blast radius). Requirement is invariant: a non-member covering feature must retain its pre-ship status + queue location.

Constraints: NO new terminal-status predicate; do NOT touch the five isTerminalReleaseStatus release-progression callsites or blocking_cascade.go; there is no exported core.TerminalStatuses (cascade set is unexported terminalCascadeStatuses via IsCascadeTerminalStatus/CascadeTerminalStatuses — unused here). Read authoritative status from Markdown via findArtifact (not the DB fast-path), skip already-archived items, wrap errors with %w, nil-guard findArtifact (ErrNotFound). Optional defense-in-depth: also require the per-feature returned slice from returnUnreleasedFeatureItems (in scope at :188) empty before closing a listed feature — reuses existing data, no new predicate.

## Acceptance Criteria
- All Unit 1a + Unit 1b tests pass; shipment_gate_* and all other existing tests stay green.
- Children-only ship: covering feature is neither set done nor archived and remains in .backlogit/queue/; its linked deliberation is not archived. Feature-inclusive ship: feature set done and archived.
- Body-planned children-only ship: feature left at pre-ship status (not done) and in .backlogit/queue/ even though completeReleaseScope marked all its recorded children done (transitive rollup neutralized for the non-member feature).
- collectArchiveCandidateIDs receives explicit membership; no covering-feature disposition inferred from post-detach parent_id.
- No change to the five isTerminalReleaseStatus callsites or blocking_cascade.go. gofmt/go vet/golangci-lint clean (Ship gates).