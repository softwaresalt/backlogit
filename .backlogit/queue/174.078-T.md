---
artifact_type: task
created_at: 2026-09-26T04:08:47.9458229Z
dependencies:
    - 174.079-T
id: 174.078-T
labels:
    - wave-20
    - 155-S
    - review-remediation
    - harness-required
    - core-lifecycle
parent_id: 174-F
priority: high
references:
    - docs/exec-plans/2026-09-14-resumable-shipment-blocked-lifecycle-plan.md
    - docs/closure/2026-09-25-155-S-final-review.md
status: queued
title: Record unblock reason before clearing blocked envelope
updated_at: 2026-09-26T04:12:36.9244649Z
---

Wave 20 work unit U20C2 (unit token U20C2, P-002.6 prefix ^TestU20C2_) for the 155-S final-review remediation. Finding STD-P2-02 (in scope under P-021 C1/C3): the unblock event delta omits the validated prior reason, and the blocked metadata is cleared before the correlated status event is appended.

Fix (UnblockShipment in internal/core/shipment.go): set eventDelta["reason"] = envelope.reason; append the correlated shipment_status_changed event (phase applied, target and status = the unblock target, actor = opts.UnblockedBy) BEFORE the unblocked-shipment persist that deletes blocked_reason/blocked_at/blocked_by. Member restores, the terminal committed event and the journal commit keep their order after that persist. Add a guard comment: for this rollback-policy operation, applied evidence is provisional until the terminal event.

Domain: core (Go). Class: NORMAL harness-required. Files: implementation 1 (internal/core/shipment.go: UnblockShipment); harness 1 (internal/core/shipment_unblock_audit_u20c2_harness_test.go, package core, no t.Parallel). Wave W2. Depends on 174.079-T (U20C3; RED is defined on the U20C3-green tree). Ports the parked W0 draft C2 portion.

Authoritative contract: plan Wave 20 sections 20.3.2, 20.4 and 20.5 (rev23.5; attempt-4 ADVISORY approved by operator softwaresalt 2026-09-26T04:05Z) plus this task's green-regression-contract block and acceptance criteria.

<!-- BEGIN:green-regression-contract -->

```json
{
  "green_regression_cmds": [
    "go test -count=1 -timeout=15m -v -run '^(TestUR1B_|TestUR3_|TestUR10_|TestU20C3_)' ./internal/core",
    "go test -count=1 -timeout=10m -v -run '^TestUR8_' ./internal/cli"
  ]
}
```

<!-- END:green-regression-contract -->

<!-- BEGIN:acceptance-criteria -->
AC1 (Harness, test-first; P-002/P-004/P-002.6): exactly one top-level function TestU20C2_UnblockStatusEvidencePrecedesEnvelopeClear in internal/core/shipment_unblock_audit_u20c2_harness_test.go, table-driven over target in {queued, active} (TG-1: one scenario); no other function in the repository matches ^TestU20C2_.
  - Setup: canonically blocked shipment with reason R, BlockedBy, ResumeCheckpointRef C.
  - Observer: after BlockShipment returns, install an observer on an existing seam (persistArtifactWriteFn or persistArtifactPreLockHook); save the prior hook value and restore that saved value in t.Cleanup (never set to nil). At the FIRST shipment write inside UnblockShipment it records (a) whether the on-disk shipment still has blocked_reason == R, and (b) whether the shipment log already holds exactly one correlated shipment_status_changed event with phase applied, target = unblock target, delta.reason == R, delta.unblocked_by == actor, delta.resume_checkpoint_ref == C, event.Actor == actor.
  - Assertions: (a) and (b) true; afterwards status = target and no blocked_* keys; journal committed with exactly one correlated applied status event and one terminal committed event.
  - harness_cmd: go test -count=1 -timeout=10m -v -run '^TestU20C2_' ./internal/core -> exactly 1 top-level PASS when GREEN.
  - RED on the U20C3-green tree (after 174.079-T is GREEN): at the first write (b) is false and reason is absent. Compile check go test -run '^$' -count=1 ./... passes. '[no tests to run]' or 'no test files' is a halt, not RED. Record 'Compilation: PASS / Red Phase: CONFIRMED (scoped)' before harness-ready.
AC2 (Fix): the unblock delta carries the validated prior reason; the correlated applied status event is durable before the envelope-clearing persist; the remaining order (member restores, terminal committed, journal commit) is unchanged.
AC3 (Regression, exact counts, fail closed): command 1 reports exactly 11 top-level PASS (TestUR1B_ 3 + TestUR3_ 5 + TestUR10_ 1 + TestU20C3_ 2); command 2 reports exactly 3 (TestUR8_). Zero top-level FAIL/SKIP. A differing count at claim is recorded and halts to Stage.
AC4 (Gates, plan 20.4): go vet ./...; pinned golangci-lint@v1.64.8 --new-from-rev=HARNESS_BASE_SHA ./internal/core/... with 0 new findings in task files; go build -o logs/diagnostics/backlogit-wave20.exe ./cmd/backlogit; git show HEAD:<file> | gofmt -l empty (via cmd /c); git diff --check HARNESS_BASE_SHA HEAD -- <task files> clean; HARNESS_COMMIT lists exactly the harness file; unfiltered git diff --name-only START_SHA HEAD lists exactly internal/core/shipment.go plus allowed .backlogit/**, docs/memory/**, logs/** metadata.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Posture: test-first. Safety modes: investigate-first, freeze-scope.
Draft port (plan 20.3.2/20.5, Ship via the harness-architect skill): when scaffolding this W2 harness, first re-check both W0 SHA-256 values (wave20-draft-fold.patch and wave20-draft-u20c2.go.txt); a mismatch halts to the operator. Then port TestUnblockShipmentStatusEventPrecedesEnvelopeClear from logs/diagnostics/wave20-draft-u20c2.go.txt as the single top-level function: rename it; replace the copy shadow with a non-builtin name; record at the FIRST shipment write; add the target table, applied-phase check and journal/terminal assertions; save and restore the prior persistArtifactPreLockHook. The C2 portion lands only in this task's HARNESS_COMMIT; the parking file stays git-ignored and is never committed; the count stays ^TestU20C2_ = 1. Write files with the editor tool or UTF-8 without BOM.
SHAs: one harness commit per task (HARNESS_COMMIT, staged by exact path); HARNESS_BASE_SHA = its parent; START_SHA = git rev-parse HEAD at claim. Captures under logs/diagnostics/174078-*, never staged. H10 t.Parallel audit before scaffolding.
Wave W2 = {174.078-T, 174.080-T}. Edge: 174.078-T depends on 174.079-T. 174.081-T depends on this task. Wave convergence records FULL_SUITE_OPERATOR_DEFERRED: wave 2. A tree with U20C2 GREEN is not shippable without U20C5; no push until the W3 pre-push gate is GREEN. No go test ./... with tests selected.
Size: 2 files, 1 production declaration. Exceeding the 2-hour rule or declared counts STOPs and returns to Stage.
Documented deviations: EW-1, QG-1, QG-2, TG-1, TG-2. Related deferred captures (do not implement): 9900D0DD.
<!-- END:implementation-notes -->