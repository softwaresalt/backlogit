---
artifact_type: task
created_at: 2026-09-14T03:16:00Z
dependencies:
    - 172.011-T
id: 172.012-T
parent_id: 172-F
priority: high
references:
    - docs/exec-plans/2026-09-13-concurrency-writer-hardening-plan.md
status: queued
title: Caller compatibility/adaptation for additive BulkUpdateResult typed detail
updated_at: 2026-09-14T03:16:00Z
---

U2.caller [1E0C2251] caller compatibility/adaptation (predecessor to behavior impl). Enumerate every caller of BulkUpdateStatus and every consumer of BulkUpdateResult; confirm each remains source- and behavior-compatible with the PRESERVED `Failed []string` field (no caller may depend on it being removed or retyped), and adapt the callers that should consume the NEW additive typed detail surface (per the 172.011-T declaration) to read `FailedDetails` WITHOUT changing the `Failed []string` contract. This adaptation lands BEFORE the behavior implementation (172.006-T) and GREEN testing (172.010-T) so the additive typed surface has confirmed, adapted consumers rather than an orphaned field. Domain: code. Depends on 172.011-T (source-shape declaration). AC: all existing BulkUpdateResult consumers keep compiling and behaving against `Failed []string` (backward-compatible); callers that need the typed detail are adapted to read the additive field; no caller depends on `Failed []string` being removed or retyped.
