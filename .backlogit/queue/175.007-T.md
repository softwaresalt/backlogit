---
artifact_type: task
created_at: 2026-09-17T23:59:42.6391949Z
dependencies:
    - 175.001-T
    - 175.011-T
id: 175.007-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'U7 - errcheck remediation: internal/stash'
updated_at: 2026-09-18T05:02:36.7279799Z
---

Remediate ALL golangci-lint errcheck findings in internal/stash using the per-category strategy: named-return + errors.Join for a writable Close (defer func() { err = errors.Join(err, f.Close()) }()); the checked write/short-write helper for Write paths; justified `_ =` only for genuinely discardable returns (with an inline reason); `fmt.Errorf("...: %w", err)` for propagation (two-%w form where a shared sentinel policy classifies via errors.Is). No opportunistic cross-package helper (introduce a shared helper only when >=3 identical call sites exist AND via an explicit predecessor sub-unit).

Owned harness file (resolves the P1-2 harness-ownership finding): this unit exclusively owns and authorizes creation of the deterministic errcheck-regression harness `internal/stash/errcheck_convergence_175_test.go`. The filename segment `errcheck_convergence_175_test.go` is RESERVED to this unit in package `internal/stash`; no other unit may create that exact path, and it never collides with U11's `internal/stash/staticcheck_convergence_175_test.go` in the same package. This `*_test.go` harness file is EXCLUDED from the <3 implementation-file inventory, which counts only the errcheck-flagged non-test files this unit edits and remains < 3.

Acceptance criteria:
- Scope (package/files): internal/stash only; edit only files within internal/stash that errcheck flags (<3 implementation files, width-isolated to Go code). Do NOT touch files owned by another unit (file-partition invariant). EXCLUDE every file listed in U11's owned handoff artifact `docs/closure/175-U11-overlap-inventory.md` (the fenced OVERLAP-INVENTORY payload) — U11 runs first (exactly-one-owner contract) and owns those files for ALL findings (staticcheck and errcheck); this unit depends on U11 (`175.011-T`) and U1 (`175.001-T`) and consumes that exact artifact BY PATH to exclude the listed files. If the artifact is absent at execution, treat it as a blocking U11-incomplete dependency violation, never as "no overlap". Ownership is decided by U11's up-front inventory, never by serialization order.
- Owned harness (required): the reserved `internal/stash/errcheck_convergence_175_test.go` RED-first regression drives any behavior-changing errcheck fix green; the harness path is exclusive to this unit and excluded from the implementation-file count.
- Verification (zero-finding): `golangci-lint run ./internal/stash` reports 0 errcheck; `gofmt -l` on touched files is empty; `go build ./internal/stash` and existing internal/stash tests still pass (no regression).
- Existing test expectations: all current internal/stash tests continue to pass unchanged; success-path behavior preserved (fixes add capture/propagation only on failure paths).
- Test posture (Principle II): NORMAL harness-required task — no harness exemption is claimed (this task carries no `harness-exempt` label and no exemption contract). The harness-architect authors a failing (RED) harness/test in the owned harness file that compiles before the fix and fails on an assertion (P-002/P-004), and implementation drives it green. For any errcheck fix introducing genuinely NEW reachable failure-path behavior, the RED test exercises the propagated error FIRST, then make it green. There is NO mechanical/generic/closure-time harness exemption, exempt contract, waiver, or Principle II deviation for this task; every remediated call site is gated by the required red harness.