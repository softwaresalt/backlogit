---
artifact_type: task
created_at: 2026-09-17T23:59:42.6391949Z
dependencies:
    - 175.001-T
id: 175.007-T
parent_id: 175-F
priority: medium
references:
    - docs/exec-plans/2026-09-17-baseline-convergence-plan.md
status: queued
title: 'Lint fix (errcheck): internal/cli/checkpoint.go'
updated_at: 2026-09-18T07:46:29.4111686Z
---

NORMAL harness-required, file-owned lint-remediation task. This task exclusively owns EXACTLY ONE flagged source file and modifies NO other implementation file (file-partition / exactly-one-owner invariant).

Linter: errcheck
Package: internal/cli
Owned source file (exactly one): internal/cli/checkpoint.go
Findings (2) from baseline-lint-inventory.json (golangci-lint v2.13.2, baseline HEAD c80d7c6b; durable evidence artifact: docs/decisions/2026-09-17-baseline-lint-inventory.md):
- L347: Error return value of `fmt.Fprintln` is not checked
- L348: Error return value of `fmt.Fprintln` is not checked
Affected functions (1 distinct, < 5): RenderCheckpointRemediationBlock
Test scenarios: 1 (the file-scoped red-before-green lint selector); < 4.

Owned harness file (exactly one, RESERVED to this task, uniquely named, non-colliding): internal/cli/errcheck_checkpoint_175_test.go
Ship's harness phase scaffolds this harness together with its admitted ready wave -- every non-exempt member of the same ready_k wave is scaffolded together in ONE pass (per _ship.agent.md Step 2 and the harness-architect skill), leaving them all red; the harnesses are NOT precommitted at Stage. It is a source-shape or command-backed test scoped to internal/cli/checkpoint.go ONLY: it asserts this file's errcheck / behavior contract without referencing any not-yet-existing symbol. Sibling task-scoped red within the SAME ready wave is EXPECTED and TOLERATED until wave convergence; this harness's own scoped selector must turn GREEN when this task's fix lands. It is EXCLUDED from the implementation-file inventory.

Harness test function (P-002.6): the owned harness declares a single task-scoped Go test function named `TestU175_007_ErrcheckCheckpoint` (deterministic `TestU175_007_` unit token; no other task uses this prefix). RED before the fix (the assertion fails; NOT a build error), GREEN after.
P-002.6 scoped verification command (EXECUTABLE non-vacuity wrapper; explicit package, anchored selector, verbose, fail-closed, fails on zero matched tests):
  pwsh -NoProfile -Command '$o = & go test "./internal/cli" -run "^TestU175_007_" -v -count=1 2>&1; $c = $LASTEXITCODE; $o | ForEach-Object { Write-Output $_ }; if ($c -ne 0) { exit $c }; $p = @($o | Where-Object { $_ -match "^--- PASS: TestU175_007_" }).Count; if ($p -lt 1) { Write-Output "NON-VACUITY-FAIL: zero named --- PASS: TestU175_007_ lines"; exit 1 }; exit 0'
Wrapper contract: runs the task's explicit package (`./internal/cli`) and anchored `^TestU175_007_` selector with `-v -count=1`; captures and re-emits the combined go test output; preserves and propagates the native `go test` nonzero exit; counts lines matching `^--- PASS: TestU175_007_` and EXITS NONZERO when that count is < 1 (so a zero-match/absent selector fails vacuously instead of passing). Before implementation the same command is RED via an assertion failure on `TestU175_007_ErrcheckCheckpoint`; after the green phase it exits 0 only when at least one named PASS line is observed.

Fix strategy (errcheck; constitution: returned errors MUST be handled; `_ =` / ignored-return-with-comment is NOT an allowed resolution): the flagged `fmt.Fprintf`/`fmt.Fprintln` returns are inside `RenderCheckpointRemediationBlock(w io.Writer, ...)`, whose signature is intentionally VOID and whose behavioral tests live in the U8-owned `internal/cli/checkpoint_remediation_test.go` (NOT editable by this task) — so the errcheck `(n int, err error)` returns CANNOT be propagated through the signature and the function contract MUST NOT change. Instead check each flagged `fmt.Fprint*` return and, on non-nil error, emit a diagnostic through the EXISTING package `slog` sink already imported (`log/slog`) and used in this file (e.g. `if _, err := fmt.Fprintln(w, ...); err != nil { slog.Warn("render checkpoint remediation block: write failed", "error", err) }`). The void signature, the rendered output, and all existing behavior are preserved; no error is discarded via `_`; no cross-file or cross-package helper and no signature change is introduced; the edit is confined to `internal/cli/checkpoint.go` plus this task's OWN deterministic harness. This task's owned harness asserts the write path (a failing `io.Writer` exercises the checked `slog` branch; a working writer still renders the expected block).

Bounds (verified from inventory + source enclosing-function map):
- Modified files <= 2 (one flagged source file + one owned harness file).
- Implementation files < 3 (exactly 1 flagged source file; owned harness excluded).
- Affected functions < 5 (1).
- Test scenarios < 4 (1).
- Human effort < 2 hours (single-file, mechanical/localized change).
Test-only-safe compile: verify the package's tests with go test ./internal/cli -run=^$ -count=1 (compiles _test.go without running the full suite), never go build (which skips _test.go).

Acceptance criteria:
- The P-002.6 executable wrapper command exits 0 AND (via its counted `--- PASS: TestU175_007_` guard) proves at least one named PASS (non-vacuous), and the file-scoped lint verification (FSLV) command below exits 0 (no owned errcheck finding remains in internal/cli/checkpoint.go).
- File-scoped lint verification (FSLV) command (structured JSON output; `--issues-exit-code 0` so lint findings never mask infrastructure failures; parses and fails ONLY on this task's owned path+linter finding set; any native golangci-lint config/execution failure is preserved and propagated, never masked):
  pwsh -NoProfile -Command '$t = New-TemporaryFile; & golangci-lint run --output.json.path $t.FullName --issues-exit-code 0 ./internal/cli; $c = $LASTEXITCODE; if ($c -ne 0) { Remove-Item $t.FullName -Force -ErrorAction SilentlyContinue; Write-Output ("LINT-NATIVE-FAIL:" + $c); exit $c }; $j = Get-Content $t.FullName -Raw | ConvertFrom-Json; Remove-Item $t.FullName -Force -ErrorAction SilentlyContinue; $hits = @($j.Issues | Where-Object { [string]::Equals($_.FromLinter, "errcheck", [System.StringComparison]::Ordinal) -and [string]::Equals(($_.Pos.Filename -replace "\\", "/"), "internal/cli/checkpoint.go", [System.StringComparison]::Ordinal) }); if ($hits.Count -gt 0) { exit 1 }; exit 0'
  FSLV contract: exit 0 = no owned errcheck finding remains in internal/cli/checkpoint.go (this task is INDEPENDENTLY COMPLETABLE while same-wave sibling findings stay red); exit 1 = an owned finding still remains; any OTHER nonzero exit is a preserved native golangci-lint config/execution failure. This does NOT weaken U12's terminal full `golangci-lint run`.
- gofmt -l on the modified file(s) is empty.
- Owned harness internal/cli/errcheck_checkpoint_175_test.go was RED (assertion on TestU175_007_ErrcheckCheckpoint) before the fix and GREEN after. Within the same ready wave, sibling task-scoped red is EXPECTED and TOLERATED; this task's own scoped selector must turn GREEN when its fix lands, and full-suite green is required only at Ship's Step 4.6 wave-convergence gate (when the wave's open-red set is empty). No later-wave harness is scaffolded early.
- Every flagged error return is HANDLED (checked propagation/wrapping, named-return errors.Join, or test failure); NO `_ =` / ignored-return resolution is used.
- ONLY internal/cli/checkpoint.go (plus its owned harness) is modified; no other implementation file is touched; no file owned by another lint task is edited.
- Depends on U1 (175.001-T) line-ending baseline as its sole predecessor.
- NORMAL harness-required: NO harness-exempt label, NO exemption contract block, NO generic/mechanical/closure-time exemption. There is NO U11 overlap-inventory dependency and NO dynamic scope subtraction: the staticcheck and errcheck file sets are proven DISJOINT by the baseline evidence.