---
artifact_type: task
created_at: 2026-08-25T16:55:32.105849Z
id: 147.032-T
labels:
    - checkpoint
    - events
    - plan-u1d
    - declaration
    - harness-exempt
parent_id: 147-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md
status: queued
title: 'U1d: structured non-executable remediation intent'
updated_at: 2026-08-25T16:55:32.105849Z
---

Plan unit **U1d** (partition 1). Declares the structured, non-executable remediation carrier that replaces the ambient-cwd command string.

Files: `internal/events/checkpoint_schema.go`, `internal/events/checkpoint_remediation_test.go` (new).

Created in PR #377 cycle 17 to close gate finding **H1** (P0). `internal/events` has no knowledge of the caller''s working directory, so any command string it emits is bound to an ambient cwd, carries no approval, preimage, or no-clobber obligation, and is paste-runnable by whoever reads it.

```go
type RemediationIntent struct {
    Verb             string `json:"verb"`              // "quarantine"
    TargetFilename   string `json:"target_filename"`   // bare filename, never a path
    RequiresApproval bool   `json:"requires_approval"` // always true
    ApprovalClass    string `json:"approval_class"`    // "A4c"
    Reason           string `json:"reason"`            // schema_invalid | non_conforming | unparseable
}
```

Add `RemediationIntent *RemediationIntent` with tag `json:"remediation_intent"` to `CheckpointSummary` (`internal/events/checkpoint_schema.go:371`). No `omitempty` on any field.

The shipped `CheckpointSummary.RemediationCommand` keeps its shipped meaning and its shipped parse-failure population. Mark it `// Deprecated: use RemediationIntent`. Do **not** extend it to any new branch. Removal is follow-up stash `F350503F`.

This task has no dependencies inside the plan; with 147.001-T it is one of the two ready roots.

**Test-lifecycle classification: `harness-exempt: declaration-only` (cycle-20).** This task''s whole delta is a struct declaration plus one tagged field on `CheckpointSummary`. Once the declared shape lands, every planned scenario passes immediately: a struct literal marshals its five tagged keys, a bare filename round-trips, and a nil pointer without `omitempty` marshals as `null`. There is no compilable intermediate state in which any of them fails on an assertion, so cycle 19''s claim that "cases 1 and 2 fail (the type does not exist until the declaration step)" described a **build error**, which P-004 explicitly does not accept as a red phase. The cycle-20 Copilot review raised this as a P1 and it is closed by withdrawing the fabricated RED rather than inventing behaviour to fail against.

**No harness test function is scaffolded for this task.** The three shape assertions land in this task''s own declaration commit as green-step guards named `TestU1dGuard_*`.

**Where the RED for this carrier lives**: `147.011-T` / U6 (the non-conforming branch) and `147.043-T` / U6e (the parse-failure and schema-invalid branches) each carry a failing harness for the behaviour that *populates* the carrier; `147.022-T` / U6c carries the projection RED and `147.039-T` / U16 the rendering RED. Test-first is preserved for every behaviour that consumes this declaration.

Verification: `go test -count=1 -run '^TestU1dGuard_' ./internal/events`.

<!-- BEGIN:acceptance-criteria -->
- A `RemediationIntent` marshals with all five keys present and no `omitempty` elision
- `TargetFilename` round-trips a bare filename unchanged and the struct contains no field holding shell text
- A `CheckpointSummary` with a nil intent marshals `remediation_intent: null` rather than omitting the key
- Red harness: none. This task is `harness-exempt: declaration-only`; the RED for every behaviour consuming this carrier lives in 147.011-T, 147.043-T, 147.022-T, and 147.039-T
- Green-step guards (`TestU1dGuard_*`): the three shape assertions above, landing in the declaration commit
<!-- END:acceptance-criteria -->