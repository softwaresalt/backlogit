# Shipment Reconcile Report — 060-S (mode: pre)

- **Shipment**: 060-S — Shipment State Integrity
- **Mode**: pre
- **Expected status**: active (shipment was claimed in a prior session; feature correctly active pending ship)
- **Timestamp**: 2026-06-27T17:07:40-07:00
- **Invoked by**: Ship Step 6 (post-merge closure), merge commit 7a51904bc159d0f16aa5a9d8866e0bd4c324717d
- **Lock**: held on .backlogit/queue/060-S.md (acquired before pre-mode)

## Manifest

`custom_fields.items` = [061-F, 061.002-T, 061.001-T] (3 items)

> Note: benign `NNN-S → (NNN+1)-F` ID offset (cosmetic, confirmed in
> `docs/decisions/2026-06-25-shipment-manifest-drift-determination-deliberation.md`).
> Shipment 060-S legitimately carries feature 061-F + tasks 061.001-T / 061.002-T.
> The separate shipment 061-S (carrying 062-F) is NOT part of this manifest and is untouched.

## Per-item classification

| Item | Location | Declared status | Classification |
|---|---|---|---|
| 061-F | .backlogit/queue/061-F.md | active | matched |
| 061.001-T | .backlogit/archive/061.001-T.md | done | pre-archived |
| 061.002-T | .backlogit/archive/061.002-T.md | done | pre-archived |

## Orphan scan

Scanned `.backlogit/queue/*.md` for frontmatter declaring `shipment_id: 060-S`.
Result: **0 orphan files**. Item files do not carry a `shipment_id` frontmatter field;
membership is tracked only in the shipment manifest. No queue file claims membership in
060-S outside the manifest.

## Status-rationale note

`expected_status: active` was chosen (the skill's documented variant for a shipment
already claimed in a prior session) rather than `done`, because the covering feature
061-F is legitimately `active` until `ShipShipment` runs. `internal/core/shipment_lifecycle.go:ShipShipment`
itself transitions the feature active → done and archives the release scope. The 2 tasks were
already archived (done) during the feature build session, so they classify as `pre-archived`
(valid) regardless of `expected_status`. This mirrors the immediately-prior 067-S closure pattern.

## Recommendation

**PROCEED** — all 3 manifest items are `matched` or `pre-archived`; no `missing`,
`status-mismatch`, or `orphan` items. Lock retained until post-mode completes.
