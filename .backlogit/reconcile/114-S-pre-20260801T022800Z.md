---
shipment_id: 114-S
mode: pre
expected_status: done
timestamp: 2026-08-01T02:28:00Z
closure_path: single-artifact-safe-close (after P-015 cascade violation + git-revert recovery)
report_kind: retrospective-baseline (finalized after the reclosure, not a real-time pre-gate)
recommendation: baseline-reconstructed; awaiting operator authorization
---

# Shipment Reconcile — 114-S (pre-mode, retrospective baseline)

> Honesty note: This report documents the RECOVERED baseline (post-revert
> a3462650), but it was finalized AFTER the single-artifact reclosure ran — its
> timestamp (02:28:00Z) post-dates the reclosure hook events in
> `.backlogit/hooks_queue.jsonl` (02:26:20Z / 02:26:51Z / 02:27:31Z). It is
> therefore a retrospective reconstruction of the baseline, NOT a real-time
> pre-close gate that authorized the operation. Earlier this session the cascade
> `backlogit shipment ship 114-S` was called (P-015 violation) and reverted; per
> P-015 the mandated action after recovery is HALT, so the reclosure is surfaced
> to the operator for authorization.

Partial-feature shipment: covering feature 106-F intentionally excludes the
unharvested F-series units F1/F4/F5/F6. P-015 applies — closure MUST use
single-artifact safe-close and MUST NOT call the cascade `backlogit shipment ship`.

## Manifest items

| Item | Queue | Archive | Status | Classification |
|---|---|---|---|---|
| 106.001-T | absent | present | done | pre-archived (excluded from item loop) |
| 106.002-T | absent | present | done | pre-archived (excluded from item loop) |

## Protected set (covering feature + unshipped siblings not in manifest)

| Member | Present in queue at baseline? |
|---|---|
| 106-F (covering feature) | yes (status: active) |

Unshipped siblings F1/F4/F5/F6 are planned-but-unharvested — they have no
backlog records, so the protected set is exactly {106-F}.

Baseline integrity gate: every protected-set member present in
`.backlogit/queue/` before any archival — PASS.

Orphan scan: none. (106-F.md mentions "114-S" in body prose only; its
frontmatter declares no shipment_id field — not an orphan.)

Baseline (recovered): manifest items pre-archived, protected set {106-F} intact,
no orphans. This is the state the reclosure ran against after the git-revert
recovery (a3462650).
recommendation: baseline-reconstructed — the single-artifact reclosure (archive
shipment record 114-S only; manifest items already archived; 106-F remains in
queue) is surfaced to the operator for authorization per P-015 (HALT after
recovery).
