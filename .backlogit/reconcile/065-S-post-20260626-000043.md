---
title: "065-S Shipment Reconcile — Post-Mode"
shipment_id: 065-S
mode: post
merge_commit_sha: 23a8b045
timestamp: 2026-06-26T00:00:43-07:00
recommendation: PROCEED
---

## Shipment Reconcile — Post-Mode (GR)

Post-ship reconciliation for shipment `065-S` after
`backlogit shipment ship 065-S --sha 23a8b045`. Lock: per-file lock skipped —
single-agent, single-branch session; Git branch isolation
(`post-merge/065-docline-frontmatter`) provides sufficient concurrency safety.

## Ship Result

```json
{
  "shipment_id": "065-S",
  "shipment_status": "shipped",
  "archived_ids": ["065.001-T","065.002-T","065.003-T","065.004-T","065.005-T",
    "065.006-T","065.007-T","065.008-T","065.009-T","065.010-T","065.011-T",
    "065-F","065-S"],
  "returned_ids": [],
  "commit_sha": "23a8b045"
}
```

## Per-Item Archive Check

Every manifest item (and the shipment artifact itself) has a corresponding file
in `.backlogit/archive/`:

| Item | Archive file present | Notes |
|---|---|---|
| `065-S` | yes | shipment artifact, `status: archived`, `archived_status: shipped` |
| `065-F` | yes | feature, `status: archived` |
| `065.001-T` | yes | `commit: 23a8b045`, `status: archived` |
| `065.002-T` | yes | `status: archived` |
| `065.003-T` | yes | `status: archived` |
| `065.004-T` | yes | `status: archived` |
| `065.005-T` | yes | `status: archived` |
| `065.006-T` | yes | `status: archived` |
| `065.007-T` | yes | `status: archived` |
| `065.008-T` | yes | `status: archived` |
| `065.009-T` | yes | `status: archived` |
| `065.010-T` | yes | `status: archived` |
| `065.011-T` | yes | `status: archived` |

Index verification: `SELECT count(*) ... WHERE status IN ('active','queued')`
returns `0` for all 13 IDs. All 13 resolve to `status: archived` in the index
and archive files; the shipment (`065-S`) additionally carries
`archived_status: shipped` to record that it completed the ship lifecycle. (The
`shipment ship` command reports `shipment_status: shipped` at the moment of
shipping; the registry then routes the artifact to `.backlogit/archive/` with
`status: archived` + `archived_status: shipped`.)

## Deleted-File Guard (P-007)

`git status -- ".backlogit/archive/"`:

* 11 task files reported as **modified** (merge SHA `23a8b045` recorded,
  `status: done → archived`, `archived_from`/`archived_status` added) — benign.
* `065-F.md`, `065-S.md` reported as **untracked** (queue → archive move).
* **Zero archive files reported as deleted.** No `git restore` required.

## Gate Decision

All archive files present; no missing archive files; no unrestored deletions.
**Recommendation: PROCEED** to backlog-state commit.
