# Shipment Reconcile Report — 086-S (pre-mode)

- **Shipment**: 086-S — Unify malformed-JSONL-line handling across event-log parsers
- **Mode**: pre
- **Expected status**: done
- **Timestamp**: 2026-07-07T10:36:12-07:00
- **Invoked by**: Ship Step 6 (post-merge closure, pre-ship gate)

## Manifest items

| Item | Queue file | Archive file | Classification |
|------|-----------|-------------|----------------|
| 086-F | absent | `.backlogit/archive/086-F.md` present | `pre-archived` (valid) |
| 086.001-T | absent | `.backlogit/archive/086.001-T.md` present | `pre-archived` (valid) |

Both member items were moved to `done` during Step 4.5 (which auto-archives
queue→archive). They are correctly pre-archived before the shipment ship.

## Orphan scan

Scanned `.backlogit/queue/` for files declaring `shipment_id: 086-S` not present
in the manifest `items` list `[086-F, 086.001-T]`. The only 086-S queue file is
the shipment artifact itself (`086-S.md`, status `active`), which is the
shipment, not a member item. **No orphans.**

## Gate decision

All manifest items classify as `pre-archived`; no `missing`, `status-mismatch`,
or `orphan` items.

**recommendation: PROCEED**
