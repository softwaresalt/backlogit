---
shipment_id: 115-S
mode: pre
expected_status: done
timestamp: 2026-08-01T19:56:00Z
recommendation: PROCEED
---

# Shipment Reconcile — 115-S (pre-mode)

Full-feature shipment: covering feature 133-F is itself an explicit manifest
member (not merely an ancestor of shipped tasks), alongside its five
member tasks. 133-F is a top-level feature (no parent), so this shipment has
no non-member covering-feature ancestors in scope — the protected set is
empty.

## Manifest items

| Item | Queue | Archive | Status | Classification |
|---|---|---|---|---|
| 133-F | absent | present | done | pre-archived |
| 133.002-T | absent | present | done | pre-archived |
| 133.003-T | absent | present | done | pre-archived |
| 133.004-T | absent | present | done | pre-archived |
| 133.005-T | absent | present | done | pre-archived |
| 133.006-T | absent | present | done | pre-archived |

Each item was individually completed via `backlogit move {id} --status done`
immediately prior to this reconcile check (Step 4.5.2 catch-up — the
per-task completion moves were deferred during the build loop and are being
performed now, before shipment close, since each task's underlying code/doc
work was already committed, tested, reviewed, and merged in PR #327, merge
commit 47dfcc93698a6b0b2c5420c701c365a538895580). Marking each item `done`
relocated it from `.backlogit/queue/` to `.backlogit/archive/` per the
registry's status-based routing, matching the expected pre-ship state.

## Protected set (covering feature ancestors not in manifest)

None. 133-F has no parent feature, so `featureScopeRoots` resolves an empty
non-member ancestor set for this shipment. There is no risk of an
unintended-rollup covering feature being stranded by this ship.

## Orphan scan

Scanned `.backlogit/queue/*.md` for frontmatter/body references to `115-S`.
Only `115-S.md` itself matched (the shipment record); no queue item declares
membership in 115-S outside the manifest. Zero orphans.

## Pre-existing orphan (out of scope, confirmed untouched)

`133.001-T` was archived during Stage prior to this shipment's creation and is
NOT a manifest member. Verified via `backlogit get 133.001-T`: status
`archived`, `archived_from: .backlogit/queue/133.001-T.md` — unchanged by this
session. Per the task's explicit instruction, this orphan is excluded from the
reconcile scope and was not swept or touched.

## Gate decision

All 6 manifest items are `pre-archived` (present in archive with status
`done`); protected set is empty (nothing to verify); zero orphans.

**recommendation: PROCEED**
