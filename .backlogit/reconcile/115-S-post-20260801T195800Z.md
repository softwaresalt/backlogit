---
shipment_id: 115-S
mode: post
merge_commit_sha: 47dfcc93698a6b0b2c5420c701c365a538895580
timestamp: 2026-08-01T19:58:00Z
recommendation: PROCEED
---

# Shipment Reconcile — 115-S (post-mode)

## Archive presence (shipment record)

`.backlogit/archive/115-S.md` present (473 bytes). Shipment status: `shipped`.
`commit_sha` recorded: `47dfcc93698a6b0b2c5420c701c365a538895580`.

## Per-item archive check

| Item | Archive file | Present |
|---|---|---|
| 133-F | .backlogit/archive/133-F.md | yes (3211 bytes) |
| 133.002-T | .backlogit/archive/133.002-T.md | yes (2690 bytes) |
| 133.003-T | .backlogit/archive/133.003-T.md | yes (2222 bytes) |
| 133.004-T | .backlogit/archive/133.004-T.md | yes (3861 bytes) |
| 133.005-T | .backlogit/archive/133.005-T.md | yes (1808 bytes) |
| 133.006-T | .backlogit/archive/133.006-T.md | yes (1661 bytes) |

All 6 manifest items + the shipment record itself (7 total) are present in
`.backlogit/archive/`.

## Deleted-file guard (P-007)

`git status --short -- ".backlogit/archive/"` shows only additions and one
rename pair (`.backlogit/queue/115-S.md` -> `.backlogit/archive/115-S.md`,
displayed as `RM` by git's automatic rename detection). No plain `D` (deletion
without a corresponding archive addition) appears anywhere under
`.backlogit/archive/`. No restore action needed.

## `archived_ids` / `returned_ids` from `shipment ship` response

`archived_ids`: `["133.002-T","133.003-T","133.004-T","133.005-T","133.006-T","115-S","133-F"]`
(exactly the 6 manifest items + the shipment record — no unexpected inclusions).
`returned_ids`: `[]` (empty — no unreleased sibling items were returned, consistent
with 133-F having no unshipped children outside the manifest).

## Non-member covering feature check

133-F has no parent feature (top-level), so there is no non-member ancestor
whose status/location needed to be restored by the 133-F fix in this
shipment. This is consistent with the pre-mode report's "protected set is
empty" finding — the runtime-verification dogfood test (separate scratch
workspace, cleaned up) exercised the actual non-member-ancestor restore path
that this real shipment's topology does not require.

## Orphan / pre-existing exclusion re-check

`133.001-T` remains `archived` from its prior Stage-time action
(`archived_from: .backlogit/queue/133.001-T.md`), untouched by this shipment's
`shipment ship` call — confirmed not present in `archived_ids` above.

## Gate decision

All archive files present, no unrestored deletions, `archived_ids` matches the
manifest + shipment record exactly.

**recommendation: PROCEED**
