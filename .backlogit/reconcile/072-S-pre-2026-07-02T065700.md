# Shipment Reconcile Report — 072-S (mode: pre)

- **Shipment**: 072-S — doctor --target: fail closed on nil header-def (072-F)
- **Mode**: pre
- **Expected status**: done
- **Timestamp**: 2026-07-02T06:57:00-07:00
- **Merge commit**: d3f0facf530592c526e261b3818dc6e0d0dd0ced (PR #158)
- **Invoked from**: Ship Step 6 (pre-ship gate)

## Manifest items (2)

| Item | Queue? | Archive? | Declared status | Classification |
|---|---|---|---|---|
| 072-F | no | yes | done | pre-archived |
| 072.001-T | no | yes | done | pre-archived |

`072.001-T` was archived during the feature-branch build (committed via merged
PR #158). `072-F` was finalized `active → done` at the start of this closure
step (`backlogit move 072-F --status done`), which relocated its flat file from
`.backlogit/queue/072-F.md` to `.backlogit/archive/072-F.md` (status: done). Both
manifest items are therefore present in `.backlogit/archive/` carrying their
terminal `done` status. `backlogit shipment ship` will subsequently re-stamp each
archived item to `status: archived` and record the merge SHA — the expected
transition, not a discrepancy.

## Orphan scan

Scanned `.backlogit/queue/*.md` for files declaring `shipment_id: 072-S` that are
NOT in the manifest. Only `.backlogit/queue/072-S.md` (the shipment artifact
itself, `id: 072-S`, still `active`) matches "072-S". No feature/task queue file
references shipment 072-S. Remaining queue files (`048-DL.md`,
`054.001-R-...md`) are unrelated. **No orphans.**

## Lock

Single-writer lock acquired on `.backlogit/queue/072-S.md`
(`.backlogit/queue/.072-S.md.lock`) via `scripts/acquire_lock.ps1`. A human
operator is concurrently active in the workspace but is editing unrelated files
(`.github/agents/*.agent.md`, `.cursor/`, `.github/copilot/`, `.gitignore`) — no
contention on the backlog queue. Lock retained until post-mode completes.

## Recommendation

**PROCEED** — both manifest items are `pre-archived`; no missing,
status-mismatch, or orphan items. Safe to run `backlogit shipment ship 072-S`.
