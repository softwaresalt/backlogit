# Shipment Reconcile Report - 091-S (pre-mode)

* **Shipment**: 091-S - Reconcile spike skill findings-artifact example with docline frontmatter contract
* **Mode**: pre
* **Expected item status**: done (members completed during Ship build loop)
* **Timestamp**: 2026-07-13T15:31:00-07:00
* **Invoked by**: Ship Step 6 (post-merge closure, pre-ship GI/GR gate)

## Manifest

`custom_fields.items = [102-F, 102.001-T]`; shipment status `active`.

## Per-item classification

| Item | Queue file | Archive file | Status | Classification |
|---|---|---|---|---|
| 102-F (feature) | absent | `.backlogit/archive/102-F.md` present | `done` (commit `fd0a30b`) | `pre-archived` |
| 102.001-T (task) | absent | `.backlogit/archive/102.001-T.md` present | `done` (commit `fd0a30b`) | `pre-archived` |

Both members were completed and archived during the Ship build loop (task-completion
gate passed, exit 0), so no queue file remains; the archive files exist and are the
valid resolution. `pre-archived` is a valid pre-mode classification.

## Orphan scan

Scanned `.backlogit/queue/*.md` for frontmatter declaring `shipment_id: 091-S` that is
NOT a manifest member. The only `091-S` match is `.backlogit/queue/091-S.md` — the
shipment file itself (still `active`, awaiting `ship_shipment`), not an unlisted member.
**0 orphans.**

## Gate decision

All manifest items are `pre-archived`; no `missing`, no `status-mismatch`, no `orphan`.

**recommendation: PROCEED**
