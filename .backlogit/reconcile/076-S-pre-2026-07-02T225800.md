---
artifact_type: reconcile-report
title: Shipment 076-S pre-ship reconciliation
source: .github/skills/shipment-reconcile/SKILL.md
doc_type: report
shipment_id: 076-S
mode: pre
expected_status: done
generated_at: 2026-07-02T22:58:00-07:00
recommendation: PROCEED
---

# Shipment Reconcile — 076-S (mode: pre, expected_status: done)

Pre-ship reconciliation gate executed by Ship immediately before
`backlogit shipment ship 076-S`. Merge commit for the closed feature PR #166:
`ef9dc20468d865bbaf7d7b1e9b982ff7f4045422` (admin merge commit, P-009 preserved).

## Manifest

Source of truth: `backlogit shipment get 076-S` → `custom_fields.items`
(`076-F`, `076.001-T`, `076.002-T`).

| Item | Type | Location | Declared status | Classification |
|---|---|---|---|---|
| 076-F | feature | `.backlogit/archive/076-F.md` | done | `pre-archived` (valid) |
| 076.001-T | task | `.backlogit/archive/076.001-T.md` | done | `pre-archived` (valid) |
| 076.002-T | task | `.backlogit/archive/076.002-T.md` | done | `pre-archived` (valid) |

Both tasks were moved to `done`/archived during the build loop of the prior
session. `076-F` was completed (`active` → `done`) during this closure session as
all its child tasks were already `done` (consistent with the 075-S precedent).

## Orphan scan

Scanned `.backlogit/queue/` for any artifact declaring membership in shipment
`076-S` but absent from the manifest. Result: **no orphans**. The only queue file
that references `076-S` is the shipment container `076-S.md` itself (still
`active`, awaiting `shipment ship`).

## Gate decision

- All manifest items are `matched` or `pre-archived`: **yes**
- Orphans present: **no**

**recommendation: PROCEED** → proceed to `backlogit shipment ship 076-S --sha ef9dc20…`.
