---
doc_type: shipment-reconcile
mode: pre
shipment_id: 073-S
expected_status: done
generated_at: 2026-07-02T12:29:14-07:00
recommendation: PROCEED
---

# Shipment Reconcile — 073-S (pre-mode)

Pre-archive reconciliation gate run immediately before `backlogit shipment ship 073-S`.
Single-agent / single-branch context: branch isolation provides concurrency safety;
no multi-agent file lock acquired (per Ship Concurrency Control).

## Manifest

Manifest items (from `custom_fields.items`): `073-F`, `073.001-T`.
Expected status: `done`.

| Item | Location | Status | Classification |
|---|---|---|---|
| `073-F` | `.backlogit/archive/073-F.md` | `done` | `pre-archived` |
| `073.001-T` | `.backlogit/archive/073.001-T.md` | `done` | `pre-archived` |

- `073.001-T` was archived during the build/complete phase (status `done`).
- `073-F` was moved to `done` on the post-merge closure branch prior to this gate
  (routing rule `done → archive`), so it is now pre-archived.

## Orphan Scan

Scanned `.backlogit/queue/*.md` for frontmatter `shipment_id: 073-S` not present in
the manifest. **No orphans found.** The only queue file referencing `073-S` is the
shipment artifact `073-S.md` itself (`artifact_type: shipment`, `status: active`),
which is not a manifest member and is not an orphan.

## Classification Summary

- `matched`: 0
- `pre-archived`: 2 (`073-F`, `073.001-T`)
- `missing`: 0
- `status-mismatch`: 0
- `orphan`: 0

## Recommendation

**PROCEED** — all manifest items are `pre-archived` (valid), no orphans, no missing or
status-mismatched items. Safe to run `backlogit shipment ship 073-S --sha 00b9b1d…`.
