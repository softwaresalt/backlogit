{"id":"7F0A6E89","priority":"low","kind":"task","text":"Follow-up (after 102.001-T lands): update the upstream spike/SKILL.md.tmpl in the external autoharness repo so regeneration preserves the in-repo docline findings-artifact fix (Principle IV, no out-of-tree writes). Refs: 102.001-T, 102-F, docs/exec-plans/2026-07-13-spike-findings-docline-reconciliation-plan.md.","created_at":"2026-07-13T20:05:35.8455958Z"}
{"id":"8CD8F46A","priority":"medium","kind":"task","text":"Enable (or formally waive) the formal multi-persona plan-review skill gate. This environment cannot dispatch independent reviewer persona sub-agents, so Stage plans (091-S through 093-S) ran an honest inline single-agent self-assessment instead of the formal .github/skills/plan-review/SKILL.md gate (AGENTS.md:378 orders plan-review -> harvest). Establish a real persona-dispatch path OR a documented operator waiver so the pre-harvest gate is satisfiable and not silently skipped.","created_at":"2026-07-14T16:11:55.0375147Z"}
{"id":"CA877CD1","priority":"low","kind":"task","text":"Add a labeled 'Constitution Check' section to the impl-plan skill output. Governance requires every implementation plan to map proposed work against the constitution; recent 091-S/092-S/093-S plans omitted an explicitly-labeled Constitution Check section.","created_at":"2026-07-14T16:11:59.1903437Z"}
{"id":"D5FA1EE9","priority":"low","kind":"task","text":"P2 follow-up (deferred from SE-6 / 108.008-T, shipment 099-S): add CLI human-column size + composition parity to the non-JSON list surfaces (backlogit list, shipment list, queue view). SE-6 intentionally scoped to MCP projection + CLI --json to stay two-file within the 2h envelope; the cosmetic human-readable columns were deferred. Owner: unassigned (triage). References impl-plan docs/exec-plans/2026-07-18-108-F-size-estimation-impl-plan.md SE-6.","created_at":"2026-07-19T01:25:59.4398482Z"}
{"id":"387DE4BF","priority":"low","kind":"task","text":"CLI-JSON composition parity gap (deferred from SE-6 / 108.008-T, shipment 099-S). The SE-4 size composition (XS-XL histogram + unsized + members) is exposed only on the MCP read surfaces (get_item/get_shipment/get_queue) in 099-S. CLI get JSON (internal/cli/get.go buildDetailMap) and queue JSON (internal/cli/queue_cmd.go / QueryQueue) use SEPARATE shapers that do not carry the derived composition. Size/provenance parity (custom_fields.size, size_source, size_ruleset_version) DOES hold on both transports because both already emit custom_fields. Follow-up: add the derived composition to the CLI JSON shapers (buildDetailMap, QueryQueue) and test every CLI/MCP read pair, or introduce a shared read-shaper used by both transports. Distinct from stash D5FA1EE9 (cosmetic CLI human-column table parity).","created_at":"2026-07-19T02:07:10.7734599Z"}
{"id":"131CEAE4","priority":"low","kind":"task","text":"OS-crash / power-loss durability for the size event+write path (out of scope for 099-S, Copilot G3). Both shared writers are sync-free: atomicfile.WriteFileAtomic (internal/atomicfile/atomicfile.go:15-63) does temp-write -> Chmod -> Close -> Rename with NO fsync before rename; events.EventWriter.AppendEvent (internal/events/stream.go:40-64) does O_APPEND write with NO f.Sync. So the 099-S event-before-write ordering guarantees PROCESS-crash safety only, NOT OS-crash/power-loss. Closing that gap requires a separate fsync protocol coordinated across BOTH shared writers (fsync the appended event file AND the atomic temp file before rename, plus parent-dir fsync). This is cross-cutting (changes the whole repo write path, not just the size seam) and must be its own release unit with its own review. Follow-up: design a repo-wide durability/fsync protocol for atomicfile + events.","created_at":"2026-07-19T02:12:12.5218196Z"}
{"id":"9D5BB492","priority":"low","kind":"task","text":"Crash-window exactly-once size mutation, deferred from 099-S per Copilot cycle-3 H1/H5 descope (Option B2). Requires stable transport-visible OpID ingress on the CLI/MCP interface (SE-5 exposes none and no caller carries one), deterministic multi-orphan event-ordering, and a reachable offline doctor reconciliation routed through the real internal/core/doctor.go Doctor() workspace scan (doctor_target.go is single-file validation-only and is never called by the scan). Cycle-3 removed SE-3c (108.011-T) and narrowed SE-3b (108.006-T) to best-effort append-before-write audit ordering with fail-closed read (durable custom_fields.size is source of truth; orphan events ignored on read). Out of scope for a size-estimation feature; revisit only if exactly-once crash-window semantics become a product requirement.","created_at":"2026-07-19T02:58:44.4818498Z"}
{"id":"47ED88ED","priority":"low","kind":"task","text":"get_queue size-composition rollup is O(aggregates x members x all-artifacts) filesystem scan (perf; deferred from 099-S / 108-F SE-4, Copilot cycle-3 SIEy9). handleGetQueue -> queueViewWithSizeComposition -> injectQueueSizeComposition (internal/mcp/tools.go ~1421) calls core.SizeComposition per feature/shipment queue item. SizeComposition (internal/core/size_composition.go:38) resolves each child/member via findArtifact (internal/core/artifacts.go:818), which does a full filepath.WalkDir parsing EVERY .md in the search dirs per lookup. childIDsByParent already uses the SQLite index; the members do not. On a large queue this is N (aggregates) x M (members) x full-tree walk. Fix: batch-resolve members from the index (single SELECT over parent_id / membership) instead of per-member findArtifact, or add a cached in-memory artifact map for the request. Correctness is fine; read-only surface; low reachability at current backlog size. Same perf class as SHaOX. References impl-plan docs/exec-plans/2026-07-18-108-F-size-estimation-impl-plan.md SE-4.","created_at":"2026-07-20T19:44:33.6269935Z"}
{"id":"A3C349DD","priority":"medium","kind":"task","text":"Reconcile ship-gate descope exemption with canonical TerminalStatuses. archivedFromNonTerminalStatus (internal/core/shipment_gate.go) exempts a member from per-member F4 gate evidence when archived_status is recognized AND !isTerminalReleaseStatus. But isTerminalReleaseStatus={done,accepted,rejected,archived} OMITS shipped+abandoned, while canonical core.TerminalStatuses (blocking_cascade.go:14)={done,accepted,archived,shipped,abandoned,rejected}. So a member archived from shipped/abandoned is currently EXEMPTED though both are terminal. Decide the intended descope-eligible set (likely genuine in-flight {queued,active,blocked,review} plus possibly abandoned/rejected as descopes; shipped/done/accepted are completions and must NOT be exempt) and introduce a dedicated descope predicate for the exemption WITHOUT altering the other 5 isTerminalReleaseStatus call sites (shipment_lifecycle.go:250,271,304,326 release-progression). Add regression tests per status. Surfaced by Copilot review on PR #265.","created_at":"2026-07-20T21:36:54.4717753Z"}
