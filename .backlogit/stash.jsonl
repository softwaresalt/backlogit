{"id":"EA1F5912","priority":"medium","kind":"task","text":"Classify syncWriteFileAtomic outcomes by converging internal/events onto the existing internal/atomicfile leaf - which already tags ErrWriteNotApplied and ErrWriteIndeterminate - rather than adding a second classifier inside the private duplicate, then surface indeterminate checkpoint creates with path and context keys on both transports. Today events.CreateCheckpoint writes through syncWriteFileAtomic (internal/events/fsutil.go) with no outcome classification and no injectable seam, and neither the MCP errorResponse nor the CLI error path can carry a path or a key list, so a create whose write is indeterminate returns a bare error and the caller cannot tell \"probably written\" from \"not written\". Needs a classified write result, a handler-level seam, and a new error envelope on both surfaces. Provenance: scope-boundary follow-up 2 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md; parent work feature 146-F, shipment 129-S. Descoped from 129-S because it is traceable to neither external source 3C7AAC71 nor 90F2A9F8. NOT release-blocking for 129-S.","created_at":"2026-08-22T01:48:35.8650949Z"}
{"id":"EC987334","priority":"medium","kind":"task","text":"Drop omitempty from MigrateReport collection fields. MigrateReport.Applied and MigrateReport.Skipped (internal/docline/report.go) carry omitempty and therefore vanish entirely on a zero-apply run - a known adjacent instance of the always-an-array contract that 146-F / 129-S fixes only for LintReport.Findings and the new checkpoint context_keys. Residual exposure: a zero-apply migrate report omits both arrays, so an agent cannot distinguish \"nothing applied\" from \"field absent\". Compound reference: docs/compound/2026-07-21-omitempty-defeats-arrays-always-json-contract.md. Provenance: scope-boundary follow-up 3 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md; parent work feature 146-F, shipment 129-S; external source 90F2A9F8. NOT release-blocking for 129-S.","created_at":"2026-08-22T01:48:43.4780902Z"}
{"id":"1787FD85","priority":"high","kind":"task","text":"Converge LintTree and PlanMigration on the one classifyDecodeFailure helper. After 146-F / 129-S lands, docline.LintTree reports-and-continues on a per-file frontmatter decode failure while docline.PlanMigration still aborts the whole corpus on the first one - two decode policies over one frontmatter grammar inside the same package. PlanMigration is read-only, so the write-path safety rationale that justifies ApplyMigration's all-or-nothing abort does not apply to it. U8 introduces the unexported classifyDecodeFailure(err) (rule string, fatal error) helper in internal/docline/service.go with a containment/decode/read-failure/nil table test as its convergence lock; this follow-up adopts that same helper in PlanMigration so a second decode policy is never written. Provenance: scope-boundary follow-up 4 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md, and named as the recorded convergence follow-up in plan unit U8; parent work feature 146-F, shipment 129-S; external source 90F2A9F8. NOT release-blocking for 129-S: the divergence is introduced by 129-S and is recorded as accepted residual exposure.","created_at":"2026-08-22T01:48:52.3815233Z"}
{"id":"5F4E0FC3","priority":"medium","kind":"unknown","text":"Decide whether create_checkpoint becomes a governed operation. 146-F / 129-S deliberately does NOT add a governed: true marker or a governed_name for create_checkpoint in .autoharness/backlog-registry.yaml, because the governed-parity design requires a named behavioral fixture per marker and that is a separate decision. Neither the registry nor the MCP-to-CLI fallback guide describes a create_checkpoint result shape or flag semantics affected by U6, so 129-S introduces no registry drift. Residual exposure: create_checkpoint stays ungoverned, with CLI/MCP parity enforced only by U5a's cross-surface harness rather than by the registry drift test. Needs deliberation before implementation. References: docs/design-docs/governed-operation-parity.md, docs/compound/2026-08-15-governed-parity-fixtures-must-dispatch-authoritative-registry.md. Provenance: scope-boundary follow-up 5 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md; parent work feature 146-F, shipment 129-S. NOT release-blocking for 129-S.","created_at":"2026-08-22T01:49:00.2249037Z"}
{"id":"360A183F","priority":"high","kind":"task","text":"Upstream the checkpoint context Continuity Protocol wording into backlogit.instructions.md.tmpl. Plan unit U10b edits .github/instructions/backlogit.instructions.md in this repository so agents learn that for a schema_version: 1 dump the arbitrary recovery keys belong INSIDE context, that the top level and progress are closed, that a rejected create means \"retry with the keys nested under context\" rather than \"session state is lost\", and that checkpoint context is unredacted git-tracked durable state that must not carry secrets. That file is autoharness-generated from backlogit.instructions.md.tmpl, which lives in a repository 146-F / 129-S treats as strictly read-only under Principle IV / PA-5, so the in-repo edit is silently reverted by the next autoharness regeneration. This follow-up upstreams the same wording into the template. The durable, non-generated statement of the contract remains U4b's backlogit_create_checkpoint MCP tool description. Provenance: scope-boundary follow-up 6 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md, referenced from plan unit U10b's Generated-artifact caveat and acceptance; parent work feature 146-F, shipment 129-S; external source 3C7AAC71. NOT release-blocking for 129-S: it is executed in the upstream harness repository, not in this one, so it cannot be a member of 129-S.","created_at":"2026-08-22T01:49:10.6716138Z"}
{"id":"63E810D9","priority":"medium","kind":"task","text":"Provide a structured JSON error envelope for CLI validation failures mirroring the MCP shape. After 146-F / 129-S lands, MCP carries a structured unknown_fields []string array on an unknown-key checkpoint rejection (plan unit U4b) while the CLI prints a wrapped error string, so an agent driving the CLI must read message text to learn which keys were rejected while an agent driving MCP gets a parseable list. Adding a JSON error-serialization path to the CLI is a general output-contract change affecting every CLI error, not just this one, and is untraceable to either in-scope external source. Compound reference: docs/compound/2026-07-03-cli-mcp-honest-fallback-map-and-registry-drift-test.md. Provenance: scope-boundary follow-up 7 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md; parent work feature 146-F, shipment 129-S. NOT release-blocking for 129-S.","created_at":"2026-08-22T01:49:18.3835638Z"}
{"id":"6CE00B88","priority":"medium","kind":"unknown","text":"Decide the gitignore and redaction posture for checkpoint context. .backlogit/checkpoints/ is NOT gitignored and its files are tracked. 146-F / 129-S opens the checkpoint context namespace, which makes it materially easier for an agent to persist arbitrary unredacted session state to a git-tracked path; changing the tracking posture mid-shipment would rewrite unrelated repository conventions, so 129-S deliberately leaves it unchanged and mitigates only with U10b's documented secrets caveat. Residual exposure: unredacted agent-supplied context continues to be committed to git, and Principle XI forbids history rewriting so git revert cannot purge a committed blob. Needs deliberation covering gitignore posture, redaction at the write boundary, and whether a size or key allowlist is warranted. Provenance: scope-boundary follow-up 8 of 8 recorded in docs/exec-plans/2026-08-21-success-shaped-evidence-loss-plan.md, and cross-referenced from the plan's Decisions table row on unredacted durable state and its Plan Hardening Signals security row; parent work feature 146-F, shipment 129-S; external source 3C7AAC71. NOT release-blocking for 129-S.","created_at":"2026-08-22T01:49:27.3126547Z"}
{"id":"5672D73E","priority":"low","kind":"task","text":"Review follow-up (129-S / 146-F): no MCP tool equivalent of `backlogit docs classify <path>` exists (backlogit_docs_lint/migrate/scope are all present, but classify is CLI-only). Consider adding a `backlogit_docs_classify` MCP tool wrapping docline.Classify for CLI/MCP parity. Raised by Agent-Native Parity Reviewer during 129-S structured review; pre-existing gap, not introduced by this shipment.","created_at":"2026-08-22T09:06:57.5280174Z"}
{"id":"EB93E236","priority":"low","kind":"task","text":"Review follow-up (129-S / 146-F): The CLI's `checkpoint create` closed-schema-namespace rejection only exposes a plain-text error (via fmt.Errorf wrapping *CheckpointUnknownFieldError), while the MCP surface returns a structured {error, message, unknown_fields} response. Consider factoring a shared, transport-agnostic response builder from *CheckpointUnknownFieldError so the CLI (and its --jsonrpc mode) can expose unknown_fields structurally too, and/or add an optional `data` field to internal/cli/format.JSONRPCError per JSON-RPC 2.0 so structured domain errors (unknown_fields, mutation-partial classification, checkpoint disposition codes, etc.) don't degrade to a string on the CLI's JSON-RPC surface. Raised by Agent-Native Parity Reviewer during 129-S structured review; explicitly out of scope for 146.012-T (task text scoped this to MCP only).","created_at":"2026-08-22T09:06:57.671557Z"}
{"id":"66834D9E","priority":"low","kind":"task","text":"Review follow-up (129-S / 146-F, harness hygiene): commit-message.instructions.md documents a closed commit-scope vocabulary (mcp, cli, db, core, models, telemetry, config, docs, tests, ci, harness) and a 300-byte body limit, but repo-wide practice (including this shipment's own commits) regularly uses additional scopes (events, docline, memory, compound, cli-reference) and longer bodies for larger multi-bullet commits. Raised by Constitution Reviewer during 129-S structured review. Consider either updating commit-message.instructions.md's scope enumeration/body-length guidance to match actual practice, or tightening future commits to the documented limits — this is a pre-existing, repo-wide drift, not specific to this shipment.","created_at":"2026-08-22T09:07:09.2745352Z"}
{"id":"F89CADB7","priority":"low","kind":"bug","text":"Review follow-up (129-S / 146-F, PR #373, deferred at review-fix cycle limit): CheckpointContext.Extra is an exported field and can be constructed directly (bypassing UnmarshalJSON's guarantee that every stored json.RawMessage came from a real decode). CheckpointContext.emit() (used by both MarshalJSON and Keys()) appends each Extra value verbatim without validating it is well-formed JSON, so a directly-constructed CheckpointContext with a nil or malformed json.RawMessage in Extra makes MarshalJSON() return invalid JSON bytes with a NIL error, and Keys() likewise reports success despite the same emit() result being unserializable. Fix: validate each Extra raw value (e.g. json.Valid(v)) in emit() before appending, and return an error naming the offending key if invalid. Add a regression test constructing a CheckpointContext directly with a malformed Extra value and asserting MarshalJSON/Keys() both return an error rather than silently producing broken JSON. Deferred past the 3-cycle review-fix limit (circuit-breaker.instructions.md).","created_at":"2026-08-22T21:07:32.5511264Z"}
{"id":"BE32CAE2","priority":"low","kind":"task","text":"Add a supported, audited, operator-invoked forward repair operation that reconciles ONLY a shipment record whose status is still `queued` although its manifest work items have already advanced consistently beyond queued state.\n\nProblem: a shipment can end up in a split state where the shipment artifact still reads `queued` while its manifest members (covering feature and tasks) are already `active` or further along. This arises when work legitimately started but the shipment-record transition did not land or was not applied through the claim path (for example an interrupted, externally-mutated, or partially applied activation). There is currently no supported, non-destructive way to correct just the shipment record. The safe report-only behavior already exists upstream: autoharness feature 112-F / shipment 118-S delivered report-only detection and operator guidance for exactly this split state, so an operator can already see and understand the condition but has no sanctioned operation to resolve it. That is why this is low priority - it closes a capability gap rather than an active correctness or data-loss risk.\n\nRequested behavior contract (the safety boundaries are the requirement; internal design is deliberately left open for deliberation):\n- Target-specific and inspection-gated. The operator names one specific shipment. The operation first inspects actual current state and proceeds only when it matches the narrow, unambiguous split shape it is designed to repair. Ambiguous, mixed, or incompatible states are rejected with a clear explanation rather than repaired on a guess.\n- Forward-only reconciliation. The sole correction is advancing the shipment record to `active` (or the semantically equivalent sanctioned state correction for the observed shape). There is explicitly NO backward `active -> queued` repair.\n- Shipment-record-only. It must update the shipment artifact and nothing else. It must NOT replay the manifest-wide activation that ClaimShipment performs, must NOT mutate member task or feature statuses, and must NOT cascade parent state. Members being already correct is the PRECONDITION for the repair, not something the repair re-establishes.\n- Atomic and concurrency-safe, consistent with existing shipment mutation and rollback handling, so a failed or concurrent invocation can never leave a new torn state.\n- Auditable, emitting structured before / after / result evidence capturing what was inspected, what changed, and the operator justification.\n- Never silent, never automatic. No background or implicit self-healing; explicit operator invocation remains required in all cases.\n\nExplicit non-goals:\n- Not a generic repair, state-override, or force-transition framework, and not a general stale-evidence repair path.\n- Specifically EXCLUDES the separate DD957688 issue (shipment ship-time gate blocked by an archived task whose recorded gate evidence points at a dangling commit). That is a different problem with a different shape and must not be folded into this operation.\n- Adjacent shipment-lifecycle audit and prevention work in features 143-F and 144-F is related in area but does not provide this operation; neither repairs an under-advanced shipment record.\n\nProvenance: upstream prerequisite for autoharness tracker 936C68F3, which stays unresolved until backlogit exposes this operation.\n\nNeeds Stage deliberation before planning. Open questions include: the exact surface (CLI, MCP, or both) and whether it should be a governed operation; how the acceptable pre-state is defined precisely (which member statuses beyond queued count as \"advanced consistently\", and how gaps or descoped members are treated); what justification the operator must supply; and where and how the before/after/result evidence is recorded.","created_at":"2026-08-24T05:06:40.6356546Z"}
{"id":"3A33E404","priority":"high","kind":"bug","text":"CreateCheckpoint accepts malformed JSON and writes a corrupt checkpoint file while returning success.\n\nDefect (verified at current HEAD b07729386a316384d1d740ac77d60ff7e0ea9a36): in internal/events/memory.go, CreateCheckpoint decides whether an incoming state_dump is a V1 checkpoint by probing with json.Unmarshal(data, &probe) == nil && probe.SchemaVersion == 1. That single condition conflates two different questions: \"is this syntactically valid JSON?\" and \"is this a V1 checkpoint?\". When state_dump is malformed or truncated JSON, the probe unmarshal fails, so the condition is false and the V1 branch is skipped entirely - ParseCheckpoint, checkClosedSchemaNamespace, ValidateCheckpoint, Context.Keys() and the re-marshal never run. Control falls through to the legacy path, which hands the raw unvalidated bytes straight to syncWriteFileAtomic. This happens even when the payload plainly begins with {\"schema_version\":1,... and was obviously intended as a V1 checkpoint: the truncation is exactly what makes the probe fail, so the more V1-shaped-but-broken the input is, the more silently it is treated as legacy.\n\nImpact: the call returns a success-shaped CreateCheckpointResult (path plus best-effort legacy context keys) and leaves a corrupt checkpoint file on disk. The damage surfaces later and elsewhere: subsequent checkpoint enumeration and recovery correctly fail closed on the malformed record, which under the agent recovery protocols can halt Stage or Ship startup and force operator handoff. The write is reported as good at the moment it happens and only proves bad at the moment an agent needs it.\n\nNot the defect: the atomic file writer is behaving correctly. syncWriteFileAtomic faithfully and durably persists the bytes it is given; those bytes were already malformed when they arrived. Please do not describe or fix this as backlogit truncating or corrupting a valid payload - the corruption is inherited from the caller-supplied input and the bug is that nothing rejects it.\n\nRequired behavior contract:\n- Establish that state_dump is syntactically valid JSON FIRST, as a separate step, and only then classify it as V1 or legacy. Validity and schema selection must stop being one combined test.\n- Reject unparseable/malformed JSON using this workspace's established typed/structured error behavior, consistently on the core, CLI, and MCP surfaces.\n- On rejection, return no success-shaped result and leave no checkpoint file behind (no partial, no temp remnant). Same \"fail before the write\" ordering discipline already pinned for the Keys()-before-write case.\n- Preserve legitimate legacy compatibility: input that is valid JSON but lacks schema_version: 1 must keep flowing through the existing legacy path with unchanged behavior. This fix must not turn legacy dumps into rejections.\n- Preserve current V1 behavior for valid V1 payloads: the existing parse, closed-schema-namespace validation, context-key reporting, and atomic persistence must all be unchanged.\n- Add focused regression coverage: a truncated V1-shaped payload that asserts BOTH a typed error is returned AND no checkpoint file exists afterward. Add transport parity coverage only to the extent needed to pin the same contract on CLI and MCP.\n\nExplicit non-goals and scope boundaries:\n- No generic checkpoint refactor. This is a narrow input-validation ordering fix.\n- No payload size limit. A size cap is not required to close this defect and should only be considered if a later, independent deliberation justifies it on its own merits.\n- No changes to quarantine/abandon disposition behavior (136-F), recovery policy, checkpoint retention, agent templates, or unrelated checkpoint context handling.\n- Do NOT reopen or duplicate the sibling work already shipped under 146-F: 3C7AAC71 (context-key preservation) and 90F2A9F8 (docs-lint continuation). In particular, task 146.011-T added the closed unknown-schema-namespace rejection that runs only AFTER the probe has already succeeded, so it does not and cannot catch this malformed-input fallthrough - the malformed case never reaches it.\n\nProvenance: external tracker autoharness B57F9E24. Reverified by reading internal/events/memory.go at current backlogit HEAD b07729386a316384d1d740ac77d60ff7e0ea9a36; the probe-then-fallthrough code path is present as described and the defect is unresolved. Captured for later Stage triage and deliberation; implementation approach is deliberately left open.","created_at":"2026-08-24T05:13:49.3442034Z"}
{"id":"E429A031","priority":"medium","kind":"task","text":"Checkpoint create-boundary context duplicates: CreateCheckpoint accepts a caller-supplied context object whose exact-duplicate or modeled-alias member names collapse under map[string]json.RawMessage decode before the file is written. 147.028-T/U2g closes this at the READ boundary only. Deferred from the PR #377 cycle-15 plan review (security-lens scope split, non-blocking): the create path writes a new file from caller bytes, so no pre-existing on-disk evidence is at risk. Close it by extending checkClosedSchemaNamespace with an ordered context walk reusing decodeTopLevelEntries.","created_at":"2026-08-25T04:45:36.934926Z"}
{"id":"35A27CD0","priority":"medium","kind":"task","text":"Checkpoint filesystem containment hardening plus the withdrawn checkpoint repair/restore runbook: the checkpoint read/write paths do not enforce no-follow on symlinked entries under the checkpoint directory, and the shipped parse-mutate-remarshal shape carries a read-to-write TOCTOU window between the conformance read and the atomic rewrite. Both are pre-existing properties unchanged by PR #377 / shipment 130-S; deferred from the cycle-15 plan review security-lens split as non-blocking because 130-S narrows what may be written without introducing either window. PR #377 cycle 16 additionally folds in the body-preserving hand-repair and post-quarantine restore runbook withdrawn from 147.018-T / U9b and 147.026-T / U10b: it performs rename, copy, in-place rewrite and conditional delete against this same unhardened directory, so it cannot be published as workspace-wide agent instructions until this stash lands. Needs its own bounded hardening unit: O_NOFOLLOW or equivalent real-root open, a handle-or-content compare-and-swap precondition on rewrite, no-clobber destinations, and adversarial tests for symlinked and concurrently-replaced paths; only then republish the repair/restore procedure.","created_at":"2026-08-25T04:45:45.108357Z"}
{"id":"6FA45E69","priority":"low","kind":"task","text":"Pin the conforming + status:resolved checkpoint double-refusal state-conflict class (I3 table row 3) as a tested invariant in a unit that owns it. Withdrawn from 147.009-T (U5) in PR #377 cycle 16: it asserts an out-of-scope class on a document state U5's production delta does not touch, and counting it kept U5 at four effective scenarios. Source: docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md","created_at":"2026-08-25T05:41:36.4722615Z"}
{"id":"DBBA62AA","priority":"low","kind":"task","text":"CLI coverage for 'backlogit checkpoint resolve' on an already-abandoned checkpoint document. Deliberately excluded from 147.015-T (U8) in PR #377 cycle 16: U8 already carries three scenarios and the CLI does not consume domainError, so U7e's mapping change does not alter CLI behaviour. Add the assertion in its own unit rather than breaching the four-scenario limit.","created_at":"2026-08-25T05:41:43.8631668Z"}
{"id":"F350503F","priority":"medium","kind":"task","text":"Remove the deprecated CheckpointSummary.RemediationCommand field (internal/events/checkpoint_schema.go:~398, populated by internal/events/checkpoint_lifecycle.go:~54,74 via remediationQuarantineCommand) after every consumer has migrated to the structured RemediationIntent carrier (U1d, 147.032-T) that PR #377 cycle 17 introduced. RemediationCommand is deprecated in place (not removed) in this release because internal/events must not know the caller's working directory, so any command string it emits is bound to an ambient cwd and is unsafe to publish as the sole remediation surface (cycle-17 gate finding H1). Once CLI (checkpoint list / get), MCP (backlogit_list_checkpoints / backlogit_get_checkpoint), and any other reader all consume RemediationIntent instead, remove the field, its JSON tag, remediationQuarantineCommand, and the tests that assert against it (internal/events/checkpoint_corpus_test.go, internal/events/checkpoint_lifecycle_test.go including TestListCheckpoints_RemediationCommandIsShellSafe). Include compatibility coverage proving no remaining caller reads RemediationCommand, plus removal tests proving the field and helper are gone. Provenance: follow-up recorded in docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md (U6, U16, Constitution Check, PR #377 plan remediation cycle 17); parent feature 147-F, shipment 130-S.","created_at":"2026-08-25T18:05:15.6209423Z"}
{"id":"A12BBAFA","priority":"medium","kind":"task","text":"Harden the checkpoint quarantine disposition sidecar write to no-replace semantics. internal/core/checkpoint_disposition.go's quarantine path moves the payload with moveNoReplace (~lines 259-282, genuinely no-clobber) but writes the '<filename>.disposition.json' sidecar with atomicfile.WriteFileAtomic (~lines 229-231), which is a documented idempotent upsert that replaces an existing destination. The evidence pair is protected only in aggregate, because the payload move refuses the collision first and the sidecar step is never reached on a repeat quarantine of the same filename; the sidecar itself has no independent no-clobber guarantee. Give the sidecar its own create-only (no-replace / O_EXCL-equivalent) write path so it cannot silently overwrite prior quarantine evidence, then add tests for: (1) a sidecar-only collision (payload move succeeds into a fresh destination but the sidecar path is already occupied) refusing without clobbering the existing sidecar, and (2) a concurrent-quarantine race on the same filename refusing the second writer's sidecar without clobbering the first writer's evidence. Provenance: follow-up recorded in docs/exec-plans/2026-08-24-checkpoint-toplevel-key-disposition-plan.md (U10b narrowed evidence-pair claim, PR #377 plan remediation cycle 17); parent feature 147-F, shipment 130-S.","created_at":"2026-08-25T18:05:15.652649Z"}
