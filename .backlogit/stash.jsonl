{"id":"F51BAEC0","priority":"medium","kind":"feature","text":"Need a way to implement disaster recovery of agent session ending suddenly, which can happen when using a remote server or if the power gets shut off.  This may involve a combination of agent harness improvements and backlogit tooling.  Need to perform adequate research and analysis to understand what this feature entails."}
{"id":"2599179A","priority":"high","deliberation_id":"007-DL","kind":"feature","text":"Internal lifecycle hooks for validation, cascade, and enforcement","created_at":"2026-04-10T22:21:17.6026439Z"}
{"id":"C7550B6E","priority":"medium","deliberation_id":"032-DL","kind":"feature","text":"External system hooks with prioritized notification push support for Slack, Teams, and webhooks","created_at":"2026-04-10T22:24:23.6518517Z"}
{"id":"C00AA592","priority":"medium","kind":"task","text":"When AdoptItem rewrites an artifact's ID and renames its file, other artifacts whose frontmatter references the old ID (e.g. parent_id, dependencies) are not updated. Stale edges reappear on next rehydration. Need a cross-artifact frontmatter reference rewrite step in AdoptItem that scans all artifacts for references to the old ID and updates them atomically.","created_at":"2026-04-12T23:03:56.1492388Z"}
{"id":"46CC1C9D","priority":"medium","kind":"feature","text":"Add a stash.jsonl file to .backlogit/archive/ that collects each stash entry as it is staged and pulled into a shipment after deliberation. This preserves a durable record of all prior stash entries, including their original text, priority, kind, linked deliberation ID, and the harvested artifact ID. Currently, removed stash entries disappear entirely from stash.jsonl with no historical trace.","created_at":"2026-04-10T20:12:47.9098831Z"}
{"id":"A8F688A7","priority":"medium","kind":"task","text":"Add stash hygiene to the Stage workflow. Stage agent guidance still lacks a standard stale-stash pruning step even though stash entries now expose `age_days` and support `stash remove` and `stash edit`; update the agent protocol and refresh the stale compound doc so future agents stop falling back to raw `stash.jsonl` edits.","created_at":"2026-04-10T20:35:28.3667444Z"}
{"id":"2FEAE1B9","priority":"critical","kind":"task","text":"Increase SQLite busy_timeout to 30s for MCP server processes. Current 5s timeout in connection.go is too short when multiple processes contend on backlogit.db write lock. Effort: trivial. Impact: high.","created_at":"2026-04-13T16:24:25.5290288Z"}
{"id":"B7EA14AF","priority":"critical","kind":"task","text":"Add write-retry wrapper with exponential backoff for SQLite operations. Wrap all DB write operations with a retry helper that catches SQLITE_BUSY errors and retries (3 retries: 1s/2s/4s backoff). No retry logic exists anywhere currently. Effort: moderate. Impact: high.","created_at":"2026-04-13T16:24:58.7314029Z"}
{"id":"D36ED34A","priority":"high","kind":"task","text":"Use BEGIN IMMEDIATE for write transactions in SQLite. Rehydrate, UpsertItem, and other write paths currently use default deferred transactions, which only acquire the write lock at first write statement. This means lock conflicts are detected late (at commit or mid-transaction), wasting work that gets rolled back. BEGIN IMMEDIATE acquires the lock at transaction start, failing fast and enabling retry at the boundary. Effort: moderate -- add sql.TxOptions to BeginTx calls in rehydration.go, queries.go, etc. Impact: moderate -- prevents wasted work under contention.","created_at":"2026-04-13T16:25:19.163743Z"}
{"id":"C0D6D7F2","priority":"high","kind":"task","text":"Break Rehydrate into smaller batched transactions. Currently Rehydrate() in rehydration.go holds a single write transaction for the entire DELETE-all + filesystem-walk + rebuild cycle. On large workspaces this holds the SQLite write lock for hundreds of milliseconds to seconds, blocking all other writers. Restructure to commit in batches (e.g., N items per transaction) to reduce maximum lock hold time. Effort: moderate -- restructure rehydration loop with batch commits while preserving atomicity guarantees. Impact: moderate -- reduces max contention window during sync operations.","created_at":"2026-04-13T16:25:35.5925956Z"}
{"id":"21E17BFC","priority":"medium","kind":"feature","text":"Consider singleton MCP server with multiplexed transport (HTTP/SSE) instead of per-client stdio processes. Currently each MCP client (VS Code, Copilot CLI) spawns its own backlogit mcp process, each with an independent sql.DB connection pool against the same backlogit.db. A single long-lived server process with HTTP or SSE transport would eliminate multi-process SQLite contention entirely. NOTE: This is a contingency item -- only pursue if fixes 1-4 (busy_timeout increase, write-retry wrapper, BEGIN IMMEDIATE transactions, batched rehydration) prove insufficient to address concurrent session reliability. Effort: significant -- requires transport layer refactoring and process lifecycle management. Impact: high if needed -- eliminates the root cause of multi-process contention.","created_at":"2026-04-13T16:26:12.5693951Z"}
