{"id":"7F0A6E89","priority":"low","kind":"task","text":"Follow-up (after 102.001-T lands): update the upstream spike/SKILL.md.tmpl in the external autoharness repo so regeneration preserves the in-repo docline findings-artifact fix (Principle IV, no out-of-tree writes). Refs: 102.001-T, 102-F, docs/exec-plans/2026-07-13-spike-findings-docline-reconciliation-plan.md.","created_at":"2026-07-13T20:05:35.8455958Z"}
{"id":"8CD8F46A","priority":"medium","kind":"task","text":"Enable (or formally waive) the formal multi-persona plan-review skill gate. This environment cannot dispatch independent reviewer persona sub-agents, so Stage plans (091-S through 093-S) ran an honest inline single-agent self-assessment instead of the formal .github/skills/plan-review/SKILL.md gate (AGENTS.md:378 orders plan-review -> harvest). Establish a real persona-dispatch path OR a documented operator waiver so the pre-harvest gate is satisfiable and not silently skipped.","created_at":"2026-07-14T16:11:55.0375147Z"}
{"id":"CA877CD1","priority":"low","kind":"task","text":"Add a labeled 'Constitution Check' section to the impl-plan skill output. Governance requires every implementation plan to map proposed work against the constitution; recent 091-S/092-S/093-S plans omitted an explicitly-labeled Constitution Check section.","created_at":"2026-07-14T16:11:59.1903437Z"}
{"id":"80DD65C4","priority":"medium","kind":"bug","text":"MoveInQueue (internal/core/queue.go ~293) persists DB-sourced artifacts from QueryQueue, which do not carry ArchivedFrom/ArchivedStatus (nor other unmodeled frontmatter). Reordering an archived item in a queue view whose status filter includes 'archived' rewrites the Markdown with empty provenance, dropping archived_from/archived_status (same class as 111-F, DB-sourced path). Fix: reload each artifact from Markdown before persisting position, or add provenance to the DB codec. Pre-existing, low reachability. Source: 111-F adversarial review (gpt-5.6-sol P2).","created_at":"2026-07-18T06:24:23.9808076Z"}
{"id":"7EEADCD3","priority":"low","kind":"bug","text":"CreateArtifact (internal/core/artifacts.go ~219) accepts status 'archived' as an initial status, but fresh items have no archive provenance and the create serializer emits none, producing an archived record UnarchiveItem cannot restore. Reject 'archived' as an initial creation status (require create-then-ArchiveItem), or emit provenance in the create path. Pre-existing edge case. Source: 111-F adversarial review (gpt-5.6-sol P2).","created_at":"2026-07-18T06:24:38.4738305Z"}
{"id":"12B5649E","priority":"low","kind":"task","text":"Consolidate the two frontmatter serializers to prevent divergence: WriteArtifactFile (artifacts.go ~682) and the inline map builder in createArtifact (artifacts.go ~289) each hand-build map[string]any. Extract models.Artifact.ToFrontmatterMap() and call from both so new fields (e.g. archive provenance) can never be emitted by one path and dropped by the other. Source: 111-F adversarial review (gemini-3.1-pro P2).","created_at":"2026-07-18T06:24:52.505765Z"}
