{"id":"7F0A6E89","priority":"low","kind":"task","text":"Follow-up (after 102.001-T lands): update the upstream spike/SKILL.md.tmpl in the external autoharness repo so regeneration preserves the in-repo docline findings-artifact fix (Principle IV, no out-of-tree writes). Refs: 102.001-T, 102-F, docs/exec-plans/2026-07-13-spike-findings-docline-reconciliation-plan.md. [BLOCKED 2026-07-22 by Stage: blocked-on-external-repo. Requires writes to the EXTERNAL autoharness repo (spike/SKILL.md.tmpl), outside this workspace and FORBIDDEN by Constitution Principle IV (CLI Workspace Containment; no out-of-tree writes). Cannot be actioned from this workspace. Kept active (NOT harvested/archived). Action when operating in the autoharness repo.]","created_at":"2026-07-13T20:05:35.8455958Z"}
{"id":"6FA0829B","priority":"low","kind":"task","text":"Follow-up to 8CD8F46A decision (docs/decisions/2026-07-22-plan-review-dispatch-capability-deliberation.md): propagate the capability-aware plan-review gate to the upstream plan-review/SKILL.md.tmpl in the external autoharness repo so regeneration preserves the in-repo dispatch-capability + declared-degradation + persona-adapter fix (Principle IV, no out-of-tree writes from this workspace). Mirrors spike template follow-up 7F0A6E89. [BLOCKED 2026-07-22 by Stage: blocked-on-external-repo. Requires writes to the EXTERNAL autoharness repo (plan-review/SKILL.md.tmpl), outside this workspace and FORBIDDEN by Constitution Principle IV (CLI Workspace Containment; no out-of-tree writes). Cannot be actioned from this workspace. Kept active (NOT harvested/archived). Action when operating in the autoharness repo.]","created_at":"2026-07-23T00:38:41.2865984Z"}
{"id":"918BCDAF","priority":"medium","kind":"task","text":"Register the repo-wide md-lint CI job (from shipment 106-S / feature 126-F) as a REQUIRED status check via GitHub branch protection. The gate is repo-wide and blocking from its first run (0 violations repo-wide via the MD025 _title config + the ~21 structural remediations), so NO advisory cycle is needed — this supersedes the earlier advisory-first framing. Branch-protection required-status-check configuration is done in the GitHub repo settings OUTSIDE the repository tree (not a code/CI file change), so it needs an operator/admin action and cannot be a normal in-repo Ship task. Refs: 126-F, 126.004-T, docs/exec-plans/2026-07-25-markdownlint-p008-provisioning-plan.md, deliberation 054-DL.","created_at":"2026-07-25T23:06:30.321091Z"}
{"id":"D04D63D0","priority":"medium","kind":"bug","text":"Ship bug: shipment_lifecycle.go attachCommitToItems fails when a shipment's archive candidates include an already-archived linked deliberation (e.g. 054-DL). collectArchiveCandidateIDs pulls in linkedDeliberationIDs; attachCommitToItems -> loadArtifact -> persistArtifact drops top-level archived_from/archived_status (generic Artifact codec doesn't model them), so the archived-write guard refuses ('refusing to write archived artifact without provenance') and ship_shipment aborts after stamping members but before archiving the manifest. Fix: skip/guard already-archived linked deliberations in attachCommitToItems, or preserve provenance on re-persist. Stamping a shipment merge commit onto a pre-existing linked deliberation is semantically wrong anyway. Recovery for partial ship: complete via single-item 'backlogit archive <shipment-id>' (proper ArchiveItem stamps provenance). Surfaced during 108-S closure (PR #305 ef9218d2 / PR #306 c5892c7e). Refs: internal/core/shipment_lifecycle.go collectArchiveCandidateIDs + attachCommitToItems.","created_at":"2026-07-28T04:17:24.2845136Z"}
{"id":"50471E28","priority":"medium","kind":"feature","text":"durable_writes second-layer hardening: complete ErrWriteIndeterminate caller reconciliation and retry-idempotency before durable mode is promoted toward default/GA. Surfaced by Copilot review cycle-4 on PR #308 (feature 123-F, shipment 109-S). All items are opt-in (durable_writes default=false) and triple-gated (durable ON + fsync failure + retry); MD is source of truth and the SQLite index self-heals on sync, so severity is P2. Acceptance criteria: (1) UnarchiveItem non-git branch (archive.go ~770): on ErrWriteIndeterminate from the restore write, complete-or-rollback the restore instead of returning early and leaving archive copy + DB row + duplicate files with no recovery. (2) persistArtifact Site-1 (shipment.go ~390): legacy callers AddDependency/RemoveDependency treat every error as file-untouched and roll back the DB edge; add explicit ErrWriteIndeterminate handling/reconciliation so durable-mode fsync failure does not diverge MD from the live index (or document reliance on sync-rebuild). (3) appendDurable (events/stream.go ~171): after a parent-dir fsync fails, a retry early-returns and never re-fsyncs the parent, so a later append can report success while the logs dir is not power-loss durable — make the durable append retry re-attempt the parent flush (ErrWriteNotApplied safe-retry contract). (4) mkdirAllDurable (durable_fs.go ~39): if mkdir succeeds but the parent fsync fails, the os.Stat early-return on retry skips the failed flush permanently — make durable mkdir retry re-fsync the parent of an already-existing dir when its dirent durability was not confirmed. (5) MCP writer durable mode (mcp/server.go ~76): append_comment can surface ErrWriteIndeterminate but the handler maps every append failure to a generic internal error; map both durability classes (not-applied vs indeterminate) to explicit machine-readable MCP outcomes so agents do not duplicate comments on retry.","created_at":"2026-07-28T15:45:17.5073375Z"}
{"id":"7A965F8A","priority":"medium","kind":"bug","text":"BUG: ArchiveItem re-persist drops modeled item_links (e.g. spike_ref) from artifact frontmatter. Observed: 123-F->120-F spike_ref link dropped during shipment 109-S archive; PR #309 restored it by hand. Root cause likely the archive load->persist path (SQLite GetItem fast-path projection omitting links, mirroring known archived_from/archived_status drop). Related to D04D63D0. Fix: preserve item_links on archive re-persist + regression test asserting frontmatter links survive ArchiveItem.","created_at":"2026-07-28T16:08:30.907358Z"}
