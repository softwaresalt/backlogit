---
title: "036-S Ship Session Complete — Post-Merge Closure Fully Landed"
description: "Final memory for 036-S: all PRs merged, closure artifacts on main, archived_from bug fixed."
ms.date: 2026-04-20
---

## Session Summary

Shipment **036-S** ("Workflow Hygiene: Source Artifact Archival Pattern") is fully
complete. All code, protocol changes, closure artifacts, and backlog data corrections
are on `main`.

## Shipment Timeline

| PR | SHA | Purpose | Status |
|---|---|---|---|
| #47 | `9a7af9f` | Ship agent + operational-closure skill changes | Merged |
| #48 | `8a67bcb` | Post-merge closure artifacts + `archived_from` path fix | Merged |

## Items Shipped

| ID | Title |
|---|---|
| `034-F` | Source Artifact Archival in Ship Protocol |
| `034.001-T` | Ship agent post-merge closure protocol update |
| `034.002-T` | Operational-closure skill Source Artifact Cleanup section |
| `034.003-T` | Stash entry archival via backlogit_stash_remove |
| `034.004-T` | Deliberation artifact archival via backlogit_archive_item |

## Key Decisions and Fixes

### `archived_from` self-referential bug (PR #48, commit `b1179d9`)

Copilot caught a real defect: `backlogit_ship_shipment` calls `ArchiveItem` after
the registry has already moved items into `.backlogit/archive/`. At stamp time,
`currentPath` == archive path, so `archived_from` was set to the archive path
instead of the original queue path. Direct workspace repair (permitted exception)
corrected all 5 affected frontmatter entries. Root cause in `archive.go` line 105
remains a future stash item.

### Never squash merge

`gh pr merge --merge` is the only allowed strategy. Repo branch protection blocks
squash. Stored in agent memory as `git-merge-policy`.

### GraphQL thread resolution

Copilot review threads must be resolved via `resolveReviewThread` GraphQL mutation
using `PRRT_*` thread IDs (not `PRRC_*` comment node IDs). Replies go via REST.

## Files Modified

- `.github/agents/ship.agent.md` — post-merge source artifact archival step added
- `.github/skills/operational-closure/SKILL.md` — Source Artifact Cleanup section added
- `.backlogit/archive/034-F.md` — `archived_from` corrected
- `.backlogit/archive/034.001-T.md` through `034.004-T.md` — `archived_from` corrected
- `docs/closure/2026-04-20-036-s-source-artifact-archival-closure.md` — closure artifact
- `docs/memory/20260420-173500-ship-036-s-post-merge-closure.md` — prior memory

## State

- Shipment `036-S`: **archived** (shipped via `backlogit_ship_shipment` at `9a7af9f`)
- Branch `chore/036-s-post-merge-closure`: deleted (local + remote)
- `main` HEAD: `8a67bcb`
- No open PRs for this shipment

## Next Steps

- Future stash: fix `archived_from` self-referential path bug in
  `backlogit_ship_shipment` / `archiveItems` in `shipment_lifecycle.go` — record
  the pre-archive path before status transition
- `033.013-T` (tech debt: `newRenderer` accept `format.Format`) remains queued
