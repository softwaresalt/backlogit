---
title: "Compacted Memory — 2026-08-14/15 (139-F..142-F Staging and Ship)"
doc_type: memory
schema_version: "1.0"
ingested_at: "2026-08-20T04:55:00Z"
---

Source files:

* `2026-08-14/stage-dark-factory-staging-groups-1-3-memory.md` (archived to
  `docs/archive/memory/2026-08-14/`)
* `2026-08-15/ship-139-142-final-memory.md`,
  `2026-08-15/ship-140-closure-memory.md` (archived to
  `docs/archive/memory/2026-08-15/`)

## Stage Staging (2026-08-14) — Groups 1-3

Stage triaged 5 stash entries into 3 deliberations and harvested 5 new
features (`139-F`…`142-F`, plus blocked `138-F`):

* **Group 1** (`FDA30F35` → `056-DL` → `139-F`): MCP startup-timeout treated
  as a genuine reliability/architecture bug (91s vs 20s evidence, repeated-
  rescan root cause), not an environment-only issue.
* **Group 2** (`3A649F8E` → `057-DL` → `140-F`): `ShipShipment`
  rollback/CAS hardening kept as its own dedicated feature — the
  compensating-rollback and CAS redesign cross shared persistence
  boundaries and exceeded the narrow scope of the originating `126-S`
  follow-up task. This is the harvest of the `3A649F8E` stash noted as
  still-open in the 2026-08-11/12 compacted memory — **now resolved**.
* **Group 3** (`EA3BC800` + `4CF89803` → `058-DL` → `141-F`, `142-F`): split
  into two independent low-priority features for lower blast radius (CLI
  parity test hardening + registry `governed: true` expansion).
* `138-F` (external autoharness template propagation) kept `blocked` —
  workspace containment (Principle IV) forbids writing outside this repo;
  mirrored as blocked backlog tasks while source stash stays active.

## Ship Execution (2026-08-15) — 139-F through 142-F (ALL COMPLETE, shipped)

* **139-F** (MCP startup-timeout fix) — merged, archived.
* **140-F** (ShipShipment rollback/CAS hardening) — PR #362 merged at
  `fa54a35ba2c7f2147e8cb0694b9b12be7238070c`. Key changes: compensating
  rollback leaves the raw event log untouched when event capture/diffing
  fails; task/event-log stale-sidecar reclamation replaced with stable OS
  advisory handles; adoption destination ID allocation serialized with the
  parent artifact lock; old/new event-log locks held in stable order through
  adoption migration; scanner-limit rollback + advisory-lock regression
  tests added. This is the direct resolution of the deferred rollback gap
  flagged in the 126-S / `3A649F8E` compacted memory.
* **141-F** (CLI parity test hardening) — PR #363 merged at
  `22827b1eedeed7e6cbc31ff870f33950dbefb1ee`.
* **142-F** (registry governed-operation expansion) — PR #364 merged at
  `17530fe30f68034bff502362e489eff82fb86fe7`. Governed registry parity
  learning captured. Race-enabled full test suite passed.
* **138-F remains blocked** — external-repository writes required, outside
  this workspace's containment boundary (Principle IV). Stash entries
  `7F0A6E89`/`6FA0829B` remain active, administratively closed/archived in
  *this* workspace only; upstream autoharness implementation not performed
  here. No follow-up item created — documented scope boundary, not an
  oversight.
* Closure artifacts landed via PR #365. All merges verified merge-commit
  only (P-009).

## Status

139-F, 140-F, 141-F, 142-F fully shipped/archived. 138-F remains
intentionally blocked (external scope boundary) — do not attempt to
"complete" it in a future session without a workspace/containment policy
change; it is blocked by design, not by omission.
