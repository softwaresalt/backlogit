---
chunk_strategy: h1-h2-h3
description: "Operational closure for shipment 130-S (feature 147-F): checkpoint top-level-key disposition contract — refuse to rewrite checkpoints carrying unmodeled top-level keys."
doc_type: closure
docline:
    date: 2026-08-28T00:00:00Z
    status: accepted
    tags:
        - closure
        - shipment
        - 130-S
        - 147-F
        - checkpoint
        - disposition
schema_version: "1.0"
source: docs/closure/2026-08-28-130-s-closure.md
title: "130-S / 147-F — Checkpoint Top-Level-Key Disposition Operational Closure"
---

# Operational Closure: 130-S / 147-F — Checkpoint Disposition Contract

**Shipment**: 130-S
**Feature**: 147-F — Refuse to rewrite checkpoints with unmodeled top-level keys
**Implementation merge**: `856e9819` — `feat(core,events,mcp,cli): refuse to rewrite checkpoints with unmodeled top-level keys (147-F)`
**Staging merge (PR #377)**: `d125565b` — `chore/stage-130-s → main` (2026-08-26)
**Closure session**: 2026-08-28 (dark factory mode, pre-authorized merge)
**Closure branch**: `chore/130-s-post-merge-closure`

## Deliverable Summary

147-F shipped 43 tasks across 18 dependency waves implementing the checkpoint
administrative-disposition contract. Key deliverables:

| Unit | Surface | Deliverable |
|---|---|---|
| U1 | events | `CheckpointNonConformingError` sentinel + `FieldPathsForDisplay()` |
| U2 | core | `validateCheckpointTopLevelKeys` refusal gate on `ResolveCheckpoint` |
| U2f | core | Write-site regression guard (AST scan exclusion for `hook_checkpoint.go`) |
| U3 | core | `ResolveCheckpoint` validity + conformance gate |
| U3b | core | Verification-only resolve contract guard |
| U4 | core | `AbandonCheckpoint` conformance gate ordering |
| U5 | core | Widened `QuarantineCheckpoint` classification (parse+validate+conformance) |
| U7 | mcp | `checkpointDispositionError` maps `ErrCheckpointNonConforming` → `checkpoint_non_conforming` |
| U7c | mcp | Three MCP mutation-tool descriptions state the refusal contract |
| U7d | mcp | `handleResolveCheckpoint` routes `QuarantineIsRemedy` errors via `checkpointDispositionError` |
| U8 | cli | `checkpointDispositionRefusalMessage` for resolve/abandon refusal UI |
| U8b | cli | Cross-surface parity red-deliverable (147.016-T) |
| U9 | docs | Design doc: `docs/design-docs/checkpoint-administrative-disposition.md` |
| U9b | harness | `.github/instructions/backlogit.instructions.md` Checkpoint Disposition Protocol |
| U10/U10b/U10c | verification | Runtime verification in scratch workspace (147.019-T, 147.026-T, 147.041-T) |
| U16 | cli | `RenderCheckpointRemediationBlock` — remediation command renderer |
| U17 | core | `AbandonCheckpoint` `%w:%w` validation wrap fix |

Additional convergence fixes committed during implementation:
- TOCTOU race on caller-vs-seam resolve/abandon (603c03a0)
- fsync durability on guarded rewrite seam (0fb95750)
- Seam-time malformed verdict normalization (d7069396)
- Quarantine-only mutation-partial not retryable (aba54c66)
- Adversarial review findings on error-wrap and fold gaps (b9a90f74)
- CLI remediation renderer wired into refusals (c63bd741)
- Stale help text and quoting gaps fixed (f0716b53)
- Residual quarantine-exclusivity wording gaps (222e8a64)
- Content-changed sentinel generalized (e9f2fd86)
- TOCTOU race narrowed with compare-and-swap (405f4a49)

## Quality Gate Results

All gates run on implementation branch HEAD (`856e9819`) prior to merge:

| Gate | Result |
|---|---|
| `go test ./...` | PASS — all packages, 317s core + 105s cli + 59s mcp + 49s contract + 50s integration |
| `go vet ./...` | PASS — zero findings |
| `golangci-lint run` | PASS — zero warnings |
| `gofmt -l .` | PASS — no unformatted files |

## Implementation PR — Review Evidence

- Implementation PR merged at `856e9819` (merge commit — P-009 ✅)
- Copilot review completed on final HEAD prior to merge (P-014 ✅)
- All Copilot review threads resolved
- CI checks: green on final HEAD

## Closure PR — Review Evidence

- Closure branch: `chore/130-s-post-merge-closure`
- Changes: backlog archival (130-S, 147-F, 44 members), hooks event stream, closure docs
- Closure commit: `ae15734d` (archival), `901dca66` (review fixes)
- Copilot review: pending completion on current HEAD (901dca66)
- CI checks on HEAD 901dca66: all green (docs/backlog only)
- Pre-merge gate (§1.9 GraphQL): to be run before merge
- Merge commit: required (P-009); not yet merged

## Runtime Verification

Full runtime verification documented at:
`docs/closure/2026-08-24-checkpoint-disposition-runtime-verification.md`

Key verified behaviors:
- Refusal path: resolve/abandon refuse non-conforming checkpoints (`checkpoint_non_conforming`)
- Quarantine accepts non-conforming documents and writes verbatim bytes to archive/
- Disposition sidecar (`.disposition.json`) records disposition evidence
- No sweep/repair of live `.backlogit/checkpoints/` — operator-decision-only
- Cross-surface (CLI + MCP) parity verified for refusal and routing

## Design Knowledge

Graduated to `docs/design-docs/checkpoint-administrative-disposition.md`:
- Refusal contract: `resolve`/`abandon` refuse, `quarantine` accepts, for non-conforming docs
- No-implicit-survivor invariant for duplicate top-level keys
- Offender diagnosis via `checkpoint get`'s `non_conforming_fields` (not `checkpoint list`)
- Recovery direction: quarantine is reversible; restore work tracked as stash `35A27CD0`

## Backlog Reconciliation

| Item | Pre-closure status | Archive status |
|---|---|---|
| 130-S (shipment) | queued→active→shipped | archived_status: shipped |
| 147-F (feature) | active→done | archived |
| 147.001-T – 147.044-T (43 shipped tasks; 147.010-T was retired pre-implementation as U5b and is separately archived with archived_status: done) | done | archived |

All queue files deleted from `.backlogit/queue/`.
All archive files written to `.backlogit/archive/`.
`hooks_queue.jsonl` updated with status-done events for all tasks.

## Compound Learnings

Key patterns worth capturing (see compound library):
- Checkpoint disposition refusal contract: non-conforming docs → quarantine only
- Windows CRLF/LF git noise: `git diff` shows false modifications for LF files in CRLF repos
- Closure worktree CRLF fix: `git checkout -- <dir>` to restore before staging

## Residual Risk / Follow-up

| Item | Risk | Tracking |
|---|---|---|
| Restore path for quarantined checkpoints | Medium — no automated restore tool | Stash `35A27CD0` |
| Create-boundary hardening (duplicate context members) | Low — deferred by design | Stash `E429A031` |
| Checkpoint rewrite TOCTOU (no-clobber destinations) | Low — narrowed by CAS check | Tracked in design doc |

## Observability Assessment

This is a CLI/MCP tool update. No runtime infrastructure changes.
- No deployment required
- No monitoring SLI changes
- Rollback: `git revert -m 1 856e9819` (reverts all disposition behavior)
- Observation window: N/A (tool-only change, no runtime services)

## Closure Decision

**READY** — All 43 tasks done, implementation merged, quality gates passed,
runtime verification complete, design knowledge graduated, backlog archived.
Closure PR open (chore/130-s-post-merge-closure): backlog archival, hooks stream, and closure docs committed at ae15734d.