---
title: "Ship 035-S PR Ready — Awaiting Merge Approval"
description: "Session memory for shipment 035-S CLI UX Polish follow-ups, PR #45 open and CI green"
ms.date: 2026-04-20
---

## Session Summary

Shipment 035-S ("CLI UX Polish Follow-ups") has been fully executed. PR #45 is open,
all CI checks pass, and the shipment is awaiting user merge approval.

## Shipment State

| Field | Value |
|---|---|
| Shipment | 035-S |
| Branch | `shipment/035-s-cli-ux-polish` |
| Status | active (awaiting merge) |
| PR | [#45](https://github.com/softwaresalt/backlogit/pull/45) |
| CI | ✅ all 3 checks green |

## Completed Items

| Item | Title | Status |
|---|---|---|
| 033.011-T | Move workflow permissions to job level | done |
| 033.012-T | Wire TTY detection into newRenderer call sites | done |

## Branch Commits

| SHA | Message |
|---|---|
| `60ef930` | docs(plans): add 035-S CLI UX polish plan and stage memory |
| `76d8f13` | fix(cli,workflows): 035-S CLI UX Polish follow-ups |
| `1bb2060` | fix(cli): address review findings F-001 and F-002 |

## Review Gate

Artifact: `033.002-R` — Gate **PASS**

| Finding | Severity | Resolution |
|---|---|---|
| F-001: go directive bumped to 1.25 | P1 | Fixed — pinned x/term@v0.38.0, x/sys@v0.39.0, directive at 1.24.0 |
| F-002: tty_test.go missing os.File coverage | P2 | Fixed — added TestIsTerminal_PipeReturnsFalse |
| F-003: newRenderer raw string param | P3 | Deferred — tracked as 033.013-T (queued, low) |

## Follow-up Items

- `033.013-T`: `newRenderer`: accept `format.Format` instead of raw string (queued, low priority)

## Key Technical Decisions

- **x/term pinning**: `go get golang.org/x/term` without a pin fetched v0.42.0 → pulled
  x/sys@v0.43.0 → propagated `go 1.25` directive. Fix: pin both `x/term@v0.38.0` and
  `x/sys@v0.39.0` explicitly, then manually reset go directive (go mod tidy never lowers).
- **`TestWorkflowGoVersionMatchesMod`**: enforces CI matrices include go.mod major.minor;
  caught the 1.25 regression automatically.
- **`isTerminal` is unexported**: tty_test.go uses `package cli` (white-box), not `package cli_test`.
- **Review artifact type**: `review` items require a non-shipment parent; used `033-F` (archived)
  as parent → yielded `033.002-R`.

## Next Steps (post-merge)

1. **User approves merge** → merge PR #45
2. **Record merge SHA** → `backlogit shipment ship 035-S --sha <sha>`
3. **Invoke operational-closure** skill in `mode=post-merge`
4. **Check docs** for updates needed: ARCHITECTURE.md, README.md
5. **Archive items** as needed via backlogit
6. **compound** skill for x/term pinning gotcha (non-trivial learning)

## Pending

- Merge approval from user (never auto-merge)
