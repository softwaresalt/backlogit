---
title: "021-F fix-ci round 2 complete: PR #15 ready for merge"
description: "Session memory after second-pass Copilot review remediation on feat/021-token-telemetry"
ms.date: 2026-04-09
---

## Session State

**Shipment:** 009-S
**Feature:** 021-F (token telemetry harvest)
**Branch:** `feat/021-token-telemetry`
**PR:** [#15](https://github.com/softwaresalt/backlogit/pull/15)
**HEAD commit:** `de87ac2`
**Status:** CI green, all review comments addressed — **awaiting user merge approval**

## Completed This Session

### fix-ci round 2 — 5 Copilot second-pass review comments

Commit `de87ac2` addressed all 5 second-pass review comments posted against
`88f10aa`:

| File | Issue | Resolution |
|---|---|---|
| `correlator.go` | `loadCompletedTasks` read `.backlogit/events.jsonl` which does not exist | Rewrote to scan `.backlogit/logs/*.jsonl` for `status_changed` events with `delta.to == "done"`; added `doneTasksFromLog` helper with per-ID dedup |
| `telemetry_schema.go` | `RehydrateTelemetry` scanner had default 64KB limit | Added `scanner.Buffer(1MB, 1MB)` matching the fix already in parser.go |
| `harvest.go` | `writeTelemetryJSONL` used `os.Create` — crash mid-write = corrupt file | Replaced with temp-file-then-rename atomic pattern (`jsonlPath + ".tmp"` → rename) |
| `harvest.go` | O(S×E) nested loop over all events per session to compute `serverCalls` | Derived `serverCallsPerSession` from already-built `toolStats` in O(T) |
| `records.go` + `telemetry_schema.go` | `TokensByServer` stored call counts not token sums — naming mismatch | Renamed to `ToolCallsByServer` / JSON tag `tool_calls_by_server` throughout |

### CI results

- `go test ./...` — all 16 packages pass ✅
- `golangci-lint run` — clean ✅
- `gofmt -l .` — clean ✅
- Remote CI: `test (1.23)` ✅ `test (1.24)` ✅

### Comment thread status

All 16 Copilot comment threads replied to:
- 11 first-pass threads: all resolved ✅
- 5 second-pass threads: replied with fix references, not yet GitHub-resolved (resolves after Copilot re-review)

## Architecture Note — Events Storage

Confirmed: backlogit does NOT write a workspace-level `.backlogit/events.jsonl`.
All events are per-item under `.backlogit/logs/<item_id>.jsonl` via
`internal/events/stream.go` → `LogPathForItem`. The telemetry correlator now
correctly scans the per-item log directory.

## Prior Session Commits (cumulative)

| SHA | Message |
|---|---|
| `ea0185c` | fix(telemetry): address review findings from 021-F |
| `1d88a91` | fix(ci): commit missing source files (records.go, test files, errors.go, root.go) |
| `88f10aa` | fix(ci): address Copilot review comments on PR #15 |
| `de87ac2` | fix(ci): address second-pass Copilot review comments on PR #15 |

## Pending Actions

- **Awaiting user merge approval for PR #15** — no auto-merge will occur
- After merge: run `backlogit shipment ship 009-S --sha <merge-sha>`
- After merge: invoke `operational-closure` skill
- After merge: update `docs/ARCHITECTURE.md` with telemetry pipeline section
- After merge: check `README.md` for user-facing telemetry capability mention
